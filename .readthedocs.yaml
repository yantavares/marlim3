# .readthedocs.yaml
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

  jobs:
    pre_install:
      - pip install mkdocs-material pymdown-extensions mkdocs-jupyter

mkdocs:
  configuration: mkdocs.yml
