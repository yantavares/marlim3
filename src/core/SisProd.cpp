/*
 * SisProd.cpp
 *
 *  Created on: 21 de dez de 2016
 *      Author: Eduardo
 */
#define _USE_MATH_DEFINES // para M_PI
#include "SisProd.h"
#include "FA_Hidratos.h"
#include "FA_Hidratos_Servico.h"
#include "OutputI18n.h"
#include <chrono>
#include <math.h>

SProd::SProd(string nomeArquivoEntrada, string nomeArquivoLog, tipoValidacaoJson_t validacaoJson,
             tipoSimulacao_t tipoSimulacao, varGlob1D *Vvg1dSP, int TD, int vbloq, int temporario, int reverso, double *compfonte,
             int *posicfonte, int nfontes, int redeperm) : arq(nomeArquivoEntrada, nomeArquivoLog, validacaoJson, tipoSimulacao, reverso, Vvg1dSP, redeperm), flut(arq.ncelp, arq.nvarprofp + 2 + 1 + 1 + 1 + 1),
                                                           flutG(arq.ncelg, arq.nvarprofg + 2 + 1 + 1 + 1 + 1 + 1), matglobP(2 * arq.ncelp, 3, 2), termolivreP(
                                                                                                                                                       2 * arq.ncelp),
                                                           matglobG(3 * arq.ncelg, 5, 5), termolivreG(3 * arq.ncelg) {

    zdranP = 0;
    dzdpP = 0;
    dzdtP = 0;
    cpg = 0;
    cpl = 0;
    drholdT = 0;
    npontos = 0;
    nfluP = 0;
    ModelCp = 0;
    Modeljtl = 0;
    CalcLat = 0;
    trackRGO = 0;
    trackDeng = 0;
    ninjgas = 0;
    lingas = 0;
    chokeVGL = 0;
    posicVGLP = 0;
    posicVGLG = 0;
    receb = 0;
    fechaM1 = 0;
    abreM1 = 0;
    celulaG = 0;
    celula = 0;
    celInter = 1e7;
    dtInter = 0.;
    velInter = 0.;
    celInterIni = celInter;
    dtInterIni = dtInter;
    velInterIni = velInter;

    ncelperftransg = 0;
    TrendLengthG = 0;
    MatTrendG = 0;
    resettrendg = 0;
    ntrendg = 0;
    ntrendgB = 0;
    TrendLengthTransG = 0;
    MatTrendTransG = 0;
    resettrendtransg = 0;
    ntrendtransg = 0;
    ntrendtransgB = 0;
    ncelperftransp = 0;
    TrendLengthP = 0;
    MatTrendP = 0;
    resettrend = 0;
    ntrend = 0;
    ntrendB = 0;
    TrendLengthTransP = 0;
    MatTrendTransP = 0;
    resettrendtrans = 0;
    ntrendtrans = 0;
    ntrendtransB = 0;
    LerPB = 0;
    PBPVTSim = 0;
    TBPVTSim = 0;
    RSLivia = 0;
    lerRS = 0;
    noextremo = 1;
    noinicial = 1;
    derivaAnel = -1;

    fontemassPRBuf = 0.;
    fontemassCRBuf = 0.;
    fontemassGRBuf = 0.;

    presE = -1;
    tempE = -1;
    titE = -1;
    betaE = -1;
    alfE = -1;
    presEini = -1;
    tempEini = -1;
    titEini = -1;
    betaEini = -1;
    alfEini = -1;

    tempMedContDesc = 10.;
    maxVecContDesc = 1000;
    vazmedDesc = 0;
    tempmedDEsc = 0;

    tGSup = 0.;
    tGSupIni = 0.;
    redeTemporario = temporario;

    betaRev = 0;
    betaRevini = 0;
    titRev = 1.;
    titRevini = 1.;

    dtCFLMed = 1.;
    dtSimMed = 1.;
    restriDt = 0;
    kontarestriDt = 0;
    dtCFLTotal = 0.;
    dtSimTotal = 0.;
    dtauxCFL = 0;
    dtauxFinal = 0.;

    kimpT = 0.;

    kontaGolfada = 1000;

    mudaModoChk = 0;
    mudaModoChkini = 0;

    momentoDesesp = 0;

    modeloCompleto = 1;
    modeloCompleto0 = 1;
    kontaMudaModelo = 0;
    kontarestriSegrega = 0;

    DpMaxMed = 1.;
    DTMaxMed = 1.;

    chuteHol = -1.;

    buscaIni = 0;

    bloq = vbloq;

    vg1dSP = Vvg1dSP;

    if (TD >= 0)
        arq.tabelaDinamica = TD;

    for (int i = 0; i < 10; i++) {
        vRazMast0[i] = 0.;
        vRazMast1[i] = 0.;
        vRazMastCrit[i] = 0.5;
    }

    kontaRenovaComp = 0;

    fluiRevRede = ProFlu();
    tempRev = 0.;
    revPerm = 0;
    ntabDin = 0;

    nCelulaPoisson2D = 0;
    trocaTermicaLenta = 0.01;

    semTermo = 0;

    monitConvPerm = 1000.;
    monitConvPermBase = 1.;

    alteraTempo = 0;

    redeParalelaCCsecundario = -1;
    redeParalelaP = -1;
    redeParalelaS = -1;
    montasistema(compfonte, posicfonte, nfontes);
}
SProd::SProd() : arq(), flut(1, 1 + 2 + 1 + 1 + 1), flutG(1, 1 + 2 + 1 + 1 + 1 + 1), matglobP(2 * 1, 3, 2), termolivreP(2 * 1), matglobG(
                                                                                                                                    3 * 1, 5, 5),
                 termolivreG(3 * 1) {
    tfinal = 0;
    dtini = 0;
    contaLog = 0;

    menorDx = 0.;
    iterperm = 0.;
    kSP = 0.;
    KontaImprime = 0.;
    indevento = 0.;
    modoPerm = 0.;
    ktMedMov = 0.;
    pTotal = 0.;
    jTotal = 0.;

    alfTotal = 0.;
    kontaTempoProf = 0;
    //kontaTempoCelUni = 0;
    dt = 0.;
    nabreM1 = 0;
    nfechaM1 = 0;
    zdranP = 0;
    dzdpP = 0;
    dzdtP = 0;
    cpg = 0;
    cpl = 0;
    drholdT = 0;
    HLat = 0;
    npontos = 0;
    nfluP = 0;
    chokeVGL = 0;
    posicVGLP = 0;
    posicVGLG = 0;
    fechaM1 = 0;
    abreM1 = 0;
    celulaG = 0;
    celula = 0;
    celInter = 1e7;
    dtInter = 0.;
    velInter = 0.;

    celInterIni = 0.;
    dtInterIni = 0.;
    velInterIni = 0.;

    ModelCp = 0;
    Modeljtl = 0;
    CalcLat = 0;
    trackRGO = 0;
    trackDeng = 0;
    ninjgas = 0;
    lingas = 0;
    injPoc = 0;

    indTramo = -1;
    ncel = 0;
    reinicia = 0;
    presfim = 0;
    presfimini = 0;

    pGSup = 0;
    pGSupIni = 0.;
    tGSup = 0;
    tGSupIni = 0;
    temperatura = 0;

    masSup = 0;
    tempSup = 0;

    ncelGas = 0;
    presiniG = 0;
    tempiniG = 0;
    massfonte = 0;

    mult = 0;
    presMedMov = 0;
    jMedMov = 0;
    alfMedMov = 0;
    tMedMov = 0;

    aberto = 0;
    abertoini = 0;
    tempoaberto = 0;
    tempoabertoini = 0;
    EstadoMaster1 = 0;
    contaMaster1 = 0;
    masChkSup = 0;
    masChkSupini = 0;
    TransMassModel = 0;
    indpigP = 0;
    indpigPini = indpigP;
    npig = 0;
    receb = 0;

    ncelperftransg = 0;
    TrendLengthG = 0;
    MatTrendG = 0;
    resettrendg = 0;
    ntrendg = 0;
    ntrendgB = 0;
    TrendLengthTransG = 0;
    MatTrendTransG = 0;
    resettrendtransg = 0;
    ntrendtransg = 0;
    ntrendtransgB = 0;
    ncelperftransp = 0;
    TrendLengthP = 0;
    MatTrendP = 0;
    resettrend = 0;
    ntrend = 0;
    ntrendB = 0;
    TrendLengthTransP = 0;
    MatTrendTransP = 0;
    resettrendtrans = 0;
    ntrendtrans = 0;
    ntrendtransB = 0;

    AnulaColunaIni = 0;
    AnulaColunaFim = 0;
    ColunaAnulaIni = 0;
    ColunaAnulaFim = 0;
    verificaAcop = 0;
    verificaAcopRedeP = 0;
    verificaAcopRedeS = 0;
    SecPrimIniRedeP = 0;
    SecPrimFimRedeP = 0;
    PrimSecIniRedeP = 0;
    PrimSecFimRedeP = 0;
    kontaTempoProf = 0;
    //kontaTempoCelUni = 0;
    kontaTempoProfG = 0;
    kontaTempoTransProf = 0;
    kontaTempoTransProfG = 0;

    LerPB = 0;
    PBPVTSim = 0;
    TBPVTSim = 0;
    RSLivia = 0;
    lerRS = 0;

    noextremo = 1;
    noinicial = 1;
    derivaAnel = -1;

    fontemassPRBuf = 0.;
    fontemassCRBuf = 0.;
    fontemassGRBuf = 0.;

    presE = -1;
    tempE = -1;
    titE = -1;
    betaE = -1;
    alfE = -1;
    presEini = -1;
    tempEini = -1;
    titEini = -1;
    betaEini = -1;
    alfEini = -1;

    titRev = 1.;
    titRevini = 1.;

    tempMedContDesc = 10.;
    maxVecContDesc = 1000;
    vazmedDesc = 0;
    tempmedDEsc = 0;
    betaRev = 0.;
    betaRevini = 1.;
    redeTemporario = 0;

    dtCFLMed = 1.;
    dtSimMed = 1.;
    restriDt = 0;
    kontarestriDt = 0;
    dtCFLTotal = 0.;
    dtSimTotal = 0.;
    dtauxCFL = 0.;
    dtauxFinal = 0.;

    kimpT = 0.;

    kontaGolfada = 1000.;

    mudaModoChk = 0;
    mudaModoChkini = 0;

    momentoDesesp = 0;

    modeloCompleto = 1;
    modeloCompleto0 = 1;
    kontaMudaModelo = 0;
    kontarestriSegrega = 0;

    DpMaxMed = 1.;
    DTMaxMed = 1.;

    chuteHol = -1.;

    buscaIni = 0;
    ntabDin = 0;

    for (int i = 0; i < 10; i++) {
        vRazMast0[i] = 0.;
        vRazMast1[i] = 0.;
        vRazMastCrit[i] = 0.5;
    }

    kontaRenovaComp = 0;

    bloq = 0;

    fluiRevRede = ProFlu();
    tempRev = 0.;
    revPerm = 0;

    vg1dSP = 0;

    nCelulaPoisson2D = 0;

    trocaTermicaLenta = 0.01;

    semTermo = 0;
    dtCicMin = dt;

    monitConvPerm = 1000.;
    monitConvPermBase = 1.;

    alteraTempo = 0;

    redeParalelaCCsecundario = -1;
    redeParalelaP = -1;
    redeParalelaS = -1;
}

SProd &SProd::operator=(const SProd &sp) {
    if (arq.lingas > 0)
        delete[] celulaG;
    if (chokeVGL!=0 && arq.lingas > 0)
        delete[] chokeVGL;
    if (posicVGLP!=0 && arq.lingas > 0)
        delete[] posicVGLP;
    if (posicVGLG!=0 && arq.lingas > 0)
        delete[] posicVGLG;
    if (nabreM1 > 0)
        delete[] abreM1;
    if (nfechaM1 > 0)
        delete[] fechaM1;

    if (arq.nperfistransp > 0)
        delete[] ncelperftransp;
    if (arq.nperfistransg > 0 && arq.lingas > 0)
        delete[] ncelperftransg;

    if (arq.ntendp > 0 && redeTemporario == 0) {
        for (int i = 0; i < arq.ntendp && MatTrendP && TrendLengthP; i++) {
            if (MatTrendP[i]) {
                for (int j = 0; j < TrendLengthP[i]; j++)
                    delete[] MatTrendP[i][j];
                delete[] MatTrendP[i];
            }
        }
        if (MatTrendP!=0)
            delete[] MatTrendP;
        if (TrendLengthP!=0)
            delete[] TrendLengthP;
        if (resettrend!=0)
            delete[] resettrend;
        if (ntrend!=0)
            delete[] ntrend;
        if (ntrendB!=0)
            delete[] ntrendB;
    }


    if (arq.ntendg > 0 && arq.lingas > 0 && redeTemporario == 0) {
        for (int i = 0; i < arq.ntendg && MatTrendG && TrendLengthG; i++) {
            if (MatTrendG[i]) {
                for (int j = 0; j < TrendLengthG[i]; j++)
                    delete[] MatTrendG[i][j];
                delete[] MatTrendG[i];
            }
        }
        if (MatTrendG!=0)
            delete[] MatTrendG;
        if (TrendLengthG!=0)
            delete[] TrendLengthG;
        if (resettrendg!=0)
            delete[] resettrendg;
        if (ntrendg!=0)
            delete[] ntrendg;
        if (ntrendgB!=0)
            delete[] ntrendgB;
    }

    if (arq.ntendtransp > 0 && redeTemporario == 0) {
        for (int i = 0; i < arq.ntendtransp && MatTrendTransP && TrendLengthTransP; i++) {
            if (MatTrendTransP[i]) {
                for (int j = 0; j < TrendLengthTransP[i]; j++)
                    delete[] MatTrendTransP[i][j];
                delete[] MatTrendTransP[i];
            }
        }
        if (MatTrendTransP!=0)
            delete[] MatTrendTransP;
        if (TrendLengthTransP!=0)
            delete[] TrendLengthTransP;
        if (resettrendtrans!=0)
            delete[] resettrendtrans;
        if (ntrendtrans!=0)
            delete[] ntrendtrans;
        if (ntrendtransB!=0)
            delete[] ntrendtransB;
    }

    if (arq.ntendtransg > 0 && redeTemporario == 0) {
        for (int i = 0; i < arq.ntendtransg && MatTrendTransG && TrendLengthTransG; i++) {
            if (MatTrendTransG[i]) {
                for (int j = 0; j < TrendLengthTransG[i]; j++)
                    delete[] MatTrendTransG[i][j];
                delete[] MatTrendTransG[i];
            }
        }
        if (MatTrendTransG!=0)
            delete[] MatTrendTransG;
        if (TrendLengthTransG!=0)
            delete[] TrendLengthTransG;
        if (resettrendtransg!=0)
            delete[] resettrendtransg;
        if (ntrendtransg!=0)
            delete[] ntrendtransg;
        if (ntrendtransgB!=0)
            delete[] ntrendtransgB;
    }

    int ndiv = arq.tabent.npont - 1;
    if (CalcLat > 0 && arq.flashCompleto == 0) {
        for (int i = 0; i < ndiv + 2; i++)
            delete[] HLat[i];
        delete[] HLat;
    }
    if (LerPB > 0) {
        delete[] PBPVTSim;
        delete[] TBPVTSim;
        if (lerRS > 0) {
            for (int i = 0; i < ndiv + 2; i++)
                delete[] RSLivia[i];
            delete[] RSLivia;
        }
    }

    if (ncel > 0)
        delete[] celula;
    if (npig > 0)
        delete[] receb;

    if (arq.tabelaDinamica == 1) {
        tabDin.clear();
    }

    arq = sp.arq;
    flut = sp.flut;
    flutG = sp.flutG;
    matglobP = sp.matglobP;
    termolivreP = sp.termolivreP;
    matglobG = sp.matglobG;
    termolivreG = sp.termolivreG;
    vg1dSP = sp.vg1dSP;
    zdranP = 0;
    dzdpP = 0;
    dzdtP = 0;
    cpg = 0;
    cpl = 0;
    drholdT = 0;
    npontos = 0;
    nfluP = 0;
    ModelCp = 0;
    Modeljtl = 0;
    CalcLat = 0;
    LerPB = 0;
    PBPVTSim = 0;
    TBPVTSim = 0;
    trackRGO = 0;
    trackDeng = 0;
    ninjgas = 0;
    lingas = 0;
    chokeVGL = 0;
    posicVGLP = 0;
    posicVGLG = 0;
    receb = 0;
    fechaM1 = 0;
    abreM1 = 0;
    celulaG = 0;
    celula = 0;
    celInter = 1e7;
    dtInter = 0.;
    velInter = 0.;
    celInterIni = celInter;
    dtInterIni = dtInter;
    velInterIni = velInter;

    ncelperftransg = 0;
    TrendLengthG = 0;
    MatTrendG = 0;
    resettrendg = 0;
    ntrendg = 0;
    ntrendgB = 0;
    TrendLengthTransG = 0;
    MatTrendTransG = 0;
    resettrendtransg = 0;
    ntrendtransg = 0;
    ntrendtransgB = 0;
    ncelperftransp = 0;
    TrendLengthP = 0;
    MatTrendP = 0;
    resettrend = 0;
    ntrend = 0;
    ntrendB = 0;
    TrendLengthTransP = 0;
    MatTrendTransP = 0;
    resettrendtrans = 0;
    ntrendtrans = 0;
    ntrendtransB = 0;

    noextremo = sp.noextremo;
    noinicial = sp.noinicial;
    derivaAnel = sp.derivaAnel;

    fontemassPRBuf = 0.;
    fontemassCRBuf = 0.;
    fontemassGRBuf = 0.;

    presE = -1;
    tempE = -1;
    titE = -1;
    betaE = -1;
    alfE = -1;
    presEini = -1;
    tempEini = -1;
    titEini = -1;
    betaEini = -1;
    alfEini = -1;

    betaRev = sp.betaRev;
    betaRevini = sp.betaRevini;
    titRev = sp.titRev;
    titRevini = sp.titRevini;

    tempMedContDesc = 10.;
    maxVecContDesc = 1000;
    vazmedDesc = 0;
    tempmedDEsc = 0;

    tGSup = 0.;
    tGSupIni = 0.;

    dtCFLMed = 1.;
    dtSimMed = 1.;
    restriDt = 0;
    kontarestriDt = 0;
    dtauxCFL = 0.;
    dtauxFinal = 0.;

    kimpT = 0.;
    kontaGolfada = 1000.;

    mudaModoChk = 0;
    mudaModoChkini = 0;

    momentoDesesp = 0;

    modeloCompleto = 1;
    modeloCompleto0 = 1;
    kontaMudaModelo = 0;
    kontarestriSegrega = 0;

    DpMaxMed = 1.;
    DTMaxMed = 1.;

    chuteHol = -1.;

    buscaIni = 0;

    for (int i = 0; i < 10; i++) {
        vRazMast0[i] = 0.;
        vRazMast1[i] = 0.;
        vRazMastCrit[i] = 0.5;
    }

    kontaRenovaComp = 0;

    bloq = sp.bloq;

    fluiRevRede = ProFlu();
    tempRev = 0.;
    revPerm = 0;

    ntabDin = 0;

    nCelulaPoisson2D = 0;

    trocaTermicaLenta = 0.01;

    semTermo = 0;

    dtCicMin = sp.dtCicMin;

    monitConvPerm = 1000.;
    monitConvPermBase = 1.;

    alteraTempo = 0;
    redeParalelaCCsecundario = sp.redeParalelaCCsecundario;
    redeParalelaP = sp.redeParalelaP;
    redeParalelaS = sp.redeParalelaS;

    presVet.clear();
    jVet.clear();
    alfVet.clear();
    tVet.clear();
    dtSim.clear();
    dtCFL.clear();
    vazmaxMedDesc.clear();
    dtDesc.clear();
    taxaDpMax.clear();
    taxaDTMax.clear();
    tabDin.clear();
    acertaIndAcop.clear();
    indCelPoisson2D.clear();
    indFonteRedeParalelaIni.clear();
    fonteMpRedeParalelaIni.clear();
    fonteMcRedeParalelaIni.clear();
    fonteMgRedeParalelaIni.clear();

    montasistema();

    return *this;
}

void SProd::copiaSemJson(Ler &sp, int vnoextremo, int vnoinicial, int vderivaAnel, int vbloq, double vbetaRev,
                         double vbetaRevini, double vtitRev, double vtitRevini, double vdtCicMin) {
    if (arq.lingas > 0)
        delete[] celulaG;
    if (chokeVGL!=0 && arq.lingas > 0)
        delete[] chokeVGL;
    if (posicVGLP!=0 && arq.lingas > 0)
        delete[] posicVGLP;
    if (posicVGLG!=0 && arq.lingas > 0)
        delete[] posicVGLG;
    if (nabreM1 > 0)
        delete[] abreM1;
    if (nfechaM1 > 0)
        delete[] fechaM1;

    if (arq.nperfistransp > 0)
        delete[] ncelperftransp;
    if (arq.nperfistransg > 0 && arq.lingas > 0)
        delete[] ncelperftransg;

    if (arq.ntendp > 0 && redeTemporario == 0) {
         for (int i = 0; i < arq.ntendp && MatTrendP && TrendLengthP; i++) {
             if (MatTrendP[i]) {
                 for (int j = 0; j < TrendLengthP[i]; j++)
                     delete[] MatTrendP[i][j];
                 delete[] MatTrendP[i];
             }
         }
         if (MatTrendP!=0)
             delete[] MatTrendP;
         if (TrendLengthP!=0)
             delete[] TrendLengthP;
         if (resettrend!=0)
             delete[] resettrend;
         if (ntrend!=0)
             delete[] ntrend;
         if (ntrendB!=0)
             delete[] ntrendB;
     }

     if (arq.ntendg > 0 && arq.lingas > 0 && redeTemporario == 0) {
         for (int i = 0; i < arq.ntendg && MatTrendG && TrendLengthG; i++) {
             if (MatTrendG[i]) {
                 for (int j = 0; j < TrendLengthG[i]; j++)
                     delete[] MatTrendG[i][j];
                 delete[] MatTrendG[i];
             }
         }
         if (MatTrendG!=0)
             delete[] MatTrendG;
         if (TrendLengthG!=0)
             delete[] TrendLengthG;
         if (resettrendg!=0)
             delete[] resettrendg;
         if (ntrendg!=0)
             delete[] ntrendg;
         if (ntrendgB!=0)
             delete[] ntrendgB;
     }

     if (arq.ntendtransp > 0 && redeTemporario == 0) {
         for (int i = 0; i < arq.ntendtransp && MatTrendTransP && TrendLengthTransP; i++) {
             if (MatTrendTransP[i]) {
                 for (int j = 0; j < TrendLengthTransP[i]; j++)
                     delete[] MatTrendTransP[i][j];
                 delete[] MatTrendTransP[i];
             }
         }
         if (MatTrendTransP)
             delete[] MatTrendTransP;
         if (TrendLengthTransP)
             delete[] TrendLengthTransP;
         if (resettrendtrans)
             delete[] resettrendtrans;
         if (ntrendtrans!=0)
             delete[] ntrendtrans;
         if (ntrendtransB!=0)
             delete[] ntrendtransB;
     }

     if (arq.ntendtransg > 0 && redeTemporario == 0) {
         for (int i = 0; i < arq.ntendtransg && MatTrendTransG && TrendLengthTransG; i++) {
             if (MatTrendTransG[i]) {
                 for (int j = 0; j < TrendLengthTransG[i]; j++)
                     delete[] MatTrendTransG[i][j];
                 delete[] MatTrendTransG[i];
             }
         }
         if (MatTrendTransG)
             delete[] MatTrendTransG;
         if (TrendLengthTransG)
             delete[] TrendLengthTransG;
         if (resettrendtransg)
             delete[] resettrendtransg;
         if (ntrendtransg!=0)
             delete[] ntrendtransg;
         if (ntrendtransgB!=0)
             delete[] ntrendtransgB;
     }

    int ndiv = arq.tabent.npont - 1;
    if (CalcLat > 0 && arq.flashCompleto == 0) {
        for (int i = 0; i < ndiv + 2; i++)
            delete[] HLat[i];
        delete[] HLat;
    }
    if (LerPB > 0) {
        delete[] PBPVTSim;
        delete[] TBPVTSim;
        if (lerRS > 0) {
            for (int i = 0; i < ndiv + 2; i++)
                delete[] RSLivia[i];
            delete[] RSLivia;
        }
    }

    if (ncel > 0)
        delete[] celula;
    if (npig > 0)
        delete[] receb;

    if (arq.tabelaDinamica == 1) {
        tabDin.clear();
    }

    arq.copiaSemJson(sp);
    flut = FullMtx<double>(arq.ncelp, arq.nvarprofp + 2 + 1 + 1 + 1 + 1);
    flutG = FullMtx<double>(arq.ncelg, arq.nvarprofg + 2 + 1 + 1 + 1 + 1 + 1);
    matglobP = BandMtx<double>(2 * arq.ncelp, 3, 2);
    termolivreP = Vcr<double>(2 * arq.ncelp);
    matglobG = BandMtx<double>(3 * arq.ncelg, 5, 5);
    termolivreG = Vcr<double>(3 * arq.ncelg);
    vg1dSP = arq.vg1dSP;
    zdranP = 0;
    dzdpP = 0;
    dzdtP = 0;
    cpg = 0;
    cpl = 0;
    drholdT = 0;
    npontos = 0;
    nfluP = 0;
    ModelCp = 0;
    Modeljtl = 0;
    CalcLat = 0;
    LerPB = 0;
    PBPVTSim = 0;
    TBPVTSim = 0;
    trackRGO = 0;
    trackDeng = 0;
    ninjgas = 0;
    lingas = 0;
    chokeVGL = 0;
    posicVGLP = 0;
    posicVGLG = 0;
    receb = 0;
    fechaM1 = 0;
    abreM1 = 0;
    celulaG = 0;
    celula = 0;
    celInter = 1e7;
    dtInter = 0.;
    velInter = 0.;
    celInterIni = celInter;
    dtInterIni = dtInter;
    velInterIni = velInter;

    ncelperftransg = 0;
    TrendLengthG = 0;
    MatTrendG = 0;
    resettrendg = 0;
    ntrendg = 0;
    ntrendgB = 0;
    TrendLengthTransG = 0;
    MatTrendTransG = 0;
    resettrendtransg = 0;
    ntrendtransg = 0;
    ntrendtransgB = 0;
    ncelperftransp = 0;
    TrendLengthP = 0;
    MatTrendP = 0;
    resettrend = 0;
    ntrend = 0;
    ntrendB = 0;
    TrendLengthTransP = 0;
    MatTrendTransP = 0;
    resettrendtrans = 0;
    ntrendtrans = 0;
    ntrendtransB = 0;

    noextremo = vnoextremo;
    noinicial = vnoinicial;
    derivaAnel = vderivaAnel;

    fontemassPRBuf = 0.;
    fontemassCRBuf = 0.;
    fontemassGRBuf = 0.;

    presE = -1;
    tempE = -1;
    titE = -1;
    betaE = -1;
    alfE = -1;
    presEini = -1;
    tempEini = -1;
    titEini = -1;
    betaEini = -1;
    alfEini = -1;

    betaRev = vbetaRev;
    betaRevini = vbetaRevini;
    titRev = vtitRev;
    titRevini = vtitRevini;

    tempMedContDesc = 10.;
    maxVecContDesc = 1000;
    vazmedDesc = 0;
    tempmedDEsc = 0;

    tGSup = 0.;
    tGSupIni = 0.;

    dtCFLMed = 1.;
    dtSimMed = 1.;
    restriDt = 0;
    kontarestriDt = 0;
    dtauxCFL = 0.;
    dtauxFinal = 0.;

    kimpT = 0.;
    kontaGolfada = 1000.;

    mudaModoChk = 0;
    mudaModoChkini = 0;

    momentoDesesp = 0;

    modeloCompleto = 1;
    modeloCompleto0 = 1;
    kontaMudaModelo = 0;
    kontarestriSegrega = 0;

    DpMaxMed = 1.;
    DTMaxMed = 1.;

    chuteHol = -1.;

    buscaIni = 0;

    for (int i = 0; i < 10; i++) {
        vRazMast0[i] = 0.;
        vRazMast1[i] = 0.;
        vRazMastCrit[i] = 0.5;
    }

    kontaRenovaComp = 0;

    bloq = vbloq;

    fluiRevRede = ProFlu();
    tempRev = 0.;
    revPerm = 0;

    ntabDin = 0;

    nCelulaPoisson2D = 0;

    trocaTermicaLenta = 0.01;

    semTermo = 0;

    dtCicMin = vdtCicMin;

    monitConvPerm = 1000.;
    monitConvPermBase = 1.;

    alteraTempo = 0;

    redeParalelaCCsecundario = -1;
    redeParalelaP = -1;
    redeParalelaS = -1;

    montasistema();
}

void SProd::HidroDescargaG() {
    celulaG[0].massfonteCH = 0;
    double pmed;
    double tmed;

    if (arq.gasinj.tipoCC == 0 && arq.controDesc == 0)
        pmed = arq.gasinj.presinj[0];
    else {
        pmed = 10.;
        if (arq.controDesc == 1) {
            pmed = arq.presIniDescG;
            presiniG = arq.presIniDescG;
        }
    }

    celulaG[0].presL = pmed;
    celulaG[0].pres = pmed;
    celulaG[0].presini = pmed;
    celulaG[1].presL = pmed;
    tmed = celulaG[0].calor.Textern1;
    celulaG[0].tempL = tmed;
    celulaG[0].temp = tmed;
    celulaG[1].tempL = tmed;
    double rho0 = celulaG[0].flui.MasEspGas(pmed, tmed);
    double rho1;
    celulaG[0].u1L = celulaG[0].duto.area * rho0;
    celulaG[0].u1LL = celulaG[0].u1L;
    celulaG[1].u1LL = celulaG[0].u1L;
    celulaG[0].VGasL = 0;
    celulaG[0].VGasR = 0;
    celulaG[1].VGasL = 0;
    celulaG[0].massfonteCH = 0.;
    for (int i = 1; i <= ncelGas; i++) {
        double A0 = celulaG[i - 1].duto.area;
        double dx0 = 0.5 * celulaG[i].dxL;
        double A1 = celulaG[i].duto.area;
        double dx1 = 0.5 * celulaG[i].dx0;
        pmed -= rho0 * 9.81 * dx0 * sin(celulaG[i - 1].duto.teta) / 98066.52;
        tmed = celulaG[i].calor.Textern1;
        if (i < celInter)
            rho1 = celulaG[i].flui.MasEspGas(pmed, tmed);
        else
            rho1 = celulaG[i].MasEspFlu(pmed, tmed);
        pmed -= rho1 * 9.81 * dx1 * sin(celulaG[i].duto.teta) / 98066.52;
        rho0 = rho1;

        celulaG[i].pres = pmed;
        celulaG[i].presini = pmed;
        celulaG[i - 1].presR = pmed;
        celulaG[i].temp = tmed;
        celulaG[i - 1].tempR = tmed;
        celulaG[i].u1L = celulaG[i].duto.area * rho0;
        celulaG[i - 1].u1R = celulaG[i].u1L;
        celulaG[i].VGasR = 0;
        celulaG[i - 1].VGasRR = 0;
        celulaG[i].massfonteCH = 0.;
        celulaG[i - 1].u1R = celulaG[i].u1L;
        if (i < ncelGas) {
            celulaG[i + 1].presL = pmed;
            celulaG[i + 1].tempL = tmed;
            celulaG[i + 1].u1LL = celulaG[i].u1L;
            celulaG[i + 1].VGasL = 0;
        }
    }
}

void SProd::HidroDescargaP() {
    celula[0].massfonteCH = 0;
    celula[0].fontemassCL = 0;
    celula[0].fontemassLL = 0;
    celula[0].fontemassGL = 0;
    celula[0].fontemassCR = 0;
    celula[0].fontemassLR = 0;
    celula[0].fontemassGR = 0;
    if (0 <= arq.celdescargaP) {
        celula[0].alf = 0.;
        celula[0].bet = 1.;
        celula[0].betI = celula[0].bet;
        celula[0].alfini = celula[0].alf;
        celula[0].betini = celula[0].bet;
        celula[0].alfPigD = celula[0].alf;
        celula[0].betPigD = celula[0].bet;
        celula[0].alfPigE = celula[0].alf;
        celula[0].betPigE = celula[0].bet;
        celula[1].alfL = celula[0].alf;
        celula[1].betL = celula[0].bet;
        celula[1].betLI = celula[0].bet;
        celula[1].alfLini = celula[0].alf;
        celula[1].betLini = celula[0].bet;
    } else {
        celula[0].alf = 1.;
        celula[0].bet = 0.;
        celula[0].betI = celula[0].bet;
        celula[0].alfini = celula[0].alf;
        celula[0].betini = celula[0].bet;
        celula[0].alfPigD = celula[0].alf;
        celula[0].betPigD = celula[0].bet;
        celula[0].alfPigE = celula[0].alf;
        celula[0].betPigE = celula[0].bet;
        celula[1].alfL = celula[0].alf;
        celula[1].betL = celula[0].bet;
        celula[1].betLI = celula[0].bet;
        celula[1].alfLini = celula[0].alf;
        celula[1].betLini = celula[0].bet;
    }
    double pmed;
    double tmed;
    pmed = celula[0].acsr.ipr.Pres;
    celula[0].presL = pmed;
    celula[1].presL = pmed;
    celula[0].pres = pmed;
    celula[0].presini = pmed;
    celula[1].presLini = pmed;
    celula[0].presauxL = pmed;
    tmed = celula[0].calor.Textern1;
    celula[0].tempL = tmed;
    celula[0].temp = tmed;
    celula[0].tempini = tmed;
    celula[1].tempL = tmed;
    double rho0;
    if (0 <= arq.celdescargaP)
        rho0 = celula[0].fluicol.MasEspFlu(pmed, tmed);
    else
        rho0 = celula[0].flui.MasEspGas(pmed, tmed);
    double rho1;
    rho1 = celula[0].fluicol.MasEspFlu(pmed, tmed);

    celula[0].FW = 0;
    celula[0].FWini = 0;
    celula[0].arranjo = 0;
    celula[0].QLL = 0;
    celula[1].QLL = celula[0].QLL;
    celula[0].QL = 0;
    celula[0].QG = 0;
    celula[0].rpL = rho1;
    celula[0].rpC = rho1;
    celula[1].rpL = celula[0].rpC;
    celula[0].rcL = rho1;
    celula[0].rcC = rho1;
    celula[1].rcL = celula[0].rcC;

    celula[0].MC = 0.;
    celula[0].ML = 0.;
    celula[1].ML = celula[0].MC;
    celula[0].Mliqini = celula[0].MC;
    celula[1].MliqiniL = celula[0].MC;
    celula[0].MliqiniL = celula[0].MC;

    celula[0].rpLi = rho1;
    celula[0].rpCi = rho1;
    celula[0].rcLi = rho1;
    celula[0].rcCi = rho1;

    for (int i = 1; i <= ncel; i++) {
        double A0 = celula[i - 1].duto.area;
        double dx0 = 0.5 * celula[i].dxL;
        double A1 = celula[i].duto.area;
        double dx1 = 0.5 * celula[i].dx;
        pmed -= rho0 * 9.81 * dx0 * sin(celula[i - 1].duto.teta) / 98066.52;
        tmed = celula[i].calor.Textern1;
        double taux = (dx0 * celula[i - 1].temp + dx1 * tmed) / (dx0 + dx1);
        celula[i].presaux = pmed;
        celula[i - 1].presauxR = celula[i].presaux;
        celula[i].presauxL = celula[i - 1].presaux;
        if (i <= arq.celdescargaP)
            rho1 = celula[i].fluicol.MasEspFlu(pmed, tmed);
        else
            rho1 = celula[i].flui.MasEspGas(pmed, tmed);
        pmed -= rho1 * 9.81 * dx1 * sin(celula[i].duto.teta) / 98066.52;
        rho0 = rho1;

        celula[i].presL = celula[i - 1].pres;
        celula[i].pres = pmed;
        celula[i - 1].presR = pmed;
        celula[i].presini = pmed;
        celula[i].presLini = celula[i - 1].presini;
        celula[i].presauxL = celula[i - 1].presaux;
        celula[i].tempL = celula[i - 1].temp;
        celula[i].temp = tmed;
        celula[i - 1].tempR = tmed;
        celula[i].tempini = tmed;

        celula[i].FW = 0;
        celula[i].FWini = 0;
        celula[i].arranjo = 0;
        celula[i].QL = 0;
        celula[i - 1].QLR = celula[i].QL;
        celula[i].QG = 0;
        celula[i].rpL = rho1;
        celula[i].rpC = rho1;
        celula[i - 1].rpR = celula[i].rpC;
        celula[i].rcL = rho1;
        celula[i].rcC = rho1;
        celula[i - 1].rcR = celula[i].rcC;

        double rhoaux;
        if (i <= arq.celdescargaP)
            rhoaux = celula[i].fluicol.MasEspFlu(celula[i].presaux, taux);
        else
            rhoaux = celula[0].flui.MasEspGas(celula[i].presaux, taux);
        celula[i].rpCi = rhoaux;
        celula[i - 1].rpRi = rhoaux;
        celula[i].rcCi = rhoaux;
        celula[i - 1].rcRi = rhoaux;

        celula[i].massfonteCH = 0;
        celula[i].fontemassCL = 0;
        celula[i].fontemassLL = 0;
        celula[i].fontemassGL = 0;
        celula[i].fontemassCR = 0;
        celula[i].fontemassLR = 0;
        celula[i].fontemassGR = 0;
        if (i <= arq.celdescargaP) {
            celula[i].alf = 0.;
            celula[i].bet = 1.;
            celula[i - 1].betR = celula[i].bet;
            celula[i - 1].betRini = celula[i].bet;
            celula[i - 1].alfR = celula[i].alf;
            celula[i - 1].alfRini = celula[i].alf;
            celula[i].betI = celula[i].bet;
            celula[i].alfini = celula[i].alf;
            celula[i].betini = celula[i].bet;
            celula[i].alfPigD = celula[i].alf;
            celula[i].betPigD = celula[i].bet;
            celula[i].alfPigE = celula[i].alf;
            celula[i].betPigE = celula[i].bet;
            celula[i - 1].alfR = celula[i].alf;
            celula[i - 1].alfRini = celula[i].alf;
            celula[i - 1].betR = celula[i].bet;
            celula[i - 1].betRini = celula[i].bet;
        } else {
            celula[i].alf = 1.;
            celula[i].bet = 0.;
            celula[i - 1].betR = celula[i].bet;
            celula[i - 1].betRini = celula[i].bet;
            celula[i - 1].alfR = celula[i].alf;
            celula[i - 1].alfRini = celula[i].alf;
            celula[i].betI = celula[i].bet;
            celula[i].alfini = celula[i].alf;
            celula[i].betini = celula[i].bet;
            celula[i].alfPigD = celula[i].alf;
            celula[i].betPigD = celula[i].bet;
            celula[i].alfPigE = celula[i].alf;
            celula[i].betPigE = celula[i].bet;
            celula[i - 1].alfR = celula[i].alf;
            celula[i - 1].alfRini = celula[i].alf;
            celula[i - 1].betR = celula[i].bet;
            celula[i - 1].betRini = celula[i].bet;
        }

        if (i < ncel) {
            celula[i + 1].tempL = tmed;
            celula[i + 1].QLL = celula[i].QL;
            celula[i + 1].rpL = celula[i].rpC;
            celula[i + 1].rcL = celula[i].rcC;
            celula[i + 1].rpLi = rhoaux;
            celula[i + 1].rcLi = rhoaux;
            celula[i + 1].alfL = celula[i].alf;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
            celula[i + 1].alfLini = celula[i].alf;
            celula[i + 1].betLini = celula[i].bet;
        }
    }
}

void SProd::montasistema(double *compfonte, int *posicfonte, int nfontes) {

    try {
        indTramo = -1;
        KontaImprime = 0;
        tempoaberto = 0.;
        tempoabertoini = 0.;
        modoPerm = 0;
        iterperm = 0;
        masChkSup = 0;
        dt = arq.dtmax;
        dtini = arq.dtmax;
        dtInter = arq.dtmax;
        tfinal = arq.tfinal;
        TransMassModel = 0;
        indpigP = 0;
        indpigPini = indpigP;
        reinicia = 0;
        masChkSupini = 0;
        betaRev = 0.;
        trackRGO = arq.trackRGO;
        trackDeng = arq.trackDeng;
        ninjgas = arq.ninjgas;
        lingas = arq.lingas;
        nfluP = arq.nfluP;
        ModelCp = arq.modelcp;
        Modeljtl = arq.modelJTL;
        CalcLat = arq.latente;
        if (arq.flashCompleto == 1)
            arq.trackRGO = 1;
        trackRGO = arq.trackRGO;
        trackDeng = arq.trackDeng;
        ninjgas = arq.ninjgas;
        lingas = arq.lingas;
        injPoc = arq.pocinjec + arq.condpocinj.tipoFlui;
        arq.fluc.injPoc = injPoc;
        if (arq.flashCompleto == 1)
            arq.fluc.injPoc = 0;
        (*vg1dSP).localtiny = arq.mono;
        (*vg1dSP).CritCond = arq.critcond;
        titRev = -1;
        if (injPoc >= 1 && arq.nipr == 0 && arq.condpocinj.CC != 3 && arq.condpocinj.CC != 4 && arq.condpocinj.CC != 5)
            NumError(
                "O simulador esta no modo Injecao de Agua em uma condicao de contorno que pede uma IPR e nao foi incluido nenhuma IPR no sistema");
        else if (injPoc >= 1 && arq.condpocinj.CC != 3 && arq.condpocinj.CC != 4 && arq.condpocinj.CC != 5) {
            int testaIPR = 0;
            for (int i = 0; i < arq.nipr; i++)
                if (arq.IPRS[i].indcel == arq.ncelp - 1)
                    testaIPR = 1;
            if (testaIPR == 0)
                NumError(
                    "O simulador esta no modo Injecao de Agua em uma condicao de contorno que pede uma IPR e a ultima celula nao contem uma IPR, e necessario neste modo se ter uma IPR na ultima celula");
        }

        npontos = arq.tabent.npont;
        ModelCp = arq.modelcp;
        if (arq.modelcp > 0)
            arq.geraTabCp();
        Modeljtl = arq.modelJTL;
        if (arq.modelJTL == 1)
            arq.geraTabDrholDt();
        cpg = arq.cpg;
        cpl = arq.cpl;
        drholdT = arq.drholdT;

        zdranP = arq.zdranP;
        dzdpP = arq.dzdpP;
        dzdtP = arq.dzdtP;
        fluiRevRede = arq.flup[0];
        if (arq.flashCompleto == 2)
            fluiRevRede.atualizaPropCompStandard();
        for (int i = 0; i < arq.nfluP; i++) {
            arq.flup[i].npontos = arq.tabent.npont;
            arq.flup[i].cpg = cpg;
            arq.flup[i].cpl = cpl;
            arq.flup[i].drholdT = drholdT;
            arq.flup[i].nfluP = nfluP;
        }
        arq.flug.npontos = arq.tabent.npont;
        arq.flug.cpg = cpg;
        arq.flug.cpl = cpl;
        arq.flug.drholdT = drholdT;
        arq.flug.nfluP = nfluP;

        arq.fluc.npontos = arq.tabent.npont;
        arq.fluc.RhoInj = arq.RhoInj;
        arq.fluc.ViscInj = arq.ViscInj;
        arq.fluc.CondInj = arq.CondInj;
        arq.fluc.CpInj = arq.CpInj;
        arq.fluc.DrhoDtInj = arq.DrhoDtInj;

        if (arq.modelcp > 0 || arq.modelJTL == 1 || arq.latente > 0 || (arq.pocinjec == 1 && (arq.condpocinj.tipoFlui == 2 || arq.condpocinj.tipoFlui == 3)))
            TransMassModel = arq.transmass;
        arq.geraduto();
        ncel = arq.ncelp - 1;
        temperatura = arq.celp[0].textern;
        tempRev = arq.tempReves;
        celula = new Cel[ncel + 1];
        arq.geracelp(celula);
        if (arq.nipr > 0)
            arq.geraipr(celula);
        if (arq.nvalvgas > 0 && arq.lingas > 0)
            arq.gerafgasVGL(celula);
        if (arq.ninjgas > 0)
            arq.gerafgasFonte(celula);
        for (int i = arq.ninjgas; i < arq.ninjgas + nfontes; i++) {
            int iposp = arq.buscaIndiceMeioP(compfonte[i - arq.ninjgas]);
            posicfonte[i - arq.ninjgas] = iposp;
            InjGas injgasMRT(0, 0, arq.flug);
            celula[iposp].acsr.tipo = 1;
            celula[iposp].acsr.injg = injgasMRT;
        }
        if (arq.ninjliq > 0)
            arq.gerafliqFonte(celula);
        if (arq.ninjmass > 0)
            arq.gerafmassFonte(celula);
        if (arq.nfuro > 0)
            arq.geraFuro(celula);
        if (arq.nPoroRad > 0)
            arq.gerafPoroRadFonte(celula);
        if (arq.nPoro2D > 0)
            arq.gerafPoro2DFonte(celula);
        if (celula[0].acsr.tipo != 0 && arq.ConContEntrada == 1) {
            // RN-302: The inlet has both a pressure boundary condition and a mass source. Report a warning.
            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                       "Foi escolhida uma condicao de contorno de pressao no inicio da tubulacao e foi colocada uma fonte de massa neste inicio, isto pode causar inconsistencias",
                       "", "");
        }
        if ((arq.perm == 1 || (*vg1dSP).chaverede > 0) && celula[0].acsr.tipo == 0) {
            if ((arq.ConContEntrada == 0 && nfontes == 0 && (*vg1dSP).chaverede == 0)) {
                NumError("O simulador pede calculo de permanente com condicao de vazao na entrada, mas sem nenhuma fonte na entrada");
            }
            if ((arq.ConContEntrada == 0 && nfontes == 0 && (*vg1dSP).chaverede > 0) || arq.CCPres.tit[0] < (1 - (*vg1dSP).localtiny)) {
                if (arq.tipoFluido == 1)
                    celula[0].acsr.tipo = 1;
                else if (arq.tipoFluido == 0)
                    celula[0].acsr.tipo = 2;
                else
                    celula[0].acsr.tipo = 10;
                InjMult injmassMRT(0, 0, 0, celula[0].temp, celula[0].flui, celula[0].fluicol);
                celula[0].acsr.injm.condTermo = 1;
                celula[0].acsr.injm = injmassMRT;
                InjLiq injliqMRT(0, 0, 0, celula[0].flui, celula[0].fluicol);
                celula[0].acsr.injl = injliqMRT;
                InjGas injgasMRT(0, 0, celula[0].flui, celula[0].fluicol);
                celula[0].acsr.injg = injgasMRT;
            } else if (nfontes == 0 || arq.ConContEntrada == 1) {
                InjGas injgasMRT(0, 0, celula[0].flui, celula[0].fluicol);
                injgasMRT.seco = 0;
                celula[0].acsr.injg = injgasMRT;
                InjMult injmassMRT(0, 0, 0, celula[0].temp, celula[0].flui, celula[0].fluicol);
                celula[0].acsr.injm.condTermo = 1;
                celula[0].acsr.injm = injmassMRT;
                InjLiq injliqMRT(0, 0, 0, celula[0].flui, celula[0].fluicol);
                celula[0].acsr.injl = injliqMRT;
                if (arq.tipoFluido == 1)
                    celula[0].acsr.tipo = 1;
                else if (arq.tipoFluido == 0)
                    celula[0].acsr.tipo = 2;
                else
                    celula[0].acsr.tipo = 10;
            }
        }
        if (bloq == 1 && celula[1].acsr.tipo == 0) {
            if ((*vg1dSP).fluidoRede == 1)
                celula[1].acsr.tipo = 2;
            else if ((*vg1dSP).fluidoRede == 0)
                celula[1].acsr.tipo = 1;
            else
                celula[1].acsr.tipo = 10;
            InjMult injmassMRT(0, 0, 0, celula[0].temp, celula[0].flui, celula[0].fluicol);
            celula[1].acsr.injm.condTermo = 1;
            celula[1].acsr.injm = injmassMRT;
            InjLiq injliqMRT(0, 0, 0, celula[0].flui, celula[0].fluicol);
            celula[1].acsr.injl = injliqMRT;
            InjGas injgasMRT(0, 0, celula[0].flui, celula[0].fluicol);
            celula[1].acsr.injg = injgasMRT;
        } else if (bloq == 1) {
            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                       "Foi escolhida uma condicao de bloqueio que afeta este tramo, para que a selecao de fontes seja"
                       "feita corretamente, e necessario que a segunda celula do tramo nao tenha fontes catalogadas",
                       "", "");
        }
        if (arq.nbcs > 0)
            arq.gerafBCS(celula);
        if (arq.nmultibcs > 0)
            arq.gerafmultiBCS(celula);
        if (arq.nbvol > 0)
            arq.gerafBVOL(celula);
        if (arq.ndpreq > 0)
            arq.geraDPReq(celula);
        if (arq.ncalor > 0)
            arq.geraFonteCalor(celula);
        if (arq.master1.posic > 0.)
            arq.geraMaster1(celula);
        else {
            int verifica = 0;
            double Lverifica = 0.5 * celula[verifica].dx;
            while (celula[verifica].acsr.tipo != 0) {
                verifica++;
                Lverifica += 0.5 * (celula[verifica].dx + celula[verifica - 1].dx);
            }
            arq.master1.posic = verifica;
            arq.master1.comp = Lverifica;
            arq.geraMaster1(celula);
        }
        if (arq.nvalv > 0)
            arq.geraValv(celula);
        arq.gerapresfim(presfim, pGSup);
        pGSupIni = pGSup;
        chokeSup = choke(1., 1.);
        chokeInj = ChokeGas();
        arq.gerachokesup(chokeSup);
        npig = arq.npig;
        if (npig > 0) {
            receb = new int[npig];
            for (int i = 0; i < npig; i++)
                receb[i] = arq.pig[i].receb;
        }
        if (arq.lingas == 0)
            ncelGas = 0;
        else if (arq.lingas > 0) {
            ncelGas = arq.ncelg;
            ncelGas--;
            celInter = arq.celdescarga;
            celulaG = new CelG[ncelGas + 1];
            arq.geracelg(celulaG);
            arq.geraMaster2(celulaG);
            arq.gerachokeinj(chokeInj);
            if (arq.nvalvgas > 0) {
                chokeVGL = new ChokeGas[arq.nvalvgas];
                posicVGLP = new int[arq.nvalvgas];
                posicVGLG = new int[arq.nvalvgas];
            }
            for (int i = 0; i < arq.nvalvgas; i++) {
                double diaG = arq.valvgl[i].diagarg;
                double presEstag = celulaG[arq.valvgl[i].posicG].pres;
                double tempEstag = celulaG[arq.valvgl[i].posicG].temp;
                double presGarg = celula[arq.valvgl[i].posicP].pres;
                posicVGLP[i] = arq.valvgl[i].posicP;
                posicVGLG[i] = arq.valvgl[i].posicG;
                celulaG[posicVGLG[i]].vgl = 1;
                if (arq.valvgl[i].tipo == 2) {
                    arq.valvgl[i].frec = 0.;
                    arq.valvgl[i].cd = 1.;
                }
                chokeVGL[i] = ChokeGas(arq.flug, M_PI * diaG * diaG / 4.,
                                       arq.valvgl[i].diaexter, arq.valvgl[i].cd, presEstag, presGarg,
                                       tempEstag, arq.valvgl[i].frec, arq.valvgl[i].tipo,
                                       (M_PI * diaG * diaG / 4.) / arq.valvgl[i].razarea,
                                       arq.valvgl[i].pcali, arq.valvgl[i].tcali, arq.valvgl[i].cdLiq, arq.valvgl[i].frecLiq);
                celulaG[posicVGLG[i]].pEstag = chokeVGL[i].presEstag;
                celulaG[posicVGLG[i]].tEstag = chokeVGL[i].tempEstag;
                celulaG[posicVGLG[i]].pGarg = chokeVGL[i].presGarg;
                celulaG[posicVGLG[i]].tGarg = chokeVGL[i].tempGarg;
                celulaG[posicVGLG[i]].qGarg = chokeVGL[i].qGarg;
                celulaG[posicVGLG[i]].areaGarg = chokeVGL[i].areagarg;
            }
            for (int i = 0; i <= ncelGas; i++) {
                celulaG[i].celInter = &celInter;
                celulaG[i].razInter = 1.;
                celulaG[i].razInterIni = 1.;
                if (i >= celInter) {
                    celulaG[i].razInter = 0.;
                    celulaG[i].razInterIni = 0.;
                }
            }
        }
        if (celula[0].acsr.tipo != 1 && celula[0].acsr.tipo != 2 && celula[0].acsr.tipo != 3 && celula[0].acsr.tipo != 10 && celula[0].acsr.tipo != 15 && celula[0].acsr.tipo != 16) {
            if (arq.perm == 0 && arq.ConContEntrada == 0) {
                // RN-300: No source is defined in the first production system cell. Report a warning.
                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                           "Nao existe nenhuma fonte na primeira celula do sistema de producao, esta nao e a condicao mais adequada para a simulacao transiente, problemas podem ocorrer nesta situacao, embora nao seja impeditivo para o modo",
                           "", "");
            } else {
                // RN-301: No source is defined in the first production system cell
                // under steady-state conditions. Report a failure.
                NumError("Nao existe nenhuma fonte na primeira celula do sistema de producao, no modo permanente esta e uma condicao necessaria");
            }
        }

        if (arq.descarga == 1 && celula[0].acsr.tipo != 3) {
            NumError(
                "Nao existe nenhuma IPR na primeira celula do sistema de producao, no modo descarga de gas lift esta e uma condicao necessaria");
        }
        if (arq.descarga == 1) {
            HidroDescargaG();
            HidroDescargaP();
        }

        arq.geraevento(noinicial, noextremo);
        if (arq.saidaClassica == 1 && (arq.transiente == 1 || (*vg1dSP).chaveredeT == 1)) {
            cout << "------------------------------------------   Eventos no Tramo -------------------------------------------" << endl;
            int contaImp = 0;
            for (int i = 0; i < arq.logevento.size(); i++)
                if (arq.logevento[i].descricao != "Gravando Perfil Linha de Producao" && arq.logevento[i].descricao != "Gravando Perfil Linha de Gas") {
                    cout << contaImp << "  momento = " << arq.logevento[i].instante << " segundos " << " tipo de evento = " << arq.logevento[i].descricao << endl;
                    contaImp++;
                }
        }

        if (arq.pocinjec == 0 && arq.perm == 1 && ((*vg1dSP).chaverede == 0 || noextremo == 1) && arq.ConContEntrada != 2 && arq.psep.pres[0] < -1e6) {
            NumError("sem pressao a jusante como condição de contorno para um tramo onde esta condicao é necessaria ");
        }

        if ((celula[ncel].acsr.tipo != 0 || celula[ncel - 1].acsr.tipo != 0) && arq.transiente == 1) {
            NumError("As duas celulas finais não devem ter acessorios, estas duas celulas devem ser preservadas, principalmente para simulacoes transientes ");
        }

        (*vg1dSP).lixo5 = 0.;
        (*vg1dSP).contador = 0;
        if (arq.pocinjec == 0) {
            arq.atualiza(noinicial, noextremo, derivaAnel, chokeSup, chokeInj, celula, celulaG, pGSup, temperatura, presiniG,
                         tempiniG, presE, tempE, titE, betaE, (*vg1dSP).lixo5, dt);
            pGSupIni = pGSup;
            // presiniG,tempiniG,presE,tempE,titE,betaE,(*vg1dSP).lixo5);//alteracao7
            if (chokeSup.AreaGarg >= 0.6 * celula[ncel - 1].duto.area) {
                aberto = 1;
                abertoini = 1;
            } else {
                aberto = 0;
                abertoini = 1;
            }
        }

        tempSup = celula[ncel].temp;
        masSup = celula[ncel - 1].MC;

        tGSup = celula[ncel].calor.Textern1;
        if (arq.tabelaDinamica == 1) {
            int minNPontos = 0;
            ntabDin = 1;
            tabelaDinamica temp;
            temp.npontosP = minNPontos;
            temp.npontosT = minNPontos;
            temp.celIni = 0;
            temp.rhogF = 0;
            temp.rholF = 0;
            temp.DrhogDpF = 0;
            temp.DrhogDtF = 0;
            temp.DrholDpF = 0;
            temp.DrholDtF = 0;
            temp.valBO = 0;
            temp.HgF = 0;
            temp.HlF = 0;
            temp.cpgF = 0;
            temp.cplF = 0;
            temp.valZ = 0;
            temp.valdZdT = 0;
            temp.valdZdP = 0;
            temp.tit = 0;
            temp.rs = 0;
            temp.viscG = 0;
            temp.viscO = 0;
            temp.TBF = 0;
            temp.PBF = 0;
            tabDin.push_back(temp);
            for (int i = 1; i < ncel; i++) {
                if ((celula[i].acsr.tipo == 1) ||
                    (celula[i].acsr.tipo == 2) ||
                    celula[i].acsr.tipo == 3 ||
                    (celula[i].acsr.tipo == 10) || celula[i].acsr.tipo == 9 || celula[i].acsr.tipo == 15 || celula[i].acsr.tipo == 16) {
                    tabelaDinamica temp;
                    temp.npontosP = minNPontos;
                    temp.npontosT = minNPontos;
                    temp.celIni = i + 1;
                    tabDin[ntabDin - 1].celFim = i;
                    tabDin.push_back(temp);
                    ntabDin++;
                }
            }
            tabDin[ntabDin - 1].celFim = ncel;
        }

        celula[0].angEsq = celula[0].duto.teta;
        for (int i = 1; i <= ncel; i++) {
            if (fabs(celula[i].duto.teta) < 1e-10)
                celula[i].angEsq = celula[i - 1].angEsq;
            else
                celula[i].angEsq = celula[i].duto.teta;
        }
        celula[ncel].angEsq = celula[ncel].duto.teta;
        for (int i = ncel - 1; i >= 0; i--) {
            if (fabs(celula[i].duto.teta) < 1e-10)
                celula[i].angDir = celula[i + 1].angDir;
            else
                celula[i].angDir = celula[i].duto.teta;
        }

        CalcLat = arq.latente;
        if (arq.latente > 0) {
            if (arq.flashCompleto == 0) {
                string impfile;
                impfile = arq.pvtsimarq;
                string dadosMR = impfile;
                ifstream lendoPVTSim(dadosMR.c_str(), ios_base::in);
                string chave;
                char *tenta;
                tenta = new char[400];
                double testatok;
                int ndiv = arq.tabent.npont - 1;
                Vcr<double> presPVTSim(ndiv + 1, 0.);
                Vcr<double> tempPVTSim(ndiv + 1, 0.);

                FullMtx<double> HLatTemp(ndiv + 2, ndiv + 2);
                char line[4000];
                lendoPVTSim.get(line, 4000);
                tenta = strtok(line, " ,()=");
                while (strcmp(tenta, "PHASE") != 0) {
                    tenta = strtok(NULL, " ,()=");
                }
                tenta = strtok(NULL, " ,()=");
                int lacoleitura = 12;
                if (strcmp(tenta, "THREE") == 0)
                    lacoleitura = 18;

                while (chave != "PRESSURE")
                    lendoPVTSim >> chave;
                lendoPVTSim.get(line, 4000);
                tenta = strtok(line, " ,()=");
                presPVTSim[0] = atof(tenta) * 1.01971621e-5;
                for (int kontaPVT = 1; kontaPVT <= ndiv; kontaPVT++) {
                    tenta = strtok(NULL, " ,");
                    presPVTSim[kontaPVT] = atof(tenta) * 1.01971621e-5;
                }
                while (chave != "TEMPERATURE")
                    lendoPVTSim >> chave;
                lendoPVTSim.get(line, 4000);
                tenta = strtok(line, " ,()=");
                tempPVTSim[0] = atof(tenta);
                for (int kontaPVT = 1; kontaPVT <= ndiv; kontaPVT++) {
                    tenta = strtok(NULL, " ,");
                    tempPVTSim[kontaPVT] = atof(tenta);
                }

                for (int i = 1; i <= ndiv + 1; i++) {
                    HLatTemp[i][0] = presPVTSim[i - 1];
                    for (int j = 1; j <= ndiv + 1; j++) {
                        HLatTemp[0][j] = tempPVTSim[j - 1];
                        while (chave != "POINT")
                            lendoPVTSim >> chave;
                        lendoPVTSim.get(line, 4000);
                        tenta = strtok(line, " ,()=");
                        for (int kontaPVT = 0; kontaPVT < lacoleitura; kontaPVT++) {
                            tenta = strtok(NULL, " ,");
                            testatok = atof(tenta);
                        }
                        tenta = strtok(NULL, " ,");
                        testatok = atof(tenta);
                        HLatTemp[i][j] = testatok;
                        tenta = strtok(NULL, " ,");
                        testatok = atof(tenta);
                        HLatTemp[i][j] -= testatok;
                        if (i == ndiv + 1 && j == ndiv + 1)
                            break;
                        while (chave != "PVTTABLE")
                            lendoPVTSim >> chave;
                    }
                }
                lendoPVTSim.close();
                HLat = new double *[ndiv + 2];
                for (int i = 0; i < ndiv + 2; i++) {
                    HLat[i] = new double[ndiv + 2];
                    for (int j = 0; j < ndiv + 2; j++)
                        HLat[i][j] = HLatTemp[i][j];
                }
                ostringstream saidaLatente;
                saidaLatente << pathPrefixoArqSaida << "perfilLatente.dat";
                string tmp = saidaLatente.str();
                ofstream escreveMass(tmp.c_str(), ios_base::out);
                escreveMass << HLatTemp;
                escreveMass.close();
                // caso nao seja simulacao POCO_INJETOR
                if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
                    arqRelatorioPerfis << tmp.c_str() << endl;
                    arqRelatorioPerfis.flush();
                }
            }
        }

        if (celula[0].flui.corrSat == -4) {
            LerPB = 1;
            int ndiv = arq.tabent.npont - 1;
            PBPVTSim = new double[ndiv + 1];
            TBPVTSim = new double[ndiv + 1];
            double *PresPVTSim;
            PresPVTSim = new double[ndiv + 1];

            string impfile;
            impfile = arq.pvtsimarq;
            string dadosMR = impfile;
            ifstream lendoPVTSim(dadosMR.c_str(), ios_base::in);
            string chave;
            char *tenta;
            tenta = new char[400];
            double testatok;
            char line[4000];
            lendoPVTSim.get(line, 4000);
            tenta = strtok(line, " ,()=");
            lendoPVTSim >> chave;
            while (chave != "PRESSURE") {
                lendoPVTSim >> chave;
            }
            int lacoleitura = ndiv;
            lendoPVTSim.get(line, 4000);
            tenta = strtok(line, " ,()=");
            PresPVTSim[0] = atof(tenta) / 98068.059233;
            double valor;
            for (int kontaPVT = 1; kontaPVT <= lacoleitura; kontaPVT++) {
                tenta = strtok(NULL, " ,");
                testatok = atof(tenta);
                PresPVTSim[kontaPVT] = testatok / 98068.059233;
            }
            while (chave != "BUBBLEPRESSURES") {
                lendoPVTSim >> chave;
            }
            lendoPVTSim.get(line, 4000);
            tenta = strtok(line, " ,()=");
            PBPVTSim[0] = atof(tenta) * 0.00014503773800722;
            for (int kontaPVT = 1; kontaPVT <= lacoleitura; kontaPVT++) {
                tenta = strtok(NULL, " ,");
                testatok = atof(tenta);
                PBPVTSim[kontaPVT] = testatok * 0.00014503773800722;
            }
            while (chave != "BUBBLETEMPERATURES") {
                lendoPVTSim >> chave;
            }
            lendoPVTSim.get(line, 4000);
            tenta = strtok(line, " ,()=");
            TBPVTSim[0] = atof(tenta);
            for (int kontaPVT = 1; kontaPVT <= lacoleitura; kontaPVT++) {
                tenta = strtok(NULL, " ,");
                testatok = atof(tenta);
                TBPVTSim[kontaPVT] = testatok;
            }
            for (int i = 0; i <= ncel; i++) {
                celula[i].flui.PBPVTSim = PBPVTSim;
                celula[i].flui.TBPVTSim = TBPVTSim;
                celula[i].flui.corrSat = 4;
                if (celula[i].acsr.tipo == 2) {
                    celula[i].acsr.injl.FluidoPro.PBPVTSim = PBPVTSim;
                    celula[i].acsr.injl.FluidoPro.TBPVTSim = TBPVTSim;
                    celula[i].acsr.injl.FluidoPro.corrSat = 4;
                }
                if (celula[i].acsr.tipo == 3) {
                    celula[i].acsr.ipr.FluidoPro.PBPVTSim = PBPVTSim;
                    celula[i].acsr.ipr.FluidoPro.TBPVTSim = TBPVTSim;
                    celula[i].acsr.ipr.FluidoPro.corrSat = 4;
                }
                if (celula[i].acsr.tipo == 10) {
                    celula[i].acsr.injm.FluidoPro.PBPVTSim = PBPVTSim;
                    celula[i].acsr.injm.FluidoPro.TBPVTSim = TBPVTSim;
                    celula[i].acsr.injm.FluidoPro.corrSat = 4;
                }
                if (celula[i].acsr.tipo == 15) {
                    celula[i].acsr.radialPoro.flup.PBPVTSim = PBPVTSim;
                    celula[i].acsr.radialPoro.flup.TBPVTSim = TBPVTSim;
                    celula[i].acsr.radialPoro.flup.corrSat = 4;
                    for (int iRP = 0; iRP < celula[i].acsr.radialPoro.ncel; iRP++) {
                        celula[i].acsr.radialPoro.celula[iRP].flup.PBPVTSim = PBPVTSim;
                        celula[i].acsr.radialPoro.celula[iRP].flup.TBPVTSim = TBPVTSim;
                        celula[i].acsr.radialPoro.celula[iRP].flup.corrSat = 4;
                    }
                }
                if (celula[i].acsr.tipo == 16) {
                    celula[i].acsr.poroso2D.dados.flup.PBPVTSim = PBPVTSim;
                    celula[i].acsr.poroso2D.dados.flup.TBPVTSim = TBPVTSim;
                    celula[i].acsr.poroso2D.dados.flup.corrSat = 4;
                    for (int iRP = 0; iRP < celula[i].acsr.poroso2D.dados.transfer.ncel; iRP++) {
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.PBPVTSim = PBPVTSim;
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.TBPVTSim = TBPVTSim;
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.corrSat = 4;
                    }
                    for (int iRP = 0; iRP < celula[i].acsr.poroso2D.malha.nele; iRP++) {
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.PBPVTSim = PBPVTSim;
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.TBPVTSim = TBPVTSim;
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.corrSat = 4;
                    }
                }
            }

            FullMtx<double> BolhaTemp(ndiv + 2, 2);
            for (int i = 0; i <= ndiv; i++) {
                BolhaTemp[i][0] = TBPVTSim[i];
                BolhaTemp[i][1] = PBPVTSim[i];
            }
            ostringstream saidaBolha;
            saidaBolha << pathPrefixoArqSaida << "perfilBolha";
            string tmp = saidaBolha.str();
            ofstream escreveMass(tmp.c_str(), ios_base::out);
            escreveMass << BolhaTemp;
            escreveMass.close();

            if (arq.tabRSPB == 1) {
                arq.tabRSPB = 1;
                lerRS = 1;
                RSLivia = new double *[ndiv + 2];
                for (int i = 0; i < ndiv + 2; i++) {
                    RSLivia[i] = new double[ndiv + 2];
                }
                for (int i = 1; i <= ndiv + 1; i++) {
                    RSLivia[i][0] = PresPVTSim[i - 1];
                    RSLivia[0][i] = TBPVTSim[i - 1];
                    for (int j = 1; j <= ndiv + 1; j++) {
                        if (TBPVTSim[j - 1] > -10) {
                            double a1, a2, a3;
                            double TFa = 1.8 * TBPVTSim[j - 1] + 32;

                            double A0, A1, A2, A3, A4;
                            double B0, B1, B2, B3, B4;
                            double C0, C1, C2, C3, C4;
                            double D0, D1, D2;

                            A0 = 6542.69213 * 1e-11;
                            A1 = 2.60464618;
                            A2 = 1.21334544;
                            A3 = 0.16464125;
                            A4 = 1.19382967;
                            B0 = 87 * 1e-8;
                            B1 = 4.77390016;
                            B2 = 1.77267703;
                            B3 = -0.73072038;
                            B4 = 1.58093857;
                            C0 = 3226.09 * 1e-6;
                            C1 = 0.09281075;
                            C2 = 0.13633665;
                            C3 = 0.09634381;
                            C4 = 0.53238728;
                            D0 = 1.00544053;
                            D1 = -0.00134177;
                            D2 = 0.51839397;

                            double yco2 = celula[0].flui.yco2;
                            double Deng = celula[0].flui.Deng;
                            double API = celula[0].flui.API;
                            double multCor = (D0 + D1 * yco2 * pow(TFa, D2));
                            double pbtemp = PBPVTSim[j - 1];

                            a1 = A0 * pow(Deng, A1) * pow(API, A2) * pow(TFa, A3) * pow(pbtemp, A4);
                            a2 = B0 * pow(Deng, B1) * pow(API, B2) * pow(TFa, B3) * pow(pbtemp, B4);
                            a3 = C0 * pow(Deng, C1) * pow(API, C2) * pow(TFa, C3) * pow(pbtemp, C4);

                            double pcor = (RSLivia[i][0] * 0.9678411) * 14.69595 * multCor;
                            double pr = pcor / pbtemp;

                            a1 = A0 * pow(Deng, A1) * pow(API, A2) * pow(TFa, A3) * pow(pbtemp, A4);
                            a2 = B0 * pow(Deng, B1) * pow(API, B2) * pow(TFa, B3) * pow(pbtemp, B4);
                            a3 = C0 * pow(Deng, C1) * pow(API, C2) * pow(TFa, C3) * pow(pbtemp, C4);

                            double Rsr = a1 * pow(pr, a2) + (1. - a1) * pow(pr, a3);
                            double rstemp = 0.;
                            if (rstemp < 0.)
                                rstemp = 0.;
                            else if ((RSLivia[i][0] * 0.9678411) * 14.69595 > pbtemp)
                                rstemp = 1.;
                            else
                                rstemp = Rsr;
                            RSLivia[i][j] = rstemp;
                        } else
                            RSLivia[i][j] = 0;
                    }
                }
                FullMtx<double> RSTemp(ndiv + 2, ndiv + 2);
                for (int i = 1; i <= ndiv + 1; i++) {
                    RSTemp[i][0] = RSLivia[i][0];
                    RSTemp[0][i] = RSLivia[0][i];
                    for (int j = 1; j <= ndiv + 1; j++) {
                        if (i > 0 || j > 0)
                            RSTemp[i][j] = RSLivia[i][j] * 6.29 / 35.31467;
                    }
                }
                ostringstream saidaRS;
                saidaRS << pathPrefixoArqSaida << "perfilRSLivia";
                tmp = saidaRS.str();
                ofstream escreveRS(tmp.c_str(), ios_base::out);
                escreveRS << RSTemp;
                escreveRS.close();

                for (int i = 0; i <= ncel; i++) {
                    celula[i].flui.TabRSLivia = RSLivia;
                    celula[i].flui.tabRSPB = 1;
                    if (celula[i].acsr.tipo == 2) {
                        celula[i].acsr.injl.FluidoPro.TabRSLivia = RSLivia;
                        celula[i].acsr.injl.FluidoPro.tabRSPB = 1;
                    }
                    if (celula[i].acsr.tipo == 3) {
                        celula[i].acsr.ipr.FluidoPro.TabRSLivia = RSLivia;
                        celula[i].acsr.ipr.FluidoPro.tabRSPB = 1;
                    }
                    if (celula[i].acsr.tipo == 10) {
                        celula[i].acsr.injm.FluidoPro.TabRSLivia = RSLivia;
                        celula[i].acsr.injm.FluidoPro.tabRSPB = 1;
                    }
                    if (celula[i].acsr.tipo == 15) {
                        celula[i].acsr.radialPoro.flup.TabRSLivia = RSLivia;
                        celula[i].acsr.radialPoro.flup.tabRSPB = 1;
                        for (int iRP = 0; iRP < celula[i].acsr.radialPoro.ncel; iRP++) {
                            celula[i].acsr.radialPoro.celula[iRP].flup.TabRSLivia = RSLivia;
                            celula[i].acsr.radialPoro.celula[iRP].flup.tabRSPB = 1;
                        }
                    }
                    if (celula[i].acsr.tipo == 16) {
                        celula[i].acsr.poroso2D.dados.flup.TabRSLivia = RSLivia;
                        celula[i].acsr.poroso2D.dados.flup.tabRSPB = 1;
                        for (int iRP = 0; iRP < celula[i].acsr.poroso2D.dados.transfer.ncel; iRP++) {
                            celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.TabRSLivia = RSLivia;
                            celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.tabRSPB = 1;
                        }
                        for (int iRP = 0; iRP < celula[i].acsr.poroso2D.malha.nele; iRP++) {
                            celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.TabRSLivia = RSLivia;
                            celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.tabRSPB = 1;
                        }
                    }
                }
            }
            delete[] PresPVTSim;

        } else if (arq.tabRSPB == 1) {

            int ndiv = arq.tabent.npont - 1;
            double pteste = arq.tabent.pmin;
            double tteste = arq.tabent.tmin;
            double dpteste = (arq.tabent.pmax - pteste) / ndiv;
            double dtteste = (arq.tabent.tmax - tteste) / ndiv;
            FullMtx<double> RSTemp(ndiv + 2, ndiv + 2);
            FullMtx<double> PBTemp(ndiv + 2, 2);
            TBPVTSim = new double[ndiv + 1];
            PBPVTSim = new double[ndiv + 1];
            RSLivia = new double *[ndiv + 2];
            for (int i = 0; i < ndiv + 2; i++) {
                RSLivia[i] = new double[ndiv + 2];
            }
            double ttestepb = tteste;
            for (int i = 0; i <= ndiv; i++) {
                TBPVTSim[i] = ttestepb;
                PBPVTSim[i] = arq.flup[0].PB(pteste, ttestepb);
                PBTemp[i][0] = TBPVTSim[i];
                PBTemp[i][1] = PBPVTSim[i];
                ttestepb += dtteste;
            }

            for (int i = 1; i <= ndiv + 1; i++) {
                RSLivia[i][0] = pteste;
                RSTemp[i][0] = RSLivia[i][0];
                ttestepb = tteste;
                for (int j = 1; j <= ndiv + 1; j++) {
                    RSLivia[0][j] = ttestepb;
                    RSLivia[i][j] = arq.flup[0].RS(pteste, ttestepb);
                    RSTemp[0][j] = RSLivia[0][j];
                    RSTemp[i][j] = RSLivia[i][j] * 6.29 / 35.31467;
                    ttestepb += dtteste;
                }
                pteste += dpteste;
            }

            ostringstream saidaBolha;
            saidaBolha << pathPrefixoArqSaida << "perfilBolha";
            string tmp = saidaBolha.str();
            ofstream escreveMass(tmp.c_str(), ios_base::out);
            escreveMass << PBTemp;
            escreveMass.close();

            ostringstream saidaRS;
            saidaRS << pathPrefixoArqSaida << "perfilRSLivia";
            tmp = saidaRS.str();
            ofstream escreveRS(tmp.c_str(), ios_base::out);
            escreveRS << RSTemp;
            escreveRS.close();

            for (int i = 0; i <= ncel; i++) {
                celula[i].flui.PBPVTSim = PBPVTSim;
                celula[i].flui.TBPVTSim = TBPVTSim;
                celula[i].flui.TabRSLivia = RSLivia;
                celula[i].flui.tabRSPB = 1;

                if (celula[i].acsr.tipo == 2) {
                    celula[i].acsr.injl.FluidoPro.PBPVTSim = PBPVTSim;
                    celula[i].acsr.injl.FluidoPro.TBPVTSim = TBPVTSim;
                    celula[i].acsr.injl.FluidoPro.TabRSLivia = RSLivia;
                    celula[i].acsr.injl.FluidoPro.tabRSPB = 1;
                }
                if (celula[i].acsr.tipo == 3) {
                    celula[i].acsr.ipr.FluidoPro.PBPVTSim = PBPVTSim;
                    celula[i].acsr.ipr.FluidoPro.TBPVTSim = TBPVTSim;
                    celula[i].acsr.ipr.FluidoPro.TabRSLivia = RSLivia;
                    celula[i].acsr.ipr.FluidoPro.tabRSPB = 1;
                }
                if (celula[i].acsr.tipo == 10) {
                    celula[i].acsr.injm.FluidoPro.PBPVTSim = PBPVTSim;
                    celula[i].acsr.injm.FluidoPro.TBPVTSim = TBPVTSim;
                    celula[i].acsr.injm.FluidoPro.TabRSLivia = RSLivia;
                    celula[i].acsr.injm.FluidoPro.tabRSPB = 1;
                }
                if (celula[i].acsr.tipo == 15) {
                    celula[i].acsr.radialPoro.flup.PBPVTSim = PBPVTSim;
                    celula[i].acsr.radialPoro.flup.TBPVTSim = TBPVTSim;
                    celula[i].acsr.radialPoro.flup.TabRSLivia = RSLivia;
                    celula[i].acsr.radialPoro.flup.tabRSPB = 1;
                    for (int iRP = 0; iRP < celula[i].acsr.radialPoro.ncel; iRP++) {
                        celula[i].acsr.radialPoro.celula[iRP].flup.PBPVTSim = PBPVTSim;
                        celula[i].acsr.radialPoro.celula[iRP].flup.TBPVTSim = TBPVTSim;
                        celula[i].acsr.radialPoro.celula[iRP].flup.TabRSLivia = RSLivia;
                        celula[i].acsr.radialPoro.celula[iRP].flup.tabRSPB = 1;
                    }
                }
                if (celula[i].acsr.tipo == 16) {
                    celula[i].acsr.poroso2D.dados.flup.PBPVTSim = PBPVTSim;
                    celula[i].acsr.poroso2D.dados.flup.TBPVTSim = TBPVTSim;
                    celula[i].acsr.poroso2D.dados.flup.TabRSLivia = RSLivia;
                    celula[i].acsr.poroso2D.dados.flup.tabRSPB = 1;
                    for (int iRP = 0; iRP < celula[i].acsr.poroso2D.dados.transfer.ncel; iRP++) {
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.PBPVTSim = PBPVTSim;
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.TBPVTSim = TBPVTSim;
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.TabRSLivia = RSLivia;
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.tabRSPB = 1;
                    }
                    for (int iRP = 0; iRP < celula[i].acsr.poroso2D.malha.nele; iRP++) {
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.PBPVTSim = PBPVTSim;
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.TBPVTSim = TBPVTSim;
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.TabRSLivia = RSLivia;
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.tabRSPB = 1;
                    }
                }
            }

            LerPB = 1;
            lerRS = 1;
        }
        for (int i = 0; i <= ncel; i++) {
            if (arq.corrDeng == 0) {
                celula[i].flui.rDgD = 1.;
                celula[i].flui.rDgL = 1.;
                celula[i].flui.PCis = celula[i].flui.PC;
                celula[i].flui.TCis = celula[i].flui.TC;
                if (celula[i].acsr.tipo == 2) {
                    celula[i].acsr.injl.FluidoPro.rDgD = 1.;
                    celula[i].acsr.injl.FluidoPro.rDgL = 1.;
                } else if (celula[i].acsr.tipo == 3) {
                    celula[i].acsr.ipr.FluidoPro.rDgD = 1.;
                    celula[i].acsr.ipr.FluidoPro.rDgL = 1.;
                } else if (celula[i].acsr.tipo == 10) {
                    celula[i].acsr.injm.FluidoPro.rDgD = 1.;
                    celula[i].acsr.injm.FluidoPro.rDgL = 1.;
                }
                if (celula[i].acsr.tipo == 15) {
                    celula[i].acsr.radialPoro.flup.rDgD = 1.;
                    celula[i].acsr.radialPoro.flup.rDgD = 1.;
                    for (int iRP = 0; iRP < celula[i].acsr.radialPoro.ncel; iRP++) {
                        celula[i].acsr.radialPoro.celula[iRP].flup.rDgD = 1.;
                        celula[i].acsr.radialPoro.celula[iRP].flup.rDgD = 1.;
                    }
                }
                if (celula[i].acsr.tipo == 16) {
                    celula[i].acsr.poroso2D.dados.flup.rDgD = 1.;
                    celula[i].acsr.poroso2D.dados.flup.rDgD = 1.;
                    for (int iRP = 0; iRP < celula[i].acsr.poroso2D.dados.transfer.ncel; iRP++) {
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.rDgD = 1.;
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.rDgL = 1.;
                    }
                    for (int iRP = 0; iRP < celula[i].acsr.poroso2D.malha.nele; iRP++) {
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.rDgD = 1.;
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.rDgL = 1.;
                    }
                }
            } else {
                celula[i].flui.razDegD(celula[i].pres, celula[i].temp);
                celula[i].flui.rzDegL(celula[i].pres, celula[i].temp);
                celula[i].flui.PcTcIS();
                if (celula[i].acsr.tipo == 2) {
                    celula[i].acsr.injl.FluidoPro.razDegD(celula[i].pres, celula[i].temp);
                    celula[i].acsr.injl.FluidoPro.rzDegL(celula[i].pres, celula[i].temp);
                } else if (celula[i].acsr.tipo == 3) {
                    celula[i].acsr.ipr.FluidoPro.razDegD(celula[i].pres, celula[i].temp);
                    celula[i].acsr.ipr.FluidoPro.rzDegL(celula[i].pres, celula[i].temp);
                } else if (celula[i].acsr.tipo == 10) {
                    celula[i].acsr.injm.FluidoPro.razDegD(celula[i].pres, celula[i].temp);
                    celula[i].acsr.injm.FluidoPro.rzDegL(celula[i].pres, celula[i].temp);
                } else if (celula[i].acsr.tipo == 15) {
                    celula[i].acsr.radialPoro.flup.razDegD(celula[i].pres, celula[i].temp);
                    celula[i].acsr.radialPoro.flup.rzDegL(celula[i].pres, celula[i].temp);
                    for (int iRP = 0; iRP < celula[i].acsr.radialPoro.ncel; iRP++) {
                        double pres = celula[i].acsr.radialPoro.celula[iRP].Pcamada;
                        double temp = celula[i].acsr.radialPoro.tRes;
                        celula[i].acsr.radialPoro.celula[iRP].flup.razDegD(pres, temp);
                        celula[i].acsr.radialPoro.celula[iRP].flup.rzDegL(pres, temp);
                    }
                } else if (celula[i].acsr.tipo == 16) {
                    celula[i].acsr.poroso2D.dados.flup.razDegD(celula[i].pres, celula[i].temp);
                    celula[i].acsr.poroso2D.dados.flup.rzDegL(celula[i].pres, celula[i].temp);
                    for (int iRP = 0; iRP < celula[i].acsr.poroso2D.dados.transfer.ncel; iRP++) {
                        double pres = celula[i].acsr.poroso2D.dados.transfer.celula[iRP].Pcamada;
                        double temp = celula[i].acsr.poroso2D.dados.transfer.tRes;
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.razDegD(pres, temp);
                        celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.rzDegL(pres, temp);
                    }
                    for (int iRP = 0; iRP < celula[i].acsr.poroso2D.malha.nele; iRP++) {
                        double pres = celula[i].acsr.poroso2D.malha.mlh2d[iRP].cel2D.presC;
                        double temp = celula[i].acsr.poroso2D.malha.mlh2d[iRP].tRes;
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.razDegD(pres, temp);
                        celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.rzDegL(pres, temp);
                    }
                }
            }
        }

        if (celula[0].acsr.tipo == 1) {
            celula[0].flui = celula[0].acsr.injg.FluidoPro;
        } else if (celula[0].acsr.tipo == 2) {
            celula[0].flui = celula[0].acsr.injl.FluidoPro;
        } else if (celula[0].acsr.tipo == 3) {
            celula[0].flui = celula[0].acsr.ipr.FluidoPro;
        } else if (celula[0].acsr.tipo == 10) {
            celula[0].flui = celula[0].acsr.injm.FluidoPro;
            ;
        } else if (celula[0].acsr.tipo == 15) {
            celula[0].flui = celula[0].acsr.radialPoro.flup;
        } else if (celula[0].acsr.tipo == 16) {
            celula[0].flui = celula[0].acsr.poroso2D.dados.flup;
        }

        for (int i = 0; i < ncel; i++) {
            if (celula[i].calor.difus2D == 1) {
                indCelPoisson2D.push_back(i);
                nCelulaPoisson2D++;
            }
        }

        nabreM1 = arq.eventoabre;
        nfechaM1 = arq.eventofecha;
        abreM1 = new double[nabreM1];
        fechaM1 = new double[nfechaM1];
        for (int i = 0; i < nabreM1; i++)
            abreM1[i] = arq.Tevento[i];
        for (int i = 0; i < nfechaM1; i++)
            fechaM1[i] = arq.Teventof[i];

        int ntempGas = 0;
        if (arq.lingas > 0)
            ntempGas = ncelGas;

        if (arq.nperfistransp > 0) {
            ncelperftransp = new int[arq.nperfistransp];
            for (int i = 0; i < arq.nperfistransp; i++) {
                int posiccel = arq.proftransp.posic[i];
                ncelperftransp[i] = celula[posiccel].calor.nglobal;
            }
        }
        if (arq.nperfistransg > 0 && arq.lingas > 0) {
            ncelperftransg = new int[arq.nperfistransg];
            for (int i = 0; i < arq.nperfistransg; i++) {
                int posiccel = arq.proftransg.posic[i];
                ncelperftransg[i] = celulaG[posiccel].calor.nglobal;
            }
        }

        if ((*vg1dSP).chaverede == 0)
            (*vg1dSP).TmaxR = arq.tfinal;
        if (redeTemporario == 0) {
            if (arq.ntendp > 0) {
                TrendLengthP = new int[arq.ntendp];
                for (int i = 0; i < arq.ntendp; i++)
                    TrendLengthP[i] = 1 + 1 + ceil((*vg1dSP).TmaxR / arq.trendp[i].dt); // round(arq.tfinal / arq.trendp[i].dt);
            }
            if (arq.ntendp > 0) {
                resettrend = new double[arq.ntendp];
                ntrend = new int[arq.ntendp];
                ntrendB = new int[arq.ntendp];
                MatTrendP = new double **[arq.ntendp];
                for (int i = 0; i < arq.ntendp; i++) {
                    MatTrendP[i] = new double *[TrendLengthP[i]];
                    for (int j = 0; j < TrendLengthP[i]; j++) {
                        MatTrendP[i][j] = new double[arq.nvartrendp[i] + 2];
                        for (int k = 0; k <= arq.nvartrendp[i]; k++)
                            MatTrendP[i][j][k] = -10000.;
                    }
                    resettrend[i] = 0;
                    ntrend[i] = 0;
                    ntrendB[i] = 0;
                }
            }

            if (arq.ntendg > 0) {
                TrendLengthG = new int[arq.ntendg];
                for (int i = 0; i < arq.ntendg; i++)
                    TrendLengthG[i] = 1 + 1 + ceil((*vg1dSP).TmaxR / arq.trendg[i].dt);
            }
            if (arq.ntendg > 0) {
                resettrendg = new double[arq.ntendg];
                ntrendg = new int[arq.ntendg];
                ntrendgB = new int[arq.ntendg];
                MatTrendG = new double **[arq.ntendg];
                for (int i = 0; i < arq.ntendg; i++) {
                    MatTrendG[i] = new double *[TrendLengthG[i]];
                    for (int j = 0; j < TrendLengthG[i]; j++) {
                        MatTrendG[i][j] = new double[arq.nvartrendg[i] + 2];
                        for (int k = 0; k <= arq.nvartrendg[i]; k++)
                            MatTrendG[i][j][k] = -10000.;
                    }
                    resettrendg[i] = 0;
                    ntrendg[i] = 0;
                    ntrendgB[i] = 0;
                }
            }
            if (arq.ntendtransp > 0) {
                TrendLengthTransP = new int[arq.ntendtransp];
                for (int i = 0; i < arq.ntendtransp; i++)
                    TrendLengthTransP[i] = 1 + 1 + ceil((*vg1dSP).TmaxR / arq.trendtransp[i].dt);
            }
            if (arq.ntendtransp > 0) {
                resettrendtrans = new double[arq.ntendtransp];
                ntrendtrans = new int[arq.ntendtransp];
                ntrendtransB = new int[arq.ntendtransp];
                MatTrendTransP = new double **[arq.ntendtransp];
                for (int i = 0; i < arq.ntendtransp; i++) {
                    MatTrendTransP[i] = new double *[TrendLengthTransP[i]];
                    for (int j = 0; j < TrendLengthTransP[i]; j++)
                        MatTrendTransP[i][j] = new double[2];
                    for (int j = 0; j < TrendLengthTransP[i]; j++)
                        for (int k = 0; k < 2; k++)
                            MatTrendTransP[i][j][k] = -10000.;

                    resettrendtrans[i] = 0;
                    ntrendtrans[i] = 0;
                    ntrendtransB[i] = 0;
                }
            }
            if (arq.ntendtransg > 0 && arq.lingas > 0) {
                TrendLengthTransG = new int[arq.ntendtransg];
                for (int i = 0; i < arq.ntendtransg; i++)
                    TrendLengthTransG[i] = 1 + 1 + ceil((*vg1dSP).TmaxR / arq.trendtransg[i].dt);
            }
            if (arq.ntendtransg > 0) {
                resettrendtransg = new double[arq.ntendtransg];
                ntrendtransg = new int[arq.ntendtransg];
                ntrendtransgB = new int[arq.ntendtransg];
                MatTrendTransG = new double **[arq.ntendtransg];
                for (int i = 0; i < arq.ntendtransg; i++) {
                    MatTrendTransG[i] = new double *[TrendLengthTransG[i]];
                    for (int j = 0; j < TrendLengthTransG[i]; j++)
                        MatTrendTransG[i][j] = new double[2];
                    for (int j = 0; j < TrendLengthTransG[i]; j++)
                        for (int k = 0; k < 2; k++)
                            MatTrendTransG[i][j][k] = -10000.;

                    resettrendtransg[i] = 0;
                    ntrendtransg[i] = 0;
                    ntrendtransgB[i] = 0;
                }
            }
        }

        verificaAcop = 0;

        if (arq.lingas > 0) {
            AnulaColunaIni = arq.anulcoluini();
            AnulaColunaFim = arq.anulcolufim();
            ColunaAnulaIni = arq.coluanulini();
            ColunaAnulaFim = arq.coluanulfim();
            if (ColunaAnulaFim == -1) {
                ColunaAnulaFim = 0;
                AnulaColunaFim--;
            }
            if (AnulaColunaIni >= 0 && AnulaColunaFim >= 0 && ColunaAnulaIni >= 0 && ColunaAnulaFim >= 0)
                verificaAcop = 1;
        }

        verificaAcopRedeP = 0;
        verificaAcopRedeS = 0;
        SecPrimIniRedeP = 0;
        SecPrimFimRedeP = 0;
        PrimSecIniRedeP = 0;
        PrimSecFimRedeP = 0;

        kontaTempoProf = 0;
        kontaTempoProfG = 0;
        if (arq.nperfisp > 0) {
            arq.imprimeProfile(celula, flut, (*vg1dSP).lixo5, indTramo);
            if (arq.profp.tempo[0] <= 0 + (*vg1dSP).localtiny)
                kontaTempoProf++;
        }
        if (arq.nperfisg > 0 && arq.lingas > 0) {
            arq.imprimeProfileG(celulaG, flutG, (*vg1dSP).lixo5, indTramo);
            if (arq.profg.tempo[0] <= 0 + (*vg1dSP).localtiny)
                kontaTempoProfG++;
        }
        kontaTempoTransProf = 0;
        kontaTempoTransProfG = 0;
        if (arq.nperfistransp > 0) {
            arq.imprimeProfileTrans(celula, ncelperftransp, (*vg1dSP).lixo5, indTramo);
            if (arq.proftransp.tempo[0] <= 0 + (*vg1dSP).localtiny)
                kontaTempoTransProf++;
        }
        if (arq.nperfistransg > 0 && arq.lingas > 0) {
            arq.imprimeProfileTransG(celulaG, ncelperftransg, (*vg1dSP).lixo5, indTramo);
            if (arq.proftransg.tempo[0] <= 0 + (*vg1dSP).localtiny)
                kontaTempoTransProfG++;
        }

        saidaLog << pathPrefixoArqSaida << "LogEvento" << ".dat";
        tmpLog = saidaLog.str();
        // if it's not a simulation POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            contaLog = 0;
            int nevent = arq.logevento.size();
            while (fabs(arq.logevento[contaLog].instante - (*vg1dSP).lixo5) < dt && contaLog < nevent) {
                time_t now = time(0);
                tm *ltm = localtime(&now); // Retirado de https://www.tutorialspoint.com/cplusplus/cpp_date_time.htm
                ofstream escreveIni(tmpLog.c_str(), ios_base::app);
                escreveIni << "Evento Externo = ";
                escreveIni << arq.logevento[contaLog].instante << " ; ";
                escreveIni << arq.logevento[contaLog].duracao << " ; ";
                escreveIni << arq.logevento[contaLog].descricao << " ; ";
                escreveIni << "datahora = ";
                escreveIni << ltm->tm_mday << "/";
                escreveIni << 1 + ltm->tm_mon << "/";
                escreveIni << 1900 + ltm->tm_year << " ";
                escreveIni << 0 + ltm->tm_hour << ":";
                escreveIni << 0 + ltm->tm_min << ":";
                escreveIni << 0 + ltm->tm_sec;
                escreveIni << endl;
                contaLog++;
                escreveIni.close();
            }
        }

        menorDx = 1e10;
        for (int i = 0; i <= ncel; i++) {
            if (celula[i].dx < menorDx)
                menorDx = celula[i].dx;
            if (i > 0)
                celula[i].razdxTM = celula[i - 1].dx / (celula[i - 1].dx + celula[i].dx);
            if (i > 1)
                celula[i].razdxTM0 = celula[i - 2].dx / (celula[i - 1].dx + celula[i - 2].dx);
        }

        if (arq.nCelUnit>0) {
        	for(int iCelU=0;iCelU<arq.nCelUnit;iCelU++){
        		kontaTempoCelUni.push_back(0);
        	}
        }

        kSP = 0;
        indevento = 1;
        mult = 0.8;
        presMedMov = 0.;
        jMedMov = 0.;
        tMedMov = 60.;
        ktMedMov = 0.;
        pTotal = 0.;
        jTotal = 0.;
        alfTotal = 0.;
    } catch (exception &excInt) {
        cout << "EXCECAO INESPERADA: " << excInt.what() << endl;
        // incluir falha
        logger.log(LOGGER_FALHA, LOG_ERR_UNEXPECTED_EXCEPTION, "", "", excInt.what());
        // gravar arquivo de log
        logger.writeOutputLog();
        // encerrar a aplicacao
        exit(EXIT_SUCCESS);
    }
}

void SProd::renovaGas() {
    for (int i = 0; i <= ncelGas; i++) {
        if (i != 0 && i != ncelGas) {
            celulaG[i].pres = termolivreG[3 * i];
            celulaG[i].presL = termolivreG[3 * i - 3];
            celulaG[i].presR = termolivreG[3 * i + 3];
            celulaG[i].VGasR = termolivreG[3 * i + 1];
            celulaG[i].VGasL = termolivreG[3 * i - 2];
            celulaG[i].VGasRR = termolivreG[3 * i + 4];
        } else if (i == 0) {
            celulaG[i].pres = termolivreG[3 * i];
            celulaG[i].presL = celulaG[i].pres;
            celulaG[i].presR = termolivreG[3 * i + 3];
            celulaG[i].VGasR = termolivreG[3 * i + 1];
            celulaG[i].VGasRR = termolivreG[3 * i + 4];
            double auxpres = celulaG[i].presR;
        } else {
            celulaG[i].pres = termolivreG[3 * i];
            celulaG[i].presL = termolivreG[3 * i - 3];
            celulaG[i].VGasR = termolivreG[3 * i + 1];
            celulaG[i].VGasL = termolivreG[3 * i - 2];
            celulaG[i].presR = celulaG[i].pres;
        }
    }
}

void SProd::renovaGasBuf() {
    for (int i = 0; i <= ncelGas; i++) {
        if (i != 0 && i != ncelGas) {
            celulaG[i].VGasRBuf = termolivreG[3 * i + 1];
        } else if (i == 0) {
            celulaG[i].VGasRBuf = termolivreG[3 * i + 1];
        } else {
            celulaG[i].VGasRBuf = termolivreG[3 * i + 1];
        }
    }
}

double SProd::areaValvCali(double PCal, double TCal, double PVO, double PT,
                           double dextern, double areagarg, double Rvalv, double Temp) {
    // PCal: Calibration pressure.
    // PVO: Casing pressure.
    // PT: Tubing pressure.
    // Rvalv: Area ratio.
    // Temp: Bottom-hole temperature in Fahrenheit.

    double PB80 = PCal * (1 - Rvalv);
    PB80 = (PB80 + 14.6959488) * (80 + 460.67) / (TCal * 1.8 + 491.67) - 14.6959488;
    double PBT = PB80 * (1 + 0.00215 * (Temp - 80));
    double compara = PVO * (1 - Rvalv) + PT * Rvalv;
    double abertura = 0.;
    if (compara > PBT)
        abertura = 1.;

    double areafol = areagarg / Rvalv;
    double BSR;
    if (dextern * 100. / 2.54 > 1.1)
        BSR = 500.0 * areafol;
    else
        BSR = 1950.0 * areafol;
    double XMVS = ((PVO - PBT) * areafol - (PVO - PT) * areagarg) / BSR;

    // IF THE VALVE IS CLOSED, QG = 0
    if (XMVS <= 0.0)
        abertura = 0.;

    double DP = sqrt(areagarg * 4. / M_PI);
    double RP = DP / 2.0;
    double RB = sqrt(areafol / M_PI);
    double APE = M_PI * RP * XMVS * (XMVS + 2.0 * sqrt(RB * RB - RP * RP));
    APE = APE / sqrt((XMVS + sqrt(RB * RB - RP * RP)) * (XMVS + sqrt(RB * RB - RP * RP)) + RP * RP);
    if (APE > areagarg)
        APE = areagarg;
    abertura = APE / areagarg;

    return abertura;
}

void SProd::calctempGas(int i, double tempantiga, int modoPerm) {

    if (semTermo == 0) {
        double dx = celulaG[i].dx0;
        double dxmed = 0.5 * (celulaG[i].dx0 + celulaG[i - 1].dx0);
        double area = celulaG[i].duto.area;
        double ugsmed;
        if (i < ncelGas)
            ugsmed = celulaG[i].VGasR / celulaG[i].u1L;
        else {
            ugsmed = celulaG[i].VGasL / celulaG[i].u1L;
        }
        double rhog = celulaG[i].rg;
        double cpg = celulaG[i].flui.CalorGas(celulaG[i].presini, celulaG[i].tempini);
        double cvg = celulaG[i].flui.CalorGasVolMod(celulaG[i].presini, celulaG[i].tempini);
        double jtg = celulaG[i].flui.JTG(celulaG[i].presini, celulaG[i].tempini);
        double hidro = (rhog * ugsmed) * area * 9.82 * sin(celulaG[i].duto.teta);

        celulaG[i].calor.Tint = celulaG[i].tempini;
        celulaG[i].calor.dtL = celulaG[i].tempini - celulaG[i - 1].tempini;
        celulaG[i].calor.Vint = ugsmed;
        celulaG[i].calor.dt = celulaG[i].dt;
        celulaG[i].calor.kint = celulaG[i].flui.CondGas(celulaG[i].presini, celulaG[i].tempini);
        celulaG[i].calor.cpint = cpg;
        celulaG[i].calor.rhoint = rhog;
        celulaG[i].calor.viscint = celulaG[i].flui.ViscGas(celulaG[i].presini, celulaG[i].tempini) * 1.e-3;
        double dtemp = celulaG[i].temp * 0.01;
        if (fabs(celulaG[i].temp) < 1e-15)
            dtemp = 0.1;
        double rhogdT = celulaG[i].flui.MasEspGas(celulaG[i].presini, celulaG[i].tempini + dtemp) - rhog;
        celulaG[i].calor.betint = -(1 / celulaG[i].calor.rhoint) * rhogdT / (dtemp);
        if (modoPerm == 0)
            celulaG[i].fluxcal = celulaG[i].calor.transtrans();
        else
            celulaG[i].fluxcal = celulaG[i].calor.transperm();
        if (i >= AnulaColunaIni && i <= AnulaColunaFim && verificaAcop == 1) {
            int kconecte = i - AnulaColunaIni;
            int iconecte = ColunaAnulaIni - kconecte;
            celulaG[i].fluxcal -= celula[iconecte].calor.fluxFim;
        }

        double razdx;
        if (i < ncelGas)
            razdx = dx / (dx + celulaG[i + 1].dx0);
        else
            razdx = dx / (dx + celulaG[i - 1].dx0);
        double coefTempo = rhog * cvg * area;
        double coefPresTempo = celulaG[i].flui.CalorGasPresMod(celulaG[i].presini, celulaG[i].tempini, celulaG[i].rg) *
                               (rhog * area);

        double coefdxT = rhog * ugsmed * cpg * area;
        double coefdxP = rhog * ugsmed * jtg * area;
        double dpdx;
        if (i < ncelGas)
            dpdx = 2. * (((1 - razdx) * celulaG[i + 1].presini + razdx * celulaG[i].presini) - celulaG[i].presini) * 98066.5 / dx;
        else
            dpdx = 2. * (celulaG[i].presini - ((1 - razdx) * celulaG[i - 1].presini + razdx * celulaG[i].presini)) * 98066.5 / dx;
        double dtdx = (celulaG[i].tempini - celulaG[i - 1].tempini) / dxmed;
        if (i < ncelGas)
            if (ugsmed < 0)
                dtdx = (celulaG[i + 1].tempini - celulaG[i].tempini) / dxmed;
        if ((i == 1 && ugsmed <= 0) || (i == ncelGas && ugsmed <= 0))
            dtdx = 0.;

        double cinetico;
        double deljmix = 0.;
        double rhomix = rhog;
        double ugsmed0 = celulaG[i].VGasL / celulaG[i - 1].u1L;
        deljmix = (ugsmed - ugsmed0) / dx;

        cinetico = rhomix * area * ugsmed * ugsmed * deljmix;

        double fontemassG = 0.;
        double fontemassL = 0.;

        double fator = 1.;
        if ((*vg1dSP).lixo5 < 1000.)
            fator = 1.;

        celulaG[i].temp = ((coefTempo / celulaG[i].dt) * celulaG[i].temp - (fator) * (coefPresTempo * (celulaG[i].pres - celulaG[i].presini) * 98066.5 / celulaG[i].dt) + celulaG[i].dTdLCor * (-coefdxT * dtdx + coefdxP * dpdx - cinetico - hidro + fontemassL + fontemassG + celulaG[i].fluxcal)) / (coefTempo / celulaG[i].dt);

        if (celulaG[i].temp < -50.)
            celulaG[i].temp = -50.;
        if (celulaG[i].temp > 200.)
            celulaG[i].temp = 200.;

        if (i > 0)
            celulaG[i - 1].tempR = celulaG[i].temp;
        if (i < ncelGas)
            celulaG[i + 1].tempL = celulaG[i].temp;
    } else {
        celulaG[i].temp = celulaG[i].calor.Textern1;

        if (i > 0)
            celulaG[i - 1].tempR = celulaG[i].temp;
        if (i < ncelGas)
            celulaG[i + 1].tempL = celulaG[i].temp;
    }
}

void SProd::resolveDescarga() {
    int nvalv = arq.nvalvgas;
    for (int i = celInter; i <= ncelGas; i++) {
        double dx0 = 0.5 * celulaG[i].dxL;
        double dx1 = 0.5 * celulaG[i].dx0;
        double RgasR = 1.;
        if (celulaG[i].razInter <= 0.5)
            RgasR = 2 * celulaG[i].razInter;
        double RgasL = 0.;
        if (celulaG[i - 1].razInter >= 0.5)
            RgasL = 2 * (celulaG[i - 1].razInter - 0.5);
        double LGasL = dx0 * RgasL;
        double LGasR = dx1 * RgasR;
        double LLiqL = dx0 - LGasL;
        double LLiqR = dx1 - LGasR;
        double temp = celulaG[i - 1].temp;
        double pres = celulaG[i - 1].pres;
        double rhoL = celulaG[i].MasEspFlu(pres, temp);
        double viscL = celulaG[i].VisFlu(pres, temp);
        double rhoG = celulaG[i].flui.MasEspGas(pres, temp);
        double viscG = celulaG[i].flui.ViscGas(pres, temp);
        double vel1 = celulaG[i].VGasL / (rhoL * celulaG[i - 1].duto.area);
        if (celulaG[i].razInter > (*vg1dSP).localtiny)
            vel1 = celulaG[i].VGasL / (rhoG * celulaG[i - 1].duto.area);
        double vel2 = celulaG[i].VGasL / (rhoL * celulaG[i].duto.area);
        if (celulaG[i].razInter > (*vg1dSP).localtiny)
            vel2 = celulaG[i].VGasL / (rhoG * celulaG[i].duto.area);
        double re1G;
        double re1L;
        double re2G;
        double re2L;
        if (celulaG[i - 1].duto.revest == 0)
            re1L = celulaG[i - 1].Rey(celulaG[i - 1].duto.a, vel1, rhoL, viscL);
        else {
            double dhid = 4 * celulaG[i - 1].duto.area / celulaG[i - 1].duto.peri;
            re1L = celulaG[i - 1].Rey(dhid, vel1, rhoL, viscL);
        }
        if (celulaG[i].duto.revest == 0)
            re2L = celulaG[i].Rey(celulaG[i].duto.a, vel2, rhoL, viscL);
        else {
            double dhid = 4 * celulaG[i].duto.area / celulaG[i].duto.peri;
            re2L = celulaG[i].Rey(dhid, vel2, rhoL, viscL);
        }
        if (celulaG[i - 1].duto.revest == 0)
            re1G = celulaG[i - 1].Rey(celulaG[i - 1].duto.a, vel1, rhoG, viscG);
        else {
            double dhid = 4 * celulaG[i - 1].duto.area / celulaG[i - 1].duto.peri;
            re1G = celulaG[i - 1].Rey(dhid, vel1, rhoG, viscG);
        }
        if (celulaG[i].duto.revest == 0)
            re2G = celulaG[i].Rey(celulaG[i].duto.a, vel2, rhoG, viscG);
        else {
            double dhid = 4 * celulaG[i].duto.area / celulaG[i].duto.peri;
            re2G = celulaG[i].Rey(dhid, vel2, rhoG, viscG);
        }
        double f1L = celulaG[i - 1].fric(re1L, celulaG[i - 1].duto.rug / celulaG[i - 1].duto.a) * LLiqL;
        double f2L = celulaG[i].fric(re2L, celulaG[i].duto.rug / celulaG[i].duto.a) * LLiqR;
        double hidro1L = 1 * (9.82 * sin(celulaG[i - 1].duto.teta) * rhoL) * LLiqL;
        double hidro2L = 1 * (9.82 * sin(celulaG[i].duto.teta) * rhoL) * LLiqR;
        double f1G = celulaG[i - 1].fric(re1G, celulaG[i - 1].duto.rug / celulaG[i - 1].duto.a) * LGasL;
        double f2G = celulaG[i].fric(re2G, celulaG[i].duto.rug / celulaG[i].duto.a) * LGasR;
        double hidro1G = 1 * (9.82 * sin(celulaG[i - 1].duto.teta) * rhoG) * LGasL;
        double hidro2G = 1 * (9.82 * sin(celulaG[i].duto.teta) * rhoG) * LGasR;
        celulaG[i].pres = celulaG[i - 1].pres + (-0.5 * (f1L * rhoL + f1G * rhoG) * vel1 * fabs(vel1) * celulaG[i - 1].duto.peri / celulaG[i - 1].duto.area - 0.5 * (f2L * rhoL + f2G * rhoG) * vel2 * fabs(vel2) * celulaG[i].duto.peri / celulaG[i].duto.area - hidro1L - hidro2L - hidro1G - hidro2G) / 98066.52;
        celulaG[i].presL = celulaG[i - 1].pres;
        celulaG[i - 1].presR = celulaG[i].pres;

        tempDescarga(i);

        celulaG[i].u1L = ((1. - celulaG[i].razInter) * celulaG[i].MasEspFlu(celulaG[i].pres, celulaG[i].temp) + celulaG[i].razInter * celulaG[i].flui.MasEspGas(pres, temp)) * celulaG[i].duto.area;
        celulaG[i - 1].u1R = celulaG[i].u1L;
        celulaG[i].u1LL = celulaG[i - 1].u1L;
    }

    double Qtotal = 0.;
    for (int i = celInter; i <= ncelGas; i++) {
        double temp = celulaG[i].temp;
        double pres = celulaG[i].pres;
        double rhoL = celulaG[i].MasEspFlu(pres, temp);
        double rhoG = celulaG[i].flui.MasEspGas(pres, temp);
        double qfonte = celulaG[i].massfonteCH / rhoL;
        if (celulaG[i].razInter > 0.5)
            qfonte = celulaG[i].massfonteCH / rhoG;
        Qtotal += qfonte;
    }
    double temp = celulaG[celInter].temp;
    double pres = celulaG[celInter].pres;
    double rhoL = celulaG[celInter].MasEspFlu(pres, temp);
    double rhoG = celulaG[celInter].flui.MasEspGas(pres, temp);
    celulaG[celInter].VGasL = Qtotal * rhoG;
    celulaG[celInter - 1].VGasR = celulaG[celInter].VGasL;
    celulaG[celInter - 2].VGasRR = celulaG[celInter].VGasL;
    for (int i = celInter; i <= ncelGas; i++) {
        double temp = celulaG[i].temp;
        double pres = celulaG[i].pres;
        double rhoL = celulaG[i].MasEspFlu(pres, temp);
        double rhoG = celulaG[i].flui.MasEspGas(pres, temp);
        double qfonte = celulaG[i].massfonteCH / rhoL;
        if (celulaG[i].razInter > 0.5)
            qfonte = celulaG[i].massfonteCH / rhoG;
        Qtotal -= qfonte;
        if (i < ncelGas) {
            celulaG[i + 1].VGasL = (Qtotal)*rhoL;
            celulaG[i].VGasR = celulaG[i + 1].VGasL;
        } else
            celulaG[i].VGasR = 0.;
        celulaG[i - 1].VGasRR = celulaG[i].VGasL;
    }

    velInter = celulaG[celInter + 1].VGasL / (celulaG[celInter + 1].MasEspFlu(celulaG[celInter + 1].pres, celulaG[celInter + 1].temp) * celulaG[celInter].duto.area);
}

void SProd::tempDescarga(int i) {
    double dx0 = 0.5 * celulaG[i].dxL;
    double dx1 = 0.5 * celulaG[i].dx0;
    double RgasR = 0.;
    if (celulaG[i].razInter <= 0.5)
        RgasR = 2 * celulaG[i].razInter;
    double RgasL = 0.;
    if (celulaG[i - 1].razInter >= 0.5)
        RgasL = 2 * (celulaG[i - 1].razInter - 0.5);
    double LGasL = dx0 * RgasL;
    double LGasR = dx1 * RgasR;
    double LLiqL = dx0 - LGasL;
    double LLiqR = dx1 - LGasR;
    double LTotal = LLiqL + LLiqR + LGasL + LGasR;
    double dia = celulaG[i - 1].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double pres;
    double temp;

    pres = celulaG[i - 1].pres;
    temp = celulaG[i - 1].temp;
    double rho = ((LLiqL + LLiqR) * celulaG[i].MasEspFlu(pres, temp) + (LGasL + LGasR) * celulaG[i].flui.MasEspGas(pres, temp)) / LTotal;

    double vel1 = celulaG[i].VGasL / (celulaG[i].MasEspFlu(pres, temp) * celulaG[i - 1].duto.area);
    if (celulaG[i].razInter > (*vg1dSP).localtiny)
        vel1 = celulaG[i].VGasL / (celulaG[i].flui.MasEspGas(pres, temp) * celulaG[i - 1].duto.area);
    double cpl = ((LLiqL + LLiqR) * celulaG[i].CalorLiq(pres, temp) + (LGasL + LGasR) * celulaG[i].flui.CalorGas(pres, temp)) / LTotal;

    celulaG[i - 1].calor.Tint = temp;
    celulaG[i - 1].calor.Vint = vel1;
    double condliq = ((LLiqL + LLiqR) * celulaG[i - 1].CondLiq(pres, temp) + (LGasL + LGasR) * celulaG[i].flui.CondGas(pres, temp)) / LTotal;
    celulaG[i - 1].calor.kint = condliq;
    celulaG[i - 1].calor.cpint = cpl;
    celula[i - 1].calor.rhoint = rho;
    double viscliq = (((LLiqL + LLiqR) * celulaG[i].VisFlu(pres, temp) + (LGasL + LGasR) * celulaG[i].flui.ViscGas(pres, temp)) * 1e-3) / LTotal;
    celula[i - 1].calor.viscint = viscliq;

    double fluxcal = celula[i - 1].calor.transtrans();
    if ((i - 1) >= AnulaColunaIni && (i - 1) <= AnulaColunaFim && verificaAcop == 1) {
        int kconecte = (i - 1) - AnulaColunaIni;
        int iconecte = ColunaAnulaIni - kconecte;
        fluxcal -= celula[iconecte].calor.fluxFim;
    }

    celulaG[i - 1].tempR = celulaG[i].temp;
    celulaG[i].tempL = celulaG[i - 1].temp;
}

void SProd::avancInter() {

    celulaG[celInter].razInter = (celulaG[celInter].razInterIni * celulaG[celInter].dx0 + velInter * dt) / celulaG[celInter].dx0;
    if (celInter == (ncelGas - 1) && celulaG[celInter].razInter >= 0.99) {

        double pmed = celulaG[celInter].pres;
        double tmed = celulaG[celInter].temp;
        double A1 = celulaG[celInter].duto.area;
        double rho1 = celulaG[celInter].flui.MasEspGas(pmed, tmed);
        double rhoL = celulaG[celInter].MasEspFlu(pmed, tmed);
        celulaG[celInter].VGasR = celulaG[celInter].VGasR * rho1 / rhoL;
        celulaG[celInter].u1L = A1 * rho1;
        celulaG[celInter - 1].u1R = celulaG[celInter].u1L;
        celulaG[celInter - 1].VGasRR = celulaG[celInter].VGasR;
        celulaG[celInter + 1].u1LL = celulaG[celInter].u1L;
        celulaG[celInter + 1].VGasL = celulaG[celInter].VGasR;

        celulaG[celInter].razInter = 1.0;
        celulaG[celInter + 1].razInter = 1.0;

        celInter++;

        celulaG[celInter].presini = celulaG[celInter].pres;
        pmed = celulaG[celInter].pres;
        tmed = celulaG[celInter].temp;
        A1 = celulaG[celInter].duto.area;
        rho1 = celulaG[celInter].flui.MasEspGas(pmed, tmed);
        rhoL = celulaG[celInter].MasEspFlu(pmed, tmed);
        celulaG[celInter].VGasR = 0 * celulaG[celInter].VGasR * rho1 / rhoL;
        celulaG[celInter].u1L = A1 * rho1;
        celulaG[celInter].u1R = A1 * rho1;
        celulaG[celInter - 1].u1R = celulaG[celInter].u1L;
        celulaG[celInter - 1].VGasRR = celulaG[celInter].VGasR;

        celInter = 1e7;
        arq.descarga = 0;
    }

    if (arq.descarga == 1) {
        if (((celulaG[celInter].razInter <= (*vg1dSP).localtiny) && (celulaG[celInter].razInter >= -(*vg1dSP).localtiny)))
            celulaG[celInter].razInter =
                0;
        else if (celulaG[celInter].razInter < -(*vg1dSP).localtiny) {
            double dtaux;
            dtaux = -celulaG[celInter].razInterIni * celulaG[celInter].dx0 / velInter;
            if (dtaux > (*vg1dSP).localtiny) {
                dtInter = dtaux;
                celulaG[celInter].razInter = fabs(0.);
            } else
                celulaG[celInter].razInter = fabs(0.);
        } else if ((celulaG[celInter].razInter >= (1. - (*vg1dSP).localtiny) && celulaG[celInter].razInter <= (1. + (*vg1dSP).localtiny))) {
            celulaG[celInter].razInter = 1.;
        } else if (celulaG[celInter].razInter > (1. + (*vg1dSP).localtiny)) {
            double dtaux;
            dtaux = (1. - celulaG[celInter].razInterIni) * celulaG[celInter].dx0 / velInter;
            if (dtaux > (*vg1dSP).localtiny) {
                dtInter = dtaux;
                celulaG[celInter].razInter = 1.;
            } else
                celulaG[celInter].razInter = 1.;
        }

        if (fabs(celulaG[celInter].razInter) < (*vg1dSP).localtiny && velInter < -fabs((*vg1dSP).localtiny)) {
            celulaG[celInter].razInter = 0.;
            if (celInter > 0) {
                celInter--;
                celulaG[celInter].razInter = 1.;
            }
        } else if (fabs(celulaG[celInter].razInter - 1.) < (*vg1dSP).localtiny && velInter > (*vg1dSP).localtiny) {
            celulaG[celInter].razInter = 1.;
            if (celInter < ncelGas) {
                celInter++;
                celulaG[celInter].razInter = 0.;
            }
        }
    }
}

double SProd::TempDescGL(int igl) {
    int passo = floor((chokeVGL[igl].presEstag - chokeVGL[igl].presGarg) / 50) + 1;
    double deltaP = -(chokeVGL[igl].presEstag - chokeVGL[igl].presGarg) / passo;
    double PD0 = chokeVGL[igl].presEstag;
    double PD = chokeVGL[igl].presEstag + deltaP;
    double TD0 = chokeVGL[igl].tempEstag;
    double T1;
    for (int i = 0; i < passo; i++) {
        double cpg = chokeVGL[igl].flui.CalorGas(PD0, TD0);
        double DZDT = chokeVGL[igl].flui.DZDT(PD0, TD0);
        double tK = TD0 + 273.23;
        T1 = 1.0 / (1.0 / tK - ((286.998 / chokeVGL[igl].flui.Deng) * DZDT / cpg) * log((PD) / (PD0)));
        PD0 = PD;
        PD = PD - deltaP;
        TD0 = T1 - 273.23;
    }
    if (passo == 0) {
        if ((*vg1dSP).lixo5 > 1000) {
            int para;
            para = 0;
        }
        double cpg = chokeVGL[igl].flui.CalorGas(PD0, TD0);
        double jtg = chokeVGL[igl].flui.JTG(PD0, TD0) / cpg;
        TD0 -= jtg * (chokeVGL[igl].presEstag - chokeVGL[igl].presGarg) * 98066.52;
    }
    return TD0;
}

void SProd::ValvGasTrans() {
    int nvalv = arq.nvalvgas;
    int posicM2 = arq.master2.posic;
    for (int i = 0; i < nvalv; i++) {
        if (posicVGLG[i] < celInter) {
            chokeVGL[i].presEstag = celulaG[posicVGLG[i]].pres;
            chokeVGL[i].presGarg = (celula[posicVGLP[i]].pres - chokeVGL[i].presEstag * chokeVGL[i].frec) / (1. - chokeVGL[i].frec);
            chokeVGL[i].tempEstag = celulaG[posicVGLG[i]].temp;
            if (celula[posicVGLP[i]].pres < celulaG[posicVGLG[i]].pres)
                celulaG[posicVGLG[i]].massfonteCH =
                    chokeVGL[i].massica();
            else
                celulaG[posicVGLG[i]].massfonteCH = 0.;
            if (chokeVGL[i].tipo == 1) {
                double abre = areaValvCali(chokeVGL[i].pcalib * 14.223595, chokeVGL[i].tcalib, (chokeVGL[i].presEstag - 1.033211) * 14.223595,
                                           (chokeVGL[i].presGarg - 1.033211) * 14.223595, chokeVGL[i].dextern, chokeVGL[i].areagarg, chokeVGL[i].areagarg / chokeVGL[i].areafole,
                                           1.8 * chokeVGL[i].tempEstag + 32);
                celulaG[posicVGLG[i]].massfonteCH *= abre;
            }
        }
    }
    for (int i = celInter; i <= ncelGas; i++) {

        celulaG[i].massfonteCH = 0.;
        if (celulaG[i].razInter < 0.5) {
            for (int j = 0; j < nvalv; j++) {
                if (i == posicVGLG[j]) {
                    chokeVGL[j].presEstag = celulaG[posicVGLG[j]].pres;
                    chokeVGL[j].presGarg = (celula[posicVGLP[j]].pres - chokeVGL[j].presEstag * chokeVGL[j].frecliq) / (1. - chokeVGL[j].frecliq);
                    chokeVGL[j].tempEstag = celulaG[posicVGLG[j]].temp;
                    if (celula[posicVGLP[j]].pres < celulaG[posicVGLG[j]].pres)
                        celulaG[posicVGLG[j]].massfonteCH =
                            chokeVGL[j].massica(1, arq.salinDescarga);
                    else
                        celulaG[posicVGLG[j]].massfonteCH = 0.;
                    if (chokeVGL[j].tipo == 1) {
                        double abre = areaValvCali(chokeVGL[j].pcalib * 14.223595, chokeVGL[j].tcalib,
                                                   (chokeVGL[j].presEstag - 1.033211) * 14.223595, (chokeVGL[j].presGarg - 1.033211) * 14.223595, chokeVGL[j].dextern,
                                                   chokeVGL[j].areagarg, chokeVGL[j].areagarg / chokeVGL[j].areafole, 1.8 * chokeVGL[j].tempEstag + 32);
                        celulaG[posicVGLG[j]].massfonteCH *= abre;
                    }
                }
            }
        } else {
            for (int j = 0; j < nvalv; j++) {
                if (i == posicVGLG[j]) {
                    chokeVGL[j].presEstag = celulaG[posicVGLG[j]].pres;
                    chokeVGL[j].presGarg = (celula[posicVGLP[j]].pres - chokeVGL[j].presEstag * chokeVGL[j].frec) / (1. - chokeVGL[j].frec);
                    chokeVGL[j].tempEstag = celulaG[posicVGLG[j]].temp;
                    if (celula[posicVGLP[j]].pres < celulaG[posicVGLG[j]].pres)
                        celulaG[posicVGLG[j]].massfonteCH =
                            chokeVGL[j].massica();
                    else
                        celulaG[posicVGLG[j]].massfonteCH = 0.;
                    if (chokeVGL[j].tipo == 1) {
                        double abre = areaValvCali(chokeVGL[j].pcalib * 14.223595, chokeVGL[j].tcalib,
                                                   (chokeVGL[j].presEstag - 1.033211) * 14.223595, (chokeVGL[j].presGarg - 1.033211) * 14.223595, chokeVGL[j].dextern,
                                                   chokeVGL[j].areagarg, chokeVGL[j].areagarg / chokeVGL[j].areafole, 1.8 * chokeVGL[j].tempEstag + 32);
                        celulaG[posicVGLG[j]].massfonteCH *= abre;
                    }
                }
            }
        }
    }

    double areamenor = celulaG[posicM2].duto.area;
    if (areamenor > celulaG[posicM2].dutoR.area)
        areamenor = celulaG[posicM2].dutoR.area;
    if (celulaG[posicM2].chkcell.areagarg <= 0.01 * areamenor &&
        celulaG[posicM2].chkcell.areagarg >= 1e-5 * areamenor) {
        celulaG[posicM2].chkcell.tempEstag = celulaG[posicM2].temp;
        double rhoM = celulaG[posicM2].flui.MasEspGas(celulaG[posicM2].pres, celulaG[posicM2].temp);
        double rhoJ = celulaG[posicM2 + 1].flui.MasEspGas(celulaG[posicM2 + 1].pres, celulaG[posicM2 + 1].temp);
        double hidroM = -0.5 * rhoM * celulaG[posicM2].dx0 * sin(celulaG[posicM2].duto.teta) / 98066.52;
        double hidroJ = 0.5 * rhoJ * celulaG[posicM2 + 1].dx0 * sin(celulaG[posicM2 + 1].duto.teta) / 98066.52;
        celulaG[posicM2].chkcell.presEstag = celulaG[posicM2].pres + hidroM;
        celulaG[posicM2].chkcell.presGarg = celulaG[posicM2 + 1].pres + hidroJ;
        if (celulaG[posicM2 + 1].pres < celulaG[posicM2].pres)
            celulaG[posicM2].fonteM2 = -celulaG[posicM2].chkcell.massica();
        else {
            celulaG[posicM2].chkcell.presEstag = celulaG[posicM2 + 1].pres;
            celulaG[posicM2].chkcell.presGarg = celulaG[posicM2].pres;
            celulaG[posicM2].chkcell.tempEstag = celulaG[posicM2 + 1].temp;
            celulaG[posicM2].fonteM2 = celulaG[posicM2].chkcell.massica();
        }
        celulaG[posicM2 + 1].fonteM2 = -celulaG[posicM2].fonteM2;
    } else {
        celulaG[posicM2].fonteM2 = 0.;

        celulaG[posicM2 + 1].fonteM2 = 0.;
    }
}

double SProd::prescordesc(double vazmax, int ivalv, double fator, int sinal) {
    int iG = posicVGLG[ivalv];
    int iP = posicVGLP[ivalv];
    double pmed = celula[iP].pres;
    chokeVGL[ivalv].presEstag = celulaG[iG].pres;
    chokeVGL[ivalv].tempEstag = celulaG[iG].temp;
    double rho0 = celulaG[iG].MasEspFlu(chokeVGL[ivalv].presEstag, chokeVGL[ivalv].tempEstag);
    double massica = (fator * arq.vazDescControl - vazmax) * rho0;
    double precorr = 0.;
    precorr = pow(massica / chokeVGL[ivalv].areagarg, 2.) / (2. * rho0 * 98066.52);
    return sinal * precorr;
}

double SProd::CalcPresValvDesc(double vazGarg, int ivalv) {

    double velmax = 0;
    double laz1 = 0.1;
    double laz2 = 0.4;

    double pmed;
    double tmed;
    double massica;
    pmed = pGSup;
    arq.presMaxDesc = 100000.;
    for (int i = ncel; i >= 0; i--) {

        double tmed = celula[i].temp;
        double A1 = celula[i].duto.area;
        double S1 = celula[i].duto.peri;
        double dx1 = celula[i].dx;
        double rhoG = celula[i].flui.MasEspGas(pmed, tmed);
        double rhoP = celula[i].flui.MasEspLiq(pmed, tmed);
        double rhoC = celula[i].fluicol.MasEspFlu(pmed, tmed);
        double viscG = celula[i].flui.ViscGas(pmed, tmed);
        double viscP = celula[i].flui.ViscOleo(pmed, tmed);
        double viscC = celula[i].fluicol.VisFlu(pmed, tmed);
        double alf = celula[i].alf;
        double bet = celula[i].bet;
        double rholiq = bet * rhoC + (1. - bet) * rhoP;
        double viscliq = bet * viscC + (1. - bet) * viscP;
        double rhomix = alf * rhoG + (1. - alf) * rholiq;
        double viscmix = alf * viscG + (1. - alf) * viscliq;
        double vel1 = celula[i].QL / (A1 * rholiq) + celula[i].QG / (A1 * rhoG);
        double re1;
        if (fabs(vel1) > 1e-15) {
            if (celula[i].duto.revest == 0)
                re1 = celula[i].Rey(celula[i].duto.a, vel1, rhomix, viscmix);
            else {
                double dhid = 4 * A1 / S1;
                re1 = celula[i].Rey(dhid, vel1, rhomix, viscmix);
            }
        }
        double f1;
        if (fabs(vel1) > 1e-15)
            f1 = celula[i].fric(re1, celula[i].duto.rug / celula[i].duto.a);
        else
            f1 = 0.;
        double tens1 = f1 * rhomix * vel1 * fabs(vel1) / 2.;
        pmed -= (-9.82 * rhomix * sin(celula[i].duto.teta) - tens1 * S1 / A1) * dx1 / 98066.5;
        if (celula[i].acsr.tipo == 3) {
            double auxpresmax = celula[i].acsr.ipr.Pres - (pmed - pGSup);
            if (auxpresmax < arq.presMaxDesc)
                arq.presMaxDesc = auxpresmax;
        }
    }
    if (pGSup >= arq.presMaxDesc * 0.9999999 && vazGarg > (1 - laz1) * arq.vazDescControl && celulaG[0].VGasR > 0.) {
        double precorr = prescordesc(vazGarg, ivalv, (1 - laz1), -1);
        if (fabs(precorr) > 0.01 * presiniG * celula[0].dt)
            precorr = (fabs(precorr) / precorr) * 0.01 * presiniG * celula[0].dt;
        presiniG += precorr;
        if (presiniG < arq.presMinDescG) {
            presiniG = arq.presMinDescG;
        }

    } else if (vazGarg <= (1 - laz2) * arq.vazDescControl && pGSup <= arq.presMinDesc * 1.0000001) {
        double precorr = prescordesc(vazGarg, ivalv, 1 - laz2, 1);
        if (fabs(precorr) > 0.01 * presiniG * celula[0].dt)
            precorr = (fabs(precorr) / precorr) * 0.01 * presiniG * celula[0].dt;
        presiniG += precorr;
        if (presiniG > arq.presMaxDescG) {
            presiniG = arq.presMaxDescG;
        }
    }

    return velmax;
}

double SProd::BuscaPresInjDesc() {
    double vazmax = 0;
    double vazmaxinst = 0;

    if ((*vg1dSP).lixo5 > 1000) {
        int para;
        para = 0;
    }
    double pGSupAux = pGSup;
    double laz1 = 0.1;
    double laz2 = 0.4;
    if (arq.descarga == 1) {
        double presDescini = celula[ncel].pres;
        if ((*vg1dSP).lixo5 > arq.tempoLatenciaDesc) {
            int nvalv = arq.nvalvgas;
            int ivalv = 0;
            for (int j = 0; j < nvalv; j++) {
                int iG = posicVGLG[j];
                int iP = posicVGLP[j];
                double pmed = celula[iP].pres;
                if (iG > celInter) {
                    double rho1 = celulaG[iG].MasEspFlu(celulaG[iG].pres, celulaG[iG].temp);
                    chokeVGL[j].presEstag = celulaG[iG].pres;
                    chokeVGL[j].presGarg = (pmed - chokeVGL[j].presEstag * chokeVGL[j].frec) /
                                           (1. - chokeVGL[j].frec);
                    chokeVGL[j].tempEstag = celulaG[iG].temp;
                    double massica;
                    if (pmed < celulaG[iG].pres)
                        massica = chokeVGL[j].massica(1, arq.salinDescarga);
                    else
                        massica = 0.;
                    if (chokeVGL[j].tipo == 1) {

                        double abre = areaValvCali(chokeVGL[j].pcalib * 14.223595, chokeVGL[j].tcalib, (chokeVGL[j].presEstag - 1.033211) * 14.223595,
                                                   (chokeVGL[j].presGarg - 1.033211) * 14.223595, chokeVGL[j].dextern, chokeVGL[j].areagarg, chokeVGL[j].areagarg / chokeVGL[j].areafole,
                                                   1.8 * chokeVGL[j].tempEstag + 32);
                        massica *= abre;
                    }
                    double vazmaxAux = massica / (rho1);
                    if (vazmaxAux > vazmax) {
                        vazmax = vazmaxAux;
                        ivalv = j;
                    }
                }
            }
            if (dtDesc.size() > maxVecContDesc || tempmedDEsc > tempMedContDesc) {
                tempmedDEsc -= dtDesc[0];
                dtDesc.erase(dtDesc.begin());
                vazmedDesc -= vazmaxMedDesc[0];
                vazmaxMedDesc.erase(vazmaxMedDesc.begin());
            }

            vazmaxMedDesc.push_back(vazmax * dt);
            vazmedDesc += vazmax * dt;
            dtDesc.push_back(dt);
            tempmedDEsc += dt;

            double pondera = 0.5;
            vazmaxinst = vazmax;
            vazmax = pondera * vazmax + (1. - pondera) * vazmedDesc / tempmedDEsc;
            CalcPresValvDesc(vazmax, ivalv);
            if (posicVGLG[ivalv] >= celInter) {
                if (celula[ncel - 1].MC > 0.0) {
                    if (vazmax > (1. - laz1) * arq.vazDescControl) {

                        double precorr = 0.;
                        precorr = prescordesc(vazmax, ivalv, 1. - laz1, -1);
                        if (fabs(precorr) > 0.01 * pGSup * celula[0].dt)
                            precorr = (fabs(precorr) / precorr) * 0.01 * pGSup * celula[0].dt;
                        pGSup -= precorr;
                        if (pGSup > arq.presMaxDesc)
                            pGSup = arq.presMaxDesc;
                    } else if (vazmax <= (1 - laz2) * arq.vazDescControl) {

                        double precorr = 0.;
                        precorr = prescordesc(vazmax, ivalv, 1. - laz2, 1);
                        if (fabs(precorr) > 0.01 * pGSup * celula[0].dt)
                            precorr = (fabs(precorr) / precorr) * 0.01 * pGSup * celula[0].dt;
                        pGSup -= precorr;
                        if (pGSup < arq.presMinDesc)
                            pGSup = arq.presMinDesc;
                    }
                } else {
                    pGSup *= (1 - 0.01 * dt);
                    if (pGSup < arq.presMinDesc)
                        pGSup = arq.presMinDesc;
                }
            }
        } else {
            if ((*vg1dSP).lixo5 < 0.5 * arq.tempoLatenciaDesc)
                pGSup = presDescini - (presDescini - arq.presMinDesc) * celula[0].dt / (0.5 * arq.tempoLatenciaDesc - (*vg1dSP).lixo5);
            else
                pGSup = arq.presMinDesc;
            presiniG = arq.presIniDescG;
        }
    } else {
        pGSup *= 0.95;
        if (pGSup < arq.presMinDesc)
            pGSup = arq.presMinDesc;
        if (presiniG > arq.presIniDesc) {
            presiniG *= 0.95;
            if (presiniG < arq.presIniDesc)
                presiniG = arq.presIniDesc;
        } else if (presiniG < arq.presIniDesc) {
            presiniG *= 1.05;
            if (presiniG > arq.presIniDesc)
                presiniG = arq.presIniDesc;
        }
    }
    return vazmaxinst;
}

void SProd::subtempoGas() {

    for (int i = 0; i <= ncelGas; i++)
        celulaG[i].DeVoltaParaoFuturo();
    dtInter = dt;
    if (arq.descarga == 1) {
        celInterIni = celInter;
        dtInterIni = dtInter;
        velInterIni = velInter;
        avancInter();
    }
    if (dtInter < dt) {
        dt = dtInter;
        for (int i = 0; i <= ncel; i++) {
            celula[i].dt = dt;
            celula[i].dt2 = dt;
            celula[i].dtPig = dt;
        }
    }

    for (int i = 0; i <= ncelGas; i++) {
        celulaG[i].dt = dt;
    }

    int celLimi;
    if (celInter <= ncelGas)
        celLimi = celInter - 1;
    else
        celLimi = ncelGas + 1;
    for (int i = 0; i < ncelGas + 1; i++)
        celulaG[i].dTdt = 0;

    int ciclomax = arq.cicloAcopTerm;
    for (int ciclo = 0; ciclo <= ciclomax; ciclo++) {
        double abertoChk = 1.;
        if (celulaG[0].tipoCC == 0) {
            abertoChk = chokeInj.areagarg / celulaG[0].duto.area;
            if (abertoChk < 0.2) {
                chokeInj.presEstag = presiniG;
                chokeInj.tempEstag = tempiniG;
                chokeInj.presGarg = celulaG[0].pres;
                celulaG[0].massfonteCH = chokeInj.massica();
            }
        }
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
        for (int i = 0; i <= ncelGas; i++) {

            celulaG[i].GeraLocal(ncelGas, presiniG, tempiniG, abertoChk);
            for (int j = 0; j < 9; j++) {
                matglobG[3 * i][j - 3] = celulaG[i].local[0][j];
                matglobG[3 * i + 1][j - 4] = celulaG[i].local[1][j];
                matglobG[3 * i + 2][j - 5] = celulaG[i].local[2][j];
            }
            termolivreG[3 * i] = celulaG[i].TL[0];
            termolivreG[3 * i + 1] = celulaG[i].TL[1];
            termolivreG[3 * i + 2] = celulaG[i].TL[2];
        }
        matglobG.GaussElimPP(termolivreG);
        renovaGas();

        double verifica = celulaG[0].pres;
        celulaG[0].temp = tempiniG;
        celulaG[0].dTdt = (celulaG[0].temp - celulaG[0].tempini) / dt;
        if (celulaG[ncelGas].VGasR < 0.)
            celulaG[ncelGas].temp = 20.;
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
        for (int i = 1; i < celLimi; i++) {
            calctempGas(i, celulaG[i - 1].tempini);
            celulaG[i].dTdt = (celulaG[i].temp - celulaG[i].tempini) / dt;
        }
        if (ciclo < ciclomax)
            for (int k = 0; k <= ncelGas; k++)
                celulaG[k].FeiticoDoTempo();
    }

#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = 0; i < celLimi; i++)
        celulaG[i].rg = celulaG[i].flui.MasEspGas(celulaG[i].pres, celulaG[i].temp);

    for (int i = 0; i < celLimi; i++) {
        if (i > 0)
            celulaG[i - 1].rgR = celulaG[i].rg;
        celulaG[i].u1L = celulaG[i].rg * celulaG[i].duto.area;
        if (i == 0)
            celulaG[i].u1LL = celulaG[i].u1L;
        else
            celulaG[i].u1LL = celulaG[i - 1].u1L;
        if (i > 0)
            celulaG[i - 1].u1R = celulaG[i].u1L;
        if (i == ncelGas) {
            celulaG[i].u1R = celulaG[i].u1L;
            celulaG[ncelGas].rgR = celulaG[ncelGas].rg;
        }
    }

    for (int i = 0; i <= ncelGas; i++)
        celulaG[i].presini = celulaG[i].pres;

    if (arq.descarga == 1)
        resolveDescarga();
}

void SProd::subtempoGasBuf() {

    for (int i = 0; i < ncelGas + 1; i++)
        celulaG[i].dTdt = 0;
    double abertoChk = 1.;
    if (celulaG[0].tipoCC == 0) {
        abertoChk = chokeInj.areagarg / celulaG[0].duto.area;
        if (abertoChk < 0.2) {
            chokeInj.presEstag = presiniG;
            chokeInj.tempEstag = tempiniG;
            chokeInj.presGarg = celulaG[0].pres;
            celulaG[0].massfonteCH = chokeInj.massica();
        }
    }
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = 0; i <= ncelGas; i++) {

        celulaG[i].GeraLocal(ncelGas, presiniG, tempiniG, abertoChk);
        for (int j = 0; j < 9; j++) {
            matglobG[3 * i][j - 3] = celulaG[i].local[0][j];
            matglobG[3 * i + 1][j - 4] = celulaG[i].local[1][j];
            matglobG[3 * i + 2][j - 5] = celulaG[i].local[2][j];
        }
        termolivreG[3 * i] = celulaG[i].TL[0];
        termolivreG[3 * i + 1] = celulaG[i].TL[1];
        termolivreG[3 * i + 2] = celulaG[i].TL[2];
    }
    matglobG.GaussElimPP(termolivreG);
    renovaGasBuf();
}

void SProd::conectaColuna() {
    for (int i = ColunaAnulaIni; i >= ColunaAnulaFim; i--) {
        int j = ColunaAnulaIni + AnulaColunaIni - i;
        celula[i].calor.Textern2 = celulaG[j].calor.Tcamada[0][0];
        celula[i].calor.betext = celulaG[j].calor.betint;
        int icam = celula[i].calor.geom.ncamadas - 1;
        int idisc = celula[i].calor.ncamada[icam] - 1;
        celulaG[j].calor.Tint2 = celula[i].calor.Tcamada[icam][idisc];
        celula[i].calor.colunaDia = celulaG[j].duto.dia;
        celula[i].calor.geom.b = celulaG[j].calor.geom.a;

        celula[i].calor.Textern1 = celulaG[j].temp;
        celula[i].calor.Vextern1 = celulaG[j].VGasR / celulaG[j].u1L;
        if (j < celInter) {
            double cpg = celulaG[j].flui.CalorGas(celulaG[j].pres, celulaG[j].temp);
            double rhog = celulaG[j].flui.MasEspGas(celulaG[j].pres, celulaG[j].temp);
            celula[i].calor.kextern1 = celulaG[j].flui.CondGas(celulaG[j].pres, celulaG[j].temp);
            celula[i].calor.cpextern1 = cpg;
            celula[i].calor.rhoextern1 = rhog;
            celula[i].calor.viscextern1 = celulaG[j].flui.ViscGas(celulaG[j].pres, celulaG[j].temp) * 1.e-3;
        } else {
            double cpg = celulaG[j].CalorLiq(celulaG[j].pres, celulaG[j].temp);
            double rhog = celulaG[j].MasEspFlu(celulaG[j].pres, celulaG[j].temp);
            celula[i].calor.kextern1 = celulaG[j].CondLiq(celulaG[j].pres, celulaG[j].temp);
            celula[i].calor.cpextern1 = cpg;
            celula[i].calor.rhoextern1 = rhog;
            celula[i].calor.viscextern1 = celulaG[j].VisFlu(celulaG[j].pres, celulaG[j].temp) * 1.e-3;
        }
    }
}

double SProd::interpolaHLatente(double pres, double temp) {
    int ndiv = arq.tabent.npont - 1;
    int ipres = 0.;
    int itemp = 0.;
    int ipmarcador;
    int itmarcador;
    double latt;
    if (pres < HLat[1][0] || pres >= HLat[ndiv + 1][0] || temp < HLat[0][1] || temp >= HLat[0][ndiv + 1])
        latt = 0.;

    else {
        int e, m, d;
        e = 1;
        d = ndiv + 1;
        while (e <= d) {
            m = (e + d) / 2;
            ipmarcador = m;
            if (m == 1) {
                ipres = m;
                break;
            } else if (m == ndiv + 1 && HLat[m][0] == pres) {
                ipres = m - 1;
                break;
            }
            if (HLat[m][0] > pres && HLat[m - 1][0] <= pres) {
                ipres = m - 1;
                break;
            }
            if (HLat[m][0] < pres)
                e = m + 1;
            else
                d = m - 1;
        }
        e = 1;
        d = ndiv + 1;
        while (e <= d) {
            m = (e + d) / 2;
            itmarcador = m;
            if (m == 1) {
                itemp = m;
                break;
            } else if (m == ndiv + 1 && HLat[0][m] == temp) {
                itemp = m - 1;
                break;
            }
            if (HLat[0][m] > temp && HLat[0][m - 1] <= temp) {
                itemp = m - 1;
                break;
            }
            if (HLat[0][m - 1] < temp)
                e = m + 1;
            else
                d = m - 1;
        }
        double razpres = (HLat[ipres][0] - pres) / (HLat[ipres][0] - HLat[ipres + 1][0]);
        double raztemp = (HLat[0][itemp] - temp) / (HLat[0][itemp] - HLat[0][itemp + 1]);
        double latp1 = (1 - razpres) * (HLat[ipres][itemp]) + razpres * (HLat[ipres + 1][itemp]);
        double latp2 = (1 - razpres) * (HLat[ipres][itemp + 1]) + razpres * (HLat[ipres + 1][itemp + 1]);
        latt = (1 - raztemp) * latp1 + raztemp * latp2;
    }
    return latt;
}

void SProd::calctemp(int i, double tempantiga, int modoPerm) {
    if (semTermo == 0) {
        double dx = celula[i].dx;
        double dxmed;
        if (i > 0)
            dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
        else
            dxmed = 0.5 * celula[i].dx;
        double dia = celula[i].duto.a;
        double area = 0.25 * M_PI * dia * dia;
        double alfmed = celula[i].alf;
        double betmed = celula[i].bet;
        double ugsmed;
        double ulsmed;
        if (i > 0 && (celula[i - 1].acsr.tipo != 5 ||
                      celula[i - 1].acsr.chk.AreaGarg > (1e-3 + arq.master1.razareaativ) * celula[i - 1].duto.area)) {
            if (celula[i].alf > (*vg1dSP).localtiny)
                ugsmed = celula[i].QG / area;
            else {
                ugsmed = 0.;
            }
            if (celula[i].alf < 1. - (*vg1dSP).localtiny)
                ulsmed = celula[i].QL / area;
            else {
                ulsmed = 0.;
            }
        } else {
            if (celula[i].alf > (*vg1dSP).localtiny)
                ugsmed = celula[i + 1].QG / area;
            else {
                ugsmed = 0.;
            }
            if (celula[i].alf < 1. - (*vg1dSP).localtiny)
                ulsmed = celula[i + 1].QL / area;
            else {
                ulsmed = 0.;
            }
        }
        double JmixRef = fabs(ugsmed) + fabs(ulsmed);
        double rp = celula[i].rpC;
        double rc = celula[i].rcC;
        double rhol = (1. - betmed) * rp + betmed * rc;
        double rhog = celula[i].rgC;
        double cpl = (1. - betmed) * celula[i].flui.CalorLiq(celula[i].presini, celula[i].temp) + betmed * celula[i].fluicol.CalorLiq(celula[i].presini, celula[i].temp);
        double cvl = cpl;
        double cpg = celula[i].flui.CalorGas(celula[i].presini, celula[i].temp);
        double cvg = celula[i].flui.CalorGasVolMod(celula[i].presini, celula[i].temp, celula[i].rgC);
        double jtl = (1. - betmed) * celula[i].flui.JTL(celula[i].presini, celula[i].temp) - betmed / rc;
        double jtg = celula[i].flui.JTG(celula[i].presini, celula[i].temp, celula[i].rgC);
        double hidro = (rhol * ulsmed + rhog * ugsmed) * area * 9.82 * sin(celula[i].duto.teta);

        double fluxcal = 0.;
        celula[i].calor.Tint = celula[i].temp;
        if (i > 0)
            celula[i].calor.dtL = celula[i].temp - celula[i - 1].tempini;
        else
            celula[i].calor.dtL = 0;
        celula[i].calor.Vint = ugsmed + ulsmed;
        celula[i].calor.dt = celula[i].dt;
        double condliq = (1. - betmed) * celula[i].flui.CondLiq(celula[i].presini, celula[i].temp) + betmed * celula[i].fluicol.CondLiq(celula[i].presini, celula[i].temp);
        celula[i].calor.kint = condliq * (1 - alfmed) + celula[i].flui.CondGas(celula[i].presini, celula[i].temp) * alfmed;
        celula[i].calor.cpint = cpl * (1 - alfmed) + cpg * alfmed;
        celula[i].calor.rhoint = rhol * (1 - alfmed) + rhog * alfmed;
        double viscliq = (1. - betmed) * celula[i].mipC + betmed * celula[i].micC;
        celula[i].calor.viscint = viscliq * (1 - alfmed) * 1.e-3 + celula[i].migC * alfmed * 1.e-3;
        double dtemp = celula[i].temp * 0.01;
        if (fabs(celula[i].temp) < 1e-15)
            dtemp = 0.1;
        double rholdT = (1. - betmed) * celula[i].flui.MasEspLiq(celula[i].presini, celula[i].temp + dtemp) +
                        betmed * celula[i].fluicol.MasEspFlu(celula[i].presini, celula[i].temp + dtemp) - rhol;
        double rhogdT = celula[i].flui.MasEspGas(celula[i].presini, celula[i].temp + dtemp) - rhog;
        celula[i].calor.betint = -(1 / celula[i].calor.rhoint) * (rholdT * (1 - alfmed) + rhogdT * alfmed) / (dtemp);
        if (arq.modoDifus3D == 0 || modoPerm != 0) {
            if (modoPerm == 0)
                fluxcal = celula[i].calor.transtrans();
            else
                fluxcal = celula[i].calor.transperm();
            if (verificaAcopRedeS == 1 && i >= SecPrimIniRedeP && i <= SecPrimFimRedeP) {
                fluxcal -= celula[i].fluxcalAcopRedeP;
            }
        } else if (arq.modoDifus3D == 1) {
            int acoplado = -1;
            int icelAcop;
            for (int iacop = 0; iacop < arq.nacop; iacop++) {
                icelAcop = arq.celAcop[iacop].indCel;
                if (i == icelAcop) {
                    acoplado = iacop;
                    break;
                }
            }
            if (acoplado == -1)
                fluxcal = celula[i].calor.transtrans();
            else {
                int iacop1 = acertaIndAcop[acoplado];
                fluxcal = -arq.celAcop[acoplado].FE * poisson3D.dados.qTotal[iacop1] / celula[i].dx;
            }
        }

        celula[i].fluxcalmed = fluxcal;

        double coefTempo = (rhol * (1 - alfmed) * cvl + rhog * alfmed * cvg) * area;
        double coefPresTempo;
        coefPresTempo = -celula[i].flui.CalorGasPresMod(celula[i].presini, celula[i].temp) * (rhog * alfmed * area); //-celula[i].flui.CalorGasPresMod(celula[i].pres, celula[i].temp) * (rhog * alfmed * area);
        double coefdxT = (rhol * ulsmed * cpl + rhog * ugsmed * cpg) * area;
        double coefdxP = (rhol * ulsmed * jtl + rhog * ugsmed * jtg) * area;
        double dpdx;
        if (i < ncel - 1)
            dpdx = 2. * (celula[i + 1].presaux - celula[i].pres) * 98066.5 / celula[i].dx;
        else if (i == ncel - 1 && chokeSup.AreaGarg > 0.5 * celula[ncel - 1].duto.area)
            dpdx = 2. * (celula[i + 1].presaux - celula[i].pres) * 98066.5 / celula[i].dx;
        else
            dpdx = 2. * (celula[i].pres - celula[i].presaux) * 98066.5 / celula[i].dx;
        if (i > 0 && celula[i - 1].acsr.tipo == 5 &&
            celula[i - 1].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i].duto.area)
            dpdx = 2. * (celula[i + 1].presaux - celula[i].pres) * 98066.5 / celula[i].dx;
        else if (i > 0 && celula[i].acsr.tipo == 5 &&
                 celula[i].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i].duto.area) {
            dpdx = (celula[i].pres - celula[i - 1].pres) * 98066.5 / dxmed;
        } else if (i == 0)
            dpdx = 2. * (celula[i + 1].presaux - celula[i].pres) * 98066.5 / celula[i].dx;

        celula[i].VTemper = coefdxT / coefTempo;
        if ((i == 0 && celula[i].VTemper < 0.) || (((i < ncel || celula[i].VTemper >= 0.) && i > 0) ||
                                                   (i == ncel && masChkSup == 1) || (i == ncel && arq.chkv == 1))) {
            double dtdx = 0.;
            if (i > 0)
                dtdx = (celula[i].temp - celula[i - 1].tempini) / dxmed;

            if (i < ncel)
                if (celula[i].VTemper < 0)
                    dtdx = (celula[i + 1].tempini - celula[i].temp) / (0.5 * (celula[i + 1].dx + celula[i].dx));
            if (celula[i].acsr.tipo == 5 && celula[i].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i].duto.area && celula[i].VTemper <= 0)
                dtdx = 0.;
            if (i > 0 && celula[i - 1].acsr.tipo == 5 &&
                celula[i - 1].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i - 1].duto.area && celula[i].VTemper >= 0)
                dtdx = 0 * (celula[i + 1].tempini - celula[i].temp) / (0.5 * (celula[i + 1].dx + celula[i].dx));
            if ((i <= 1 && celula[i].VTemper <= 0) || (i == ncel && celula[i].VTemper <= 0))
                dtdx = 0.;
            if (celula[i].acsr.tipo == 8 && celula[i].acsr.bvol.freq > 1.) {
                dpdx = (celula[i].pres - celula[i - 1].pres) * 98066.5 / dxmed;
            }

            double cinetico = 0;
            double ugmed0 = 0;
            double ulmed0 = 0;
            double ugmed = 0;
            double ulmed = 0;
            double ugmedini = 0;
            double ulmedini = 0;
            if (i <= ncel - 1 && celula[i].acsr.tipo == 0 && celula[i + 1].acsr.tipo == 0) {

                double dxCin = celula[i].dx;
                double diaaux = celula[i].duto.a;
                double areaaux = 0.25 * M_PI * diaaux * diaaux;

                double rpcin = celula[i + 1].rpCi;
                double rccin = celula[i + 1].rcCi;
                double rholcin = (1. - betmed) * rpcin + betmed * rccin;
                double rhogcin = celula[i + 1].rgCi;

                double alf;
                double alfini;

                ugsmed = celula[i + 1].QG / area;
                ulsmed = celula[i + 1].QL / area;

                if (celula[i + 1].QG > 0)
                    alf = celula[i].alf;
                else
                    alf = celula[i + 1].alf;

                if (celula[i + 1].QGini > 0)
                    alfini = celula[i].alfini;
                else
                    alfini = celula[i + 1].alfini;

                double alf0;
                if (celula[i].QG > 0)
                    alf0 = celula[i - 1].alf;
                else
                    alf0 = celula[i].alf;

                if (alf0 > 1e-3) {
                    ugmed0 = celula[i].QG / (areaaux);
                    ugmed0 /= alf0;
                }

                if (alf0 < 1. - 1e-3) {
                    ulmed0 = celula[i].QL / (areaaux);
                    ulmed0 /= (1. - alf0);
                }

                if (alf > 1e-3) {
                    ugmed = ugsmed;
                    ugmed /= alf;
                }

                if (alf < 1. - 1e-3) {
                    ulmed = ulsmed;
                    ulmed /= (1. - alf);
                }

                if (alfini > 1e-3) {

                    ugmedini = celula[i + 1].QGini / (areaaux);
                    ugmedini /= alfini;
                }

                if (alfini < 1. - 1e-3) {

                    ulmedini = celula[i + 1].QLini / (areaaux);
                    ulmedini /= (1. - alfini);
                }

                cinetico = rholcin * (1 - alf) * areaaux * (0.5 * (ulmed * ulmed - ulmedini * ulmedini)) / celula[i].dt +
                           rhogcin * alf * areaaux * (0.5 * (ugmed * ugmed - ugmedini * ugmedini)) / celula[i].dt +
                           ((celula[i + 1].MC - celula[i + 1].Mliqini) * ugmed * (ugmed - ugmed0) / dxCin +
                            celula[i + 1].Mliqini * ulmed * (ulmed - ulmed0) / dxCin);
            }

            double fontemassG = 0.;
            double fontemassL = 0.;
            double fontemassC = 0.;
            double tfonte = celula[i].temp;
            double cpgF;
            double razcpF = 0.;
            double cplF;
            if (celula[i].acsr.tipo == 1) {
                tfonte = celula[i].acsr.injg.temp;
                cpgF = celula[i].acsr.injg.FluidoPro.CalorGas(celula[i].presini, celula[i].temp);
                razcpF = celula[i].acsr.injg.FluidoPro.ConstAdG(celula[i].presini, celula[i].temp);
                cplF = 0.;
            } else if (celula[i].acsr.tipo == 2) {
                tfonte = celula[i].acsr.injl.temp;
                cpgF = celula[i].acsr.injl.FluidoPro.CalorGas(celula[i].presini, celula[i].temp);                                                                                                                                        // celula[i].acsr.injl.FluidoPro.CalorGas(celula[i].pres, celula[i].temp);
                razcpF = celula[i].acsr.injl.FluidoPro.CalorGas(celula[i].presini, celula[i].temp);                                                                                                                                      // celula[i].acsr.injl.FluidoPro.ConstAdG(celula[i].pres, celula[i].temp);
                cplF = (1. - celula[i].acsr.injl.bet) * celula[i].acsr.injl.FluidoPro.CalorLiq(celula[i].presini, celula[i].temp) + celula[i].acsr.injl.bet * celula[i].acsr.injl.fluidocol.CalorLiq(celula[i].presini, celula[i].temp); //(1. - celula[i].acsr.injl.bet) * celula[i].acsr.injl.FluidoPro.CalorLiq(celula[i].pres, celula[i].temp)
            } else if (celula[i].acsr.tipo == 10) {
                tfonte = celula[i].acsr.injm.temp;
                cpgF = celula[i].acsr.injm.FluidoPro.CalorGas(celula[i].presini, celula[i].temp);
                razcpF = celula[i].acsr.injm.FluidoPro.ConstAdG(celula[i].presini, celula[i].temp);
                if ((celula[i].fontemassLR + celula[i].fontemassCR) > 0.) {
                    double titbet = celula[i].fontemassCR / (celula[i].fontemassLR + celula[i].fontemassCR);
                    cplF = (1. - titbet) * celula[i].acsr.injl.FluidoPro.CalorLiq(celula[i].presini, celula[i].temp) +
                           titbet * celula[i].acsr.injl.fluidocol.CalorLiq(celula[i].presini, celula[i].temp);
                } else
                    cplF = 0.;
            } else if (celula[i].acsr.tipo == 3) {
                tfonte = celula[i].acsr.ipr.Tres;
                cpgF = celula[i].acsr.ipr.FluidoPro.CalorGas(celula[i].presini, celula[i].temp);
                razcpF = celula[i].acsr.ipr.FluidoPro.ConstAdG(celula[i].presini, celula[i].temp);
                cplF = celula[i].acsr.ipr.FluidoPro.CalorLiq(celula[i].presini, celula[i].temp);
            } else if (celula[i].acsr.tipo == 15) {
                tfonte = celula[i].acsr.radialPoro.tRes;
                cpgF = celula[i].acsr.radialPoro.flup.CalorGas(celula[i].presini, celula[i].temp);
                razcpF = celula[i].acsr.radialPoro.flup.ConstAdG(celula[i].presini, celula[i].temp);
                cplF = celula[i].acsr.radialPoro.flup.CalorLiq(celula[i].presini, celula[i].temp);
            } else if (celula[i].acsr.tipo == 9) {
                double betM = celula[i].acsr.fontechk.betISamb;
                double pamb = celula[i].acsr.fontechk.pamb;
                double tamb = celula[i].acsr.fontechk.tamb;
                cpgF = celula[i].acsr.fontechk.fluidoPamb.CalorGas(pamb, tamb);
                cplF = (1. - betM) * celula[i].acsr.fontechk.fluidoPamb.CalorLiq(pamb, tamb) + betM * celula[i].acsr.fontechk.fluidocol.CalorLiq(pamb, tamb);
                razcpF = celula[i].acsr.fontechk.fluidoPamb.ConstAdG(pamb, tamb);
                tfonte = celula[i].acsr.fontechk.tamb;
            } else if (celula[i].acsrL != 0 && i < ncel) {
                if ((*celula[i].acsrL).tipo == 5 && (celula[i].fontemassLR + celula[i].fontemassCR + celula[i].fontemassGR) > 0.) {
                    if ((*celula[i].acsrL).chk.AreaGarg < arq.master1.razareaativ * celula[i].dutoL.area && (*celula[i].acsrL).chk.AreaGarg > 1e-5 * celula[i].dutoL.area) {
                        double tE = celula[i - 1].tempini;
                        double alfE = celula[i - 1].alf;
                        double betE = celula[i - 1].bet;

                        double rholp = celula[i - 1].rpC;
                        double rholc = celula[i - 1].rcC;
                        double rholmix = (1 - betE) * rholp + betE * rholc;

                        double alfJ = celula[i].alf;
                        double betJ = celula[i].bet;
                        double rholpJ = celula[i].rpC;
                        double rholcJ = celula[i].rcC;
                        double rholmixJ = (1 - betJ) * rholpJ + betJ * rholcJ;

                        double hidroM = sin(celula[i - 1].duto.teta) * (0.5 * celula[i - 1].dx) * (rholmix * (1 - alfE) + alfE * celula[i - 1].rgC) * 9.82 / 98600.;
                        double hidroJ = sin(celula[i].duto.teta) * (0.5 * celula[i].dx) * (rholmixJ * (1 - alfJ) + alfJ * celula[i].rgC) * 9.82 / 98600.;
                        double tit = alfE * celula[i - 1].rgC / (celula[i - 1].rgC * alfE + rholmix * (1. - alfE));
                        cpgF = celula[i - 1].flui.CalorGas(celula[i - 1].presini, celula[i - 1].tempini);                                                                                                                              // celula[i - 1].flui.CalorGas(celula[i - 1].pres, celula[i - 1].temp);
                        cplF = (1 - celula[i - 1].bet) * celula[i - 1].flui.CalorLiq(celula[i - 1].presini, celula[i - 1].tempini) + celula[i - 1].bet * celula[i - 1].fluicol.CalorLiq(celula[i - 1].presini, celula[i - 1].tempini); //(1 - celula[i - 1].bet) * celula[i - 1].flui.CalorLiq(celula[i - 1].pres, celula[i - 1].temp)
                        double jtlM = (1. - betE) * celula[i - 1].flui.JTL(celula[i - 1].presini - hidroM, celula[i - 1].tempini) - betE / rholcJ;                                                                                     //(1. - betE) * celula[i - 1].flui.JTL(celula[i - 1].pres - hidroM, celula[i - 1].temp)
                        double jtgM = celula[i - 1].flui.JTG(celula[i - 1].presini - hidroM, celula[i - 1].tempini);                                                                                                                   // celula[i - 1].flui.JTG(celula[i - 1].pres - hidroM, celula[i - 1].temp);
                        tfonte = tE + ((1. - tit) * jtlM / cplF + tit * jtgM / cpgF) *
                                          (celula[i].pres + hidroJ - celula[i - 1].pres - hidroM) * 98066.52;

                        razcpF = celula[i - 1].flui.ConstAdG(celula[i - 1].presini, celula[i - 1].tempini);

                    } else {
                        cpgF = celula[i - 1].flui.CalorGas(celula[i - 1].presini, celula[i - 1].tempini);
                        cplF = (1 - celula[i - 1].bet) * celula[i - 1].flui.CalorLiq(celula[i - 1].presini, celula[i - 1].tempini) + celula[i - 1].bet * celula[i - 1].fluicol.CalorLiq(celula[i - 1].presini, celula[i - 1].tempini);
                        tfonte = celula[i].temp;
                        razcpF = celula[i - 1].flui.ConstAdG(celula[i - 1].presini, celula[i - 1].tempini);
                    }
                } else if ((*celula[i].acsrL).tipo == 8) {
                    double betM = celula[i - 1].bet;
                    cpgF = celula[i - 1].flui.CalorGas(celula[i - 1].presini, celula[i - 1].tempini);
                    cplF = (1. - betM) * celula[i - 1].flui.CalorLiq(celula[i - 1].presini, celula[i - 1].tempini) + betM * celula[i - 1].fluicol.CalorLiq(celula[i - 1].presini, celula[i - 1].tempini);
                    razcpF = celula[i - 1].flui.ConstAdG(celula[i - 1].presini, celula[i - 1].tempini);
                    double n = (*celula[i].acsrL).bvol.npoli;
                    double ypres = (celula[i].pres) / (celula[i - 1].pres);
                    tfonte = celula[i - 1].tempini * pow(ypres, (n - 1) / n);
                } else {
                    cpgF = 0.;
                    razcpF = 1.;
                    cplF = 0.;
                }
            } else if (i == ncel && (celula[i].fontemassLR + celula[i].fontemassCR + celula[i].fontemassGR) > 0.) {
                if ((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)
                    tfonte = celula[i].calor.Textern1;
                else
                    tfonte = tGSup;
                cpgF = celula[i].flui.CalorGas(celula[i].presini, tfonte);
                double betM = 0.;
                if (fabs(celula[i].fontemassCR) > 1e-15)
                    betM = celula[i].fontemassCR /
                           (celula[i].fontemassCR + celula[i].fontemassLR);
                cplF = (1. - betM) * celula[i].flui.CalorLiq(celula[i].presini, tfonte) + betM * celula[i].fluicol.CalorLiq(celula[i].presini, tfonte);
                razcpF = celula[i].flui.ConstAdG(celula[i].pres, tfonte);
            } else {
                cpgF = 0.;
                razcpF = 1.;
                cplF = 0.;
            }

            fontemassL = 0;
            if (celula[i].fontemassLR > 0.)
                fontemassL = celula[i].fontemassLR / dx;
            if (celula[i].fontemassCR > 0.)
                fontemassL += celula[i].fontemassCR / dx;
            fontemassL *= (cplF) * (tfonte - celula[i].temp);

            fontemassG = celula[i].fontemassGR / dx;
            if (fontemassG > 0.)
                fontemassG *= (cpgF / razcpF) * (tfonte - celula[i].temp);
            else
                fontemassG = 0;

            double latente;
            double valTransMass = fabs(celula[i].FonteMudaFase);
            double sigTransMass = 1.;
            if (valTransMass > 1e-25)
                sigTransMass = celula[i].FonteMudaFase / valTransMass;
            if (arq.limTransMass < valTransMass)
                valTransMass = sigTransMass * arq.limTransMass;
            else
                valTransMass *= sigTransMass;
            if (CalcLat > 0 && arq.flashCompleto == 0) {
                latente = interpolaHLatente(celula[i].presini, celula[i].temp) * valTransMass;
            } else if ((arq.flashCompleto == 1 || arq.flashCompleto == 2) && CalcLat > 0)
                latente = (celula[i].flui.EntalpGas(celula[i].presini, celula[i].temp) -
                           celula[i].flui.EntalpLiq(celula[i].presini, celula[i].temp)) * valTransMass;
            else
                latente = 0;

            if (arq.latente == 0)
                latente = 0.;
            else if (arq.condlatente == 0 && latente < 0)
                latente = 0.;

            double alfinter;
            double alfinterL;
            if (ugsmed > 0) {
                alfinter = celula[i].alf;
                alfinterL = celula[i].alfL;
            } else {
                alfinter = celula[i].alfR;
                alfinterL = celula[i].alf;
            }
            double delvel;
            if (alfinter > (*vg1dSP).localtiny && alfinter < (1. - (*vg1dSP).localtiny))
                delvel = ugsmed / alfinter - ulsmed / (1. - alfinter);
            else if (alfinter > (*vg1dSP).localtiny)
                delvel = ugsmed;
            else
                delvel = ulsmed;
            double verifica = area * celula[i].pres * 98066.5 * delvel * (alfinter - alfinterL) / celula[i].dx;

            double vPot = 0.;
            if (i > 0)
                vPot = celula[i - 1].potTermo;
            celula[i].temp = ((coefTempo / celula[i].dt) * celula[i].temp - (-coefPresTempo * (celula[i].pres - celula[i].presini) * 98066.5 / celula[i].dt) + celula[i].dTdLCor * (-coefdxT * dtdx + coefdxP * dpdx - cinetico - (hidro - 0. * verifica) + (vPot + celula[i].fonteCal) / dxmed + fontemassL + fontemassG + fluxcal - latente) - (rc - rp) * (1 - alfmed) * celula[i].pres * 98066.5 * area * (celula[i].bet - celula[i].betini) / (rhol * celula[i].dt)) / (coefTempo / celula[i].dt);

            if ((i < 148 && i > 144) && (*vg1dSP).lixo5 > 53879) {
                int para;
                para = 0.;
            }

            if (fabs(celula[i].temp - celula[i].tempini) / celula[i].dt > 10.) {
                celula[i].temp = celula[i].tempini +
                                 (fabs(celula[i].temp - celula[i].tempini) / (celula[i].temp - celula[i].tempini)) * 10 * celula[i].dt;
            } else if (fabs(celula[i].temp - celula[i].tempini) / celula[i].dt > 1. && fabs(celula[i].VTemper) > 10 * JmixRef)
                celula[i].temp = celula[i].tempini;
            double tminimo = -50.;
            if (arq.usaTabela == 1)
                tminimo = arq.tabent.tmin + 1.;
            if (celula[i].temp < tminimo)
                celula[i].temp = tminimo;
            if (celula[i].temp > 200.)
                celula[i].temp = 200.;
        } else if (i == ncel) {
            if ((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)
                celula[i].temp = celula[i].calor.Textern1;
            else
                celula[i].temp = tGSup;
        }
    } else {
        celula[i].temp = celula[i].calor.Textern1;
    }
}

double SProd::calcHmix(int i) {

    double dx = celula[i].dx;
    double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
    double dia = celula[i].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double alfmed = celula[i].alf;
    double betmed = celula[i].bet;
    double alfmed0 = celula[i].alfini;
    double betmed0 = celula[i].betini;
    double pmed = celula[i].pres;
    double pmed0 = celula[i].presini;
    double tmed = celula[i].temp;
    double ugsL;
    double ulsL;
    double ugsR;
    double ulsR;

    ugsL = celula[i].QG / area;
    ulsL = celula[i].QL / area;
    ugsR = celula[i + 1].QG / area;
    ulsR = celula[i + 1].QL / area;

    double betL = celula[i].bet;
    double betR = celula[i].bet;
    if (ugsL > 0.)
        betL = celula[i - 1].bet;
    if (ugsL < 0.)
        betL = celula[i + 1].bet;

    double ugsmed = 0.5 * (ugsL + ugsR);
    double ulsmed = 0.5 * (ulsL + ulsR);

    double presL = celula[i].presaux;
    double presR = celula[i + 1].presaux;
    if ((celula[i].acsr.tipo == 5 && celula[i].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i].duto.area) ||
        (celula[i].acsr.tipo == 4 && celula[i].acsr.bcs.freq > 0) ||
        (celula[i].acsr.tipo == 8 && celula[i].acsr.bvol.freq > 0.) ||
        (celula[i].acsr.tipo == 7 && fabs(celula[i].acsr.delp) > 0.) ||
        (celula[i].acsr.tipo == 17 && celula[i].acsr.multibcs.freq > 0)) {
        presR = celula[i].pres + (celula[i].pres - celula[i].presaux) * 0.5;
    }

    double tmedL = celula[i].tempL;
    if (celula[i].VTemper < 0.)
        tmedL = celula[i].temp;
    double tmedR = celula[i].temp;
    if (celula[i + 1].VTemper < 0.)
        tmedR = celula[i].tempR;

    double rhog = celula[i].flui.MasEspGas(pmed0, tmed);
    double rhop = celula[i].flui.MasEspLiq(pmed0, tmed);
    double rhoc = celula[i].fluicol.MasEspFlu(pmed0, tmed);
    double rhog1 = celula[i].flui.MasEspGas(pmed, tmed);
    double rhop1 = celula[i].flui.MasEspLiq(pmed, tmed);
    double hg = celula[i].flui.EntalpGas(pmed0, tmed);
    double hp = celula[i].flui.EntalpLiq(pmed0, tmed);
    double hc = celula[i].fluicol.CalorLiq(pmed0, tmed) * tmed; // corrigir entalpia

    double rhogL = celula[i].flui.MasEspGas(presL, tmedL);
    double rhopL = celula[i].flui.MasEspLiq(presL, tmedL);
    double rhocL = celula[i].fluicol.MasEspFlu(presL, tmedL);
    double hgL = celula[i].flui.EntalpGas(presL, tmedL);
    double hpL = celula[i].flui.EntalpLiq(presL, tmedL);
    double hcL = celula[i].fluicol.CalorLiq(presL, tmedL) * tmedL; // corrigir entalpia

    double rhogR = celula[i].flui.MasEspGas(presR, tmedR);
    double rhopR = celula[i].flui.MasEspLiq(presR, tmedR);
    double rhocR = celula[i].fluicol.MasEspFlu(presR, tmedR);
    double hgR = celula[i].flui.EntalpGas(presR, tmedR);
    double hpR = celula[i].flui.EntalpLiq(presR, tmedR);
    double hcR = celula[i].fluicol.CalorLiq(presR, tmedR) * tmedR; // corrigir entalpia

    double energintmixT0 = rhog * alfmed0 * (hg - pmed0 * 98066.5 / rhog) + rhop * (1 - betmed0) * (1. - alfmed0) * (hp - pmed0 * 98066.5 / rhop) +
                           betmed0 * rhoc * (1. - alfmed0) * (hc - pmed0 * 98066.5 / rhoc);
    double fluxHL = rhogL * ugsL * hgL + rhopL * (1. - betL) * ulsL * hpL + rhocL * betL * ulsL * hcL;
    double fluxHR = rhogR * ugsR * hgR + rhopR * (1. - betR) * ulsR * hpR + rhocR * betR * ulsR * hcR;
    double delFlux = (fluxHR - fluxHL) / dx;
    double hidro = (rhog1 * ugsmed + (1 - betmed) * ulsmed * rhop1 + betmed * ulsmed) * sin(celula[i].duto.teta) * 9.82;

    double fontemassG = 0.;
    double fontemassL = 0.;
    double fontemassC = 0.;
    double tfonte = celula[i].temp;
    double hgF;
    double hlF;
    double hcF = 0.;

    if (celula[i].acsr.tipo == 1) {
        tfonte = celula[i].acsr.injg.temp;
        hgF = celula[i].acsr.injg.FluidoPro.EntalpGas(celula[i].pres, tfonte);
        hlF = 0;
        hcF = 0;
    } else if (celula[i].acsr.tipo == 2) {
        tfonte = celula[i].acsr.injl.temp;
        hgF = celula[i].acsr.injl.FluidoPro.EntalpGas(celula[i].pres, tfonte);
        hlF = celula[i].acsr.injl.FluidoPro.EntalpLiq(celula[i].pres, tfonte);
        hcF = celula[i].acsr.injl.fluidocol.CalorLiq(celula[i].pres, tfonte) * tfonte
            /*entalpia fluido complementar a ser corrigida*/;
    } else if (celula[i].acsr.tipo == 3) {
        tfonte = celula[i].acsr.ipr.Tres;
        hgF = celula[i].acsr.ipr.FluidoPro.EntalpGas(celula[i].pres, tfonte);
        hlF = celula[i].acsr.ipr.FluidoPro.EntalpLiq(celula[i].pres, tfonte);
        hcF = 0;
    } else if (celula[i].acsrL != 0) {
        if ((*celula[i].acsrL).tipo == 5) {
            if ((*celula[i].acsrL).chk.AreaGarg < arq.master1.razareaativ * celula[i].dutoL.area && (*celula[i].acsrL).chk.AreaGarg > 1e-5 * celula[i].dutoL.area) {
                double tE = celula[i - 1].temp;
                double alfE = celula[i - 1].alf;
                double betE = celula[i - 1].bet;

                double rholp = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
                double rholc = celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
                double rholmix = (1 - betE) * rholp + betE * rholc;

                double alfJ = celula[i].alf;
                double betJ = celula[i].bet;
                double rholpJ = celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp);
                double rholcJ = celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
                double rholmixJ = (1 - betJ) * rholpJ + betJ * rholcJ;

                double hidroM = sin(celula[i - 1].duto.teta) * (0.5 * celula[i - 1].dx) * (rholmix * (1 - alfE) + alfE * celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp)) * 9.82 / 98600.;
                double hidroJ = sin(celula[i].duto.teta) * (0.5 * celula[i].dx) * (rholmixJ * (1 - alfJ) + alfJ * celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp)) * 9.82 / 98600.;

                double tit = alfE * celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp) / (celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp) * alfE + rholmix * (1. - alfE));

                double jtlM = (1. - betE) * celula[i - 1].flui.JTL(celula[i - 1].pres - hidroM, celula[i - 1].temp) - betE / rholcJ;
                double jtgM = celula[i - 1].flui.JTG(celula[i - 1].pres - hidroM, celula[i - 1].temp);
                tfonte = tE + ((1. - tit) * jtlM + tit * jtgM) * (celula[i].pres + hidroJ - celula[i - 1].pres - hidroM);

                hgF = celula[i - 1].flui.EntalpGas(celula[i - 1].pres, tfonte);
                hlF = celula[i - 1].flui.EntalpLiq(celula[i - 1].pres, tfonte);
                hcF = celula[i - 1].fluicol.CalorLiq(celula[i - 1].pres, tfonte) * tfonte;
                /*entalpia fluido complementar a ser corrigida*/

            } else {
                tfonte = celula[i].temp;
                hgF = celula[i - 1].flui.EntalpGas(celula[i - 1].pres, celula[i - 1].temp);
                hlF = celula[i - 1].flui.EntalpLiq(celula[i - 1].pres, celula[i - 1].temp);
                hcF = celula[i - 1].fluicol.CalorLiq(celula[i - 1].pres, celula[i - 1].temp) * tfonte;
                /*entalpia fluido complementar a ser corrigida*/
            }
        } else if ((*celula[i].acsrL).tipo == 8) {
            double alfM = celula[i - 1].alf;
            double betM = celula[i - 1].bet;

            double n = (*celula[i].acsrL).bvol.npoli;
            double ypres = (celula[i].pres) / (celula[i - 1].pres);
            tfonte = celula[i - 1].temp * pow(ypres, (n - 1) / n);

            hgF = celula[i - 1].flui.EntalpGas(celula[i - 1].pres, tfonte);
            hlF = celula[i - 1].flui.EntalpLiq(celula[i - 1].pres, tfonte);
            hcF = celula[i - 1].fluicol.CalorLiq(celula[i - 1].pres, tfonte) * tfonte;
            /*entalpia fluido complementar a ser corrigida*/
        } else {
            hgF = 0.;
            hlF = 0.;
        }
    } else {
        hgF = 0.;
        hlF = 0.;
    }

    fontemassL = 0;
    fontemassG = 0;
    if (celula[i].fontemassLR > 0.)
        fontemassL = hlF * celula[i].fontemassLR / dx;
    if (celula[i].fontemassCR > 0.)
        fontemassL += hcF * celula[i].fontemassCR / dx;
    if (fontemassG > 0.)
        fontemassG = hgF * celula[i].fontemassGR / dx;

    return energintmixT0 - (delFlux + hidro - (fontemassG + fontemassL) / area) * celula[i].dt;
}

double SProd::energmix(int i, int jp0, int jt, double razp) {
    int jp1 = jp0 + 1;

    double pres0 = celula[i].flui.rhogF[jp0][0];
    double pres1 = celula[i].flui.rhogF[jp1][0];
    double temp = celula[i].flui.rhogF[0][jt];
    double alfmed = celula[i].alf;
    double betmed = celula[i].bet;

    double rhogp0 = celula[i].flui.rhogF[jp0][jt];
    double rhogp1 = celula[i].flui.rhogF[jp1][jt];
    double hgp0 = celula[i].flui.HgF[jp0][jt];
    double hgp1 = celula[i].flui.HgF[jp1][jt];

    double rholp0 = celula[i].flui.rholF[jp0][jt];
    double rholp1 = celula[i].flui.rholF[jp1][jt];
    double hlp0 = celula[i].flui.HlF[jp0][jt];
    double hlp1 = celula[i].flui.HlF[jp1][jt];

    double rhocp0 = celula[i].fluicol.MasEspFlu(pres0, temp);
    double rhocp1 = celula[i].fluicol.MasEspFlu(pres1, temp);
    double hlc0 = celula[i].fluicol.CalorLiq(pres0, temp) * temp; // corrigir entalpia
    double hlc1 = celula[i].fluicol.CalorLiq(pres1, temp) * temp; // corrigir en;talpia

    double energ0 = alfmed * rhogp0 * (hgp0 - pres0 * 98066.5 / rhogp0) +
                    (1 - alfmed) * (1 - betmed) * rholp0 * (hlp0 - pres0 * 98066.5 / rholp0) +
                    (1 - alfmed) * (betmed)*rhocp0 * (hlc0 - pres0 * 98066.5 / rhocp0);

    double energ1 = alfmed * rhogp1 * (hgp1 - pres1 * 98066.5 / rhogp0) +
                    (1 - alfmed) * (1 - betmed) * rholp1 * (hlp1 - pres1 * 98066.5 / rholp1) +
                    (1 - alfmed) * (betmed)*rhocp1 * (hlc1 - pres1 * 98066.5 / rhocp1);

    return razp * energ0 + (1. - razp) * energ1;
}

void SProd::calcTempEntalp(int i) {
    double pres = celula[i].pres;
    double **Var = celula[i].flui.rholF;

    int ipres = 0;
    int ipmarcador;
    int ndiv = celula[i].flui.npontos - 1;
    if (pres < Var[1][0] || pres >= Var[ndiv + 1][0]) {
        cout << "pressÃƒÂ£o fora dos limites de tabela";
        getchar();
    }

    int e, m, d;
    e = 1;
    d = ndiv + 1;
    while (e <= d) {
        m = (e + d) / 2;
        ipmarcador = m;
        if (m == 1) {
            ipres = m;
            break;
        } else if (m == ndiv + 1 && Var[m][0] == pres) {
            ipres = m - 1;
            break;
        }
        if (Var[m][0] > pres && Var[m - 1][0] <= pres) {
            ipres = m - 1;
            break;
        }
        if (Var[m][0] < pres)
            e = m + 1;
        else
            d = m - 1;
    }

    double razpres = 1. - (Var[ipres][0] - pres) / (Var[ipres][0] - Var[ipres + 1][0]);

    double energint = calcHmix(i);
    int j = 1;
    double val1 = -1;
    double val2 = 0;
    while (j < ndiv + 1 || (energint >= val1 && energint <= val2) || (energint <= val1 && energint >= val2)) {
        val1 = energmix(i, ipres, j, razpres);
        val2 = energmix(i, ipres, j + 1, razpres);
        j++;
    }
    double raztemp = 1. - (val1 - energint) / (val1 - val2);
    celula[i].temp = Var[0][j] * raztemp + (1. - raztemp) * Var[0][j + 1];

    if (celula[i].temp < -50.)
        celula[i].temp = -50.;
    if (celula[i].temp > 200.)
        celula[i].temp = 200.;
}

void SProd::calcTransMassTermo(int i) {

    double dx = celula[i].dx;
    double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
    double dia = celula[i].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double alfmed = celula[i].alf;
    double betmed = celula[i].bet;
    double ugsmed;

    if (celula[i].alf > (*vg1dSP).localtiny)
        ugsmed = celula[i].QG / area;
    else {
        ugsmed = 0.;
    }
    double ulsmed;
    if (celula[i].alf < 1. - (*vg1dSP).localtiny)
        ulsmed = celula[i].QL / area;
    else {
        ulsmed = 0.;
    }
    double rp = celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp);
    double rc = celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
    double rhol = (1. - betmed) * rp + betmed * rc;
    double rhog = celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp);
    double cpl = (1. - betmed) * celula[i].flui.CalorLiq(celula[i].pres, celula[i].temp) + betmed * celula[i].fluicol.CalorLiq(celula[i].pres, celula[i].temp);
    double cvl = cpl;
    double cpg = celula[i].flui.CalorGas(celula[i].pres, celula[i].temp);
    double cvg = celula[i].flui.CalorGasVolMod(celula[i].pres, celula[i].temp);
    double jtl = (1. - betmed) * celula[i].flui.JTL(celula[i].pres, celula[i].temp) - betmed / rc;
    double jtg = celula[i].flui.JTG(celula[i].pres, celula[i].temp);
    double hidro = (rhol * ulsmed + rhog * ugsmed) * area * 9.82 * sin(celula[i].duto.teta);

    celula[i].calor.Tint = celula[i].temp;
    celula[i].calor.Vint = ugsmed + ulsmed;
    celula[i].calor.dt = celula[i].dt;
    double condliq = (1. - betmed) * celula[i].flui.CondLiq(celula[i].pres, celula[i].temp) + betmed * celula[i].fluicol.CondLiq(celula[i].pres, celula[i].temp);
    celula[i].calor.kint = condliq * (1 - alfmed) + celula[i].flui.CondGas(celula[i].pres, celula[i].temp) * alfmed;
    celula[i].calor.cpint = cpl * (1 - alfmed) + cpg * alfmed;
    celula[i].calor.rhoint = rhol * (1 - alfmed) + rhog * alfmed;
    double viscliq = (1. - betmed) * celula[i].flui.ViscOleo(celula[i].pres, celula[i].temp) + betmed * celula[i].fluicol.VisFlu(celula[i].pres, celula[i].temp);
    celula[i].calor.viscint = viscliq * (1 - alfmed) * 1.e-3 + celula[i].flui.ViscGas(celula[i].pres, celula[i].temp) * alfmed * 1.e-3;
    double fluxcal = celula[i].calor.transtrans();

    double coefTempo = (rhol * (1 - alfmed) * cvl + rhog * alfmed * cvg) * area;
    double coefPresTempo;
    coefPresTempo = -celula[i].flui.CalorGasPresMod(celula[i].pres, celula[i].temp) * (rhog * alfmed * area);
    double coefdxT = (rhol * ulsmed * cpl + rhog * ugsmed * cpg) * area;
    double coefdxP = (rhol * ulsmed * jtl + rhog * ugsmed * jtg) * area;
    double dpdx;
    if (i < ncel)
        dpdx = 2. * (celula[i + 1].presaux - celula[i].pres) * 98066.5 / celula[i].dx;
    else
        dpdx = 2. * (celula[i].pres - celula[i].presaux) * 98066.5 / celula[i].dx;
    if (celula[i].acsr.tipo == 5 && celula[i].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i].duto.area)
        dpdx = 2. * (celula[i].pres - celula[i].presaux) * 98066.5 / celula[i].dx;
    celula[i].VTemper = coefdxT / coefTempo;
    double dtdx = (celula[i].temp - celula[i - 1].temp) / dxmed;
    if (i < ncel)
        if (celula[i].VTemper < 0)
            dtdx = (celula[i + 1].temp - celula[i].temp) / (0.5 * (celula[i + 1].dx + celula[i].dx));
    if (celula[i].acsr.tipo == 5 && celula[i].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i].duto.area && celula[i].VTemper <= 0)
        dtdx = 0.;
    if (celula[i - 1].acsr.tipo == 5 && celula[i - 1].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i - 1].duto.area && celula[i].VTemper >= 0)
        dtdx = 0.;
    if ((i == 1 && celula[i].VTemper <= 0) || (i == ncel && celula[i].VTemper <= 0))
        dtdx = 0.;
    if (celula[i].acsr.tipo == 4 && celula[i].acsr.bcs.freqnova > 1.) {
        dpdx = 0.;
    }
    if (celula[i].acsr.tipo == 8 && celula[i].acsr.bvol.freq > 1.) {
        dpdx = (celula[i].pres - celula[i - 1].pres) * 98066.5 / dxmed;
    }
    if (celula[i].acsr.tipo == 17 && celula[i].acsr.multibcs.freqnova > 1.) {
        dpdx = 0.;
    }

    double cinetico = 0;
    double ugmed0 = 0;
    double ulmed0 = 0;
    double ugmed = 0;
    double ulmed = 0.;
    if (celula[i].acsr.tipo == 0 && celula[i - 1].acsr.tipo == 0 && i > 1) {

        double dxCin = celula[i - 1].dx;
        double diaaux = celula[i - 1].duto.a;
        double areaaux = 0.25 * M_PI * diaaux * diaaux;

        double alf;
        if (celula[i].QL > 0)
            alf = celula[i - 1].alf;
        else
            alf = celula[i].alf;
        double alf0;
        if (celula[i - 1].QL > 0)
            alf0 = celula[i - 2].alf;
        else
            alf0 = celula[i - 1].alf;

        if (alf0 > 1e-3) {
            ugmed0 = celula[i - 1].QG / (areaaux);
            ugmed0 /= alf0;
        }

        if (alf0 < 1. - 1e-3) {
            ulmed0 = celula[i - 1].QL / (areaaux);
            ulmed0 /= (1. - alf0);
        }

        if (alf > 1e-3) {
            ugmed = ugsmed;
            ugmed /= alf;
        }

        if (alf < 1. - 1e-3) {
            ulmed = ulsmed;
            ulmed /= (1. - alf);
        }

        cinetico = (celula[i].MC - celula[i - 1].Mliqini) * ugmed * (ugmed - ugmed0) / dxCin + celula[i - 1].Mliqini * ulmed * (ulsmed - ulmed0) / dxCin;
    }

    double fontemassG = 0.;
    double fontemassL = 0.;
    double tfonte = celula[i].temp;
    double cpgF;
    double razcpF = 0.;
    double cplF;
    if (celula[i].acsr.tipo == 1) {
        tfonte = celula[i].acsr.injg.temp;
        cpgF = celula[i].acsr.injg.FluidoPro.CalorGas(celula[i].pres, celula[i].temp);
        razcpF = celula[i].acsr.injg.FluidoPro.ConstAdG(celula[i].pres, celula[i].temp);
        cplF = 0.;
    } else if (celula[i].acsr.tipo == 2) {
        tfonte = celula[i].acsr.injl.temp;
        ;
        cpgF = celula[i].acsr.injl.FluidoPro.CalorGas(celula[i].pres, celula[i].temp);
        razcpF = celula[i].acsr.injl.FluidoPro.ConstAdG(celula[i].pres, celula[i].temp);
        cplF = (1. - celula[i].acsr.injl.bet) * celula[i].acsr.injl.FluidoPro.CalorLiq(celula[i].pres, celula[i].temp) + celula[i].acsr.injl.bet * celula[i].acsr.injl.fluidocol.CalorLiq(celula[i].pres, celula[i].temp);
    } else if (celula[i].acsr.tipo == 3) {
        tfonte = celula[i].acsr.ipr.Tres;
        cpgF = celula[i].acsr.ipr.FluidoPro.CalorGas(celula[i].pres, celula[i].temp);
        razcpF = celula[i].acsr.ipr.FluidoPro.ConstAdG(celula[i].pres, celula[i].temp);
        cplF = celula[i].acsr.ipr.FluidoPro.CalorLiq(celula[i].pres, celula[i].temp);
    } else if (celula[i].acsrL != 0) {
        if ((*celula[i].acsrL).tipo == 5) {
            if ((*celula[i].acsrL).chk.AreaGarg < arq.master1.razareaativ * celula[i].dutoL.area && (*celula[i].acsrL).chk.AreaGarg > 1e-5 * celula[i].dutoL.area) {
                double tE = celula[i - 1].temp;
                double alfE = celula[i - 1].alf;
                double betE = celula[i - 1].bet;

                double rholp = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
                double rholc = celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
                double rholmix = (1 - betE) * rholp + betE * rholc;

                double alfJ = celula[i].alf;
                double betJ = celula[i].bet;
                double rholpJ = celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp);
                double rholcJ = celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
                double rholmixJ = (1 - betJ) * rholpJ + betJ * rholcJ;

                double hidroM = sin(celula[i - 1].duto.teta) * (0.5 * celula[i - 1].dx) * (rholmix * (1 - alfE) + alfE * celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp)) * 9.82 / 98600.;
                double hidroJ = sin(celula[i].duto.teta) * (0.5 * celula[i].dx) * (rholmixJ * (1 - alfJ) + alfJ * celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp)) * 9.82 / 98600.;

                double tit = alfE * celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp) / (celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp) * alfE + rholmix * (1. - alfE));

                double jtlM = (1. - betE) * celula[i - 1].flui.JTL(celula[i - 1].pres - hidroM, celula[i - 1].temp) - betE / rholcJ;
                double jtgM = celula[i - 1].flui.JTG(celula[i - 1].pres - hidroM, celula[i - 1].temp);
                tfonte = tE + ((1. - tit) * jtlM + tit * jtgM) * (celula[i].pres + hidroJ - celula[i - 1].pres - hidroM);

                cpgF = celula[i - 1].flui.CalorGas(celula[i - 1].pres, celula[i - 1].temp);
                cplF = (1 - celula[i - 1].bet) * celula[i - 1].flui.CalorLiq(celula[i - 1].pres, celula[i - 1].temp) + celula[i - 1].bet * celula[i - 1].fluicol.CalorLiq(celula[i - 1].pres, celula[i - 1].temp);
                razcpF = celula[i - 1].flui.ConstAdG(celula[i - 1].pres, celula[i - 1].temp);

            } else {
                cpgF = celula[i - 1].flui.CalorGas(celula[i - 1].pres, celula[i - 1].temp);
                cplF = (1 - celula[i - 1].bet) * celula[i - 1].flui.CalorLiq(celula[i - 1].pres, celula[i - 1].temp) + celula[i - 1].bet * celula[i - 1].fluicol.CalorLiq(celula[i - 1].pres, celula[i - 1].temp);
                tfonte = celula[i].temp;
                razcpF = celula[i - 1].flui.ConstAdG(celula[i - 1].pres, celula[i - 1].temp);
            }
        } else if ((*celula[i].acsrL).tipo == 8) {
            double alfM = celula[i - 1].alf;
            double betM = celula[i - 1].bet;
            cpgF = celula[i - 1].flui.CalorGas(celula[i - 1].pres, celula[i - 1].temp);
            cplF = (1. - betM) * celula[i - 1].flui.CalorLiq(celula[i - 1].pres, celula[i - 1].temp) + betM * celula[i - 1].fluicol.CalorLiq(celula[i - 1].pres, celula[i - 1].temp);
            double n = (*celula[i].acsrL).bvol.npoli;
            double ypres = (celula[i].pres) / (celula[i - 1].pres);
            tfonte = celula[i - 1].temp * pow(ypres, (n - 1) / n);
        } else {
            cpgF = 0.;
            razcpF = 1.;
            cplF = 0.;
        }
    } else {
        cpgF = 0.;
        razcpF = 1.;
        cplF = 0.;
    }

    fontemassL = 0;
    if (celula[i].fontemassLR > 0.)
        fontemassL = celula[i].fontemassLR / dx;
    if (celula[i].fontemassCR > 0.)
        fontemassL += celula[i].fontemassCR / dx;
    fontemassL *= (cplF) * (tfonte - celula[i].temp);

    fontemassG = celula[i].fontemassGR / dx;
    if (fontemassG > 0.)
        fontemassG *= (cpgF / razcpF) * (tfonte - celula[i].temp);
    else
        fontemassG = 0;

    double latente;
    if (CalcLat > 0 && arq.flashCompleto == 0) {
        latente = interpolaHLatente(celula[i].pres, celula[i].temp);
    } else if (arq.flashCompleto == 1)
        latente = (celula[i].flui.EntalpGas(celula[i].pres, celula[i].temp) -
                   celula[i].flui.EntalpLiq(celula[i].pres, celula[i].temp));
    else
        latente = 0;

    double alfinter;
    double alfinterL;
    if (ugsmed > 0) {
        alfinter = celula[i].alf;
        alfinterL = celula[i].alfL;
    } else {
        alfinter = celula[i].alfR;
        alfinterL = celula[i].alf;
    }
    double delvel;
    if (alfinter > (*vg1dSP).localtiny && alfinter < (1. - (*vg1dSP).localtiny))
        delvel = ugsmed / alfinter - ulsmed / (1. - alfinter);
    else if (alfinter > (*vg1dSP).localtiny)
        delvel = ugsmed;
    else
        delvel = ulsmed;
    double verifica = area * celula[i].pres * 98066.5 * delvel * (alfinter - alfinterL) / celula[i].dx;

    if (celula[i].temp < 0 || celula[i].temp > 100) {
        int para;
        para = 0.;
    }

    celula[i].FonteMudaFase = (-(coefTempo / celula[i].dt) * (celula[i].temp - celula[i].tempini) - (coefPresTempo * (celula[i].pres - celula[i].presini) * 98066.5 / celula[i].dt) - coefdxT * dtdx + coefdxP * dpdx - cinetico - (hidro - 0. * verifica) + celula[i - 1].potB / dxmed + fontemassL + fontemassG + fluxcal - (rc - rp) * (1 - alfmed) * celula[i].pres * 98066.5 * area * (celula[i].bet - celula[i].betini) / (rhol * celula[i].dt)); // / (coefTempo / celula[i].dt);

    celula[i].FonteMudaFase /= latente;
}

void SProd::FonteValv(int ind) {
    if (celula[ind].acsr.chk.AreaGarg < (1e-3 + arq.master1.razareaativ) * celula[ind].duto.area && celula[ind].acsr.chk.AreaGarg > 1e-3 * celula[ind].duto.area) {
        double tE = celula[ind].temp;
        double alfE = celula[ind].alf;
        double betE = celula[ind].bet;
        double sense = 1.;

        double maxSup = 0.;

        double rholp = celula[ind].flui.MasEspLiq(celula[ind].pres, celula[ind].temp);
        double rholc = celula[ind].fluicol.MasEspFlu(celula[ind].pres, celula[ind].temp);
        double rholmix = (1 - betE) * rholp + betE * rholc;

        double alfJ = celula[ind + 1].alf;
        double betJ = celula[ind + 1].bet;
        double rholpJ = celula[ind + 1].flui.MasEspLiq(celula[ind + 1].pres, celula[ind + 1].temp);
        double rholcJ = celula[ind + 1].fluicol.MasEspFlu(celula[ind + 1].pres, celula[ind + 1].temp);
        double rholmixJ = (1 - betJ) * rholpJ + betJ * rholcJ;

        double hidroM = sin(celula[ind].duto.teta) * (0.5 * celula[ind].dx) * (rholmix * (1 - alfE) + alfE * celula[ind].flui.MasEspGas(celula[ind].pres, celula[ind].temp)) * 9.82 / 98600.;
        double hidroJ = sin(celula[ind + 1].duto.teta) * (0.5 * celula[ind + 1].dx) * (rholmixJ * (1 - alfJ) + alfJ * celula[ind + 1].flui.MasEspGas(celula[ind + 1].pres, celula[ind + 1].temp)) * 9.82 / 98600.;

        double masentrada = celula[ind].MC;
        double massgas = celula[ind].MC - celula[ind].Mliqini;
        double tit;
        if (fabs(masentrada < 1e-9) && fabs(massgas < 1e-9)) {
            tit = alfE * celula[ind].flui.MasEspGas(celula[ind].pres, celula[ind].temp) / (celula[ind].flui.MasEspGas(celula[ind].pres, celula[ind].temp) * alfE + rholmix * (1. - alfE));
        } else {
            if ((massgas >= 0 && celula[ind].Mliqini <= 0) || (massgas < 0 && celula[ind].Mliqini == 0))
                tit = 1.;
            else if (massgas <= 0 && celula[ind].Mliqini > 0)
                tit = 0.;
            else if (masentrada < 0)
                tit = 1.;
            else
                tit = fabs(massgas / masentrada);
            if (tit > 1)
                tit = 1;
        }

        double masChk;

        double ypres = (celula[ind + 1].pres + hidroJ) / (celula[ind].pres - hidroM);
        int check = 1;
        if (ypres < 1. || check == 1) {
            if ((*vg1dSP).lixo5 > 7059) {
                int para;
                para = 1;
            }
            masChk = celula[ind].acsr.chk.vazmassSachd(ypres, celula[ind].pres - hidroM, tE, alfE,
                                                       betE, tit, celula[ind].flui, celula[ind].fluicol);
            maxSup = celula[ind].acsr.chk.vazmaxSachd(celula[ind].pres - hidroM, tE, alfE,
                                                      betE, tit, celula[ind].flui, celula[ind].fluicol);
        } else {
            tE = celula[ind + 1].temp;
            alfE = celula[ind + 1].alf;
            betE = celula[ind + 1].bet;
            ypres = 1. / ypres;
            sense = -1;
            if ((*vg1dSP).lixo5 > 7059) {
                int para;
                para = 1;
            }
            masChk = celula[ind].acsr.chk.vazmassSachd(ypres, celula[ind + 1].pres + hidroJ, tE, alfE, betE, tit, celula[ind + 1].flui,
                                                       celula[ind + 1].fluicol);
            maxSup = celula[ind].acsr.chk.vazmaxSachd(celula[ind + 1].pres + hidroJ, tE, alfE, betE, tit, celula[ind + 1].flui,
                                                      celula[ind + 1].fluicol);
        }

        if (fabs(ypres) > fabs(celula[ind].acsr.chk.razpres))
            maxSup = masChk;

        if (celula[ind].acsr.chk.AreaGarg < (1e-3) * celula[ind].duto.area || (celula[ind].pres < celula[ind + 1].pres && check == 1))
            maxSup =
                0.;

        double masliq;
        double masgas;
        masliq = sense * maxSup * (1. - tit);
        masgas = sense * maxSup * tit;

        celula[ind].fontemassLR += (-1 * masliq * (1 - betE) * rholp / rholmix);
        celula[ind].fontemassCR += (-1 * masliq * betE * rholc / rholmix);
        celula[ind].fontemassGR += (-1 * masgas);

        celula[ind + 1].fontemassLR += (masliq * (1 - betE) * rholp / rholmix);
        celula[ind + 1].fontemassCR += (masliq * betE * rholc / rholmix);
        celula[ind + 1].fontemassGR += (masgas);
    }
}

void SProd::salvaFonte() {
    for (int i = 0; i <= ncel; i++) {
        celula[i].fontemassLRini = celula[i].fontemassLR;
        celula[i].fontemassCRini = celula[i].fontemassCR;
        celula[i].fontemassGRini = celula[i].fontemassGR;
    }
}

void SProd::renovaFonte(int ind) {

    double pr = celula[ind].pres;
    double tr = celula[ind].temp;
    if (ind == 0) {
        celula[ind].fontemassLR = 0.;
        celula[ind].fontemassCR = 0.;
        celula[ind].fontemassGR = 0.;
    } else {
        if (celula[ind - 1].acsr.tipo != 5 && celula[ind - 1].acsr.tipo != 8 && (ind < ncel || celula[ncel].acsr.tipo == 3)) {
            celula[ind].fontemassLR = 0.;
            celula[ind].fontemassCR = 0.;
            celula[ind].fontemassGR = 0.;
        }
    }

    double agua_consumida_Mw = 0.;
    double gas_consumido_Mg = 0.;

    if (arq.calculaEnvelope == 1 && arq.tipoHmodel == 2 && (*vg1dSP).lixo5 > 0.01) {

        agua_consumida_Mw = celula[ind].agua_consumida_massa_step;

        gas_consumido_Mg = celula[ind].gas_consumido_massa_step;

        // Atualizar BSW
        double A_cross = celula[ind].duto.area;
        double Lcel = celula[ind].dx;
        double Vcel = A_cross * Lcel;
        double frac_agua = (1 - celula[ind].alfR) * (1 - celula[ind].betR) * celula[ind].FW;
        double frac_oleo = (1 - celula[ind].alfR) * (1 - celula[ind].betR) * (1 - celula[ind].FW);
        double Vagua = frac_agua * Vcel;
        double Vagua_new = Vagua - (agua_consumida_Mw / celula[ind].flui.MasEspAgua(celula[ind].pres, celula[ind].temp));
        double Voil = frac_oleo * Vcel;

        double BSW = celula[ind].flui.BSW;
        celula[ind].flui.BSW = Vagua_new / (Voil + Vagua_new);
    }

    if (celula[ind].acsr.tipo == 1) {
        if (celula[ind].acsr.injg.tipoflu == 0) {
            double masgas = celula[ind].acsr.injg.VMas(pr, tr);
            if (fabs(masgas) < (*vg1dSP).localtiny)
                masgas = 0.;
            if (celula[ind].acsr.injg.seco == 1) {
                celula[ind].fontemassGR += masgas;
                celula[ind].fontemassLR = 0.;
                celula[ind].fontemassCR = 0.;
            } else {
                double tit;
                if (arq.flashCompleto != 2)
                    tit = celula[ind].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
                else
                    tit = celula[ind].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
                double masT = masgas / tit;
                tit = celula[ind].acsr.injg.FluidoPro.FracMassHidra(pr, tr);
                celula[ind].fontemassGR += masT * tit;
                celula[ind].fontemassLR += masT * (1. - tit);
                double rcomp = celula[ind].acsr.injg.fluidocol.MasEspFlu(1., 20.);
                celula[ind].fontemassCR += rcomp * celula[ind].acsr.injg.razCompGas * celula[ind].acsr.injg.QGas / 86400.;
            }
        } else {
            celula[ind].fontemassGR += 0.;
            celula[ind].fontemassLR = 0.;
            celula[ind].fontemassCR = celula[ind].acsr.injg.QGas;
        }
    }
    if (celula[ind].acsr.tipo == 2) {
        double rlcA = celula[ind].acsr.injl.fluidocol.MasEspFlu(1.001, 15.);
        celula[ind].fontemassCR += rlcA * celula[ind].acsr.injl.QLiq * celula[ind].acsr.injl.bet / 86400;
        double massic = celula[ind].acsr.injl.QLiq * (1. - celula[ind].acsr.injl.bet) / 86400;
        double Rhogs = celula[ind].acsr.injl.FluidoPro.Deng * 1.225; // cel[ind].acsr.injl.FluidoPro.MasEspGas(1, 15);
        double Rhols = (1000 * 141.5 / (131.5 + celula[ind].acsr.injl.FluidoPro.API)) * (1 - celula[ind].acsr.injl.FluidoPro.BSW) + 1000. * celula[ind].acsr.injl.FluidoPro.Denag * celula[ind].acsr.injl.FluidoPro.BSW;
        double multiplicador = (Rhols + celula[ind].acsr.injl.FluidoPro.RGO * Rhogs * (1 - celula[ind].acsr.injl.FluidoPro.BSW));
        massic *= multiplicador;
        double fracmasshidra = celula[ind].acsr.injl.FluidoPro.FracMassHidra(pr, tr);
        celula[ind].fontemassLR += (1. - fracmasshidra) * massic;
        celula[ind].fontemassGR += fracmasshidra * massic;
    }
    if (celula[ind].acsr.tipo == 3) {
        if (pr < celula[ind].acsr.ipr.Pres) {
            celula[ind].fontemassLR += celula[ind].acsr.ipr.MasL(pr, tr);
            celula[ind].fontemassCR = 0.;
            celula[ind].fontemassGR += celula[ind].acsr.ipr.MasG(pr, tr);
        } else {
            double tit;
            tit = celula[ind].alf * celula[ind].flui.MasEspGas(pr, tr) /
                  (celula[ind].alf * celula[ind].flui.MasEspGas(pr, tr) +
                   (1. - celula[ind].alf) * (1. - celula[ind].bet) * celula[ind].flui.MasEspLiq(pr, tr) +
                   (1. - celula[ind].alf) * celula[ind].bet * celula[ind].fluicol.MasEspFlu(pr, tr));
            celula[ind].fontemassLR += (1. - celula[ind].alf) * (1. - celula[ind].bet) * celula[ind].acsr.ipr.VMas(pr, tr) * celula[ind].flui.MasEspLiq(pr, tr);
            celula[ind].fontemassCR += (1. - celula[ind].alf) * celula[ind].bet * celula[ind].acsr.ipr.VMas(pr, tr) * celula[ind].fluicol.MasEspFlu(pr, tr);
            celula[ind].fontemassGR += 1 * celula[ind].alf * celula[ind].acsr.ipr.VMas(pr, tr) * celula[ind].flui.MasEspGas(pr, tr);
            celula[ind].acsr.ipr.deriP *= (1. - celula[ind].alf) * (1. - celula[ind].bet) * celula[ind].flui.MasEspLiq(pr, tr);
            celula[ind].acsr.ipr.deriC *= (1. - celula[ind].alf) * celula[ind].bet * celula[ind].fluicol.MasEspFlu(pr, tr);
            celula[ind].acsr.ipr.deriG *= 1 * celula[ind].alf * celula[ind].flui.MasEspGas(pr, tr);
        }
    }
    if (celula[ind].acsr.tipo == 5) {
        celula[ind + 1].fontemassLR = 0.;
        celula[ind + 1].fontemassCR = 0.;
        celula[ind + 1].fontemassGR = 0.;
        if (modoPerm == 0)
            FonteValv(ind);
    }
    if (celula[ind].acsr.tipo == 8) {
        celula[ind + 1].fontemassLR = 0.;
        celula[ind + 1].fontemassCR = 0.;
        celula[ind + 1].fontemassGR = 0.;
        celula[ind].acsr.bvol.fluido = celula[ind].flui;
        celula[ind].acsr.bvol.fluicol = celula[ind].fluicol;
        if (fabs(celula[ind].acsr.bvol.freq) > 1) {
            double alfM = celula[ind].alf;
            double betM = celula[ind].bet;
            double presM = celula[ind].pres;
            double tempM = celula[ind].temp;
            double presM1 = celula[ind].presR;
            double tempM1 = celula[ind].tempR;
            celula[ind].acsr.bvol.vazmass(presM, tempM, presM1, tempM1, betM, alfM);
            celula[ind].fontemassLR -= celula[ind].acsr.bvol.MLiqP;
            celula[ind].fontemassCR -= celula[ind].acsr.bvol.MLiqC;
            celula[ind].fontemassGR -= celula[ind].acsr.bvol.MGas;
            celula[ind + 1].fontemassLR += celula[ind].acsr.bvol.MLiqP;
            celula[ind + 1].fontemassCR += celula[ind].acsr.bvol.MLiqC;
            celula[ind + 1].fontemassGR += celula[ind].acsr.bvol.MGas;
        }
    }
    if (celula[ind].acsr.tipo == 9) {
        celula[ind].acsr.fontechk.fluidoP = celula[ind].flui;
        celula[ind].acsr.fontechk.presT = celula[ind].pres;
        celula[ind].acsr.fontechk.tempT = celula[ind].temp;
        double pres = celula[ind].pres;
        double temp = celula[ind].temp;
        double alf = celula[ind].alf;
        double bet = celula[ind].bet;
        double rhog = celula[ind].flui.MasEspGas(pres, temp);
        double rhoP = celula[ind].flui.MasEspLiq(pres, temp);
        double rhoC = celula[ind].fluicol.MasEspFlu(pres, temp);
        celula[ind].acsr.fontechk.titT = alf * rhog / (alf * rhog + (1 - alf) * (bet * rhoC + (1 - bet) * rhoP));
        celula[ind].acsr.fontechk.betIST = bet;
        if ((*vg1dSP).chaveRedeParalela == 0 || (*vg1dSP).iterRede > 0 || redeParalelaS == 1 || redeParalelaCCsecundario == 1) {
            celula[ind].acsr.fontechk.VMas();
            celula[ind].fontemassLR += celula[ind].acsr.fontechk.masP;
            celula[ind].fontemassCR += celula[ind].acsr.fontechk.masC;
            celula[ind].fontemassGR += celula[ind].acsr.fontechk.masG;
        } else {
            int nfonte = indFonteRedeParalelaIni.size();
            int match = 0;
            int iconex;
            for (int ifonte = 0; ifonte < nfonte; ifonte++) {
                if (indFonteRedeParalelaIni[ifonte] == ind) {
                    match = 1;
                    iconex = ifonte;
                    break;
                }
            }
            if (match == 0) {
                celula[ind].acsr.fontechk.VMas();
                celula[ind].fontemassLR += celula[ind].acsr.fontechk.masP;
                celula[ind].fontemassCR += celula[ind].acsr.fontechk.masC;
                celula[ind].fontemassGR += celula[ind].acsr.fontechk.masG;
            } else {
                celula[ind].fontemassLR += fonteMpRedeParalelaIni[iconex];
                celula[ind].fontemassCR += fonteMcRedeParalelaIni[iconex];
                celula[ind].fontemassGR += fonteMgRedeParalelaIni[iconex];
            }
        }
    }
    if (celula[ind].acsr.tipo == 10) {
        if (celula[ind].acsr.injm.condTermo == 1) {
            celula[ind].fontemassLR = celula[ind].acsr.injm.MassP;
            celula[ind].fontemassCR = celula[ind].acsr.injm.MassC;
            celula[ind].fontemassGR = celula[ind].acsr.injm.MassG;
        } else {
            celula[ind].fontemassCR = celula[ind].acsr.injm.MassC;
            double masT = celula[ind].acsr.injm.MassP + celula[ind].acsr.injm.MassG;
            double tit = celula[ind].acsr.injm.FluidoPro.FracMassHidra(celula[ind].pres, celula[ind].temp);
            celula[ind].acsr.injm.MassG = celula[ind].fontemassGR = masT * tit;
            celula[ind].acsr.injm.MassP = celula[ind].fontemassLR = masT - celula[ind].fontemassGR;
        }
    }
    if (celula[ind].acsr.tipo == 15) {
        celula[ind].acsr.radialPoro.pW.val[0] = celula[ind].pres;
        double rs;
        double bo;
        double ba;
        if (celula[ind].flui.RGO < 1e7) {
            rs = celula[ind + 1].flui.RS(celula[ind].pres, celula[ind].temp);
            bo = celula[ind + 1].flui.BOFunc(celula[ind].pres, celula[ind].temp, rs);
            ba = celula[ind + 1].flui.BAFunc(celula[ind].pres, celula[ind].temp);
            rs = rs * 6.29 / 35.31467;
        } else {
            bo = 1;
            rs = 0;
            ba = 0.;
        }
        // BSW in-situ da celula anterior, na marcha, a i-esima celula
        double vfw = celula[ind + 1].flui.BSW * ba / (bo + ba * celula[ind + 1].flui.BSW - celula[ind + 1].flui.BSW * bo);
        celula[ind].acsr.radialPoro.sWPoc = vfw * (1. - celula[ind].acsr.radialPoro.satAconat) + celula[ind].acsr.radialPoro.satAconat;
        celula[ind].acsr.radialPoro.Pint = celula[ind].pres;
        if (modoPerm == 0) {
            celula[ind].acsr.radialPoro.avancoPressao();
            celula[ind].fontemassLR = celula[ind].acsr.radialPoro.fluxIni + celula[ind].acsr.radialPoro.fluxIniA;
            celula[ind].fontemassCR = 0.;
            celula[ind].fontemassGR = celula[ind].acsr.radialPoro.fluxIniG;
        } else {
            celula[ind].acsr.radialPoro.pseudoTrans();
            celula[ind].fontemassLR = celula[ind].acsr.radialPoro.fluxIni + celula[ind].acsr.radialPoro.fluxIniA;
            celula[ind].fontemassCR = 0.;
            celula[ind].fontemassGR = celula[ind].acsr.radialPoro.fluxIniG;
        }
    }
    if (celula[ind].acsr.tipo == 16) {
        celula[ind].acsr.poroso2D.dados.pW.val[0] = celula[ind].pres;
        double rs;
        double bo;
        double ba;
        if (celula[ind].flui.RGO < 1e7) {
            rs = celula[ind + 1].flui.RS(celula[ind].pres, celula[ind].temp);
            bo = celula[ind + 1].flui.BOFunc(celula[ind].pres, celula[ind].temp, rs);
            ba = celula[ind + 1].flui.BAFunc(celula[ind].pres, celula[ind].temp);
            rs = rs * 6.29 / 35.31467;
        } else {
            bo = 1;
            rs = 0;
            ba = 0.;
        }
        // BSW in-situ da celula anterior, na marcha, a i-esima celula
        double vfw = celula[ind + 1].flui.BSW * ba / (bo + ba * celula[ind + 1].flui.BSW - celula[ind + 1].flui.BSW * bo);
        celula[ind].acsr.poroso2D.sWPoc = vfw * (1. - celula[ind].acsr.poroso2D.dados.satAconat) + celula[ind].acsr.poroso2D.dados.satAconat;
        celula[ind].acsr.poroso2D.dados.transfer.sWPoc = celula[ind].acsr.poroso2D.sWPoc;
        celula[ind].acsr.poroso2D.dados.pInt = celula[ind].pres;
        celula[ind].acsr.poroso2D.dados.transfer.Pint = celula[ind].pres;
        if (modoPerm == 0) {
            celula[ind].acsr.poroso2D.avancoPressao();
            celula[ind].fontemassLR = celula[ind].acsr.poroso2D.dados.transfer.fluxIni + celula[ind].acsr.poroso2D.dados.transfer.fluxIniA;
            celula[ind].fontemassCR = 0.;
            celula[ind].fontemassGR = celula[ind].acsr.poroso2D.dados.transfer.fluxIniG;
        } else {
            celula[ind].acsr.poroso2D.pseudoTransientePoroso();
            celula[ind].fontemassLR = celula[ind].acsr.poroso2D.dados.transfer.fluxIni + celula[ind].acsr.poroso2D.dados.transfer.fluxIniA;
            celula[ind].fontemassCR = 0.;
            celula[ind].fontemassGR = celula[ind].acsr.poroso2D.dados.transfer.fluxIniG;
        }
    }

    if (arq.calculaEnvelope == 1 && arq.tipoHmodel == 2 && celula[ind].flui.BSW > 1e-12 && (*vg1dSP).lixo5 > 0.01) {
        celula[ind].fontemassLR -= (agua_consumida_Mw / (*vg1dSP).lixo5);
        celula[ind].fontemassGR -= (gas_consumido_Mg / (*vg1dSP).lixo5);
    }
}

void SProd::renovaalbetini() {

    celula[0].alfini = celula[0].alf;
    celula[0].betini = celula[0].bet;
    celula[0].alfPigDini = celula[0].alfPigD;
    celula[0].betPigDini = celula[0].betPigD;
    celula[0].alfPigEini = celula[0].alfPigE;
    celula[0].betPigEini = celula[0].betPigE;
    celula[1].alfLini = celula[0].alfini;
    celula[1].betLini = celula[0].betini;

    if (arq.ConContEntrada == 0) {
        celula[0].alfLini = celula[0].alfini;
        celula[0].betLini = celula[0].betini;
    } else {
        celula[0].alfLini = titE;
        celula[0].betLini = betaE;
    }

    indpigPini = indpigP;
    indpigP = 0;

    for (int i = 0; i <= ncel; i++) {
        celula[i].estadoPigini = celula[i].estadoPig;
        celula[i].alfini = celula[i].alf;
        if (i > 0)
            celula[i - 1].alfRini = celula[i].alfini;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alfini;
        celula[i].betini = celula[i].bet;
        if (i > 0)
            celula[i - 1].betRini = celula[i].betini;
        if (i < ncel)
            celula[i + 1].betLini = celula[i].betini;

        celula[i].alfPigDini = celula[i].alfPigD;
        celula[i].betPigDini = celula[i].betPigD;
        celula[i].alfPigEini = celula[i].alfPigE;
        celula[i].betPigEini = celula[i].betPigE;

        celula[i].alfPigERini = celula[i].alfPigER;
        celula[i].betIini = celula[i].betI;
        celula[i].betRIini = celula[i].betRI;
        celula[i].betLIini = celula[i].betLI;

        if (i > 0)
            celula[i - 1].alfPigER = celula[i].alfPigE;
        if (i == ncel)
            celula[i].alfPigER = celula[i].alf;
        celula[i].DelPig = 0.;
        celula[i].RazAreaPig = 0.;
        celula[i].cdpig = 1.;
        if (celula[i].estadoPig == 1) {
            indpigP++;
            int ipig = celula[i].indpig;
            celula[i].DelPig = arq.pig[ipig].delpres;
            celula[i].RazAreaPig = arq.pig[ipig].razarea;
            celula[i].cdpig = arq.pig[ipig].cdPig;
            double AC = celula[i].duto.area;
            double jL = (celula[i].QL + celula[i].QG) / AC;
            double jR;
            if (i < ncel)
                jR = (celula[i + 1].QL + celula[i + 1].QG) / AC;
            else
                jR = (celula[i].QL + celula[i].QG) / AC;
            celula[i].velPigini = celula[i].velPig;
            celula[i].velPig = jL * celula[i].razPig + jR * (1. - celula[i].razPig) - celula[i].VazaPig / AC;
            for (int j = 0; j < npig; j++) {
                if (receb[j] == i) {
                    celula[i].velPig = 0.;
                    celula[i].estadoPig = 0;
                    celula[i].razPig = 0.;
                    celula[i].razPigini = 0.;
                    celula[i].alfPigDini = celula[i].alf;
                    celula[i].betPigDini = celula[i].bet;
                    celula[i].alfPigEini = celula[i].alf;
                    celula[i].betPigEini = celula[i].bet;
                    celula[i].alfPigD = celula[i].alf;
                    celula[i].betPigD = celula[i].bet;
                    celula[i].alfPigE = celula[i].alf;
                    celula[i].betPigE = celula[i].bet;
                    if (i > 0)
                        celula[i - 1].alfPigER = celula[i].alfPigE;
                    if (i == ncel)
                        celula[i].alfPigER = celula[i].alf;

                    indpigP--;
                }
            }
        }
        celula[i].razPigini = celula[i].razPig;
    }
    celula[ncel].alfRini = celula[ncel].alfini;
    celula[ncel].betRini = celula[ncel].betini;
}

void SProd::renovaMasEsp() {

    celula[0].rpC = celula[0].flui.MasEspLiq(celula[0].pres, celula[0].temp);
    celula[0].rgC = celula[0].flui.MasEspGas(celula[0].pres, celula[0].temp /*,1*/);
    celula[0].rcC = celula[0].fluicol.MasEspFlu(celula[0].pres, celula[0].temp);
    celula[0].rpL = celula[0].rpC;
    celula[0].rgL = celula[0].rgC;
    celula[0].rcL = celula[0].rcC;

    celula[0].rpCi = celula[0].rpC;
    celula[0].rgCi = celula[0].rgC;
    celula[0].rcCi = celula[0].rcC;
    celula[0].rpLi = celula[0].rpCi;
    celula[0].rgLi = celula[0].rgCi;
    celula[0].rcLi = celula[0].rcCi;

    celula[0].mipC = celula[0].flui.ViscOleo(celula[0].pres, celula[0].temp);
    celula[0].migC = celula[0].flui.ViscGas(celula[0].pres, celula[0].temp /*,1*/);
    celula[0].micC = celula[0].fluicol.VisFlu(celula[0].pres, celula[0].temp);

#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = 1; i <= ncel; i++) {
        double p;
        double t;
        p = celula[i].pres;
        t = celula[i].temp;
        celula[i].rpC = celula[i].flui.MasEspLiq(p, t);
        celula[i].rgC = celula[i].flui.MasEspGas(p, t);
        celula[i].rcC = celula[i].fluicol.MasEspFlu(p, t);

        celula[i].mipC = celula[i].flui.ViscOleo(p, t);
        celula[i].migC = celula[i].flui.ViscGas(p, t);
        celula[i].micC = celula[i].fluicol.VisFlu(p, t);

        double tmed = celula[i - 1].temp;
        if (celula[i].VTemper < 0.)
            tmed = celula[i].temp;
        ProFlu flu;
        if (celula[i].QL < 0.)
            flu = celula[i].flui;
        else
            flu = celula[i - 1].flui;
        celula[i].rpCi = flu.MasEspLiq(celula[i].presaux, tmed);
        celula[i].rgCi = flu.MasEspGas(celula[i].presaux, tmed);
        celula[i].rcCi = celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
    }
    for (int i = 1; i <= ncel; i++) {

        celula[i - 1].rpR = celula[i].rpC;
        celula[i - 1].rgR = celula[i].rgC;
        celula[i - 1].rcR = celula[i].rcC;
        celula[i].rpL = celula[i - 1].rpC;
        celula[i].rgL = celula[i - 1].rgC;
        celula[i].rcL = celula[i - 1].rcC;

        celula[i - 1].mipR = celula[i].mipC;
        celula[i - 1].migR = celula[i].migC;
        celula[i - 1].micR = celula[i].micC;

        celula[i - 1].rpRi = celula[i].rpCi;
        celula[i - 1].rgRi = celula[i].rgCi;
        celula[i - 1].rcRi = celula[i].rcCi;
        celula[i].rpLi = celula[i - 1].rpCi;
        celula[i].rgLi = celula[i - 1].rgCi;
        celula[i].rcLi = celula[i - 1].rcCi;
    }
    celula[ncel].rpR = celula[ncel].rpC;
    celula[ncel].rgR = celula[ncel].rgC;
    celula[ncel].rcR = celula[ncel].rcC;

    celula[ncel].mipR = celula[ncel].mipC;
    celula[ncel].migR = celula[ncel].migC;
    celula[ncel].micR = celula[ncel].micC;
}

void SProd::BhagwatGhajar(double rhol, double rhog, double tensup, double alf, double reymix, double reymixL,
                          double ug1, double ul1, double dia, double rug, double tet, double &c0,
                          double &ud, double correcHor) {

    double Beta, Froude, MassQuality, termo1, termo2, C01, C1;
    double termoud1, termoud2, La, C2, C3, C4;
    double A1 = M_PI * dia * dia / 4.;
    double rhomix = alf * rhog + (1. - alf) * rhol;

    double sinal = 1.;
    if (tet < 0.)
        sinal = 1.;
    if (tet == 0) {
        int para;
        para = 1.;
    }

    double rmed = alf * rhog + (1. - alf) * rhol;
    Froude = sqrt(rhog / (rhol - rhog)) * ((ug1) / A1) / sqrt(9.81 * dia * sinal * cos(tet));
    MassQuality = (rhog * fabs(ug1) / A1) / ((rhog * fabs(ug1) / A1) + (rhol * fabs(ul1) / A1));
    Beta = (fabs(ug1) / A1) / ((fabs(ug1) / A1) + (fabs(ul1) / A1));

    double eps;      // rugosidade relativa.
    eps = rug / dia; // rug - rugosidade absoluta. eps - rugosidade relativa

    if (reymix < 0.0000001)
        reymix = 0.0000001;
    double fat, valHalland, den, dif;
    int III;
    if (reymix > 2400) { // regime turbulento do escoamento
        valHalland = (1 / (-18e-1 * log10(pow((eps / (3.7)), 1.11) + (69e-1 / (reymix + 1e-15)))));
        valHalland *= valHalland; // Halland.
        III = 0;
    repeat7:
        III = III + 1;
        den = -2 * log10(((eps) / 3.7) + 2.51 / ((reymix + 1e-15) * sqrt(abs(valHalland))));
        fat = 1 / (den * den); // Colebrook.
        dif = abs(fat - valHalland);
        valHalland = fat;
        if (dif >= 1e-3)
            goto repeat7;
    } else {                  // regime laminar
        fat = 64. / (reymix); // 16.
    }

    double rgrl2 = rhog / rhol;
    rgrl2 *= rgrl2;
    double reymix2 = reymix / 1000;
    reymix2 *= reymix2;
    termo1 = (2 - rgrl2) / (1 + reymix2);
    termo2 = (pow(((1 + rgrl2 * sinal * cos(tet)) / (1 + cos(tet))), (1 - alf) / 5.)) /
             (1 + 1 / reymix2);
    C1 = 0.2; // duto circular ou anular. Retangular seria 0.4.
    C01 = (C1 - C1 * sqrt(rhog / rhol)) * (pow((2.6 - Beta), 0.15) - sqrt(fat)) * pow((1 - MassQuality), 1.5);
    if (ug1 * ul1 < 0.)
        C01 = 0;
    if (tet >= -50 * M_PI / 180. && tet <= 0 && Froude <= 0.1)
        C01 = 0.0;
    c0 = termo1 + termo2 + C01; // Calculo do ParÃƒÂ¢metro de Distribuicao.

    double viscl1 = dia * (fabs(ug1 / A1) + fabs(ul1 / A1)) * rhomix / reymix;
    termoud1 = (0.35 * sin(tet) + 0.45 * cos(tet) * sinal);
    termoud2 = sqrt((9.81 * dia * (rhol - rhog) / rhol)) * sqrt(1 - alf);
    if (viscl1 / 0.001 > 10) {
        C2 = pow((0.434 / (log10(viscl1 / 0.001))), 0.15);
    } else {
        C2 = 1.0;
    }
    La = sqrt(tensup / (9.81 * (rhol - rhog))) / dia;
    if (La < 0.025) {
        C3 = pow((La / 0.025), 0.90);
    } else {
        C3 = 1.0;
        ;
    }
    C4 = 1.0;
    if (tet >= -(50 * M_PI / 180.) && tet < 0 && Froude <= 0.1)
        C4 = -1.0;
    ud = correcHor * termoud1 * termoud2 * C2 * C3 * C4; // Calculo da Velocidade de Deslizamento.
    if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet > 0. && ud < 0.)
        ud = fabs(ud);
    else if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet < 0. && ud > 0.)
        ud = -fabs(ud);
}

void SProd::BhagwatGhajarMod(double rhol, double rhog, double tensup, double alf, double reymix, double reymixL,
                             double ug1, double ul1, double dia, double rug, double tet, double &c0,
                             double &ud, double correcHor) {
    double Beta, Froude, MassQuality, termo1, termo2, C01, C1;
    double termoud1, termoud2, La, C2, C3, C4;
    double A1 = M_PI * dia * dia / 4.;
    double rhomix = alf * rhog + (1. - alf) * rhol;

    double sinal = 1.;
    if (tet < 0.)
        sinal = 1.;
    if (tet == 0) {
        int para;
        para = 1.;
    }

    double rmed = alf * rhog + (1. - alf) * rhol;
    Froude = sqrt(rhog / (rhol - rhog)) * ((ug1) / A1) / sqrt(9.81 * dia * sinal * cos(tet));
    MassQuality = (rhog * fabs(ug1) / A1) / ((rhog * fabs(ug1) / A1) + (rhol * fabs(ul1) / A1));
    Beta = (fabs(ug1) / A1) / ((fabs(ug1) / A1) + (fabs(ul1) / A1));

    double eps;      // rugosidade relativa.
    eps = rug / dia; // rug - rugosidade absoluta. eps - rugosidade relativa

    if (reymixL < 0.0000001)
        reymixL = 0.0000001;
    double fat, valHalland, den, dif;
    int III;
    if (reymixL > 2400) { // regime turbulento do escoamento
        valHalland = (1 / (-18e-1 * log10(pow((eps / (3.7)), 1.11) + (69e-1 / (reymixL + 1e-15)))));
        valHalland *= valHalland; // Halland.
        III = 0;
    repeat7:
        III = III + 1;
        den = -2 * log10(((eps) / 3.7) + 2.51 / ((reymixL + 1e-15) * sqrt(abs(valHalland))));
        fat = 1 / (den * den); // Colebrook.
        dif = abs(fat - valHalland);
        valHalland = fat;
        if (dif >= 1e-3)
            goto repeat7;
    } else {                   // regime laminar
        fat = 64. / (reymixL); // 16.
    }

    double rgrl2 = rhog / rhol;
    rgrl2 *= rgrl2;
    double reymix2 = reymixL / 1000;
    reymix2 *= reymix2;
    termo1 = (2 - rgrl2) / (1 + reymix2);
    termo2 = (pow(((1 + rgrl2 * sinal * cos(tet)) / (1 + cos(tet))), (1 - alf) / 5.)) /
             (1 + 1 / reymix2);
    C1 = 0.2; // duto circular ou anular. Retangular seria 0.4.
    C01 = (C1 - C1 * sqrt(rhog / rhol)) * (pow((2.6 - Beta), 0.15) - sqrt(fat)) * pow((1 - MassQuality), 1.5);
    if (ug1 * ul1 < 0.)
        C01 = 0;
    if (tet >= -50 * M_PI / 180. && tet <= 0 && Froude <= 0.1)
        C01 = 0.0;
    c0 = termo1 + termo2 + C01; // Calculo do ParÃƒÂ¢metro de Distribuicao.

    double viscl1 = dia * (fabs(ug1 / A1) + fabs(ul1 / A1)) * rhomix / reymixL;
    termoud1 = (0.35 * sin(tet) + 0.45 * cos(tet) * sinal);
    termoud2 = sqrt((9.81 * dia * (rhol - rhog) / rhol)) * sqrt(1 - alf);
    if (viscl1 / 0.001 > 10) {
        C2 = pow((0.434 / (log10(viscl1 / 0.001))), 0.15);
    } else {
        C2 = 1.0;
    }
    La = sqrt(tensup / (9.81 * (rhol - rhog))) / dia;
    if (La < 0.025) {
        C3 = pow((La / 0.025), 0.90);
    } else {
        C3 = 1.0;
        ;
    }
    C4 = 1.0;
    if (tet >= -(50 * M_PI / 180.) && tet < 0 && Froude <= 0.1)
        C4 = -1.0;
    ud = correcHor * termoud1 * termoud2 * C2 * C3 * C4; // Calculo da Velocidade de Deslizamento.
    if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet > 0. && ud < 0.)
        ud = fabs(ud);
    else if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet < 0. && ud > 0.)
        ud = -fabs(ud);
}

void SProd::Choi(double rhol, double rhog, double tensup, double alf, double reymix, double reymixL,
                 double ug1, double ul1, double dia, double rug, double tet, double &c0,
                 double &ud, double correcHor) {

    double sinal = 1.;
    if (tet < 0.)
        sinal = -1.;
    double A1 = M_PI * dia * dia / 4.;
    ud = correcHor * sinal * 0.0246 * cos(tet) + 1.606 * pow(9.82 * tensup * (rhol - rhog) / (rhol * rhol), 0.25) * sin(tet);
    c0 = 2. / (1 + pow(reymix / 1000., 2.)) + (1.2 - 0.2 * sqrt(rhog / rhol) * (1 - exp(-18 * alf))) / (1 + pow(1000. / reymix, 2.));
    if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet > 0. && ud < 0.)
        ud = fabs(ud);
    else if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet < 0. && ud > 0.)
        ud = -fabs(ud);
}

void SProd::HibikiIshii(double rhol, double rhog, double tensup, double alf, double reymix, double reymixL,
                        double ug1, double ul1, double dia, double rug, double tet, double &c0,
                        double &ud, double correcHor) {

    double sinal = 1.;
    if (tet < 0.) {
        sinal = -1.;
    }
    double A1 = M_PI * dia * dia / 4.;
    c0 = 1. + (1. - alf) / (alf + 4. * sqrt(rhog / rhol));
    ud = (correcHor * sinal * (1. - alf) / (alf + 4. * sqrt(rhog / rhol))) * sqrt(9.82 * fabs(sin(tet)) * dia * (rhol - rhog) * (1. - alf) / (0.015 * rhol));
    if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet > 0. && ud < 0.)
        ud = fabs(ud);
    else if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet < 0. && ud > 0.)
        ud = -fabs(ud);
}

void SProd::FrancaLahey(double rhol, double rhog, double tensup, double alf, double reymix, double reymixL,
                        double ug1, double ul1, double dia, double rug, double tet, double &c0,
                        double &ud, double correcHor) {

    double sinal = 1.;
    if (tet < 0.)
        sinal = -1.;
    c0 = 1.04;
    ud = correcHor * sinal * 0.466;
    double A1 = M_PI * dia * dia / 4.;
    if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet > 0. && ud < 0.)
        ud = fabs(ud);
    else if ((fabs(ug1 / A1) + fabs(ul1 / A1)) < 0.01 && tet < 0. && ud > 0.)
        ud = -fabs(ud);
}

void SProd::C0UdDisperso(double rhol, double rhog, double tensup, double alf, double reymix, double reymixL,
                         double ug1, double ul1, double dia, double rug, double tet, double &c0,
                         double &ud, double correcHor, int estabCol) {

    // case 0:
    switch (arq.CorreDisper) {
    case 0:
        Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet, c0,
             ud, correcHor);
        break;
    case 1:
        BhagwatGhajar(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                      c0, ud, correcHor);
        break;
    case 4:
        BhagwatGhajarMod(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                         c0, ud, correcHor);
        break;
    case 5:
        if (fabs(tet) < 5 * M_PI / 180.)
            BhagwatGhajar(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                          c0, ud, correcHor);
        else if (fabs(tet) > 20 * M_PI / 180.)
            Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet, c0,
                 ud, correcHor);
        else {
            double raz = (fabs(tet) - 5 * M_PI / 180.) / (15 * M_PI / 180.);
            BhagwatGhajarMod(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                             c0, ud, correcHor);
            double c0temp = c0;
            double udtemp = ud;
            Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet, c0,
                 ud, correcHor);
            c0 = raz * c0 + (1. - raz) * c0temp;
            ud = raz * ud + (1. - raz) * udtemp;
        }
        break;
    }
}
void SProd::C0UdAnularChurn(double rhol, double rhog, double tensup, double alf, double reymix, double reymixL,
                            double ug1, double ul1, double dia, double rug, double tet, double &c0,
                            double &ud, double correcHor, int estabCol) {

    switch (arq.CorreAnular) {
    case 3:
        HibikiIshii(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                    c0, ud, correcHor);
        break;
    case 0:
        Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
             c0, ud, correcHor);
        break;
    case 1:
        BhagwatGhajar(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                      c0, ud, correcHor);
        break;
    case 4:
        BhagwatGhajarMod(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                         c0, ud, correcHor);
        break;
    case 5:
        if (fabs(tet) < 5 * M_PI / 180.)
            BhagwatGhajar(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                          c0, ud, correcHor);
        else if (fabs(tet) > 20 * M_PI / 180.)
            Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet, c0,
                 ud, correcHor);
        else {
            double raz = (fabs(tet) - 5 * M_PI / 180.) / (15 * M_PI / 180.);
            BhagwatGhajarMod(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                             c0, ud, correcHor);
            double c0temp = c0;
            double udtemp = ud;
            Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet, c0,
                 ud, correcHor);
            c0 = raz * c0 + (1. - raz) * c0temp;
            ud = raz * ud + (1. - raz) * udtemp;
        }
        break;
    }
}
void SProd::C0UdEstratificado(double rhol, double rhog, double tensup, double alf, double reymix, double reymixL,
                              double ug1, double ul1, double dia, double rug, double tet, double &c0,
                              double &ud, double correcHor, int estabCol) {
    // case 0:
    switch (arq.CorreEstrat) {
    case (2):
        FrancaLahey(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                    c0, ud, correcHor);
        break;
    case (0):
        Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
             c0, ud, correcHor);
        break;
    case (1):
        BhagwatGhajar(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                      c0, ud, correcHor);
        break;
    case (4):
        BhagwatGhajarMod(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                         c0, ud, correcHor);
        break;
    case 5:
        if (fabs(tet) < 5 * M_PI / 180.)
            BhagwatGhajar(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                          c0, ud, correcHor);
        else if (fabs(tet) > 20 * M_PI / 180.)
            Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet, c0,
                 ud, correcHor);
        else {
            double raz = (fabs(tet) - 5 * M_PI / 180.) / (15 * M_PI / 180.);
            BhagwatGhajarMod(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet,
                             c0, ud, correcHor);
            double c0temp = c0;
            double udtemp = ud;
            Choi(rhol, rhog, tensup, alf, reymix, reymixL, ug1, ul1, dia, rug, tet, c0,
                 ud, correcHor);
            c0 = raz * c0 + (1. - raz) * c0temp;
            ud = raz * ud + (1. - raz) * udtemp;
        }
        break;
    }
}

void SProd::CalcC0Ud(int ind, double &c0, double &ud) {
    int timeStep = 20;
    celula[ind].transic0 = celula[ind].transic;
    if (celula[ind].dt < 0.8)
        timeStep *= (0.8 / celula[ind].dt);
    c0 = 1.;
    ud = 0.;
    if (celula[ind - 1].velPig > 0 && celula[ind - 1].estadoPig == 1) {
        c0 = 1.;
        ud = 0.;
        celula[ind].arranjo = 1.;
        celula[ind - 1].arranjoR = 1.;
        celula[ind - 1].perdaEstratL = 0.;
        celula[ind - 1].perdaEstratG = 0.;
    } else if (celula[ind].velPig < 0 && celula[ind].estadoPig == 1) {
        c0 = 1.;
        ud = 0.;
        celula[ind].arranjo = 1.;
        celula[ind - 1].arranjoR = 1.;
        celula[ind - 1].perdaEstratL = 0.;
        celula[ind - 1].perdaEstratG = 0.;
    } else if ((celula[ind].acsr.tipo != 4 || celula[ind].acsr.bcs.freqnova <= 1.)) {
        double hns;
        double razdx = celula[ind].dxL / (celula[ind].dx + celula[ind].dxL);
        double razdx0;
        if (ind > 0)
            razdx0 = celula[ind - 1].dxL / (celula[ind - 1].dx + celula[ind - 1].dxL);
        else
            razdx0 = razdx;

        if (ind > 0)
            hns = 1. - celula[ind - 1].alfPigD;
        else
            hns = 1. - celula[ind].alf;
        if (celula[ind].QG < 0)
            hns = 1. - celula[ind].alfPigE;
        if (fabs(celula[ind].QG) < (*vg1dSP).localtiny * 1e-5) {
            if (fabs(celula[ind].alfPigE) < (*vg1dSP).localtiny && fabs(1. - celula[ind].alfPigE) < (*vg1dSP).localtiny && fabs(celula[ind - 1].alfPigD) > (*vg1dSP).localtiny && fabs(1. - celula[ind - 1].alfPigD) > (*vg1dSP).localtiny)
                hns = 1. - celula[ind - 1].alfPigD;
            else if (fabs(celula[ind - 1].alfPigD) < (*vg1dSP).localtiny && fabs(1. - celula[ind - 1].alfPigD) < (*vg1dSP).localtiny && fabs(celula[ind].alfPigE) > (*vg1dSP).localtiny && fabs(1. - celula[ind].alfPigE) > (*vg1dSP).localtiny)
                hns = 1. - celula[ind].alfPigE;
            else
                hns = 0.5 * (1. - celula[ind].alfPigE + 1. - celula[ind - 1].alfPigD);
        }
        if (hns < (*vg1dSP).localtiny || hns > 1. - (*vg1dSP).localtiny)
            hns = 0.5 * (1. - celula[ind].alfPigE + 1. - celula[ind - 1].alfPigD);

        double hol0 = hns;
        double alf0 = 1 - hol0;
        double alf1;
        alf1 = celula[ind].alf;

        double alfneg;
        if (ind > 1)
            alfneg = celula[ind - 2].alf;
        else if (ind > 0)
            alfneg = celula[ind - 1].alf;
        else
            alfneg = celula[ind].alf;

        double betI = celula[ind].betL;
        if (ind > 0)
            betI = celula[ind - 1].betPigD;
        if (((0. * celula[ind].QG + 1 * celula[ind].QL) < 0.))
            betI = celula[ind].betPigE; // duvidabeta

        double betneg;
        if (ind > 0) {
            betneg = celula[ind - 1].betL;
            if (ind > 1)
                betneg = celula[ind - 2].betPigD;
            if ((0.99 * celula[ind - 1].QG + 0.01 * celula[ind - 1].QL) < 0.)
                betneg = celula[ind - 1].betPigE; // duvidabeta

        } else
            betneg = celula[ind].bet;

        double pmed;
        double pmed0 = 0.;

        pmed = celula[ind].presaux;
        if (ind > 0)
            pmed0 = celula[ind - 1].presaux;
        if (ind == ncel)
            pmed = celula[ind].pres;
        else
            pmed0 = celula[ind].presaux;
        double tmed = razdx * celula[ind].temp + (1 - razdx) * celula[ind].tempL;
        tmed = celula[ind].tempL;
        if (celula[ind].VTemper < 0.)
            tmed = celula[ind].temp;
        double tmed0;
        if (ind > 0)
            tmed0 = razdx0 * celula[ind - 1].temp + (1 - razdx0) * celula[ind - 1].tempL;
        else
            tmed0 = tmed;
        if (ind < ncel)
            tmed = celula[ind].temp;
        else
            tmed = tGSup;

        double correcHor = 1.;
        if (fabs(celula[ind].duto.teta) < 1e-10) {
            if (celula[ind - 1].acsr.tipo != 5 || celula[ind - 1].acsr.chk.AreaGarg > 1e-10) {
                if (celula[ind].angEsq < 0 && celula[ind].angDir < 0)
                    correcHor = -1.;
                else if (celula[ind].angEsq > 0 && celula[ind].angDir > 0)
                    correcHor = 1.;
            } else {
                if (celula[ind].angDir < 0)
                    correcHor = -1.;
                else if (celula[ind].angDir > 0)
                    correcHor = 1.;
            }
        }

        double rlm;
        double viscl1;
        double tensup1;
        if (celula[ind].QL < 0.) { // testeBeta
            if (ind == 0 || ind == ncel)
                rlm = (1 - betI) * celula[ind].flui.MasEspLiq(pmed, tmed) + betI * celula[ind].fluicol.MasEspFlu(pmed, tmed);
            else
                rlm = (1 - betI) * celula[ind].rpCi + betI * celula[ind].rcCi;
            viscl1 = (1 - betI) * celula[ind].flui.ViscOleo(pmed, tmed) + betI * celula[ind].fluicol.VisFlu(pmed, tmed);
            tensup1 = (1 - betI) * celula[ind].flui.TensSuper(pmed, tmed) + betI * celula[ind].fluicol.TensSuper(pmed, tmed);
        } else {
            if (ind == 0 || ind == ncel)
                rlm = (1 - betI) * celula[ind - 1].flui.MasEspLiq(pmed, tmed) + betI * celula[ind - 1].fluicol.MasEspFlu(pmed, tmed);
            else
                rlm = (1 - betI) * celula[ind].rpCi + betI * celula[ind - 1].rcCi;
            viscl1 = (1 - betI) * celula[ind - 1].flui.ViscOleo(pmed, tmed) + betI * celula[ind - 1].fluicol.VisFlu(pmed, tmed);
            tensup1 = (1 - betI) * celula[ind - 1].flui.TensSuper(pmed, tmed) + betI * celula[ind - 1].fluicol.TensSuper(pmed, tmed);
        }

        double rgm;
        double viscg1;
        if (celula[ind].QG < 0.) {
            if (ind == 0 || ind == ncel)
                rgm = celula[ind].flui.MasEspGas(pmed, tmed);
            else
                rgm = celula[ind].rgCi;
            viscg1 = celula[ind].flui.ViscGas(pmed, tmed);
        } else {
            if (ind == 0 || ind == ncel)
                rgm = celula[ind - 1].flui.MasEspGas(pmed, tmed);
            else
                rgm = celula[ind].rgCi;
            viscg1 = celula[ind - 1].flui.ViscGas(pmed, tmed);
        }

        double ug1 = (celula[ind].MC - celula[ind].Mliqini) / rgm;
        double ul1 = celula[ind].Mliqini / rlm;
        double dia1 = celula[ind].duto.a;
        if (ind > 0 && ug1 >= 0)
            dia1 = celula[ind - 1].duto.a;

        double A1 = M_PI * dia1 * dia1 / 4.;

        double rmed = hns * rlm + (1 - hns) * rgm;
        double visc = (hns * viscl1 + (1 - hns) * viscg1) / pow(10., 3.);
        double nrey = dia1 * rmed * (fabs(ug1) / A1 + fabs(ul1) / A1) / visc;
        double nreyl = dia1 * rlm * (fabs(ug1) / A1 + fabs(ul1) / A1) / (viscl1 / 1000.);

        int xarr1 = 1;
        double dtot = celula[ind].dxL + celula[ind].dx;
        double razL = celula[ind].dx;
        double raz = celula[ind].dxL;
        double ang = (razL * celula[ind].dutoL.teta + raz * celula[ind].duto.teta) / dtot;
        if (ind >= 2) {
            if (celula[ind - 2].acsr.tipo == 5 && celula[ind - 2].acsr.chk.AreaGarg <= (1e-3)) {
                if (celula[ind].QG >= 0)
                    ang = celula[ind].duto.teta;
                else
                    ang = celula[ind].dutoR.teta;
            } else {
                if (celula[ind].QG >= 0)
                    ang = celula[ind].dutoL.teta;
                else
                    ang = celula[ind].duto.teta;
            }
        }

        double atenua = 20;

        if (nrey > 1e-30) {
            if (fabs(0 * ang + 1 * celula[ind].duto.teta) < 45. * M_PI / 180. && hol0 < 0.99 && hol0 > 0.01 && ind < ncel - 1) {
                double ug0;
                double ul0;

                ug1 = (celula[ind].MC - celula[ind].Mliqini) / rgm;
                ul1 = celula[ind].Mliqini / rlm;

                ug0 = (celula[ind - 1].MC - celula[ind - 1].Mliqini) / celula[ind].rgLi;
                // ul0 = celula[ind - 1].Mliqini
                //  / ((1 - betneg) * celula[ind].flui.MasEspLiq(pmed0, tmed0)
                ul0 = celula[ind - 1].Mliqini / ((1 - betneg) * celula[ind].rpLi + betneg * celula[ind].rcLi);

                estratificado testamapa(dia1, ul1, ug1, rlm, rgm, viscl1 / pow(10., 3.), viscg1 / pow(10., 3.), hol0,
                                        celula[ind].duto.teta, celula[ind].duto.rug / dia1);

                if (arq.CorreEstrat == 2)
                    testamapa.mapaTD();
                else
                    testamapa.mapaTD(1);

                xarr1 = testamapa.arr;

                if (xarr1 == -1) {
                    if (celula[ind].arranjo != 0) {
                        if (((celula[ind].arranjo != xarr1) || celula[ind].transic > 0)) {
                            if ((celula[ind].arranjo != xarr1) && celula[ind].transic > 0)
                                celula[ind].transic = 0;
                            celula[ind].transic++;
                            if (celula[ind].transic > atenua - 1)
                                celula[ind].transic = 0;
                        } else
                            celula[ind].transic = 0;
                    }
                    celula[ind].arranjo = xarr1 = testamapa.arr;
                    celula[ind - 1].arranjoR = testamapa.arr;
                    celula[ind - 1].perdaEstratL = testamapa.fatorperdaLiq;
                    celula[ind - 1].perdaEstratG = testamapa.fatorperdaGas;
                    double c0D;
                    double udD;
                    double c0E;
                    double udE;

                    C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                 c0D, udD, correcHor, celula[ind].estabCol);
                    C0UdEstratificado(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                      c0E, udE, correcHor, celula[ind].estabCol);

                    double mult0, mult1;
                    mult0 = 1.;
                    if (ul0 < 0.)
                        mult0 = 0.;
                    mult1 = 0.;
                    if (ul1 < 0.)
                        mult1 = 1.;
                    double alf0E = celula[ind - 1].alf;

                    double jmax = 0.05;
                    double jmin = 0.005;
                    if ((fabs(ug1) + fabs(ul1)) / A1 > jmax) {
                        c0 = c0E;
                        ud = udE;
                    } else if ((fabs(ug1) + fabs(ul1)) / A1 < jmin) {
                        c0 = c0D;
                        ud = udD;
                    } else {
                        double raz = (jmax - (fabs(ug1) + fabs(ul1)) / A1) / (jmax - jmin);
                        c0 = ((1. - raz) * c0E + raz * c0D);
                        ud = ((1. - raz) * udE + raz * udD);
                    }

                    if (celula[ind].transic > 0) {
                        c0 = (c0 * celula[ind].transic + celula[ind].c0 * (atenua - celula[ind].transic)) / atenua;
                        ud = (ud * celula[ind].transic + celula[ind].ud * (atenua - celula[ind].transic)) / atenua;
                    }
                }
            }
            if (xarr1 == 1) {

                arranjo testamapa2(dia1, ul1 / A1, ug1 / A1, rlm, rgm, viscl1 / pow(10., 3.), viscg1 / pow(10., 3.), hol0,
                                   celula[ind].duto.teta, tensup1, arq.mapaArranjo, vg1dSP);
                xarr1 = testamapa2.verificaArr();

                C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                             c0, ud, correcHor, celula[ind].estabCol);

                if (xarr1 == -2) {
                    C0UdAnularChurn(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                    c0, ud, correcHor, celula[ind].estabCol);
                }
                if (fabs(ug1 / celula[ind].duto.area) > 5. && alf0 >= 0.75) {
                    atenua = 20;
                    if (arq.CorreAnular == 3 && arq.CorreDisper == 1)
                        atenua = 200;
                }

                if (celula[ind].arranjo != 0) {
                    if ((xarr1 != celula[ind].arranjo || celula[ind].transic > 0)) {
                        if (xarr1 != celula[ind].arranjo && celula[ind].transic > 0)
                            celula[ind].transic = 0;
                        celula[ind].transic++;
                        if (celula[ind].transic > atenua - 1)
                            celula[ind].transic = 0;
                    } else
                        celula[ind].transic = 0;
                }
                if (celula[ind].transic > 0) {
                    c0 = (c0 * celula[ind].transic + celula[ind].c0 * (atenua - celula[ind].transic)) / atenua;
                    ud = (ud * celula[ind].transic + celula[ind].ud * (atenua - celula[ind].transic)) / atenua;
                }
                celula[ind].arranjo = xarr1;
                celula[ind - 1].arranjoR = xarr1;
            }
            celula[ind].c0Spare = c0;
            celula[ind].udSpare = ud;
        }
    }
    if (arq.escorregaTran == 0) {
        double ulsmed = celula[ind].QL / celula[ind].duto.area;
        double correcaoUd = 1 - (ulsmed - 0.15) / 0.35;
        double correcaoCo = c0 - (c0 - 1) * (ulsmed - 0.15) / 0.35;
        if (correcaoUd > 1.)
            correcaoUd = 1.;
        if (correcaoUd < 0.)
            correcaoUd = 0.;
        if (correcaoCo > c0)
            correcaoCo = c0;
        if (correcaoCo < 1)
            correcaoCo = 1;
        c0 = 1. + 0 * correcaoCo;
        ud = 0. + 0. * ud * correcaoUd;
    }
}

void SProd::CalcC0UdBuf(int ind, double &c0, double &ud) {
    int timeStep = 20;
    if (celula[ind].dt < 0.8)
        timeStep *= (0.8 / celula[ind].dt);
    c0 = 1.;
    ud = 0.;
    if (celula[ind - 1].velPig > 0 && celula[ind - 1].estadoPig == 1) {
        c0 = 1.;
        ud = 0.;
        celula[ind].arranjo = 1.;
        celula[ind - 1].arranjoR = 1.;
        celula[ind - 1].perdaEstratL = 0.;
        celula[ind - 1].perdaEstratG = 0.;
    } else if (celula[ind].velPig < 0 && celula[ind].estadoPig == 1) {
        c0 = 1.;
        ud = 0.;
        celula[ind].arranjo = 1.;
        celula[ind - 1].arranjoR = 1.;
        celula[ind - 1].perdaEstratL = 0.;
        celula[ind - 1].perdaEstratG = 0.;
    } else if ((celula[ind].acsr.tipo != 4 || celula[ind].acsr.bcs.freqnova <= 1.)) {
        double hns;
        double razdx = celula[ind].dxL / (celula[ind].dx + celula[ind].dxL);
        double razdx0;
        if (ind > 0)
            razdx0 = celula[ind - 1].dxL / (celula[ind - 1].dx + celula[ind - 1].dxL);
        else
            razdx0 = razdx;

        if (ind > 0)
            hns = 1. - celula[ind - 1].alfPigD;
        else
            hns = 1. - celula[ind].alf;
        if ((celula[ind].MCBuf - celula[ind].MliqiniBuf) < 0)
            hns = 1. - celula[ind].alfPigE;
        if (fabs((celula[ind].MCBuf - celula[ind].MliqiniBuf)) < (*vg1dSP).localtiny * 1e-5) {
            if (fabs(celula[ind].alfPigE) < (*vg1dSP).localtiny && fabs(1. - celula[ind].alfPigE) < (*vg1dSP).localtiny && fabs(celula[ind - 1].alfPigD) > (*vg1dSP).localtiny && fabs(1. - celula[ind - 1].alfPigD) > (*vg1dSP).localtiny)
                hns = 1. - celula[ind - 1].alfPigD;
            else if (fabs(celula[ind - 1].alfPigD) < (*vg1dSP).localtiny && fabs(1. - celula[ind - 1].alfPigD) < (*vg1dSP).localtiny && fabs(celula[ind].alfPigE) > (*vg1dSP).localtiny && fabs(1. - celula[ind].alfPigE) > (*vg1dSP).localtiny)
                hns = 1. - celula[ind].alfPigE;
            else
                hns = 0.5 * (1. - celula[ind].alfPigE + 1. - celula[ind - 1].alfPigD);
        }
        if (hns < (*vg1dSP).localtiny || hns > 1. - (*vg1dSP).localtiny)
            hns = 0.5 * (1. - celula[ind].alfPigE + 1. - celula[ind - 1].alfPigD);

        double hol0 = hns;
        double alf0 = 1 - hol0;
        double alf1;
        alf1 = celula[ind].alf;

        double alfneg;
        if (ind > 1)
            alfneg = celula[ind - 2].alf;
        else if (ind > 0)
            alfneg = celula[ind - 1].alf;
        else
            alfneg = celula[ind].alf;

        double betI = celula[ind].betL;
        if (ind > 0)
            betI = celula[ind - 1].betPigD;
        if ((celula[ind].MliqiniBuf) < 0.)
            betI = celula[ind].betPigE; // testeBeta
        betI = celula[ind].betPigE;     // duvidabeta
        double betneg;
        if (ind > 0) {
            betneg = celula[ind - 1].betL;
            if (ind > 1)
                betneg = celula[ind - 2].betPigD;
            if (celula[ind].MliqiniLBuf < 0.)
                betneg = celula[ind - 1].betPigE; // testeBeta
            betneg = celula[ind - 1].betPigE;     // duvidabeta
        } else
            betneg = celula[ind].bet;

        double pmed;
        double pmed0 = 0.;

        pmed = razdx * celula[ind].presBuf + (1 - razdx) * celula[ind].presLBuf;
        if (ind == ncel)
            pmed = celula[ind].presBuf;
        pmed0 = celula[ind].presauxL;
        double tmed = razdx * celula[ind].temp + (1 - razdx) * celula[ind].tempL;
        tmed = celula[ind].tempL;
        if (celula[ind].VTemper < 0.) {
            if (ind < ncel)
                tmed = celula[ind].temp;
            else
                tmed = tGSup;
        }
        double tmed0;
        if (ind > 0)
            tmed0 = razdx0 * celula[ind - 1].temp + (1 - razdx0) * celula[ind - 1].tempL;
        else
            tmed0 = tmed;

        double correcHor = 1.;
        if (fabs(celula[ind].duto.teta) < 1e-10) {
            if (celula[ind].acsr.tipo != 5 || celula[ind].acsr.chk.AreaGarg > 1e-10) {
                if (celula[ind].angEsq < 0 && celula[ind].angDir < 0)
                    correcHor = -1.;
                else if (celula[ind].angEsq > 0 && celula[ind].angDir > 0)
                    correcHor = 1.;
            } else {
                if (celula[ind].angDir < 0)
                    correcHor = -1.;
                else if (celula[ind].angDir > 0)
                    correcHor = 1.;
            }
        }

        double rlm;
        double viscl1;
        double tensup1;
        if ((celula[ind].MliqiniBuf) < 0.) { // testeBeta
            rlm = (1 - betI) * celula[ind].flui.MasEspLiq(pmed, tmed) + betI * celula[ind].fluicol.MasEspFlu(pmed, tmed);
            viscl1 = (1 - betI) * celula[ind].flui.ViscOleo(pmed, tmed) + betI * celula[ind].fluicol.VisFlu(pmed, tmed);
            tensup1 = (1 - betI) * celula[ind].flui.TensSuper(pmed, tmed) + betI * celula[ind].fluicol.TensSuper(pmed, tmed);
        } else {
            rlm = (1 - betI) * celula[ind - 1].flui.MasEspLiq(pmed, tmed) + betI * celula[ind - 1].fluicol.MasEspFlu(pmed, tmed);
            viscl1 = (1 - betI) * celula[ind - 1].flui.ViscOleo(pmed, tmed) + betI * celula[ind - 1].fluicol.VisFlu(pmed, tmed);
            tensup1 = (1 - betI) * celula[ind - 1].flui.TensSuper(pmed, tmed) + betI * celula[ind - 1].fluicol.TensSuper(pmed, tmed);
        }

        double rgm;
        double viscg1;
        if ((celula[ind].MCBuf - celula[ind].MliqiniBuf) < 0.) {
            rgm = celula[ind].flui.MasEspGas(pmed, tmed);
            viscg1 = celula[ind].flui.ViscGas(pmed, tmed);
        } else {
            rgm = celula[ind - 1].flui.MasEspGas(pmed, tmed);
            viscg1 = celula[ind - 1].flui.ViscGas(pmed, tmed);
        }

        double ug1 = (celula[ind].MCBuf - celula[ind].MliqiniBuf) / rgm;
        double ul1 = (celula[ind].MliqiniBuf) / rlm;
        double dia1 = celula[ind].duto.a;
        if (ind > 0 && ug1 >= 0)
            dia1 = celula[ind - 1].duto.a;
        double A1 = M_PI * dia1 * dia1 / 4.;

        double rmed = hns * rlm + (1 - hns) * rgm;
        double visc = (hns * viscl1 + (1 - hns) * viscg1) / pow(10., 3.);
        double nrey = dia1 * rmed * (fabs(ug1) / A1 + fabs(ul1) / A1) / visc;
        double nreyl = dia1 * rlm * (fabs(ug1) / A1 + fabs(ul1) / A1) / (viscl1 / 1000.);

        int xarr1 = 1;
        double dtot = celula[ind].dxL + celula[ind].dx;
        double razL = celula[ind].dxL;
        double raz = celula[ind].dx;
        double ang = (raz * celula[ind].dutoL.teta + razL * celula[ind].duto.teta) / dtot;
        if (ind >= 2) {
            if (celula[ind - 2].acsr.tipo == 5 && celula[ind - 2].acsr.chk.AreaGarg <= (1e-3)) {
                if ((celula[ind].MCBuf - celula[ind].MliqiniBuf) >= 0)
                    ang = celula[ind].duto.teta;
                else
                    ang = celula[ind].dutoR.teta;
            } else {
                if ((celula[ind].MCBuf - celula[ind].MliqiniBuf) >= 0)
                    ang = celula[ind].dutoL.teta;
                else
                    ang = celula[ind].duto.teta;
            }
        }
        double atenua = 20;
        if (nrey > 1e-30) {
            if (fabs(0 * ang + 1 * celula[ind].duto.teta) < 45. * M_PI / 180. && hol0 < 0.99 && hol0 > 0.01 && ind < ncel - 1) {
                double ug0;
                double ul0;

                ug1 = (celula[ind].MCBuf - celula[ind].MliqiniBuf) / rgm;
                ul1 = (celula[ind].MliqiniBuf) / rlm;

                ug0 = (celula[ind - 1].MCBuf - celula[ind - 1].MliqiniBuf) / celula[ind].flui.MasEspGas(pmed0, tmed0);
                ul0 = (celula[ind - 1].MliqiniBuf) / ((1 - betneg) * celula[ind].flui.MasEspLiq(pmed0, tmed0) + betneg * celula[ind].fluicol.MasEspFlu(pmed0, tmed0));

                xarr1 = celula[ind].arranjo;
                if (xarr1 == -1) {

                    double c0D;
                    double udD;
                    double c0E;
                    double udE;
                    C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                 c0D, udD, correcHor, celula[ind].estabCol);
                    C0UdEstratificado(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                      c0E, udE, correcHor, celula[ind].estabCol);

                    double mult0, mult1;
                    mult0 = 1.;
                    if (ul0 < 0.)
                        mult0 = 0.;
                    mult1 = 0.;
                    if (ul1 < 0.)
                        mult1 = 1.;
                    double alf0E = celula[ind - 1].alf;

                    double jmax = 0.05;
                    double jmin = 0.005;
                    if ((fabs(ug1) + fabs(ul1)) / A1 > jmax) {
                        c0 = c0E;
                        ud = udE;
                    } else if ((fabs(ug1) + fabs(ul1)) / A1 < jmin) {
                        c0 = c0D;
                        ud = udD;
                    } else {
                        double raz = (jmax - (fabs(ug1) + fabs(ul1)) / A1) / (jmax - jmin);
                        c0 = ((1. - raz) * c0E + raz * c0D);
                        ud = ((1. - raz) * udE + raz * udD);
                    }

                    if (celula[ind].transic > 0) {
                        c0 = (c0 * celula[ind].transic + celula[ind].c0 * (atenua - celula[ind].transic)) / atenua;
                        ud = (ud * celula[ind].transic + celula[ind].ud * (atenua - celula[ind].transic)) / atenua;
                    }
                }
            }
            if (xarr1 != -1) {

                C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                             c0, ud, correcHor, celula[ind].estabCol);
                if (xarr1 == -2) {
                    C0UdAnularChurn(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                    c0, ud, correcHor, celula[ind].estabCol);
                }
                if (fabs(ug1 / celula[ind].duto.area) > 5. && alf0 >= 0.75) {
                    atenua = 20;
                    if (arq.CorreAnular == 3 && arq.CorreDisper == 1)
                        atenua = 200;
                }

                if (celula[ind].transic > 0) {
                    c0 = (c0 * celula[ind].transic + celula[ind].c0 * (atenua - celula[ind].transic)) / atenua;
                    ud = (ud * celula[ind].transic + celula[ind].ud * (atenua - celula[ind].transic)) / atenua;
                }
                celula[ind].arranjo = xarr1;
                celula[ind - 1].arranjoR = xarr1;
            }
            celula[ind].c0Spare = c0;
            celula[ind].udSpare = ud;
        }
    }
    if (arq.escorregaTran == 0) {
        double ulsmed = celula[ind].QL / celula[ind].duto.area;
        double correcaoUd = 1 - (ulsmed - 0.15) / 0.35;
        double correcaoCo = c0 - (c0 - 1) * (ulsmed - 0.15) / 0.35;
        if (correcaoUd > 1.)
            correcaoUd = 1.;
        if (correcaoUd < 0.)
            correcaoUd = 0.;
        if (correcaoCo > c0)
            correcaoCo = c0;
        if (correcaoCo < 1)
            correcaoCo = 1;
        c0 = 1. + 0 * correcaoCo;
        ud = 0. + 0 * ud * correcaoUd;
    }
}

void SProd::CalcC0UdIni(int ind, double &c0, double &ud) {
    int timeStep = 20;
    if (celula[ind].dt < 0.8)
        timeStep *= (0.8 / celula[ind].dt);
    c0 = 1.;
    ud = 0.;
    if (celula[ind].velPig < 0 && celula[ind].estadoPig == 1) {
        c0 = 1.;
        ud = 0.;
        celula[ind].arranjo = 1.;

    } else if ((celula[ind].acsr.tipo != 4 || celula[ind].acsr.bcs.freqnova <= 1.)) {
        double hns;

        hns = 1 - alfE;

        if (celula[ind].QG < 0)
            hns = 1. - celula[ind].alfPigE;
        if (fabs(celula[ind].QG) < (*vg1dSP).localtiny * 1e-5) {
            if (fabs(celula[ind].alfPigE) < (*vg1dSP).localtiny && fabs(1. - celula[ind].alfPigE) < (*vg1dSP).localtiny && fabs(alfE) > (*vg1dSP).localtiny && fabs(1. - alfE) > (*vg1dSP).localtiny)
                hns = 1 - alfE;
            else if (fabs(alfE) < (*vg1dSP).localtiny && fabs(1. - celula[ind - 1].alfPigD) < (*vg1dSP).localtiny && fabs(celula[ind].alfPigE) > (*vg1dSP).localtiny && fabs(1. - celula[ind].alfPigE) > (*vg1dSP).localtiny)
                hns = 1. - celula[ind].alfPigE;
            else
                hns = 0.5 * (1. - celula[ind].alfPigE + 1. - alfE);
        }
        if (hns < (*vg1dSP).localtiny || hns > 1. - (*vg1dSP).localtiny)
            hns = 0.5 * (1. - celula[ind].alfPigE + 1. - alfE);

        double hol0 = hns;
        double alf0 = 1 - hol0;
        double alf1;
        alf1 = celula[ind].alf;

        double alfneg;
        if (ind > 1)
            alfneg = alfE;
        else if (ind > 0)
            alfneg = alfE;
        else
            alfneg = celula[ind].alf;

        double betI = celula[ind].betL;
        if (ind > 0)
            betI = betaE;
        if (celula[ind].QL < 0.)
            betI = celula[ind].betPigE; // testeBeta
        betI = celula[ind].betPigE;     // duvidabeta
        double betneg;
        if (ind > 0) {
            betneg = betaE;

        } else
            betneg = celula[ind].bet;

        double pmed;
        double pmed0 = 0.;

        pmed = presE;
        if (ind > 0)
            pmed0 = presE;
        if (ind == ncel)
            pmed = celula[ind].pres;
        else
            pmed0 = presE;
        double tmed = tempE;

        double tmed0;
        tmed0 = tmed;

        double correcHor = 1.;
        if (fabs(celula[ind].duto.teta) < 1e-10) {
            if (celula[ind].acsr.tipo != 5 || celula[ind].acsr.chk.AreaGarg > 1e-10) {
                if (celula[ind].angEsq < 0 && celula[ind].angDir < 0)
                    correcHor = -1.;
                else if (celula[ind].angEsq > 0 && celula[ind].angDir > 0)
                    correcHor = 1.;
            } else {
                if (celula[ind].angDir < 0)
                    correcHor = -1.;
                else if (celula[ind].angDir > 0)
                    correcHor = 1.;
            }
        }

        double rlm;
        double viscl1;
        double tensup1;
        if (celula[ind].QL < 0.) { // testeBeta
            rlm = (1 - betI) * celula[ind].flui.MasEspLiq(pmed, tmed) + betI * celula[ind].fluicol.MasEspFlu(pmed, tmed);
            viscl1 = (1 - betI) * celula[ind].flui.ViscOleo(pmed, tmed) + betI * celula[ind].fluicol.VisFlu(pmed, tmed);
            tensup1 = (1 - betI) * celula[ind].flui.TensSuper(pmed, tmed) + betI * celula[ind].fluicol.TensSuper(pmed, tmed);
        } else {
            rlm = (1 - betI) * (*celula[ind].fluiL).MasEspLiq(pmed, tmed) + betI * celula[ind].fluicol.MasEspFlu(pmed, tmed);
            viscl1 = (1 - betI) * (*celula[ind].fluiL).ViscOleo(pmed, tmed) + betI * celula[ind].fluicol.VisFlu(pmed, tmed);
            tensup1 = (1 - betI) * (*celula[ind].fluiL).TensSuper(pmed, tmed) + betI * celula[ind].fluicol.TensSuper(pmed, tmed);
        }

        double rgm;
        double viscg1;
        if (celula[ind].QG < 0.) {
            rgm = celula[ind].flui.MasEspGas(pmed, tmed);
            viscg1 = celula[ind].flui.ViscGas(pmed, tmed);
        } else {
            rgm = (*celula[ind].fluiL).MasEspGas(pmed, tmed);
            viscg1 = (*celula[ind].fluiL).ViscGas(pmed, tmed);
        }

        double ug1 = (celula[ind].MC - celula[ind].Mliqini) / rgm;
        double ul1 = celula[ind].Mliqini / rlm;
        double dia1 = celula[ind].duto.a;
        if (ind > 0 && ug1 >= 0)
            dia1 = celula[ind - 1].duto.a;
        double A1 = M_PI * dia1 * dia1 / 4.;

        double rmed = hns * rlm + (1 - hns) * rgm;
        double visc = (hns * viscl1 + (1 - hns) * viscg1) / pow(10., 3.);
        double nrey = dia1 * rmed * (fabs(ug1) / A1 + fabs(ul1) / A1) / visc;
        double nreyl = dia1 * rlm * (fabs(ug1) / A1 + fabs(ul1) / A1) / (viscl1 / 1000.);

        int xarr1 = 1;
        double dtot = celula[ind].dxL + celula[ind].dx;
        double razL = celula[ind].dxL;
        double raz = celula[ind].dx;
        double ang = (raz * celula[ind].dutoL.teta + razL * celula[ind].duto.teta) / dtot;
        double atenua = 20.;
        if (nrey > 1e-30) {
            if (fabs(0 * ang + 1 * celula[ind].duto.teta) < 45. * M_PI / 180. && hol0 < 0.99 && hol0 > 0.01 && ind < ncel - 1) {
                double ug0;
                double ul0;

                ug1 = (celula[ind].MC - celula[ind].Mliqini) / rgm;
                ul1 = celula[ind].Mliqini / rlm;

                ug0 = (celula[ind].MC - celula[ind].Mliqini) / (*celula[ind].fluiL).MasEspGas(pmed0, tmed0);
                ul0 = celula[ind].Mliqini / ((1 - betneg) * (*celula[ind].fluiL).MasEspLiq(pmed0, tmed0) + betneg * celula[ind].fluicol.MasEspFlu(pmed0, tmed0));

                estratificado testamapa(dia1, ul1, ug1, rlm, rgm, viscl1 / pow(10., 3.), viscg1 / pow(10., 3.), hol0,
                                        celula[ind].duto.teta, celula[ind].duto.rug / dia1);

                testamapa.mapaTD();
                xarr1 = testamapa.arr;
                if (xarr1 == -1) {
                    if (celula[ind].arranjo != 0) {
                        if (((celula[ind].arranjo != xarr1) || celula[ind].transic > 0)) {
                            if ((celula[ind].arranjo != xarr1) && celula[ind].transic > 0)
                                celula[ind].transic = 0;
                            celula[ind].transic++;
                            if (celula[ind].transic > 19)
                                celula[ind].transic = 0;
                        }
                    } else
                        celula[ind].transic = 0;
                    celula[ind].arranjo = xarr1 = testamapa.arr;
                    double c0D;
                    double udD;
                    double c0E;
                    double udE;
                    C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                 c0D, udD, correcHor, celula[ind].estabCol);
                    C0UdEstratificado(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                      c0E, udE, correcHor, celula[ind].estabCol);
                    double mult0, mult1;
                    mult0 = 1.;
                    if (ul0 < 0.)
                        mult0 = 0.;
                    mult1 = 0.;
                    if (ul1 < 0.)
                        mult1 = 1.;
                    double alf0E = alfE;

                    double jmax = 0.05;
                    double jmin = 0.005;
                    if ((fabs(ug1) + fabs(ul1)) / A1 > jmax) {
                        c0 = c0E;
                        ud = udE;
                    } else if ((fabs(ug1) + fabs(ul1)) / A1 < jmin) {
                        c0 = c0D;
                        ud = udD;
                    } else {
                        double raz = (jmax - (fabs(ug1) + fabs(ul1)) / A1) / (jmax - jmin);
                        c0 = ((1. - raz) * c0E + raz * c0D);
                        ud = ((1. - raz) * udE + raz * udD);
                    }

                    if (celula[ind].transic > 0) {
                        c0 = (c0 * celula[ind].transic + celula[ind].c0 * (atenua - celula[ind].transic)) / atenua;
                        ud = (ud * celula[ind].transic + celula[ind].ud * (atenua - celula[ind].transic)) / atenua;
                    }
                }
            }
            if (xarr1 == 1) {

                arranjo testamapa2(dia1, ul1 / A1, ug1 / A1, rlm, rgm, viscl1 / pow(10., 3.), viscg1 / pow(10., 3.), hol0,
                                   celula[ind].duto.teta, tensup1, arq.mapaArranjo, vg1dSP);
                xarr1 = testamapa2.verificaArr();

                C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                             c0, ud, correcHor, celula[ind].estabCol);
                if (xarr1 == -2) {
                    C0UdAnularChurn(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                    c0, ud, correcHor, celula[ind].estabCol);
                }

                if (fabs(ug1 / celula[ind].duto.area) > 5. && alf0 >= 0.75) {
                    atenua = 20;
                    if (arq.CorreAnular == 3 && arq.CorreDisper == 1)
                        atenua = 200;
                }
                if (celula[ind].arranjo != 0) {
                    if ((xarr1 != celula[ind].arranjo || celula[ind].transic > 0)) {
                        if (xarr1 != celula[ind].arranjo && celula[ind].transic > 0)
                            celula[ind].transic = 0;
                        celula[ind].transic++;
                        if (celula[ind].transic > atenua - 1)
                            celula[ind].transic = 0;
                    }
                } else
                    celula[ind].transic = 0;
                if (celula[ind].transic > 0) {
                    c0 = (c0 * celula[ind].transic + celula[ind].c0 * (atenua - celula[ind].transic)) / atenua;
                    ud = (ud * celula[ind].transic + celula[ind].ud * (atenua - celula[ind].transic)) / atenua;
                }
                celula[ind].arranjo = xarr1;
            }
            celula[ind].c0Spare = c0;
            celula[ind].udSpare = ud;
        }
    }
    if (arq.escorregaTran == 0) {
        double ulsmed = celula[ind].QL / celula[ind].duto.area;
        double correcaoUd = 1 - (ulsmed - 0.15) / 0.35;
        double correcaoCo = c0 - (c0 - 1) * (ulsmed - 0.15) / 0.35;
        if (correcaoUd > 1.)
            correcaoUd = 1.;
        if (correcaoUd < 0.)
            correcaoUd = 0.;
        if (correcaoCo > c0)
            correcaoCo = c0;
        if (correcaoCo < 1)
            correcaoCo = 1;
        c0 = 1. + 0 * correcaoCo;
        ud = 0. + 0 * ud * correcaoUd;
    }
}

void SProd::CalcC0UdIniBuf(int ind, double &c0, double &ud) {
    int timeStep = 20;
    if (celula[ind].dt < 0.8)
        timeStep *= (0.8 / celula[ind].dt);
    c0 = 1.;
    ud = 0.;
    if (celula[ind].velPig < 0 && celula[ind].estadoPig == 1) {
        c0 = 1.;
        ud = 0.;
        celula[ind].arranjo = 1.;

    } else if ((celula[ind].acsr.tipo != 4 || celula[ind].acsr.bcs.freqnova <= 1.)) {
        double hns;

        hns = 1 - alfE;

        if ((celula[ind].MCBuf - celula[ind].MliqiniBuf) < 0)
            hns = 1. - celula[ind].alfPigE;
        if (fabs(celula[ind].MCBuf - celula[ind].MliqiniBuf) < (*vg1dSP).localtiny * 1e-5) {
            if (fabs(celula[ind].alfPigE) < (*vg1dSP).localtiny && fabs(1. - celula[ind].alfPigE) < (*vg1dSP).localtiny && fabs(alfE) > (*vg1dSP).localtiny && fabs(1. - alfE) > (*vg1dSP).localtiny)
                hns = 1 - alfE;
            else if (fabs(alfE) < (*vg1dSP).localtiny && fabs(1. - celula[ind - 1].alfPigD) < (*vg1dSP).localtiny && fabs(celula[ind].alfPigE) > (*vg1dSP).localtiny && fabs(1. - celula[ind].alfPigE) > (*vg1dSP).localtiny)
                hns = 1. - celula[ind].alfPigE;
            else
                hns = 0.5 * (1. - celula[ind].alfPigE + 1. - alfE);
        }
        if (hns < (*vg1dSP).localtiny || hns > 1. - (*vg1dSP).localtiny)
            hns = 0.5 * (1. - celula[ind].alfPigE + 1. - alfE);

        double hol0 = hns;
        double alf0 = 1 - hol0;
        double alf1;
        alf1 = celula[ind].alf;

        double alfneg;
        if (ind > 1)
            alfneg = alfE;
        else if (ind > 0)
            alfneg = alfE;
        else
            alfneg = celula[ind].alf;

        double betI = celula[ind].betL;
        if (ind > 0)
            betI = betaE;
        if (celula[ind].QL < 0.)
            betI = celula[ind].betPigE; // testeBeta
        betI = celula[ind].betPigE;     // duvidabeta
        double betneg;
        if (ind > 0) {
            betneg = betaE;

        } else
            betneg = celula[ind].bet;

        double pmed;
        double pmed0 = 0.;

        pmed = presE;
        if (ind > 0)
            pmed0 = presE;
        else
            pmed0 = presE;
        double tmed = tempE;

        double tmed0;
        tmed0 = tmed;

        double correcHor = 1.;
        if (fabs(celula[ind].duto.teta) < 1e-10) {
            if (celula[ind].acsr.tipo != 5 || celula[ind].acsr.chk.AreaGarg > 1e-10) {
                if (celula[ind].angEsq < 0 && celula[ind].angDir < 0)
                    correcHor = -1.;
                else if (celula[ind].angEsq > 0 && celula[ind].angDir > 0)
                    correcHor = 1.;
            } else {
                if (celula[ind].angDir < 0)
                    correcHor = -1.;
                else if (celula[ind].angDir > 0)
                    correcHor = 1.;
            }
        }

        double rlm;
        double viscl1;
        double tensup1;
        if (celula[ind].MliqiniBuf < 0.) { // testeBeta
            rlm = (1 - betI) * celula[ind].flui.MasEspLiq(pmed, tmed) + betI * celula[ind].fluicol.MasEspFlu(pmed, tmed);
            viscl1 = (1 - betI) * celula[ind].flui.ViscOleo(pmed, tmed) + betI * celula[ind].fluicol.VisFlu(pmed, tmed);
            tensup1 = (1 - betI) * celula[ind].flui.TensSuper(pmed, tmed) + betI * celula[ind].fluicol.TensSuper(pmed, tmed);
        } else {
            rlm = (1 - betI) * (*celula[ind].fluiL).MasEspLiq(pmed, tmed) + betI * celula[ind].fluicol.MasEspFlu(pmed, tmed);
            viscl1 = (1 - betI) * (*celula[ind].fluiL).ViscOleo(pmed, tmed) + betI * celula[ind].fluicol.VisFlu(pmed, tmed);
            tensup1 = (1 - betI) * (*celula[ind].fluiL).TensSuper(pmed, tmed) + betI * celula[ind].fluicol.TensSuper(pmed, tmed);
        }

        double rgm;
        double viscg1;
        if ((celula[ind].MCBuf - celula[ind].MliqiniBuf) < 0.) {
            rgm = celula[ind].flui.MasEspGas(pmed, tmed);
            viscg1 = celula[ind].flui.ViscGas(pmed, tmed);
        } else {
            rgm = (*celula[ind].fluiL).MasEspGas(pmed, tmed);
            viscg1 = (*celula[ind].fluiL).ViscGas(pmed, tmed);
        }

        double ug1 = (celula[ind].MCBuf - celula[ind].MliqiniBuf) / rgm;
        double ul1 = celula[ind].MliqiniBuf / rlm;
        double dia1 = celula[ind].duto.a;
        if (ind > 0 && ug1 >= 0)
            dia1 = celula[ind - 1].duto.a;
        double A1 = M_PI * dia1 * dia1 / 4.;

        double rmed = hns * rlm + (1 - hns) * rgm;
        double visc = (hns * viscl1 + (1 - hns) * viscg1) / pow(10., 3.);
        double nrey = dia1 * rmed * (fabs(ug1) / A1 + fabs(ul1) / A1) / visc;
        double nreyl = dia1 * rlm * (fabs(ug1) / A1 + fabs(ul1) / A1) / (viscl1 / 1000.);

        int xarr1 = 1;
        double dtot = celula[ind].dxL + celula[ind].dx;
        double razL = celula[ind].dxL;
        double raz = celula[ind].dx;
        double ang = (raz * celula[ind].dutoL.teta + razL * celula[ind].duto.teta) / dtot;
        double atenua = 20.;
        if (nrey > 1e-30) {
            if (fabs(0 * ang + 1 * celula[ind].duto.teta) < 45. * M_PI / 180. && hol0 < 0.99 && hol0 > 0.01 && ind < ncel - 1) {
                double ug0;
                double ul0;

                ug1 = (celula[ind].MCBuf - celula[ind].MliqiniBuf) / rgm;
                ul1 = celula[ind].MliqiniBuf / rlm;

                ug0 = (celula[ind].MCBuf - celula[ind].MliqiniBuf) / (*celula[ind].fluiL).MasEspGas(pmed0, tmed0);
                ul0 = celula[ind].MliqiniBuf / ((1 - betneg) * (*celula[ind].fluiL).MasEspLiq(pmed0, tmed0) + betneg * celula[ind].fluicol.MasEspFlu(pmed0, tmed0));

                xarr1 = celula[ind].arranjo;
                if (xarr1 == -1) {

                    double c0D;
                    double udD;
                    double c0E;
                    double udE;
                    C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                 c0D, udD, correcHor, celula[ind].estabCol);
                    C0UdEstratificado(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                      c0E, udE, correcHor, celula[ind].estabCol);
                    double mult0, mult1;
                    mult0 = 1.;
                    if (ul0 < 0.)
                        mult0 = 0.;
                    mult1 = 0.;
                    if (ul1 < 0.)
                        mult1 = 1.;

                    double jmax = 0.05;
                    double jmin = 0.005;
                    if ((fabs(ug1) + fabs(ul1)) / A1 > jmax) {
                        c0 = c0E;
                        ud = udE;
                    } else if ((fabs(ug1) + fabs(ul1)) / A1 < jmin) {
                        c0 = c0D;
                        ud = udD;
                    } else {
                        double raz = (jmax - (fabs(ug1) + fabs(ul1)) / A1) / (jmax - jmin);
                        c0 = ((1. - raz) * c0E + raz * c0D);
                        ud = ((1. - raz) * udE + raz * udD);
                    }

                    if (celula[ind].transic > 0) {
                        c0 = (c0 * celula[ind].transic + celula[ind].c0 * (atenua - celula[ind].transic)) / atenua;
                        ud = (ud * celula[ind].transic + celula[ind].ud * (atenua - celula[ind].transic)) / atenua;
                    }
                }
            }
            if (xarr1 != -1) {

                C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                             c0, ud, correcHor, celula[ind].estabCol);
                if (xarr1 == -2) {
                    C0UdAnularChurn(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                    c0, ud, correcHor, celula[ind].estabCol);
                }
                if (fabs(ug1 / celula[ind].duto.area) > 5. && alf0 >= 0.75) {
                    atenua = 20;
                    if (arq.CorreAnular == 3 && arq.CorreDisper == 1)
                        atenua = 200;
                }

                if (celula[ind].transic > 0) {
                    c0 = (c0 * celula[ind].transic + celula[ind].c0 * (atenua - celula[ind].transic)) / atenua;
                    ud = (ud * celula[ind].transic + celula[ind].ud * (atenua - celula[ind].transic)) / atenua;
                }
                celula[ind].arranjo = xarr1;
            }
            celula[ind].c0Spare = c0;
            celula[ind].udSpare = ud;
        }
    }
    if (arq.escorregaTran == 0) {
        double ulsmed = celula[ind].QL / celula[ind].duto.area;
        double correcaoUd = 1 - (ulsmed - 0.15) / 0.35;
        double correcaoCo = c0 - (c0 - 1) * (ulsmed - 0.15) / 0.35;
        if (correcaoUd > 1.)
            correcaoUd = 1.;
        if (correcaoUd < 0.)
            correcaoUd = 0.;
        if (correcaoCo > c0)
            correcaoCo = c0;
        if (correcaoCo < 1)
            correcaoCo = 1;
        c0 = 1. + 0 * correcaoCo;
        ud = 0. + 0 * ud * correcaoUd;
    }
}

void SProd::correcHidroFric(int i, double &hidro, double &fric) {

    double dx = 0.5 * celula[i].dx;
    double dia = celula[i].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double si = celula[i].duto.peri;
    double alfmed = celula[i].alf;
    double rhog = celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp);
    double rhol = (1 - celula[i].bet) * celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp) + celula[i].bet * celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
    double ugsmed = (celula[i].MC - celula[i].Mliqini) / (area * rhog);
    double ulsmed = celula[i].Mliqini / (area * rhol);
    double j = ugsmed + ulsmed;

    double rhomix = alfmed * rhog + (1 - alfmed) * rhol;
    double viscmix = alfmed * celula[i].flui.ViscGas(celula[i].pres, celula[i].temp) + (1 - alfmed) * ((1 - celula[i].bet) * celula[i].flui.ViscOleo(celula[i].pres, celula[i].temp) + celula[i].bet * celula[i].fluicol.VisFlu(celula[i].pres, celula[i].temp));

    double re1;
    if (celula[i].duto.revest == 0)
        re1 = celula[i].Rey(celula[i].duto.a, j, rhomix, viscmix);
    else {
        double dhid = 4 * area / si;
        re1 = celula[i].Rey(dhid, j, rhomix, viscmix);
    }
    double f1 = celula[i].fric(re1, celula[i].duto.rug / dia);
    fric = (1 - arq.MedSimpPresFront) * 0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area;
    hidro = (1 - arq.MedSimpPresFront) * 9.82 * sin(celula[i].duto.teta) * rhomix * dx;
}

void SProd::auxMiniTab(ProFlu &flui) {
    ProFlu fluC;
    fluC = flui;
    fluC.atualizaPropCompStandard();
    if (fluC.dCalculatedBeta > 0. && fluC.dCalculatedBeta < 1.)
        fluC.atualizaPropComp(flui.miniTabDin.pmin, flui.miniTabDin.tmin,
                              fluC.dCalculatedBeta, fluC.oCalculatedLiqComposition,
                              fluC.oCalculatedVapComposition, arq.pocinjec);
    else
        fluC.atualizaPropComp(flui.miniTabDin.pmin, flui.miniTabDin.tmin, -1, NULL, NULL, arq.pocinjec);
    flui.miniTabDin.rholF[0][0] =
        fluC.MasEspoleo(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.rhogF[0][0] =
        fluC.MasEspGas(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.DrhogDpF[0][0] =
        fluC.drhodp(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.DrholDtF[0][0] =
        fluC.DrholDT(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.DrhogDtF[0][0] =
        fluC.drhodt(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.valBO[0][0] =
        fluC.BOFunc(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.valZ[0][0] =
        fluC.Zdran(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.valdZdT[0][0] =
        fluC.DZDT(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.valdZdP[0][0] =
        fluC.DZDP(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.tit[0][0] =
        fluC.FracMass(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.rs[0][0] =
        fluC.RS(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.cplF[0][0] =
        fluC.CalorLiq(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.cpgF[0][0] =
        fluC.CalorGas(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.HlF[0][0] =
        fluC.EntalpLiq(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.HgF[0][0] =
        fluC.EntalpGas(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    flui.miniTabDin.PBF[0] =
        fluC.PB(flui.miniTabDin.pmin, flui.miniTabDin.tmin);
    if (fluC.dCalculatedBeta > 0. && fluC.dCalculatedBeta < 1.)
        fluC.atualizaPropComp(flui.miniTabDin.pmin, flui.miniTabDin.tmax,
                              fluC.dCalculatedBeta, fluC.oCalculatedLiqComposition,
                              fluC.oCalculatedVapComposition, arq.pocinjec);
    else
        fluC.atualizaPropComp(flui.miniTabDin.pmin, flui.miniTabDin.tmax, -1, NULL, NULL, arq.pocinjec);
    flui.miniTabDin.rholF[0][1] =
        fluC.MasEspoleo(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.rhogF[0][1] =
        fluC.MasEspGas(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.DrhogDpF[0][1] =
        fluC.drhodp(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.DrholDtF[0][1] =
        fluC.DrholDT(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.DrhogDtF[0][1] =
        fluC.drhodt(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.valBO[0][1] =
        fluC.BOFunc(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.valZ[0][1] =
        fluC.Zdran(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.valdZdT[0][1] =
        fluC.DZDT(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.valdZdP[0][1] =
        fluC.DZDP(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.tit[0][1] =
        fluC.FracMass(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.rs[0][1] =
        fluC.RS(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.cplF[0][1] =
        fluC.CalorLiq(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.cpgF[0][1] =
        fluC.CalorGas(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.HlF[0][1] =
        fluC.EntalpLiq(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.HgF[0][1] =
        fluC.EntalpGas(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    flui.miniTabDin.PBF[1] =
        fluC.PB(flui.miniTabDin.pmin, flui.miniTabDin.tmax);
    if (fluC.dCalculatedBeta > 0. && fluC.dCalculatedBeta < 1.)
        fluC.atualizaPropComp(flui.miniTabDin.pmax, flui.miniTabDin.tmin,
                              fluC.dCalculatedBeta, fluC.oCalculatedLiqComposition,
                              fluC.oCalculatedVapComposition, arq.pocinjec);
    else
        fluC.atualizaPropComp(flui.miniTabDin.pmax, flui.miniTabDin.tmin, -1, NULL, NULL, arq.pocinjec);
    flui.miniTabDin.rholF[1][0] =
        fluC.MasEspoleo(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.rhogF[1][0] =
        fluC.MasEspGas(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.DrhogDpF[1][0] =
        fluC.drhodp(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.DrholDtF[1][0] =
        fluC.DrholDT(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.DrhogDtF[1][0] =
        fluC.drhodt(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.valBO[1][0] =
        fluC.BOFunc(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.valZ[1][0] =
        fluC.Zdran(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.valdZdT[1][0] =
        fluC.DZDT(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.valdZdP[1][0] =
        fluC.DZDP(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.tit[1][0] =
        fluC.FracMass(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.rs[1][0] =
        fluC.RS(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.cplF[1][0] =
        fluC.CalorLiq(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.cpgF[1][0] =
        fluC.CalorGas(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.HlF[1][0] =
        fluC.EntalpLiq(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    flui.miniTabDin.HgF[1][0] =
        fluC.EntalpGas(flui.miniTabDin.pmax, flui.miniTabDin.tmin);
    if (fluC.dCalculatedBeta > 0. && fluC.dCalculatedBeta < 1.)
        fluC.atualizaPropComp(flui.miniTabDin.pmax, flui.miniTabDin.tmax,
                              fluC.dCalculatedBeta, fluC.oCalculatedLiqComposition,
                              fluC.oCalculatedVapComposition, arq.pocinjec);
    else
        fluC.atualizaPropComp(flui.miniTabDin.pmax, flui.miniTabDin.tmax, -1, NULL, NULL, arq.pocinjec);
    flui.miniTabDin.rholF[1][1] =
        fluC.MasEspoleo(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.rhogF[1][1] =
        fluC.MasEspGas(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.DrhogDpF[1][1] =
        fluC.drhodp(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.DrholDtF[1][1] =
        fluC.DrholDT(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.DrhogDtF[1][1] =
        fluC.drhodt(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.valBO[1][1] =
        fluC.BOFunc(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.valZ[1][1] =
        fluC.Zdran(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.valdZdT[1][1] =
        fluC.DZDT(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.valdZdP[1][1] =
        fluC.DZDP(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.tit[1][1] =
        fluC.FracMass(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.rs[1][1] =
        fluC.RS(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.cplF[1][1] =
        fluC.CalorLiq(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.cpgF[1][1] =
        fluC.CalorGas(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.HlF[1][1] =
        fluC.EntalpLiq(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
    flui.miniTabDin.HgF[1][1] =
        fluC.EntalpGas(flui.miniTabDin.pmax, flui.miniTabDin.tmax);
}

void SProd::geraMiniTabFlu() {
    (*vg1dSP).modoTransiente = 0;
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = 0; i <= ncel; i++) {
        double delp;
        double delt;
        delp = 0.5 * celula[i].pres;
        if (delp > arq.miniTabDp)
            delp = arq.miniTabDp;
        if (delp < 5) {
            celula[i].flui.miniTabDin.pmax = celula[i].pres + 5.;
            celula[i].flui.miniTabDin.pmin = celula[i].pres - delp;
            if (celula[i].flui.miniTabDin.pmin < 0.9)
                celula[i].flui.miniTabDin.pmin = 0.9;
        } else {
            celula[i].flui.miniTabDin.pmax = celula[i].pres + delp;
            celula[i].flui.miniTabDin.pmin = celula[i].pres - delp;
        }
        delt = arq.miniTabDt;
        celula[i].flui.miniTabDin.tmax = celula[i].temp + delt;
        celula[i].flui.miniTabDin.tmin = celula[i].temp - delt;
        auxMiniTab(celula[i].flui);
        if (celula[i].acsr.tipo == 1) {
            celula[i].acsr.injg.FluidoPro.miniTabDin.pmax = celula[i].flui.miniTabDin.pmax;
            celula[i].acsr.injg.FluidoPro.miniTabDin.pmin = celula[i].flui.miniTabDin.pmin;
            celula[i].acsr.injg.FluidoPro.miniTabDin.tmax = celula[i].flui.miniTabDin.tmax;
            celula[i].acsr.injg.FluidoPro.miniTabDin.tmin = celula[i].flui.miniTabDin.tmin;
            celula[i].acsr.injg.FluidoPro.atualizaPropCompStandard();
            if (celula[i].acsr.injg.FluidoPro.dCalculatedBeta > 0. && celula[i].acsr.injg.FluidoPro.dCalculatedBeta < 1.)
                celula[i].acsr.injg.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                               celula[i].acsr.injg.FluidoPro.dCalculatedBeta, celula[i].acsr.injg.FluidoPro.oCalculatedLiqComposition,
                                                               celula[i].acsr.injg.FluidoPro.oCalculatedVapComposition, arq.pocinjec);
            else
                celula[i].acsr.injg.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            auxMiniTab(celula[i].acsr.injg.FluidoPro);
        } else if (celula[i].acsr.tipo == 2) {
            celula[i].acsr.injl.FluidoPro.miniTabDin.pmax = celula[i].flui.miniTabDin.pmax;
            celula[i].acsr.injl.FluidoPro.miniTabDin.pmin = celula[i].flui.miniTabDin.pmin;
            celula[i].acsr.injl.FluidoPro.miniTabDin.tmax = celula[i].flui.miniTabDin.tmax;
            celula[i].acsr.injl.FluidoPro.miniTabDin.tmin = celula[i].flui.miniTabDin.tmin;
            celula[i].acsr.injl.FluidoPro.atualizaPropCompStandard();
            if (celula[i].acsr.injl.FluidoPro.dCalculatedBeta > 0. && celula[i].acsr.injl.FluidoPro.dCalculatedBeta < 1.)
                celula[i].acsr.injl.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                               celula[i].acsr.injl.FluidoPro.dCalculatedBeta, celula[i].acsr.injl.FluidoPro.oCalculatedLiqComposition,
                                                               celula[i].acsr.injl.FluidoPro.oCalculatedVapComposition, arq.pocinjec);
            else
                celula[i].acsr.injl.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            auxMiniTab(celula[i].acsr.injl.FluidoPro);
        } else if (celula[i].acsr.tipo == 3) {
            celula[i].acsr.ipr.FluidoPro.miniTabDin.pmax = celula[i].flui.miniTabDin.pmax;
            celula[i].acsr.ipr.FluidoPro.miniTabDin.pmin = celula[i].flui.miniTabDin.pmin;
            celula[i].acsr.ipr.FluidoPro.miniTabDin.tmax = celula[i].flui.miniTabDin.tmax;
            celula[i].acsr.ipr.FluidoPro.miniTabDin.tmin = celula[i].flui.miniTabDin.tmin;
            celula[i].acsr.ipr.FluidoPro.atualizaPropCompStandard();
            if (celula[i].acsr.ipr.FluidoPro.dCalculatedBeta > 0. && celula[i].acsr.ipr.FluidoPro.dCalculatedBeta < 1.)
                celula[i].acsr.ipr.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                              celula[i].acsr.ipr.FluidoPro.dCalculatedBeta, celula[i].acsr.ipr.FluidoPro.oCalculatedLiqComposition,
                                                              celula[i].acsr.ipr.FluidoPro.oCalculatedVapComposition, arq.pocinjec);
            else
                celula[i].acsr.ipr.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            auxMiniTab(celula[i].acsr.ipr.FluidoPro);
        } else if (celula[i].acsr.tipo == 15) {
            celula[i].acsr.radialPoro.geraMiniTabFlu();
        } else if (celula[i].acsr.tipo == 16) {
            celula[i].acsr.poroso2D.geraMiniTabFlu();
        } else if (celula[i].acsr.tipo == 9) {
            celula[i].acsr.fontechk.fluidoP.miniTabDin.pmax = celula[i].flui.miniTabDin.pmax;
            celula[i].acsr.fontechk.fluidoP.miniTabDin.pmin = celula[i].flui.miniTabDin.pmin;
            celula[i].acsr.fontechk.fluidoP.miniTabDin.tmax = celula[i].flui.miniTabDin.tmax;
            celula[i].acsr.fontechk.fluidoP.miniTabDin.tmin = celula[i].flui.miniTabDin.tmin;
            celula[i].acsr.fontechk.fluidoP.atualizaPropCompStandard();
            if (celula[i].acsr.fontechk.fluidoP.dCalculatedBeta > 0. && celula[i].acsr.fontechk.fluidoP.dCalculatedBeta < 1.)
                celula[i].acsr.fontechk.fluidoP.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                                 celula[i].acsr.fontechk.fluidoP.dCalculatedBeta, celula[i].acsr.fontechk.fluidoP.oCalculatedLiqComposition,
                                                                 celula[i].acsr.fontechk.fluidoP.oCalculatedVapComposition, arq.pocinjec);
            else
                celula[i].acsr.fontechk.fluidoP.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            auxMiniTab(celula[i].acsr.fontechk.fluidoP);
        } else if (celula[i].acsr.tipo == 10) {
            celula[i].acsr.injm.FluidoPro.miniTabDin.pmax = celula[i].flui.miniTabDin.pmax;
            celula[i].acsr.injm.FluidoPro.miniTabDin.pmin = celula[i].flui.miniTabDin.pmin;
            celula[i].acsr.injm.FluidoPro.miniTabDin.tmax = celula[i].flui.miniTabDin.tmax;
            celula[i].acsr.injm.FluidoPro.miniTabDin.tmin = celula[i].flui.miniTabDin.tmin;
            celula[i].acsr.injm.FluidoPro.atualizaPropCompStandard();
            if (celula[i].acsr.injm.FluidoPro.dCalculatedBeta > 0. && celula[i].acsr.injm.FluidoPro.dCalculatedBeta < 1.)
                celula[i].acsr.injm.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                               celula[i].acsr.injm.FluidoPro.dCalculatedBeta, celula[i].acsr.injm.FluidoPro.oCalculatedLiqComposition,
                                                               celula[i].acsr.injm.FluidoPro.oCalculatedVapComposition, arq.pocinjec);
            else
                celula[i].acsr.injm.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            auxMiniTab(celula[i].acsr.injm.FluidoPro);
        }
    }
    (*vg1dSP).modoTransiente = 1;
}

void SProd::renova(int expli) {
    for (int i = 0; i <= ncel; i++) {
        if (i != 0 && i != ncel) {
            celula[i].presini = celula[i].pres;
            if (expli == 0)
                celula[i].pres = termolivreP[2 * i + 1];
            if (isnan(celula[i].pres))
                NumError("Pressao na linha com valor NaN");
            celula[i].d2pdt2 = celula[i].dpdt;
            celula[i].dpdt = 0. * (celula[i].pres - celula[i].presini) / celula[i].dt;
            celula[i].MCini = celula[i].MC;
            if (expli == 0)
                celula[i].MC = termolivreP[2 * i];
            if (isnan(celula[i].MC))
                NumError("Vazao massica da mistura na linha com valor NaN");
            celula[i + 1].presLini = celula[i + 1].presL;
            celula[i - 1].presRini = celula[i - 1].presR;
            celula[i + 1].presL = celula[i - 1].presR = celula[i].pres;
            celula[i + 1].MLini = celula[i + 1].ML;
            celula[i - 1].MRini = celula[i - 1].MR;
            celula[i + 1].ML = celula[i - 1].MR = celula[i].MC;

            celula[i].Mliqini0 = celula[i].Mliqini;
            celula[i].Mliqini = celula[i].term1 * celula[i].MC + celula[i].term2;
            celula[i + 1].MliqiniL0 = celula[i + 1].MliqiniL;
            celula[i - 1].MliqiniR0 = celula[i - 1].MliqiniR;
            celula[i + 1].MliqiniL = celula[i - 1].MliqiniR = celula[i].Mliqini;

            double dx = 0.5 * celula[i].dx;
            double dia = celula[i].duto.a;
            double area = 0.25 * M_PI * dia * dia;
            double si = celula[i].duto.peri;
            double alfmed = celula[i].alf;
            double rhog = celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp);
            double rhol = (1 - celula[i].bet) * celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp) + celula[i].bet * celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
            double ugsmed = (celula[i].MC - celula[i].Mliqini) / (area * rhog);
            double ulsmed = celula[i].Mliqini / (area * rhol);
            double j = ugsmed + ulsmed;

            double rhomix = alfmed * rhog + (1 - alfmed) * rhol;
            double viscmix = alfmed * celula[i].flui.ViscGas(celula[i].pres, celula[i].temp) + (1 - alfmed) * ((1 - celula[i].bet) * celula[i].flui.ViscOleo(celula[i].pres, celula[i].temp) + celula[i].bet * celula[i].fluicol.VisFlu(celula[i].pres, celula[i].temp));

            double re1;
            if (celula[i].duto.revest == 0)
                re1 = celula[i].Rey(celula[i].duto.a, j, rhomix, viscmix);
            else {
                double dhid = 4 * area / si;
                re1 = celula[i].Rey(dhid, j, rhomix, viscmix);
            }
            double f1 = celula[i].fric(re1, celula[i].duto.rug / dia);
            double medpres = 0;
            if (arq.MedSimpPresFront == 0) {
                if (celula[i].presaux <= 10)
                    medpres = 1;
                else
                    medpres = 0.;
            } else
                medpres = 1;
            double gradfric = (1 - medpres) * 0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area;
            double gradhidro = (1 - medpres) * 9.82 * sin(celula[i].duto.teta) * rhomix * dx;
            celula[i].presauxini = celula[i].presaux;
            celula[i].presaux = celula[i].pres + (gradfric + gradhidro - celula[i - 1].dpB) / 98066.5;
            celula[i].dpresaux = 0.5 * (gradfric + gradhidro - celula[i - 1].dpB) / 98066.5;
            dx = 0.5 * celula[i].dxL;
            dia = celula[i - 1].duto.a;
            area = 0.25 * M_PI * dia * dia;
            si = celula[i - 1].duto.peri;
            alfmed = celula[i - 1].alf;
            rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
            rhol = (1 - celula[i - 1].bet) * celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp) + celula[i - 1].bet * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
            ugsmed = (celula[i - 1].MC - celula[i - 1].Mliqini) / (area * rhog);
            ulsmed = celula[i - 1].Mliqini / (area * rhol);
            j = ugsmed + ulsmed;

            rhomix = alfmed * rhog + (1 - alfmed) * rhol;
            viscmix = alfmed * celula[i - 1].flui.ViscGas(celula[i - 1].pres, celula[i - 1].temp) + (1 - alfmed) * ((1 - celula[i - 1].bet) * celula[i - 1].flui.ViscOleo(celula[i - 1].pres, celula[i - 1].temp) + celula[i - 1].bet * celula[i - 1].fluicol.VisFlu(celula[i - 1].pres, celula[i - 1].temp));

            if (celula[i - 1].duto.revest == 0)
                re1 = celula[i - 1].Rey(celula[i - 1].duto.a, j, rhomix, viscmix);
            else {
                double dhid = 4 * area / si;
                re1 = celula[i - 1].Rey(dhid, j, rhomix, viscmix);
            }
            f1 = celula[i - 1].fric(re1, celula[i - 1].duto.rug / dia);

            gradfric = (1 - medpres) * 0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area;
            gradhidro = (1 - medpres) * 9.82 * sin(celula[i - 1].duto.teta) * rhomix * dx;

            if (celula[i - 1].acsr.tipo != 5 || celula[i - 1].acsr.chk.AreaGarg > celula[i - 1].acsr.chk.AreaTub * 0.5)
                celula[i].presaux = 0.5 * (celula[i].presaux) +
                                    0.5 * (celula[i - 1].pres - (gradfric + gradhidro) / 98066.5);
            celula[i].dpresaux -= 0.5 * (gradfric + gradhidro) / 98066.5;
            celula[i - 1].presauxRini = celula[i - 1].presauxR;
            celula[i - 1].presauxR = celula[i].presaux;
            if (i < ncel) {
                celula[i + 1].presauxLini = celula[i + 1].presauxL;
                celula[i + 1].presauxL = celula[i].presaux;
            }

            double tmed = celula[i - 1].temp;
            if (celula[i].VTemper < 0.)
                tmed = celula[i].temp;

            ProFlu flud;
            if (celula[i].Mliqini < 0.)
                flud = celula[i].flui;
            else
                flud = celula[i - 1].flui;

            double betI;
            if (((celula[i].MC - celula[i].Mliqini) * 0 + 1 * celula[i].Mliqini) < 0)
                betI = celula[i].bet; // duvidabeta
            else
                betI = celula[i].betL;

            double rl = flud.MasEspLiq(celula[i].presaux, tmed);
            rhol = (1 - betI) * rl + betI * celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);

            double rg = flud.MasEspGas(celula[i].presaux, tmed);
            double vLiqTest = 1 + 0 * fabs(celula[i].Mliqini / (rhol * area));
            double vGasTest = 1 + 0 * fabs((celula[i].MC - celula[i].Mliqini) / (rg * area));

            celula[i].QLini = celula[i].QL;
            if (vLiqTest > 1e-3)
                celula[i].QL = celula[i].Mliqini / rhol;
            else {
                celula[i].QL = 0.;
                celula[i].MC = (celula[i].MC - celula[i].Mliqini);
                celula[i].Mliqini = 0;
                celula[i + 1].ML = celula[i - 1].MR = celula[i].MC;
                celula[i + 1].MliqiniL = celula[i - 1].MliqiniR = celula[i].Mliqini;
            }
            celula[i].QGini = celula[i].QG;
            if (vGasTest > 1e-3)
                celula[i].QG = (celula[i].MC - celula[i].Mliqini) / rg;
            else {
                celula[i].QG = 0;
                celula[i].MC = celula[i].Mliqini;
                celula[i + 1].ML = celula[i - 1].MR = celula[i].MC;
            }
            celula[i - 1].QLRini = celula[i - 1].QLR;
            celula[i - 1].QLR = celula[i].QL;
            if (i < ncel) {
                celula[i + 1].QLLini = celula[i + 1].QLL;
                celula[i + 1].QLL = celula[i].QL;
            }

        } else if (i == 0) {
            celula[0].presini = celula[0].pres;
            if (expli == 0)
                celula[0].pres = termolivreP[1];
            celula[i].d2pdt2 = celula[i].dpdt;
            celula[i].dpdt = 0 * (celula[i].pres - celula[i].presini) / celula[i].dt;
            celula[0].presauxini = celula[0].presaux;
            celula[0].presaux = celula[0].pres;
            celula[0].dpresaux = 0.;
            celula[0].MCini = celula[0].MC;
            if (expli == 0)
                celula[0].MC = termolivreP[0];
            celula[1].presLini = celula[1].presL;
            celula[1].presL = celula[0].pres;
            celula[1].MLini = celula[1].ML;
            celula[1].ML = celula[0].MC;
            celula[0].Mliqini0 = celula[0].Mliqini;
            celula[0].Mliqini = celula[i].term1 * celula[i].MC + celula[i].term2;
            celula[1].MliqiniL0 = celula[1].MliqiniL;
            celula[1].MliqiniL = celula[0].Mliqini;

            celula[0].QLini = celula[0].QL;
            celula[0].QL = 0.;
            celula[1].QLLini = celula[1].QLL;
            celula[1].QLL = celula[0].QL;
            celula[0].QGini = celula[0].QG;
            celula[0].QG = 0.;

            if (arq.ConContEntrada > 0) {
                double rhogC = celula[i].flui.MasEspGas(presE, tempE);
                double rhopC = celula[i].flui.MasEspLiq(presE, tempE);
                double rhocC = celula[i].fluicol.MasEspFlu(presE, tempE);
                double rholC = rhopC * (1 - betaE) + rhocC * betaE;
                celula[0].QL = celula[0].Mliqini / rholC;
                celula[1].QLL = celula[0].QL;
                celula[0].QG = (celula[0].MC - celula[0].Mliqini) / rhogC;
            }
        } else {
            celula[i].presini = celula[i].pres;
            if (expli == 0)
                celula[ncel].pres = termolivreP[2 * ncel + 1];
            celula[i].d2pdt2 = celula[i].dpdt;
            celula[i].dpdt = 0 * (celula[i].pres - celula[i].presini) / celula[i].dt;
            celula[i].d2pdt2 = (celula[i].dpdt - celula[i].d2pdt2) / celula[i].dt;
            celula[ncel].MCini = celula[ncel].MC;
            if (expli == 0)
                celula[ncel].MC = termolivreP[2 * ncel];
            // teste!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
            // teste!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
            celula[ncel - 1].presRini = celula[ncel - 1].presR;
            celula[ncel - 1].presR = celula[ncel].pres;
            celula[ncel - 1].MRini = celula[ncel - 1].MR;
            celula[ncel - 1].MR = celula[ncel].MC;
            celula[ncel].MRini = celula[ncel].MR;
            celula[ncel].MR = celula[ncel].MC;

            celula[ncel].Mliqini0 = celula[ncel].Mliqini;
            celula[ncel].Mliqini = celula[ncel].term1 * celula[ncel].MC + celula[ncel].term2;
            celula[i - 1].MliqiniR0 = celula[i - 1].MliqiniR;
            celula[ncel - 1].MliqiniR = celula[ncel].Mliqini;
            celula[ncel].MliqiniR0 = celula[ncel].MliqiniR;
            celula[ncel].MliqiniR = celula[ncel].Mliqini;

            double dx = 0.5 * celula[i].dx;
            double dia = celula[i].duto.a;
            double area = 0.25 * M_PI * dia * dia;
            double si = celula[i].duto.peri;
            double alfmed = celula[i].alf;
            double rhog = celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp);
            double rhol = (1 - celula[i].bet) * celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp) + celula[i].bet * celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
            double ugsmed = (celula[i].QG) / (area);
            double ulsmed = celula[i].QL / (area);
            double j = ugsmed + ulsmed;
            double ABSjL = (fabs(celula[i - 1].QG) + fabs(celula[i - 1].QL)) / celula[i - 1].duto.area;

            double rhomix = alfmed * rhog + (1 - alfmed) * rhol;
            double viscmix = alfmed * celula[i].flui.ViscGas(celula[i].pres, celula[i].temp) + (1 - alfmed) * ((1 - celula[i].bet) * celula[i].flui.ViscOleo(celula[i].pres, celula[i].temp) + celula[i].bet * celula[i].fluicol.VisFlu(celula[i].pres, celula[i].temp));

            double re1;
            if (celula[i].duto.revest == 0)
                re1 = celula[i].Rey(celula[i].duto.a, j, rhomix, viscmix);
            else {
                double dhid = 4 * area / si;
                re1 = celula[i].Rey(dhid, j, rhomix, viscmix);
            }
            double f1 = celula[i].fric(re1, celula[i].duto.rug / dia);
            double medpres = 0;
            double gradfric = (1 - medpres) * 0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area;
            double gradhidro = (1 - medpres) * 9.82 * sin(celula[i].duto.teta) * rhomix * dx;

            celula[i].presauxini = celula[i].presaux;
            celula[i].presaux = celula[i].pres + (gradfric + gradhidro - celula[i - 1].dpB) / 98066.5;
            celula[i].dpresaux = 0.5 * (gradfric + gradhidro - celula[i - 1].dpB) / 98066.5;
            dx = 0.5 * celula[i].dxL;
            dia = celula[i - 1].duto.a;
            area = 0.25 * M_PI * dia * dia;
            si = celula[i - 1].duto.peri;
            alfmed = celula[i - 1].alf;
            rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
            rhol = (1 - celula[i - 1].bet) * celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp) + celula[i - 1].bet * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
            ugsmed = (celula[i - 1].MC - celula[i - 1].Mliqini) / (area * rhog);
            ulsmed = celula[i - 1].Mliqini / (area * rhol);
            j = ugsmed + ulsmed;

            rhomix = alfmed * rhog + (1 - alfmed) * rhol;
            viscmix = alfmed * celula[i - 1].flui.ViscGas(celula[i - 1].pres, celula[i - 1].temp) + (1 - alfmed) * ((1 - celula[i - 1].bet) * celula[i - 1].flui.ViscOleo(celula[i - 1].pres, celula[i - 1].temp) + celula[i - 1].bet * celula[i - 1].fluicol.VisFlu(celula[i - 1].pres, celula[i - 1].temp));

            if (celula[i - 1].duto.revest == 0)
                re1 = celula[i - 1].Rey(celula[i - 1].duto.a, j, rhomix, viscmix);
            else {
                double dhid = 4 * area / si;
                re1 = celula[i - 1].Rey(dhid, j, rhomix, viscmix);
            }
            f1 = celula[i - 1].fric(re1, celula[i - 1].duto.rug / dia);
            gradfric = (1 - medpres) * 0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area;
            gradhidro = (1 - medpres) * 9.82 * sin(celula[i - 1].duto.teta) * rhomix * dx;

            celula[i].presaux = 0.5 * (celula[i].presaux) +
                                0.5 * (celula[i - 1].pres - (gradfric + gradhidro) / 98066.5);
            celula[i].dpresaux -= 0.5 * (gradfric + gradhidro) / 98066.5;
            celula[i - 1].presauxRini = celula[i - 1].presauxR;
            celula[i - 1].presauxR = celula[i].presaux;
            if (i < ncel) {
                celula[i + 1].presauxLini = celula[i + 1].presauxL;
                celula[i + 1].presauxL = celula[i].presaux;
            }

            double tmed = celula[i - 1].temp;
            if (celula[i].VTemper < 0.)
                tmed = celula[i].temp;

            ProFlu flud;
            if (celula[i].Mliqini < 0.)
                flud = celula[i].flui;
            else
                flud = celula[i - 1].flui;

            double betI;
            if (((celula[i].MC - celula[i].Mliqini) * 0.99 + 0.01 * celula[i].Mliqini) < 0)
                betI = celula[i].bet; // duvidabeta
            else
                betI = celula[i].betL;

            double rl = flud.MasEspLiq(celula[i].presaux, tmed);
            rhol = (1 - betI) * rl + betI * celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);

            double rg = flud.MasEspGas(celula[i].presaux, tmed);

            celula[i].QL = celula[i].Mliqini / rhol;
            celula[i].QG = (celula[i].MC - celula[i].Mliqini) / rg;
            celula[i - 1].QLR = celula[i].QL;
        }
    }
}

void SProd::renovaVaz() {
    for (int i = 0; i <= ncel; i++) {
        if (i != 0 && i != ncel) {
            celula[i].Mliqini = celula[i].term1 * celula[i].MC + celula[i].term2;
            celula[i + 1].MliqiniL = celula[i - 1].MliqiniR = celula[i].Mliqini;

            double tmed = celula[i - 1].temp;
            if (celula[i].VTemper < 0.)
                tmed = celula[i].temp;
            ProFlu flud;
            if (celula[i].Mliqini < 0.)
                flud = celula[i].flui;
            else
                flud = celula[i - 1].flui;

            double betI;
            if (((celula[i].MC - celula[i].Mliqini) * 0 + 1 * celula[i].Mliqini) < 0)
                betI = celula[i].bet; // duvidabeta
            else
                betI = celula[i].betL;

            double rl = flud.MasEspLiq(celula[i].presaux, tmed);
            double rhol = (1 - betI) * rl + betI * celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);

            double rg = flud.MasEspGas(celula[i].presaux, tmed);
            celula[i].QL = celula[i].Mliqini / rhol;
            celula[i].QG = (celula[i].MC - celula[i].Mliqini) / rg;
            celula[i - 1].QLR = celula[i].QL;
            if (i < ncel) {
                celula[i + 1].QLL = celula[i].QL;
            }

        } else if (i == 0) {

            celula[0].Mliqini = celula[i].term1 * celula[i].MC + celula[i].term2;
            celula[1].MliqiniL = celula[0].Mliqini;

            if (arq.ConContEntrada > 0) {
                double rhogC = celula[i].flui.MasEspGas(presE, tempE);
                double rhopC = celula[i].flui.MasEspLiq(presE, tempE);
                double rhocC = celula[i].fluicol.MasEspFlu(presE, tempE);
                double rholC = rhopC * (1 - betaE) + rhocC * betaE;
                celula[0].QL = celula[0].Mliqini / rholC;
                celula[1].QLL = celula[0].QL;
                celula[0].QG = (celula[0].MC - celula[0].Mliqini) / rhogC;
            }
        } else {

            celula[ncel].Mliqini = celula[ncel].term1 * celula[ncel].MC + celula[ncel].term2;
            celula[ncel - 1].MliqiniR = celula[ncel].Mliqini;
            celula[ncel].MliqiniR = celula[ncel].Mliqini;

            double tmed = celula[i - 1].temp;
            if (celula[i].VTemper < 0.)
                tmed = celula[i].temp;

            ProFlu flud;
            if (celula[i].Mliqini < 0.)
                flud = celula[i].flui;
            else
                flud = celula[i - 1].flui;

            double betI;
            if (((celula[i].MC - celula[i].Mliqini) * 0.99 + 0.01 * celula[i].Mliqini) < 0)
                betI = celula[i].bet; // duvidabeta
            else
                betI = celula[i].betL;

            double rl = flud.MasEspLiq(celula[i].presaux, tmed);
            double rhol = (1 - betI) * rl + betI * celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);

            double rg = flud.MasEspGas(celula[i].presaux, tmed);

            celula[i].QL = celula[i].Mliqini / rhol;
            celula[i].QG = (celula[i].MC - celula[i].Mliqini) / rg;
            celula[i - 1].QLR = celula[i].QL;
        }
    }
}

void SProd::renovaBuffer() {

    celula[0].presBuf = termolivreP[1];
    celula[0].presauxBuf = celula[0].presBuf;
    celula[0].MCBuf = termolivreP[0];
    celula[1].presLiniBuf = celula[1].presLBuf;
    celula[1].presLBuf = celula[0].presBuf;
    celula[1].MLBuf = celula[0].MCBuf;
    celula[0].MliqiniBuf = celula[0].term1 * celula[0].MCBuf + celula[0].term2;
    celula[1].MliqiniLBuf = celula[0].MliqiniBuf;

    int i = 1;
    celula[i].presBuf = termolivreP[2 * i + 1];
    celula[i].MCBuf = termolivreP[2 * i];
    celula[i + 1].presLiniBuf = celula[i + 1].presLBuf;
    celula[i + 1].presLBuf = celula[i - 1].presRBuf = celula[i].presBuf;
    celula[i - 1].MRiniBuf = celula[i - 1].MRBuf;
    celula[i + 1].MLBuf = celula[i - 1].MRBuf = celula[i].MCBuf;

    celula[i].MliqiniBuf = celula[i].term1 * celula[i].MCBuf + celula[i].term2;
    celula[i + 1].MliqiniLBuf = celula[i - 1].MliqiniRBuf = celula[i].MliqiniBuf;

    celula[ncel].presBuf = termolivreP[2 * ncel + 1];
    celula[ncel].MCBuf = termolivreP[2 * ncel];
    celula[ncel - 1].presRBuf = celula[ncel].presBuf;
    celula[ncel - 1].MRBuf = celula[ncel].MCBuf;
    celula[ncel].MRBuf = celula[ncel].MCBuf;

    celula[ncel].MliqiniBuf = celula[ncel].term1 * celula[ncel].MCBuf + celula[ncel].term2;
    celula[ncel - 1].MliqiniRBuf = celula[ncel].MliqiniBuf;
    celula[ncel].MliqiniRBuf = celula[ncel].MliqiniBuf;
}

void SProd::renovaBufferCego() {

    celula[0].presBuf = celula[0].pres;
    celula[0].presauxBuf = celula[0].presaux;
    celula[0].MCBuf = celula[0].MC;
    celula[1].presLiniBuf = celula[1].presLini;
    celula[1].presLBuf = celula[1].presL;
    celula[1].MLBuf = celula[1].ML;
    celula[0].MliqiniBuf = celula[0].Mliqini;
    celula[1].MliqiniLBuf = celula[1].MliqiniL;

    int i = 1;
    celula[i].presBuf = celula[i].pres;
    celula[i].MCBuf = celula[i].MC;
    celula[i + 1].presLiniBuf = celula[i + 1].presLini;
    celula[i + 1].presLBuf = celula[i - 1].presRBuf = celula[i].pres;
    celula[i - 1].MRiniBuf = celula[i - 1].MR;
    celula[i + 1].MLBuf = celula[i - 1].MRBuf = celula[i].MC;

    celula[i].MliqiniBuf = celula[i].Mliqini;
    celula[i + 1].MliqiniLBuf = celula[i - 1].MliqiniRBuf = celula[i].Mliqini;

    celula[ncel].presBuf = celula[ncel].pres;
    celula[ncel].MCBuf = celula[ncel].MC;
    celula[ncel - 1].presRBuf = celula[ncel].pres;
    celula[ncel - 1].MRBuf = celula[ncel].MC;

    celula[ncel].MliqiniBuf = celula[ncel].Mliqini;
    celula[ncel - 1].MliqiniRBuf = celula[ncel].Mliqini;

    int fim = ncel - 1;
    fontemassCRBuf = celula[fim + 1].fontemassCR;

    fontemassPRBuf = celula[fim + 1].fontemassLR;

    fontemassGRBuf = celula[fim + 1].fontemassGR;
}

int SProd::sign(double var) {
    if (var <= 0.)
        return -1;
    else
        return 1;
}

void SProd::renovaTemp() {
    // #pragma omp parallel for num_threads(arq.nthrd)
    double rhol0 = 0.;
    double boL = 0.;
    double rsL = 0.;
    double DRsBoL = 0.;
    for (int i = 0; i <= ncel; i++) {
        if (i != 0 && i != ncel + 1) {

            if ((*vg1dSP).lixo5 >= 12264.7 && i == ncel - 1) {
                int para;
                para = 0;
            }

            renovaFonte(i);

            if (i < ncel) {
                celula[i + 1].fontemassLLini = celula[i].fontemassLR;
                celula[i + 1].fontemassCLini = celula[i].fontemassCR;
                celula[i + 1].fontemassGLini = celula[i].fontemassGR;
                celula[i + 1].fontemassLL = celula[i].fontemassLR;
                celula[i + 1].fontemassCL = celula[i].fontemassCR;
                celula[i + 1].fontemassGL = celula[i].fontemassGR;
            }
            double razdx = celula[i - 1].dx / (celula[i].dx + celula[i].dxL);
            double razdxL = celula[i].dx / (celula[i - 1].dx + celula[i - 1].dxL);
            double tmed = razdx * celula[i].temp + (1 - razdx) * celula[i - 1].temp;
            double tmedL = razdxL * celula[i].tempL + (1 - razdxL) * celula[i - 1].tempL;
            tmed = celula[i - 1].temp;
            if (celula[i].VTemper < 0.)
                tmed = celula[i].temp;
            tmedL = celula[i - 1].tempL;
            if (celula[i - 1].VTemper < 0.)
                tmedL = celula[i - 1].temp;

            double dia = celula[i].duto.a;
            double area = 0.25 * M_PI * dia * dia;
            double ugsmed = (celula[i].QG) / (area);
            double ulsmed = celula[i].QL / (area);
            double j = ugsmed + ulsmed;
            double ABSjL = (fabs(celula[i - 1].QG) + fabs(celula[i - 1].QL)) / celula[i - 1].duto.area;

            ProFlu flue;
            ProFlu flud;
            double fwd;
            double fwe;
            double boC = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baC = celula[i - 1].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwC = celula[i - 1].flui.BSW * baC / (boC + baC * celula[i - 1].flui.BSW - celula[i - 1].flui.BSW * boC);
            if (celula[i].Mliqini < 0.) {
                flud = celula[i].flui;
                double bo1 = flud.BOFunc(celula[i].pres, celula[i].temp);
                double ba1 = flud.BAFunc(celula[i].pres, celula[i].temp);
                fwd = flud.BSW * ba1 / (bo1 + ba1 * flud.BSW - flud.BSW * bo1);
            } else {
                flud = celula[i - 1].flui;
                double bo1 = flud.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
                double ba1 = flud.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
                fwd = flud.BSW * ba1 / (bo1 + ba1 * flud.BSW - flud.BSW * bo1);
            }
            if (celula[i - 1].Mliqini < 0) {
                flue = celula[i - 1].flui;
                double bo0 = flue.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
                double ba0 = flue.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
                fwe = flue.BSW * ba0 / (bo0 + ba0 * flue.BSW - flue.BSW * bo0);

            } else {
                if (i > 1) {
                    flue = celula[i - 2].flui;
                    double bo0 = flue.BOFunc(celula[i - 2].pres, celula[i - 2].temp);
                    double ba0 = flue.BAFunc(celula[i - 2].pres, celula[i - 2].temp);
                    fwe = flue.BSW * ba0 / (bo0 + ba0 * flue.BSW - flue.BSW * bo0);
                } else {
                    flue = celula[i - 1].flui;
                    double bo0 = flue.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
                    double ba0 = flue.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
                    fwe = flue.BSW * ba0 / (bo0 + ba0 * flue.BSW - flue.BSW * bo0);
                }
            }

            double rl;
            double rg;
            double betI;

            // casoComp

            rl = flud.MasEspLiq(celula[i].presaux, tmed);
            if (celula[i].Mliqini < 0)
                betI = celula[i].bet; // testeBeta
            else
                betI = celula[i].betL;

            rg = flud.MasEspGas(celula[i].presaux, tmed);

            double betL = celula[i - 1].betL;
            if (celula[i - 1].Mliqini < 0)
                betL = celula[i - 1].bet; // testeBeta

            if (i > 0)
                betI = celula[i - 1].betPigD;
            if (celula[i].Mliqini < 0)
                betI = celula[i].betPigE; // testeBeta
            if (i > 1)
                betL = celula[i - 2].betPigD;
            if (celula[i - 1].Mliqini < 0)
                betL = celula[i - 1].betPigE; // testebeta

            double rhol = (1 - betI) * rl + betI * celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            double boR;
            double rsR;
            double boR0;
            double rsR0;
            if (arq.flashCompleto != 2 || arq.miniTabAtraso > 0) {
                boR = flud.BOFunc(celula[i].presaux, tmed);
                rsR = flud.RS(celula[i].presaux, tmed);
                boR0 = flud.BOFunc(celula[i].presaux * 0.999, tmed);
                rsR0 = flud.RS(celula[i].presaux * 0.999, tmed);
            } else {
                boR = flud.BOFunc(celula[i].presaux, tmed);
                rsR = flud.RS(celula[i].presaux, tmed);
                flud.atualizaPropComp(celula[i].presaux * 0.999, tmed, flud.dCalculatedBeta,
                                      flud.oCalculatedLiqComposition,
                                      flud.oCalculatedVapComposition, arq.pocinjec);
                boR0 = flud.BOFunc(celula[i].presaux * 0.999, tmed);
                rsR0 = flud.RS(celula[i].presaux * 0.999, tmed);
            } // casoComp
            double DRsBoR = (rsR / boR - rsR0 / boR0) / (celula[i].presaux * 0.001);
            double boM;
            double rsM;
            double boM0;
            double rsM0;
            if (arq.flashCompleto != 2 || arq.miniTabAtraso > 0) {
                boM = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
                rsM = celula[i - 1].flui.RS(celula[i - 1].pres, celula[i - 1].temp);
                boM0 = celula[i - 1].flui.BOFunc(celula[i - 1].pres * 0.999, celula[i - 1].temp);
                rsM0 = celula[i - 1].flui.RS(celula[i - 1].pres * 0.999, celula[i - 1].temp);
            } else {
                boM = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
                rsM = celula[i - 1].flui.RS(celula[i - 1].pres, celula[i - 1].temp);
                ProFlu flutemp = celula[i - 1].flui;
                flutemp.atualizaPropComp(celula[i - 1].pres * 0.999, celula[i - 1].temp, flutemp.dCalculatedBeta,
                                         flutemp.oCalculatedLiqComposition,
                                         flutemp.oCalculatedVapComposition, arq.pocinjec);
                boM0 = flutemp.BOFunc(celula[i - 1].pres * 0.999, celula[i - 1].temp);
                rsM0 = flutemp.RS(celula[i - 1].pres * 0.999, celula[i - 1].temp);
            } // casoComp
            double DRsBoM = (rsM / boM - rsM0 / boM0) / (celula[i - 1].pres * 0.001);
            double boM0T = 0.;
            double rsM0T = 0.;
            double DRsBoMT = 0;
            if (arq.cicloAcopTerm == 1) {
                if (arq.flashCompleto != 2 || arq.miniTabAtraso > 0) {
                    boM0T = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp * 0.999);
                    rsM0T = celula[i - 1].flui.RS(celula[i - 1].pres, celula[i - 1].temp * 0.999);
                } else {
                    ProFlu flutemp = celula[i - 1].flui;
                    flutemp.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp * 0.999, flutemp.dCalculatedBeta,
                                             flutemp.oCalculatedLiqComposition,
                                             flutemp.oCalculatedVapComposition, arq.pocinjec);
                    boM0T = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp * 0.999);
                    rsM0T = celula[i - 1].flui.RS(celula[i - 1].pres, celula[i - 1].temp * 0.999);
                } // casoComp
                DRsBoMT = (rsM / boM - rsM0T / boM0T) / (celula[i - 1].temp * 0.001);
            }
            double ativa = 1.;
            double limipres = 10;
            if (modeloCompleto == 1)
                limipres = 0;
            if (celula[i - 1].pres < limipres || TransMassModel != 0)
                ativa = 0.;
            double acop = 1.;
            if (i < 2 || i == ncel)
                acop = 0;

            double A1 = celula[i].dutoL.area;
            celula[i - 1].ativaDeri = ativa;
            celula[i - 1].DTransDtp = ativa * A1 * (1. - celula[i - 1].alf) *
                                      (1. - celula[i - 1].bet) * (1. - fwC) * flud.Deng * 1.225 * DRsBoM * (6.29 / 35.31467);
            celula[i].DTransDtpL = celula[i - 1].DTransDtp;
            if (i == ncel) {
                double boM;
                double rsM;
                double boM0;
                double rsM0;
                if (arq.flashCompleto != 2 || arq.miniTabAtraso > 0) {
                    boM = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp);
                    rsM = celula[i].flui.RS(celula[i].pres, celula[i].temp);
                    boM0 = celula[i].flui.BOFunc(celula[i].pres * 0.999, celula[i].temp);
                    rsM0 = celula[i].flui.RS(celula[i].pres * 0.999, celula[i].temp);
                } else {
                    boM = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp);
                    rsM = celula[i].flui.RS(celula[i].pres, celula[i].temp);
                    ProFlu flutemp = celula[i].flui;
                    flutemp.atualizaPropComp(celula[i].pres * 0.999, celula[i].temp, flutemp.dCalculatedBeta,
                                             flutemp.oCalculatedLiqComposition,
                                             flutemp.oCalculatedVapComposition, arq.pocinjec);
                    boM0 = flutemp.BOFunc(celula[i].pres * 0.999, celula[i].temp);
                    rsM0 = flutemp.RS(celula[i].pres * 0.999, celula[i].temp);
                } // casoComp
                double DRsBoM = (rsM / boM - rsM0 / boM0) / (celula[i].pres * 0.001);
                celula[i].DTransDtp = ativa * A1 * (1. - celula[i].alf) *
                                      (1. - celula[i].bet) * (1. - fwC) * flud.Deng * 1.225 * DRsBoM * (6.29 / 35.31467);
            }
            if (arq.cicloAcopTerm == 1) {
                celula[i - 1].DTransDtT = ativa * A1 * (1. - celula[i - 1].alf) *
                                          (1. - celula[i - 1].bet) * (1. - fwC) * flud.Deng * 1.225 * DRsBoMT * (6.29 / 35.31467);
                celula[i].DTransDtTL = celula[i - 1].DTransDtT;
                if (i == ncel) {
                    double boM;
                    double rsM;
                    double boM0;
                    double rsM0;
                    if (arq.flashCompleto != 2 || arq.miniTabAtraso > 0) {
                        boM = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp);
                        rsM = celula[i].flui.RS(celula[i].pres, celula[i].temp);
                        boM0 = celula[i].flui.BOFunc(celula[i].pres * 0.999, celula[i].temp);
                        rsM0 = celula[i].flui.RS(celula[i].pres * 0.999, celula[i].temp);
                    } else {
                        boM = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp);
                        rsM = celula[i].flui.RS(celula[i].pres, celula[i].temp);
                        ProFlu flutemp = celula[i].flui;
                        flutemp.atualizaPropComp(celula[i].pres * 0.999, celula[i].temp, flutemp.dCalculatedBeta,
                                                 flutemp.oCalculatedLiqComposition,
                                                 flutemp.oCalculatedVapComposition, arq.pocinjec);
                        boM0 = flutemp.BOFunc(celula[i].pres * 0.999, celula[i].temp);
                        rsM0 = flutemp.RS(celula[i].pres * 0.999, celula[i].temp);
                    } // casoComp
                    double DRsBoM = (rsM / boM - rsM0 / boM0) / (celula[i].pres * 0.001);
                    celula[i].DTransDtT = ativa * A1 * (1. - celula[i].alf) *
                                          (1. - celula[i].bet) * (1. - fwC) * flud.Deng * 1.225 * DRsBoMT * (6.29 / 35.31467);
                }
            }

            tmed = celula[i - 1].temp;
            if (celula[i].VTemper < 0.)
                tmed = celula[i].temp;
            tmedL = celula[i - 1].tempL;
            if (celula[i - 1].VTemper < 0.)
                tmedL = celula[i - 1].temp;

            celula[i - 1].TMModel = TransMassModel;
            if (TransMassModel != 3) {
                if ((((celula[i - 1].alf < 0.001) ||
                      (celula[i - 1].alf > 0.999) || (celula[i - 1].bet > 0.999 && celula[i - 1].alf < 0.999)) &&
                     ABSjL < 0.1) ||
                    celula[i - 1].flui.RGO >= (*vg1dSP).RGOMax)
                    celula[i - 1].TMModel = 3;
                else if (celula[i - 1].estadoPig == 1)
                    celula[i - 1].TMModel = 3;
                else if (celula[i - 1].acsr.tipo == 2 || celula[i - 1].acsr.tipo == 3 || celula[i - 1].acsr.tipo == 9 || celula[i - 1].acsr.tipo == 15 || celula[i - 1].acsr.tipo == 16)
                    celula[i - 1].TMModel = 3;
            	else if (i >= 2) {
                    if (celula[i - 2].acsr.tipo == 5 &&
                        (celula[i - 2].acsr.chk.AreaGarg < (1e-3 + arq.master1.razareaativ) * celula[i - 2].duto.area))
                        celula[i - 1].TMModel = 3;
                    else if (celula[i - 2].acsr.tipo == 4 || celula[i - 2].acsr.tipo == 7 || celula[i - 2].acsr.tipo == 17)
                        celula[i - 1].TMModel = 0;
                }
            	if (arq.flashCompleto == 2) {
            		double titTeste =
            				celula[i - 1].flui.FracMass(celula[i - 1].pres, celula[i - 1].temp);
            		if (titTeste > 1.0 - 1e-2 || titTeste < 1e-2)
            			celula[i - 1].TMModel = 3;
            	}

        	}
            if (celula[i - 1].TMModel == 0 && celula[i - 1].alf <= (*vg1dSP).CritCond)
                celula[i - 1].TMModel = 1;
            if (celula[i - 1].TMModel == 0 && i == ncel)
                celula[i - 1].TMModel = 1;
            if (i == ncel)
                celula[i].FonteMudaFase = 0.;

            celula[i - 1].fontedissolv = 0.;

            celula[i - 1].transmassRini = celula[i - 1].transmassR;
            celula[i - 1].FonteMudaFaseini = celula[i - 1].FonteMudaFase;
            celula[i].DTransDxRini = celula[i].DTransDxR;
            celula[i].DTransDxLini = celula[i].DTransDxL;
            celula[i].DTransDt1ini = celula[i].DTransDt1;
            celula[i].DTransDt0ini = celula[i].DTransDt0;
            celula[i].DTransDxRpini = celula[i].DTransDxRp;
            celula[i].DTransDxLpini = celula[i].DTransDxLp;
            celula[i - 1].CoefDTLini = celula[i - 1].CoefDTL;
            celula[i - 1].coefTransBetini = celula[i - 1].coefTransBet;
            celula[i].transmassLini = celula[i].transmassL;

            celula[i].TMModelL = celula[i - 1].TMModel;
            celula[i - 1].FonteMudaFase = 0.;
            if (celula[i - 1].TMModel == 0 || celula[i - 1].TMModel == 1) {

                celula[i - 1].transmassR = -(celula[i].QL * (1 - betI) * (flud.rDgD) * flud.Deng * 1.225 * (1. - fwd) * rsR * (6.29 / 35.31467) / boR) + (celula[i - 1].QL * (1 - betL) * (flue.rDgD) * flue.Deng * 1.225 *
                                                                                                                                                          (1. - fwe) * rsL * (6.29 / 35.31467) / boL);

                celula[i - 1].transmassR /= celula[i - 1].dx;
                celula[i - 1].transmassR += celula[i - 1].fontedissolv;
                celula[i].transmassL = celula[i - 1].transmassR;
                celula[i - 1].FonteMudaFase = celula[i - 1].transmassR -
                                              celula[i - 1].DTransDtp * celula[i - 1].d2pdt2 - celula[i - 1].DTransDtT * celula[i - 1].dTdtIni;
                if (celula[i - 1].TMModel == 1) {
                    celula[i - 1].transmassR -= ativa * ((1. - celula[i - 1].bet) * (1. - celula[i - 1].alf) * (1. - fwC) * A1 * (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM * (6.29 / 35.31467) / boM) /
                                                celula[i - 1].dt;
                    celula[i - 1].transmassR += ativa * ((1. - celula[i - 1].betini) * (1. - celula[i - 1].alfini) * (1. - fwC) * A1 * (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM * (6.29 / 35.31467) / boM) /
                                                celula[i - 1].dt;

                    celula[i].transmassL = celula[i - 1].transmassR;
                    celula[i - 1].FonteMudaFase = celula[i - 1].transmassR;
                }
                if (celula[i - 1].TMModel == 0) {
                    celula[i - 1].FonteMudaFase -= ativa * ((1. - celula[i - 1].bet) * (1. - celula[i - 1].alf) * (1. - fwC) * A1 * (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM * (6.29 / 35.31467) / boM) / celula[i - 1].dt;
                    celula[i - 1].FonteMudaFase += ativa * ((1. - celula[i - 1].betini) * (1. - celula[i - 1].alfini) * (1. - fwC) * A1 * (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM * (6.29 / 35.31467) / boM) / celula[i - 1].dt;
                }

                if (celula[i - 1].TMModel == 0) {
                    celula[i].DTransDxR = -((1 - betI) * (flud.rDgD) * flud.Deng * 1.225 * (1. - fwd) * rsR * (6.29 / 35.31467) / boR) / (rhol * celula[i - 1].dx);
                    celula[i].DtransDxLinear = -acop * celula[i].QL *
                                                   ((1 - betI) * (flud.rDgD) * flud.Deng * 1.225 * (1. - fwd) * (6.29 / 35.31467) * (DRsBoR * celula[i].dpresaux)) / (celula[i - 1].dx) +
                                               acop * celula[i].QL *
                                                   ((1 - betI) * (flud.rDgD) * flud.Deng * 1.225 * (1. - fwd) * (6.29 / 35.31467) * (DRsBoR * celula[i].presaux)) / (celula[i - 1].dx);
                    celula[i].DTransDxRp = -acop * celula[i].QL *
                                           ((1 - betI) * (flud.rDgD) * flud.Deng * 1.225 * (1. - fwd) * (6.29 / 35.31467) * 0.5 * DRsBoR) / (celula[i - 1].dx);
                    celula[i].DTransDxL = ((1 - betL) * (flue.rDgD) * flue.Deng * 1.225 * (1. - fwe) * rsL * (6.29 / 35.31467) / boL) / (rhol0 * celula[i - 1].dx);
                    celula[i].DtransDxLinear = celula[i].DtransDxLinear +
                                               acop * celula[i - 1].QL * ((1 - betL) * (flue.rDgD) * flue.Deng * 1.225 * (1. - fwe) * (6.29 / 35.31467) * (DRsBoL * celula[i - 1].dpresaux)) / (celula[i - 1].dx) -
                                               acop * celula[i - 1].QL * ((1 - betL) * (flue.rDgD) * flue.Deng * 1.225 * (1. - fwe) * (6.29 / 35.31467) * (DRsBoL * celula[i - 1].presaux)) / (celula[i - 1].dx);
                    celula[i].DTransDxLp = acop * celula[i - 1].QL *
                                           ((1 - betL) * (flue.rDgD) * flue.Deng * 1.225 * (1. - fwe) * (6.29 / 35.31467) * 0.5 * DRsBoL) / (celula[i - 1].dx);
                    celula[i].DTransDt1 = -ativa * ((1. - fwC) * A1 * (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM *
                                                    (6.29 / 35.31467) / boM);
                    celula[i].DTransDt0 = -celula[i].DTransDt1;

                    celula[i - 1].CoefDTR = -((1. - celula[i - 1].bet) * (1. - fwC) * A1 *
                                              (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM * (6.29 / 35.31467) / boM);
                    celula[i - 1].CoefDTL = -celula[i - 1].CoefDTR;
                    celula[i - 1].coefTransBet = ((1. - fwC) * A1 * (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM * (6.29 / 35.31467) / boM);

                    // celula[i-1].DTransDtp=((1. - celula[i - 1].bet) * (1. - celula[i - 1].alf) * (1. - fwC)*A1
                    celula[i].transmassL -= ativa * ((1. - celula[i - 1].bet) * (1. - celula[i - 1].alf) * (1. - fwC) * A1 * (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM * (6.29 / 35.31467) / boM) / celula[i - 1].dt;
                    celula[i].transmassL += ativa * ((1. - celula[i - 1].betini) * (1. - celula[i - 1].alfini) * (1. - fwC) * A1 * (celula[i - 1].flui.rDgD) * celula[i - 1].flui.Deng * 1.225 * rsM * (6.29 / 35.31467) / boM) / celula[i - 1].dt;

                } else {
                    celula[i].DTransDxR = 0.;
                    celula[i].DTransDxL = 0.;
                    celula[i].DTransDt1 = 0.;
                    celula[i].DTransDxRp = 0.;
                    celula[i].DTransDxLp = 0.;
                    if (arq.desligaDeriTransMassDTemp == 1) {
                        celula[i - 1].DTransDtT = 0;
                        celula[i].DTransDtTL = 0.;
                    }
                    celula[i - 1].CoefDTR = 0.;
                    celula[i - 1].CoefDTL = 0.;
                    celula[i - 1].coefTransBet = 0.;
                }
            }
            if (celula[i - 1].TMModel == -2) {
                double veltit;
                if (celula[i].alfL > (*vg1dSP).localtiny && betI < (1. - (*vg1dSP).localtiny))
                    veltit = (celula[i].QG * rg + celula[i].QL * (1. - betI) * rl) / (A1 * (celula[i].alfL * rg + (1. - celula[i].alfL) * (1. - betI) * rl));
                else
                    veltit = 0.;
                double tit = celula[i - 1].flui.FracMassHidra(celula[i - 1].pres, celula[i - 1].temp);
                double raz = 0.999;
                double dtit = (tit - celula[i - 1].flui.FracMassHidra(celula[i - 1].pres * raz, celula[i - 1].temp)) / ((1 - raz) * celula[i - 1].pres);
                double dpres;
                dpres = (celula[i].presaux - celula[i - 1].presaux) / celula[i].dxL;
                celula[i].transmassL = celula[i - 1].transmassR = 1 * (celula[i].alfL * rg + (1. - celula[i].alfL) * (1. - betI) * rl) * veltit * (dpres * dtit) * A1;

                celula[i].DTransDxR = 0.;
                celula[i].DTransDxL = 0.;
                celula[i].DTransDt1 = 0.;
                celula[i].DTransDt0 = 0.;
                if (arq.desligaDeriTransMassDTemp == 1) {
                    celula[i - 1].DTransDtT = 0;
                    celula[i].DTransDtTL = 0.;
                }
                celula[i - 1].CoefDTR = 0.;
                celula[i - 1].CoefDTL = 0.;
                celula[i - 1].coefTransBet = 0.;
            }
            if (celula[i - 1].TMModel == 3) {
                celula[i].transmassL = celula[i - 1].transmassR = 0.;
                celula[i].DTransDxR = 0.;
                celula[i].DTransDxL = 0.;
                celula[i].DTransDt1 = 0.;
                celula[i].DTransDt0 = 0.;
                celula[i].DTransDxRp = 0.;
                celula[i].DTransDxLp = 0.;
                celula[i - 1].CoefDTR = 0.;
                celula[i - 1].CoefDTL = 0.;
                celula[i - 1].coefTransBet = 0.;
                if (arq.desligaDeriTransMassDTemp == 1) {
                    celula[i - 1].DTransDtT = 0;
                    celula[i].DTransDtTL = 0.;
                }
            }
            if (celula[i - 1].transmassR > 0 && (celula[i].alfL > (1. - (*vg1dSP).localtiny) || celula[i].betL > (1. - (*vg1dSP).localtiny))) {
                celula[i].transmassL = celula[i - 1].transmassR = -(*vg1dSP).localtiny;
                celula[i].DTransDxR = 0.;
                celula[i].DTransDxL = 0.;
                celula[i].DTransDt1 = 0.;
                celula[i].DTransDt0 = 0.;
                celula[i].DTransDxRp = 0.;
                celula[i].DTransDxLp = 0.;
                celula[i - 1].CoefDTR = 0.;
                celula[i - 1].CoefDTL = 0.;
                celula[i - 1].coefTransBet = 0.;
                if (arq.desligaDeriTransMassDTemp == 1) {
                    celula[i - 1].DTransDtT = 0;
                    celula[i].DTransDtTL = 0.;
                }
            }
            if (celula[i - 1].transmassR < 0 && celula[i].alfL < (*vg1dSP).localtiny) {
                celula[i].transmassL = celula[i - 1].transmassR = (*vg1dSP).localtiny;
                celula[i].DTransDxR = 0.;
                celula[i].DTransDxL = 0.;
                celula[i].DTransDt1 = 0.;
                celula[i].DTransDt0 = 0.;
                celula[i].DTransDxRp = 0.;
                celula[i].DTransDxLp = 0.;
                celula[i - 1].CoefDTR = 0.;
                celula[i - 1].CoefDTL = 0.;
                celula[i - 1].coefTransBet = 0.;
                if (arq.desligaDeriTransMassDTemp == 1) {
                    celula[i - 1].DTransDtT = 0;
                    celula[i].DTransDtTL = 0.;
                }
            }
            rhol0 = rhol;
            boL = boR;
            rsL = rsR;
            DRsBoL = DRsBoR;

        } else if (i == 0) {
            celula[0].transmassLini = celula[0].transmassL;
            celula[0].transmassL = 0.;
            renovaFonte(i);
            celula[1].fontemassLLini = celula[1].fontemassLL;
            celula[1].fontemassLL = celula[0].fontemassLR;
            celula[1].fontemassCLini = celula[1].fontemassCL;
            celula[1].fontemassCL = celula[0].fontemassCR;
            celula[1].fontemassGLini = celula[1].fontemassGL;
            celula[1].fontemassGL = celula[0].fontemassGR;
            rhol0 = (1 - celula[i].bet) * celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp) +
                    celula[i].bet * celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
            boL = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp);
            rsL = celula[i].flui.RS(celula[i].pres, celula[i].temp);
            if (arq.flashCompleto != 2 || arq.miniTabAtraso > 0) {
                double boL0 = celula[i].flui.BOFunc(celula[i].pres * 0.999, celula[i].temp);
                double rsL0 = celula[i].flui.RS(celula[i].pres * 0.999, celula[i].temp);
                DRsBoL = (rsL / boL - rsL0 / boL0) / (celula[i].pres * 0.001);
            } else {
                ProFlu flutemp = celula[i].flui;
                flutemp.atualizaPropComp(celula[i].pres * 0.999, celula[i].temp, flutemp.dCalculatedBeta,
                                         flutemp.oCalculatedLiqComposition,
                                         flutemp.oCalculatedVapComposition, arq.pocinjec);
                double boL0 = flutemp.BOFunc(celula[i].pres * 0.999, celula[i].temp);
                double rsL0 = flutemp.RS(celula[i].pres * 0.999, celula[i].temp);
                DRsBoL = (rsL / boL - rsL0 / boL0) / (celula[i].pres * 0.001);
            }
            ProFlu flutemp = celula[i].flui;
        }
    }
}

void SProd::avaliaParafina() {
    for (int i = 0; i <= ncel; i++) {
        if (i < ncel)
            celula[i].WaxDeposition(arq.detalParafina, ncel);
        else {
            if (masChkSup == 0 || celula[ncel].Mliqini > 0) {
                double cpDep = celula[i - 1].duto.cp[0];
                double kDep = celula[i - 1].duto.cond[0];
                double rhoDep = celula[i - 1].duto.rhoC[0];
                celula[i].deltaPar = celula[i - 1].deltaPar;
                if (celula[i].parafinado == 0 && celula[i].deltaPar > 0.) {
                    celula[i].duto.atualizaCamada(celula[i].deltaPar, arq.detalParafina.rug, cpDep, kDep, rhoDep);
                    celula[i].calor.atualiza(celula[i].duto, 1);
                    celula[i].parafinado = 1;
                } else if (celula[i].deltaPar > 0.) {
                    celula[i].duto.atualizaCamada2(celula[i].deltaPar, cpDep, kDep, rhoDep);
                    celula[i].calor.atualiza2(celula[i].duto);
                }
            } else
                celula[i].WaxDeposition(arq.detalParafina, ncel);
        }
    }
}

void SProd::renovaRGOdgYco2(ProFlu fluiRev) {
    Vcr<double> rgo(ncel);
    Vcr<double> dg(ncel);
    Vcr<double> yco2(ncel);
    Vcr<double> API(ncel);
    Vcr<double> BSW(ncel);
    Vcr<double> denag(ncel);
    Vcr<double> VISCL(ncel);
    Vcr<double> VISCH(ncel);
    double dt = celula[1].dt;
    double tL;
    if (arq.flashCompleto == 1 && (arq.tabent.tmin - 0) > (*vg1dSP).localtiny)
        tL = arq.tabent.tmin + 0.1;
    else
        tL = 0.;
    double tH;
    if (arq.flashCompleto == 1 && (70 - arq.tabent.tmax) > (*vg1dSP).localtiny)
        tH = arq.tabent.tmax - 0.1;
    else
        tH = 70.;
    int imin = 1;
    if ((*vg1dSP).chaverede != 0 && (celula[0].acsr.tipo == 10 || arq.ConContEntrada > 0))
        imin = 0;
    if (celula[0].acsr.tipo == 15 && (celula[0].fontemassCR + celula[0].fontemassGR + celula[0].fontemassLR) > 0.)
        celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
    else if (celula[0].acsr.tipo == 16 && (celula[0].fontemassCR + celula[0].fontemassGR + celula[0].fontemassLR) > 0.)
        celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = imin; i < ncel; i++) {
        double MultOe;
        double MultOd;
        double at = celula[i].duto.area;
        double dx = celula[i].dx;
        double ti0;
        if (celula[i].VTemper < 0.)
            ti0 = celula[i].temp;
        else {
            if (i > 0)
                ti0 = celula[i - 1].temp;
            else if (arq.ConContEntrada == 1)
                ti0 = tempE;
            else
                ti0 = celula[i].temp;
        }
        double ti1 = celula[i].temp;
        if (celula[i + 1].VTemper < 0.)
            ti1 = celula[i + 1].temp;

        double rgo0;
        double betI0;
        double bo0;
        double ba0;
        double bsw0;
        double rs0;
        double dg0O;
        double yco20O;
        double API0;
        double BSW0;
        double denag0;
        double viscL0;
        double viscH0;
        double razdgd0;
        double razdgl0;
        if (i > 0 || arq.ConContEntrada == 0) {
            if (i > 0 && celula[i].QG >= 0.)
                betI0 = celula[i - 1].betPigD;
            else
                betI0 = celula[i].betPigE;
        } else {
            if (celula[i].QG >= 0.)
                betI0 = betaE;
            else
                betI0 = celula[i].betPigE;
        }
        if ((i > 0 || arq.ConContEntrada == 1) && celula[i].QL >= 0.) {
            double pe;
            double te;
            if (i > 0) {
                pe = celula[i - 1].pres;
                te = celula[i - 1].temp;
            } else {
                pe = presE;
                te = tempE;
            }
            rgo0 = (*celula[i].fluiL).RGO;
            if (arq.ConContEntrada == 0)
                betI0 = celula[i - 1].betPigD; // testeBeta
            else
                betI0 = betaE; // testeBeta
            rs0 = (*celula[i].fluiL).RS(pe, te);
            bo0 = (*celula[i].fluiL).BOFunc(pe, te, rs0);
            ba0 = (*celula[i].fluiL).BAFunc(pe, te);
            bsw0 = (*celula[i].fluiL).BSW * ba0 / (bo0 + ba0 * (*celula[i].fluiL).BSW - (*celula[i].fluiL).BSW * bo0);
            rs0 = rs0 * 6.29 / 35.31467;
            dg0O = (*celula[i].fluiL).Deng;
            razdgd0 = 1 / (*celula[i].fluiL).rDgD;
            razdgl0 = 1 / (*celula[i].fluiL).rDgL;
            yco20O = (*celula[i].fluiL).yco2;
            API0 = (*celula[i].fluiL).API;
            BSW0 = (*celula[i].fluiL).BSW;
            denag0 = (*celula[i].fluiL).Denag;
            viscL0 = 0 * 30 + 1 * (*celula[i].fluiL).VisOM(tL);
            viscH0 = 0 * 20 + 1 * (*celula[i].fluiL).VisOM(tH);
        } else {
            betI0 = celula[i].betPigE; // testebeta
            rgo0 = celula[i].flui.RGO;
            rs0 = celula[i].flui.RS(celula[i].pres, celula[i].temp);
            bo0 = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs0);
            ba0 = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
            bsw0 = celula[i].flui.BSW * ba0 / (bo0 + ba0 * celula[i].flui.BSW - celula[i].flui.BSW * bo0);
            rs0 = rs0 * 6.29 / 35.31467;
            dg0O = celula[i].flui.Deng;
            razdgd0 = 1 / celula[i].flui.rDgD;
            razdgl0 = 1 / celula[i].flui.rDgL;
            yco20O = celula[i].flui.yco2;
            API0 = celula[i].flui.API;
            BSW0 = celula[i].flui.BSW;
            denag0 = celula[i].flui.Denag;
            viscL0 = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
            viscH0 = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
        }
        if (bo0 < 1e-15)
            bo0 = 1e-15;

        double rgo1 = celula[i].flui.RGO;
        double betI1 = celula[i].betPigD;
        double rs1 = celula[i].flui.RS(celula[i].pres, celula[i].temp);
        double bo1 = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs1);
        double ba1 = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
        double bsw1 = celula[i].flui.BSW * ba1 / (bo1 + ba1 * celula[i].flui.BSW - celula[i].flui.BSW * bo1);
        rs1 = rs1 * 6.29 / 35.31467;
        double dg1O = celula[i].flui.Deng;
        double yco21O = celula[i].flui.yco2;
        double API1 = celula[i].flui.API;
        double BSW1 = celula[i].flui.BSW;
        double denag1 = celula[i].flui.Denag;
        double viscL1 = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
        double viscH1 = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
        double razdgd1 = 1 / celula[i].flui.rDgD;
        double razdgl1 = 1 / celula[i].flui.rDgL;

        // betI1 = celula[i + 1].betPigE;    //duvidabeta
        if (celula[i + 1].QL < 0.) {
            betI1 = celula[i + 1].betPigE; // testeBeta
            rgo1 = celula[i + 1].flui.RGO;
            rs1 = celula[i + 1].flui.RS(celula[i + 1].pres, celula[i + 1].temp);
            bo1 = celula[i + 1].flui.BOFunc(celula[i + 1].pres, celula[i + 1].temp, rs1);
            ba1 = celula[i + 1].flui.BAFunc(celula[i + 1].pres, celula[i + 1].temp);
            bsw1 = celula[i + 1].flui.BSW * ba1 / (bo1 + ba1 * celula[i + 1].flui.BSW - celula[i + 1].flui.BSW * bo1);
            rs1 = rs1 * 6.29 / 35.31467;
            dg1O = celula[i + 1].flui.Deng;
            razdgd1 = 1 / celula[i + 1].flui.rDgD;
            razdgl1 = 1 / celula[i + 1].flui.rDgL;
            yco21O = celula[i + 1].flui.yco2;
            API1 = celula[i + 1].flui.API;
            BSW1 = celula[i + 1].flui.BSW;
            denag1 = celula[i + 1].flui.Denag;
            viscL1 = 0 * 30 + 1 * celula[i + 1].flui.VisOM(tL);
            viscH1 = 0 * 20 + 1 * celula[i + 1].flui.VisOM(tH);
        }
        if (bo1 < 1e-15)
            bo1 = 1e-15;

        double rhog0;
        double rhogST0;
        double dg0G;
        double yco20G;
        if ((i > 0 || arq.ConContEntrada == 1) && celula[i].QG > 0) {
            double pe;
            double te;
            if (i > 0) {
                pe = celula[i - 1].pres;
                te = celula[i - 1].temp;
            } else {
                pe = presE;
                te = tempE;
            }
            rhog0 = celula[i].rgL;
            rhogST0 = (*celula[i].fluiL).Deng * 1.225;
            dg0G = (*celula[i].fluiL).Deng;
            yco20G = (*celula[i].fluiL).yco2;
        } else {
            rhog0 = celula[i].rgC;
            rhogST0 = celula[i].flui.Deng * 1.225;
            dg0G = celula[i].flui.Deng;
            yco20G = celula[i].flui.yco2;
        }

        double rhog1 = celula[i].rgC;
        double rhogST1 = celula[i].flui.Deng * 1.225;
        double dg1G = celula[i].flui.Deng;
        double yco21G = celula[i].flui.yco2;
        if (celula[i + 1].QG <= 0.) {
            rhog1 = celula[i].rgR;
            rhogST1 = celula[i + 1].flui.Deng * 1.225;
            dg1G = celula[i + 1].flui.Deng;
            yco21G = celula[i + 1].flui.yco2;
        }

        if (i == 237) {
            int para;
            para = 0;
        }

        double hol = 1. - celula[i].alf;
        double bet = celula[i].bet;
        double rholST = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) + celula[i].flui.BSW * 1000 * celula[i].flui.Denag;
        double rhog = celula[i].rgC;
        double rhogST = celula[i].flui.Deng * 1.225;
        double rs = celula[i].flui.RS(celula[i].pres, celula[i].temp);
        double razdgd = 1 / celula[i].flui.rDgD;
        double razdgl = 1 / celula[i].flui.rDgL;
        double bo = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs);
        double ba = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
        double bsw = celula[i].flui.BSW * ba / (bo + ba * celula[i].flui.BSW - celula[i].flui.BSW * bo);
        rs = rs * 6.29 / 35.31467;

        double dgini = celula[i].flui.Deng;
        double yco2ini = celula[i].flui.yco2;
        double rgoini = celula[i].flui.RGO;
        double fonteO = celula[i].fontemassLR;
        double fonteG = celula[i].fontemassGR;
        double fonteP = celula[i].fontemassLR;
        double fonteA = celula[i].fontemassLR;
        double APIini = celula[i].flui.API;
        double BSWini = celula[i].flui.BSW;
        double denagini = celula[i].flui.Denag;
        double APIF = APIini;
        double BSWF = BSWini;
        double denagF = denagini;
        double dgFO = dgini;
        double dgFG = dgini;
        double yco2FO = yco2ini;
        double yco2FG = yco2ini;
        double rgoFO = rgoini;
        double viscLini = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
        double viscHini = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
        double viscLF = viscLini;
        double viscHF = viscHini;
        double rholSTF = rholST;
        double rhogSTF = rhogST;
        double razdgdF = 1.;
        double razdglF = 1.;
        double titFonte = 0.;
        ProFlu fluF;
        if (celula[i].acsr.tipo == 1 && celula[i].acsr.injg.seco == 1) {
            if (celula[i].acsr.injg.QGas > 0.)
                fluF = celula[i].acsr.injg.FluidoPro;
            else
                fluF = celula[i].flui;
            dgFG = fluF.Deng;
            yco2FG = fluF.yco2;
            rhogSTF = fluF.Deng * 1.225;
        } else if (celula[i].acsr.tipo == 1 && celula[i].acsr.injg.seco == 0) {
            if (celula[i].acsr.injg.QGas > 0.)
                fluF = celula[i].acsr.injg.FluidoPro;
            else
                fluF = celula[i].flui;

            titFonte = fluF.dStockTankVaporMassFraction;
            dgFO = fluF.Deng;
            yco2FO = fluF.yco2;
            rgoFO = fluF.RGO;

            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            razdgdF = 1 / fluF.rDgD;
            razdglF = 1 / fluF.rDgL;
            if (celula[i].acsr.injg.FluidoPro.BSW < 1 - (*vg1dSP).localtiny)
                rholSTF = (1 - fluF.BSW) * (1000 * 141.5 / (131.5 + fluF.API)) + fluF.BSW * 1000 * fluF.Denag + fluF.Deng * 1.225 * rgoFO * (1. - fluF.BSW);
            else
                rholSTF = fluF.BSW * 1000 * fluF.Denag;
            fonteO = (fonteO + fonteG) * razdgdF * (fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467) * (1. - fluF.BSW) / rholSTF);

            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = fluF.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw < (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) + (bswaux / contrabsw) * 1000 * fluF.Denag + fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (fluF.BSW * 1000 * fluF.Denag));
                }
                APIF = fluF.API;
                BSWF = fluF.BSW;
                denagF = celula[i].acsr.injg.FluidoPro.Denag;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 2) {
            if (celula[i].acsr.injl.QLiq > 0.)
                fluF = celula[i].acsr.injl.FluidoPro;
            else
                fluF = celula[i].flui;

            titFonte = fluF.dStockTankVaporMassFraction;
            dgFO = fluF.Deng;
            yco2FO = fluF.yco2;
            rgoFO = fluF.RGO;

            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            razdgdF = 1 / fluF.rDgD;
            razdglF = 1 / fluF.rDgL;
            if (fluF.BSW < 1 - (*vg1dSP).localtiny)
                rholSTF = (1 - fluF.BSW) * (1000 * 141.5 / (131.5 + fluF.API)) + fluF.BSW * 1000 * fluF.Denag + fluF.Deng * 1.225 * rgoFO * (1. - fluF.BSW);
            else
                rholSTF = fluF.BSW * 1000 * fluF.Denag;
            fonteO = (fonteO + fonteG) * razdgdF * (fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467) * (1. - fluF.BSW) / rholSTF);

            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = fluF.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw < (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) + (bswaux / contrabsw) * 1000 * fluF.Denag + fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (fluF.BSW * 1000 * fluF.Denag));
                }
                APIF = fluF.API;
                BSWF = fluF.BSW;
                denagF = fluF.Denag;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 10) {
            if ((celula[i].acsr.injm.MassC + celula[i].acsr.injm.MassG + celula[i].acsr.injm.MassP) > 0.)
                fluF = celula[i].acsr.injm.FluidoPro;
            else
                fluF = celula[i].flui;

            titFonte = fluF.dStockTankVaporMassFraction;
            dgFO = fluF.Deng;
            yco2FO = fluF.yco2;
            rgoFO = fluF.RGO;

            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            razdgdF = 1 / fluF.rDgD;
            razdglF = 1 / fluF.rDgL;
            rholSTF = (1 - fluF.BSW) * (1000 * 141.5 / (131.5 + fluF.API)) +
                      fluF.BSW * 1000 *
                          fluF.Denag +
                      fluF.Deng * 1.225 * rgoFO * (1. - fluF.BSW);
            fonteO = (fonteO + fonteG) * razdgdF * (fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467) * (1. - fluF.BSW) / rholSTF);

            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = fluF.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw < (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) +
                              (bswaux / contrabsw) * 1000 *
                                  fluF.Denag +
                              fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (fluF.BSW * 1000 * fluF.Denag));
                }
                APIF = fluF.API;
                BSWF = fluF.BSW;
                denagF = fluF.Denag;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 3) {
            if ((celula[i].acsr.ipr.Pres) > celula[i].pres)
                fluF = celula[i].acsr.ipr.FluidoPro;
            else
                fluF = celula[i].flui;

            titFonte = fluF.dStockTankVaporMassFraction;
            dgFO = fluF.Deng;
            yco2FO = fluF.yco2;
            rgoFO = fluF.RGO;

            rholSTF = (1 - fluF.BSW) * (1000 * 141.5 / (131.5 + fluF.API)) + fluF.BSW * 1000 * fluF.Denag + fluF.Deng * 1.225 * rgoFO * (1. - fluF.BSW);
            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            razdgdF = 1 / fluF.rDgD;
            razdglF = 1 / fluF.rDgL;
            fonteO = (fonteO + fonteG) * razdgdF * (fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467) * (1. - fluF.BSW) / rholSTF);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = fluF.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) + (bswaux / contrabsw) * 1000 * fluF.Denag + fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (fluF.BSW * 1000 * fluF.Denag));
                }
                APIF = fluF.API;
                BSWF = fluF.BSW;
                denagF = fluF.Denag;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 9) {
            ProFlu fluF;
            if (celula[i].acsr.fontechk.presT > celula[i].acsr.fontechk.pamb) {
                fluF = celula[i].acsr.fontechk.fluidoP;
            } else {
                fluF = celula[i].acsr.fontechk.fluidoPamb;
            }
            dgFO = fluF.Deng;
            yco2FO = fluF.yco2;
            rgoFO = fluF.RGO;
            titFonte = fluF.dStockTankVaporMassFraction;

            if (fluF.BSW < 1 - (*vg1dSP).localtiny)
                rholSTF = (1 - fluF.BSW) * (1000 * 141.5 / (131.5 + fluF.API)) + fluF.BSW * 1000 * fluF.Denag + fluF.Deng * 1.225 * rgoFO * (1. - fluF.BSW);
            else
                rholSTF = fluF.BSW * 1000 * fluF.Denag;

            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            razdgdF = 1 / fluF.rDgD;
            razdglF = 1 / fluF.rDgL;
            if (celula[i].acsr.fontechk.ambGas != 1 || (fonteO + fonteG) < 0.)
                fonteO = (fonteO + fonteG) * razdgdF * (fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467) * (1. - fluF.BSW) / rholSTF);
            else
                fonteO = 0.;
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = fluF.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) + (bswaux / contrabsw) * 1000 * fluF.Denag + fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (fluF.BSW * 1000 * fluF.Denag));
                }
                APIF = fluF.API;
                BSWF = fluF.BSW;
                denagF = fluF.Denag;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 15) {
            if ((celula[i].fontemassLR + celula[i].fontemassGR) > 1e-15)
                fluF = celula[i].acsr.radialPoro.flup;
            else
                fluF = celula[i].flui;

            titFonte = fluF.dStockTankVaporMassFraction;
            dgFO = fluF.Deng;
            yco2FO = fluF.yco2;
            rgoFO = fluF.RGO;

            rholSTF = (1 - celula[i].acsr.radialPoro.BSW) * (1000 * 141.5 / (131.5 + fluF.API)) + celula[i].acsr.radialPoro.BSW * 1000 * fluF.Denag + fluF.Deng * 1.225 * rgoFO * (1. - celula[i].acsr.radialPoro.BSW);
            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            razdgdF = 1 / fluF.rDgD;
            razdglF = 1 / fluF.rDgL;
            fonteO = (fonteO + fonteG) * razdgdF * (fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467) * (1. - celula[i].acsr.radialPoro.BSW) / rholSTF);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.radialPoro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) + (bswaux / contrabsw) * 1000 * fluF.Denag + fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.radialPoro.BSW * 1000 * fluF.Denag));
                }
                APIF = fluF.API;
                BSWF = celula[i].acsr.radialPoro.BSW;
                denagF = fluF.Denag;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 16) {
            if ((celula[i].fontemassLR + celula[i].fontemassGR) > 1e-15)
                fluF = celula[i].acsr.radialPoro.flup;
            else
                fluF = celula[i].flui;

            titFonte = fluF.dStockTankVaporMassFraction;
            dgFO = fluF.Deng;
            yco2FO = fluF.yco2;
            rgoFO = fluF.RGO;

            rholSTF = (1 - celula[i].acsr.poroso2D.dados.transfer.BSW) * (1000 * 141.5 / (131.5 + fluF.API)) + celula[i].acsr.poroso2D.dados.transfer.BSW * 1000 * fluF.Denag + fluF.Deng * 1.225 * rgoFO * (1. - celula[i].acsr.poroso2D.dados.transfer.BSW);
            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            razdgdF = 1 / fluF.rDgD;
            razdglF = 1 / fluF.rDgL;
            fonteO = (fonteO + fonteG) * razdgdF * (fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467) * (1. - celula[i].acsr.poroso2D.dados.transfer.BSW) / rholSTF);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.poroso2D.dados.transfer.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) + (bswaux / contrabsw) * 1000 * fluF.Denag + fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.poroso2D.dados.transfer.BSW * 1000 * fluF.Denag));
                }
                APIF = fluF.API;
                BSWF = celula[i].acsr.poroso2D.dados.transfer.BSW;
                denagF = fluF.Denag;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if ((fabs(fonteO) > (*vg1dSP).localtiny && celula[i].acsr.tipo != 2 && celula[i].acsr.tipo != 3 &&
                    celula[i].acsr.tipo != 9 && celula[i].acsr.tipo != 15 && celula[i].acsr.tipo != 16) ||
                   (fabs(fonteG) > (*vg1dSP).localtiny && celula[i].acsr.tipo != 1 && celula[i].acsr.tipo != 2 && celula[i].acsr.tipo != 3 && celula[i].acsr.tipo != 9 && celula[i].acsr.tipo != 15 && celula[i].acsr.tipo != 16)) {
            if (celula[i].acsr.tipo == 5 || celula[i].acsr.tipo == 8) {
                dgFO = celula[i].flui.Deng;
                yco2FO = celula[i].flui.yco2;
                rgoFO = celula[i].flui.RGO;
                titFonte = celula[i].flui.dStockTankVaporMassFraction;
                double rholiq = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) + celula[i].flui.BSW * 1000 * celula[i].flui.Denag;
                double rhogas = celula[i].flui.Deng * 1.225;
                rholSTF = rholiq + rhogas * rgoFO * (1. - celula[i].flui.BSW);
                rhogSTF = rhogas;
                double rsF = celula[i].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                razdgdF = 1 / celula[i].flui.rDgD;
                razdglF = 1 / celula[i].flui.rDgL;
                fonteO = (fonteO + fonteG) * (razdgdF * rsF * (1. - celula[i].flui.BSW) / rholSTF);
                if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                    double rhoPSTF = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) + celula[i].flui.BSW * 1000 * celula[i].flui.Denag;
                    fonteP *= ((1 - celula[i].flui.BSW) / rhoPSTF);
                    fonteA *= (celula[i].flui.BSW / rhoPSTF);
                    APIF = celula[i].flui.API;
                    BSWF = celula[i].flui.BSW;
                    viscLF = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
                    viscHF = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
                }
            } else if ((*celula[i].acsrL).tipo == 5 || (*celula[i].acsrL).tipo == 8) {
                double rholiq;
                double rhogas;

                if (i > 0) {
                    dgFO = celula[i - 1].flui.Deng;
                    yco2FO = celula[i - 1].flui.yco2;
                    rgoFO = celula[i - 1].flui.RGO;
                    rholiq = (1 - celula[i - 1].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i - 1].flui.API)) +
                             celula[i - 1].flui.BSW * 1000 * celula[i - 1].flui.Denag;
                    rhogas = celula[i - 1].flui.Deng * 1.225;
                    titFonte = celula[i - 1].flui.dStockTankVaporMassFraction;
                    rholSTF = rholiq + rhogas * rgoFO * (1. - celula[i - 1].flui.BSW);
                    rhogSTF = rhogas;
                    double rsF = celula[i - 1].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                    razdgdF = 1 / celula[i - 1].flui.rDgD;
                    razdglF = 1 / celula[i - 1].flui.rDgL;
                    fonteO = (fonteO + fonteG) * (razdgdF * rsF *
                                                  (1. - celula[i - 1].flui.BSW) / rholSTF);
                    if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                        double rhoPSTF = (1 - celula[i - 1].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i - 1].flui.API)) +
                                         celula[i - 1].flui.BSW * 1000 *
                                             celula[i - 1].flui.Denag;
                        fonteP *= ((1 - celula[i - 1].flui.BSW) / rhoPSTF);
                        fonteA *= (celula[i - 1].flui.BSW / rhoPSTF);
                        APIF = celula[i - 1].flui.API;
                        BSWF = celula[i - 1].flui.BSW;
                        denagF = celula[i - 1].flui.Denag;
                        viscLF = 0 * 30 + 1 * celula[i - 1].flui.VisOM(tL);
                        viscHF = 0 * 20 + 1 * celula[i - 1].flui.VisOM(tH);
                    }
                } else {
                    dgFO = celula[i].flui.Deng;
                    yco2FO = celula[i].flui.yco2;
                    rgoFO = celula[i].flui.RGO;
                    rholiq = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) +
                             celula[i].flui.BSW * 1000 * celula[i].flui.Denag;
                    rhogas = celula[i].flui.Deng * 1.225;
                    titFonte = celula[i].flui.dStockTankVaporMassFraction;
                    rholSTF = rholiq + rhogas * rgoFO * (1. - celula[i].flui.BSW);
                    rhogSTF = rhogas;
                    double rsF = celula[i].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                    razdgdF = 1 / celula[i].flui.rDgD;
                    razdglF = 1 / celula[i].flui.rDgL;
                    fonteO = (fonteO + fonteG) * (razdgdF * celula[i].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467) *
                                                  (1. - celula[i].flui.BSW) / rholSTF);
                    if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                        double rhoPSTF = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) +
                                         celula[i].flui.BSW * 1000 *
                                             celula[i].flui.Denag;
                        fonteP *= ((1 - celula[i].flui.BSW) / rhoPSTF);
                        fonteA *= (celula[i].flui.BSW / rhoPSTF);
                        APIF = celula[i].flui.API;
                        BSWF = celula[i].flui.BSW;
                        denagF = celula[i].flui.Denag;
                        viscLF = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
                        viscHF = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
                    }
                }
            }
        }
        fonteG *= (razdglF / (rhogSTF));

        if (titFonte > 1. - 1e-15) {
            fonteO = 0.;
            fonteP = 0.;
            fonteA = 0.;
        }

        MultOe = 0.;
        if (celula[i].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            MultOe = celula[i].QL * (1 - betI0) * (1 - bsw0) * razdgd0 * rs0 / bo0;
        MultOd = 0.;
        if (celula[i + 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            MultOd = celula[i + 1].QL * (1 - betI1) * (1 - bsw1) * razdgd1 * rs1 / bo1;
        double MultGe = (celula[i].MC - celula[i].Mliqini) * razdgl0 / (rhogST0);
        double MultGd = (celula[i + 1].MC - celula[i + 1].Mliqini) * razdgl1 / (rhogST1);
        double volleveFim = (((1 - hol) * rhog * razdgl / (rhogST)) + hol * (1 - bet) * (1. - bsw) * rs * razdgd / (bo));
        if (volleveFim < 1e-15)
            volleveFim = 0.;
        double residuo = (volleveFim - celula[i].VolLeveST) * at / dt + (MultOd - MultOe) / dx + (MultGd - MultGe) / dx - (fonteO / dx + fonteG / dx);
        double volpesFim = hol * (1 - bet) * (1 - bsw) / bo;
        double volaguaFim = hol * (1 - bet) * bsw; // nao deveria ser dividido por Bo???????????
        double MultPe = 0.;
        double MultPd = 0.;
        double residuoP = 0.;
        double MultAe = 0.;
        double MultAd = 0.;
        double residuoA;
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            MultPe = 0.;
            if (celula[i].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultPe = celula[i].QL * (1 - betI0) * (1 - bsw0) / bo0;
            MultPd = 0.;
            if (celula[i + 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultPd = celula[i + 1].QL * (1 - betI1) * (1 - bsw1) / bo1;
            residuoP = (volpesFim - celula[i].VolPesaST) * at / dt + (MultPd - MultPe) / dx - fonteP / dx;
            MultAe = 0.;
            if (celula[i].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultAe = celula[i].QL * (1 - betI0) * bsw0 / bo0;
            MultAd = 0.;
            if (celula[i + 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultAd = celula[i + 1].QL * (1 - betI1) * bsw1 / bo1;
            residuoA = (volaguaFim - celula[i].VolAguaST) * at / dt + (MultAd - MultAe) / dx - fonteA / dx;
        }
        rgo[i] = (*vg1dSP).RGOMax;
        if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny)) {
            rgo[i] = (volleveFim - residuo * dt / at) * bo / (hol * (1 - bet) * (1 - bsw));
            if (rgo[i] > (*vg1dSP).RGOMax)
                rgo[i] = (*vg1dSP).RGOMax;
        } else if (bet >= (1. - (*vg1dSP).localtiny) || bsw >= (1. - (*vg1dSP).localtiny))
            rgo[i] = 0.;
        else
            rgo[i] = (*vg1dSP).RGOMax;

        if (volleveFim > 1e-5 && arq.flashCompleto == 0 && ((fonteG >= 0 || fonteO > 0) || ((MultGd < 0 || MultGe > 0) || (MultOd < 0 || MultOe > 0)))) {
            dg[i] = (dt * (dgFO * fonteO / dx + dgFG * fonteG / dx + 1. * dgini * residuo - (dg1O * MultOd - dg0O * MultOe) / dx - (dg1G * MultGd - dg0G * MultGe) / dx) + dgini * celula[i].VolLeveST * at) /
                    (volleveFim * at - 0. * residuo * dt);
            yco2[i] = (dt * (yco2FO * fonteO / dx + yco2FG * fonteG / dx + 1. * yco2ini * residuo - (yco21O * MultOd - yco20O * MultOe) / dx - (yco21G * MultGd - yco20G * MultGe) / dx) + yco2ini * celula[i].VolLeveST * at) /
                      (volleveFim * at - 0. * residuo * dt);
            if (yco2[i] < 0.)
                yco2[i] = 0.;
            else if (yco2[i] > 1.)
                yco2[i] = 1.;
        } else {
            dg[i] = dgini;
            yco2[i] = yco2ini;
        }
        if ((arq.nfluP > 1 && arq.flashCompleto == 0) || (*vg1dSP).chaverede != 0) {
            if (volpesFim > 1e-3 && (fonteP > 0 || (MultPd < 0 || MultPe > 0))) {
                double denmixSTDF = 141.5 / (131.5 + APIF);
                double denmixSTDini = 141.5 / (131.5 + APIini);
                double denmixSTD1 = 141.5 / (131.5 + API1);
                double denmixSTD0 = 141.5 / (131.5 + API0);
                API[i] = (dt * (denmixSTDF * fonteP / dx + 1. * denmixSTDini * residuoP - (denmixSTD1 * MultPd - denmixSTD0 * MultPe) / dx) + denmixSTDini * celula[i].VolPesaST * at) / (volpesFim * at - 0. * residuoP * dt);
                API[i] = 141.5 / API[i] - 131.5;
                VISCL[i] = (dt * (viscLF * fonteP / dx + 1. * viscLini * residuoP - (viscL1 * MultPd - viscL0 * MultPe) / dx) + viscLini * celula[i].VolPesaST * at) / (volpesFim * at - 0. * residuoP * dt);
                VISCH[i] = (dt * (viscHF * fonteP / dx + 1. * viscHini * residuoP - (viscH1 * MultPd - viscH0 * MultPe) / dx) + viscHini * celula[i].VolPesaST * at) / (volpesFim * at - 0. * residuoP * dt);
            } else {
                API[i] = APIini;
                VISCL[i] = viscLini;
                VISCH[i] = viscHini;
            }
        }
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            if ((volaguaFim + volpesFim) > 1e-3 && ((fonteP > 0 || fonteA > 0) ||
                                                    ((MultPd < 0 || MultPe > 0) || (MultAd < 0 || MultAe > 0)))) {
                BSW[i] = (dt * (BSWF * (fonteA + fonteP) / dx + 1. * BSWini * (residuoA + residuoP) - (BSW1 * (MultAd + MultPd) - BSW0 * (MultAe + MultPe)) / dx) + BSWini * (celula[i].VolAguaST + celula[i].VolPesaST) * at) /
                         ((volaguaFim + volpesFim) * at - 0. * (residuoA + residuoP) * dt);
                denag[i] = (dt * (denagF * (fonteA) / dx + 1. * denagini * (residuoA) - (denag1 * (MultAd)-denag0 * (MultAe)) / dx) + denagini * (celula[i].VolAguaST) * at) /
                           ((volaguaFim)*at - 0. * (residuoA)*dt);
                if (BSW[i] < 0.)
                    BSW[i] = 0.;
                else if (BSW[i] > 1.)
                    BSW[i] = 1.;
                if (denag[i] < 1.)
                    denag[i] = 1.;
            } else
                BSW[i] = BSWini;
            denag[i] = denagini;
        }
        celula[i].VolLeveST = volleveFim;
        celula[i].VolPesaST = volpesFim;
        celula[i].VolAguaST = volaguaFim;
    }
    for (int i = imin; i <= ncel - 1; i++) {
        if (trackRGO > 0 && celula[i].flui.corrSat != 4)
            celula[i].flui.RGO = rgo[i];
        if (trackDeng > 0 && arq.flashCompleto == 0) {
            celula[i].flui.Deng = dg[i];
            if ((celula[i].flui.Deng > 5 || celula[i].flui.Deng < 0) && i > 0)
                celula[i].flui.Deng = celula[i - 1].flui.Deng;
            celula[i].flui.yco2 = yco2[i];
        }
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            celula[i].flui.BSW = BSW[i];
            celula[i].flui.Denag = denag[i];
            if (arq.flashCompleto == 0) {
                celula[i].flui.API = API[i];
                celula[i].flui.LVisL = VISCL[i];
                celula[i].flui.LVisH = VISCH[i];
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
            }
        }
        if (arq.flashCompleto == 0)
            celula[i].flui.RenovaFluido();
        corrDeng(i);
    }

    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || celula[ncel].Mliqini > -(*vg1dSP).localtiny) {
        if (trackRGO > 0 && celula[ncel].flui.corrSat != 4)
            celula[ncel].flui.RGO = rgo[ncel - 1];
        if (trackDeng > 0 && arq.flashCompleto == 0) {
            celula[ncel].flui.Deng = dg[ncel - 1];
            celula[ncel].flui.yco2 = yco2[ncel - 1];
        }
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            celula[ncel].flui.BSW = BSW[ncel - 1];
            celula[ncel].flui.Denag = denag[ncel - 1];
            if (arq.flashCompleto == 0) {
                celula[ncel].flui.API = API[ncel - 1];
                celula[ncel].flui.LVisL = VISCL[ncel - 1];
                celula[ncel].flui.LVisH = VISCH[ncel - 1];
                celula[ncel].flui.TempL = tL;
                celula[ncel].flui.TempH = tH;
            }
        }
    } else {
        celula[ncel].flui.RGO = fluiRev.RGO;
        celula[ncel].flui.Deng = fluiRev.Deng;
        celula[ncel].flui.yco2 = fluiRev.yco2;
        celula[ncel].flui.API = fluiRev.API;
        celula[ncel].flui.BSW = fluiRev.BSW;
        celula[ncel].flui.Denag = fluiRev.Denag;
        celula[ncel].flui.LVisL = fluiRev.LVisL;
        celula[ncel].flui.LVisH = fluiRev.LVisH;
        celula[ncel].flui.TempL = fluiRev.TempL;
        celula[ncel].flui.TempH = fluiRev.TempH;
    }
    if (arq.flashCompleto == 0)
        celula[ncel].flui.RenovaFluido();
    if (arq.corrDeng == 0) {
        celula[ncel].flui.rDgD = 1.;
        celula[ncel].flui.rDgL = 1.;
        celula[ncel].flui.PCis = celula[ncel].flui.PC;
        celula[ncel].flui.TCis = celula[ncel].flui.TC;
    } else {
        celula[ncel].flui.razDegD(celula[ncel].pres, celula[ncel].temp);
        celula[ncel].flui.rzDegL(celula[ncel].pres, celula[ncel].temp);
        celula[ncel].flui.PcTcIS();
    }
}

/*** alteracao4 ***/

void SProd::renovaFracMol(ProFlu fluiRev) {
    Vcr<double> BSW(ncel);
    Vcr<double> VISCL(ncel);
    Vcr<double> VISCH(ncel);
    int ncomp = arq.npseudo;
    ProFlu *fluC;
    fluC = new ProFlu[ncel];
    Vcr<double> fracMol0(ncomp);
    Vcr<double> fracMol1(ncomp);
    Vcr<double> fracMolF(ncomp);
    double pesoMol0;
    double pesoMol1;
    double pesoMolF;
    double pesoMolC;
    double dt = celula[1].dt;
    double tL = 0.;
    double tH = 70.;
    if (celula[0].acsr.tipo == 15 && (celula[0].fontemassCR + celula[0].fontemassGR + celula[0].fontemassLR) > 0.)
        celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
    else if (celula[0].acsr.tipo == 16 && (celula[0].fontemassCR + celula[0].fontemassGR + celula[0].fontemassLR) > 0.)
        celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
    int imin = 1;
    if ((*vg1dSP).chaverede != 0 && (celula[0].acsr.tipo == 10 || arq.ConContEntrada > 0))
        imin = 0;
    for (int i = imin; i < ncel; i++) {
        double at = celula[i].duto.area;
        double dx = celula[i].dx;
        double ti0;
        if (celula[i].VTemper < 0.)
            ti0 = celula[i].temp;
        else {
            if (i > 0)
                ti0 = celula[i - 1].temp;
            else if (arq.ConContEntrada == 1)
                ti0 = tempE;
            else
                ti0 = celula[i].temp;
        }
        double ti1 = celula[i].temp;
        if (celula[i + 1].VTemper < 0.)
            ti1 = celula[i + 1].temp;

        double titF = 0.;
        ProFlu fluF;

        double boF = 1.;
        double baF = 1.;
        double fwF = 1.;
        double rhoOF = 900.;
        double rhoWF = 1000.;

        double betI0;
        double bo0;
        double ba0;
        double bsw0;
        double BSW0;
        double viscL0;
        double viscH0;
        if (i > 0 || arq.ConContEntrada == 0) {
            if (i > 0 && celula[i].QG >= 0.)
                betI0 = celula[i - 1].betPigD;
            else
                betI0 = celula[i].betPigE;
        } else {
            if (celula[i].QG >= 0.)
                betI0 = betaE;
            else
                betI0 = celula[i].betPigE;
        }
        if ((i > 0 || arq.ConContEntrada == 1) && celula[i].QL >= 0.) {
            double pe;
            double te;
            if (i > 0) {
                pe = celula[i - 1].pres;
                te = celula[i - 1].temp;
            } else {
                pe = presE;
                te = tempE;
            }
            if (arq.ConContEntrada == 0)
                betI0 = celula[i - 1].betPigD; // testeBeta
            else
                betI0 = betaE; // testeBeta
            double rs0 = (*celula[i].fluiL).RS(pe, te);
            bo0 = (*celula[i].fluiL).BOFunc(pe, te, rs0);
            ba0 = (*celula[i].fluiL).BAFunc(pe, te);
            bsw0 = (*celula[i].fluiL).BSW * ba0 / (bo0 + ba0 * (*celula[i].fluiL).BSW - (*celula[i].fluiL).BSW * bo0);
            BSW0 = (*celula[i].fluiL).BSW;
            viscL0 = 0 * 30 + 1 * (*celula[i].fluiL).VisOM(tL);
            viscH0 = 0 * 20 + 1 * (*celula[i].fluiL).VisOM(tH);
        } else {
            betI0 = celula[i].betPigE; // testebeta
            double rs0 = celula[i].flui.RS(celula[i].pres, celula[i].temp);
            bo0 = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs0);
            ba0 = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
            bsw0 = celula[i].flui.BSW * ba0 / (bo0 + ba0 * celula[i].flui.BSW - celula[i].flui.BSW * bo0);
            BSW0 = celula[i].flui.BSW;
            viscL0 = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
            viscH0 = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
        }
        if (bo0 < 1e-15)
            bo0 = 1e-15;

        double betI1 = celula[i].betPigD;
        double rs1 = celula[i].flui.RS(celula[i].pres, celula[i].temp);
        double bo1 = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs1);
        double ba1 = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
        double bsw1 = celula[i].flui.BSW * ba1 / (bo1 + ba1 * celula[i].flui.BSW - celula[i].flui.BSW * bo1);
        double BSW1 = celula[i].flui.BSW;
        double viscL1 = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
        double viscH1 = 0 * 20 + 1 * celula[i].flui.VisOM(tH);

        // betI1 = celula[i + 1].betPigE;    //duvidabeta
        if (celula[i + 1].QL < 0.) {
            betI1 = celula[i + 1].betPigE; // testeBet
            rs1 = celula[i + 1].flui.RS(celula[i + 1].pres, celula[i + 1].temp);
            bo1 = celula[i + 1].flui.BOFunc(celula[i + 1].pres, celula[i + 1].temp, rs1);
            ba1 = celula[i + 1].flui.BAFunc(celula[i + 1].pres, celula[i + 1].temp);
            bsw1 = celula[i + 1].flui.BSW * ba1 / (bo1 + ba1 * celula[i + 1].flui.BSW - celula[i + 1].flui.BSW * bo1);
            BSW1 = celula[i + 1].flui.BSW;
            viscL1 = 0 * 30 + 1 * celula[i + 1].flui.VisOM(tL);
            viscH1 = 0 * 20 + 1 * celula[i + 1].flui.VisOM(tH);
        }
        if (bo1 < 1e-15)
            bo1 = 1e-15;
        double titV0;
        double titV1;
        double vazMasLiq0 = celula[i].MliqiniL;
        double vazMasLiq1 = celula[i + 1].MliqiniL;
        double vazMasGas0 = celula[i].MC - celula[i].MliqiniL;
        double vazMasGas1 = celula[i + 1].MC - celula[i + 1].MliqiniL;
        pesoMolC = 0.;
        for (int kfrac = 0; kfrac < ncomp; kfrac++) {
            pesoMolC += celula[i].flui.masMol[kfrac] * celula[i].flui.fracMol[kfrac];
        }
        if ((i > 0 || arq.ConContEntrada == 1) && celula[i].MC >= 0.) {
            double pe;
            double te;
            if (i > 0) {
                pe = celula[i - 1].pres;
                te = celula[i - 1].temp;
            } else {
                pe = presE;
                te = tempE;
            }
            double fwV = bsw0;
            double rhoOV = (*celula[i].fluiL).MasEspoleo(pe, te);
            double rhoWV = (*celula[i].fluiL).MasEspAgua(pe, te);
            titV0 = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
            vazMasLiq0 -= betI0 * celula[i].QL * celula[i].fluicol.MasEspFlu(pe, te);
            vazMasLiq0 *= titV0;
            pesoMol0 = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol0[kfrac] = (*celula[i].fluiL).fracMol[kfrac];
                pesoMol0 += (*celula[i].fluiL).masMol[kfrac] * (*celula[i].fluiL).fracMol[kfrac];
            }
        } else {
            double fwV = bsw0;
            double rhoOV = celula[i].flui.MasEspoleo(celula[i].pres, celula[i].temp);
            double rhoWV = celula[i].flui.MasEspAgua(celula[i].pres, celula[i].temp);
            titV0 = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
            vazMasLiq0 -= betI0 * celula[i].QL * celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
            vazMasLiq0 *= titV0;
            pesoMol0 = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol0[kfrac] = celula[i].flui.fracMol[kfrac];
                pesoMol0 += celula[i].flui.masMol[kfrac] * celula[i].flui.fracMol[kfrac];
            }
        }
        if (celula[i + 1].MC >= 0.) {
            double fwV = bsw1;
            double rhoOV = celula[i].flui.MasEspoleo(celula[i].pres, celula[i].temp);
            double rhoWV = celula[i].flui.MasEspAgua(celula[i].pres, celula[i].temp);
            titV1 = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
            vazMasLiq1 -= betI1 * celula[i].QL * celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
            vazMasLiq1 *= titV1;
            pesoMol1 = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol1[kfrac] = celula[i].flui.fracMol[kfrac];
                pesoMol1 += celula[i].flui.masMol[kfrac] * celula[i].flui.fracMol[kfrac];
            }
        } else {
            double fwV = bsw1;
            double rhoOV = celula[i + 1].flui.MasEspoleo(celula[i + 1].pres, celula[i + 1].temp);
            double rhoWV = celula[i + 1].flui.MasEspAgua(celula[i + 1].pres, celula[i + 1].temp);
            titV1 = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
            vazMasLiq1 -= betI1 * celula[i + 1].QL * celula[i].fluicol.MasEspFlu(celula[i + 1].pres, celula[i + 1].temp);
            vazMasLiq1 *= titV1;
            pesoMol1 = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol1[kfrac] = celula[i + 1].flui.fracMol[kfrac];
                pesoMol1 += celula[i + 1].flui.masMol[kfrac] * celula[i + 1].flui.fracMol[kfrac];
            }
        }
        double fonteO = celula[i].fontemassLR;
        double fonteG = celula[i].fontemassGR;
        double fonteP = celula[i].fontemassLR;
        double fonteA = celula[i].fontemassLR;

        if (celula[i].acsr.tipo == 1) {
            celula[i].acsr.injg.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, celula[i].flui.dCalculatedBeta,
                                                           celula[i].flui.oCalculatedLiqComposition, celula[i].flui.oCalculatedVapComposition, arq.pocinjec);
            if (celula[i].acsr.injg.QGas > 0.)
                fluF = celula[i].acsr.injg.FluidoPro;
            else
                fluF = celula[i].flui;
            fwF = 0.;
            titF = 1.;
        } else if (celula[i].acsr.tipo == 2) {
            celula[i].acsr.injl.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, celula[i].flui.dCalculatedBeta,
                                                           celula[i].flui.oCalculatedLiqComposition, celula[i].flui.oCalculatedVapComposition, arq.pocinjec);
            if (celula[i].acsr.injl.QLiq > 0.)
                fluF = celula[i].acsr.injl.FluidoPro;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 3) {
            celula[i].acsr.ipr.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, celula[i].flui.dCalculatedBeta,
                                                          celula[i].flui.oCalculatedLiqComposition, celula[i].flui.oCalculatedVapComposition, arq.pocinjec);
            if ((celula[i].acsr.ipr.Pres) > celula[i].pres)
                fluF = celula[i].acsr.ipr.FluidoPro;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 10) {
            celula[i].acsr.injm.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, celula[i].flui.dCalculatedBeta,
                                                           celula[i].flui.oCalculatedLiqComposition, celula[i].flui.oCalculatedVapComposition, arq.pocinjec);
            if ((celula[i].acsr.injm.MassC + celula[i].acsr.injm.MassG + celula[i].acsr.injm.MassP) > 0.)
                fluF = celula[i].acsr.injm.FluidoPro;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 9 && celula[i].acsr.fontechk.abertura > 1e-6) {
            celula[i].acsr.fontechk.fluidoP.atualizaPropComp(celula[i].pres, celula[i].temp, celula[i].flui.dCalculatedBeta,
                                                             celula[i].flui.oCalculatedLiqComposition, celula[i].flui.oCalculatedVapComposition, arq.pocinjec);
            celula[i].acsr.fontechk.fluidoPamb.atualizaPropComp(celula[i].pres, celula[i].temp, celula[i].flui.dCalculatedBeta,
                                                                celula[i].flui.oCalculatedLiqComposition, celula[i].flui.oCalculatedVapComposition, arq.pocinjec);
            if (celula[i].acsr.fontechk.presT > celula[i].acsr.fontechk.pamb) {
                fluF = celula[i].acsr.fontechk.fluidoP;
            } else {
                fluF = celula[i].acsr.fontechk.fluidoPamb;
            }
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 15) {
            double tRes = celula[i].acsr.radialPoro.tRes;
            celula[i].acsr.radialPoro.flup.atualizaPropComp(celula[i].pres, tRes, celula[i].flui.dCalculatedBeta,
                                                            celula[i].flui.oCalculatedLiqComposition, celula[i].flui.oCalculatedVapComposition, arq.pocinjec);
            if ((celula[i].fontemassLR + celula[i].fontemassGR) > 1e-15)
                fluF = celula[i].acsr.radialPoro.flup;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 16) {
            double tRes = celula[i].acsr.poroso2D.dados.tRes;
            celula[i].acsr.poroso2D.dados.flup.atualizaPropComp(celula[i].pres, tRes, celula[i].flui.dCalculatedBeta,
                                                                celula[i].flui.oCalculatedLiqComposition, celula[i].flui.oCalculatedVapComposition, arq.pocinjec);
            if ((celula[i].fontemassLR + celula[i].fontemassGR) > 1e-15)
                fluF = celula[i].acsr.poroso2D.dados.flup;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        }

        pesoMolF = 0;
        for (int j = 0; j < ncomp; j++)
            pesoMolF += fluF.masMol[j] * fluF.fracMol[j];
        fonteO *= titF;
        double tempMol;
        fluC[i] = celula[i].flui;
        for (int corrige = 0; corrige < 2; corrige++) {
            tempMol = (fluC[i].MasEspoleo(celula[i].pres, celula[i].temp) * (1 - celula[i].alf) +
                       fluC[i].MasEspGas(celula[i].pres, celula[i].temp) *
                           celula[i].alf) *
                      celula[i].duto.area / pesoMolC;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fluC[i].fracMol[kfrac] = (celula[i].nMolIni * celula[i].flui.fracMol[kfrac] +
                                          ((fonteO + fonteG) * fracMolF[kfrac] / pesoMolF -
                                           (vazMasLiq1 + vazMasGas1) * fracMol1[kfrac] / pesoMol1 -
                                           (vazMasLiq0 + vazMasGas0) * fracMol0[kfrac] / pesoMol0) *
                                              dt / dx) /
                                         tempMol;
            }
            pesoMolC = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++)
                pesoMolC += fluC[i].masMol[kfrac] * celula[i].flui.fracMol[kfrac];
            fluC[i].atualizaPropCompStandard();
            fluC[i].atualizaPropComp(celula[i].pres, celula[i].temp, fluC[i].dCalculatedBeta,
                                     fluC[i].oCalculatedLiqComposition,
                                     fluC[i].oCalculatedVapComposition, arq.pocinjec);
        }
        celula[i].nMol = (fluC[i].MasEspoleo(celula[i].pres, celula[i].temp) * (1 - celula[i].alf) +
                          fluC[i].MasEspGas(celula[i].pres, celula[i].temp) *
                              celula[i].alf) *
                         celula[i].duto.area / pesoMolC;
        fluC[i].Pmol = pesoMolC;
        double hol = 1. - celula[i].alf;
        double bet = celula[i].bet;

        double rs = celula[i].flui.RS(celula[i].pres, celula[i].temp);
        double bo = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs);
        double ba = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
        double bsw = celula[i].flui.BSW * ba / (bo + ba * celula[i].flui.BSW - celula[i].flui.BSW * bo);

        double BSWini = celula[i].flui.BSW;
        double BSWF = 0.;
        double viscLini = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
        double viscHini = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
        double viscLF = viscLini;
        double viscHF = viscHini;

        if (celula[i].acsr.tipo == 2) {
            double rsF = celula[i].acsr.injl.FluidoPro.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.injl.FluidoPro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw < (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.injl.FluidoPro.API)) + (bswaux / contrabsw) * 1000 * celula[i].acsr.injl.FluidoPro.Denag + celula[i].acsr.injl.FluidoPro.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.injl.FluidoPro.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.injl.FluidoPro.BSW * 1000 * celula[i].acsr.injl.FluidoPro.Denag));
                }
                BSWF = celula[i].acsr.injl.FluidoPro.BSW;
                viscLF = 0 * 30 + 1 * celula[i].acsr.injl.FluidoPro.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.injl.FluidoPro.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 10) {
            double rsF = celula[i].acsr.injm.FluidoPro.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.injm.FluidoPro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw < (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.injm.FluidoPro.API)) +
                              (bswaux / contrabsw) * 1000 *
                                  celula[i].acsr.injm.FluidoPro.Denag +
                              celula[i].acsr.injm.FluidoPro.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.injm.FluidoPro.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.injm.FluidoPro.BSW * 1000 * celula[i].acsr.injm.FluidoPro.Denag));
                }
                BSWF = celula[i].acsr.injm.FluidoPro.BSW;
                viscLF = 0 * 30 + 1 * celula[i].acsr.injm.FluidoPro.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.injm.FluidoPro.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 3) {
            double rsF = celula[i].acsr.ipr.FluidoPro.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.ipr.FluidoPro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.ipr.FluidoPro.API)) + (bswaux / contrabsw) * 1000 * celula[i].acsr.ipr.FluidoPro.Denag + celula[i].acsr.ipr.FluidoPro.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.ipr.FluidoPro.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.ipr.FluidoPro.BSW * 1000 * celula[i].acsr.ipr.FluidoPro.Denag));
                }
                BSWF = celula[i].acsr.ipr.FluidoPro.BSW;
                viscLF = 0 * 30 + 1 * celula[i].acsr.ipr.FluidoPro.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.ipr.FluidoPro.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 9) {

            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = fluF.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) + (bswaux / contrabsw) * 1000 * fluF.Denag + fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (fluF.BSW * 1000 * fluF.Denag));
                }
                BSWF = fluF.BSW;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 15) {
            double rsF = celula[i].acsr.radialPoro.flup.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.radialPoro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.radialPoro.flup.API)) + (bswaux / contrabsw) * 1000 * celula[i].acsr.radialPoro.flup.Denag + celula[i].acsr.radialPoro.flup.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.radialPoro.flup.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.radialPoro.BSW * 1000 * celula[i].acsr.radialPoro.flup.Denag));
                }
                BSWF = celula[i].acsr.radialPoro.BSW;
                viscLF = 0 * 30 + 1 * celula[i].acsr.radialPoro.flup.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.radialPoro.flup.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 16) {
            double rsF = celula[i].acsr.poroso2D.dados.flup.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.poroso2D.dados.transfer.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.poroso2D.dados.flup.API)) + (bswaux / contrabsw) * 1000 * celula[i].acsr.poroso2D.dados.flup.Denag + celula[i].acsr.poroso2D.dados.flup.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.poroso2D.dados.flup.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.poroso2D.dados.transfer.BSW * 1000 * celula[i].acsr.poroso2D.dados.flup.Denag));
                }
                BSWF = celula[i].acsr.poroso2D.dados.transfer.BSW;
                viscLF = 0 * 30 + 1 * celula[i].acsr.poroso2D.dados.flup.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.poroso2D.dados.flup.VisOM(tH);
            }
        } else if ((fabs(fonteO) > (*vg1dSP).localtiny && celula[i].acsr.tipo != 2 && celula[i].acsr.tipo != 3 &&
                    celula[i].acsr.tipo != 9 && celula[i].acsr.tipo != 15 && celula[i].acsr.tipo != 16) ||
                   (fabs(fonteG) > (*vg1dSP).localtiny && celula[i].acsr.tipo != 1 && celula[i].acsr.tipo != 2 && celula[i].acsr.tipo != 3 && celula[i].acsr.tipo != 9 && celula[i].acsr.tipo != 15 && celula[i].acsr.tipo != 16)) {
            if (celula[i].acsr.tipo == 5 || celula[i].acsr.tipo == 8) {
                double rsF = celula[i].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                    double rhoPSTF = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) + celula[i].flui.BSW * 1000 * celula[i].flui.Denag;
                    fonteP *= ((1 - celula[i].flui.BSW) / rhoPSTF);
                    fonteA *= (celula[i].flui.BSW / rhoPSTF);
                    BSWF = celula[i].flui.BSW;
                    viscLF = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
                    viscHF = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
                }
            } else if ((*celula[i].acsrL).tipo == 5 || (*celula[i].acsrL).tipo == 8) {
                if (i > 0) {
                    double rsF = celula[i - 1].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                    if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                        double rhoPSTF = (1 - celula[i - 1].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i - 1].flui.API)) +
                                         celula[i - 1].flui.BSW * 1000 *
                                             celula[i - 1].flui.Denag;
                        fonteP *= ((1 - celula[i - 1].flui.BSW) / rhoPSTF);
                        fonteA *= (celula[i - 1].flui.BSW / rhoPSTF);
                        BSWF = celula[i - 1].flui.BSW;
                        viscLF = 0 * 30 + 1 * celula[i - 1].flui.VisOM(tL);
                        viscHF = 0 * 20 + 1 * celula[i - 1].flui.VisOM(tH);
                    }
                } else {
                    double rsF = celula[i].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                    if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                        double rhoPSTF = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) +
                                         celula[i].flui.BSW * 1000 *
                                             celula[i].flui.Denag;
                        fonteP *= ((1 - celula[i].flui.BSW) / rhoPSTF);
                        fonteA *= (celula[i].flui.BSW / rhoPSTF);
                        BSWF = celula[i].flui.BSW;
                        viscLF = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
                        viscHF = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
                    }
                }
            }
        }
        if (titF > 1. - 1e-15) {
            fonteO = 0.;
            fonteP = 0.;
            fonteA = 0.;
        }

        double volpesFim = hol * (1 - bet) * (1 - bsw) / bo;
        double volaguaFim = hol * (1 - bet) * bsw; // nao deveria ser dividido por Bo???????????
        double MultPe;
        double MultPd = 0.;
        double residuoP;
        double MultAe = 0.;
        double MultAd;
        double residuoA;
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            MultPe = 0.;
            if (celula[i].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultPe = celula[i].QL * (1 - betI0) * (1 - bsw0) / bo0;
            MultPd = 0.;
            if (celula[i + 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultPd = celula[i + 1].QL * (1 - betI1) * (1 - bsw1) / bo1;
            residuoP = (volpesFim - celula[i].VolPesaST) * at / dt + (MultPd - MultPe) / dx - fonteP / dx;
            MultAe = 0.;
            if (celula[i].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultAe = celula[i].QL * (1 - betI0) * bsw0 / bo0;
            MultAd = 0.;
            if (celula[i + 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultAd = celula[i + 1].QL * (1 - betI1) * bsw1 / bo1;
            residuoA = (volaguaFim - celula[i].VolAguaST) * at / dt + (MultAd - MultAe) / dx - fonteA / dx;
        }
        if ((arq.nfluP > 1 && (arq.flashCompleto == 0 || celula[i].flui.viscBlackOil == 1)) || (*vg1dSP).chaverede != 0) {
            if (volpesFim > 1e-3 && (fonteP > 0 || (MultPd < 0 || MultPe > 0))) {
                VISCL[i] = (dt * (viscLF * fonteP / dx + viscLini * residuoP - (viscL1 * MultPd - viscL0 * MultPe) / dx) + viscLini * celula[i].VolPesaST * at) / (volpesFim * at);
                VISCH[i] = (dt * (viscHF * fonteP / dx + viscHini * residuoP - (viscH1 * MultPd - viscH0 * MultPe) / dx) + viscHini * celula[i].VolPesaST * at) / (volpesFim * at);
            } else {
                VISCL[i] = viscLini;
                VISCH[i] = viscHini;
            }
        }
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            if ((volaguaFim + volpesFim) > 1e-3 && ((fonteP > 0 || fonteA > 0) ||
                                                    ((MultPd < 0 || MultPe > 0) || (MultAd < 0 || MultAe > 0))))
                BSW[i] = (dt * (BSWF * (fonteA + fonteP) / dx + BSWini * (residuoA + residuoP) - (BSW1 * (MultAd + MultPd) - BSW0 * (MultAe + MultPe)) / dx) + BSWini * (celula[i].VolAguaST + celula[i].VolPesaST) * at) / ((volaguaFim + volpesFim) * at);
            else
                BSW[i] = BSWini;
        }
        celula[i].VolPesaST = volpesFim;
        celula[i].VolAguaST = volaguaFim;
    }
    for (int i = imin; i <= ncel - 1; i++) {
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            celula[i].flui = fluC[i];
            celula[i].flui.BSW = BSW[i];
            if (arq.flashCompleto == 0 || celula[i].flui.viscBlackOil == 1) {
                celula[i].flui.LVisL = VISCL[i];
                celula[i].flui.LVisH = VISCH[i];
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
            }
        }
        if (arq.flashCompleto == 0)
            celula[i].flui.RenovaFluido();
        corrDeng(i);
    }

    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || celula[ncel].Mliqini > (*vg1dSP).localtiny) {
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            celula[ncel].flui = celula[ncel - 1].flui;
            celula[ncel].flui.BSW = BSW[ncel - 1];
            if (arq.flashCompleto == 0) {
                celula[ncel].flui.LVisL = VISCL[ncel - 1];
                celula[ncel].flui.LVisH = VISCH[ncel - 1];
                celula[ncel].flui.TempL = tL;
                celula[ncel].flui.TempH = tH;
            }
        }
    } else {
        celula[ncel].flui = fluiRev;
        celula[ncel].flui.BSW = fluiRev.BSW;
        celula[ncel].flui.LVisL = fluiRev.LVisL;
        celula[ncel].flui.LVisH = fluiRev.LVisH;
        celula[ncel].flui.TempL = fluiRev.TempL;
        celula[ncel].flui.TempH = fluiRev.TempH;
    }
    delete[] fluC;
}

void SProd::renovaFracMol2(ProFlu fluiRev) {
    Vcr<double> BSW(ncel);
    Vcr<double> denag(ncel);
    Vcr<double> VISCL(ncel);
    Vcr<double> VISCH(ncel);
    int ncomp = arq.npseudo;
    ProFlu *fluC;
    fluC = new ProFlu[ncel];
    Vcr<double> fracMol0(ncomp);
    Vcr<double> fracMol1(ncomp);
    Vcr<double> fracMol0O(ncomp);
    Vcr<double> fracMol1O(ncomp);
    Vcr<double> fracMol0G(ncomp);
    Vcr<double> fracMol1G(ncomp);
    Vcr<double> fracMolF(ncomp);
    if (celula[0].acsr.tipo == 15 && (celula[0].fontemassCR + celula[0].fontemassGR + celula[0].fontemassLR) > 0.)
        celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
    else if (celula[0].acsr.tipo == 16 && (celula[0].fontemassCR + celula[0].fontemassGR + celula[0].fontemassLR) > 0.)
        celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
    double dt = celula[1].dt;
    double tL = 0.;
    double tH = 70.;
    if (kontaRenovaComp == arq.miniTabAtraso)
        (*vg1dSP).modoTransiente = 0;
    int imin = 1;
    if ((*vg1dSP).chaverede != 0 && (celula[0].acsr.tipo == 10 || arq.ConContEntrada > 0))
        imin = 0;
    for (int i = imin; i < ncel; i++) {

        double pesoMol0;
        double pesoMol1;
        double pesoMol0O;
        double pesoMol1O;
        double pesoMol0G;
        double pesoMol1G;
        double pesoMolF;
        double pesoMolC;
        double at = celula[i].duto.area;
        double dx = celula[i].dx;
        double ti0;
        if (celula[i].VTemper < 0.)
            ti0 = celula[i].temp;
        else {
            if (i > 0)
                ti0 = celula[i - 1].temp;
            else if (arq.ConContEntrada == 1)
                ti0 = tempE;
            else
                ti0 = celula[i].temp;
        }
        double ti1 = celula[i].temp;
        if (celula[i + 1].VTemper < 0.)
            ti1 = celula[i + 1].temp;

        double titF = 0.;
        ProFlu fluF;

        double boF = 1.;
        double baF = 1.;
        double fwF = 1.;
        double rhoOF = 900.;
        double rhoWF = 1000.;

        double betI0;
        double bo0;
        double ba0;
        double bsw0;
        double BSW0;
        double denag0;
        double viscL0;
        double viscH0;
        if (i > 0 || arq.ConContEntrada == 0) {
            if (i > 0 && celula[i].QG >= 0.)
                betI0 = celula[i - 1].betPigD;
            else
                betI0 = celula[i].betPigE;
        } else {
            if (celula[i].QG >= 0.)
                betI0 = betaE;
            else
                betI0 = celula[i].betPigE;
        }
        if ((i > 0 || arq.ConContEntrada == 1) && celula[i].QL >= 0.) {
            double pe;
            double te;
            if (i > 0) {
                pe = celula[i - 1].pres;
                te = celula[i - 1].temp;
            } else {
                pe = presE;
                te = tempE;
            }
            if (arq.ConContEntrada == 0)
                betI0 = celula[i - 1].betPigD; // testeBeta
            else
                betI0 = betaE; // testeBeta
            double rs0 = (*celula[i].fluiL).RS(pe, te);
            bo0 = (*celula[i].fluiL).BOFunc(pe, te, rs0);
            ba0 = (*celula[i].fluiL).BAFunc(pe, te);
            bsw0 = (*celula[i].fluiL).BSW * ba0 / (bo0 + ba0 * (*celula[i].fluiL).BSW - (*celula[i].fluiL).BSW * bo0);
            BSW0 = (*celula[i].fluiL).BSW;
            denag0 = (*celula[i].fluiL).Denag;
            viscL0 = 0 * 30 + 1 * (*celula[i].fluiL).VisOM(tL);
            viscH0 = 0 * 20 + 1 * (*celula[i].fluiL).VisOM(tH);
        } else {
            betI0 = celula[i].betPigE; // testebeta
            double rs0 = celula[i].flui.RS(celula[i].pres, celula[i].temp);
            bo0 = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs0);
            ba0 = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
            bsw0 = celula[i].flui.BSW * ba0 / (bo0 + ba0 * celula[i].flui.BSW - celula[i].flui.BSW * bo0);
            BSW0 = celula[i].flui.BSW;
            denag0 = celula[i].flui.Denag;
            viscL0 = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
            viscH0 = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
        }
        if (bo0 < 1e-15)
            bo0 = 1e-15;

        double betI1 = celula[i].betPigD;
        double rs1 = celula[i].flui.RS(celula[i].pres, celula[i].temp);
        double bo1 = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs1);
        double ba1 = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
        double bsw1 = celula[i].flui.BSW * ba1 / (bo1 + ba1 * celula[i].flui.BSW - celula[i].flui.BSW * bo1);
        double BSW1 = celula[i].flui.BSW;
        double denag1 = celula[i].flui.Denag;
        double viscL1 = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
        double viscH1 = 0 * 20 + 1 * celula[i].flui.VisOM(tH);

        // betI1 = celula[i + 1].betPigE;    //duvidabeta
        if (celula[i + 1].QL < 0.) {
            betI1 = celula[i + 1].betPigE; // testeBet
            rs1 = celula[i + 1].flui.RS(celula[i + 1].pres, celula[i + 1].temp);
            bo1 = celula[i + 1].flui.BOFunc(celula[i + 1].pres, celula[i + 1].temp, rs1);
            ba1 = celula[i + 1].flui.BAFunc(celula[i + 1].pres, celula[i + 1].temp);
            bsw1 = celula[i + 1].flui.BSW * ba1 / (bo1 + ba1 * celula[i + 1].flui.BSW - celula[i + 1].flui.BSW * bo1);
            BSW1 = celula[i + 1].flui.BSW;
            denag1 = celula[i + 1].flui.Denag;
            viscL1 = 0 * 30 + 1 * celula[i + 1].flui.VisOM(tL);
            viscH1 = 0 * 20 + 1 * celula[i + 1].flui.VisOM(tH);
        }
        if (bo1 < 1e-15)
            bo1 = 1e-15;
        double titV0;
        double titV1;
        double vazMasLiq0 = celula[i].Mliqini;
        double vazMasLiq1 = celula[i + 1].Mliqini;
        double vazMasGas0 = celula[i].MC - celula[i].Mliqini;
        double vazMasGas1 = celula[i + 1].MC - celula[i + 1].Mliqini;
        pesoMolC = 0.;
        for (int kfrac = 0; kfrac < ncomp; kfrac++) {
            pesoMolC += celula[i].flui.masMol[kfrac] * celula[i].flui.fracMol[kfrac];
        }

        if ((i > 0 || arq.ConContEntrada == 1) && celula[i].Mliqini >= 0.) {
            double pe;
            double te;
            if (i > 0) {
                pe = celula[i - 1].pres;
                te = celula[i - 1].temp;
            } else {
                pe = presE;
                te = tempE;
            }
            double fwV = bsw0;
            double rhoOV = (*celula[i].fluiL).MasEspoleo(pe, te);
            double rhoWV = (*celula[i].fluiL).MasEspAgua(pe, te);
            titV0 = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
            vazMasLiq0 -= betI0 * celula[i].QL * celula[i].fluicol.MasEspFlu(pe, te);
            vazMasLiq0 *= titV0;
            pesoMol0O = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol0O[kfrac] = (*celula[i].fluiL).oCalculatedLiqComposition[kfrac];
                pesoMol0O += (*celula[i].fluiL).masMol[kfrac] * (*celula[i].fluiL).oCalculatedLiqComposition[kfrac];
            }
        } else {
            double fwV = bsw0;
            double rhoOV = celula[i].flui.MasEspoleo(celula[i].pres, celula[i].temp);
            double rhoWV = celula[i].flui.MasEspAgua(celula[i].pres, celula[i].temp);
            titV0 = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
            vazMasLiq0 -= betI0 * celula[i].QL * celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
            vazMasLiq0 *= titV0;
            pesoMol0O = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol0O[kfrac] = celula[i].flui.oCalculatedLiqComposition[kfrac];
                pesoMol0O += celula[i].flui.masMol[kfrac] * celula[i].flui.oCalculatedLiqComposition[kfrac];
            }
        }
        if (celula[i + 1].Mliqini >= 0.) {
            double fwV = bsw1;
            double rhoOV = celula[i].flui.MasEspoleo(celula[i].pres, celula[i].temp);
            double rhoWV = celula[i].flui.MasEspAgua(celula[i].pres, celula[i].temp);
            titV1 = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
            vazMasLiq1 -= betI1 * celula[i].QL * celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
            vazMasLiq1 *= titV1;
            pesoMol1O = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol1O[kfrac] = celula[i].flui.oCalculatedLiqComposition[kfrac];
                pesoMol1O += celula[i].flui.masMol[kfrac] * celula[i].flui.oCalculatedLiqComposition[kfrac];
            }
        } else {
            double fwV = bsw1;
            double rhoOV = celula[i + 1].flui.MasEspoleo(celula[i + 1].pres, celula[i + 1].temp);
            double rhoWV = celula[i + 1].flui.MasEspAgua(celula[i + 1].pres, celula[i + 1].temp);
            titV1 = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
            vazMasLiq1 -= betI1 * celula[i + 1].QL * celula[i].fluicol.MasEspFlu(celula[i + 1].pres, celula[i + 1].temp);
            vazMasLiq1 *= titV1;
            pesoMol1O = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol1O[kfrac] = celula[i + 1].flui.oCalculatedLiqComposition[kfrac];
                pesoMol1O += celula[i + 1].flui.masMol[kfrac] * celula[i + 1].flui.oCalculatedLiqComposition[kfrac];
            }
        }

        if ((i > 0 || arq.ConContEntrada == 1) && (celula[i].MC - celula[i].Mliqini) >= 0.) {
            pesoMol0G = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol0G[kfrac] = (*celula[i].fluiL).oCalculatedVapComposition[kfrac];
                pesoMol0G += (*celula[i].fluiL).masMol[kfrac] * (*celula[i].fluiL).oCalculatedVapComposition[kfrac];
            }
        } else {
            pesoMol0G = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol0G[kfrac] = celula[i].flui.oCalculatedVapComposition[kfrac];
                pesoMol0G += celula[i].flui.masMol[kfrac] * celula[i].flui.oCalculatedVapComposition[kfrac];
            }
        }
        if ((celula[i + 1].MC - celula[i + 1].Mliqini) >= 0.) {
            pesoMol1G = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol1G[kfrac] = celula[i].flui.oCalculatedVapComposition[kfrac];
                pesoMol1G += celula[i].flui.masMol[kfrac] * celula[i].flui.oCalculatedVapComposition[kfrac];
            }
        } else {
            pesoMol1G = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol1G[kfrac] = celula[i + 1].flui.oCalculatedVapComposition[kfrac];
                pesoMol1G += celula[i + 1].flui.masMol[kfrac] * celula[i + 1].flui.oCalculatedVapComposition[kfrac];
            }
        }

        if ((i > 0 || arq.ConContEntrada == 1) && (vazMasLiq0 + vazMasGas0) >= 0) {
            pesoMol0 = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol0[kfrac] = (*celula[i].fluiL).fracMol[kfrac];
                pesoMol0 += (*celula[i].fluiL).masMol[kfrac] * (*celula[i].fluiL).fracMol[kfrac];
            }
        } else {
            pesoMol0 = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol0[kfrac] = celula[i].flui.fracMol[kfrac];
                pesoMol0 += celula[i].flui.masMol[kfrac] * celula[i].flui.fracMol[kfrac];
            }
        }
        if ((vazMasLiq1 + vazMasGas1) >= 0) {
            pesoMol1 = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol1[kfrac] = celula[i].flui.fracMol[kfrac];
                pesoMol1 += celula[i].flui.masMol[kfrac] * celula[i].flui.fracMol[kfrac];
            }
        } else {
            pesoMol1 = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fracMol1[kfrac] = celula[i + 1].flui.fracMol[kfrac];
                pesoMol1 += celula[i + 1].flui.masMol[kfrac] * celula[i + 1].flui.fracMol[kfrac];
            }
        }

        double fonteO = celula[i].fontemassLR;
        double fonteG = celula[i].fontemassGR;
        double fonteP = celula[i].fontemassLR;
        double fonteA = celula[i].fontemassLR;

        if (celula[i].acsr.tipo == 1) {
            if (celula[i].acsr.injg.FluidoPro.dCalculatedBeta < 0. || celula[i].acsr.injg.FluidoPro.dCalculatedBeta > 1.)
                celula[i].acsr.injg.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].acsr.injg.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                               celula[i].acsr.injg.FluidoPro.dCalculatedBeta, celula[i].acsr.injg.FluidoPro.oCalculatedLiqComposition,
                                                               celula[i].acsr.injg.FluidoPro.oCalculatedVapComposition, arq.pocinjec);
            if (celula[i].acsr.injg.QGas > 0.)
                fluF = celula[i].acsr.injg.FluidoPro;
            else
                fluF = celula[i].flui;
            fwF = 0.;
            titF = 1.;
        } else if (celula[i].acsr.tipo == 2) {
            if (celula[i].acsr.injl.FluidoPro.dCalculatedBeta < 0. || celula[i].acsr.injl.FluidoPro.dCalculatedBeta > 1.)
                celula[i].acsr.injl.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].acsr.injl.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                               celula[i].acsr.injl.FluidoPro.dCalculatedBeta, celula[i].acsr.injl.FluidoPro.oCalculatedLiqComposition,
                                                               celula[i].acsr.injl.FluidoPro.oCalculatedVapComposition, arq.pocinjec);
            if (celula[i].acsr.injl.QLiq > 0.)
                fluF = celula[i].acsr.injl.FluidoPro;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 3) {
            if (celula[i].acsr.ipr.FluidoPro.dCalculatedBeta < 0. || celula[i].acsr.ipr.FluidoPro.dCalculatedBeta > 1.)
                celula[i].acsr.ipr.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].acsr.ipr.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                              celula[i].acsr.ipr.FluidoPro.dCalculatedBeta, celula[i].acsr.ipr.FluidoPro.oCalculatedLiqComposition,
                                                              celula[i].acsr.ipr.FluidoPro.oCalculatedVapComposition, arq.pocinjec);
            if ((celula[i].acsr.ipr.Pres) > celula[i].pres)
                fluF = celula[i].acsr.ipr.FluidoPro;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 10) {
            if (celula[i].acsr.injm.FluidoPro.dCalculatedBeta < 0. || celula[i].acsr.injm.FluidoPro.dCalculatedBeta > 1.)
                celula[i].acsr.injm.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].acsr.injm.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                               celula[i].acsr.injm.FluidoPro.dCalculatedBeta, celula[i].acsr.injm.FluidoPro.oCalculatedLiqComposition,
                                                               celula[i].acsr.injm.FluidoPro.oCalculatedVapComposition, arq.pocinjec);
            if ((celula[i].acsr.injm.MassC + celula[i].acsr.injm.MassG + celula[i].acsr.injm.MassP) > 0.)
                fluF = celula[i].acsr.injm.FluidoPro;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 9 && celula[i].acsr.fontechk.abertura > 1e-6) {
            if (celula[i].acsr.fontechk.fluidoP.dCalculatedBeta < 0. || celula[i].acsr.fontechk.fluidoP.dCalculatedBeta > 1.)
                celula[i].acsr.fontechk.fluidoP.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].acsr.fontechk.fluidoP.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                                 celula[i].acsr.fontechk.fluidoP.dCalculatedBeta, celula[i].acsr.fontechk.fluidoP.oCalculatedLiqComposition,
                                                                 celula[i].acsr.fontechk.fluidoP.oCalculatedVapComposition, arq.pocinjec);
            if (celula[i].acsr.fontechk.fluidoPamb.dCalculatedBeta < 0. || celula[i].acsr.fontechk.fluidoPamb.dCalculatedBeta > 1.)
                celula[i].acsr.fontechk.fluidoPamb.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].acsr.fontechk.fluidoPamb.atualizaPropComp(celula[i].pres, celula[i].temp,
                                                                    celula[i].acsr.fontechk.fluidoPamb.dCalculatedBeta, celula[i].acsr.fontechk.fluidoPamb.oCalculatedLiqComposition,
                                                                    celula[i].acsr.fontechk.fluidoPamb.oCalculatedVapComposition, arq.pocinjec);
            if (celula[i].acsr.fontechk.presT > celula[i].acsr.fontechk.pamb) {
                fluF = celula[i].acsr.fontechk.fluidoP;
            } else {
                fluF = celula[i].acsr.fontechk.fluidoPamb;
            }
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 15) {
            double tRes = celula[i].acsr.radialPoro.tRes;
            if (celula[i].acsr.radialPoro.flup.dCalculatedBeta < 0. || celula[i].acsr.radialPoro.flup.dCalculatedBeta > 1.)
                celula[i].acsr.radialPoro.flup.atualizaPropComp(celula[i].pres, tRes, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].acsr.radialPoro.flup.atualizaPropComp(celula[i].pres, tRes,
                                                                celula[i].acsr.radialPoro.flup.dCalculatedBeta, celula[i].acsr.radialPoro.flup.oCalculatedLiqComposition,
                                                                celula[i].acsr.radialPoro.flup.oCalculatedVapComposition, arq.pocinjec);
            if ((celula[i].fontemassLR + celula[i].fontemassGR) > 1e-15)
                fluF = celula[i].acsr.radialPoro.flup;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else if (celula[i].acsr.tipo == 16) {
            double tRes = celula[i].acsr.poroso2D.dados.tRes;
            if (celula[i].acsr.poroso2D.dados.flup.dCalculatedBeta < 0. || celula[i].acsr.poroso2D.dados.flup.dCalculatedBeta > 1.)
                celula[i].acsr.poroso2D.dados.flup.atualizaPropComp(celula[i].pres, tRes, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].acsr.poroso2D.dados.flup.atualizaPropComp(celula[i].pres, tRes,
                                                                    celula[i].acsr.poroso2D.dados.flup.dCalculatedBeta, celula[i].acsr.poroso2D.dados.flup.oCalculatedLiqComposition,
                                                                    celula[i].acsr.poroso2D.dados.flup.oCalculatedVapComposition, arq.pocinjec);
            if ((celula[i].fontemassLR + celula[i].fontemassGR) > 1e-15)
                fluF = celula[i].acsr.poroso2D.dados.flup;
            else
                fluF = celula[i].flui;
            boF = fluF.BOFunc(celula[i].pres, celula[i].temp);
            baF = fluF.BAFunc(celula[i].pres, celula[i].temp);
            fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
            rhoOF = fluF.MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWF = fluF.MasEspAgua(celula[i].pres, celula[i].temp);
            titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        } else {
            fluF = celula[i].flui;
            titF = 0.;
        }

        pesoMolF = 0;
        for (int j = 0; j < ncomp; j++) {
            pesoMolF += fluF.masMol[j] * fluF.fracMol[j];
        }
        fonteO *= titF;
        double tempMol;
        fluC[i] = celula[i].flui;
        double betIV;
        double rsV;
        double boV;
        double baV;
        double bswV;
        double rhoOVol;
        double rhoWVol;
        double titVol;
        int limCorrige = 1;
        if (kontaRenovaComp == arq.miniTabAtraso)
            limCorrige = 2;
        for (int corrige = 0; corrige < limCorrige; corrige++) {

            betIV = celula[i].bet;
            rsV = fluC[i].RS(celula[i].pres, celula[i].temp);
            boV = fluC[i].BOFunc(celula[i].pres, celula[i].temp, rsV);
            baV = fluC[i].BAFunc(celula[i].pres, celula[i].temp);
            bswV = fluC[i].BSW * baV / (boV + baV * celula[i].flui.BSW - celula[i].flui.BSW * boV);
            rhoOVol = fluC[i].MasEspoleo(celula[i].pres, celula[i].temp);
            rhoWVol = fluC[i].MasEspAgua(celula[i].pres, celula[i].temp);
            titVol = (1 - bswV) * rhoOVol / ((1 - bswV) * rhoOVol + bswV * rhoWVol);

            tempMol = (fluC[i].MasEspLiq(celula[i].pres, celula[i].temp) * (1. - celula[i].alf) * (1. - betIV) * titVol +
                       fluC[i].MasEspGas(celula[i].pres, celula[i].temp) * celula[i].alf) *
                      celula[i].duto.area * celula[i].dx / pesoMolC;

            double menorFrac = 0.;
            for (int kfrac = 0; kfrac < ncomp; kfrac++) {
                fluC[i].fracMol[kfrac] = (celula[i].nMolIni * celula[i].flui.fracMol[kfrac] +
                                          ((fonteO + fonteG) * fluF.fracMol[kfrac] / pesoMolF -
                                           (vazMasLiq1 + vazMasGas1) * fracMol1[kfrac] / pesoMol1 +
                                           (vazMasLiq0 + vazMasGas0) * fracMol0[kfrac] / pesoMol0) *
                                              dt) /
                                         tempMol;
                if (menorFrac > fluC[i].fracMol[kfrac]) {
                    menorFrac = fluC[i].fracMol[kfrac];
                }
            }
            double fracTot = 0.;
            for (int kfrac = 0; kfrac < ncomp; kfrac++)
                fluC[i].fracMol[kfrac] -= menorFrac;
            for (int kfrac = 0; kfrac < ncomp; kfrac++)
                fracTot += fluC[i].fracMol[kfrac];
            for (int kfrac = 0; kfrac < ncomp; kfrac++)
                fluC[i].fracMol[kfrac] /= fracTot;
            pesoMolC = 0;
            for (int kfrac = 0; kfrac < ncomp; kfrac++)
                pesoMolC += fluC[i].masMol[kfrac] * celula[i].flui.fracMol[kfrac];
            if (kontaRenovaComp == arq.miniTabAtraso) {
                fluC[i].atualizaPropCompStandard();
                if (fluC[i].dCalculatedBeta < 0. || fluC[i].dCalculatedBeta > 1.)
                    fluC[i].atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
                else
                    fluC[i].atualizaPropComp(celula[i].pres, celula[i].temp, fluC[i].dCalculatedBeta,
                                             fluC[i].oCalculatedLiqComposition,
                                             fluC[i].oCalculatedVapComposition, arq.pocinjec);
                if (fluC[i].iIER != 0) {
                    int para;
                    para = 0;
                }
            }
        }
        betIV = celula[i].bet;
        rsV = celula[i].flui.RS(celula[i].pres, celula[i].temp);
        boV = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rsV);
        baV = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
        bswV = celula[i].flui.BSW * baV / (boV + baV * celula[i].flui.BSW - celula[i].flui.BSW * boV);
        rhoOVol = celula[i].flui.MasEspoleo(celula[i].pres, celula[i].temp);
        rhoWVol = celula[i].flui.MasEspAgua(celula[i].pres, celula[i].temp);
        titVol = (1 - bswV) * rhoOVol / ((1 - bswV) * rhoOVol + bswV * rhoWVol);
        celula[i].nMol = (fluC[i].MasEspLiq(celula[i].pres, celula[i].temp) * (1. - celula[i].alf) * (1. - betIV) * titVol +
                          fluC[i].MasEspGas(celula[i].pres, celula[i].temp) *
                              celula[i].alf) *
                         celula[i].duto.area * celula[i].dx / pesoMolC;
        fluC[i].Pmol = pesoMolC;
        double hol = 1. - celula[i].alf;
        double bet = celula[i].bet;

        double rs = celula[i].flui.RS(celula[i].pres, celula[i].temp);
        double bo = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rs);
        double ba = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
        double bsw = celula[i].flui.BSW * ba / (bo + ba * celula[i].flui.BSW - celula[i].flui.BSW * bo);

        double BSWini = celula[i].flui.BSW;
        double BSWF = celula[i].flui.BSW;
        double denagini = celula[i].flui.Denag;
        double denagF = celula[i].flui.Denag;
        double viscLini = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
        double viscHini = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
        double viscLF = viscLini;
        double viscHF = viscHini;

        if (celula[i].acsr.tipo == 2) {
            double rsF = celula[i].acsr.injl.FluidoPro.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.injl.FluidoPro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw < (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.injl.FluidoPro.API)) + (bswaux / contrabsw) * 1000 * celula[i].acsr.injl.FluidoPro.Denag + celula[i].acsr.injl.FluidoPro.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.injl.FluidoPro.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.injl.FluidoPro.BSW * 1000 * celula[i].acsr.injl.FluidoPro.Denag));
                }
                BSWF = celula[i].acsr.injl.FluidoPro.BSW;
                denagF = celula[i].acsr.injl.FluidoPro.Denag;
                viscLF = 0 * 30 + 1 * celula[i].acsr.injl.FluidoPro.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.injl.FluidoPro.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 10) {
            double rsF = celula[i].acsr.injm.FluidoPro.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.injm.FluidoPro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw < (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.injm.FluidoPro.API)) +
                              (bswaux / contrabsw) * 1000 *
                                  celula[i].acsr.injm.FluidoPro.Denag +
                              celula[i].acsr.injm.FluidoPro.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.injm.FluidoPro.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.injm.FluidoPro.BSW * 1000 * celula[i].acsr.injm.FluidoPro.Denag));
                }
                BSWF = celula[i].acsr.injm.FluidoPro.BSW;
                denagF = celula[i].acsr.injl.FluidoPro.Denag;
                viscLF = 0 * 30 + 1 * celula[i].acsr.injm.FluidoPro.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.injm.FluidoPro.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 3) {
            double rsF = celula[i].acsr.ipr.FluidoPro.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.ipr.FluidoPro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.ipr.FluidoPro.API)) + (bswaux / contrabsw) * 1000 * celula[i].acsr.ipr.FluidoPro.Denag + celula[i].acsr.ipr.FluidoPro.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.ipr.FluidoPro.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.ipr.FluidoPro.BSW * 1000 * celula[i].acsr.ipr.FluidoPro.Denag));
                }
                BSWF = celula[i].acsr.ipr.FluidoPro.BSW;
                denagF = celula[i].acsr.ipr.FluidoPro.Denag;
                viscLF = 0 * 30 + 1 * celula[i].acsr.ipr.FluidoPro.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.ipr.FluidoPro.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 9) {

            double rsF = fluF.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                double bswaux = fluF.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > (*vg1dSP).localtiny)
                    rhoPSTF = (1000 * 141.5 / (131.5 + fluF.API)) + (bswaux / contrabsw) * 1000 * fluF.Denag + fluF.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * fluF.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (fluF.BSW * 1000 * fluF.Denag));
                }
                BSWF = fluF.BSW;
                denagF = fluF.Denag;
                viscLF = 0 * 30 + 1 * fluF.VisOM(tL);
                viscHF = 0 * 20 + 1 * fluF.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 15) {
            double rsF = celula[i].acsr.radialPoro.flup.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.radialPoro.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.radialPoro.flup.API)) + (bswaux / contrabsw) * 1000 * celula[i].acsr.radialPoro.flup.Denag + celula[i].acsr.radialPoro.flup.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.radialPoro.flup.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.radialPoro.BSW * 1000 * celula[i].acsr.radialPoro.flup.Denag));
                }
                BSWF = celula[i].acsr.radialPoro.BSW;
                denagF = celula[i].acsr.radialPoro.flup.Denag;
                viscLF = 0 * 30 + 1 * celula[i].acsr.radialPoro.flup.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.radialPoro.flup.VisOM(tH);
            }
        } else if (celula[i].acsr.tipo == 16) {
            double rsF = celula[i].acsr.poroso2D.dados.flup.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
            if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                double bswaux = celula[i].acsr.poroso2D.dados.transfer.BSW;
                double contrabsw = 1. - bswaux;
                if (contrabsw <= (*vg1dSP).localtiny)
                    contrabsw = 0.9 * (*vg1dSP).localtiny;
                double rhoPSTF;
                if (contrabsw > 0)
                    rhoPSTF = (1000 * 141.5 / (131.5 + celula[i].acsr.poroso2D.dados.flup.API)) + (bswaux / contrabsw) * 1000 * celula[i].acsr.poroso2D.dados.flup.Denag + celula[i].acsr.poroso2D.dados.flup.Deng * 1.225 * rsF;
                else
                    rhoPSTF = 1000 * celula[i].acsr.poroso2D.dados.flup.Denag;
                if (contrabsw > (*vg1dSP).localtiny) {
                    fonteP *= (1. / rhoPSTF);
                    fonteA *= ((bswaux / contrabsw) / rhoPSTF);
                } else {
                    fonteP = 0.;
                    fonteA *= (1 / (celula[i].acsr.poroso2D.dados.transfer.BSW * 1000 * celula[i].acsr.poroso2D.dados.flup.Denag));
                }
                BSWF = celula[i].acsr.poroso2D.dados.transfer.BSW;
                denagF = celula[i].acsr.poroso2D.dados.flup.Denag;
                viscLF = 0 * 30 + 1 * celula[i].acsr.poroso2D.dados.flup.VisOM(tL);
                viscHF = 0 * 20 + 1 * celula[i].acsr.poroso2D.dados.flup.VisOM(tH);
            }
        } else if ((fabs(fonteO) > (*vg1dSP).localtiny && celula[i].acsr.tipo != 2 && celula[i].acsr.tipo != 3 &&
                    celula[i].acsr.tipo != 9 && celula[i].acsr.tipo != 15 && celula[i].acsr.tipo != 16) ||
                   (fabs(fonteG) > (*vg1dSP).localtiny && celula[i].acsr.tipo != 1 && celula[i].acsr.tipo != 2 && celula[i].acsr.tipo != 3 && celula[i].acsr.tipo != 9 && celula[i].acsr.tipo != 15 && celula[i].acsr.tipo != 16)) {
            if (celula[i].acsr.tipo == 5 || celula[i].acsr.tipo == 8) {
                double rsF = celula[i].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                    double rhoPSTF = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) + celula[i].flui.BSW * 1000 * celula[i].flui.Denag;
                    fonteP *= ((1 - celula[i].flui.BSW) / rhoPSTF);
                    fonteA *= (celula[i].flui.BSW / rhoPSTF);
                    BSWF = celula[i].flui.BSW;
                    denagF = fluF.Denag;
                    viscLF = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
                    viscHF = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
                }
            } else if ((*celula[i].acsrL).tipo == 5 || (*celula[i].acsrL).tipo == 8) {
                if (i > 0) {
                    double rsF = celula[i - 1].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                    if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                        double rhoPSTF = (1 - celula[i - 1].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i - 1].flui.API)) +
                                         celula[i - 1].flui.BSW * 1000 *
                                             celula[i - 1].flui.Denag;
                        fonteP *= ((1 - celula[i - 1].flui.BSW) / rhoPSTF);
                        fonteA *= (celula[i - 1].flui.BSW / rhoPSTF);
                        BSWF = celula[i - 1].flui.BSW;
                        denagF = celula[i - 1].flui.Denag;
                        viscLF = 0 * 30 + 1 * celula[i - 1].flui.VisOM(tL);
                        viscHF = 0 * 20 + 1 * celula[i - 1].flui.VisOM(tH);
                    }
                } else {
                    double rsF = celula[i].flui.RS(celula[i].pres, celula[i].temp) * (6.29 / 35.31467);
                    if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
                        double rhoPSTF = (1 - celula[i].flui.BSW) * (1000 * 141.5 / (131.5 + celula[i].flui.API)) +
                                         celula[i].flui.BSW * 1000 *
                                             celula[i].flui.Denag;
                        fonteP *= ((1 - celula[i].flui.BSW) / rhoPSTF);
                        fonteA *= (celula[i].flui.BSW / rhoPSTF);
                        BSWF = celula[i].flui.BSW;
                        denagF = celula[i - 1].flui.Denag;
                        viscLF = 0 * 30 + 1 * celula[i].flui.VisOM(tL);
                        viscHF = 0 * 20 + 1 * celula[i].flui.VisOM(tH);
                    }
                }
            }
        }
        if (titF > 1. - 1e-15) {
            fonteO = 0.;
            fonteP = 0.;
            fonteA = 0.;
        }

        double volpesFim = hol * (1 - bet) * (1 - bsw) / bo;
        double volaguaFim = hol * (1 - bet) * bsw / bo; // nao deveria ser dividido por Bo???????????
        double MultPe;
        double MultPd = 0.;
        double residuoP = 0.;
        double MultAe = 0.;
        double MultAd;
        double residuoA = 0.;
        if ((*vg1dSP).lixo5 < 1e-15 && arq.snaps != 1) {
            celula[i].VolPesaST = volpesFim;
            celula[i].VolAguaST = volaguaFim;
        }
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            MultPe = 0.;
            if (celula[i].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultPe = celula[i].QL * (1 - betI0) * (1 - bsw0) / bo0;
            MultPd = 0.;
            if (celula[i + 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultPd = celula[i + 1].QL * (1 - betI1) * (1 - bsw1) / bo1;
            residuoP = (volpesFim - celula[i].VolPesaST) * at / dt + (MultPd - MultPe) / dx - fonteP / dx;
            MultAe = 0.;
            if (celula[i].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultAe = celula[i].QL * (1 - betI0) * bsw0 / bo0;
            MultAd = 0.;
            if (celula[i + 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
                MultAd = celula[i + 1].QL * (1 - betI1) * bsw1 / bo1;
            residuoA = (volaguaFim - celula[i].VolAguaST) * at / dt + (MultAd - MultAe) / dx - fonteA / dx;
        }
        if ((arq.nfluP > 1) || (*vg1dSP).chaverede != 0) {
            if (((volpesFim > 1e-3) && (fonteP > 0 || (MultPd < 0 || MultPe > 0))) && arq.nfluP > 0) {
                VISCL[i] = (dt * (viscLF * fonteP / dx + 1. * viscLini * residuoP - (viscL1 * MultPd - viscL0 * MultPe) / dx) + viscLini * celula[i].VolPesaST * at) / (volpesFim * at - 0. * residuoP * dt);
                VISCH[i] = (dt * (viscHF * fonteP / dx + 1. * viscHini * residuoP - (viscH1 * MultPd - viscH0 * MultPe) / dx) + viscHini * celula[i].VolPesaST * at) / (volpesFim * at - 0. * residuoP * dt);
            } else {
                VISCL[i] = viscLini;
                VISCH[i] = viscHini;
            }
        }
        if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
            if ((
                    (volaguaFim + volpesFim) > 1e-3) &&
                ((fonteP > 0 || fonteA > 0) ||
                 ((MultPd < 0 || MultPe > 0) || (MultAd < 0 || MultAe > 0)))) {
                BSW[i] = (dt * (BSWF * (fonteA + fonteP) / dx + 1. * BSWini * (residuoA + residuoP) - (BSW1 * (MultAd + MultPd) - BSW0 * (MultAe + MultPe)) / dx) + BSWini * (celula[i].VolAguaST + celula[i].VolPesaST) * at) / ((volaguaFim + volpesFim) * at -
                                                                                                                                                                                                                                  0. * dt * (residuoA + residuoP));
                denag[i] = (dt * (denagF * (fonteA) / dx + 1. * denagini * (residuoA) - (denag1 * (MultAd)-denag0 * (MultAe)) / dx) + denagini * (celula[i].VolAguaST) * at) / ((volaguaFim)*at -
                                                                                                                                                                                0. * dt * (residuoA));
                if (BSW[i] < 0.)
                    BSW[i] = 0.;
                else if (BSW[i] > 1.)
                    BSW[i] = 1.;
                else if (isnan(BSW[i]))
                    BSW[i] = BSWini;
                if (denag[i] < 1.)
                    denag[i] = 1.;
                else if (isnan(denag[i]))
                    denag[i] = denagini;
            } else
                BSW[i] = BSWini;
            denag[i] = denagini;
        }
        celula[i].VolPesaST = volpesFim - 0. * residuoP * dt / at;
        celula[i].VolAguaST = volaguaFim - 0. * residuoA * dt / at;
    }
    for (int i = imin; i <= ncel - 1; i++) {
        if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
            celula[i].flui = fluC[i];
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                celula[i].flui.BSW = BSW[i];
                celula[i].flui.Denag = denag[i];
                celula[i].flui.LVisL = VISCL[i];
                celula[i].flui.LVisH = VISCH[i];
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
            }
        }
    }

    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || celula[ncel].Mliqini > -(*vg1dSP).localtiny) {
        if (arq.nfluP > 0 || (*vg1dSP).chaverede != 0) {
            celula[ncel].flui = celula[ncel - 1].flui;
            if (arq.nfluP > 1 || (*vg1dSP).chaverede != 0) {
                celula[ncel].flui.BSW = BSW[ncel - 1];
                celula[ncel].flui.Denag = denag[ncel - 1];
                celula[ncel].flui.LVisL = VISCL[ncel - 1];
                celula[ncel].flui.LVisH = VISCH[ncel - 1];
                celula[ncel].flui.TempL = tL;
                celula[ncel].flui.TempH = tH;
            }
        }
    } else {
        celula[ncel].flui = fluiRev;
        celula[ncel].flui.BSW = fluiRev.BSW;
        celula[ncel].flui.Denag = fluiRev.Denag;
        celula[ncel].flui.LVisL = fluiRev.LVisL;
        celula[ncel].flui.LVisH = fluiRev.LVisH;
        celula[ncel].flui.TempL = fluiRev.TempL;
        celula[ncel].flui.TempH = fluiRev.TempH;
    }
    delete[] fluC;
    (*vg1dSP).modoTransiente = 1;
}

void SProd::renovaterm(int aflu) {
    // #pragma omp parallel for num_threads(numthreads)
    aflu = 0;
    Vcr<int> bif(ncel + 1, 0);
    Vcr<int> valv(ncel + 1, 1);
    int imax = ncel;
    if (aflu == 1)
        imax = ncel + 1;
    for (int i = 0; i <= ncel; i++) {
        celula[i].c0ini = celula[i].c0;
        celula[i].udini = celula[i].ud;
        if (i == ncel && aflu == 1 && (*vg1dSP).lixo5 >= 1560) {
            int para;
            para = 0;
        }
        if (i != 0 && i != imax) {

            if (i < ncel) {
                celula[i - 1].alfR = celula[i + 1].alfL = celula[i].alf;
                celula[i - 1].betR = celula[i + 1].betL = celula[i].bet;
            } else {
                celula[i - 1].alfR = celula[i].alf;
                celula[i - 1].betR = celula[i].bet;
            }
            valv[i] = 1;
            if (celula[i - 1].acsr.tipo == 5 || celula[i - 1].acsr.tipo == 8) {
                if ((*celula[i].acsrL).tipo == 5 && (*celula[i].acsrL).chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i - 1].duto.area)
                    valv[i] = 0;
                if ((*celula[i].acsrL).tipo == 8 && fabs((*celula[i].acsrL).bvol.freq) > 1)
                    valv[i] = 0;
            }
            if (valv[i] == 1) {
                double razdx = celula[i].dxL / (celula[i].dx + celula[i].dxL);
                double pmed;
                if (i < ncel)
                    pmed = celula[i].presaux;
                else
                    pmed = celula[i].pres;
                double tmed;
                if (i < ncel)
                    tmed = celula[i].temp * razdx + celula[i - 1].temp * (1. - razdx);
                else
                    tmed = tGSup;
                if (celula[i].VTemper < 0.) {
                    if (i < ncel)
                        tmed = celula[i].temp;
                    else
                        tmed = tGSup;
                }
                double betI = celula[i - 1].betPigD;
                double rl;
                if (celula[i].QL < 0.) { // testeBeta
                    betI = celula[i].betPigE;
                    rl = (1 - betI) * celula[i].rpCi + betI * celula[i].rcCi;
                } else {
                    betI = celula[i - 1].betPigD;
                    rl = (1 - betI) * celula[i - 1].rpCi + betI * celula[i - 1].rcCi;
                    // viscl1 = (1 - betI) * celula[i - 1].flui.ViscOleo(pmed, tmed)
                    // tensup1 = (1 - betI) * celula[i - 1].flui.TensSuper(pmed, tmed)
                }
                double rg;
                double amed;
                double hns;
                if (celula[i].QG >= 0) {
                    amed = celula[i].dutoL.area;
                    hns = 1. - celula[i].alfL;
                    rg = celula[i - 1].rgCi;
                } else {
                    rg = celula[i].rgCi;
                    amed = celula[i].duto.area;
                    hns = 1. - celula[i].alf;
                }
                double ugs = celula[i].QG / (amed);
                double uls = celula[i].QL / (amed);
                double dia1 = celula[i].duto.a;
                if (ugs >= 0)
                    dia1 = celula[i - 1].duto.a;

                double rmed = hns * rl + (1 - hns) * rg;
                double ang = celula[i].duto.teta;
                if (i >= 2) {
                    if (celula[i - 2].acsr.tipo == 5 && celula[i - 2].acsr.chk.AreaGarg <= (1e-3)) {
                        if (celula[i].QG >= 0)
                            ang = celula[i].duto.teta;
                        else
                            ang = celula[i].dutoR.teta;
                    } else {
                        if (celula[i].QG >= 0)
                            ang = celula[i].dutoL.teta;
                        else
                            ang = celula[i].duto.teta;
                    }
                }
                double sinal = 1.;
                if (ang < 0.)
                    sinal = -1.;

                double amedL = celula[i].dutoL.area;
                double razdxL = celula[i - 1].dxL / (celula[i - 1].dx + celula[i - 1].dxL);
                double betIL = 0.;
                if (i < 2)
                    betIL = celula[i - 1].betL;
                else
                    betIL = celula[i - 2].betPigD;
                if (celula[i - 1].QL < 0.)
                    betIL = celula[i - 1].betPigE; // testeBeta
                // betIL = celula[i - 1].betPigE;        //duvidabeta
                double rgL = celula[i].rgLi;
                double rlL = (1 - betIL) * celula[i].rpLi + betIL * celula[i].rcLi;
                double ugs0 = (celula[i].ML - celula[i].MliqiniL) / (rgL * amedL);
                double uls0 = (celula[i].MliqiniL) / (rlL * amedL);

                double amedR = celula[i].dutoR.area;
                double razdxR = celula[i].dxR / (celula[i].dxR + celula[i].dx);
                double pmedR = celula[i].presauxR;
                double tmedR = celula[i].temp * razdxR + celula[i].tempR * (1. - razdxR);
                double betIR = celula[i].betPigD;
                if (celula[i].QLR < 0.) { // testeBeta
                    if (i > ncel - 2)
                        betIR = celula[i].betR;
                    else
                        betIR = celula[i + 1].betPigE;
                }
                double rgR = celula[i].rgRi;
                double rlR = (1 - betIR) * celula[i].rpRi + betIR * celula[i].rcRi;
                double ugs1 = (celula[i].MR - celula[i].MliqiniR) / (rgR * amedR);
                double uls1 = (celula[i].MliqiniR) / (rlR * amedR);

                bif[i] = 1;

                if ((*vg1dSP).lixo5 > 29900) {
                    int para;
                    para = 0;
                }

                if (celula[i - 1].alfPigD <= (*vg1dSP).localtiny && celula[i].alfPigE <= (*vg1dSP).localtiny && celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1;
                    celula[i].ud = 0;
                    celula[i].arranjo = 0;
                } else if (celula[i - 1].alfPigD >= (1. - (*vg1dSP).localtiny) && celula[i].alfPigE >= (1. - (*vg1dSP).localtiny) && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1;
                    celula[i].ud = 0;
                    celula[i].arranjo = 0;
                } else if (celula[i - 1].acsr.tipo == 5 && celula[i - 1].acsr.chk.AreaGarg <= (1e-3)) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1;
                    celula[i].ud = 0;
                    celula[i].arranjo = 0;
                }

                else if (ugs >= 0 && celula[i - 1].alfPigD <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1;
                    celula[i].ud = 0;
                    celula[i].arranjo = 0;
                    if (fabs(ugs) <= 1e-15 && celula[i].alfPigE > (1. - (*vg1dSP).localtiny) && uls < 0 && uls0 < 0 && celula[i].duto.teta > 0) {
                        celula[i].term1 = 0.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    }
                    if (fabs(ugs) <= 1e-15 && celula[i].alfPigE > (1. - 10 * (*vg1dSP).localtiny) && celula[i].duto.teta < 0) {
                        celula[i].term1 = 0.;
                        celula[i].term2 = 0.;
                        bif[i] = 1;
                    }
                    if (fabs(ugs) <= 1e-15 && celula[i].alf >= celula[i + 1].alf && ugs1 < 0) {
                        bif[i] = 1;
                    }
                } else if (ugs <= 0 && celula[i].alfPigE <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1;
                    celula[i].ud = 0;
                    celula[i].arranjo = 0;
                    if (fabs(ugs) <= 1e-15 && celula[i - 1].alfPigD > (1. - (*vg1dSP).localtiny) && uls > 0 && uls1 > 0) {
                        celula[i].term1 = 0.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    }
                    if (fabs(ugs) <= 1e-15 && celula[i].alfL >= celula[i - 1].alfL && ugs0 > 0) {
                        bif[i] = 1;
                    }
                    if (fabs(ugs) <= 1e-15 && fabs(uls) <= 1e-15 && celula[i].alf < celula[i - 1].alf && celula[i].duto.teta > 0) {
                        bif[i] = 1;
                    }
                } else if (uls >= 0 && celula[i - 1].alfPigD >= 1. - 1 * (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1;
                    celula[i].ud = 0;
                    celula[i].arranjo = 0;
                    if (fabs(uls) <= 1e-15 && celula[i].alfPigE < (*vg1dSP).localtiny && uls1 < 0) { // ATENCAO!!!!!!!!!!!!!!! não teria de ser bifásico, mono-liq só seo ângulo fosse negativo, não?
                        celula[i].term1 = 1.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    } else if (fabs(uls1) < (*vg1dSP).localtiny * 1e-5) {
                        if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].alfPigE < (*vg1dSP).localtiny && celula[i].fontemassGR >= (*vg1dSP).localtiny * 1e-5) { // ATENCAO!!!!!!!!!!!!!!!  sem sentido isto aqui
                            celula[i].term1 = 1.;
                            celula[i].term2 = 0.;
                            bif[i] = 0;
                        }
                        if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].alfPigE < (*vg1dSP).localtiny && celula[i].duto.teta >= 0) { // ATENCAO!!!!!!!!!!!!!!! alteracao 11/08/24, adicionado
                            bif[i] = 1;
                        }
                        if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].alfPigE < (*vg1dSP).localtiny && celula[i].duto.teta < 0) { // ATENCAO!!!!!!!!!!!!!!! alteracao 11/08/24, adicionado
                            celula[i].term1 = 0.;
                            celula[i].term2 = 0.;
                            bif[i] = 0;
                        }
                    } else if ((fabs(uls) < 1e-15 && (uls1 < 0 || celula[i].duto.teta > 0) // ATENCAO!!!!!!!!!!!!!!! alteracao 11/08/24, estava || mudado para &&
                                && ((celula[i].alfPigE <= (1 - 10 * (*vg1dSP).localtiny + .0 * celula[i].alfPigER) &&
                                     celula[i].alfPigER < 1 - 1 * (*vg1dSP).localtiny) ||
                                    celula[i].alfPigE <= 0.7)))
                        bif[i] = 1;

                } else if (uls <= 0 && celula[i].alfPigE >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1;
                    celula[i].ud = 0;
                    celula[i].arranjo = 0;
                    if (fabs(uls) <= (*vg1dSP).localtiny * 1e-5 && celula[i - 1].alfPigD < (*vg1dSP).localtiny && uls0 > 0) {
                        celula[i].term1 = 1.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    }

                    if (fabs(uls0) < (*vg1dSP).localtiny * 1e-5) {
                        if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i - 1].alfPigD < (*vg1dSP).localtiny && celula[i - 1].fontemassGR >= (*vg1dSP).localtiny * 1e-5) {
                            celula[i].term1 = 1.;
                            celula[i].term2 = 0.;
                            bif[i] = 0;
                        }
                    } else if ((i > 1 && fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta < 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 2].alfPigD && celula[i - 2].alfPigD < 0.99) || celula[i - 1].alfPigD < 0.7)) && ugs >= 0.)
                        bif[i] = 1;
                    else if ((i > 1 && fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 2].alfPigD && celula[i - 2].alfPigD < 0.99) || celula[i - 1].alfPigD < 0.7)) && ugs >= 0)
                        bif[i] = 1;
                    else {
                        if ((fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta < 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 1].alfL) || celula[i - 1].alfPigD < 0.7)) && ugs >= 0)
                            bif[i] = 1;
                        else if ((fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 1].alfL) || celula[i - 1].alfPigD < 0.7)) && ugs >= 0)
                            bif[i] = 1;
                        else if ((fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 1].alfL) || celula[i - 1].alfPigD < 0.7)) && ugs > 0)
                            bif[i] = 1;
                    }
                }

                if (bif[i] == 1) {

                    double c0;
                    double ud;
                    double alfmed;

                    alfmed = celula[i - 1].alfPigD;
                    if (ugs < 0)
                        alfmed = celula[i].alfPigE;
                    c0 = 1.2;
                    double dmed = celula[i].duto.a;
                    if (celula[i].MC >= 0)
                        dmed = celula[i].dutoL.a;
                    double sinal = 1.;
                    if (celula[i].duto.teta < 0.)
                        sinal = -1.;
                    ud = sinal * 0.32 * sqrt(9.82 * dmed);
                    if (fabs(rgR) / rlR > 0.9) {
                        c0 = 1.;
                        ud = 0.;
                    }
                    if (fabs(celula[i].QG / (0.25 * M_PI * dmed * dmed * alfmed)) > 100. ||
                        fabs(celula[i].QL / (0.25 * M_PI * dmed * dmed * (1. - alfmed))) > 100.) {
                        c0 = 1.;
                        ud = 0.;
                    } else
                        CalcC0Ud(i, c0, ud);
                    celula[i].c0 = c0;
                    celula[i].ud = ud;
                    if (i == ncel) {
                        double num = (1. - alfmed * c0);
                        double den = 1 + c0 * alfmed * ((rg / rl) - 1.);
                        celula[i].term1 = num / den;
                        celula[i].term2 = (-amed * alfmed * rg * ud) / den;
                        double jlTeste0 = (ugs - alfmed * ud) / (alfmed * c0) - ugs;
                        double jlTeste = (ugs + uls) * (1. - c0 * alfmed) - alfmed * ud;
                        if ((jlTeste > 0. || jlTeste0 > 0.) && celula[i - 1].alfPigD > 1 - 1e-15) {
                            celula[i].term1 = 0.;
                            celula[i].term2 = 0.;
                        }
                        if ((jlTeste < 0. || jlTeste0 < 0.) && celula[i].alfPigE > 1 - 1e-15) {
                            celula[i].term1 = 0.;
                            celula[i].term2 = 0.;
                        }
                        if (celula[i - 1].acsr.tipo == 5 && celula[i - 1].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i - 1].duto.area) {
                            celula[i].term1 = 0.;
                            celula[i].term2 = 0.;
                        }
                    }
                }
            } else {
                celula[i].c0 = 1.;
                celula[i].ud = 0.;
                celula[i].term1 = 0.;
                celula[i].term2 = 0.;
                celula[i].term1L = celula[i - 1].term1;
                celula[i].term2L = celula[i - 1].term2;
                celula[i - 1].term1R = celula[i].term1;
                celula[i - 1].term2R = celula[i].term2;
            }
        } else if (i == 0) {
            if (arq.ConContEntrada == 0) {
                celula[1].alfL = celula[0].alf;
                celula[0].alfL = celula[0].alf;
                celula[1].betL = celula[0].bet;
                celula[0].betL = celula[0].bet;
                celula[0].term1 = 0.;
                celula[0].term2 = 0.;
                celula[0].term1L = 0.;
                celula[0].term2L = 0.;
            } else {
                if (titE < 1) {
                    int para;
                    para = 0;
                }

                double razdx = 0.5;
                double pmed;
                pmed = presE;

                double tmed;
                if (celula[0].QL < 0.)
                    tmed = celula[i].temp;
                else
                    tmed = tempE;

                double rg = celula[0].flui.MasEspGas(presE, tempE);
                double rl = celula[0].flui.MasEspLiq(presE, tempE);
                double rcis = celula[0].fluicol.MasEspFlu(presE, tempE);

                double rlmist = betaE * rcis + (1 - betaE) * rl;
                alfE = (-titE * rlmist / (titE * rg - rg - titE * rlmist)) / (celula[0].c0);

                double betI;
                double viscl1;
                double tensup1;
                if (celula[i].QL < 0.) { // testeBeta
                    betI = celula[i].betPigE;
                    rl = (1 - betI) * celula[i].flui.MasEspLiq(pmed, tmed) + betI * celula[i].fluicol.MasEspFlu(pmed, tmed);
                    viscl1 = (1 - betI) * celula[i].flui.ViscOleo(pmed, tmed) + betI * celula[i].fluicol.VisFlu(pmed, tmed);
                    tensup1 = (1 - betI) * celula[i].flui.TensSuper(pmed, tmed) + betI * celula[i].fluicol.TensSuper(pmed, tmed);
                } else {
                    betI = betaE;
                    rl = (1 - betI) * (*celula[i].fluiL).MasEspLiq(pmed, tmed) + betI * celula[i].fluicol.MasEspFlu(pmed, tmed);
                    viscl1 = (1 - betI) * (*celula[i].fluiL).ViscOleo(pmed, tmed) + betI * celula[i].fluicol.VisFlu(pmed, tmed);
                    tensup1 = (1 - betI) * (*celula[i].fluiL).TensSuper(pmed, tmed) + betI * celula[i].fluicol.TensSuper(pmed, tmed);
                }

                double viscg1;
                double amed;
                double hns;
                if (celula[i].QG >= 0) {
                    amed = celula[i].duto.area;
                    rg = (*celula[i].fluiL).MasEspGas(pmed, tmed);
                    viscg1 = (*celula[i].fluiL).ViscGas(pmed, tmed);
                    hns = 1. - alfE;
                } else {
                    rg = celula[i].flui.MasEspGas(pmed, tmed);
                    viscg1 = celula[i].flui.ViscGas(pmed, tmed);
                    amed = celula[i].duto.area;
                    hns = 1. - celula[i].alf;
                }
                double ugs = celula[i].QG / (amed);
                double uls = celula[i].QL / (amed);
                double dia1 = celula[i].duto.a;

                double rmed = hns * rl + (1 - hns) * rg;
                double visc = (hns * viscl1 + (1 - hns) * viscg1) / pow(10., 3.);
                double ang = celula[i].duto.teta;
                double sinal = 1.;
                if (ang < 0.)
                    sinal = -1.;
                double xc0 = 2.;
                double xud = sinal * 0.0246 * cos(ang) + 1.606 * pow(9.82 * tensup1 * (rl - rg) / (rl * rl), 0.25) * sin(ang);

                double amedR = celula[i].dutoR.area;
                double razdxR = celula[i].dx / (celula[i].dxR + celula[i].dx);
                double pmedR = celula[i].presauxR;
                double tmedR = celula[i].temp * razdxR + celula[i].tempL * (1. - razdxR);
                double betIR = celula[i].betPigD;
                if (celula[i].QLR < 0.) // testeBeta
                    betIR = celula[i + 1].betPigE;

                double rgR = celula[i].flui.MasEspGas(pmedR, tmedR);
                double rlR = (1 - betIR) * celula[i].flui.MasEspLiq(pmedR, tmedR) + betIR * celula[i].fluicol.MasEspFlu(pmedR, tmedR);
                double ugs1 = (celula[i].MR - celula[i].MliqiniR) / (rgR * amedR);
                double uls1 = (celula[i].MliqiniR) / (rlR * amedR);

                bif[i] = 1;

                if ((*vg1dSP).lixo5 > 29900) {
                    int para;
                    para = 0;
                }

                if (alfE < (*vg1dSP).localtiny && celula[i].alfPigE <= (*vg1dSP).localtiny && celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1 + 0 * xc0;
                    celula[i].ud = 0 * xud;
                    celula[i].arranjo = 0;
                } else if (alfE >= (1. - (*vg1dSP).localtiny) && celula[i].alfPigE >= (1. - (*vg1dSP).localtiny) && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1 + 0 * xc0;
                    celula[i].ud = 0 * xud;
                    celula[i].arranjo = 0;
                } else if (ugs >= 0 && alfE <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1 + 0 * xc0;
                    celula[i].ud = 0 * xud;
                    celula[i].arranjo = 0;
                    if (fabs(ugs) <= 1e-15 && celula[i].alfPigE > (1. - (*vg1dSP).localtiny) && uls < 0 && celula[i].duto.teta > 0) {
                        celula[i].term1 = 0.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    }
                    if (fabs(ugs) <= 1e-15 && celula[i].alfPigE > (1. - 1 * (*vg1dSP).localtiny) && celula[i].duto.teta < 0 && uls > 0) {
                        celula[i].term1 = 0.;
                        celula[i].term2 = 0.;
                        bif[i] = 1;
                    }
                    if (fabs(ugs) <= 1e-15 && celula[i].alf >= alfE && uls < 0) {
                        bif[i] = 1;
                    }
                } else if (ugs <= 0 && celula[i].alfPigE <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1 + 0 * xc0;
                    celula[i].ud = 0 * xud;
                    celula[i].arranjo = 0;
                    if (fabs(ugs) <= 1e-15 && alfE > (1. - (*vg1dSP).localtiny) && uls > 0) {
                        celula[i].term1 = 0.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    }
                    if (fabs(ugs) <= 1e-15 && alfE > (*vg1dSP).localtiny && uls > 0) {
                        bif[i] = 1;
                    }
                } else if (uls >= 0 && alfE >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1 + 0 * xc0;
                    celula[i].ud = 0 * xud;
                    celula[i].arranjo = 0;
                    if (fabs(uls) <= 1e-15 && celula[i].alfPigE < (*vg1dSP).localtiny && uls1 < 0) {
                        celula[i].term1 = 1.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    } else if (fabs(uls1) < (*vg1dSP).localtiny * 1e-5) {
                        if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].alfPigE < (*vg1dSP).localtiny && celula[i].fontemassGR >= (*vg1dSP).localtiny * 1e-5) {
                            celula[i].term1 = 1.;
                            celula[i].term2 = 0.;
                            bif[i] = 0;
                        }
                    } else if (fabs(uls) < 1e-15 && uls1 < 0 && ((celula[i].alfPigE <= (1 - 1 * (*vg1dSP).localtiny + .0 * celula[i].alfPigER) && celula[i].alfPigER < 1 - 1 * (*vg1dSP).localtiny) || celula[i].alfPigE <= 0.7))
                        bif[i] = 1;

                } else if (uls <= 0 && celula[i].alfPigE >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                    celula[i].c0 = 1 + 0 * xc0;
                    celula[i].ud = 0 * xud;
                    celula[i].arranjo = 0;
                    if (fabs(uls) <= (*vg1dSP).localtiny * 1e-5 && alfE < (*vg1dSP).localtiny && uls1 < 0) {
                        celula[i].term1 = 1.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    }

                    if (fabs(uls1) < (*vg1dSP).localtiny * 1e-5) {
                        if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && alfE < (*vg1dSP).localtiny) {
                            celula[i].term1 = 1.;
                            celula[i].term2 = 0.;
                            bif[i] = 0;
                        }
                    } else {
                        if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta < 0.95 * M_PI / 2. && ugs < 0 && (alfE < 0.7))
                            bif[i] = 1;
                        else if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && ugs < 0 && (alfE < 0.7))
                            bif[i] = 1;
                    }
                }
                if (uls > 0 && fabs(ugs) <= (*vg1dSP).localtiny * 1e-5 && alfE > (*vg1dSP).localtiny && alfE < 1 - (*vg1dSP).localtiny)
                    bif[i] = 1;
                if (ugs > 0 && fabs(uls) <= (*vg1dSP).localtiny * 1e-5 && alfE > (*vg1dSP).localtiny && alfE < 1 - (*vg1dSP).localtiny)
                    bif[i] = 1;
                if (uls >= 0 && alfE > 1 - (*vg1dSP).localtiny) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                }
                if (ugs >= 0 && alfE < (*vg1dSP).localtiny) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                }

                if (bif[i] == 1) {

                    double c0;
                    double ud;
                    double alfmed;

                    alfmed = alfE;
                    if (ugs < 0)
                        alfmed = celula[i].alfPigE;
                    c0 = 1.2;
                    double dmed = celula[i].duto.a;
                    if (celula[i].MC >= 0)
                        dmed = celula[i].dutoL.a;
                    double sinal = 1.;
                    if (celula[i].duto.teta < 0.)
                        sinal = -1.;
                    ud = sinal * 0.32 * sqrt(9.82 * dmed);
                    if (fabs(rgR) / rlR > 0.9) {
                        c0 = 1.;
                        ud = 0.;
                    }
                    CalcC0UdIni(i, c0, ud);
                    celula[i].c0 = c0;
                    celula[i].ud = ud;
                    double num = (1. - alfmed * c0);
                    double den = 1 + c0 * alfmed * ((rg / rl) - 1.);
                    celula[i].term1 = num / den;
                    celula[i].term2 = (-amed * alfmed * rg * ud) / den;
                }
                celula[1].term1L = celula[i].term1;
                celula[1].term2L = celula[i].term2;

                celula[1].alfL = celula[0].alf;
                celula[0].alfL = alfE;
                celula[1].betL = celula[0].bet;
                celula[0].betL = betaE;
            }

        } else if (aflu == 0) {
            celula[ncel - 1].alfR = celula[ncel].alf;
            celula[ncel].alfR = celula[ncel].alf;
            celula[ncel - 1].betR = celula[ncel].bet;
            celula[ncel].betR = celula[ncel].bet;

            double razdx = celula[i].dxL / (celula[i].dx + celula[i].dxL);
            double pmed = celula[i].presaux;
            double tmed = celula[i].temp * razdx + celula[i - 1].temp * (1. - razdx);
            double betI = celula[i].betL;
            if (celula[i].QL < 0.)
                betI = celula[i].bet; // testeBeta
            // betI = celula[i].bet;            //duvidabeta
            double rg = celula[i].flui.MasEspGas(pmed, tmed);
            double rl = (1 - betI) * celula[i].flui.MasEspLiq(pmed, tmed) + betI * celula[i].fluicol.MasEspFlu(pmed, tmed);
            double amed = celula[i].duto.area;
            if (celula[i].MC >= 0)
                amed = celula[i].dutoL.area;
            double ugs = celula[i].QG / (amed);
            double uls = celula[i].QL / (amed);

            double amedL = celula[i].dutoL.area;
            double razdxL = celula[i - 1].dxL / (celula[i - 1].dx + celula[i - 1].dxL);
            double pmedL = celula[i - 1].presaux;
            double tmedL = celula[i - 1].temp * razdxL + celula[i - 1].tempL * (1. - razdxL);
            double betIL = celula[i - 1].betL;
            if (celula[i - 1].QL < 0.)
                betIL = celula[i - 1].bet; // testeBeta
            // betIL = celula[i - 1].bet;            //duvidabeta
            double rgL = celula[i].flui.MasEspGas(pmedL, tmedL);
            double rlL = (1 - betIL) * celula[i].flui.MasEspLiq(pmedL, tmedL) + betIL * celula[i].fluicol.MasEspFlu(pmedL, tmedL);
            double uls0 = (celula[i].MliqiniL) / (rlL * amedL);

            double amedR = celula[i].dutoR.area;
            double razdxR = celula[i].dxR / (celula[i].dxR + celula[i].dx);
            double pmedR = celula[i].pres;
            double tmedR = celula[i].temp * razdxR + celula[i].tempR * (1. - razdxR);
            double betIR = celula[i].bet;
            double rgR = celula[i].flui.MasEspGas(pmedR, tmedR);
            double rlR = (1 - betIR) * celula[i].flui.MasEspLiq(pmedR, tmedR) + betIR * celula[i].fluicol.MasEspFlu(pmedR, tmedR);
            double uls1 = (celula[i].MliqiniR) / (rlR * amedR);

            bif[i] = 1;

            if (celula[i].alfL <= (*vg1dSP).localtiny && celula[i].alf <= (*vg1dSP).localtiny && celula[i].fontemassGL <= 0 && celula[i].fontemassGR <= 0) {
                celula[i].term1 = 1.;
                celula[i].term2 = 0.;
                bif[i] = 0;
                celula[i].c0 = 1. - (*vg1dSP).localtiny;
                celula[i].ud = 0.;
                celula[i].arranjo = 0;
            } else if (celula[i].alfL >= (1. - (*vg1dSP).localtiny) && celula[i].alf >= (1. - (*vg1dSP).localtiny) && celula[i].fontemassLL <= 0 && celula[i].fontemassLR <= 0) {
                celula[i].term1 = 0.;
                celula[i].term2 = 0.;
                bif[i] = 0;
                celula[i].c0 = 1. - (*vg1dSP).localtiny;
                celula[i].ud = 0.;
                celula[i].arranjo = 0;
            } else if (ugs > 0 && celula[i].alfL <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= 0. && celula[i].fontemassGR <= 0.)) {
                celula[i].term1 = 1.;
                celula[i].term2 = 0.;
                bif[i] = 0;
                celula[i].c0 = 1. - (*vg1dSP).localtiny;
                celula[i].ud = 0.;
                celula[i].arranjo = 0;
                if (fabs(ugs) <= 1e-15 && celula[i].alf > (1. - (*vg1dSP).localtiny) && uls < 0 && uls0 < 0) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                }
            } else if (ugs < 0 && celula[i].alf <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= 0. && celula[i].fontemassGR <= 0.)) {
                celula[i].term1 = 1.;
                celula[i].term2 = 0.;
                bif[i] = 0;
                celula[i].c0 = 1. - (*vg1dSP).localtiny;
                celula[i].ud = 0.;
                celula[i].arranjo = 0;
                if (fabs(ugs) <= 1e-15 && celula[i].alfL > (1. - (*vg1dSP).localtiny) && uls > 0 && uls1 > 0) {
                    celula[i].term1 = 0.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                }
            } else if (uls >= 0 && celula[i - 1].alf >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
                celula[i].term1 = 0.;
                celula[i].term2 = 0.;
                bif[i] = 0;
                celula[i].c0 = 1;
                celula[i].ud = 0;
                celula[i].arranjo = 0;
                if (fabs(uls) <= 1e-15 && celula[i].alf < (*vg1dSP).localtiny && uls1 < 0) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                } else if (fabs(uls1) < (*vg1dSP).localtiny * 1e-5) {
                    if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].alf < (*vg1dSP).localtiny && celula[i].fontemassGR >= (*vg1dSP).localtiny * 1e-5) {
                        celula[i].term1 = 1.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    }
                } else if (fabs(uls) < 1e-15 && uls1 < 0 && celula[i].alf <= (1 - 1 * (*vg1dSP).localtiny))
                    bif[i] = 1;
            }

            else if (uls <= 0 && celula[i].alf >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
                celula[i].term1 = 0.;
                celula[i].term2 = 0.;
                bif[i] = 0;
                celula[i].c0 = 1;
                celula[i].ud = 0;
                celula[i].arranjo = 0;
                if (fabs(uls) <= (*vg1dSP).localtiny * 1e-5 && celula[i - 1].alf < (*vg1dSP).localtiny && uls0 > 0) {
                    celula[i].term1 = 1.;
                    celula[i].term2 = 0.;
                    bif[i] = 0;
                }
                if (fabs(uls0) < (*vg1dSP).localtiny * 1e-5) {
                    if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i - 1].alf < (*vg1dSP).localtiny && celula[i - 1].fontemassGR >= (*vg1dSP).localtiny * 1e-5) {
                        celula[i].term1 = 1.;
                        celula[i].term2 = 0.;
                        bif[i] = 0;
                    }
                } else if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta < 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 2].alfPigD && celula[i - 2].alfPigD < 0.99) || celula[i - 1].alfPigD < 0.7))
                    bif[i] = 1;
                else if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 2].alfPigD && celula[i - 2].alfPigD < 0.99) || celula[i - 1].alfPigD < 0.7))
                    bif[i] = 1;
                else {
                    if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta < 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 1].alfL) || celula[i - 1].alfPigD < 0.7))
                        bif[i] = 1;
                    else if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 1].alfL) || celula[i - 1].alfPigD < 0.7))
                        bif[i] = 1;
                }
            }

            if (bif[i] == 1) {
                double alfmed;
                alfmed = celula[i].alfL;
                double c0 = 1.2;
                double dmed = celula[i].duto.a;
                if (celula[i].MC >= 0)
                    dmed = celula[i].dutoL.a;
                double sinal = 1.;
                if (celula[i].duto.teta < 0.)
                    sinal = 1.;
                double ud = sinal * 0.32 * sqrt(9.82 * dmed);
                if (fabs(rgR) / rlR > 0.9) {
                    c0 = 1.;
                    ud = 0.;
                }
                CalcC0Ud(i, c0, ud);
                if (arq.escorregamentoCelulaContorno == 0) {
                    c0 = 1.;
                    ud = 0.;
                }
                celula[i].c0 = c0;
                celula[i].ud = ud;
                double num = (1. - alfmed * c0);
                double den = 1. + alfmed * (rg / rl) * c0 - alfmed * c0;
                celula[i].term1 = num / den;
                celula[i].term2 = (-amed * alfmed * rg * ud) / den;

                // teste!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
                // teste!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
            }

            celula[i].term1L = celula[i - 1].term1;
            celula[i].term2L = celula[i - 1].term2;
            celula[i - 1].term1R = celula[i].term1;
            celula[i - 1].term2R = celula[i].term2;
        }
    }

    int parada = 0;
    for (int i = 1; i < ncel; i++) {
        if (celula[i].acsr.tipo == 5 && celula[i].acsr.chk.AreaGarg <= 1e-15 * celula[i].acsr.chk.AreaTub)
            parada = 1;
        else if (chokeSup.AreaGarg <= 1.e-15 * chokeSup.AreaTub)
            parada = 1;
    }

    Vcr<double> c0V(ncel, 0.);
    Vcr<double> udV(ncel, 0.);
    for (int i = 1; i < ncel; i++) {
        c0V[i] = celula[i].c0;
        udV[i] = celula[i].ud;
        if (bif[i] == 1 && valv[i] == 1 && i > 2) {
            int iViz = i - 1;
            int iViz2 = i - 2;
            if (celula[i].QG < 0) {
                iViz = i + 1;
                iViz2 = i;
            }
            if ((celula[iViz].acsr.tipo == 0 && (celula[iViz2].acsr.tipo != 5 || celula[iViz2].acsr.chk.AreaGarg > (1e-3))) &&
                (celula[i].arranjo != celula[iViz].arranjo && bif[iViz] != 0)) {
                c0V[i] = (celula[i].dx * celula[i].c0 + celula[iViz].dx * celula[iViz].c0) / (celula[i].dx + celula[iViz].dx);
                if (celula[i].duto.teta * celula[iViz].duto.teta >= 0)
                    udV[i] = (celula[i].dx * celula[i].ud + celula[iViz].dx * celula[iViz].ud) / (celula[i].dx + celula[iViz].dx);
            } else if (i > 2) {
                double ang;
                double angL;
                double dia;
                double diaL;
                if ((celula[i - 1].acsr.tipo == 0 && (celula[i - 2].acsr.tipo != 5 || celula[i - 2].acsr.chk.AreaGarg > (1e-3))) &&
                    celula[i].QG >= 0) {
                    ang = celula[i].duto.teta;
                    angL = celula[i - 1].duto.teta;
                    dia = celula[i].duto.dia;
                    diaL = celula[i - 1].duto.dia;
                    if (((ang != angL) && bif[iViz] != 0) ||
                        (celula[i].arranjo != celula[iViz].arranjo && bif[iViz] != 0)) {
                        c0V[i] = (celula[i].dx * celula[i].c0 + celula[i - 1].dx * celula[i - 1].c0) / (celula[i].dx + celula[i - 1].dx);
                        if (ang * angL >= 0)
                            udV[i] = (celula[i].dx * celula[i].ud + celula[i - 1].dx * celula[i - 1].ud) / (celula[i].dx + celula[i - 1].dx);
                    }
                } else if ((celula[i + 1].acsr.tipo == 0 && (celula[i].acsr.tipo != 5 || celula[i].acsr.chk.AreaGarg > (1e-3))) &&
                           celula[i].QG < 0) {
                    ang = celula[i].duto.teta;
                    angL = celula[i + 1].duto.teta;
                    dia = celula[i].duto.dia;
                    diaL = celula[i + 1].duto.dia;
                    if (((ang != angL) && bif[iViz] != 0) ||
                        (celula[i].arranjo != celula[iViz].arranjo && bif[iViz] != 0)) {
                        c0V[i] = (celula[i].dx * celula[i].c0 + celula[i + 1].dx * celula[i + 1].c0) / (celula[i].dx + celula[i + 1].dx);
                        if (ang * angL >= 0)
                            udV[i] = (celula[i].dx * celula[i].ud + celula[i + 1].dx * celula[i + 1].ud) / (celula[i].dx + celula[i + 1].dx);
                    }
                }
            }
        }
    }

    for (int i = 1; i < ncel; i++) {
        if (bif[i] == 1 && valv[i] == 1) {
            double betI = celula[i - 1].betPigD;
            double rl;

            if (celula[i].QL < 0.) {
                betI = celula[i].betPigE;
                rl = (1 - betI) * celula[i].rpCi + betI * celula[i].rcCi;
            } else {
                betI = celula[i - 1].betPigD;
                rl = (1 - betI) * celula[i - 1].rpCi + betI * celula[i - 1].rcCi;
            }

            double rg;
            double amed;
            double hns;
            if (celula[i].QG >= 0) {
                amed = celula[i].dutoL.area;
                hns = 1. - celula[i].alfL;
                rg = celula[i - 1].rgCi;
            } else {
                rg = celula[i].rgCi;
                amed = celula[i].duto.area;
                hns = 1. - celula[i].alf;
            }
            double ugs = celula[i].QG / (amed);
            double uls = celula[i].QL / (amed);

            double alfmed;
            alfmed = celula[i - 1].alfPigD;
            if (ugs < 0)
                alfmed = celula[i].alfPigE;
            double num = (1. - alfmed * celula[i].c0);
            double den = 1 + celula[i].c0 * alfmed * ((rg / rl) - 1.);
            celula[i].term1 = num / den;
            celula[i].term2 = (-amed * alfmed * rg * celula[i].ud) / den;
            double jlTeste0 = (ugs - alfmed * celula[i].ud) / (alfmed * celula[i].c0) - ugs;
            double jlTeste = (ugs + uls) * (1. - celula[i].c0 * alfmed) - alfmed * celula[i].ud;
            double MLTeste = celula[i].term1 * celula[i].MC + celula[i].term2;
            double MGTeste = (1 - celula[i].term1) * celula[i].MC - celula[i].term2;
            if ((jlTeste > 0. || jlTeste0 > 0.) && celula[i - 1].alfPigD > 1 - 1e-15) {
                celula[i].term1 = 0.;
                celula[i].term2 = 0.;
            }
            if ((jlTeste < 0. || jlTeste0 < 0.) && celula[i].alfPigE > 1 - 1e-15) {
                celula[i].term1 = 0.;
                celula[i].term2 = 0.;
            }
            if (celula[i - 1].acsr.tipo == 5 && celula[i - 1].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i - 1].duto.area) {
                celula[i].term1 = 0.;
                celula[i].term2 = 0.;
            }
            if ((jlTeste < 0. || jlTeste0 < 0.) && ((fabs(celula[i - 1].QG / (amed)) + fabs(celula[i - 1].QL / (amed))) < 0.1) &&
                (parada == 1 && arq.modoSegrega == 1) && (celula[i].duto.teta > 0 && celula[i - 1].duto.teta <= 0) &&
                (MLTeste > 0 && celula[i].term2 > 0) &&
                ((1. - celula[i].alfL) /**fabs(sin(celula[i-1].duto.teta))*/ < (1. - celula[i].alf) /**fabs(sin(celula[i].duto.teta))*/)) {
                celula[i].term2 = 0.;
            }
            if (celula[i].duto.teta > 0 && MGTeste < 0 && ((parada == 1 && arq.modoSegrega == 1)) && celula[i].alfPigE > 1 - 1e-15) {
                celula[i].term1 = 0.;
                celula[i].term2 = 0.;
            }
        }
    }
    for (int i = 1; i <= ncel; i++) {
        celula[i].term1L = celula[i - 1].term1;
        celula[i].term2L = celula[i - 1].term2;
        celula[i - 1].term1R = celula[i].term1;
        celula[i - 1].term2R = celula[i].term2;
    }
}

void SProd::renovatermAfluFim() {

    int i = ncel;

    double razdx = celula[i].dx / (celula[i].dx + celula[i].dxL);
    double pmed = celula[i].presBuf;
    double tmed = tGSup;
    tmed = celula[i - 1].temp;
    if (celula[i].VTemper < 0.)
        tmed = tGSup;
    double betI = celula[i - 1].betPigD;
    double rl;
    double viscl1;
    double tensup1;
    if (celula[i].MliqiniBuf < 0.) { // testeBeta
        betI = celula[i].betPigE;
        rl = (1 - betI) * celula[i].flui.MasEspLiq(pmed, tmed) + betI * celula[i].fluicol.MasEspFlu(pmed, tmed);
        viscl1 = (1 - betI) * celula[i].flui.ViscOleo(pmed, tmed) + betI * celula[i].fluicol.VisFlu(pmed, tmed);
        tensup1 = (1 - betI) * celula[i].flui.TensSuper(pmed, tmed) + betI * celula[i].fluicol.TensSuper(pmed, tmed);
    } else {
        betI = celula[i - 1].betPigD;
        rl = (1 - betI) * celula[i - 1].flui.MasEspLiq(pmed, tmed) + betI * celula[i - 1].fluicol.MasEspFlu(pmed, tmed);
        viscl1 = (1 - betI) * celula[i - 1].flui.ViscOleo(pmed, tmed) + betI * celula[i - 1].fluicol.VisFlu(pmed, tmed);
        tensup1 = (1 - betI) * celula[i - 1].flui.TensSuper(pmed, tmed) + betI * celula[i - 1].fluicol.TensSuper(pmed, tmed);
    }
    double rg;
    double viscg1;
    double amed;
    double hns;
    if ((celula[i].MCBuf - celula[i].MliqiniBuf) >= 0) {
        amed = celula[i].dutoL.area;
        hns = 1. - celula[i].alfL;
        rg = celula[i - 1].flui.MasEspGas(pmed, tmed);
        viscg1 = celula[i - 1].flui.ViscGas(pmed, tmed);
    } else {
        rg = celula[i].flui.MasEspGas(pmed, tmed);
        viscg1 = celula[i].flui.ViscGas(pmed, tmed);
        amed = celula[i].duto.area;
        hns = 1. - celula[i].alf;
    }
    double ugs = (celula[i].MCBuf - celula[i].MliqiniBuf) / (rg * amed);
    double uls = (celula[i].MliqiniBuf) / (rl * amed);
    double dia1 = celula[i].duto.a;
    if (ugs >= 0)
        dia1 = celula[i - 1].duto.a;

    double rmed = hns * rl + (1 - hns) * rg;
    double visc = (hns * viscl1 + (1 - hns) * viscg1) / pow(10., 3.);
    double nrey = dia1 * rmed * (fabs(ugs) / amed + fabs(uls) / amed) / visc;
    double ang = celula[i].duto.teta;
    if (i >= 2) {
        if (celula[i - 2].acsr.tipo == 5 && celula[i - 2].acsr.chk.AreaGarg <= (1e-3)) {
            if ((celula[i].MCBuf - celula[i].MliqiniBuf) >= 0)
                ang = celula[i].duto.teta;
            else
                ang = celula[i].dutoR.teta;
        } else {
            if ((celula[i].MCBuf - celula[i].MliqiniBuf) >= 0)
                ang = celula[i].dutoL.teta;
            else
                ang = celula[i].duto.teta;
        }
    }
    double sinal = 1.;
    if (ang < 0.)
        sinal = -1.;
    double xc0 = 2.;
    double xud = sinal * 0.0246 * cos(ang) + 1.606 * pow(9.82 * tensup1 * (rl - rg) / (rl * rl), 0.25) * sin(ang);

    double amedL = celula[i].dutoL.area;
    double razdxL = celula[i - 1].dxL / (celula[i - 1].dx + celula[i - 1].dxL);
    double pmedL = celula[i - 1].presaux;
    double tmedL = celula[i - 1].temp * razdxL + celula[i - 1].tempL * (1. - razdxL);
    double betIL = 0.;
    if (i < 2)
        betIL = celula[i - 1].betL;
    else
        betIL = celula[i - 2].betPigD;
    if (celula[i - 1].MliqiniBuf < 0.)
        betIL = celula[i - 1].betPigE; // testeBeta
    // betIL = celula[i - 1].betPigE;    //duvidabeta
    double rgL = celula[i].flui.MasEspGas(pmedL, tmedL);
    double rlL = (1 - betIL) * celula[i].flui.MasEspLiq(pmedL, tmedL) + betIL * celula[i].fluicol.MasEspFlu(pmedL, tmedL);
    double ugs0 = (celula[i].MLBuf - celula[i].MliqiniLBuf) / (rgL * amedL);
    double uls0 = (celula[i].MliqiniLBuf) / (rlL * amedL);

    double amedR = celula[i].dutoR.area;
    double razdxR = celula[i].dxR / (celula[i].dxR + celula[i].dx);
    double pmedR = celula[i].presRBuf;
    double tmedR = celula[i].temp * razdxR + celula[i].tempR * (1. - razdxR);
    double betIR = celula[i].betPigD;
    if (celula[i].MliqiniRBuf < 0.) { // testeBeta
        if (i > ncel - 2)
            betIR = celula[i].betR;
        else
            betIR = celula[i + 1].betPigE;
    }
    double rgR = celula[i].flui.MasEspGas(pmedR, tmedR);
    double rlR = (1 - betIR) * celula[i].flui.MasEspLiq(pmedR, tmedR) + betIR * celula[i].fluicol.MasEspFlu(pmedR, tmedR);
    double ugs1 = (celula[i].MRBuf - celula[i].MliqiniRBuf) / (rgR * amedR);
    double uls1 = (celula[i].MliqiniRBuf) / (rlR * amedR);

    int bif = 1;

    if ((*vg1dSP).lixo5 > 29900) {
        int para;
        para = 0;
    }

    if (celula[i - 1].alfPigD <= (*vg1dSP).localtiny && celula[i].alfPigE <= (*vg1dSP).localtiny && celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5) {
        celula[i].term1 = 1.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
    } else if (celula[i - 1].alfPigD >= (1. - (*vg1dSP).localtiny) && celula[i].alfPigE >= (1. - (*vg1dSP).localtiny) && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 0.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
    } else if (celula[i - 1].acsr.tipo == 5 && celula[i - 1].acsr.chk.AreaGarg <= (1e-3)) {
        celula[i].term1 = 0.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
    }

    else if (ugs >= 0 && celula[i - 1].alfPigD <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 1.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
        if (fabs(ugs) <= 1e-15 && celula[i].alfPigE > (1. - (*vg1dSP).localtiny) && uls < 0 && uls0 < 0 && celula[i].duto.teta > 0) {
            celula[i].term1 = 0.;
            celula[i].term2 = 0.;
            bif = 0;
        }
        if (fabs(ugs) <= 1e-15 && celula[i].alfPigE > (1. - 10 * (*vg1dSP).localtiny) && celula[i].duto.teta < 0) {
            celula[i].term1 = 0.;
            celula[i].term2 = 0.;
            bif = 1;
        }
        if (fabs(ugs) <= 1e-15 && celula[i].alf >= celula[i + 1].alf && ugs1 < 0) {
            bif = 1;
        }
    } else if (ugs <= 0 && celula[i].alfPigE <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 1.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
        if (fabs(ugs) <= 1e-15 && celula[i - 1].alfPigD > (1. - (*vg1dSP).localtiny) && uls > 0 && uls1 > 0) {
            celula[i].term1 = 0.;
            celula[i].term2 = 0.;
            bif = 0;
        }
        if (fabs(ugs) <= 1e-15 && celula[i].alfL >= celula[i - 1].alfL && ugs0 > 0) {
            bif = 1;
        }
    } else if (uls >= 0 && celula[i - 1].alfPigD >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 0.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
        if (fabs(uls) <= 1e-15 && celula[i].alfPigE < (*vg1dSP).localtiny && uls1 < 0) {
            celula[i].term1 = 1.;
            celula[i].term2 = 0.;
            bif = 0;
        } else if (fabs(uls1) < (*vg1dSP).localtiny * 1e-5) {
            if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].alfPigE < (*vg1dSP).localtiny && celula[i].fontemassGR >= (*vg1dSP).localtiny * 1e-5) {
                celula[i].term1 = 1.;
                celula[i].term2 = 0.;
                bif = 0;
            }
        } else if (fabs(uls) < 1e-15 && uls1 < 0 && ((celula[i].alfPigE <= (1 - 1 * (*vg1dSP).localtiny + .0 * celula[i].alfPigER) && celula[i].alfPigER < 1 - 1 * (*vg1dSP).localtiny) || celula[i].alfPigE <= 0.7))
            bif = 1;

    } else if (uls <= 0 && celula[i].alfPigE >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 0.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
        if (fabs(uls) <= (*vg1dSP).localtiny * 1e-5 && celula[i - 1].alfPigD < (*vg1dSP).localtiny && uls0 > 0) {
            celula[i].term1 = 1.;
            celula[i].term2 = 0.;
            bif = 0;
        }

        if (fabs(uls0) < (*vg1dSP).localtiny * 1e-5) {
            if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i - 1].alfPigD < (*vg1dSP).localtiny && celula[i - 1].fontemassGR >= (*vg1dSP).localtiny * 1e-5) {
                celula[i].term1 = 1.;
                celula[i].term2 = 0.;
                bif = 0;
            }
        } else if (i > 1 && fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta < 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 2].alfPigD && celula[i - 2].alfPigD < 0.99) || celula[i - 1].alfPigD < 0.7))
            bif = 1;
        else if (i > 1 && fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 2].alfPigD && celula[i - 2].alfPigD < 0.99) || celula[i - 1].alfPigD < 0.7))
            bif = 1;
        else {
            if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta < 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 1].alfL) || celula[i - 1].alfPigD < 0.7))
                bif = 1;
            else if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && uls0 > 0 && ((celula[i - 1].alfPigD <= 1.0 * celula[i - 1].alfL) || celula[i - 1].alfPigD < 0.7))
                bif = 1;
        }
    }

    if (ugs < 0 && celula[i].alf > (*vg1dSP).localtiny && celula[i].alf < 1. - (*vg1dSP).localtiny)
        bif = 1;

    if (bif == 1) {

        double c0;
        double ud;
        double alfmed;

        alfmed = celula[i - 1].alfPigD;
        if (ugs < 0)
            alfmed = celula[i].alfPigE;
        c0 = 1.2;
        double dmed = celula[i].duto.a;
        if (celula[i].MC >= 0)
            dmed = celula[i].dutoL.a;
        double sinal = 1.;
        if (celula[i].duto.teta < 0.)
            sinal = -1.;
        ud = sinal * 0.32 * sqrt(9.82 * dmed);
        if (fabs(rgR) / rlR > 0.9) {
            c0 = 1.;
            ud = 0.;
        }
        CalcC0UdBuf(i, c0, ud);
        if (arq.escorregamentoCelulaContorno == 0) {
            c0 = 1.;
            ud = 0.;
        }
        double num = (1. - alfmed * c0);
        double den = 1 + c0 * alfmed * ((rg / rl) - 1.);
        celula[i].term1 = num / den;
        celula[i].term2 = (-amed * alfmed * rg * ud) / den;
        if (celula[i - 1].acsr.tipo == 5 && celula[i - 1].acsr.chk.AreaGarg <= (1e-3 + arq.master1.razareaativ) * celula[i - 1].duto.area) {
            celula[i].term1 = 0.;
            celula[i].term2 = 0.;
        }
    }
    celula[i].term1L = celula[i - 1].term1;
    celula[i].term2L = celula[i - 1].term2;
    celula[i - 1].term1R = celula[i].term1;
    celula[i - 1].term2R = celula[i].term2;
}

void SProd::renovatermColIni() {

    if (titE < 1) {
        int para;
        para = 0;
    }

    int i = 0;

    double razdx = 0.5;
    double pmed;
    pmed = presE;

    double tmed;
    if (celula[0].MliqiniBuf < 0.)
        tmed = celula[i].temp;
    else
        tmed = tempE;

    double betI;
    double viscl1;
    double tensup1;
    double rg = celula[0].flui.MasEspGas(presE, tempE);
    double rl = celula[0].flui.MasEspLiq(presE, tempE);
    double rcis = celula[0].fluicol.MasEspFlu(presE, tempE);

    double rlmist = betaE * rcis + (1 - betaE) * rl;
    alfE = (-titE * rlmist / (titE * rg - rg - titE * rlmist)) / (celula[0].c0);

    if ((celula[i].MCBuf - celula[0].MliqiniBuf) * 0 + 1 * celula[0].MliqiniBuf < 0.) { // duvidabeta
        betI = celula[i].betPigE;
        rl = (1 - betI) * celula[i].flui.MasEspLiq(pmed, tmed) + betI * celula[i].fluicol.MasEspFlu(pmed, tmed);
        viscl1 = (1 - betI) * celula[i].flui.ViscOleo(pmed, tmed) + betI * celula[i].fluicol.VisFlu(pmed, tmed);
        tensup1 = (1 - betI) * celula[i].flui.TensSuper(pmed, tmed) + betI * celula[i].fluicol.TensSuper(pmed, tmed);
    } else {
        betI = betaE;
        rl = (1 - betI) * (*celula[i].fluiL).MasEspLiq(pmed, tmed) + betI * celula[i].fluicol.MasEspFlu(pmed, tmed);
        viscl1 = (1 - betI) * (*celula[i].fluiL).ViscOleo(pmed, tmed) + betI * celula[i].fluicol.VisFlu(pmed, tmed);
        tensup1 = (1 - betI) * (*celula[i].fluiL).TensSuper(pmed, tmed) + betI * celula[i].fluicol.TensSuper(pmed, tmed);
    }
    double viscg1;
    double amed;
    double hns;
    if (celula[i].MCBuf - celula[0].MliqiniBuf >= 0) {
        amed = celula[i].dutoL.area;
        rg = (*celula[i].fluiL).MasEspGas(pmed, tmed);
        viscg1 = (*celula[i].fluiL).ViscGas(pmed, tmed);
        hns = 1. - alfE;
    } else {
        rg = celula[i].flui.MasEspGas(pmed, tmed);
        viscg1 = celula[i].flui.ViscGas(pmed, tmed);
        amed = celula[i].duto.area;
        hns = 1. - celula[i].alf;
    }
    double ugs = (celula[i].MCBuf - celula[i].MliqiniBuf) / (rg * amed);
    double uls = celula[i].MliqiniBuf / (rl * amed);
    double dia1 = celula[i].duto.a;
    if (ugs >= 0)
        dia1 = celula[i].duto.a;

    double rmed = hns * rl + (1 - hns) * rg;
    double visc = (hns * viscl1 + (1 - hns) * viscg1) / pow(10., 3.);
    double ang = celula[i].duto.teta;
    double sinal = 1.;
    if (ang < 0.)
        sinal = -1.;
    double xc0 = 2.;
    double xud = sinal * 0.0246 * cos(ang) + 1.606 * pow(9.82 * tensup1 * (rl - rg) / (rl * rl), 0.25) * sin(ang);

    double amedR = celula[i].dutoR.area;
    double razdxR = celula[i].dxR / (celula[i].dxR + celula[i].dx);
    double pmedR = celula[i].presRBuf * razdxR + celula[i].presBuf * (1. - razdxR);
    double tmedR = celula[i].temp * razdxR + celula[i].tempR * (1. - razdxR);
    double betIR = celula[i].betPigD;
    if (celula[i].QLR < 0.) // testeBeta
        betIR = celula[i + 1].betPigE;

    double rgR = celula[i].flui.MasEspGas(pmedR, tmedR);
    double rlR = (1 - betIR) * celula[i].flui.MasEspLiq(pmedR, tmedR) + betIR * celula[i].fluicol.MasEspFlu(pmedR, tmedR);
    double ugs1 = (celula[i].MRBuf - celula[i].MliqiniRBuf) / (rgR * amedR);
    double uls1 = (celula[i].MliqiniRBuf) / (rlR * amedR);

    int bif = 1;

    if ((*vg1dSP).lixo5 > 29900) {
        int para;
        para = 0;
    }

    if (alfE < (*vg1dSP).localtiny && celula[i].alfPigE <= (*vg1dSP).localtiny && celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5) {
        celula[i].term1 = 1.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
    } else if (alfE >= (1. - (*vg1dSP).localtiny) && celula[i].alfPigE >= (1. - (*vg1dSP).localtiny) && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 0.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
    } else if (ugs >= 0 && alfE <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 1.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
        if (fabs(ugs) <= 1e-15 && celula[i].alfPigE > (1. - (*vg1dSP).localtiny) && uls < 0 && celula[i].duto.teta > 0) {
            celula[i].term1 = 0.;
            celula[i].term2 = 0.;
            bif = 0;
        }
        if (fabs(ugs) <= 1e-15 && celula[i].alfPigE > (1. - 1 * (*vg1dSP).localtiny) && celula[i].duto.teta < 0 && uls > 0) {
            celula[i].term1 = 0.;
            celula[i].term2 = 0.;
            bif = 1;
        }
        if (fabs(ugs) <= 1e-15 && celula[i].alf >= alfE && uls < 0) {
            bif = 1;
        }
    } else if (ugs <= 0 && celula[i].alfPigE <= (*vg1dSP).localtiny && (celula[i].fontemassGL <= (*vg1dSP).localtiny * 1e-5 && celula[i].fontemassGR <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 1.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
        if (fabs(ugs) <= 1e-15 && alfE > (1. - (*vg1dSP).localtiny) && uls > 0) {
            celula[i].term1 = 0.;
            celula[i].term2 = 0.;
            bif = 0;
        }
        if (fabs(ugs) <= 1e-15 && alfE > (*vg1dSP).localtiny && uls > 0) {
            bif = 1;
        }
    } else if (uls >= 0 && alfE >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 0.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
        if (fabs(uls) <= 1e-15 && celula[i].alfPigE < (*vg1dSP).localtiny && uls1 < 0) {
            celula[i].term1 = 1.;
            celula[i].term2 = 0.;
            bif = 0;
        } else if (fabs(uls1) < (*vg1dSP).localtiny * 1e-5) {
            if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].alfPigE < (*vg1dSP).localtiny && celula[i].fontemassGR >= (*vg1dSP).localtiny * 1e-5) {
                celula[i].term1 = 1.;
                celula[i].term2 = 0.;
                bif = 0;
            }
        } else if (fabs(uls) < 1e-15 && uls1 < 0 && ((celula[i].alfPigE <= (1 - 1 * (*vg1dSP).localtiny + .0 * celula[i].alfPigER) && celula[i].alfPigER < 1 - 1 * (*vg1dSP).localtiny) || celula[i].alfPigE <= 0.7))
            bif = 1;

    } else if (uls <= 0 && celula[i].alfPigE >= 1. - (*vg1dSP).localtiny && ((celula[i].fontemassLL + celula[i].fontemassCL) <= (*vg1dSP).localtiny * 1e-5 && (celula[i].fontemassLR + celula[i].fontemassCR) <= (*vg1dSP).localtiny * 1e-5)) {
        celula[i].term1 = 0.;
        celula[i].term2 = 0.;
        bif = 0;
        celula[i].c0 = 1 + 0 * xc0;
        celula[i].ud = 0 * xud;
        celula[i].arranjo = 0;
        if (fabs(uls) <= (*vg1dSP).localtiny * 1e-5 && alfE < (*vg1dSP).localtiny && uls1 < 0) {
            celula[i].term1 = 1.;
            celula[i].term2 = 0.;
            bif = 0;
        }

        if (fabs(uls1) < (*vg1dSP).localtiny * 1e-5) {
            if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && alfE < (*vg1dSP).localtiny) {
                celula[i].term1 = 1.;
                celula[i].term2 = 0.;
                bif = 0;
            }
        } else {
            if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta < 0.95 * M_PI / 2. && ugs < 0 && (alfE < 0.7))
                bif = 1;
            else if (fabs(uls) < (*vg1dSP).localtiny * 1e-5 && celula[i].duto.teta >= 0.95 * M_PI / 2. && ugs < 0 && (alfE < 0.7))
                bif = 1;
        }
    }
    if (uls > 0 && fabs(ugs) <= (*vg1dSP).localtiny * 1e-5 && alfE > (*vg1dSP).localtiny && alfE < 1 - (*vg1dSP).localtiny)
        bif = 1;
    if (ugs > 0 && fabs(uls) <= (*vg1dSP).localtiny * 1e-5 && alfE > (*vg1dSP).localtiny && alfE < 1 - (*vg1dSP).localtiny)
        bif = 1;

    if (bif == 1) {

        double c0;
        double ud;
        double alfmed;

        alfmed = alfE;
        if (ugs < 0)
            alfmed = celula[i].alfPigE;
        c0 = 1.2;
        double dmed = celula[i].duto.a;
        if (celula[i].MC >= 0)
            dmed = celula[i].dutoL.a;
        double sinal = 1.;
        if (celula[i].duto.teta < 0.)
            sinal = -1.;
        ud = sinal * 0.32 * sqrt(9.82 * dmed);
        if (fabs(rgR) / rlR > 0.9) {
            c0 = 1.;
            ud = 0.;
        }
        CalcC0UdIniBuf(i, c0, ud);

        double num = (1. - alfmed * c0);
        double den = 1 + c0 * alfmed * ((rg / rl) - 1.);
        celula[i].term1 = num / den;
        celula[i].term2 = (-amed * alfmed * rg * ud) / den;
    }
    celula[1].term1L = celula[i].term1;
    celula[1].term2L = celula[i].term2;
}

void SProd::calcCCpres(double titRev, double alfRev, double betRev) {

    double tESup = celula[ncel].temp;
    double alfSup = celula[ncel].alf;
    double betSup = celula[ncel].bet;

    double masentrada = celula[ncel - 1].MR;
    double massgas = celula[ncel - 1].MR - celula[ncel - 1].MliqiniR;
    double maxSup = 0.;
    double chokemas = 0;

    double rholp = celula[ncel].rpC;
    double rholc = celula[ncel].rcC;
    double rholmix = (1 - betSup) * rholp + betSup * rholc;
    double romix = alfSup * celula[ncel].rgC + (1 - alfSup) * rholmix;

    double tit;
    if ((massgas >= 0 && celula[ncel - 1].MliqiniR <= 0) || (massgas < 0 && celula[ncel - 1].MliqiniR == 0))
        tit = 1.;
    else if (massgas <= 0 && celula[ncel - 1].MliqiniR > 0)
        tit = 0.;
    else if (masentrada < 0) {
        tit = 1.;
    } else if (fabs(masentrada) < 1e-15)
        tit = alfSup * celula[ncel].flui.MasEspGas(celula[ncel].pres, celula[ncel].temp) / romix;
    else
        tit = fabs(massgas / masentrada);
    if (tit > 1)
        tit = 1;
    if (presfim < pGSup) {
        tit = 1.;
    }

    if (tit == 0 && alfSup > 0.05)
        tit = alfSup * celula[ncel].flui.MasEspGas(celula[ncel].pres, celula[ncel].temp) / romix;

    romix = tit * (1. / celula[ncel].rgC) + (1 - tit) * (1. / rholmix);
    romix = 1 / romix;

    double masChk;

    double sinal = 1.;
    double pmon = presfim;

    double ypres = pGSup / presfim;
    if (chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area && ypres < 1.) {
        double cplM = (1. - betSup) * celula[ncel].flui.CalorLiq(presfim, tESup) -
                      betSup * celula[ncel].fluicol.CalorLiq(presfim, tESup);
        double jtlM = (1. - betSup) * celula[ncel].flui.JTL(presfim, tESup) - betSup / rholc;
        double cpg = celula[ncel].flui.CalorGas(presfim, tESup);
        double jtgM = celula[ncel].flui.JTG(presfim, tESup);
        arq.valTempChokeJus = tESup + ((1. - tit) * jtlM / cplM + tit * jtgM / cpg) * (pGSup - presfim) * 98066.52;
    }
    if (ypres > 1.) {
        if (arq.chkv == 0)
            sinal = -1.;
        else
            sinal = 0.;
        tit = 1.;
        pmon = pGSup;
        ypres = 1. / ypres;
    }

    masChk = chokeSup.vazmassSachd(ypres, pmon, tESup, alfSup, betSup, tit, celula[ncel - 1].flui,
                                   celula[ncel - 1].fluicol);
    maxSup = chokeSup.vazmaxSachd(pmon, tESup, alfSup, betSup, tit, celula[ncel - 1].flui, celula[ncel - 1].fluicol);

    int fluxcri = 1;
    if (tit <= 0.01 || fabs(ypres) > fabs(chokeSup.razpres)) {
        fluxcri = 0;
        maxSup = masChk;
    }
    if (chokeSup.AreaGarg < (1e-3) * celula[ncel - 1].duto.area)
        maxSup = 0.;

    double sinal2 = 1.;
    pmon = presfim * 1.0001;
    ypres = pGSup / pmon;
    if (ypres > 1.) {
        sinal2 = -1.;
        tit = 1;
        pmon = pGSup;
        ypres = 1. / ypres;
        if (arq.chkv == 0)
            sinal2 = -1.;
        else
            sinal2 = 0.;
    }

    double masChk2 = chokeSup.vazmassSachd(ypres, pmon, tESup, alfSup, betSup, tit, celula[ncel - 1].flui,
                                           celula[ncel - 1].fluicol);
    double maxSup2 = chokeSup.vazmaxSachd(pmon, tESup, alfSup, betSup, tit, celula[ncel - 1].flui,
                                          celula[ncel - 1].fluicol);

    fluxcri = 1;
    if (tit <= 0.01 || fabs(ypres) > fabs(chokeSup.razpres)) {
        fluxcri = 0;
        maxSup2 = masChk2;
    }
    if (chokeSup.AreaGarg < (1e-3) * celula[ncel - 1].duto.area)
        maxSup2 = 0.;
    double dmaxsup = (sinal2 * maxSup2 - sinal * maxSup) / (presfim * 0.0001);

    double masliq;
    double masgas;
    int abertoini = aberto;
    double delp;
    if (chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area)
        delp = (0.5 / 98066.5) * (1 / romix) * (1 / (chokeSup.AreaGarg * chokeSup.AreaGarg * chokeSup.cdchk * chokeSup.cdchk)) * masentrada * masentrada;
    else
        delp = 0;

    int masChkSup0 = masChkSup;
    mudaModoChk = 0;

    double difdelp = presfim - pGSup;
    if (((tit < 1e-7 && chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area) ||
         (tit < 0.01 && chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area &&
          fabs(difdelp) / delp < 1.2 && fabs(difdelp) / delp > 0.8 &&
          ((fabs(maxSup) > 0 && fabs((masentrada - maxSup) / maxSup) < 0.2) ||
           (fabs(masentrada) > 0 && fabs((masentrada - maxSup) / masentrada) < 0.2))))) {
        aberto = 1;
        double sens = 1.;
        if (masentrada < 0. && celula[ncel - 1].MliqiniR <= 0)
            sens = 0.;
        presfim = pGSup + sens * delp;
        masChkSup = 0;
        if (masChkSup != masChkSup0)
            mudaModoChk = 1;

    } else {
        if (chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area &&
            ((((*vg1dSP).lixo5 - 2 * arq.dtmax > tMedMov || arq.perm == 2) &&
              fabs(presMedMov - pGSup) / presMedMov < 0.05 && fabs(jMedMov) < 0.5) ||
             (((*vg1dSP).lixo5 - 2 * arq.dtmax > tMedMov || arq.perm == 2) &&
              fabs(presMedMov - pGSup) < (0.05 * pGSup) && fabs(jMedMov) < 5. && delp < 0.01 * pGSup) ||
             (((*vg1dSP).lixo5 - 2 * arq.dtmax > tMedMov || arq.perm == 2) && (pGSup - presMedMov) / presMedMov > 0.001 && arq.chkv == 0) || (fabs(delp) < 0.1 && (presfim - pGSup) / presfim < 0.05 && arq.chkv == 0))) {
            aberto = 1;
            if (abertoini != aberto)
                tempoaberto = 1;
        } else {
            aberto = 0;
            if (tempoaberto > 60)
                tempoaberto = 0;
        }
        if ((((tit > -0.01 && celula[ncel].alf > -0.01) || chokeSup.AreaGarg < (1e-3) * celula[ncel - 1].duto.area) && chokeSup.AreaGarg < 0.6 * celula[ncel - 1].duto.area && (aberto == 0 && (tempoaberto == 0 || tempoaberto > 60)))) {

            aberto = 0;
            tempoaberto = 0;
            masliq = sinal * maxSup * (1. - tit);
            masgas = sinal * maxSup * tit;

            celula[ncel].DmasschokeG = -1 * (1. - tit) * dmaxsup;
            celula[ncel].DmasschokeL = -1 * tit * ((1 - betSup) * rholp / rholmix) * dmaxsup;
            celula[ncel].DmasschokeC = -1 * tit * (betSup * rholc / rholmix) * dmaxsup;

            masChkSup = 1;
            if (masChkSup != masChkSup0)
                mudaModoChk = 1;
            celula[ncel].fontemassLR = -masliq * (1 - betSup) * rholp / rholmix;
            celula[ncel].fontemassCR = -masliq * betSup * rholc / rholmix;
            celula[ncel].fontemassGR = -masgas;

        } else {
            if (tempoaberto != 0)
                tempoaberto++;
            if (chokeSup.AreaGarg >= 0.601 * celula[ncel - 1].duto.area)
                presfim = pGSup;
            else {

                aberto = 1;
                masChkSup = 0;
                if (masChkSup != masChkSup0)
                    mudaModoChk = 1;
                presfim = pGSup;
            }
            aberto = 1;
            masChkSup = 0;
            if (masChkSup != masChkSup0)
                mudaModoChk = 1;
        }
    }

    if (masChkSup == 0 && (*vg1dSP).chaverede == 1) {
        double betloc;
        if ((celula[ncel - 1].MR - celula[ncel - 1].MliqiniR) * 0 + 1 * celula[ncel - 1].MliqiniR > 0.)
            betloc = celula[ncel - 1].bet; // testeBeta//duvidabeta
        else
            betloc = betRev;

        if (arq.chkv == 0 || celula[ncel - 1].MR > 0.)
            sinal = 1.;
        else
            sinal = 0.;

        celula[ncel].fontemassCR = -sinal * celula[ncel - 1].QLR * (betloc)*celula[ncel - 1].rcC;
        celula[ncel].fontemassLR = -sinal * (celula[ncel - 1].MliqiniR + celula[ncel].fontemassCR);
        celula[ncel].fontemassGR = -sinal * (celula[ncel - 1].MR - celula[ncel - 1].MliqiniR);
    }
}

void SProd::calcCCBuffer(double titRev, double alfRev, double betRev) {

    double tESup = celula[ncel].temp;
    double alfSup = celula[ncel].alf;
    double betSup = celula[ncel].bet;

    double masentrada = celula[ncel - 1].MRBuf;
    double massgas = celula[ncel - 1].MRBuf - celula[ncel - 1].MliqiniRBuf;
    double maxSup = 0.;

    double rholp = celula[ncel].flui.MasEspLiq(celula[ncel].presBuf, celula[ncel].temp);
    double rholc = celula[ncel].fluicol.MasEspFlu(celula[ncel].presBuf, celula[ncel].temp);
    double rholmix = (1 - betSup) * rholp + betSup * rholc;
    double romix = alfSup * celula[ncel].flui.MasEspGas(celula[ncel].presBuf, celula[ncel].temp) + (1 - alfSup) * rholmix;

    double tit;
    if (massgas > 0 && celula[ncel - 1].MliqiniRBuf < 0)
        tit = 1.;
    else if (massgas <= 0 && celula[ncel - 1].MliqiniRBuf >= 0)
        tit = 0.;
    else if (masentrada < 0) {
        if ((*vg1dSP).chaverede == 0 || noextremo == 1)
            tit = 1.;
        else {
            tit = titRev;
            alfSup = alfRev;
            betSup = betRev;
        }
    } else if (fabs(masentrada) < 1e-15)
        tit = alfSup * celula[ncel].flui.MasEspGas(celula[ncel].presBuf, celula[ncel].temp) / romix;
    else
        tit = fabs(massgas / masentrada);
    if (tit > 1)
        tit = 1;
    if (celula[ncel].presBuf < pGSup) {
        if ((*vg1dSP).chaverede == 0 || noextremo == 1)
            tit = 1.;
        else {
            tit = titRev;
            alfSup = alfRev;
            betSup = betRev;
        }
    }

    if (tit == 0 && alfSup > 0.05)
        tit = alfSup * celula[ncel].flui.MasEspGas(celula[ncel].presBuf, celula[ncel].temp) / romix;

    romix = tit * (1. / celula[ncel].rgC) + (1 - tit) * (1. / rholmix);
    romix = 1 / romix;

    double masChk;

    double sinal = 1.;
    double pmon = celula[ncel].presBuf;

    double ypres = pGSup / celula[ncel].presBuf;
    if (ypres > 1.) {
        if (arq.chkv == 0)
            sinal = -1.;
        else
            sinal = 0.;
        if ((*vg1dSP).chaverede == 0 || noextremo == 1)
            tit = 1.;
        else {
            tit = titRev;
            alfSup = alfRev;
            betSup = betRev;
        }
        pmon = pGSup;
        ypres = 1. / ypres;
    }

    masChk = chokeSup.vazmassSachd(ypres, pmon, tESup, alfSup, betSup, tit, celula[ncel - 1].flui,
                                   celula[ncel - 1].fluicol);
    maxSup = chokeSup.vazmaxSachd(pmon, tESup, alfSup, betSup, tit, celula[ncel - 1].flui,
                                  celula[ncel - 1].fluicol);

    int fluxcri = 1;
    if (tit <= 0.01 || fabs(ypres) > fabs(chokeSup.razpres)) {
        fluxcri = 0;
        maxSup = masChk;
    }
    if (chokeSup.AreaGarg < (1e-3) * celula[ncel - 1].duto.area)
        maxSup = 0.;

    double masliq;
    double masgas;
    int abertoini = aberto;

    double delp;
    if (chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area)
        delp = (0.5 / 98066.5) * (1 / romix) *
               (1 / (chokeSup.AreaGarg * chokeSup.AreaGarg * chokeSup.cdchk * chokeSup.cdchk)) * masentrada * masentrada;
    else
        delp = 0.;

    double difdelp = fabs(fabs(celula[ncel].presBuf - pGSup) - delp);
    if (((tit < 1e-7 && chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area) ||
         (tit < 0.01 && chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area &&
          difdelp / delp < 0.2 &&
          ((fabs(maxSup) > 0 && fabs((masentrada - maxSup) / maxSup) < 0.2) ||
           (fabs(masentrada) > 0 && fabs((masentrada - maxSup) / masentrada) < 0.2))))) {
        aberto = 1;
        if (arq.chkv == 0 || celula[ncel - 1].MRBuf > 0.)
            sinal = 1.;
        else
            sinal = 0.;
        double betloc;
        if ((celula[ncel - 1].MRBuf - celula[ncel - 1].MliqiniRBuf) * 0 + 1 * celula[ncel - 1].MliqiniRBuf > 0.)
            betloc = celula[ncel - 1].bet; // testeBeta//duvidabeta
        else
            betloc = betRev;
        double rhomistBuf = betloc *
                                celula[ncel - 1].fluicol.MasEspFlu(celula[ncel].presBuf, celula[ncel].temp) +
                            (1. - betloc) * celula[ncel - 1].flui.MasEspLiq(celula[ncel].presBuf, celula[ncel].temp);
        double QLbuf = celula[ncel - 1].MliqiniRBuf / rhomistBuf;

        fontemassCRBuf = -sinal * QLbuf * (betloc)*celula[ncel - 1].rcC;
        fontemassPRBuf = -sinal * (celula[ncel - 1].MliqiniRBuf + fontemassCRBuf);
        fontemassGRBuf = -sinal * (celula[ncel - 1].MRBuf - celula[ncel - 1].MliqiniRBuf);
    } else {
        if (chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area &&
            ((((*vg1dSP).lixo5 - 2 * arq.dtmax > tMedMov || arq.perm == 2) && fabs(presMedMov - pGSup) / presMedMov < 0.05 && fabs(jMedMov) < 0.5) || (((*vg1dSP).lixo5 - 2 * arq.dtmax > tMedMov || arq.perm == 2) && fabs(presMedMov - pGSup) < (0.05 * pGSup) && fabs(jMedMov) < 5. && delp < 0.01 * pGSup) || (((*vg1dSP).lixo5 - 2 * arq.dtmax > tMedMov || arq.perm == 2) && (pGSup - presMedMov) > 0.01 && arq.chkv == 0) || (fabs(delp) < 0.1 && (celula[ncel].presBuf - pGSup) / celula[ncel].presBuf < 0.05 && arq.chkv == 0))) {
        } else {
            abertoini = aberto;
        }
        if (((tit > -0.01 && celula[ncel].alf > -0.01) || chokeSup.AreaGarg < (1e-3) * celula[ncel - 1].duto.area) &&
            chokeSup.AreaGarg < 0.6 * celula[ncel - 1].duto.area && (aberto == 0 && (tempoaberto == 0 || tempoaberto > 60))) {

            masliq = sinal * maxSup * (1. - tit);
            masgas = sinal * maxSup * tit;
            fontemassPRBuf = -masliq * (1 - betSup) * rholp / rholmix;
            fontemassCRBuf = -masliq * betSup * rholc / rholmix;
            fontemassGRBuf = -masgas;
        }
        if (masChkSup == 1 && (*vg1dSP).chaverede == 1) {
            celula[ncel].fontemassCR = fontemassCRBuf;
            celula[ncel].fontemassLR = fontemassPRBuf;
            celula[ncel].fontemassGR = fontemassGRBuf;

            celula[ncel].DmasschokeG = 0.;
            celula[ncel].DmasschokeL = 0.;
            celula[ncel].DmasschokeC = 0.;
        } else if (masChkSup == 0 && (*vg1dSP).chaverede == 1) {
            if (arq.chkv == 0 || celula[ncel - 1].MRBuf > 0.)
                sinal = 1.;
            else
                sinal = 0.;
            double betloc;
            if ((celula[ncel - 1].MRBuf - celula[ncel - 1].MliqiniRBuf) * 0.0 + 1.0 * celula[ncel - 1].MliqiniRBuf > 0.)
                betloc = celula[ncel - 1].bet; // testeBeta//duvidabeta
            else
                betloc = betRev;
            double rhomistBuf = betloc *
                                    celula[ncel - 1].fluicol.MasEspFlu(celula[ncel].presBuf, celula[ncel].temp) +
                                (1. - betloc) * celula[ncel - 1].flui.MasEspLiq(celula[ncel].presBuf, celula[ncel].temp);
            double QLbuf = celula[ncel - 1].MliqiniRBuf / rhomistBuf;

            fontemassCRBuf = -sinal * QLbuf * (betloc)*celula[ncel - 1].rcC;
            fontemassPRBuf = -sinal * (celula[ncel - 1].MliqiniRBuf + fontemassCRBuf);
            fontemassGRBuf = -sinal * (celula[ncel - 1].MRBuf - celula[ncel - 1].MliqiniRBuf);
        }
    }
}

void SProd::determinaDTExpli() {

    for (int i = 0; i <= ncel; i++) {
        double velAux = celula[i].termAdSomVel();
        double som = celula[i].somVel();
        double velpropag1 = velAux + som;
        double velpropag2 = fabs(velAux - som);
        double velMax = velpropag1;
        if (velpropag2 > velpropag1)
            velMax = velpropag2;
        double dtaux;
        double alfteste = celula[i].alf;
        dtaux = celula[i].dx / velMax;
        if (dtaux < dt)
            dt = dtaux;
    }
    for (int i = 0; i <= ncel; i++) {
        celula[i].dt = dt;
        celula[i].dt1 = dt;
        celula[i].dt2 = dt;
        celula[i].dtPig = dt;
    }
}

void SProd::determinaDT(int vexpli) {

    if ((*vg1dSP).lixo5 < (*vg1dSP).localtiny && (*vg1dSP).chaverede == 0) {
        arq.imprimeProfile(celula, flut, (*vg1dSP).lixo5, indTramo);
        if (arq.lingas > 0 && arq.nvalvgas > 0)
            arq.imprimeProfileG(celulaG, flutG, (*vg1dSP).lixo5, indTramo);
        arq.imprimeProfileTrans(celula, ncelperftransp, (*vg1dSP).lixo5, indTramo);
    }
    dt = arq.dtmax;

    int parada = 0;
    for (int i = 1; i < ncel; i++) {
        if (celula[i].acsr.tipo == 5 && celula[i].acsr.chk.AreaGarg <= 1e-15 * celula[i].acsr.chk.AreaTub)
            parada = 1;
        else if (chokeSup.AreaGarg <= 1.e-15 * chokeSup.AreaTub)
            parada = 1;
    }
    if (parada == 1) {
        Vcr<int> oscila(ncel, 0);
        for (int i = 0; i <= ncel; i++) {
            if (i > 0 && i < ncel) {
                if (fabs(celula[i - 1].alf - celula[i + 1].alf) < fabs(celula[i].alf - celula[i - 1].alf)) {
                    double area = celula[i].duto.area;
                    double vLiqTest = fabs(celula[i].QL / (area));
                    double vGasTest = fabs(celula[i].QG / (area));
                    if ((vLiqTest + vGasTest) > 0.1)
                        oscila[i] = 1;
                }
            }
        }
        int alarmOscila = 0;
        for (int i = 1; i <= ncel - 4; i++) {
            int kontaOsc = 0;
            int multOsc = 0;
            while (kontaOsc < 3) {
                multOsc += oscila[i + kontaOsc];
                kontaOsc++;
            }
            if (multOsc == 3)
                alarmOscila = 1;
        }

        if (alarmOscila == 1 && arq.desligaPenalizaDT == 0)
            dt /= 10.;
    }

    for (int i = 0; i <= ncel; i++) {
        if (celula[i].acsr.tipo == 15) {
            celula[i].acsr.radialPoro.defineDT(0);
            if (celula[i].acsr.radialPoro.dt < dt)
                dt = celula[i].acsr.radialPoro.dt;
        }
        if (celula[i].acsr.tipo == 16) {
            celula[i].acsr.poroso2D.defineDT(0);
            if (celula[i].acsr.poroso2D.dt < dt)
                dt = celula[i].acsr.poroso2D.dt;
        }
    }

    if (vexpli == 1) {
        determinaDTExpli();
    } else {

        int multChoke = 1.;
        if (celula[ncel - 1].alf < 0.9)
            multChoke = 1;
        double mgas = celula[ncel].MC - celula[ncel].Mliqini;
        double mgas0 = celula[ncel].MCini - celula[ncel].Mliqini0;
        if (arq.RelaxaDTChoke == 0 &&
            (((*vg1dSP).lixo5 > 1e-15 && masChkSup == 1 && chokeSup.AreaGarg < 0.6 * celula[ncel - 1].duto.area &&
              chokeSup.AreaGarg > 1e-3 * celula[ncel - 1].duto.area &&
              ((celula[ncel].Mliqini >= 0 && celula[ncel].Mliqini0 < 0 && mgas > 0) || (celula[ncel].Mliqini >= 0 && mgas < 0 && mgas0 > 0))) ||
             kontaGolfada < multChoke * 200)) {

            if (kontaGolfada > multChoke * 200) {
                kontaGolfada = 0;
            }
            if (((celula[ncel].Mliqini >= 0 && celula[ncel].Mliqini0 < 0 && mgas > 0) ||
                 (celula[ncel].Mliqini >= 0 && mgas < 0 && mgas0 > 0)) &&
                kontaGolfada > multChoke * 100)
                kontaGolfada = multChoke * 100;
            double progres = 1.;
            if (kontaGolfada > multChoke * 100) {
                progres = kontaGolfada - multChoke * 100;
            }
            dt *= (progres / (multChoke * 100.));
            kontaGolfada++;
        }
        if (chokeSup.AreaGarg >= 0.6 * celula[ncel - 1].duto.area)
            aberto = 1;
        if (fabs(presfim - pGSup) / presfim < 0.05 && aberto == 0 && dt > 1. && chokeSup.AreaGarg < 0.6 * celula[ncel - 1].duto.area &&
            chokeSup.AreaGarg > 1e-15 * celula[ncel - 1].duto.area)
            dt =
                1.;
        for (int i = 0; i < arq.eventoabre; i++) {
            if ((*vg1dSP).lixo5 > arq.Tevento[i] - arq.dtmax && (*vg1dSP).lixo5 < arq.Tevento[i] + 30) {
                if (dt > menorDx / 100.) {
                    dt = menorDx / 100.;
                }
            }
        }
        mult = 0.8;
        if ((((*vg1dSP).lixo5 - 2 * arq.dtmax) > tMedMov && (fabs(presMedMov - presfim) / presfim > 0.4)) || (presfim < pGSup && masChkSup == 1 && chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area) || (celula[ncel].alf <= 0.1 && chokeSup.AreaGarg < 0.6 * celula[ncel - 1].duto.area && chokeSup.AreaGarg > 1e-3 * celula[ncel - 1].duto.area && aberto == 0)) {
            double denominador = 10.;
            double progres = 0.95;

            if ((presfim < pGSup && masChkSup == 1)) {
                denominador = 100.;
                progres = 0.5;
                mult = pow(progres, 10. * fabs(pGSup - presfim) / presfim) * mult;
            } else if ((celula[ncel].alf <= 0.1 && chokeSup.AreaGarg < 0.6 * celula[ncel - 1].duto.area)) {
                denominador = 1000.;
                progres = 0.5;
                mult = (celula[ncel].alf + 1e-5) * mult;
            } else {
                mult = pow(progres, 10 * fabs(presMedMov - presfim) / presfim) * mult;
            }
            if (arq.dtmax * mult < menorDx / denominador)
                mult = (menorDx / denominador) / arq.dtmax;
            if (mult > 0.8)
                mult = 0.8;
        } else {
            if (celula[ncel].alf >= 0.11 || chokeSup.AreaGarg >= 0.6 * celula[ncel - 1].duto.area ||
                chokeSup.AreaGarg <= 1e-3 * celula[ncel - 1].duto.area || aberto == 1) {
                mult = mult / 0.95;
                if (mult > 0.8)
                    mult = 0.8;
            }
        }

        for (int i = 0; i <= ncel; i++) {
            double jmix = 0.;
            double A1 = celula[i].duto.area;
            double dtaux;
            // celula[i].Mliqini
            double alfteste = celula[i].alf;
            if (i > 0 && (celula[i].MC - celula[i].Mliqini) > 0)
                alfteste = celula[i - 1].alf;
            if (alfteste > 1e-9)
                jmix += fabs(
                    (celula[i].MC - celula[i].Mliqini) / (A1 * celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp) * (0 + 1 * alfteste)));
            if (fabs(celula[i].VTemper) > jmix)
                jmix = fabs(celula[i].VTemper);

            double jmixL = 0.;
            if (alfteste < 1 - 1e-5)
                jmixL = fabs(
                    celula[i].Mliqini / (A1 * ((1. - celula[i].bet) * celula[i].rpC + celula[i].bet * celula[i].rcC) * (1. - 0 * alfteste)));
            if (fabs(jmixL) > jmix)
                jmix = fabs(jmixL);
            if (fabs(jmix) > 1e-5)
                dtaux = 1.0 * celula[i].dx / jmix;
            else
                dtaux = dt;
            if (dtaux < dt)
                dt = dtaux;
        }
        if (arq.lingas == 1) {
            for (int i = 0; i <= ncelGas; i++) {
                double dtaux;
                double jmix = fabs(celulaG[i].VGasR / celulaG[i].u1L);
                if (fabs(jmix) > 1e-5)
                    dtaux = celulaG[i].dx0 / jmix;
                else
                    dtaux = dt;
                if (dtaux < dt)
                    dt = dtaux;
            }
        }

        if (restriDt == 1 && arq.desligaPenalizaDT == 0) {
            if (alteraTempo < 2)
                dt /= 10.;
            alteraTempo++;
            if (alteraTempo > 10)
                alteraTempo = 0;
        }

        dt = mult * dt;
        if (dt > arq.dtmax)
            dt = arq.dtmax;

        if (arq.evento.size() > indevento) {
            if ((*vg1dSP).lixo5 < arq.evento[indevento] && ((*vg1dSP).lixo5 + dt) > (arq.evento[indevento] + 0.1)) {
                dt = arq.evento[indevento] - (*vg1dSP).lixo5;
                indevento++;
            } else if ((*vg1dSP).lixo5 < arq.evento[indevento] && ((*vg1dSP).lixo5 + dt) >= (arq.evento[indevento]))
                indevento++;
        }

        EstadoMaster1 = 1;
        for (int i = 0; i < nfechaM1; i++) {
            if ((*vg1dSP).lixo5 > fechaM1[i]) {
                for (int j = 0; j < nabreM1; j++) {
                    if ((*vg1dSP).lixo5 < abreM1[j]) {
                        EstadoMaster1 = 0;
                        contaMaster1++;
                        if (contaMaster1 > 19)
                            contaMaster1 = 20;
                        if ((*vg1dSP).lixo5 >= abreM1[j] - dt)
                            contaMaster1 = 0;
                        break;
                    }
                }
            }
        }

        for (int i = 0; i <= ncel; i++) {
            celula[i].dt = dt;
            celula[i].dt1 = dt;
            celula[i].dt2 = dt;
            celula[i].dtPig = dt;
        }

        for (int i = 0; i <= ncel; i++) {
            if (celula[i].acsr.tipo == 15) {
                celula[i].acsr.radialPoro.dt = dt;
            } else if (celula[i].acsr.tipo == 16) {
                celula[i].acsr.poroso2D.dt = dt;
            }
        }
        // dtInter=dt;//alteracao2
    }
}

void SProd::atenuaDtMax() {

    dtCFL.push_back(dtauxCFL);
    dtSim.push_back(dtauxFinal);
    dtCFLTotal += dtauxCFL;
    dtSimTotal += dtauxFinal;
    kontarestriDt++;
    if (kSP > 10) {
        dtCFLTotal -= dtCFL.front();
        dtCFL.erase(dtCFL.begin());
        dtSimTotal -= dtSim.front();
        dtSim.erase(dtSim.begin());
        if (kontarestriDt > 10 && restriDt == 1) {
            restriDt = 0;
            kontarestriDt = 0;
        }
    }
    dtCFLMed = dtCFLTotal / 10;
    dtSimMed = dtSimTotal / 10.;
    if ((dtSimMed < dtCFLMed / 2 && kontarestriDt > 10)) {
        restriDt = 1;
        kontarestriDt = 0;
    }
}

void SProd::avaliaVariaDpDt(double razMast, double razMast0, int vexpli) {
    double dpdtRef = 2 * arq.taxaDespre;
    double modDpDt = 0.;
    double modDTDt = 0.;
    double dpdtMax = 0.;
    double dTdtMax = 0.;
    if (modeloCompleto == 1) {
        modeloCompleto = 0;
        int i = 0;
        while (i < ncel) {
            int nBloco = 10;
            if (ncel - i < nBloco)
                nBloco = ncel - i;
            for (int j = 0; j < nBloco; j++) {
                modDpDt += fabs(celula[i + j].pres - celula[i + j].presini) / (dt);
                modDTDt += fabs(celula[i + j].temp - celula[i + j].tempini) / (dt);
            }
            i += nBloco;
            modDpDt /= nBloco;
            modDTDt /= nBloco;
            if (modDpDt > dpdtMax)
                dpdtMax = modDpDt;
            if (modDTDt > dTdtMax)
                dTdtMax = modDTDt;
            if (modDpDt > dpdtRef)
                modeloCompleto = 1;
            modDpDt = 0.;
            modDTDt = 0.;
        }
        taxaDpMax.push_back(dpdtMax);
        taxaDTMax.push_back(dTdtMax);
        if (taxaDpMax.size() > 10)
            taxaDpMax.erase(taxaDpMax.begin());
        if (taxaDTMax.size() > 10)
            taxaDTMax.erase(taxaDTMax.begin());
        int nvec = taxaDpMax.size();
        int nvecT = taxaDTMax.size();
        DpMaxMed = 0.;
        DTMaxMed = 0.;
        for (int i = 0; i < nvec; i++)
            DpMaxMed += taxaDpMax[i];
        for (int i = 0; i < nvecT; i++)
            DTMaxMed += taxaDTMax[i];
        DpMaxMed /= nvec;
        DTMaxMed /= nvecT;
        if (DpMaxMed > dpdtRef)
            modeloCompleto = 1;
        if (chokeSup.AreaGarg / celula[ncel - 1].duto.area < 1e-3 &&
            (DpMaxMed > arq.taxaDespre / 10. || DTMaxMed > 0.001))
            modeloCompleto = 1;
        int linAberta = 1; // caso varias valvulas
        for (int i = 0; i <= arq.nvalv; i++)
            if (vRazMast1[i] <= 1e-3)
                linAberta = 0; // caso varias valvulas
        if ((linAberta == 1 && chokeSup.AreaGarg / celula[ncel - 1].duto.area > 1e-3))
            modeloCompleto = 1; // caso varias valvulas
    }
    for (int i = 0; i <= arq.nvalv; i++)
        if (vRazMast1[i] != vRazMast0[i])
            modeloCompleto = 0; // caso varias valvulas
    if ((DpMaxMed > 10 || DTMaxMed > 1) && modeloCompleto == 1 && vexpli == 0) {
        modeloCompleto = 0;
    }
}

void SProd::aberturaVal0() {
    int celpos = arq.master1.posic;
    vRazMast0[0] = celula[celpos].acsr.chk.AreaGarg / celula[celpos].duto.area;
    for (int i = 1; i <= arq.nvalv; i++) {
        celpos = arq.valv[i - 1].posicP;
        vRazMast0[i] = celula[celpos].acsr.chk.AreaGarg / celula[celpos].duto.area;
    }
}
void SProd::aberturaVal1() {
    int celpos = arq.master1.posic;
    vRazMast1[0] = celula[celpos].acsr.chk.AreaGarg / celula[celpos].duto.area;
    for (int i = 1; i <= arq.nvalv; i++) {
        celpos = arq.valv[i - 1].posicP;
        vRazMast1[i] = celula[celpos].acsr.chk.AreaGarg / celula[celpos].duto.area;
    }
}
void SProd::restringeDTporValv() {

    if (arq.ConContEntrada == 0 && (*vg1dSP).chaveRedeParalela == 0) {
        int celpos;
        double dtaux = celula[0].dt;
        if (reinicia == -1) {
            for (int i = 1; i <= ncel; i++)
                if (dtaux > celula[i].dt)
                    dtaux = celula[i].dt;
        }
        double dtvec[arq.nvalv + 1];
        for (int i = 0; i <= arq.nvalv; i++)
            dtvec[i] = celula[0].dt;
        for (int i = 0; i <= arq.nvalv; i++) {
            if (i == 0)
                celpos = arq.master1.posic;
            else
                celpos = arq.valv[i - 1].posicP;
            if (vRazMast1[i] < vRazMast0[i] && (vRazMast1[i] <= 1.1 * arq.master1.razareaativ && vRazMast1[i] >= 1e-3 * arq.master1.razareaativ)) {
                if (celula[celpos].alf < 0.05) {
                    if (celula[celpos].alf < 0.01)
                        momentoDesesp += 1.5;
                    else
                        momentoDesesp = 1;
                    if (momentoDesesp < 1.)
                        momentoDesesp = 1.;
                    celula[celpos].fontemassGL += momentoDesesp * 10000 * celula[celpos].flui.Deng * 1.225 / 86400;
                    celula[celpos - 1].fontemassGR = celula[celpos].fontemassGL;
                }
                if (celula[celpos].alf < 0.5)
                    vRazMastCrit[i] = 0.01;
            } else
                momentoDesesp = 0.;
            if (vRazMast1[i] < vRazMast0[i] && (vRazMast1[i] <= 1.1 * arq.master1.razareaativ && vRazMast1[i] >= vRazMastCrit[i] * arq.master1.razareaativ)) {
                double raz = 20.;
                if (celula[celpos].alf < 0.5)
                    raz = 40.;
                dtvec[i] = dtaux;
                if (dtvec[i] > 1)
                    dtvec[i] = 1.;
                if (celula[celpos].alf < 0.5)
                    dtvec[i] = 0.1 + 0.9 * (celula[celpos].alf) / 0.5;
                dtvec[i] /= raz;
                reinicia = -1;
            }
        }
        dtaux = dtvec[0];
        for (int i = 1; i <= arq.nvalv; i++)
            if (dtvec[i] < dtaux)
                dtaux = dtvec[i];
        celula[0].dt = dtaux;
    }
}

void SProd::solveLinGas() {
    if (arq.lingas > 0) {
        double tg;
        // dt,celula,ColunaAnulaIni,ColunaAnulaFim,
        ValvGasTrans();
        for (int i = 0; i < arq.nvalvgas; i++) {
            int posGLP = posicVGLP[i];
            int posGLG = posicVGLG[i];
            if (posGLG < celInter || (posGLG == celInter && celulaG[posGLG].razInter > 0.5)) {
                celula[posGLP].acsr.injg.QGas = celulaG[posGLG].massfonteCH * 86400. / celulaG[posGLG].flui.MasEspGas(1., 15.);
                celula[posGLP].acsr.injg.tipoflu = 0;
            } else {
                celula[posGLP].acsr.injg.QGas = celulaG[posGLG].massfonteCH;
                celula[posGLP].acsr.injg.tipoflu = 1;
            }
            if (posGLG < celInter || (posGLG == celInter && celulaG[posGLG].razInter > 0.5)) {
                if (chokeVGL[i].presEstag > chokeVGL[i].presGarg) {
                    tg = TempDescGL(i);
                } else
                    tg = celula[posGLP].temp;
                celula[posGLP].acsr.injg.temp = tg;
                if (celula[posGLP].acsr.injg.temp < -50)
                    celula[posGLP].acsr.injg.temp = -50;
            } else
                celula[posGLP].acsr.injg.temp = chokeVGL[i].tempEstag;
            celulaG[posGLG].pEstag = chokeVGL[i].presEstag;
            celulaG[posGLG].tEstag = chokeVGL[i].tempEstag;
            celulaG[posGLG].pGarg = chokeVGL[i].presGarg;
            celulaG[posGLG].tGarg = chokeVGL[i].tempGarg;
            celulaG[posGLG].qGarg = chokeVGL[i].qGarg;
            celulaG[posGLG].areaGarg = chokeVGL[i].areagarg;
        }
        subtempoGas();
    }
}

void SProd::EvoluiFrac(double alfrev, double betrev, int ciclo) {

#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = 0; i <= ncel; i++) {

        if (i < ncel) {
            celula[i].avancalf(reinicia, ncel);
        }
        if (i == ncel) {

            if (((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)) {
                if (masChkSup == 0 || celula[ncel].Mliqini > 0)
                    celula[i].alf = celula[i - 1].alf;
                else
                    celula[i].avancalf(reinicia, ncel);
            } else {
                if (celula[ncel].Mliqini > 0)
                    celula[i].alf = celula[i - 1].alf;
                else if (masChkSup == 0 && arq.chkv == 0)
                    celula[i].alf = alfrev;
                else
                    celula[i].avancalf(reinicia, ncel);
            }
        }
    }
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = 0; i <= ncel; i++) {
        if (i < ncel) {
            celula[i].avancbet(reinicia, ncel);
        }
        if (i == ncel) {
            if (((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)) {
                if (masChkSup == 0 || celula[ncel].Mliqini > 0)
                    celula[i].bet = celula[i - 1].bet;
                else
                    celula[i].avancbet(reinicia, ncel);
            } else {
                if (masChkSup == 1)
                    celula[i].avancbet(reinicia, ncel);
                else if (arq.chkv == 1)
                    celula[i].bet = celula[i - 1].bet;
            }
        }
    }
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = 0; i <= ncel; i++) {
        if (i < ncel && celula[i].estadoPig == 1) {
            celula[i].avancPig(reinicia);
            celula[i].avancalfPig();
            celula[i].avancbetPig();
        } else {
            celula[i].alfPigE = celula[i].alf;
            celula[i].betPigE = celula[i].bet;
            celula[i].alfPigD = celula[i].alf;
            celula[i].betPigD = celula[i].bet;
        }
    }
    for (int i = 0; i <= ncel; i++) {
        if (celula[i].correrGlobHol == 1) {
            if (celula[i].reiniciaAlf < 0 || celula[i].reiniciaBet < 0 || celula[i].reiniciaPig < 0)
                reinicia = -1;
            celula[i].reiniciaAlf = 0;
            celula[i].reiniciaBet = 0;
            celula[i].reiniciaPig = 0;
        }
    }
}

void SProd::ReiniEvolFrac0() {
    for (int i = 0; i <= ncel; i++) {
        if (celula[i].dt < dt)
            dt = celula[i].dt;
        if (celula[i].dt1 < dt)
            dt = celula[i].dt1;
        if (celula[i].dt2 < dt)
            dt = celula[i].dt2;
        if (celula[i].dtPig < dt)
            dt = celula[i].dtPig;
    }
}

void SProd::SubReiniEvolFrac() {
    for (int i = 0; i <= ncel; i++) {
        if (celula[i].pres > -10.) {
            celula[i].alf = celula[i].alfini;
            celula[i].alfPigE = celula[i].alfPigEini;
            celula[i].alfPigD = celula[i].alfPigDini;
        }
    }
}

void SProd::ReiniEvolFrac() {
    for (int i = 0; i <= ncel; i++) {
        celula[i].dt = dt;
        celula[i].dt1 = dt;
        celula[i].dt2 = dt;
        celula[i].dtPig = dt;
    }
    if (arq.lingas > 0) {
        for (int i = 0; i <= ncelGas; i++)
            celulaG[i].FeiticoDoTempo();
        if (arq.descarga == 1) {
            celInter = celInterIni;
            dtInter = dtInterIni;
            velInter = velInterIni;
        }
        subtempoGas();
    }
    SubReiniEvolFrac();
    for (int i = 0; i <= ncel; i++) {
        celula[i].bet = celula[i].betini;
        celula[i].razPig = celula[i].razPigini;
        celula[i].betPigE = celula[i].betPigEini;
        celula[i].betPigD = celula[i].betPigDini;
    }
}

void SProd::AtualizaPig() {
    for (int i = 1; i <= ncel; i++) {
        celula[i].velPigini = celula[i].velPig;
        celula[i].estadoPigini = celula[i].estadoPig;
        celula[i].indpigini = celula[i].indpig;
    }
    for (int i = 1; i <= ncel; i++) {
        if (i < ncel && celula[i].estadoPig == 1) {
            if (celula[i].velPig >= 0) {
                if (celula[i].razPig >= 1. - (*vg1dSP).localtiny) {
                    celula[i].estadoPig = 0;
                    celula[i + 1].estadoPig = 1;

                    celula[i].razPig = 0.;
                    celula[i + 1].razPig = 0.;
                    celula[i + 1].indpig = celula[i].indpig;
                    celula[i].indpig = -1;
                    celula[i + 1].velPig = celula[i].velPig;
                    celula[i + 1].alfPigE = celula[i].alf;
                    celula[i + 1].betPigE = celula[i].bet;
                }
            } else {
                if (celula[i].razPig <= (*vg1dSP).localtiny) {
                    celula[i].estadoPig = 0;
                    celula[i - 1].estadoPig = 1;
                    celula[i].razPig = 0.;
                    celula[i - 1].razPig = 1.;
                    celula[i - 1].indpig = celula[i].indpig;
                    celula[i].indpig = -1;
                    celula[i - 1].velPig = celula[i].velPig;
                    celula[i - 1].alfPigD = celula[i].alf;
                    celula[i - 1].betPigD = celula[i].bet;
                }
            }
        }
    }
    if (arq.ConContEntrada == 0) {
        celula[0].betI = celula[0].bet;
        celula[0].betLI = celula[0].bet;
    } else {
        celula[0].betI = betaE;
        if ((celula[0].MC - celula[0].Mliqini) * 0 + celula[0].Mliqini < 0.)
            celula[0].betI = celula[0].betPigE; // testeBeta
        celula[0].betLI = celula[0].betI;
    }
    for (int i = 1; i <= ncel; i++) {
        double betLI;
        double betI;
        double betRI;
        celula[i].betI = celula[i].betPigE;
        if (celula[i].QL > 0.)
            celula[i].betI = celula[i - 1].betPigD; // testeBeta
        celula[i - 1].betRI = celula[i].betI;
        if (i < ncel)
            celula[i + 1].betLI = celula[i].betI;
    }
}

void SProd::SolveAcopPV(int vexpli, int ciclo) {
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
    for (int i = 0; i <= ncel; i++) {
        celula[i].GeraLocal(presfim, masChkSup, ncel, arq.master1.razareaativ, presE, tempE, titE, betaE, ciclo,
                            modeloCompleto, noextremo, arq.corrigeContSep, chokeSup.AreaGarg, vexpli);
        for (int j = 0; j < 6; j++) {
            matglobP[2 * i][j - 3] = celula[i].local[0][j];
            matglobP[2 * i + 1][j - 3] = celula[i].local[1][j];
            termolivreP[2 * i] = celula[i].TL[0];
            termolivreP[2 * i + 1] = celula[i].TL[1];
        }
    }

    matglobP.GaussElimPP(termolivreP);
}

void SProd::prepDifusCalorND(int i) {
    double dia = celula[i].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double alfmed = celula[i].alf;
    double betmed = celula[i].bet;
    double ugsmed;
    double ulsmed;
    if (i > 0 && (celula[i - 1].acsr.tipo != 5 ||
                  celula[i - 1].acsr.chk.AreaGarg > (1e-3 + arq.master1.razareaativ) * celula[i - 1].duto.area)) {
        if (celula[i].alf > (*vg1dSP).localtiny)
            ugsmed = celula[i].QG / area;
        else {
            ugsmed = 0.;
        }
        if (celula[i].alf < 1. - (*vg1dSP).localtiny)
            ulsmed = celula[i].QL / area;
        else {
            ulsmed = 0.;
        }
    } else {
        if (celula[i].alf > (*vg1dSP).localtiny)
            ugsmed = celula[i + 1].QG / area;
        else {
            ugsmed = 0.;
        }

        if (celula[i].alf < 1. - (*vg1dSP).localtiny)
            ulsmed = celula[i + 1].QL / area;
        else {
            ulsmed = 0.;
        }
    }
    double rp = celula[i].rpC;
    double rc = celula[i].rcC;
    double rhol = (1. - betmed) * rp + betmed * rc;
    double rhog = celula[i].rgC;
    double cpl = (1. - betmed) * celula[i].flui.CalorLiq(celula[i].presini, celula[i].temp) + betmed * celula[i].fluicol.CalorLiq(celula[i].presini, celula[i].temp);
    double cpg = celula[i].flui.CalorGas(celula[i].presini, celula[i].temp);

    celula[i].calor.Tint = celula[i].temp;
    celula[i].calor.dtL = celula[i].temp - celula[i - 1].tempini;
    celula[i].calor.Vint = ugsmed + ulsmed;
    celula[i].calor.dt = celula[i].dt;
    double condliq = (1. - betmed) * celula[i].flui.CondLiq(celula[i].presini, celula[i].temp) + betmed * celula[i].fluicol.CondLiq(celula[i].presini, celula[i].temp); //(1. - betmed) * celula[i].flui.CondLiq(celula[i].pres, celula[i].temp)
    celula[i].calor.kint = condliq * (1 - alfmed) + celula[i].flui.CondGas(celula[i].presini, celula[i].temp) * alfmed;                                                 // condliq * (1 - alfmed) + celula[i].flui.CondGas(celula[i].pres, celula[i].temp) * alfmed;
    celula[i].calor.cpint = cpl * (1 - alfmed) + cpg * alfmed;
    celula[i].calor.rhoint = rhol * (1 - alfmed) + rhog * alfmed;
    //(1. - betmed) * celula[i].flui.ViscOleo(celula[i].pres, celula[i].temp)
    double viscliq = (1. - betmed) * celula[i].mipC + betmed * celula[i].micC;
    // viscliq * (1 - alfmed) * 1.e-3
    celula[i].calor.viscint = viscliq * (1 - alfmed) * 1.e-3 + celula[i].migC * alfmed * 1.e-3;
    double dtemp = celula[i].temp * 0.01;
    if (fabs(celula[i].temp) < 1e-15)
        dtemp = 0.1;
    double rholdT = (1. - betmed) * celula[i].flui.MasEspLiq(celula[i].presini, celula[i].temp + dtemp) +
                    betmed * celula[i].fluicol.MasEspFlu(celula[i].presini, celula[i].temp + dtemp) - rhol; //(1. - betmed) * celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp+dtemp) +
    double rhogdT = celula[i].flui.MasEspGas(celula[i].presini, celula[i].temp + dtemp) - rhog;             // celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp+dtemp)-rhog;
    celula[i].calor.betint = -(1 / celula[i].calor.rhoint) * (rholdT * (1 - alfmed) + rhogdT * alfmed) / (dtemp);
}

void SProd::marchaEnergTrans(int ciclo, int ciclomax) {
    if (((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1) && arq.chkv == 0) {
        if (celula[ncel - 1].MliqiniR < 0) {
            celula[ncel - 1].MR = celula[ncel - 1].MR - celula[ncel - 1].MliqiniR;
            celula[ncel - 1].MliqiniR = 0;
            celula[ncel - 1].term1R = 0;
            celula[ncel - 1].term2R = 0;
            celula[ncel].MC = celula[ncel].MC - celula[ncel].Mliqini;
            celula[ncel].Mliqini = 0;
            celula[ncel].term1 = 0;
            celula[ncel].term2 = 0;
        }
        if (celula[ncel - 1].QLR < 0 && (masChkSup == 0 || aberto == 1)) {
            celula[ncel - 1].QLR = 0;
            celula[ncel].QL = 0;
        }
    } else if ((arq.chkv == 1 && masChkSup == 0) || (arq.chkv == 1 && masChkSup == 1)) {
        if (celula[ncel - 1].MliqiniR < 0) {
            celula[ncel - 1].MR = 0.;
            celula[ncel - 1].MliqiniR = 0;
            celula[ncel - 1].term1R = 0;
            celula[ncel - 1].term2R = 0;
            celula[ncel].MC = 0.;
            celula[ncel].Mliqini = 0;
            celula[ncel].term1 = 0;
            celula[ncel].term2 = 0;
        }
        if (celula[ncel - 1].QLR < 0 && (masChkSup == 0 || aberto == 1)) {
            celula[ncel - 1].QLR = 0;
            celula[ncel].QL = 0;
        }
    }

    //!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
    // atencao!!!!!!!!!!!!!!!!!!
    // existe uma questao que parece mal resolvida na resolucao desta marcha, nao foi feito nenhum teste para
    // o caso em que a velocidade de transporte da temperatura é <0 neste caso, a temperatura na celula de indice
    // não deveria entrar no metodo calctemp, ja que não e mais o caso de ser uma celula com condicao de
    // contorno para temperatura??????????????????????????????????????????????????????????????????????/
    //"!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

    celula[0].tempini = celula[0].temp;
    if (arq.ConContEntrada > 0)
        celula[0].temp = tempE;
    else
        celula[0].temp = temperatura;
    celula[0].dTdt = (celula[0].temp - celula[0].tempini) / dt;
    celula[0].dTdtL = celula[0].dTdt;
    celula[1].tempLini = celula[1].tempL;
    celula[1].tempL = celula[0].temp;
    for (int i = 1; i <= ncel; i++) {
        celula[i].tempini = celula[i].temp;
    }
    if (arq.modoDifus3D == 0) {
        if (nCelulaPoisson2D > 1) {
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
            for (int iP2D = 0; iP2D < nCelulaPoisson2D; iP2D++) {
                int i = indCelPoisson2D[iP2D];
                if (i <= ncel) {
                    calctemp(i, celula[i].tempini);
                } else {
                    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)
                        celula[i].temp = celula[i].calor.Textern1;
                    else
                        celula[i].temp = tGSup;
                }
                celula[i].dTdt = (celula[i].temp - celula[i].tempini) / dt;
                celula[i].dTdtIni = celula[i].dTdt;
            }
        }
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
        for (int i = 0; i <= ncel; i++) {
            if (i == 217) {
                int para;
                para = 0;
            }
            if (celula[i].calor.difus2D == 0) {
                if (i <= ncel) {
                    calctemp(i, celula[i].tempini);
                } else {
                    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)
                        celula[i].temp = celula[i].calor.Textern1;
                    else
                        celula[i].temp = tGSup;
                }
                celula[i].dTdt = (celula[i].temp - celula[i].tempini) / dt;
                celula[i].dTdtIni = celula[i].dTdt;
            }
        }
    } else if (arq.modoDifus3D == 1) {
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
        for (int i = 0; i <= ncel; i++) {
            int acoplado = -1;
            int icelAcop;
            for (int iacop = 0; iacop < arq.nacop; iacop++) {
                icelAcop = arq.celAcop[iacop].indCel;
                if (i == icelAcop) {
                    acoplado = iacop;
                    break;
                }
            }
            if (acoplado != -1) {
                prepDifusCalorND(i);
                int iacop1 = acertaIndAcop[acoplado];
                poisson3D.dados.tInt[iacop1] = celula[icelAcop].temp;
                double hiCel = celula[icelAcop].calor.hInt();
                poisson3D.dados.hI[iacop1] = hiCel;
            }
        }
        if (ciclo < ciclomax || ciclomax == 0 || dtCicMin != dt) {
            if (ciclo == ciclomax && ciclomax > 0)
                poisson3D.FeiticoDoTempo();
            poisson3D.transientePoisson(dt);
        }
        if (nCelulaPoisson2D > 1) {
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
            for (int iP2D = 0; iP2D < nCelulaPoisson2D; iP2D++) {
                int i = indCelPoisson2D[iP2D];
                if (i <= ncel) {
                    calctemp(i, celula[i].tempini);
                } else {
                    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)
                        celula[i].temp = celula[i].calor.Textern1;
                    else
                        celula[i].temp = tGSup;
                }
                celula[i].dTdt = (celula[i].temp - celula[i].tempini) / dt;
                celula[i].dTdtIni = celula[i].dTdt;
            }
        }
#pragma omp parallel for num_threads((*vg1dSP).ntrd)
        for (int i = 0; i <= ncel; i++) {
            if (celula[i].calor.difus2D == 0) {
                if (i <= ncel) {
                    calctemp(i, celula[i].tempini);
                } else {
                    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)
                        celula[i].temp = celula[i].calor.Textern1;
                    else
                        celula[i].temp = tGSup;
                }
                celula[i].dTdt = (celula[i].temp - celula[i].tempini) / dt;
                celula[i].dTdtIni = celula[i].dTdt;
            }
        }
    }
    for (int i = 1; i <= ncel; i++) {
        celula[i].dTdtL = celula[i - 1].dTdt;
        if (i < ncel) {
            celula[i + 1].tempLini = celula[i + 1].tempL;
            celula[i + 1].tempL = celula[i].temp;
        }
        celula[i - 1].tempRini = celula[i - 1].tempR;
        celula[i - 1].tempR = celula[i].temp;
    }
    if (ciclo < ciclomax) {
        for (int k = 0; k <= ncel; k++) {
            celula[k].FeiticoDoTempo();
        }
    } else if (arq.modoDifus3D == 1)
        poisson3D.renova();
    if (modeloCompleto == 0) {
        if (ciclo < ciclomax) {
            SolveAcopPV(ciclo);
            renova();
        }
    }
}

void SProd::atualizaMiniTab() {
    geraMiniTabFlu();
    double betIV;
    double rsV;
    double boV;
    double baV;
    double bswV;
    double rhoOVol;
    double rhoWVol;
    double titVol;
    double rhoGVol;
    double ZGVol;
    double DZDPGVol;
    double DZDTGVol;
    for (int i = 0; i < ncel; i++) {

        double pres = celula[i].pres;
        double temp = celula[i].temp;

        betIV = celula[i].bet;
        rsV = celula[i].flui.RS(celula[i].pres, celula[i].temp);
        boV = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp, rsV);
        baV = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
        bswV = celula[i].flui.BSW * baV / (boV + baV * celula[i].flui.BSW - celula[i].flui.BSW * boV);
        rhoOVol = celula[i].flui.MasEspoleo(celula[i].pres, celula[i].temp);
        rhoWVol = celula[i].flui.MasEspAgua(celula[i].pres, celula[i].temp);
        titVol = (1 - bswV) * rhoOVol / ((1 - bswV) * rhoOVol + bswV * rhoWVol);

        rhoGVol = celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp);
        ZGVol = celula[i].flui.Zdran(celula[i].pres, celula[i].temp);
        DZDPGVol = celula[i].flui.FracMassHidra(celula[i].pres, celula[i].temp);
        DZDTGVol = celula[i].flui.PB(celula[i].pres, celula[i].temp);
        celula[i].nMol = (celula[i].flui.MasEspLiq(pres, temp) * (1. - celula[i].alf) * (1. - betIV) * titVol +
                          celula[i].rgC * celula[i].alf) *
                         celula[i].duto.area * celula[i].dx / celula[i].flui.Pmol;
        celula[i].nMolIni = celula[i].nMol;
    }
}

void SProd::atualizaCC1() {
    if (arq.ConContEntrada == 1) {
        if (arq.tipoFluido == 0 && arq.flashCompleto == 2) {
            double rgST = celula[0].flui.Deng * 1.225;
            double roST = 141.5 * 1000. / (131.5 + celula[0].flui.API);
            double rg = celula[0].flui.MasEspGas(presE, tempE);
            double rl = celula[0].flui.MasEspLiq(presE, tempE);
            double titH = celula[0].flui.FracMassHidra(presE, tempE);
            double rcST = celula[0].fluicol.MasEspFlu(1.01, 20.);
            double rc = celula[0].fluicol.MasEspFlu(presE, tempE);
            double rlMix = betaE * rc + (1. - betaE) * rl;
            double val1 = ((1. - betaE) * rl * titH / (1. - titH));
            titE = val1 / (rlMix + val1);
        } else if (arq.tipoFluido == 1) {
            double rgST = celula[0].flui.Deng * 1.225;
            double roST = 141.5 * 1000. / (131.5 + celula[0].flui.API);
            double rg = celula[0].flui.MasEspGas(presE, tempE);
            double rl = celula[0].flui.MasEspoleo(presE, tempE);
            double tit = celula[0].flui.FracMass(presE, tempE);
            double rcST = celula[0].fluicol.MasEspFlu(1.01, 20.);
            double rc = celula[0].fluicol.MasEspFlu(presE, tempE);
            double val1 = (rcST / rc) * (rg / rgST) * arq.CCPres.bet[0] / tit;
            double val2 = (rg / rl) * (1 - tit) / tit;
            double titT = rg / (((1. - tit) / tit) * (rg / rl) + rg + val1);
            titE = titT;
            betaE = val1 / (val2 + val1);
        }
    }
}

void SProd::SolveTrans(double titRev, double alfRev, double betRev, int nrede, ProFlu fluiRev) {
    chrono::steady_clock::time_point begin, end;
    begin = chrono::steady_clock::now();
    double velmaxdesc = 0;

    if ((*vg1dSP).chaverede == 0) {

        if (arq.calculaEnvelope == 1 && (*vg1dSP).lixo5 <= arq.tfinal) { //*vg1dSP).lixo5>0 && //chris - Hidratos
            FA_Hidrato solverHidrato(*this);
            solverHidrato.solverHidrato();
        }

        if (arq.lingas > 0 && arq.calculaEnvelope == 1 && (*vg1dSP).lixo5 <= arq.tfinal) { //*vg1dSP).lixo5>0 && //chris - Hidratos
            FA_Hidrato_Servico solverHidratoG(*this);
            solverHidratoG.solverHidratoG();
        }

        if (arq.flashCompleto == 2 && (*vg1dSP).lixo5 < 1e-15) {
            atualizaMiniTab();
        }
        if ((*vg1dSP).lixo5 >= 996) {
            int para;
            para = 0;
        }

        if ((*vg1dSP).lixo5 < 1e-15) {
        	for(int iCelU=0;iCelU<arq.nCelUnit;iCelU++)kontaTempoCelUni[iCelU]=1;
            for (int i = 0; i < arq.ntendp; i++) {
                arq.imprimeTrend(celula, MatTrendP[i], (*vg1dSP).lixo5, i, ntrend[i]);
            }
            renovaTemp();
        }
        int ciclomax = arq.cicloAcopTerm;

        int vExpli = 0;
        modeloCompleto = arq.correcaoMassaEspLiq;
        arq.atualizaSonico((*vg1dSP).lixo5, vExpli);
        determinaDT(vExpli);
        dtauxCFL = dt;
        dtauxFinal = dt;

        reinicia = 0;
        int celpos = arq.master1.posic;
        // razMast0=celula[celpos].acsr.chk.AreaGarg/celula[celpos].duto.area;//caso so Master
        aberturaVal0(); // caso varias valvulas

        if (arq.controDesc == 1)
            velmaxdesc = BuscaPresInjDesc();
        solveLinGas();
        pGSupIni = pGSup;
        arq.atualiza(noinicial, noextremo, derivaAnel, chokeSup, chokeInj, celula, celulaG, pGSup,
                     temperatura, presiniG, tempiniG,
                     presE, tempE, titE, betaE, (*vg1dSP).lixo5, dt);
        atualizaCC1();

        for (int i = 0; i <= arq.nvalv; i++)
            vRazMastCrit[i] = 0.5; // caso varias valvulas
        aberturaVal1();            // caso varias valvulas
        // razMast=celula[celpos].acsr.chk.AreaGarg/celula[celpos].duto.area;//caso so Master
        for (int i = 0; i <= arq.nvalv; i++)
            if (vRazMast1[i] != vRazMast0[i])
                modeloCompleto = 0; // caso varias valvulas
        if (modeloCompleto == 1)
            avaliaVariaDpDt(0, 0, vExpli); // caso varias valvulas
        if (modeloCompleto == 0)
            arq.cicloAcopTerm = 0;
        else
            arq.cicloAcopTerm = 1;
        ciclomax = arq.cicloAcopTerm;

        abertoini = aberto;
        tempoabertoini = tempoaberto;
        for (int kontaAcop = 0; kontaAcop <= 1 * modeloCompleto; kontaAcop++) {
            if (modeloCompleto == 0) {
                for (int i = 0; i <= ncel; i++)
                    celula[i].m2d = 0.;
            } else {
                for (int i = 0; i <= ncel; i++) {
                    double area = celula[i].duto.area;
                    double vLiqTest = fabs(celula[i].QL / (area));
                    double vGasTest = fabs(celula[i].QG / (area));
                    double razDp = 0.1;
                    double razDT = 1;
                    if (i < celpos && celula[celpos].acsr.chk.AreaGarg < 1e-15 * celula[celpos].acsr.chk.AreaTub) {
                        razDT = 1;
                    } else if (i == celpos + 1 && celula[celpos].acsr.chk.AreaGarg < 1e-15 * celula[celpos].acsr.chk.AreaTub) {
                        razDT = 1;
                    }
                    if ((fabs(celula[i].dpdtIni) / celula[i].pres < razDp) && fabs(celula[i].dTdtIni) < razDT) {
                        if (TransMassModel == 0)
                            celula[i].m2d = 1.;
                        else
                            celula[i].m2d = 0.;
                        celula[i].mudaDT = 1.;
                    } else {
                        celula[i].m2d = 0.;
                        celula[i].mudaDT = 0.;
                    }
                }
            }
            if (arq.estabCol == 1) {
                for (int i = 0; i <= celpos; i++) {
                    celula[i].m2d = 0.;
                    celula[i].mudaDT = 0.;
                    celula[i].estabCol = 1;
                }
            }
            EvoluiFrac(alfRev, betRev, kontaAcop);
            for (int i = 0; i <= ncel; i++) {
                if (celula[i].acsr.tipo == 15) {
                    celula[i].acsr.radialPoro.avancoSW(dt);
                    if (celula[i].acsr.radialPoro.reinicia == -1) {
                        if (reinicia > -1)
                            reinicia = -1;
                        // celula[i].acsr.radialPoro.reavaliaDT(Ndt)
                    }
                } else if (celula[i].acsr.tipo == 16) {
                    celula[i].acsr.poroso2D.avancoSW(dt);
                    if (celula[i].acsr.poroso2D.reinicia == -1) {
                        if (reinicia > -1)
                            reinicia = -1;
                        // celula[i].acsr.radialPoro.reavaliaDT(Ndt)
                    }
                }
            }

            if (arq.correcaoMassaEspLiq == 1) {
                for (int i = 0; i < ncel; i++)
                    celula[i + 1].mudaDTL = celula[i].mudaDT;
            }

            // caso so Master
            // caso so Master
            if (kontaAcop == 0 && arq.controleDTvalv == 1)
                restringeDTporValv(); // caso varias valvulas
            if (reinicia == -1) {
                ReiniEvolFrac0();
                for (int i = 0; i <= ncel; i++) {
                    if (celula[i].acsr.tipo == 15) {
                        celula[i].acsr.radialPoro.reavaliaDT(dt);
                    } else if (celula[i].acsr.tipo == 16) {
                        celula[i].acsr.poroso2D.reavaliaDT(dt);
                    }
                }
                for (int i = 0; i <= ncel; i++) {
                    if (celula[i].acsr.tipo == 15) {
                        celula[i].acsr.radialPoro.reiniciaEvoluiSW(dt);
                    }
                    if (celula[i].acsr.tipo == 16) {
                        celula[i].acsr.poroso2D.reiniciaEvoluiSW(dt);
                    }
                }
                dtauxFinal = dt;
                ReiniEvolFrac();
                EvoluiFrac(alfRev, betRev, kontaAcop);
                reinicia = 0;
                for (int i = 0; i <= ncel; i++) {
                    if (celula[i].acsr.tipo == 15) {
                        celula[i].acsr.radialPoro.avancoSWcorrec();
                    } else if (celula[i].acsr.tipo == 16) {
                        celula[i].acsr.poroso2D.avancoSWcorrec();
                    }
                }
            }
            AtualizaPig();

            if (kontaAcop == 0)
                dtCicMin = dt;

            if (kontaAcop == 1 * modeloCompleto)
                atenuaDtMax();

            double fonteG = 0.;
            double fonteP = 0.;
            double fonteC = 0.;
            if (modeloCompleto == 1) {
                fonteC = celula[ncel].fontemassCR;
                fonteP = celula[ncel].fontemassLR;
                fonteG = celula[ncel].fontemassGR;
            }

            calcCCpres(titRev, alfRev, betRev);
            renovaterm();

            if (celula[ncel].alf < 0.05 && masChkSup == 1)
                celula[ncel].alf = 0.05;
            // caso varias valvulas
            for (int j = 0; j <= arq.nvalv; j++) {
                int celposAux;
                if (j > 0)
                    celposAux = arq.valv[j - 1].posicP;
                else
                    celposAux = celpos;
                if (celula[celposAux].alf < 0.05 && vRazMast1[j] <= arq.master1.razareaativ)
                    celula[celposAux].alf = 0.05;
            }
            // caso varias valvulas
            SolveAcopPV(vExpli);

            if (kontaAcop < 1 * modeloCompleto) {
                for (int i = 0; i <= ncel; i++) {
                    celula[i].dpdt = 1 * (termolivreP[2 * i + 1] - celula[i].pres) / celula[i].dt;
                    celula[i].dpdtIni = celula[i].dpdt;
                }
            }
            if (kontaAcop == 1 * modeloCompleto || arq.cicloAcopTerm == 1) {
                renova();
            }
            if (arq.cicloAcopTerm == 1 && modeloCompleto == 1) {
                if (kontaAcop < 1 * modeloCompleto)
                    for (int i = 0; i <= ncel; i++)
                        celula[i].dpdt = celula[i].d2pdt2;
                marchaEnergTrans(kontaAcop, ciclomax);
            }
            if (kontaAcop != 1 * modeloCompleto) {
                for (int i = 0; i <= ncel; i++) {
                    celula[i].FeiticoDoTempo2();
                    if (celula[i].acsr.tipo == 15) {
                        celula[i].acsr.radialPoro.FeiticoDoTempoSW();
                    } else if (celula[i].acsr.tipo == 16) {
                        celula[i].acsr.poroso2D.FeiticoDoTempoSW();
                    }
                }

                celula[ncel].fontemassCR = fonteC;
                celula[ncel].fontemassLR = fonteP;
                celula[ncel].fontemassGR = fonteG;

                aberto = abertoini;
                tempoaberto = tempoabertoini;
            }
        }

        if (modeloCompleto == 0 || arq.cicloAcopTerm == 0) {
            for (int ciclo = 0; ciclo <= ciclomax; ciclo++) {
                marchaEnergTrans(ciclo, ciclomax);
            }
        }
    }
    if (poisson3D.itera > 7) {
        poisson3D.penalizaDt = 20;
    }

    for (int i = 1; i <= ncel; i++) {
        celula[i].dTdt = 0.;
        celula[i].dTdtL = 0.;
        if (modeloCompleto == 0 || celula[i].estabCol == 1) {
            celula[i].dTdtIni = 0.;
            celula[i].d2pdt2 = 0.;
        }
    }

    if (arq.modoParafina == 1)
        avaliaParafina();

    salvaFonte();
    renovaTemp();

    presfim = celula[ncel].pres;
    if (arq.lingas > 0 && verificaAcop == 1)
        conectaColuna();

    if (arq.flashCompleto == 2) {
        for (int i = 0; i < ncel; i++) {
            celula[i].nMolIni = celula[i].nMol;
        }
    }
    double totbet = 0.;
    for (int i = 0; i < ncel; i++)
        totbet += fabs(celula[i].bet);
    totbet /= ncel;
    renovaalbetini();
    if ((trackRGO > 0 || trackDeng > 0) && arq.flashCompleto != 2)
        renovaRGOdgYco2(fluiRev);
    if (arq.flashCompleto == 2) {
        renovaFracMol2(fluiRev);
        kontaRenovaComp++;
        if (kontaRenovaComp == arq.miniTabAtraso + 1 && arq.miniTabAtraso > 0) {
            geraMiniTabFlu();
            kontaRenovaComp = 0;
        }
    } // casoComp
    renovaMasEsp();

    tVet.push_back(dt);
    presVet.push_back(presfim * dt);
    double jtemporario = celula[ncel - 1].Mliqini / (celula[ncel - 1].duto.area * ((1. - celula[ncel - 1].bet) * celula[ncel - 1].rpC + celula[ncel - 1].bet * celula[ncel - 1].rcC));
    jtemporario += (celula[ncel - 1].MC - celula[ncel - 1].Mliqini) / (celula[ncel - 1].duto.area * celula[ncel - 1].flui.MasEspGas(celula[ncel - 1].pres, celula[ncel - 1].temp));
    jVet.push_back(jtemporario * dt);
    alfVet.push_back(celula[ncel - 1].alf * dt);
    ktMedMov += dt;
    pTotal += presfim * dt;
    jTotal += jtemporario * dt;
    alfTotal += celula[ncel - 1].alf * dt;
    if ((*vg1dSP).lixo5 > tMedMov) {
        pTotal -= presVet.front();
        presVet.erase(presVet.begin());
        jTotal -= jVet.front();
        jVet.erase(jVet.begin());
        alfTotal -= alfVet.front();
        alfVet.erase(alfVet.begin());
        ktMedMov -= tVet.front();
        tVet.erase(tVet.begin());
        presMedMov = pTotal / ktMedMov;
        jMedMov = jTotal / ktMedMov;
        alfMedMov = alfTotal / ktMedMov;
    }
    // enterramento
    for (int j = 0; j <= ncel; j++) {
        if (celula[j].calor.difus2D == 1) {
            celula[j].calor.poisson2D.finalizaPassoTransiente(dt, indTramo);
        }
    }

    int MaxKontaImpres = 1000;
    int ordemImpT = 0;
    if ((fabs(arq.logevento[contaLog].instante - (*vg1dSP).lixo5) < dt) ||
        (fabs((*vg1dSP).lixo5 * (100. / 5.) / arq.tfinal - round((*vg1dSP).lixo5 * (100. / 5.) / arq.tfinal)) < 0.5 * dt * (100 / 5.) / arq.tfinal) || KontaImprime > MaxKontaImpres || ((*vg1dSP).lixo5 + dt >= arq.tfinal) ||
        (*vg1dSP).lixo5 < 1e-15) {
        kimpT++;
        ordemImpT = 1;
    }

    if ((*vg1dSP).chaverede != 0)
        (*vg1dSP).lixo5 = (*vg1dSP).lixo5R;
    if (arq.nperfisp > 0) {
        if (((*vg1dSP).lixo5 > (*vg1dSP).localtiny && (*vg1dSP).lixo5 <= arq.profp.tempo[kontaTempoProf] && (*vg1dSP).lixo5 + dt >= arq.profp.tempo[kontaTempoProf])) {
            arq.imprimeProfile(celula, flut, (*vg1dSP).lixo5, indTramo, nrede);
            arq.profp.tempo[kontaTempoProf] = (*vg1dSP).lixo5;
            kontaTempoProf++;
            if (kontaTempoProf >= arq.profp.n)
                kontaTempoProf--;
        }
    }
    if (arq.nperfisg > 0 && arq.lingas > 0) {
        if (((*vg1dSP).lixo5 > (*vg1dSP).localtiny && (*vg1dSP).lixo5 <= arq.profg.tempo[kontaTempoProfG] && (*vg1dSP).lixo5 + dt >= arq.profg.tempo[kontaTempoProfG])) {
            arq.imprimeProfileG(celulaG, flutG, (*vg1dSP).lixo5, indTramo, nrede);
            arq.profg.tempo[kontaTempoProfG] = (*vg1dSP).lixo5;
            kontaTempoProfG++;
            if (kontaTempoProfG >= arq.profg.n)
                kontaTempoProfG--;
        }
    }
    if (arq.nperfistransp > 0) {
        if (((*vg1dSP).lixo5 > (*vg1dSP).localtiny && (*vg1dSP).lixo5 <= arq.proftransp.tempo[kontaTempoTransProf] && (*vg1dSP).lixo5 + dt >= arq.proftransp.tempo[kontaTempoTransProf])) {
            arq.imprimeProfileTrans(celula, ncelperftransp, (*vg1dSP).lixo5, indTramo, nrede);
            arq.proftransp.tempo[kontaTempoTransProf] = (*vg1dSP).lixo5;
            kontaTempoTransProf++;
            if (kontaTempoTransProf >= arq.proftransp.n)
                kontaTempoTransProf--;
        }
    }
    if (arq.nperfistransg > 0 && arq.lingas > 0) {
        if (((*vg1dSP).lixo5 > (*vg1dSP).localtiny && (*vg1dSP).lixo5 <= arq.proftransg.tempo[kontaTempoTransProfG] && (*vg1dSP).lixo5 + dt >= arq.proftransg.tempo[kontaTempoTransProfG])) {
            arq.imprimeProfileTransG(celulaG, ncelperftransg, (*vg1dSP).lixo5, indTramo, nrede);
            arq.proftransg.tempo[kontaTempoTransProfG] = (*vg1dSP).lixo5;
            kontaTempoTransProfG++;
            if (kontaTempoTransProfG >= arq.proftransg.n)
                kontaTempoTransProfG--;
        }
    }
    if (arq.nCelUnit>0) {
    	for(int iCelU=0;iCelU<arq.nCelUnit;iCelU++){
    		if (((*vg1dSP).lixo5 > (*vg1dSP).localtiny && (*vg1dSP).lixo5 <= arq.celUnit[iCelU].tempo[kontaTempoCelUni[iCelU]] &&
    				(*vg1dSP).lixo5 + dt >= arq.celUnit[iCelU].tempo[kontaTempoCelUni[iCelU]])) {
    			arq.relatorioCelulaUnitaria(celula,arq.celUnit[iCelU].posicP, indTramo,nrede);
    			arq.celUnit[iCelU].tempo[kontaTempoCelUni[iCelU]] = (*vg1dSP).lixo5;
    			kontaTempoCelUni[iCelU]++;
    			if (kontaTempoCelUni[iCelU] >= arq.celUnit[iCelU].parserie)
    				kontaTempoCelUni[iCelU]--;
    		}
    	}
    }
    if (arq.ntendp > 0) {
        for (int i = 0; i < arq.ntendp; i++) {
            if (resettrend[i] == 0) {
                if ((*vg1dSP).lixo5 < 1e-15)
                    ImprimeTrendPCab(i, nrede);
                if ((*vg1dSP).lixo5 > 1e-15)
                    arq.imprimeTrend(celula, MatTrendP[i], (*vg1dSP).lixo5, i, ntrend[i]);
                ntrend[i]++;
            }
            if (ordemImpT == 1) {
                if ((*vg1dSP).lixo5 >= 800.8000000000000445) {
                    int para;
                    para == 1;
                }
                ImprimeTrendP(i, nrede);
                ntrendB[i] = ntrend[i];
            }
            resettrend[i] += dt;
            if (resettrend[i] > arq.trendp[i].dt)
                resettrend[i] = 0;
        }
    }
    if (arq.ntendg > 0 && arq.lingas > 0) {
        for (int i = 0; i < arq.ntendg; i++) {
            if (resettrendg[i] == 0 || (*vg1dSP).lixo5 < 1e-15) {
                if ((*vg1dSP).lixo5 < 1e-15)
                    ImprimeTrendGCab(i, nrede);
                arq.imprimeTrendG(celulaG, MatTrendG[i], (*vg1dSP).lixo5, i, ntrendg[i], velmaxdesc);
                ntrendg[i]++;
            }
            if (ordemImpT == 1) {
                ImprimeTrendG(i, nrede);
                ntrendgB[i] = ntrendg[i];
            }
            resettrendg[i] += dt;
            if (resettrendg[i] > arq.trendg[i].dt)
                resettrendg[i] = 0;
        }
    }
    if (arq.ntendtransp > 0) {
        for (int i = 0; i < arq.ntendtransp; i++) {
            if (resettrendtrans[i] == 0 || (*vg1dSP).lixo5 < 1e-15) {
                if ((*vg1dSP).lixo5 < 1e-15)
                    ImprimeTrendTransPCab(i);
                MatTrendTransP[i][ntrendtrans[i]][0] = (*vg1dSP).lixo5;
                int poscel = arq.trendtransp[i].posic;
                int poscam = arq.trendtransp[i].camada - 1;
                int posdiscre = arq.trendtransp[i].discre - 1;
                MatTrendTransP[i][ntrendtrans[i]][1] = celula[poscel].calor.Tcamada[poscam][posdiscre];
                ntrendtrans[i]++;
            }
            if (ordemImpT == 1) {
                ImprimeTrendTransP(i);
                ntrendtransB[i] = ntrendtrans[i];
            }
            resettrendtrans[i] += dt;
            if (resettrendtrans[i] > arq.trendtransp[i].dt)
                resettrendtrans[i] = 0;
        }
    }
    if (arq.ntendtransg > 0 && arq.lingas > 0) {
        for (int i = 0; i < arq.ntendtransg; i++) {
            if (resettrendtransg[i] == 0 || (*vg1dSP).lixo5 < 1e-15) {
                if ((*vg1dSP).lixo5 < 1e-15)
                    ImprimeTrendTransGCab(i);
                MatTrendTransG[i][ntrendtransg[i]][0] = (*vg1dSP).lixo5;
                int poscel = arq.trendtransg[i].posic;
                int poscam = arq.trendtransg[i].camada - 1;
                int posdiscre = arq.trendtransg[i].discre - 1;
                MatTrendTransG[i][ntrendtransg[i]][1] = celulaG[poscel].calor.Tcamada[poscam][posdiscre];
                ntrendtransg[i]++;
            }
            if (ordemImpT == 1) {
                ImprimeTrendTransG(i);
                ntrendtransgB[i] = ntrendtransg[i];
            }
            resettrendtransg[i] += dt;
            if (resettrendtransg[i] > arq.trendtransg[i].dt)
                resettrendtransg[i] = 0;
        }
    }
    //(*vg1dSP).lixo5 += dt;//alteracao7
    end = chrono::steady_clock::now();
    (*vg1dSP).contador = kSP;
    if (arq.saidaTela == 1) {
        cout << kSP << "  " << (*vg1dSP).lixo5 << " " << dt;
        for (int i = 0; i < arq.ntela; i++) {
            int posic = arq.tela[i].posic;
            if (arq.tela[i].col == 1) {
                switch (arq.tela[i].var) {
                case 1:
                    cout << " " << modeloCompleto;
                    break;
                case 2:
                    cout << " " << celula[posic].temp;
                    break;
                case 3:
                    cout << " " << celula[posic].alf;
                    break;
                case 4:
                    cout << " " << celula[posic].bet;
                    break;
                case 5:
                    cout << " " << (celula[posic].QG / celula[posic].duto.area);
                    break;
                case 6:
                    cout << " " << (celula[posic].QL / celula[posic].duto.area);
                    break;
                }
            } else {
                switch (arq.tela[i].var) {
                case 1:
                    cout << " " << celulaG[posic].pres;
                    break;
                case 2:
                    cout << " " << celulaG[posic].temp;
                    break;
                case 3:
                    cout << " " << (celulaG[posic].VGasR / celulaG[posic].duto.area);
                    break;
                }
            }
        }
        cout << " " << chrono::duration_cast<std::chrono::microseconds>(end - begin).count();
        cout << endl;
    }

    int maxEvento = arq.logevento.size();
    if (contaLog < maxEvento) {
        while (fabs(arq.logevento[contaLog].instante - (*vg1dSP).lixo5) < dt) {
            // current date/time based on current system
            time_t now = time(0);
            tm *ltm = localtime(&now); ///////////Retirado de https://www.tutorialspoint.com/cplusplus/cpp_date_time.htm
            ostringstream saidaT;
            if (indTramo < 0) {
                saidaT << tmpLog;
            } else {
                saidaT << "Tramo" << indTramo << "-" << tmpLog;
            }
            string tmp = saidaT.str();
            ofstream escreveIni(tmp.c_str(), ios_base::app);
            escreveIni << "************************************************************************************************"
                       << endl;
            escreveIni << "Evento Externo = ";
            escreveIni << arq.logevento[contaLog].instante << " ; ";
            escreveIni << arq.logevento[contaLog].duracao << " ; ";
            escreveIni << arq.logevento[contaLog].estIni << " ; ";
            escreveIni << arq.logevento[contaLog].estFim << " ; ";
            escreveIni << arq.logevento[contaLog].descricao << " ; ";
            escreveIni << "datahora = ";
            escreveIni << ltm->tm_mday << "/";
            escreveIni << 1 + ltm->tm_mon << "/";
            escreveIni << 1900 + ltm->tm_year << " ";
            escreveIni << 0 + ltm->tm_hour << ":";
            escreveIni << 0 + ltm->tm_min << ":";
            escreveIni << 0 + ltm->tm_sec;
            escreveIni << endl;
            contaLog++;

            escreveIni.close();
        }
    }
    if ((fabs((*vg1dSP).lixo5 * (100. / 5.) / arq.tfinal - round((*vg1dSP).lixo5 * (100. / 5.) / arq.tfinal)) < 0.5 * dt * (100 / 5.) / arq.tfinal) || KontaImprime > MaxKontaImpres || ((*vg1dSP).lixo5 + dt >= arq.tfinal)) {
        if (arq.saidaTela == 0)
            cout << (*vg1dSP).lixo5 * (100.) / arq.tfinal << " % da simulacao alcancado" << endl;
        KontaImprime = 0;
        ostringstream saidaT;
        if (indTramo < 0) {
            saidaT << tmpLog;
        } else {
            saidaT << "Tramo" << indTramo << "-" << tmpLog;
        }
        string tmp = saidaT.str();
        ofstream escreveIni(tmp.c_str(), ios_base::app);
        escreveIni << "************************************************************************************************"
                   << endl;
        escreveIni << "Percentual alcancado = " << (*vg1dSP).lixo5 * (100.) / arq.tfinal << " % da simulacao alcancado" << endl;
        escreveIni << "| Passo de Tempo = " << kSP << "| Tempo (s) = " << (*vg1dSP).lixo5 << "| Incremento de Tempo (s) = " << dt
                   << " |" << " Incremento de Tempo Medio CFL (s) = "
                   << dtCFLMed << "| Incremento de Tempo Medio Simulado (s) = " << dtSimMed
                   << " |" << endl;
        for (int i = 0; i < arq.ntela; i++) {
            int posic = arq.tela[i].posic;
            if (arq.tela[i].col == 1) {
                switch (arq.tela[i].var) {
                case 1:
                    escreveIni << " Pressao na Linha de Producao (kgf/cm2), Celula " << posic << " = " << celula[posic].pres
                               << endl;
                    break;
                case 2:
                    escreveIni << " Temperatura na Linha de Producao (C), Celula " << posic << " = " << celula[posic].temp
                               << endl;
                    break;
                case 3:
                    escreveIni << " Fracao de Vazio na Linha de Producao (-), Celula " << posic << " = " << celula[posic].alf
                               << endl;
                    break;
                case 4:
                    escreveIni << " Fracao Beta na Linha de Producao (-), Celula " << posic << " = " << celula[posic].bet
                               << endl;
                    break;
                case 5:
                    escreveIni << " Velocidade Superficial de Gas na Linha de Producao (m/s), Celula " << posic << " = "
                               << (celula[posic].QG / celula[posic].duto.area) << endl;
                    break;
                case 6:
                    escreveIni << " Velocidade Superficial de Liquido na Linha de Producao (m/s), Celula " << posic << " = "
                               << (celula[posic].QL / celula[posic].duto.area) << endl;
                    break;
                }
            } else {
                switch (arq.tela[i].var) {
                case 1:
                    escreveIni << " Pressao na Linha de Servico (kgf/cm2), Celula " << posic << " = " << celulaG[posic].pres
                               << endl;
                    break;
                case 2:
                    escreveIni << " Temperatura na Linha de Servico (C), Ceula " << posic << " = " << celulaG[posic].temp
                               << endl;
                    break;
                case 3:
                    escreveIni << " Velocidade de Gas na Linha de Servico (m/s), Celula " << posic << " = "
                               << (celulaG[posic].VGasR / celulaG[posic].duto.area) << endl;
                    break;
                }
            }
        }
        if (fabs((*vg1dSP).lixo5 - arq.tfinal) <= dt) {
            time_t now = time(0);
            tm *ltm = localtime(&now);
            int diaFim = (ltm->tm_mday);
            int horaFim;
            if (diaFim == diaIni)
                horaFim = ltm->tm_hour;
            else
                horaFim = ltm->tm_hour + 24;
            horaFim *= 3600;
            int minutoFim = 60 * ltm->tm_min;
            int segundoFim = ltm->tm_sec;
            int totalFim = horaFim + minutoFim + segundoFim;
            int totalIni = horaIni * 3600 + minutoIni * 60 + segundoIni;
            escreveIni << "     DURACAO    " << totalFim - totalIni << " segundos " << endl;
            escreveIni << "     Versao    " << versao << endl;
            if (arq.saidaClassica == 1) {
                srand(time(NULL));
                int frase = rand() % 15;
                escreveIni << "*******************************************************************************" << endl;
                escreveIni << "                                  UFA!!!!!!!!                                  " << endl;
                escreveIni << saidaTextoSis[frase] << endl;
                escreveIni << saidaSubTextoSis[frase] << endl;
                escreveIni << "*******************************************************************************" << endl;
            } else
                escreveIni << "                                 FIM                                  " << endl;
        }
        time_t now = time(0);
        tm *ltm = localtime(&now); ///////////Retirado de https://www.tutorialspoint.com/cplusplus/cpp_date_time.htm
        escreveIni << "datahora = ";
        escreveIni << ltm->tm_mday << "/";
        escreveIni << 1 + ltm->tm_mon << "/";
        escreveIni << 1900 + ltm->tm_year << " ";
        escreveIni << 0 + ltm->tm_hour << ":";
        escreveIni << 0 + ltm->tm_min << ":";
        escreveIni << 0 + ltm->tm_sec;
        escreveIni << endl;

        escreveIni.close();
    }

    kSP++;
    KontaImprime++;
    if ((*vg1dSP).chaverede == 0)
        (*vg1dSP).lixo5 += dt;
}

void SProd::ImprimeTrendPCab(int i, int nrede) {
    if (arq.ntendp > 0) {
        const auto t = [this](const char *pt, const char *en) {
            return output_i18n::tr(this->arq.idiomaSaida, pt, en);
        };
        ostringstream saidaT;
        if (indTramo < 0 && arq.AP == 0) {
            saidaT << pathPrefixoArqSaida << "TENDP" << "-" << round(arq.trendp[i].comp) << ".dat";
        } else if (indTramo < 0 && arq.AP == 1) {
            saidaT << pathPrefixoArqSaida << "TENDP-AP-" << round(arq.trendp[i].comp) << ".dat";
        } else {
            saidaT << pathPrefixoArqSaida << "Tramo" << indTramo << "-R-" << nrede << "-" << "TENDP" << "-" << arq.trendp[i].comp << ".dat";
        }
        string tmp = saidaT.str();
        ofstream escreveTrend(tmp.c_str(), ios_base::out);
        if (kimpT == 1) {
            double comprimento = 0;
            int posicn = arq.trendp[i].posic;
            for (int k = 0; k <= posicn; k++)
                comprimento += arq.celp[k].dx;
            escreveTrend << t("# Comprimento a partir do Fundo de Poco (m) = ", "# Length from Bottomhole (m) = ") << comprimento << endl;
            escreveTrend << t("# Rotulo = ", "# Label = ") << arq.trendp[i].rotulo << endl;
            escreveTrend << t("# Indice da Celula = ", "# Cell index = ") << arq.trendp[i].posic << endl;
            if (indTramo < 0 && arq.AP == 1)
                escreveTrend << t(" Sequencia AP ;", " SA sequence ;");
            escreveTrend << t(" Tempo (s) ;", " Time (s) ;");
            if (arq.trendp[i].pres == 1)
                escreveTrend << t(" Pressao (kgf/cm2) ;", " Pressure (kgf/cm2) ;");
            if (arq.trendp[i].temp == 1)
                escreveTrend << t(" Temperatura (C) ;", " Temperature (C) ;");
            if (arq.trendp[i].hol == 1)
                escreveTrend << t(" Holdup de liquido (-) ;", " Liquid holdup (-) ;");
            if (arq.trendp[i].FVH == 1)
                escreveTrend << t(" Fracao Volumetrica Hidrato (-) ;", " Hydrate volumetric fraction (-) ;"); // solver de Hidratos - chris
            if (arq.trendp[i].bet == 1)
                escreveTrend << t(" Fracao vol. de liquido complementar (-) ;", " Complementary liquid vol. fraction (-) ;");
            if (arq.trendp[i].ugs == 1)
                escreveTrend << t(" Velocidade superficial do gas (m/s) ;", " Gas superficial velocity (m/s) ;");
            if (arq.trendp[i].uls == 1)
                escreveTrend << t(" Velocidade superficial do liquido (m/s) ;", " Liquid superficial velocity (m/s) ;");
            if (arq.trendp[i].ug == 1)
                escreveTrend << t(" Velocidade do gas (m/s) ;", " Gas velocity (m/s) ;");
            if (arq.trendp[i].ul == 1)
                escreveTrend << t(" Velocidade do liquido (m/s) ;", " Liquid velocity (m/s) ;");
            if (arq.trendp[i].arra == 1)
                escreveTrend << t(" Indicador de arranjo de fases (-) ;", " Phase pattern indicator (-) ;");
            if (arq.trendp[i].viscl == 1)
                escreveTrend << t(" Viscosidade do Liquido (cP) ;", " Liquid viscosity (cP) ;");
            if (arq.trendp[i].viscg == 1)
                escreveTrend << t(" Viscosidade do Gas (cP) ;", " Gas viscosity (cP) ;");
            if (arq.trendp[i].rhog == 1)
                escreveTrend << t(" Massa Especifica do Gas (kg/m3) ;", " Gas density (kg/m3) ;");
            if (arq.trendp[i].rhol == 1)
                escreveTrend << t(" Massa Especifica do Liquido (kg/m3) ;", " Liquid density (kg/m3) ;");
            if (arq.trendp[i].rhoMix == 1)
                escreveTrend << t(" Massa Especifica da Mistura (kg/m3) ;", " Mixture density (kg/m3) ;");
            if (arq.trendp[i].masg == 1)
                escreveTrend << t(" Vazao Massica do Gas (kg/s) ;", " Gas mass flow rate (kg/s) ;");
            if (arq.trendp[i].masl == 1)
                escreveTrend << t(" Vazao Massica do Liquido (kg/s) ;", " Liquid mass flow rate (kg/s) ;");
            if (arq.trendp[i].c0 == 1)
                escreveTrend << t(" Coeficiente de distribuição: C0 (-) ;", " Distribution coefficient: C0 (-) ;");
            if (arq.trendp[i].ud == 1)
                escreveTrend << t(" Velocidade de escorregamento: Ud (m/s) ;", " Slip velocity: Ud (m/s) ;");
            if (arq.trendp[i].RGO == 1)
                escreveTrend << " RGO (Sm3/Sm3) ;";
            if (arq.trendp[i].deng == 1)
                escreveTrend << t(" Densidade do Gas (-) ;", " Gas specific gravity (-) ;");
            if (arq.trendp[i].yco2 == 1)
                escreveTrend << t(" Fracao Molar de CO2 (-) ;", " CO2 molar fraction (-) ;");
            if (arq.trendp[i].calor == 1)
                escreveTrend << t(" Fluxo de calor entre escoamento e parede (W/m) ;", " Heat flow between flow and wall (W/m) ;");
            if (arq.trendp[i].masstrans == 1)
                escreveTrend << t(" Transferencia de Massa entre Fases (kg / [s m]) ;", " Interphase mass transfer (kg / [s m]) ;");
            if (arq.trendp[i].qlst == 1)
                escreveTrend << t(" Vazao volumetrica standard de oleo morto (Sm3/d) ;", " Standard dead oil volumetric flow rate (Sm3/d) ;");
            if (arq.trendp[i].qlwst == 1)
                escreveTrend << t(" Vazao volumetrica standard de oleo morto + agua (Sm3/d) ;", " Standard dead oil + water volumetric flow rate (Sm3/d) ;");
            if (arq.trendp[i].qlstTot == 1)
                escreveTrend << t(" Vazao volumetrica standard de oleo morto + agua + liquido complementar (Sm3/d) ;", " Standard dead oil + water + complementary liquid volumetric flow rate (Sm3/d) ;");
            if (arq.trendp[i].qgst == 1)
                escreveTrend << t(" Vazao volumetrica standard de gas livre + dissolvido (Sm3/d) ;", " Standard free + dissolved gas volumetric flow rate (Sm3/d) ;");
            if (arq.trendp[i].api == 1)
                escreveTrend << t(" Grau API (-) ;", " API gravity (-) ;");
            if (arq.trendp[i].bsw == 1)
                escreveTrend << " BSW (-) ;";
            if (arq.trendp[i].hidro == 1)
                escreveTrend << t(" Termo Hidrostatico (Pa/m) ;", " Hydrostatic term (Pa/m) ;");
            if (arq.trendp[i].fric == 1)
                escreveTrend << t(" Termo Friccao (Pa/m) ;", " Friction term (Pa/m) ;");
            if (arq.trendp[i].dengD == 1)
                escreveTrend << t(" Densidade Gas Dissolvido In Situ (-) ;", " In-situ dissolved gas specific gravity (-) ;");
            if (arq.trendp[i].dengL == 1)
                escreveTrend << t(" Densidade Gas Livre In Situ (-) ;", " In-situ free gas specific gravity (-) ;");
            if (arq.trendp[i].mlFonte == 1)
                escreveTrend << t(" Fonte massica - Liq. (Hidrocarb+Agua) (kg/s);", " Mass source - Liq. (Hydrocarbon+Water) (kg/s);");
            if (arq.trendp[i].mgFonte == 1)
                escreveTrend << t(" Fonte massica - Gas (kg/s);", " Mass source - Gas (kg/s);");
            if (arq.trendp[i].mcFonte == 1)
                escreveTrend << t(" Fonte massica - Liq. Complementar (kg/s);", " Mass source - Complementary liquid (kg/s);");
            if (arq.trendp[i].dpB == 1)
                escreveTrend << t(" Incremento de pressao de Bombeio (kgf/cm2);", " Pump pressure increment (kgf/cm2);");
            if (arq.trendp[i].potB == 1)
                escreveTrend << t(" Potencia de Bombeio (kW);", " Pump power (kW);");
            if (arq.trendp[i].tempChokeJus == 1)
                escreveTrend << t(" Temperatura a Jusante do Choke de Superficie (C);", " Surface choke downstream temperature (C);");
            if (arq.trendp[i].reyi == 1)
                escreveTrend << t(" Reynolds interno da mistura (-) ;", " Internal mixture Reynolds (-) ;");
            if (arq.trendp[i].reye == 1)
                escreveTrend << t(" Reynolds externo (-) ;", " External Reynolds (-) ;");
            if (arq.trendp[i].Fr == 1)
                escreveTrend << t(" Froud (-) ;", " Froude (-) ;");
            if (arq.trendp[i].grashi == 1)
                escreveTrend << t(" Grashof interno da mistura (-) ;", " Internal mixture Grashof (-) ;");
            if (arq.trendp[i].grashe == 1)
                escreveTrend << t(" Grashof externo (-) ;", " External Grashof (-) ;");
            if (arq.trendp[i].nusi == 1)
                escreveTrend << t(" Nusselt interno da mistura (-) ;", " Internal mixture Nusselt (-) ;");
            if (arq.trendp[i].nuse == 1)
                escreveTrend << t(" Nusselt externo (-) ;", " External Nusselt (-) ;");
            if (arq.trendp[i].hi == 1)
                escreveTrend << t(" Coeficiente de pelicula interno da mistura (W/(m2.K)) ;", " Internal mixture film coefficient (W/(m2.K)) ;");
            if (arq.trendp[i].he == 1)
                escreveTrend << t(" Coeficiente de pelicula externo (W/(m2.K)) ;", " External film coefficient (W/(m2.K)) ;");
            if (arq.trendp[i].pri == 1)
                escreveTrend << t(" Prandtl interno da mistura (-) ;", " Internal mixture Prandtl (-) ;");
            if (arq.trendp[i].pre == 1)
                escreveTrend << t(" Prandtl externo (-) ;", " External Prandtl (-) ;");
            if (arq.trendp[i].Rs == 1)
                escreveTrend << t(" Razao de Solubilidade (-) ;", " Solubility ratio (-) ;");
            if (arq.trendp[i].Bo == 1)
                escreveTrend << t(" Fator Volume de Formacao (-) ;", " Formation volume factor (-) ;");
            if (arq.trendp[i].volMonM1PT == 1)
                escreveTrend << t(" Volume de liquido a montante da Master1, a PT, m3 ;", " Liquid volume upstream of Master1, at PT, m3 ;");
            if (arq.trendp[i].volJusM1PT == 1)
                escreveTrend << t(" Volume de liquido a jusante da Master1, a PT, m3 ;", " Liquid volume downstream of Master1, at PT, m3 ;");
            if (arq.trendp[i].volMonM1ST == 1)
                escreveTrend << t(" Volume de liquido a montante da Master1, standard, m3 ;", " Liquid volume upstream of Master1, standard, m3 ;");
            if (arq.trendp[i].volJusM1ST == 1)
                escreveTrend << t(" Volume de liquido a jusante da Master1, standard, m3 ;", " Liquid volume downstream of Master1, standard, m3 ;");
            if (arq.trendp[i].inventarioGas == 1)
                escreveTrend << t(" Inventario de Gas em toda a tubulação, standard, m3 ;", " Gas inventory in whole tubing, standard, m3 ;");
            if (arq.trendp[i].inventarioLiq == 1)
                escreveTrend << t(" Inventario de Liquido em toda a tubulação, standard, m3 ;", " Liquid inventory in whole tubing, standard, m3 ;");
            if (arq.trendp[i].diamInt == 1)
                escreveTrend << t(" Diametro Interno da tubulacao, m ;", " Tubing inner diameter, m ;");
            if (arq.trendp[i].TempParede == 1)
                escreveTrend << t(" Temperatura Interna da Parede, C ;", " Internal wall temperature, C ;");
            if (arq.trendp[i].subResfria == 1)
                escreveTrend << t(" Subresfriamento, C ;", " Subcooling, C ;");
            if (arq.trendp[i].dadosParafina == 1) {
                escreveTrend << t(" TIAC (C) C;", " TIAC (C) C;");
                escreveTrend << t(" Cp Parafina (J/[kg C]) C;", " Paraffin Cp (J/[kg C]) C;");
                escreveTrend << t(" Condutividade Termica Parafina (W / [m K]) C;", " Paraffin thermal conductivity (W / [m K]) C;");
                escreveTrend << t(" Massa Especifica Parafina (kg/m3) C;", " Paraffin density (kg/m3) C;");
                escreveTrend << t(" Massa molar do Liquido Parafina (kg/mol) C;", " Paraffin liquid molar mass (kg/mol) C;");
                escreveTrend << t(" Difusividade Massica Parafina (m2/s) C;", " Paraffin mass diffusivity (m2/s) C;");
                escreveTrend << t(" Fluxo Massico de Parafina Total (kg/(m2-s)) C;", " Total paraffin mass flux (kg/(m2-s)) C;");
                escreveTrend << t(" Fluxo Massico de Parafina por Difusao (kg/(m2-s)) C;", " Paraffin diffusive mass flux (kg/(m2-s)) C;");
                escreveTrend << t(" Vazao Massica de Parafina por Difusao (kg/(s)) C;", " Paraffin mass rate (kg/(s)) C;");
                escreveTrend << t(" Gradiente de concentracao de parafina (1/m) C;", " Paraffin concentration gradient (1/m) C;");
                escreveTrend << t(" Condutividade do deposito (W/(m-K)) C;", " Deposit conductivity (W/(m-K)) C;");
                escreveTrend << t(" Temperatura da Interface do deposito (C) C;", " Deposit interface temperature (C) C;");
            }
            if (arq.trendp[i].autoVal == 1) {
                for (int konta1 = 0; konta1 < 3; konta1++) {
                    escreveTrend << t(" Celeridade, familia de onda ", " Celerity, wave family ") << konta1 << t(" m/s ;", " m/s ;");
                }
            }
            if (arq.trendp[i].autoVel == 1) {
                for (int konta1 = 0; konta1 < 3; konta1++) {
                    for (int konta2 = 0; konta2 < 3; konta2++)
                        escreveTrend << t(" Componente do autovetor, condicao adiabatica, familia de onda = ", " Eigenvector component, adiabatic condition, wave family = ") << konta1 << t("termo = ", " term = ") << konta2 << " ;";
                }
            }
            if (arq.trendp[i].flutuacao == 1) {
                for (int konta1 = 0; konta1 < 3; konta1++) {
                    escreveTrend << t(" Componente de flutuacao da familia de onda ", " Fluctuation component of wave family ") << konta1 << " ;";
                }
            }
            escreveTrend << endl;
        }
        escreveTrend.close();
        // caso nao seja simulacao POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            arqRelatorioPerfis << tmp.c_str() << endl;
            arqRelatorioPerfis.flush();
        }
    }
}
void SProd::ImprimeTrendP(int i, int nrede) {
    if (arq.ntendp > 0) {
        int size = ntrend[i] - ntrendB[i] + 1;
        FullMtx<double> saidatrend(size, arq.nvartrendp[i] + 1);
        for (int k = 0; k < size; k++)
            for (int j = 0; j <= arq.nvartrendp[i]; j++)
                saidatrend[k][j] = MatTrendP[i][k + ntrendB[i]][j];
        ostringstream saidaT;
        if (indTramo < 0 && arq.AP == 0) {
            saidaT << pathPrefixoArqSaida << "TENDP" << "-" << arq.trendp[i].comp << ".dat";
        } else if (indTramo < 0 && arq.AP == 1) {
            saidaT << pathPrefixoArqSaida << "TENDP-AP-" << round(arq.trendp[i].comp) << ".dat";
        } else {
            saidaT << pathPrefixoArqSaida << "Tramo" << indTramo << "-R-" << nrede << "-" << "TENDP" << "-" << arq.trendp[i].comp << ".dat";
        }
        string tmp = saidaT.str();
        ofstream escreveTrend(tmp.c_str(), ios_base::app);
        int nc = saidatrend.col();
        int nl = saidatrend.lin();
        if (indTramo < 0 && arq.AP == 1)
            nc++;
        for (int i = 0; i < nl; i++) {
            if (saidatrend[i][0] <= -9999)
                break;
            for (int j = 0; j < nc; j++) {
                escreveTrend.width(20);
                escreveTrend.precision(19);
                if (indTramo < 0 && arq.AP == 1) {
                    if (j == 0)
                        escreveTrend << (*vg1dSP).sequenciaAP << " ; ";
                    else
                        escreveTrend << saidatrend[i][j - 1] << " ; ";
                } else
                    escreveTrend << saidatrend[i][j] << " ; ";
            }
            escreveTrend << endl;
        }
        escreveTrend.close();
        // caso nao seja simulacao POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            arqRelatorioPerfis << tmp.c_str() << endl;
            arqRelatorioPerfis.flush();
        }
    }
}

void SProd::ImprimeTrendGCab(int i, int nrede) {
    if (arq.ntendg > 0 && arq.lingas > 0) {
        const auto t = [this](const char *pt, const char *en) {
            return output_i18n::tr(this->arq.idiomaSaida, pt, en);
        };
        ostringstream saidaT;
        if (indTramo < 0 && arq.AP == 0)
            saidaT << pathPrefixoArqSaida << "TENDG" << "-" << arq.trendg[i].comp << ".dat";
        else if (indTramo < 0 && arq.AP == 1) {
            saidaT << pathPrefixoArqSaida << "TENDG-AP-" << arq.trendg[i].comp << ".dat";
        } else
            saidaT << pathPrefixoArqSaida << "Tramo" << indTramo << "-R-" << nrede << "-" << "TENDG" << "-" << arq.trendg[i].comp << ".dat";
        string tmp = saidaT.str();
        ofstream escreveTrend(tmp.c_str(), ios_base::out);
        if (kimpT == 1) {
            double comprimento = 0;
            int posicn = arq.trendg[i].posic;
            for (int k = 0; k <= posicn; k++)
                comprimento += arq.celg[k].dx;
            escreveTrend << t("# Comprimento a partir da Plataforma (m) = ", "# Length from Platform (m) = ") << comprimento << endl;
            escreveTrend << t("# Rotulo = ", "# Label = ") << arq.trendg[i].rotulo << endl;
            escreveTrend << t("# Indice da Celula = ", "# Cell index = ") << arq.trendg[i].posic << endl;
            if (indTramo < 0 && arq.AP == 1)
                escreveTrend << t(" Sequencia AP ;", " SA sequence ;");
            escreveTrend << t(" Tempo (s) ;", " Time (s) ;");
            if (arq.trendg[i].pres == 1)
                escreveTrend << t(" Pressao (kgf/cm2) ;", " Pressure (kgf/cm2) ;");
            if (arq.trendg[i].temp == 1)
                escreveTrend << t(" Temperatura (C) ;", " Temperature (C) ;");
            if (arq.trendg[i].ugs == 1)
                escreveTrend << t(" Velocidade superficial do gas (m/s) ;", " Gas superficial velocity (m/s) ;");
            if (arq.trendg[i].ug == 1)
                escreveTrend << t(" Velocidade do gas (m/s) ;", " Gas velocity (m/s) ;");
            if (arq.trendg[i].tens == 1)
                escreveTrend << t(" Tensao Cisalhante (N/m2) ;", " Shear stress (N/m2) ;");
            if (arq.trendg[i].viscg == 1)
                escreveTrend << t(" Viscosidade do Gas (cP) ;", " Gas viscosity (cP) ;");
            if (arq.trendg[i].rhog == 1)
                escreveTrend << t(" Massa Especifica do Gas (kg/m3) ;", " Gas density (kg/m3) ;");
            if (arq.trendg[i].masg == 1)
                escreveTrend << t(" Vazao Massica do Gas (kg/s) ;", " Gas mass flow rate (kg/s) ;");
            if (arq.trendg[i].hidro == 1)
                escreveTrend << t(" Termo Hidrostatico (Pa/m) ;", " Hydrostatic term (Pa/m) ;");
            if (arq.trendg[i].FVHG == 1)
                escreveTrend << t(" FVHG (-) ;", " FVHG (-) ;");
            if (arq.trendg[i].fric == 1)
                escreveTrend << t(" Termo Friccao (Pa/m) ;", " Friction term (Pa/m) ;");
            if (arq.trendg[i].calor == 1)
                escreveTrend << t(" Fluxo de calor entre escoamento e parede (W/m) ;", " Heat flow between flow and wall (W/m) ;");
            if (arq.trendg[i].qgst == 1)
                escreveTrend << t(" Vazao volumetrica standard de Gas (Sm3/d) ;", " Standard gas volumetric flow rate (Sm3/d) ;");
            if (arq.trendg[i].pEstagVGL == 1)
                escreveTrend << t(" Pressao de Estagnacao VGL (kgf/cm²) ;", " VGL stagnation pressure (kgf/cm2) ;");
            if (arq.trendg[i].tEstagVGL == 1)
                escreveTrend << t(" Temperatura de Estagnacao VGL (C) ;", " VGL stagnation temperature (C) ;");
            if (arq.trendg[i].pGargVGL == 1)
                escreveTrend << t(" Pressao na Garganta VGL (kgf/cm²) ;", " VGL throat pressure (kgf/cm2) ;");
            if (arq.trendg[i].tGargVGL == 1)
                escreveTrend << t(" Temperatura na Garganta VGL (C) ;", " VGL throat temperature (C) ;");
            if (arq.trendg[i].velgarg == 1)
                escreveTrend << t(" Velocidade na VGL (m/s) ;", " Velocity in VGL (m/s) ;");
            if (arq.trendg[i].qVGL == 1)
                escreveTrend << t(" Vazao volumetrica na VGL (mÂ³/s) ;", " Volumetric flow rate in VGL (m3/s) ;");
            if (arq.trendg[i].reyi == 1)
                escreveTrend << t(" Reynolds interno (-) ;", " Internal Reynolds (-) ;");
            if (arq.trendg[i].reye == 1)
                escreveTrend << t(" Reynolds externo (-) ;", " External Reynolds (-) ;");
            if (arq.trendg[i].grashi == 1)
                escreveTrend << t(" Grashof interno (-) ;", " Internal Grashof (-) ;");
            if (arq.trendg[i].grashe == 1)
                escreveTrend << t(" Grashof externo (-) ;", " External Grashof (-) ;");
            if (arq.trendg[i].nusi == 1)
                escreveTrend << t(" Nusselt interno (-) ;", " Internal Nusselt (-) ;");
            if (arq.trendg[i].nuse == 1)
                escreveTrend << t(" Nusselt externo (-) ;", " External Nusselt (-) ;");
            if (arq.trendg[i].hi == 1)
                escreveTrend << t(" Coeficiente de pelicula interno (W/[m2 K]) ;", " Internal film coefficient (W/[m2 K]) ;");
            if (arq.trendg[i].he == 1)
                escreveTrend << t(" Coeficiente de pelicula externo (W/[m2 K]) ;", " External film coefficient (W/[m2 K]) ;");
            if (arq.trendg[i].pri == 1)
                escreveTrend << t(" Prandtl interno (-) ;", " Internal Prandtl (-) ;");
            if (arq.trendg[i].pre == 1)
                escreveTrend << t(" Prandtl externo (-) ;", " External Prandtl (-) ;");
            if (arq.trendg[i].diamInt == 1)
                escreveTrend << t(" Diametro Interno da tubulacao, m ;", " Tubing inner diameter, m ;");
            if (arq.trendg[i].TempParede == 1)
                escreveTrend << t(" Temperatura Interna da Parede, C ;", " Internal wall temperature, C ;");
            if (arq.trendg[i].subResfria == 1)
                escreveTrend << t(" Subresfriamento, C ;", " Subcooling, C ;");
            escreveTrend << endl;
        }
        escreveTrend << endl;
        escreveTrend.close();
        // caso nao seja simulacao POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            arqRelatorioPerfis << tmp.c_str() << endl;
            arqRelatorioPerfis.flush();
        }
    }
}

void SProd::ImprimeTrendG(int i, int nrede) {
    if (arq.ntendg > 0 && arq.lingas > 0) {
        int size = ntrendg[i] - ntrendgB[i] + 1;
        FullMtx<double> saidatrend(size, arq.nvartrendg[i] + 1);
        for (int k = 0; k < size; k++)
            for (int j = 0; j <= arq.nvartrendg[i]; j++)
                saidatrend[k][j] = MatTrendG[i][k + ntrendgB[i]][j];
        ostringstream saidaT;
        if (indTramo < 0 && arq.AP == 0)
            saidaT << pathPrefixoArqSaida << "TENDG" << "-" << arq.trendg[i].comp << ".dat";
        else if (indTramo < 0 && arq.AP == 1) {
            saidaT << pathPrefixoArqSaida << "TENDG-AP-" << arq.trendg[i].comp << ".dat";
        } else
            saidaT << pathPrefixoArqSaida << "Tramo" << indTramo << "-R-" << nrede << "-" << "TENDG" << "-" << arq.trendg[i].comp << ".dat";
        string tmp = saidaT.str();
        ofstream escreveTrend(tmp.c_str(), ios_base::app);
        int nc = saidatrend.col();
        int nl = saidatrend.lin();
        if (indTramo < 0 && arq.AP == 1)
            nc++;
        for (int i = 0; i < nl; i++) {
            if (saidatrend[i][0] <= -9999)
                break;
            for (int j = 0; j < nc; j++) {
                escreveTrend.width(20);
                escreveTrend.precision(19);
                if (indTramo < 0 && arq.AP == 1) {
                    if (j == 0)
                        escreveTrend << (*vg1dSP).sequenciaAP << " ; ";
                    else
                        escreveTrend << saidatrend[i][j - 1] << " ; ";
                } else
                    escreveTrend << saidatrend[i][j] << " ; ";
            }
            escreveTrend << endl;
        }
        escreveTrend.close();
        // caso nao seja simulacao POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            arqRelatorioPerfis << tmp.c_str() << endl;
            arqRelatorioPerfis.flush();
        }
    }
}

void SProd::ImprimeTrendTransPCab(int i) {

    if (arq.ntendtransp > 0) {
        const auto t = [this](const char *pt, const char *en) {
            return output_i18n::tr(this->arq.idiomaSaida, pt, en);
        };
        int poscel = arq.trendtransp[i].comp;
        int poscam = arq.trendtransp[i].camada - 1;
        int posdiscre = arq.trendtransp[i].discre - 1;
        ostringstream saidaT;
        if (indTramo < 0)
            saidaT << pathPrefixoArqSaida << "TENDTRANSP" << "-" << poscel << "-" << poscam << "-" << posdiscre << ".dat";
        else
            saidaT << pathPrefixoArqSaida << "Tramo" << indTramo << "-" << "TENDTRANSP" << "-" << poscel << "-" << poscam << "-" << posdiscre << ".dat";
        string tmp = saidaT.str();
        ofstream escreveTrend(tmp.c_str(), ios_base::out);
        if (kimpT == 1) {
            escreveTrend << t("# Rotulo = ", "# Label = ") << arq.trendtransp[i].rotulo << endl;
            escreveTrend << t(" Tempo (s) ; ", " Time (s) ; ");
            escreveTrend << t(" Temperatura (C) ;", " Temperature (C) ;");
            escreveTrend << endl;
        }
        escreveTrend.close();
        // caso nao seja simulacao POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            arqRelatorioPerfis << tmp.c_str() << endl;
            arqRelatorioPerfis.flush();
        }
    }
}

void SProd::ImprimeTrendTransP(int i) {
    if (arq.ntendtransp > 0) {
        int size = ntrendtrans[i] - ntrendtransB[i] + 1;
        FullMtx<double> saidatrend(size, 2);
        for (int k = 0; k < size; k++)
            for (int j = 0; j < 2; j++)
                saidatrend[k][j] = MatTrendTransP[i][k][j];
        int poscel = arq.trendtransp[i].comp;
        int poscam = arq.trendtransp[i].camada - 1;
        int posdiscre = arq.trendtransp[i].discre - 1;
        ostringstream saidaT;
        if (indTramo < 0)
            saidaT << pathPrefixoArqSaida << "TENDTRANSP" << "-" << poscel << "-" << poscam << "-" << posdiscre << ".dat";
        else
            saidaT << pathPrefixoArqSaida << "Tramo" << indTramo << "-" << "TENDTRANSP" << "-" << poscel << "-" << poscam << "-" << posdiscre << ".dat";
        string tmp = saidaT.str();
        ofstream escreveTrend(tmp.c_str(), ios_base::app);
        int nc = saidatrend.col();
        int nl = saidatrend.lin();
        for (int i = 0; i < nl; i++) {
            if (saidatrend[i][0] <= -9999)
                break;
            for (int j = 0; j < nc; j++) {
                escreveTrend.width(20);
                escreveTrend.precision(19);
                escreveTrend << saidatrend[i][j] << " ; ";
            }
            escreveTrend << endl;
        }
        escreveTrend << endl;
        escreveTrend.close();
        // caso nao seja simulacao POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            arqRelatorioPerfis << tmp.c_str() << endl;
            arqRelatorioPerfis.flush();
        }
    }
}

void SProd::ImprimeTrendTransGCab(int i) {
    if (arq.ntendtransg > 0 && arq.lingas > 0) {
        const auto t = [this](const char *pt, const char *en) {
            return output_i18n::tr(this->arq.idiomaSaida, pt, en);
        };
        int poscel = arq.trendtransg[i].comp;
        int poscam = arq.trendtransg[i].camada - 1;
        int posdiscre = arq.trendtransg[i].discre - 1;
        ostringstream saidaT;
        if (indTramo < 0)
            saidaT << pathPrefixoArqSaida << "TENDTRANSG" << "-" << poscel << "-" << poscam << "-" << posdiscre << ".dat";
        else
            saidaT << pathPrefixoArqSaida << "Tramo" << indTramo << "-" << "TENDTRANSP" << "-" << poscel << "-" << poscam << "-" << posdiscre << ".dat";
        string tmp = saidaT.str();
        ofstream escreveTrend(tmp.c_str(), ios_base::out);
        if (kimpT == 1) {
            escreveTrend << t("# Rotulo = ", "# Label = ") << arq.trendtransg[i].rotulo << endl;
            escreveTrend << t(" Tempo (s) ; ", " Time (s) ; ");
            escreveTrend << t(" Temperatura (C) ;", " Temperature (C) ;");
            escreveTrend << endl;
        }
        escreveTrend.close();
        // caso nao seja simulacao POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            arqRelatorioPerfis << tmp.c_str() << endl;
            arqRelatorioPerfis.flush();
        }
    }
}

void SProd::ImprimeTrendTransG(int i) {
    if (arq.ntendtransg > 0 && arq.lingas > 0) {
        int size = ntrendtrans[i] - ntrendtransB[i] + 1;
        FullMtx<double> saidatrend(size, 2);
        for (int k = 0; k < size; k++)
            for (int j = 0; j < 2; j++)
                saidatrend[k][j] = MatTrendTransG[i][k][j];
        int poscel = arq.trendtransg[i].comp;
        int poscam = arq.trendtransg[i].camada - 1;
        int posdiscre = arq.trendtransg[i].discre - 1;
        ostringstream saidaT;
        if (indTramo < 0)
            saidaT << pathPrefixoArqSaida << "TENDTRANSG" << "-" << poscel << "-" << poscam << "-" << posdiscre << ".dat";
        else
            saidaT << pathPrefixoArqSaida << "Tramo" << indTramo << "-" << "TENDTRANSP" << "-" << poscel << "-" << poscam << "-" << posdiscre << ".dat";
        string tmp = saidaT.str();
        ofstream escreveTrend(tmp.c_str(), ios_base::app);
        int nc = saidatrend.col();
        int nl = saidatrend.lin();
        for (int i = 0; i < nl; i++) {
            if (saidatrend[i][0] <= -9999)
                break;
            for (int j = 0; j < nc; j++) {
                escreveTrend.width(20);
                escreveTrend.precision(19);
                escreveTrend << saidatrend[i][j] << " ; ";
            }
            escreveTrend << endl;
        }
        escreveTrend << endl;

        escreveTrend.close();
        // caso nao seja simulacao POCO_INJETOR
        if (arq.tipoSimulacao != tipoSimulacao_t::poco_injetor) {
            arqRelatorioPerfis << tmp.c_str() << endl;
            arqRelatorioPerfis.flush();
        }
    }
}

double SProd::marchaProdPerm1(double pchute) {

    int corrigechute = 1;
    double alfini = 0.;
    double betini = 0.;

    // estimativa da fracao de vazio na primeira celula do sistema
    if (celula[0].acsr.tipo == 0) { // sem nenhuma fonte
        celula[0].temp = arq.celp[0].textern;
        alfini = 1.;
        betini = 0.;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
        }
    } else if (celula[0].acsr.tipo == 1) { // fonte de gas
        celula[0].temp = celula[0].acsr.injg.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.injg.FluidoPro.atualizaPropComp(pchute, celula[0].temp, -1, NULL, NULL, celula[0].acsr.injg.seco);
        }
        if (celula[0].acsr.injg.seco == 1) {
            alfini = 1.;
            betini = 0.;
        } else {
            double masgas = celula[0].acsr.injg.VMas(pchute, celula[0].temp);
            double tit;
            if (arq.flashCompleto != 2)
                tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
            else
                tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
            double masT = masgas / tit;
            tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(pchute, celula[0].temp);
            double qgas = masT * tit /
                          celula[0].acsr.injg.FluidoPro.MasEspGas(pchute, celula[0].temp);
            double qliq = masT * (1. - tit) /
                          celula[0].acsr.injg.FluidoPro.MasEspLiq(pchute, celula[0].temp);
            double qcomp = celula[0].acsr.injg.razCompGas *
                           celula[0].acsr.injg.QGas * celula[0].acsr.injg.fluidocol.MasEspFlu(1., 20.) /
                           celula[0].acsr.injg.fluidocol.MasEspFlu(pchute, celula[0].temp);
            qcomp /= 86400.;
            alfini = qgas / (qliq + qcomp + qgas);
            if ((fabs(qcomp) + fabs(qliq)) > 1e-15)
                betini = fabs(qcomp) / (fabs(qcomp) + fabs(qliq));
            else
                betini = 0.;
        }
    } else if (celula[0].acsr.tipo == 2) { // fonte de liquido, faz-se uma estimativa a partir
        // da vazÃƒÂ£o volumÃƒÂ©trica das fases, fracao de vazio = fracao de vazio sem escorregamento
        celula[0].temp = celula[0].acsr.injl.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.injl.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        double qgas = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                      (1. - celula[0].acsr.injl.FluidoPro.BSW) *
                      (celula[0].acsr.injl.FluidoPro.RGO -
                       celula[0].acsr.injl.FluidoPro.rDgD * celula[0].acsr.injl.FluidoPro.RS(pchute, celula[0].temp) * 6.29 / 35.31467) *
                      celula[0].acsr.injl.FluidoPro.Deng * 1.225 / celula[0].acsr.injl.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          (1. - celula[0].acsr.injl.FluidoPro.BSW) * celula[0].acsr.injl.FluidoPro.BOFunc(pchute, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          celula[0].acsr.injl.FluidoPro.BSW * celula[0].acsr.injl.FluidoPro.BAFunc(pchute, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet;
        alfini = qgas / (qliq + qgas);
        betini = celula[0].acsr.injl.bet;
    } else if (celula[0].acsr.tipo == 3) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.ipr.Tres;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.ipr.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        double qgas = celula[0].acsr.ipr.MasG(pchute, celula[0].temp) /
                      celula[0].acsr.ipr.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.ipr.MasL(pchute, celula[0].temp) /
                      celula[0].acsr.ipr.FluidoPro.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 15) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.radialPoro.tRes;
        celula[0].pres = pchute;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.radialPoro.flup.atualizaPropComp(pchute, celula[0].temp);
        }
        renovaFonte(0);
        double qgas = celula[0].acsr.radialPoro.fluxIniG /
                      celula[0].acsr.radialPoro.flup.MasEspGas(pchute, celula[0].temp);
        double qliq = (celula[0].acsr.radialPoro.fluxIni + celula[0].acsr.radialPoro.fluxIniA) /
                      celula[0].acsr.radialPoro.flup.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 16) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.poroso2D.dados.tRes;
        celula[0].pres = pchute;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.poroso2D.dados.flup.atualizaPropComp(pchute, celula[0].temp);
        }
        renovaFonte(0);
        double qgas = celula[0].acsr.poroso2D.dados.transfer.fluxIniG /
                      celula[0].acsr.poroso2D.dados.flup.MasEspGas(pchute, celula[0].temp);
        double qliq = (celula[0].acsr.poroso2D.dados.transfer.fluxIni + celula[0].acsr.poroso2D.dados.transfer.fluxIniA) /
                      celula[0].acsr.poroso2D.dados.flup.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 10) { // fonte de massa no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.injm.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.injm.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        if (celula[0].acsr.injm.condTermo == 0) {
            celula[0].pres = pchute;
            renovaFonte(0);
        }
        double qgas = celula[0].acsr.injm.MassG /
                      celula[0].acsr.injm.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.injm.MassP /
                          celula[0].acsr.injm.FluidoPro.MasEspLiq(pchute, celula[0].temp) +
                      celula[0].acsr.injm.MassC /
                          celula[0].acsr.injm.fluidocol.MasEspFlu(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else { // se nenhuma das opcoes, fracao de vazio=1
        celula[0].temp = arq.celp[0].textern;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
        }
        alfini = 1.;
        betini = 0.;
    }
    if (celula[0].acsr.tipo == 15) {
        celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
    } else if (celula[0].acsr.tipo == 16) {
        celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
    }
    if (fabs(alfini) < 1e-6)
        alfini = 0.;
    if (fabs(betini) < 1e-6)
        betini = 0.;

    // esta marcha e feita para quando se tem alguma fonte no inicio da tubulacao,
    // portanto, admite-se que o duto esta fechado e coloca-se uma fonte no centro da
    // primeira celula. As vazoes na fronteira esquerda da celula sÃ£o portanto = 0
    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].MComp = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;
    double masfim = 1.;
    double masfim0 = -10;
    double presteste = 1.;
    double presteste0 = -10;
    iterperm = 0;

    int limIter = 2; // limite de iteracoes quando a opcao de aceleracao da convergencia esta desligado. desaconselhavel
    // desligar esta opcao, os ganhos sao pouco e a convergencia se torna instavel, principalmente
    // quando o tramo faz perte de um sistema de redes
    if (arq.AceleraConvergPerm == 1) { // opcao aceleracao de convergencia ligada
        limIter = 1;                   // em geral faz-se apenas duas iteracoes de marcha para um determinado chute
        if (arq.lingas == 1 && celulaG[0].tipoCC == 0)
            limIter = 1; // no caso de se ter
        // uma condicao de contorno na injecao de gas = pressao, observou-se que o acoplamento dinamico
        // entre a linha de gas e de producao e mais difoicil, para se conseguir um sistema
        // melhor acoplado, deve-se fazer uma marcha iterativa a mais
    }
    // arq.CriterioConvergPerm Ã© um criterio de convergencia da marcha, so faz sentido
    // quando a aceleracao de convergencia esta desligada. Observe que esta convergencia nÃ£o
    // e de fato a conevregencia do problema, e apenas um repeticao de marcha para um determinado
    // chute de pressao ou de vazao no inicio da tubulacao. O que a convergencia busca de fato e
    // determinar qual a pressao ou vazao de fundo que satisfaz as condicoes de contorno no fim
    // da tubulacao, esta busca e feita nos metodos de busca.
    while ((fabs(masfim - masfim0) / fabs(masfim) > arq.CriterioConvergPerm ||
            fabs(presteste - presteste0) / fabs(presteste) > arq.CriterioConvergPerm) &&
           iterperm < limIter) {

        masfim0 = masfim;
        presteste0 = presteste;
        if (iterperm == 0 && arq.lingas > 0 && arq.nvalvgas > 0 && verificaAcop == 1)
            IniciaconectaColunaPerm(); // antes de iniciar a primeira iteracao de marcha,
        // faz-se uma estimativa inicial de como se da o acopamento termico entre a coluna e o anular,
        // caso se tenha linha de gas
        else if (arq.lingas > 0 && arq.nvalvgas > 0 && verificaAcop == 1)
            conectaColunaPerm(); // acoplamento termico feito com valores de pressao e temperatura
        // obtidas na primeira iteracao de marcha
        int i;
        corrigechute = 1;
        while (corrigechute == 1) { // opcao antiga, ja nao tem mais efeito
            // efetivamente, este while sempre so e feito uma vez, quando a marcha consegue ir ate
            // a ultima celula sem problemas, caso ocorra algum problema, a marcha e finalizada e
            // sai do metodo retornando ou 1e10 ou -1e10

            // inicializando as pressoes e fracoes volumetricas das celulas iniciais, centro de celula
            // e fronteira de celula
            celula[0].presauxL = pchute;
            celula[0].presLini = pchute;
            celula[0].presL = pchute;
            celula[0].pres = pchute;
            celula[1].presL = pchute;
            celula[0].presini = pchute;
            celula[1].presLini = pchute;
            celula[0].presaux = pchute;
            celula[1].presauxL = pchute;

            celula[0].alf = alfini;
            celula[0].alfini = alfini;
            celula[0].bet = betini;
            celula[0].betini = betini;
            celula[1].alfL = celula[0].alf;
            celula[1].alfLini = celula[0].alf;
            celula[0].alfPigD = celula[0].alf;
            celula[0].alfPigDini = celula[0].alf;
            celula[0].alfPigE = celula[0].alf;
            celula[0].alfPigEini = celula[0].alf;
            celula[1].betL = celula[0].bet;
            celula[1].betLini = celula[0].bet;
            celula[0].betPigD = celula[0].bet;
            celula[0].betPigDini = celula[0].bet;
            celula[0].betPigE = celula[0].bet;
            celula[0].betPigEini = celula[0].bet;
            celula[0].betI = celula[0].bet;
            celula[1].betLI = celula[0].bet;
            // verifica se ja existe algum problema no inicio da marcha
            if (celula[0].pres <= 0.1 ||
                (arq.usaTabela == 1 && (arq.tabent.pmin - celula[0].pres) > (*vg1dSP).localtiny))
                return -1e10;
            else if ((celula[0].acsr.tipo == 3 &&
                      (celula[0].acsr.ipr.Pres - celula[0].pres) < (*vg1dSP).localtiny))
                return 1e10;
            // IniciaVazValvGasPerm e um metodo que faz uma estimativa da vazao na valvula de GL
            // quando ainda nao foi feita a marcha na linha de gas. Neste caso, ele recebe o
            // indice da celula de producao e verifica se nesta celula existe uma VGL, se existir,
            // caso a condicao na linha de gas seja vazao injetada, divide a vazao injetada pelo numero de
            // valvulas e indica este valor para a VGL relacionada a celula de producao
            // caso a condicao seja pressao de injecao, faz-se uma estimativa da pressao na linha de gas
            // na posicao da VGL por hidrotatica e com isto se calcula a vazao de injecao da VGL
            if (arq.lingas > 0 && arq.nvalvgas > 0 && iterperm == 0 && monitConvPerm > 0.1) {
                if (celulaG[0].tipoCC == 0)
                    hidroLinServ();
                IniciaVazValvGasPerm(0);
            }
            i = 1;
            // inicio da marcha propriamente dita
            while (i <= ncel && celula[i - 1].pres >= 0.1 && fabs(pchute - celula[0].pres) < (*vg1dSP).localtiny) {

                RenovaPresPermMon(i, 0); // avanco da marcha para obter a pressao na fronteira esquerda
                // da celula i
                // teste para ver se ocorreu algum problema:
                if (arq.usaTabela == 1 && (arq.tabent.pmax - celula[i].presaux) < (*vg1dSP).localtiny)
                    return 1e10;
                if (celula[i].presaux <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i].presaux) > (*vg1dSP).localtiny)) {
                    return -1e10;
                }
                atualizaPeriPmonProd(i); // atualizacao da pressao da fronteira esquerda,
                // caso exista alguma BCS ou incremento de pressao
                if (i == 312) {
                    int para;
                    para = 0;
                }
                if (arq.flashCompleto != 2)
                    RenovaMassPerm(i); // verifica se existe alguma fonte na celula anterior, com isto, atualiza
                // as vazoes massica na fronteira a esquerda, alÃ©m das propriedades dos fluidos,
                // densidade do gas, RGO, API, BSW, beta
                else
                    RenovaMassPermComp(i);

                if (arq.acopColAnulPermForte == 0 || arq.lingas == 0 || monitConvPerm > 0.1)
                    RenovaTempPerm(i, 0); // faz o avanco da temperatura, da celula i-1 para a celula i
                // verifica se teve algum problema nos limites de temperatura
                // caso se esteja trabalhando com tabela PVTSim
                if (arq.usaTabela == 1 && (celula[i].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                    celula[i].temp = arq.tabent.tmin;
                atualizaPeriTempProd(i); // mera atualizacao de atributos de temperatura a esquerda e a direita
                RenovaPresPermJus(i, 0); // evolui a pressao  fronteira a esquerda da celula i para o
                // seu centro de celula
                // verifica se ocorreu algum problema nesta evolucao de de pressao no centro da
                // celula
                if (arq.usaTabela == 1 && (arq.tabent.pmax - celula[i].pres) < (*vg1dSP).localtiny) {
                    return 1e10;
                }
                if (celula[i].pres <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i].pres) > (*vg1dSP).localtiny)) {
                    return -1e10;
                }
                atualizaPeriPjusProd(i); // mera atualizacao de atributos que guardam valores de pressao
                // das celulas a esquerda e a direita
                for (int j = 0; j < arq.nvalvgas; j++) { // reavaliacao da vazao da valvula de gas lift, quando
                    // a celula tem uma.
                    // P.S. parece uma acao desnecessÃ¡ria e talvez atÃ© um complicador
                    // densecessario, em vavliacao
                    if (posicVGLP[j] == i) {
                        int k = posicVGLG[j];
                        calcVazGasPerm(k);
                    }
                }
                if (arq.tipoFluido == 0)
                    RenovaTransMassPerm(i - 1);
                else
                    RenovaTransMassPermGas(i - 1); // caso seja uma tabela PVTSim, calcula-se a
                // taxa de transferÃªncia de massa entre as fases para o uso no calculo de
                // calor latente da equacao de energia
                if (arq.ordperm > 1) { // correcao de segunda ordem
                    double D0presaux = celula[i].presaux - celula[i - 1].pres;
                    double D0pres = celula[i].pres - celula[i].presaux;
                    double D0temp = celula[i].temp - celula[i - 1].temp;
                    RenovaPresPermMon(i, 1);
                    atualizaPeriPmonProd(i);
                    RenovaMassPerm(i);
                    RenovaTempPerm(i, 1);
                    if (isnan(celula[i].temp)) {
                        if (arq.transiente == 0 && arq.AP == 0)
                            // neste caso, se finaliza a simulacao, nao tem um transiente
                            // a ser feito a seguir e nem se estÃ¡ em uma rede
                            NumError(
                                "Temperatrura na linha de producao com valor NaN em marchaProdPerm1");
                        else {
                            // apresenta apenas um aviso
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                            // se for em uma iteracao de rede, apos a primeira iteracao
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            // se logo apos tem uma simulacao transiente ou se esta na primeira iteracao de rede
                            else
                                return 1.1e10;
                        }
                    }
                    RenovaPresPermJus(i, 1);
                    celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                    celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                    celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                    atualizaPeriPjusProd(i);
                    atualizaPeriPmonProd(i);
                    atualizaPeriTempProd(i);
                    RenovaMassPerm(i);
                    if (arq.tipoFluido == 0)
                        RenovaTransMassPerm(i - 1);
                    else
                        RenovaTransMassPermGas(i - 1);
                }

                // apÃ³s se atingir a pressao no centro da celula i, primeira iteracao de marcha
                // verifica-se se existe uma VGL em i e faz-se uma estimativa inicial da Vazao de
                // GL (caso exista linha de gas). Observar que isto sÃ³ Ã© feito para a iteracao zero.
                if (arq.lingas > 0 && arq.nvalvgas > 0 && iterperm == 0 && monitConvPerm > 0.1)
                    IniciaVazValvGasPerm(i);
                i++;

                if (isnan(celula[i - 1].pres) || isnan(celula[i - 1].temp) || isnan(celula[i - 1].alf)) {
                    return 1e10;
                }

                // teste para verificar se a pressao do centro de celula ficou acima
                // da pressao estatica de uma eventual IPR
                if (celula[i - 1].pres <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i - 1].pres) > (*vg1dSP).localtiny)) {
                    return -1e10;
                } else if ((celula[i - 1].acsr.tipo == 3 &&
                            ((celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres) < (*vg1dSP).localtiny) && i == 1)) {
                    return 1e10;
                }
            }
            if (i == ncel + 1)
                corrigechute = 0; // fim da marcha
        }
        // apÃ³s o fim da marcha da linha de produÃ§Ã£o, Ã© feita a marcha da linha de gas
        // caso exista
        if (pchute > 0 && arq.lingas > 0 && arq.nvalvgas > 0) {
            if (celulaG[0].tipoCC == 0) {            // marcha para o caso, pressao de injecao
                if (arq.chokes.abertura[0] >= 0.2) { // choke de injecao inativo
                    for (int iter = 0; iter < 1; iter++) {
                        marchaGasPerm1();
                    }
                } else
                    buscaGasPresPerm3(); // choke de injecao ativo
            } else
                buscaGasPresPerm2(); // marcha na linha de gas para o caso de vazao de injecao
        }

        if (arq.acopColAnulPermForte > 0 && arq.lingas > 0 && semTermo == 0) {
            atualizaProp();
            atualizaVelTermPerm();
            calcDTPseudoTrans();
            conectaColuna();
            for (int kontaPseudo = 0; kontaPseudo < arq.acopColAnulPermForte; kontaPseudo++) {
                for (int iterm = 0; iterm <= ncelGas; iterm++)
                    celulaG[iterm].tempini = celulaG[iterm].temp;
                for (int iterm = 1; iterm <= ncelGas; iterm++) {
                    calctempGas(iterm, celulaG[iterm - 1].tempini, 1);
                }
                celula[0].tempini = celula[0].temp;
                celula[1].tempLini = celula[1].tempL;
                celula[1].tempL = celula[0].temp;
                for (int iterm = 1; iterm <= ncel; iterm++) {
                    celula[iterm].tempini = celula[iterm].temp;
                }
                for (int iterm = 1; iterm <= ncel; iterm++) {
                    calctemp(iterm, celula[iterm].tempini, 1);
                }
                atualizaProp();
                calcDTPseudoTrans();
                conectaColuna();
            }
        }

        masfim = celula[ncel - 1].MC; // guarda valor de vazao para se calcular o erro, quando a
        // opcao de acelerador de convergencia esta desligado
        presteste = celula[ncel].pres; // guarda valor de pressao para se calcular o erro, quando a
        // opcao de acelerador de convergencia esta desligado
        iterperm++; // atualiza a ieteracao da marcha
        if (iterperm > 200 && arq.AP == 0)
            NumError("ConvergÃƒÂªncia em marchaProdPerm1 atingiu maximo de iteracoes");
        else if (iterperm > 200)
            return 1.1e10;
    }

    double corrigePresF = 0.;
    if (((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1) && arq.corrigeContSep == 1)
        corrigePresF = RenovaPresPermNcel();

    monitConvPermBase = pGSup;
    return pGSup - (celula[ncel].pres + corrigePresF); // caso a marcha tenha conseguido ir atÃ© a Ãºltima celula,
    // retorna a diferenca entre a pressao a montante do choke e a pressao da ultima celula
    // calculada pela marcha
}

double SProd::marchaProdPerm1Rev(double pchute) {

    int corrigechute = 1;
    double alfini = 0.;
    double betini = 0.;
    trocaTermicaLenta = 0.1;

    // estimativa da fracao de vazio na primeira celula do sistema
    if (celula[0].acsr.tipo == 0) { // sem nenhuma fonte
        celula[0].temp = arq.celp[0].textern;
        alfini = 1.;
        betini = 0.;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
        }
    } else if (celula[0].acsr.tipo == 1) { // fonte de gas
        celula[0].temp = celula[0].acsr.injg.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.injg.FluidoPro.atualizaPropComp(pchute, celula[0].temp, -1, NULL, NULL, celula[0].acsr.injg.seco);
        }
        if (celula[0].acsr.injg.seco == 1) {
            alfini = 1.;
            betini = 0.;
        } else {
            double masgas = celula[0].acsr.injg.VMas(pchute, celula[0].temp);
            double tit;
            if (arq.flashCompleto != 2)
                tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
            else
                tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
            double masT = masgas / tit;
            tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(pchute, celula[0].temp);
            double qgas = masT * tit /
                          celula[0].acsr.injg.FluidoPro.MasEspGas(pchute, celula[0].temp);
            double qliq = masT * (1. - tit) /
                          celula[0].acsr.injg.FluidoPro.MasEspLiq(pchute, celula[0].temp);
            double qcomp = celula[0].acsr.injg.razCompGas *
                           celula[0].acsr.injg.QGas * celula[0].acsr.injg.fluidocol.MasEspFlu(1., 20.) /
                           celula[0].acsr.injg.fluidocol.MasEspFlu(pchute, celula[0].temp);
            qcomp /= 86400.;
            alfini = qgas / (qliq + qcomp + qgas);
            if ((fabs(qcomp) + fabs(qliq)) > 1e-15)
                betini = fabs(qcomp) / (fabs(qcomp) + fabs(qliq));
            else
                betini = 0.;
        }
    } else if (celula[0].acsr.tipo == 2) { // fonte de liquido, faz-se uma estimativa a partir
        // da vazÃƒÂ£o volumÃƒÂ©trica das fases, fracao de vazio = fracao de vazio sem escorregamento
        celula[0].temp = celula[0].acsr.injl.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.injl.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        double qgas = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                      (1. - celula[0].acsr.injl.FluidoPro.BSW) *
                      (celula[0].acsr.injl.FluidoPro.RGO -
                       celula[0].acsr.injl.FluidoPro.rDgD * celula[0].acsr.injl.FluidoPro.RS(pchute, celula[0].temp) * 6.29 / 35.31467) *
                      celula[0].acsr.injl.FluidoPro.Deng * 1.225 / celula[0].acsr.injl.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          (1. - celula[0].acsr.injl.FluidoPro.BSW) * celula[0].acsr.injl.FluidoPro.BOFunc(pchute, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          celula[0].acsr.injl.FluidoPro.BSW * celula[0].acsr.injl.FluidoPro.BAFunc(pchute, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet;
        alfini = qgas / (qliq + qgas);
        betini = celula[0].acsr.injl.bet;
    } else if (celula[0].acsr.tipo == 3) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.ipr.Tres;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.ipr.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        double qgas = celula[0].acsr.ipr.MasG(pchute, celula[0].temp) /
                      celula[0].acsr.ipr.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.ipr.MasL(pchute, celula[0].temp) /
                      celula[0].acsr.ipr.FluidoPro.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 10) { // fonte de massa no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.injm.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.injm.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        if (celula[0].acsr.injm.condTermo == 0) {
            celula[0].pres = pchute;
            renovaFonte(0);
        }
        double qgas = celula[0].acsr.injm.MassG /
                      celula[0].acsr.injm.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.injm.MassP /
                          celula[0].acsr.injm.FluidoPro.MasEspLiq(pchute, celula[0].temp) +
                      celula[0].acsr.injm.MassC /
                          celula[0].acsr.injm.fluidocol.MasEspFlu(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 15) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.radialPoro.tRes;
        celula[0].pres = pchute;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.radialPoro.flup.atualizaPropComp(pchute, celula[0].temp);
        }
        renovaFonte(0);
        double qgas = celula[0].acsr.radialPoro.fluxIniG /
                      celula[0].acsr.radialPoro.flup.MasEspGas(pchute, celula[0].temp);
        double qliq = (celula[0].acsr.radialPoro.fluxIni + celula[0].acsr.radialPoro.fluxIniA) /
                      celula[0].acsr.radialPoro.flup.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 16) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.poroso2D.dados.tRes;
        celula[0].pres = pchute;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.poroso2D.dados.flup.atualizaPropComp(pchute, celula[0].temp);
        }
        renovaFonte(0);
        double qgas = celula[0].acsr.poroso2D.dados.transfer.fluxIniG /
                      celula[0].acsr.poroso2D.dados.flup.MasEspGas(pchute, celula[0].temp);
        double qliq = (celula[0].acsr.poroso2D.dados.transfer.fluxIni + celula[0].acsr.poroso2D.dados.transfer.fluxIniA) /
                      celula[0].acsr.poroso2D.dados.flup.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else { // se nenhuma das opcoes, fracao de vazio=1
        celula[0].temp = arq.celp[0].textern;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
        }
        alfini = 1.;
        betini = 0.;
    }

    if (celula[0].acsr.tipo == 15) {
        celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
    } else if (celula[0].acsr.tipo == 16) {
        celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
    }
    // esta marcha e feita para quando se tem alguma fonte no inicio da tubulacao,
    // portanto, admite-se que o duto esta fechado e coloca-se uma fonte no centro da
    // primeira celula. As vazoes na fronteira esquerda da celula sÃ£o portanto = 0
    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].MComp = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;
    double masfim = 1.;
    double masfim0 = -10;
    double presteste = 1.;
    double presteste0 = -10;
    double tempteste = celula[0].temp;
    double tempteste0 = -1000;
    iterperm = 0;

    double pchute0 = pchute;
    int limIter = 2; // limite de iteracoes quando a opcao de aceleracao da convergencia esta desligado. desaconselhavel
    // desligar esta opcao, os ganhos sao pouco e a convergencia se torna instavel, principalmente
    // quando o tramo faz perte de um sistema de redes
    if (arq.AceleraConvergPerm == 1) { // opcao aceleracao de convergencia ligada
        limIter = 1;                   // em geral faz-se apenas duas iteracoes de marcha para um determinado chute
        if (arq.lingas == 1 && arq.gasinj.tipoCC == 0)
            limIter = 1; // no caso de se ter
        // uma condicao de contorno na injecao de gas = pressao, observou-se que o acoplamento dinamico
        // entre a linha de gas e de producao e mais difoicil, para se conseguir um sistema
        // melhor acoplado, deve-se fazer uma marcha iterativa a mais
    }
    // arq.CriterioConvergPerm Ã© um criterio de convergencia da marcha, so faz sentido
    // quando a aceleracao de convergencia esta desligada. Observe que esta convergencia nÃ£o
    // e de fato a conevregencia do problema, e apenas um repeticao de marcha para um determinado
    // chute de pressao ou de vazao no inicio da tubulacao. O que a convergencia busca de fato e
    // determinar qual a pressao ou vazao de fundo que satisfaz as condicoes de contorno no fim
    // da tubulacao, esta busca e feita nos metodos de busca.
    while ((fabs(masfim - masfim0) / fabs(masfim) > arq.CriterioConvergPerm ||
            fabs(presteste - presteste0) / fabs(presteste) > arq.CriterioConvergPerm) &&
           (iterperm < limIter || fabs(tempteste - tempteste0) / ((tempteste) + 273) > 0.001)) {

        masfim0 = masfim;
        presteste0 = presteste;
        tempteste0 = tempteste;
        if (iterperm == 0 && arq.lingas > 0 && arq.nvalvgas > 0 && verificaAcop == 1)
            IniciaconectaColunaPerm(); // antes de iniciar a primeira iteracao de marcha,
        // faz-se uma estimativa inicial de como se da o acopamento termico entre a coluna e o anular,
        // caso se tenha linha de gas
        else if (arq.lingas > 0 && arq.nvalvgas > 0 && verificaAcop == 1)
            conectaColunaPerm(); // acoplamento termico feito com valores de pressao e temperatura
        // obtidas na primeira iteracao de marcha
        int i;
        corrigechute = 1;
        while (corrigechute == 1) { // opcao antiga, ja nao tem mais efeito
            // efetivamente, este while sempre so e feito uma vez, quando a marcha consegue ir ate
            // a ultima celula sem problemas, caso ocorra algum problema, a marcha e finalizada e
            // sai do metodo retornando ou 1e10 ou -1e10

            // inicializando as pressoes e fracoes volumetricas das celulas iniciais, centro de celula
            // e fronteira de celula
            celula[0].presauxL = pchute;
            celula[0].presLini = pchute;
            celula[0].presL = pchute;
            celula[0].pres = pchute;
            celula[1].presL = pchute;
            celula[0].presini = pchute;
            celula[1].presLini = pchute;
            celula[0].presaux = pchute;
            celula[1].presauxL = pchute;

            celula[0].alf = alfini;
            celula[0].alfini = alfini;
            celula[0].bet = betini;
            celula[0].betini = betini;
            celula[1].alfL = celula[0].alf;
            celula[1].alfLini = celula[0].alf;
            celula[0].alfPigD = celula[0].alf;
            celula[0].alfPigDini = celula[0].alf;
            celula[0].alfPigE = celula[0].alf;
            celula[0].alfPigEini = celula[0].alf;
            celula[1].betL = celula[0].bet;
            celula[1].betLini = celula[0].bet;
            celula[0].betPigD = celula[0].bet;
            celula[0].betPigDini = celula[0].bet;
            celula[0].betPigE = celula[0].bet;
            celula[0].betPigEini = celula[0].bet;
            celula[0].betI = celula[0].bet;
            celula[1].betLI = celula[0].bet;
            // verifica se ja existe algum problema no inicio da marcha
            if (celula[0].pres <= 0.1 ||
                (arq.usaTabela == 1 && (arq.tabent.pmin - celula[0].pres) > (*vg1dSP).localtiny))
                return -1e10;

            if (arq.lingas > 0 && arq.nvalvgas > 0 && iterperm == 0)
                IniciaVazValvGasPerm(0);
            i = 1;
            // inicio da marcha propriamente dita
            while (i <= ncel && celula[i - 1].pres >= 0.1 && fabs(pchute - celula[0].pres) < (*vg1dSP).localtiny) {

                RenovaPresPermMon(i, 0); // avanco da marcha para obter a pressao na fronteira esquerda
                // da celula i
                // teste para ver se ocorreu algum problema:
                if (arq.usaTabela == 1 && (arq.tabent.pmax - celula[i].presaux) < (*vg1dSP).localtiny)
                    return 1e10;
                if (celula[i].presaux <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i].presaux) > (*vg1dSP).localtiny)) {
                    return -1e10;
                }
                atualizaPeriPmonProd(i); // atualizacao da pressao da fronteira esquerda,
                // caso exista alguma BCS ou incremento de pressao
                if (arq.flashCompleto != 2)
                    RenovaMassPerm(i); // verifica se existe alguma fonte na celula anterior, com isto, atualiza
                // as vazoes massica na fronteira a esquerda, alÃ©m das propriedades dos fluidos,
                // densidade do gas, RGO, API, BSW, beta
                else
                    RenovaMassPermComp(i);
                // RenovaTempPerm(i, 0);//faz o avanco da temperatura, da celula i-1 para a celula i
                // verifica se teve algum problema nos limites de temperatura
                // caso se esteja trabalhando com tabela PVTSim
                if (isnan(celula[i].temp))
                    NumError("Temperatrura na linha de producao com valor NaN");
                if (arq.usaTabela == 1 && (celula[i].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                    celula[i].temp = arq.tabent.tmin;
                atualizaPeriTempProd(i); // mera atualizacao de atributos de temperatura a esquerda e a direita
                RenovaPresPermJus(i, 0); // evolui a pressao  fronteira a esquerda da celula i para o
                // seu centro de celula
                // verifica se ocorreu algum problema nesta evolucao de de pressao no centro da
                // celula
                if (arq.usaTabela == 1 && (arq.tabent.pmax - celula[i].pres) < (*vg1dSP).localtiny) {
                    return 1e10;
                }
                if (celula[i].pres <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i].pres) > (*vg1dSP).localtiny)) {
                    return -1e10;
                }
                atualizaPeriPjusProd(i); // mera atualizacao de atributos que guardam valores de pressao
                // das celulas a esquerda e a direita
                if (arq.tipoFluido == 0)
                    RenovaTransMassPerm(i - 1);
                else
                    RenovaTransMassPermGas(i - 1); // caso seja uma tabela PVTSim, calcula-se a
                // taxa de transferÃªncia de massa entre as fases para o uso no calculo de
                // calor latente da equacao de energia
                if (arq.ordperm > 1) { // correcao de segunda ordem
                    double D0presaux = celula[i].presaux - celula[i - 1].pres;
                    double D0pres = celula[i].pres - celula[i].presaux;
                    double D0temp = celula[i].temp - celula[i - 1].temp;
                    RenovaPresPermMon(i, 1);
                    atualizaPeriPmonProd(i);
                    RenovaMassPerm(i);
                    RenovaPresPermJus(i, 1);
                    celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                    celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                    celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                    atualizaPeriPjusProd(i);
                    atualizaPeriPmonProd(i);
                    atualizaPeriTempProd(i);
                    RenovaMassPerm(i);
                    if (arq.tipoFluido == 0)
                        RenovaTransMassPerm(i - 1);
                    else
                        RenovaTransMassPermGas(i - 1);
                }

                i++;

                if (celula[i - 1].pres <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i - 1].pres) > (*vg1dSP).localtiny)) {
                    return -1e10;
                }
            }
            if (i == ncel + 1)
                corrigechute = 0; // fim da marcha
        }

        masfim = celula[ncel - 1].MC; // guarda valor de vazao para se calcular o erro, quando a
        // opcao de acelerador de convergencia esta desligado
        presteste = celula[ncel].pres; // guarda valor de pressao para se calcular o erro, quando a
        // opcao de acelerador de convergencia esta desligado
        iterperm++; // atualiza a ieteracao da marcha
        if (iterperm > 200 && arq.AP == 0)
            NumError("ConvergÃƒÂªncia em marchaProdPerm1 atingiu maximo de iteracoes");
        else if (iterperm > 200)
            return 1.1e10;
        if (arq.tipoFluido != 10000) {
            celula[ncel].temp = tempRev;
            if (iterperm < 100) {
                int lento = 0;
                double media = 0.;
                double desvio = 0;
                for (int ktemp = ncel - 1; ktemp >= 1; ktemp--) {

                    double area = celula[ktemp].duto.area;
                    double ugsmed;
                    ugsmed = fabs(celula[ktemp].QG) / area; // velocidade superficial de gas
                    double ulsmed;
                    ulsmed = fabs(celula[ktemp].QL) / area; // velocidade superficial de liquido
                    if (fabs(ugsmed + ulsmed) <= trocaTermicaLenta)
                        lento += 1;
                    media += fabs(ugsmed + ulsmed);
                }
                media /= (ncel - 1);
                desvio = (media - 0.1);
                if (lento > 0 && lento < ncel) {
                    int para;
                    para = 0;
                    trocaTermicaLenta = 100.;
                }
                for (int ktemp = ncel - 1; ktemp >= 0; ktemp--) {
                    RenovaTempPermRev(ktemp, 0); // faz o avanco da temperatura, da celula i-1 para a celula i
                    // verifica se teve algum problema nos limites de temperatura
                    // caso se esteja trabalhando com tabela PVTSim
                    if (arq.usaTabela == 1 && (celula[ktemp].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                        celula[ktemp].temp = arq.tabent.tmin;
                    atualizaPeriTempProd(ktemp); // mera atualizacao de atributos de temperatura a esquerda e a direita
                }
            }
            tempteste = celula[0].temp; // 0.5*(celula[0].temp+tempteste);
        } else {
            atualizaProp();
            atualizaVelTermPerm();
            calcDTPseudoTrans();
            for (int kontaPseudo = 0; kontaPseudo < 20; kontaPseudo++) {
                celula[0].tempini = celula[0].temp;
                celula[1].tempLini = celula[1].tempL;
                celula[1].tempL = celula[0].temp;
                celula[ncel].temp = tempRev;
                for (int i = 0; i < ncel; i++) {
                    celula[i].tempini = celula[i].temp;
                }
                for (int itemp = 1; itemp <= ncel; itemp++) {
                    calctemp(itemp, celula[itemp].tempini, 1);
                }
                atualizaProp();
                calcDTPseudoTrans();
            }
        }
    }

    double corrigePresF = 0.;
    if (((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1) && arq.corrigeContSep == 1)
        corrigePresF = RenovaPresPermNcel();

    monitConvPermBase = pGSup;
    return pGSup - (celula[ncel].pres + corrigePresF); // caso a marcha tenha conseguido ir atÃ© a Ãºltima celula,
    // retorna a diferenca entre a pressao a montante do choke e a pressao da ultima celula
    // calculada pela marcha
}

double SProd::marchaProdPerm2(double pchute) {

    int corrigechute = 1;

    double alfini = 0.;
    double betini = 0.;

    // estimativa da fracao de vazio na primeira celula do sistema
    if (celula[0].acsr.tipo == 0) { // sem nenhuma fonte
        celula[0].temp = arq.celp[0].textern;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
        }
        alfini = 1.;
        betini = 0.;
    } else if (celula[0].acsr.tipo == 1) { // fonte de gas
        celula[0].temp = celula[0].acsr.injg.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp, -1, NULL, NULL, celula[0].acsr.injg.seco);
            celula[0].acsr.injg.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        if (celula[0].acsr.injg.seco == 1) {
            alfini = 1.;
            betini = 0.;
        } else {
            double masgas = celula[0].acsr.injg.VMas(pchute, celula[0].temp);
            double tit;
            if (arq.flashCompleto != 2)
                tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
            else
                tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
            double masT = masgas / tit;
            tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(pchute, celula[0].temp);
            double qgas = masT * tit /
                          celula[0].acsr.injg.FluidoPro.MasEspGas(pchute, celula[0].temp);
            double qliq = masT * (1. - tit) /
                          celula[0].acsr.injg.FluidoPro.MasEspLiq(pchute, celula[0].temp);
            double qcomp = celula[0].acsr.injg.razCompGas *
                           celula[0].acsr.injg.QGas * celula[0].acsr.injg.fluidocol.MasEspFlu(1., 20.) /
                           celula[0].acsr.injg.fluidocol.MasEspFlu(pchute, celula[0].temp);
            qcomp /= 86400.;
            alfini = qgas / (qliq + qcomp + qgas);
            if ((fabs(qcomp) + fabs(qliq)) > 1e-15)
                betini = fabs(qcomp) / (fabs(qcomp) + fabs(qliq));
            else
                betini = 0.;
        }
    } else if (celula[0].acsr.tipo == 2) { // fonte de liquido, faz-se uma estimativa a partir
        // da Vazao volumÃ©trica das fases, fracao de vazio = fracao de vazio sem escorregamento
        celula[0].temp = celula[0].acsr.injl.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.injl.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        double qgas = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                      (1. - celula[0].acsr.injl.FluidoPro.BSW) *
                      (celula[0].acsr.injl.FluidoPro.RGO -
                       celula[0].acsr.injl.FluidoPro.rDgD * celula[0].acsr.injl.FluidoPro.RS(pchute, celula[0].temp) * 6.29 / 35.31467) *
                      celula[0].acsr.injl.FluidoPro.Deng * 1.225 / celula[0].acsr.injl.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          (1. - celula[0].acsr.injl.FluidoPro.BSW) * celula[0].acsr.injl.FluidoPro.BOFunc(pchute, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          celula[0].acsr.injl.FluidoPro.BSW * celula[0].acsr.injl.FluidoPro.BAFunc(pchute, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet;
        alfini = qgas / (qliq + qgas);
        betini = celula[0].acsr.injl.bet;
    } else if (celula[0].acsr.tipo == 3) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.ipr.Tres;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.ipr.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        double qgas = celula[0].acsr.ipr.MasG(pchute, celula[0].temp) /
                      celula[0].acsr.ipr.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.ipr.MasL(pchute, celula[0].temp) /
                      celula[0].acsr.ipr.FluidoPro.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 15) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.radialPoro.tRes;
        celula[0].pres = pchute;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.radialPoro.flup.atualizaPropComp(pchute, celula[0].temp);
        }
        renovaFonte(0);
        double qgas = celula[0].acsr.radialPoro.fluxIniG /
                      celula[0].acsr.radialPoro.flup.MasEspGas(pchute, celula[0].temp);
        double qliq = (celula[0].acsr.radialPoro.fluxIni + celula[0].acsr.radialPoro.fluxIniA) /
                      celula[0].acsr.radialPoro.flup.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 16) { // IPR no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.poroso2D.dados.tRes;
        celula[0].pres = pchute;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.poroso2D.dados.flup.atualizaPropComp(pchute, celula[0].temp);
        }
        renovaFonte(0);
        double qgas = celula[0].acsr.poroso2D.dados.transfer.fluxIniG /
                      celula[0].acsr.poroso2D.dados.flup.MasEspGas(pchute, celula[0].temp);
        double qliq = (celula[0].acsr.poroso2D.dados.transfer.fluxIni + celula[0].acsr.poroso2D.dados.transfer.fluxIniA) /
                      celula[0].acsr.poroso2D.dados.flup.MasEspLiq(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else if (celula[0].acsr.tipo == 10) { // fonte de massa no inicio da tubulacao
        // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
        celula[0].temp = celula[0].acsr.injm.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
            celula[0].acsr.injm.FluidoPro.atualizaPropComp(pchute, celula[0].temp);
        }
        if (celula[0].acsr.injm.condTermo == 0) {
            celula[0].pres = pchute;
            renovaFonte(0);
        }
        double qgas = celula[0].acsr.injm.MassG /
                      celula[0].acsr.injm.FluidoPro.MasEspGas(pchute, celula[0].temp);
        double qliq = celula[0].acsr.injm.MassP /
                          celula[0].acsr.injm.FluidoPro.MasEspLiq(pchute, celula[0].temp) +
                      celula[0].acsr.injm.MassC /
                          celula[0].acsr.injm.fluidocol.MasEspFlu(pchute, celula[0].temp);
        alfini = qgas / (qliq + qgas);
        betini = 0.;
    } else { // se nenhuma das opcoes, fracao de vazio=1
        celula[0].temp = arq.celp[0].textern;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(pchute, celula[0].temp);
        }
        alfini = 1.;
        betini = 0.;
    }

    if (celula[0].acsr.tipo == 15) {
        celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
    } else if (celula[0].acsr.tipo == 16) {
        celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
    }

    // esta marcha e feita para quando se tem alguma fonte no inicio da tubulacao,
    // portanto, admite-se que o duto esta fechado e coloca-se uma fonte no centro da
    // primeira celula. As vazoes na fronteira esquerda da celula sÃ£o portanto = 0
    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].MComp = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;
    double masfim = 1.;
    double masfim0 = -10;
    double presteste = 1.;
    double presteste0 = -10;
    iterperm = 0;

    int limIter = 2; // limite de iteracoes quando a opcao de aceleracao da convergencia esta desligado. desaconselhavel
    // desligar esta opcao, os ganhos sao pouco e a convergencia se torna instavel, principalmente
    // quando o tramo faz perte de um sistema de redes
    if (arq.AceleraConvergPerm == 1) { // opcao aceleracao de convergencia ligada
        limIter = 1;                   // em geral faz-se apenas duas iteracoes de marcha para um determinado chute
        if (arq.lingas == 1 && celulaG[0].tipoCC == 0)
            limIter = 1; // no caso de se ter
        // uma condicao de contorno na injecao de gas = pressao, observou-se que o acoplamento dinamico
        // entre a linha de gas e de producao e mais difoicil, para se conseguir um sistema
        // melhor acoplado, deve-se fazer uma marcha iterativa a mais
    }
    // arq.CriterioConvergPerm Ã© um criterio de convergencia da marcha, so faz sentido
    // quando a aceleracao de convergencia esta desligada. Observe que esta convergencia nÃ£o
    // e de fato a conevregencia do problema, e apenas um repeticao de marcha para um determinado
    // chute de pressao ou de vazao no inicio da tubulacao. O que a convergencia busca de fato e
    // determinar qual a pressao ou vazao de fundo que satisfaz as condicoes de contorno no fim
    // da tubulacao, esta busca e feita nos metodos de busca.
    while ((fabs(masfim - masfim0) / masfim > arq.CriterioConvergPerm ||
            fabs(presteste - presteste0) / fabs(presteste) > arq.CriterioConvergPerm) &&
           (iterperm < limIter)) {
        masfim0 = masfim;
        presteste0 = presteste;
        if (iterperm == 0 && arq.lingas > 0 && arq.nvalvgas > 0 && verificaAcop == 1)
            IniciaconectaColunaPerm(); // antes de iniciar a primeira iteracao de marcha,
        // faz-se uma estimativa inicial de como se da o acopamento termico entre a coluna e o anular,
        // caso se tenha linha de gas
        else if (arq.lingas > 0 && arq.nvalvgas > 0 && verificaAcop == 1)
            conectaColunaPerm(); // acoplamento termico feito com valores de pressao e temperatura
        // obtidas na primeira iteracao de marcha
        int i;
        corrigechute = 1;
        while (corrigechute == 1) { // opcao antiga, ja nao tem mais efeito
            // efetivamente, este while sempre so e feito uma vez, quando a marcha consegue ir ate
            // a ultima celula sem problemas, caso ocorra algum problema, a marcha e finalizada e
            // sai do metodo retornando ou 1e10 ou -1e10

            // inicializando as pressoes e fracoes volumetricas das celulas iniciais, centro de celula
            // e fronteira de celula
            celula[0].presauxL = pchute;
            celula[0].presLini = pchute;
            celula[0].presL = pchute;
            celula[0].pres = pchute;
            celula[1].presL = pchute;
            celula[0].presini = pchute;
            celula[1].presLini = pchute;
            celula[0].presaux = pchute;
            celula[1].presauxL = pchute;

            celula[0].alf = alfini;
            celula[0].alfini = alfini;
            celula[0].bet = betini;
            celula[0].betini = betini;
            celula[1].alfL = celula[0].alf;
            celula[1].alfLini = celula[0].alf;
            celula[0].alfPigD = celula[0].alf;
            celula[0].alfPigDini = celula[0].alf;
            celula[0].alfPigE = celula[0].alf;
            celula[0].alfPigEini = celula[0].alf;
            celula[1].betL = celula[0].bet;
            celula[1].betLini = celula[0].bet;
            celula[0].betPigD = celula[0].bet;
            celula[0].betPigDini = celula[0].bet;
            celula[0].betPigE = celula[0].bet;
            celula[0].betPigEini = celula[0].bet;
            celula[0].betI = celula[0].bet;
            celula[1].betLI = celula[0].bet;

            // verifica se ja existe algum problema no inicio da marcha
            if (celula[0].pres <= 0.1 ||
                (arq.usaTabela == 1 && (arq.tabent.pmin - celula[0].presaux) > (*vg1dSP).localtiny))
                return -1e10;
            else if ((celula[0].acsr.tipo == 3 && (celula[0].acsr.ipr.Pres - celula[0].pres) < 1e-15)) {
                return 1e10;
            } else if ((celula[0].acsr.tipo == 15 && (celula[0].acsr.radialPoro.pRes[0] - celula[0].pres) < 1e-15)) {
                return 1e10;
            } else if ((celula[0].acsr.tipo == 16 && (celula[0].acsr.poroso2D.dados.pRes - celula[0].pres) < 1e-15)) {
                return 1e10;
            }
            // IniciaVazValvGasPerm e um metodo que faz uma estimativa da vazao na valvula de GL
            // quando ainda nao foi feita a marcha na linha de gas. Neste caso, ele recebe o
            // indice da celula de producao e verifica se nesta celula existe uma VGL, se existir,
            // caso a condicao na linha de gas seja vazao injetada, divide a vazao injetada pelo numero de
            // valvulas e indica este valor para a VGL relacionada a celula de producao
            // caso a condicao seja pressao de injecao, faz-se uma estimativa da pressao na linha de gas
            // na posicao da VGL por hidrotatica e com isto se calcula a vazao de injecao da VGL
            if (arq.lingas > 0 && arq.nvalvgas > 0 && iterperm == 0 && monitConvPerm > 0.1) {
                if (celulaG[0].tipoCC == 0)
                    hidroLinServ();
                IniciaVazValvGasPerm(0);
            }
            i = 1;
            // inicio da marcha propriamente dita
            while (i <= ncel && celula[i - 1].pres >= 0.1 && fabs(pchute - celula[0].pres) < (*vg1dSP).localtiny) {

                if ((i) > ncel - 2) {
                    int val;
                    val = 0;
                }
                RenovaPresPermMon(i, 0); // avanco da marcha para obter a pressao na fronteira esquerda
                // da celula i
                // teste para ver se ocorreu algum problema:
                if (arq.usaTabela == 1 && (arq.tabent.pmax - celula[i].presaux) < (*vg1dSP).localtiny)
                    return 1e10;
                atualizaPeriPmonProd(i); // atualizacao da pressao da fronteira esquerda,
                // caso exista alguma BCS ou incremento de pressao
                if (arq.flashCompleto != 2)
                    RenovaMassPerm(i); // verifica se existe alguma fonte na celula anterior, com isto, atualiza
                // as vazoes massica na fronteira a esquerda, alÃ©m das propriedades dos fluidos,
                // densidade do gas, RGO, API, BSW, beta
                else
                    RenovaMassPermComp(i);

                if (arq.acopColAnulPermForte == 0 || arq.lingas == 0 || monitConvPerm > 0.1)
                    RenovaTempPerm(i, 0); // faz o avanco da temperatura, da celula i-1 para a celula i
                // verifica se teve algum problema nos limites de temperatura
                // caso se esteja trabalhando com tabela PVTSim
                if (isnan(celula[i].temp)) {
                    if (arq.transiente == 0 && arq.AP == 0)
                        // neste caso, se finaliza a simulacao, nao tem um transiente
                        // a ser feito a seguir e nem se estÃ¡ em uma rede
                        NumError(
                            "Temperatrura na linha de producao com valor NaN em marchaProdPerm2");
                    else {
                        // apresenta apenas um aviso
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        // se for em uma iteracao de rede, apos a primeira iteracao
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        // se logo apos tem uma simulacao transiente ou se esta na primeira iteracao de rede
                        else
                            return 1.1e10;
                    }
                }
                if (arq.usaTabela == 1 && (celula[i].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                    celula[i].temp = arq.tabent.tmin;
                atualizaPeriTempProd(i); // mera atualizacao de atributos de temperatura a esquerda e a direita
                RenovaPresPermJus(i, 0); // evolui a pressao  fronteira a esquerda da celula i para o
                // seu centro de celula
                // verifica se ocorreu algum problema nesta evolucao de de pressao no centro da
                // celula
                if (arq.usaTabela == 1 && (arq.tabent.pmax - celula[i].pres) < (*vg1dSP).localtiny)
                    return 1e10;
                atualizaPeriPjusProd(i); // mera atualizacao de atributos que guardam valores de pressao
                // das celulas a esquerda e a direita
                for (int j = 0; j < arq.nvalvgas; j++) { // reavaliacao da vazao da valvula de gas lift, quando
                    // a celula tem uma.
                    // P.S. parece uma acao desnecessÃ¡ria e talvez atÃ© um complicador
                    // densecessario, em vavliacao
                    if (posicVGLP[j] == i) {
                        int k = posicVGLG[j];
                        calcVazGasPerm(k);
                    }
                }
                if (arq.tipoFluido == 0)
                    RenovaTransMassPerm(i - 1);
                else
                    RenovaTransMassPermGas(i - 1); // caso seja uma tabela PVTSim, calcula-se a
                // taxa de transferÃªncia de massa entre as fases para o uso no calculo de
                // calor latente da equacao de energia
                if (arq.ordperm > 1) { // correcao de segunda ordem
                    double D0presaux = celula[i].presaux - celula[i - 1].pres;
                    double D0pres = celula[i].pres - celula[i].presaux;
                    double D0temp = celula[i].temp - celula[i - 1].temp;
                    RenovaPresPermMon(i, 1);
                    atualizaPeriPmonProd(i);
                    RenovaMassPerm(i);
                    RenovaTempPerm(i, 1);
                    RenovaPresPermJus(i, 1);
                    celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                    celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                    celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                    atualizaPeriPjusProd(i);
                    atualizaPeriPmonProd(i);
                    atualizaPeriTempProd(i);
                    RenovaMassPerm(i);
                    if (arq.tipoFluido == 0)
                        RenovaTransMassPerm(i - 1);
                    else
                        RenovaTransMassPermGas(i - 1);
                }
                // apÃ³s se atingir a pressao no centro da celula i, primeira iteracao de marcha
                // verifica-se se existe uma VGL em i e faz-se uma estimativa inicial da Vazao de
                // GL (caso exista linha de gas). Observar que isto sÃ³ Ã© feito para a iteracao zero.
                if (arq.lingas > 0 && arq.nvalvgas > 0 && iterperm == 0 && monitConvPerm > 0.1)
                    IniciaVazValvGasPerm(i);
                i++;
                // teste para verificar se a pressao do centro de celula ficou acima
                // da pressao estatica de uma eventual IPR ou ficou baixa demais
                if (celula[i - 1].pres <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i - 1].pres) > (*vg1dSP).localtiny))
                    return -1e10;
                else if (celula[i - 1].acsr.tipo == 3 && ((celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres) < 1e-15 && i == 1))
                    return 1e10;
                else if (celula[i - 1].acsr.tipo == 15 && ((celula[i - 1].acsr.radialPoro.pRes[0] - celula[i - 1].pres) < 1e-15 && i == 1))
                    return 1e10;
                else if (celula[i - 1].acsr.tipo == 16 && ((celula[i - 1].acsr.poroso2D.dados.pRes - celula[i - 1].pres) < 1e-15 && i == 1))
                    return 1e10;
                else if (arq.usaTabela == 1) {
                    if ((arq.tabent.pmax - celula[i - 1].pres) < (*vg1dSP).localtiny)
                        return 1e10;
                }
            }
            if (i == ncel + 1)
                corrigechute = 0; // fim da marcha
        }
        // apÃ³s o fim da marcha da linha de produÃ§Ã£o, Ã© feita a marcha da linha de gas
        // caso exista
        if (pchute > 0 && arq.lingas > 0 && arq.nvalvgas > 0) {
            if (celulaG[0].tipoCC == 0) {            // marcha para o caso, pressao de injecao
                if (arq.chokes.abertura[0] >= 0.2) { // choke de injecao inativo
                    for (int iter = 0; iter < 1; iter++)
                        marchaGasPerm1();
                } else
                    buscaGasPresPerm3(); // choke de injecao ativo
            } else
                buscaGasPresPerm2(); // marcha na linha de gas para o caso de vazao de injecao
        }

        if (arq.acopColAnulPermForte > 0 && arq.lingas > 0 && semTermo == 0) {
            atualizaProp();
            atualizaVelTermPerm();
            calcDTPseudoTrans();
            conectaColuna();
            for (int kontaPseudo = 0; kontaPseudo < arq.acopColAnulPermForte; kontaPseudo++) {
                for (int iterm = 0; iterm <= ncelGas; iterm++)
                    celulaG[iterm].tempini = celulaG[iterm].temp;
                for (int iterm = 1; iterm <= ncelGas; iterm++) {
                    calctempGas(iterm, celulaG[iterm - 1].tempini, 1);
                }
                celula[0].tempini = celula[0].temp;
                celula[1].tempLini = celula[1].tempL;
                celula[1].tempL = celula[0].temp;
                for (int iterm = 1; iterm <= ncel; iterm++) {
                    celula[iterm].tempini = celula[iterm].temp;
                }
                for (int iterm = 1; iterm <= ncel; iterm++) {
                    calctemp(iterm, celula[iterm].tempini, 1);
                }
                atualizaProp();
                calcDTPseudoTrans();
                conectaColuna();
            }
        }

        masfim = celula[ncel - 1].MC; // guarda valor de vazao para se calcular o erro, quando a
        // opcao de acelerador de convergencia esta desligado
        presteste = celula[ncel].pres; // guarda valor de pressao para se calcular o erro, quando a
        // opcao de acelerador de convergencia esta desligado
        iterperm++; // atualiza a ieteracao da marcha
        if (iterperm > 200 && arq.AP == 0)
            NumError("Convergencia em marchaProdPerm2 atingiu maximo de iteracoes");
        else if (iterperm > 200)
            return 1e10;
    }

    // nesta marcha, a condicao no fim da tubulacao nÃ£o e a pressao a montante do choke,
    // mas a pressao a jusante do choke. Neste caso, a condicao que se deseja convergir
    // e a vazao que passa pelo choke, definida a aprtir da diferenca entre a pressao
    // na ultima celula e a pressao a jusante do choke. Primeiro, portanto, deve-se
    // calcular a vazao que passa pelo choke e compara-la com a vazao massica total
    // na ultima celula
    double maxSup = 0.;
    if (derivaAnel != 0 && celula[ncel].pres > pGSup) { // se for o anel de GL, a vazao no final deve ser zero
        double tESup = celula[ncel].temp;
        double alfSup = celula[ncel].alf;
        double betSup = celula[ncel].bet;

        double masentrada = celula[ncel - 1].MR;
        double massgas = celula[ncel - 1].MR - celula[ncel - 1].MliqiniR;

        double tit;
        presfim = celula[ncel].pres;
        double rholp = celula[ncel].flui.MasEspLiq(celula[ncel].pres, celula[ncel].temp);
        double rholc = celula[ncel].fluicol.MasEspFlu(celula[ncel].pres, celula[ncel].temp);
        tit = fabs(massgas / masentrada);

        double masChk;

        double ypres = pGSup / presfim;
        masChk = chokeSup.vazmassSachd(ypres, presfim, tESup, alfSup, betSup, tit, celula[ncel - 1].flui,
                                       celula[ncel - 1].fluicol);
        maxSup = chokeSup.vazmaxSachd(presfim, tESup, alfSup, betSup, tit, celula[ncel - 1].flui, celula[ncel - 1].fluicol);
        if (fabs(ypres) > fabs(chokeSup.razpres))
            maxSup = masChk;
        // maxSup Ã© a vazao total passando pelo choke

        if (chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area && ypres < 1.) {
            double cplM = (1. - betSup) * celula[ncel].flui.CalorLiq(presfim, tESup) -
                          betSup * celula[ncel].fluicol.CalorLiq(presfim, tESup);
            double jtlM = (1. - betSup) * celula[ncel].flui.JTL(presfim, tESup) - betSup / rholc;
            double cpg = celula[ncel].flui.CalorGas(presfim, tESup);
            double jtgM = celula[ncel].flui.JTG(presfim, tESup);
            arq.valTempChokeJus = tESup + ((1. - tit) * jtlM / cplM + tit * jtgM / cpg) * (pGSup - presfim) * 98066.52;
        }

    } else {
        maxSup = 0.;
        arq.valTempChokeJus = celula[ncel].temp;
    }

    if (fabs(masfim) > 1e-15)
        monitConvPermBase = fabs(masfim);
    else if (fabs(maxSup) > 1e-15)
        monitConvPermBase = fabs(maxSup);
    else
        monitConvPermBase = 1.;
    return (masfim - maxSup); // diferenca entre a vazao total na fronteira a esquerda da
    // penultima celula e a vazao que passa pelo choke. Se o chute de pressao for alto
    // maxSup>masfim, retorna valor negativo,
    // se for uma estimativa baixa de pressao de fundo, maxSup<masfim retirna valor positivo
}

double SProd::buscaProdPfundoPerm(double chute, int kontaTenta) {
    revPerm = 0;
    monitConvPerm = 1000.;
    // busca de dois chutes iniciais com valores com sinais opostos
    // para marchaProdPerm1 e assim iniciar o prpocesso de calculo de erro de funcao.
    double pchute = pGSup; // inicializando o valor de pchute com o valor da pressao a jusante
    // do choke, pchute sera o valor de chute de fato no processo de busca
    // se o valor de chute>0, pchute=chute, senao, ele e estimado
    double taux; // valor de temperatura auxiliar para o eventual calculo
    // de pchute
    double j = 0.;
    double rmis = 0.;
    double f1 = 0.;
    double perdafric = 0.;
    double betaChute = 0.;

    if (chute < 0) {

        if (celula[0].acsr.tipo == 2 && fabs(celula[0].acsr.injl.QLiq) > 0.) {
            betaChute = celula[0].acsr.injl.bet;
            // este espaco faz uma estimativa de quanto deve ser a perda de carga media
            // a partir do valor da vazao no inicio da tubulacao, isto e feito apenas
            // se existir uma fonte de liquido, celula[0].acsr.tipo == 2
            taux = arq.celp[0].textern; // a temperatura em que esta estimativa sera feita
            // e dada pela temperatura da fonte, a pressao para o calculo das propriedades
            // e feita por pGSup
            if (taux < arq.tmin)
                taux = arq.tmin;
            ///////////propriedades fisicas:
            double bet = celula[0].acsr.injl.bet;
            double visC = celula[0].acsr.injl.fluidocol.VisFlu(pchute, taux);
            double visP = celula[0].acsr.injl.FluidoPro.ViscOleo(pchute, taux);
            double visG = celula[0].acsr.injl.FluidoPro.ViscGas(pchute, taux);
            double visMis = (1 - bet) * visP + bet * visC;
            double rC = celula[0].acsr.injl.fluidocol.MasEspFlu(pchute, taux);
            double rP = celula[0].acsr.injl.FluidoPro.MasEspLiq(pchute, taux);
            double rG = celula[0].acsr.injl.FluidoPro.MasEspGas(pchute, taux);
            rmis = (1 - bet) * rP + bet * rC;
            double rlcA = celula[0].acsr.injl.fluidocol.MasEspFlu(1.001, 15.);
            // estimativa grosseira da vazao massica do liquido complementar
            double massicC = rlcA * celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet / 86400;
            // estimativa grosseira da vazao massica de liquido e gas produzidos
            double massic;
            // massas especificas, condicao standard
            double Rhogs = celula[0].acsr.injl.FluidoPro.Deng * 1.225; // cel[ind].acsr.injl.FluidoPro.MasEspGas(1, 15);
            double Rhols = (1000 * 141.5 / (131.5 + celula[0].acsr.injl.FluidoPro.API)) * (1 - celula[0].acsr.injl.FluidoPro.BSW) + 1000. * celula[0].acsr.injl.FluidoPro.Denag * celula[0].acsr.injl.FluidoPro.BSW;
            // multiplicador da vazao standard para se obter a Vazao massicagÃ¡s+liquido produzido
            double multiplicador = (Rhols + celula[0].acsr.injl.FluidoPro.RGO * Rhogs * (1 - celula[0].acsr.injl.FluidoPro.BSW));
            // massic *= multiplicador;//alteracao8
            massic = 1 * multiplicador * celula[0].acsr.injl.QLiq * (1. - celula[0].acsr.injl.bet) / 86400;
            // titulo do gas em relacao a misturaoleo+agua+gas
            double fracmasshidra = celula[0].acsr.injl.FluidoPro.FracMassHidra(pchute, taux);
            double massicP = (1. - fracmasshidra) * massic; // vazao massica de liquido produzido
            double massicG = fracmasshidra * massic;        // vazao massaica de gas
            /// estimativa grosseira da velocidade da mistura
            j = (massicP / rP + massicC / rC + massicG / rG) / celula[0].duto.area;
            // estimativa da fracao de vazio sem escorregamento
            double alfmis = (massicG / rG) / (massicP / rP + massicC / rC + massicG / rG);
            // propriedades de mistura
            rmis = (1 - alfmis) * rmis + alfmis * rG;
            visMis = (1 - alfmis) * visMis + alfmis * visG;
            // estimativa de numero de Reynolds
            double re;
            if (celula[0].duto.revest == 0)
                re = celula[0].Rey(celula[0].duto.a, j, rmis, visMis);
            else {
                double dhid = 4 * celula[0].duto.area / celula[0].duto.peri;
                re = celula[0].Rey(dhid, j, rmis, visMis);
            }
            // estimativa de um fator de friccao
            f1 = celula[0].fric(re, celula[0].duto.rug / celula[0].duto.a);
        }
        double tmparea = celula[0].duto.area;
        double tmpperi = celula[0].duto.peri;
        // estimativa da perda por friccao no sistema, se o acessorio no inicio da tubulacao
        // nao for uma fonte de liquido, esta estimativa sera zero
        perdafric = (f1 * rmis * j * fabs(j) / 2.) * tmpperi / tmparea;
        // se o acessorio for uma IPR, a estimativa serÃ¡ uma pressao prÃ³xima a uma vazao baixa
        // no fundo do poco
        // se nao for IPR, a estimativa continua, considerando a perda de carga e a hidrostatica,
        // que serÃ¡ avaliada neste laco
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            if (taux < arq.tmin)
                taux = arq.tmin;
            double rhol = celula[i].flui.MasEspLiq(pchute, taux);
            double rhog = celula[i].flui.MasEspGas(pchute, taux);
            // se o sistema nao for um anel principal de GL
            // para a estimativa de pressao, se utilizara so a hidrostatica de liquido, o que dara um
            // chute inicial de pressao muito alta
            double alfa = 0.;
            if (betaChute < 0.5) {
                double tit = celula[i].flui.FracMassHidra(pchute, taux);
                alfa = tit * rhol / (rhog - tit * rhog + tit * rhol);
            }
            if (derivaAnel == 0)
                alfa = 1.; // se for o anel, so se tem gas
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.5 && chuteHol > -1e-15)
                alfa = 1. - chuteHol * 2.;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            // avanco da pressao por meio da hidrostatica e da perda por friccao estimada
            pchute += ((rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5);
            if (pchute < 0.8)
                pchute = 0.8;
            // incremento de pressao devido a algum ganho de pressao constante em alguma celula
            if (celula[i - 1].acsr.tipo == 7)
                pchute -= celula[i - 1].acsr.delp;
            // caso exista alguma IPR no meio do duto, verifica se a pressao estimada esta maior que
            // a pressao estatica, nÃ£o se trabalha com vazoes negativas neste solver
            // para evitar isto, se corrige a pressao para um valor proximo da
            // pressao estatica da IPR da celula
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchute) < 0.01 * celula[i - 1].acsr.ipr.Pres) && i == 1) {
                double qchute = 0.15 * celula[i - 1].duto.area * 86400;
                pchute = -(qchute / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchute > 0.99 * celula[i - 1].acsr.ipr.Pres)
                    pchute = 0.99 * celula[i - 1].acsr.ipr.Pres;
                if (pchute < 0.9 * celula[i - 1].acsr.ipr.Pres)
                    pchute = 0.9 * celula[i - 1].acsr.ipr.Pres;
            }
            // tambÃ©m se evita um chute com valores maiores que o limite maximo de uma tabela
            // no caso de se estar usando PVTSim
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
                pchute = 0.9 * arq.tabent.pmax;
        }

        if (pchute > 1000) {
            pchute = 1000; // pressao maxima de chute
        }
    } else
        pchute = chute; // caso chute nao seja negativo utiliza a estimativa enviada na
    // lista de parametro do metodo
    double pchute2;        // segundo chute de pressao da busca
    double pchuteAux = 0.; // auxiliar na busca de dos chutes de pressao
    // necessarios para se dar partida no metodo de calculo de raiz
    double val; // val recebe sempre o valor retornado pelo procedimento de marcha
    // neste caso, Ã© a pressao a montante do choke-a pressao da ultima celula, calculada pela
    // marcha
    val = marchaProdPerm1(pchute);
    buscaIni = 1;
    // a marcha pode dar problemas, ou a pressao em algum ponto deu acima da pressao estÃ¡tica de
    // de alguma IPR no meio do caminho, retorna 1e10,
    // ou antes de atingir a ultima celula, a pressao ficou proximo de zero, ou a pressao
    // retorna -1e10, ou a pressao, para o caso PVTSim, ficou acima da pressao maxima da tabela
    // retorna 1e10
    if (val < -0.9e10) { // pressao muito baixa na marcha, deve ser aumentado o valor de chute
        // faz-se uma nova estimativa, sÃ³ que agora admitindo uma hidrostÃ¡tica de Ã¡gua,
        // o que darÃ¡ uma pressao de fundpo mais alta
        pchuteAux = pGSup;
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            double rhol = 1000 + 0 * celula[i].flui.MasEspLiq(pchuteAux, taux);
            double rhog = celula[i].flui.MasEspGas(pchuteAux, taux);
            double alfa = 0.;
            if (betaChute < 0.5) {
                double tit = celula[i].flui.FracMassHidra(pchute, taux);
                alfa = tit * rhol / (rhog - tit * rhog + tit * rhol);
                alfa *= 0.5;
            }
            if (derivaAnel == 0)
                alfa = 1.;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.5 && chuteHol > -1e-15)
                alfa = 1. - chuteHol * 2.;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            pchuteAux += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            if (celula[i - 1].acsr.tipo == 7)
                pchuteAux -= celula[i - 1].acsr.delp;
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchuteAux) < 0.01 * celula[i - 1].acsr.ipr.Pres && i == 1)) {
                pchuteAux = -(10 / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchuteAux < 0.99 * celula[i - 1].acsr.ipr.Pres)
                    pchuteAux = 0.99 * celula[i - 1].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
        }
        if (pchuteAux > 1000) {
            pchuteAux = 1000; // limite de pchuteAux
        }
    } else if (val > 0.9e10) { // pressao em algum ponto ficou acima de alguma pressao estatica
        // deve-se diminuir o valor do chute
        pchuteAux = pGSup;
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            double rhol = celula[i].flui.MasEspLiq(pchuteAux, taux);
            double rhog = celula[i].flui.MasEspGas(pchuteAux, taux);
            // neste caso, utiliza-se uma fracao de vazio alta para a hidrostÃ¡tica
            double alfa = 0.8;
            if (betaChute < 0.5) {
                double tit = celula[i].flui.FracMassHidra(pchute, taux);
                alfa = tit * rhol / (rhog - tit * rhog + tit * rhol);
                if (alfa > 0.9999)
                    alfa = 0.9999;
            }
            if (derivaAnel == 0)
                alfa = 1.;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.2 && chuteHol > -1e-15)
                alfa = 1. - chuteHol;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            pchuteAux += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            if (celula[i - 1].acsr.tipo == 7)
                pchuteAux -= celula[i - 1].acsr.delp;
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchuteAux) < 0.01 * celula[i - 1].acsr.ipr.Pres && i == 1)) {
                pchuteAux = -(10 / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchuteAux < 0.99 * celula[i - 1].acsr.ipr.Pres)
                    pchuteAux = 0.99 * celula[i - 1].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
        }
        if (pchuteAux > 1000) {
            pchuteAux = 1000;
        }
    }

    int tipo = 1;

    // para o caso de ter dado eerado a primeira marcha, com pchuteAux faz-se uma nova tentativa
    int kontaiter = 0; // contador para o laco em que se tentara uma nova estimativa
    // em que ao menos os valores 1e10 ou 1e-10 nao seja retornados
    double pchuteAux0 = pchuteAux;
    if ((val < -0.9e10 || val > 0.9e10) && kontaiter < 50) {
        double valtemp;
        valtemp = marchaProdPerm1(pchuteAux); // marcha com pchuteAux
        if (fabs(valtemp) > 1.01e10) {
            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
            if (kontaTenta < 0)
                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                           "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                           "", "");
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
        while (valtemp > 0.9e10 && val > 0.9e10) { // estimativa de pressao de fundo ainda alta
            pchuteAux *= 0.99;                     // reduzindo a estimativa
            valtemp = marchaProdPerm1(pchuteAux);
            if (fabs(valtemp) > 1.01e10) {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if (kontaTenta < 0)
                    logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                               "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                               "", "");
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
            kontaiter++; // 50 iteracoes no maximo
        }
        while (valtemp < -0.9e10 && val < -0.9e10 && kontaiter <= 50) { // estimativa de pressao de fundo ainda baixa
            if (celula[0].acsr.tipo == 1 || celula[0].acsr.tipo == 2 || celula[0].acsr.tipo == 10)
                pchuteAux *= 1.1;
            else
                pchuteAux *= 1.01; // aumentando a estimativa
            // verificando se este aumento ultrapassa o limite de pressao de uma eventual IPR no
            // fundo
            int limpres = 0;
            if (celula[0].acsr.tipo == 3 &&
                (celula[0].acsr.ipr.Pres - pchuteAux) < 0.01 * celula[0].acsr.ipr.Pres) {

                pchuteAux = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                if (pchuteAux < 0.99 * celula[0].acsr.ipr.Pres)
                    pchuteAux = 0.99 * celula[0].acsr.ipr.Pres;

                limpres = 1; // indicadpor de que este avanÃ§o de pressao esta muito alto
                // outros avancos devem ser feitos em um passo menor
            }
            // verificando se este aumento de estimativa foca acima do valor maximo de pressao
            // de uma eventual tabela PVTSim
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
            valtemp = marchaProdPerm1(pchuteAux); // nova tentativa
            if (fabs(valtemp) > 1.01e10) {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if (kontaTenta < 0)
                    logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                               "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                               "", "");
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
            if (valtemp < -0.9e10 && limpres == 1) { // continua alto e existe o indicador
                // de que deve-se usar um passo de aumento de pressao menor
                int iterpres = 0;
                while (valtemp < -0.9e10 && iterpres < 10) { // novo laco com um passo menor,
                    // maximo de 10 iteracoes
                    pchuteAux *= 1.001;
                    valtemp = marchaProdPerm1(pchuteAux);
                    if (fabs(valtemp) > 1.01e10) {
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        if (kontaTenta < 0)
                            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                       "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                       "", "");
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                    iterpres++;
                }
                if (iterpres >= 10) {
                    // caso em que atingiu o maximo de passos de incremento de pressa em uma situacao
                    // de passo pequeno, retorna um aviso que deu problema ou finaliza a simulacao
                    if (arq.transiente == 0 && chute < 0 && arq.AP == 0) {
                        // neste caso, se finaliza a simulacao, nao tem um transiente
                        // a ser feito a seguir e nem se estÃ¡ em uma rede
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                        logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                   "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                   "", "");
                    } else {
                        // apresenta apenas um aviso
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        if (kontaTenta < 0)
                            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                       "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                       "", "");
                        // se for em uma iteracao de rede, apos a primeira iteracao
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        // se logo apos tem uma simulacao transiente ou se esta na primeira iteracao de rede
                        else
                            return 1.1e10;
                    }
                }
            }
            if (kontaiter == 50) {
                pchuteAux = 0.5 * pchuteAux0;
            }
            kontaiter++;
        }
        if (valtemp > -0.9e10 && valtemp < 0.9e10) { // conseguiu fazer a marcha atÃƒÂ© o final
            val = valtemp;
            pchute = pchuteAux;
        }
    }
    if (kontaiter > 50) { // chegou ao limite da iteracao
        if ((*vg1dSP).chaverede == 0) {
            // neste caso, se finaliza a simulacao, nao tem um transiente
            // a ser feito a seguir e nem se estÃ¡ em uma rede
            if (arq.transiente == 0 && chute < 0 && arq.AP == 0) {
                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                           "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                           "", "");
            } else {
                // apresenta apenas um aviso
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if (kontaTenta < 0)
                    logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                               "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                               "", "");
                // se for em uma iteracao de rede, apos a primeira iteracao
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                // se logo apos tem uma simulacao transiente ou se esta na primeira iteracao de rede
                else
                    return 1.1e10;
            }
        } else {
            // se for em uma iteracao de rede, apos a primeira iteracao
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            // se logo apÃ³s tem uma simulacao transiente ou se esta na primeira iteracao de rede
            else
                return 1.1e10;
        }
    }

    // caso o laÃ§o anterior tenha levado a estimativa de um valor 1e10 para um valor -1e10
    // ou tenha levado para um valor de -1e10 para 1e10
    // ou seja, de um chute de pressao alto demais para finalizar a marcha
    // para um chute de pressao baixo demais para terminar a marcha ou vice versa
    while ((val < -0.9e10 || val > 0.9e10) && kontaiter < 50) {
        pchute = 0.5 * (pchute + pchuteAux); // busca-se um valor medio
        // entre o alto demais e o baixo demais
        if ((fabs(pchute - pchuteAux) / pchute) < 0.001 && val < -0.9e10) {
            pchute = 1.05 * pchute;
            if (celula[0].acsr.tipo == 3 && (celula[0].acsr.ipr.Pres - pchute) < 0.00 * celula[0].acsr.ipr.Pres) {
                pchute = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                if (pchute < 0.99 * celula[0].acsr.ipr.Pres)
                    pchute = 0.99 * celula[0].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
                pchute = 0.9 * arq.tabent.pmax;
            pchuteAux = pchute;
        }
        val = marchaProdPerm1(pchute);
        if (fabs(val) > 1.01e10) {
            if (kontaTenta < 0)
                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                           "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                           "", "");
            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
        kontaiter++;
    }
    if (kontaiter >= 50) {
        if ((*vg1dSP).chaverede == 0) {
            if (arq.transiente == 0 && chute < 0 && arq.AP == 0) {
                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                           "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                           "", "");
            } else {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
        } else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    }
    // caso tenha sido possivel obter um chute de pressao em que foi possivel finalizar a marcha
    kontaiter = 0;
    pchute2 = pchute;
    double chuteNeg = 0.;
    double chutePos = 0.;
    double chutelim = 0.;
    double val0 = val;
    int reversao = 0;
    double atenua = 1.;
    double reduz = 1. - arq.buscaFC;
    double amplifica = 1. + arq.buscaFC;
    if (fabs(val) < 1e-3)
        return pchute;
    else {
        if (val < 0.) { // caso em que pressao a montante do choke < pressao da ultima celula, calculada pela
            // marcha, isto implica em pressao de chute alta , deve-se agora buscar
            // uma pressao de chute baixa para que val seja positivo e assim iniciar o processo
            // de calculo de zero de funcao
            chuteNeg = pchute; // armazenando o valor de chute de pressao que da o valor negativo
            // a cada nova busca em que val se aproxima de zero, mas ainda negativo
            // chuteNeg Ã© atualizado com a Ãºltima pressao de chute
            int kontaReverso = 0;
            while (val < 0) {
                if (fabs(pchute2 - pchute) / pchute < (1. - reduz) / 10. && kontaiter > 50 * 0.1 / arq.buscaFC) {
                    pchute2 *= 0.5;
                }
                pchuteAux = pchute2;
                pchute2 *= reduz; // Diminuindo a pressao na busca de val>0
                if (pchute2 <= chutelim)
                    pchute2 = 0.5 * (pchuteAux + chutelim); // chutelim inicialmente
                // e zero, mas pode acontecer de baixar demais pchute2 ao ponto de val=-1e10
                //(pressao abaixo de 0.5 no meio da marcha), neste caso, chutelim se torna este valor de
                // pchute2, pois, com isto, ja se sabe que nao se pode ir abaixo de chutelim
                val = marchaProdPerm1(pchute2);
                if (fabs(val) > 1.01e10) {
                    if (kontaTenta < 0)
                        logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                   "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                   "", "");
                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
                if (val < 0 && val > -0.9e10) {
                    chuteNeg = pchute2; // atualizando o chuteNeg
                    if (val < val0 && celula[0].acsr.tipo == 3 && arq.lingas == 0) {

                        kontaReverso++;
                        if (kontaReverso == 2) {
                            reversao = 1;
                            val = 1;
                        } else {
                            reduz = 1. + arq.buscaFC;
                            amplifica = 1. - arq.buscaFC;
                            val0 = val;
                        }
                    } else
                        val0 = val;
                }
                kontaiter++;
                if (kontaiter > 50 * 0.1 / arq.buscaFC) { // limite de iteracoes, falha na busca do segundo chute
                    // fim da simulacao ou aviso de falha
                    if ((*vg1dSP).chaverede == 0) {
                        if (arq.transiente == 0 && chute < 0 && arq.AP == 0) {
                            NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                       "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                       "", "");
                        } else {
                            if (kontaTenta < 0)
                                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                           "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                           "", "");
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    } else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val < -0.9e10) { // a reduÃ§Ã£o de pressao foi demais e a marcha nÃ£o foi capaz
                    // de ir ate o final sem que a pressao ficasse inferior a 0.5kgf/cm2
                    // deve-se aumentar a estimativa de pressao baixa
                    chutelim = pchute2;
                    pchute2 = 0.5 * (pchute2 + pchuteAux); // valor intermediario entre a menor pressao
                    // que leva a val<0 e a pressao baixa demais
                    val = marchaProdPerm1(pchute2);
                    if (fabs(val) > 1.01e10) {
                        if (kontaTenta < 0)
                            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                       "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                       "", "");
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                    if (val < 0 && val > -0.9e10) {
                        chuteNeg = pchute2;
                    }
                    kontaiter++;
                    if (kontaiter > 50 * 0.1 / arq.buscaFC) {
                        if ((*vg1dSP).chaverede == 0) {
                            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                                if (kontaTenta < 0)
                                    logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                               "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                               "", "");
                                else {
                                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                                    if ((*vg1dSP).iterRede > 0)
                                        return -1.1e10;
                                    else
                                        return 1.1e10;
                                }
                        } else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = pchute2;
        } else if (val > 0.) { // caso em que pressao a montante do choke > pressao da ultima celula,
            // calculada pela
            // marcha, isto implica em pressao de chute baixa , deve-se agora buscar
            // uma pressao de chute alta para que val seja negativo e assim iniciar o processo
            // de calculo de zero de funcao
            chutePos = pchute; // armazenando o valor de chute de pressao que da o valor positivo
            // a cada nova busca em que val se aproxima de zero, mas ainda positivo
            // chutePos e atualizado com a Ãºltima pressao de chute
            int kontaReverso = 0;
            while (val > 0) {
                pchuteAux = pchute2;
                pchute2 *= amplifica; // Aumentando a pressao na busca de val>0
                int limpres = 0;
                if (celula[0].acsr.tipo == 3 &&
                    (celula[0].acsr.ipr.Pres - pchute2) < 0.001 * celula[0].acsr.ipr.Pres) {
                    if (chute < 0) {
                        pchute2 = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                        if (pchute2 < 0.999 * celula[0].acsr.ipr.Pres)
                            pchute2 = 0.999 * celula[0].acsr.ipr.Pres;
                        if (fabs(pchute2 - pchute) < 1e-15) {
                            pchute2 = -(1 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                            if (pchute2 < 0.9999 * celula[0].acsr.ipr.Pres)
                                pchute2 = 0.9999 * celula[0].acsr.ipr.Pres;
                        }
                    } else {
                        pchute2 = pchuteAux * 1.0001;
                    }

                    limpres = 1;
                }
                val = marchaProdPerm1(pchute2);
                if (fabs(val) > 1.01e10) {
                    if (kontaTenta < 0)
                        logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                   "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                   "", "");
                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
                if (val > 0. && limpres == 1) {
                    int iterpres = 0;
                    if (iterpres >= 10)
                        if (arq.transiente == 0 && chute < 0 && arq.AP == 0) {
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                       "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                       "", "");
                        } else {
                            if (kontaTenta < 0)
                                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                           "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                           "", "");
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                }
                if (val > 0 && val < 0.9e10) {
                    chutePos = pchute2; // atualizando o chutePos
                    if (val > val0 && celula[0].acsr.tipo == 3 && arq.lingas == 0) {

                        kontaReverso++;
                        if (kontaReverso == 2) {
                            reversao = 1;
                            val = -1;
                        } else {
                            reduz = 1. + arq.buscaFC;
                            amplifica = 1. - arq.buscaFC;
                            val0 = val;
                        }

                    } else
                        val0 = val;
                }
                kontaiter++;
                if (kontaiter > 50 * 0.1 / arq.buscaFC) { // limite de iteracoes, falha na busca do segundo chute
                    // fim da simulacao ou aviso de falha
                    if ((*vg1dSP).chaverede == 0) {
                        if (arq.transiente == 0 && chute < 0 && arq.AP == 0) {
                            NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                       "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                       "", "");
                        } else {
                            if (kontaTenta < 0)
                                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                           "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                           "", "");
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    } else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val > 0.9e10) { // o incremento de pressao foi demais e a marcha nao foi capaz
                    // de ir ate o final sem que a pressao ficasse maior do que a pressao estatica
                    // em um eventual IPR no meio da marcha ou maior que o limitre maximo de pressao
                    // da tabela PVTSim, quando for este o caso
                    // deve-se diminuir a estimativa de pressao baixa
                    chutelim = pchute2;
                    pchute2 = 0.5 * (pchute2 + pchuteAux); // valor intermediario entre a maior pressao
                    // que leva a val>0 e a pressao alta demais
                    val = marchaProdPerm1(pchute2);
                    if (fabs(val) > 1.01e10) {
                        if (kontaTenta < 0)
                            logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                       "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                       "", "");
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                    if (val > 0 && val < 0.9e10)
                        chutePos = pchute2;
                    kontaiter++;
                    if (kontaiter > 50 * 0.1 / arq.buscaFC) {
                        if ((*vg1dSP).chaverede == 0) {
                            if (arq.transiente == 0 && chute < 0 && arq.AP == 0) {
                                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                                logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                           "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                           "", "");
                            } else {
                                if (kontaTenta < 0)
                                    logger.log(LOGGER_AVISO, LOG_ERR_PARSE_BUSINESS_RULE_VALIDATION,
                                               "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes",
                                               "", "");
                                cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;

                                if ((*vg1dSP).iterRede > 0)
                                    return -1.1e10;
                                else
                                    return 1.1e10;
                            }
                        } else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = pchute2;
        }

        if (reversao == 0)
            return zriddr(chuteNeg, chutePos, 1, 0); // com as duas estimativas de pressao
        // em posicoes de sinal contrario da curva, inicia-se o processo de calculo de zero
        // de funcao
        else
            return buscaProdPfundoPermRev(pchute * 1.);
    }
}

double SProd::buscaProdPfundoPermRev(double chute) {
    revPerm = 1;
    monitConvPerm = 1000.;
    // busca de dois chutes iniciais com valores com sinais opostos
    // para marchaProdPerm1 e assim iniciar o prpocesso de calculo de erro de funcao.
    double pchute = pGSup; // inicializando o valor de pchute com o valor da pressao a jusante
    // do choke, pchute sera o valor de chute de fato no processo de busca
    // se o valor de chute>0, pchute=chute, senao, ele e estimado
    double taux; // valor de temperatura auxiliar para o eventual calculo
    // de pchute
    double j = 0.;
    double rmis = 0.;
    double f1 = 0.;
    double perdafric = 0.;
    double betaChute = 0.;

    if (chute < 0) {

        if (celula[0].acsr.tipo == 2 && fabs(celula[0].acsr.injl.QLiq) > 0.) {
            // este espaco faz uma estimativa de quanto deve ser a perda de carga media
            // a partir do valor da vazao no inicio da tubulacao, isto e feito apenas
            // se existir uma fonte de liquido, celula[0].acsr.tipo == 2
            taux = arq.celp[0].textern; // a temperatura em que esta estimativa sera feita
            // e dada pela temperatura da fonte, a pressao para o calculo das propriedades
            // e feita por pGSup
            if (taux < arq.tmin)
                taux = arq.tmin;
            ///////////propriedades fisicas:
            double bet = celula[0].acsr.injl.bet;
            betaChute = bet;
            double visC = celula[0].acsr.injl.fluidocol.VisFlu(pchute, taux);
            double visP = celula[0].acsr.injl.FluidoPro.ViscOleo(pchute, taux);
            double visG = celula[0].acsr.injl.FluidoPro.ViscGas(pchute, taux);
            double visMis = (1 - bet) * visP + bet * visC;
            double rC = celula[0].acsr.injl.fluidocol.MasEspFlu(pchute, taux);
            double rP = celula[0].acsr.injl.FluidoPro.MasEspLiq(pchute, taux);
            double rG = celula[0].acsr.injl.FluidoPro.MasEspGas(pchute, taux);
            rmis = (1 - bet) * rP + bet * rC;
            double rlpA = celula[0].acsr.injl.FluidoPro.MasEspLiq(1., 15.);
            double rlcA = celula[0].acsr.injl.fluidocol.MasEspFlu(1.001, 15.);
            // estimativa grosseira da vazao massica do liquido complementar
            double massicC = rlcA * celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet / 86400;
            // estimativa grosseira da vazao massica de liquido e gas produzidos
            double massic;
            // massas especificas, condicao standard
            double Rhogs = celula[0].acsr.injl.FluidoPro.Deng * 1.225; // cel[ind].acsr.injl.FluidoPro.MasEspGas(1, 15);
            double Rhols = (1000 * 141.5 / (131.5 + celula[0].acsr.injl.FluidoPro.API)) * (1 - celula[0].acsr.injl.FluidoPro.BSW) + 1000. * celula[0].acsr.injl.FluidoPro.Denag * celula[0].acsr.injl.FluidoPro.BSW;
            // multiplicador da vazao standard para se obter a Vazao massicagÃ¡s+liquido produzido
            double multiplicador = (Rhols + celula[0].acsr.injl.FluidoPro.RGO * Rhogs * (1 - celula[0].acsr.injl.FluidoPro.BSW));
            // massic *= multiplicador;//alteracao8
            massic = 1 * multiplicador * celula[0].acsr.injl.QLiq * (1. - celula[0].acsr.injl.bet) / 86400;
            // titulo do gas em relacao a misturaoleo+agua+gas
            double fracmasshidra = celula[0].acsr.injl.FluidoPro.FracMassHidra(pchute, taux);
            double massicP = (1. - fracmasshidra) * massic; // vazao massica de liquido produzido
            double massicG = fracmasshidra * massic;        // vazao massaica de gas
            /// estimativa grosseira da velocidade da mistura
            j = (massicP / rP + massicC / rC + massicG / rG) / celula[0].duto.area;
            // estimativa da fracao de vazio sem escorregamento
            double alfmis = (massicG / rG) / (massicP / rP + massicC / rC + massicG / rG);
            // propriedades de mistura
            rmis = (1 - alfmis) * rmis + alfmis * rG;
            visMis = (1 - alfmis) * visMis + alfmis * visG;
            // estimativa de numero de Reynolds
            double re;
            if (celula[0].duto.revest == 0)
                re = celula[0].Rey(celula[0].duto.a, j, rmis, visMis);
            else {
                double dhid = 4 * celula[0].duto.area / celula[0].duto.peri;
                re = celula[0].Rey(dhid, j, rmis, visMis);
            }
            // estimativa de um fator de friccao
            f1 = celula[0].fric(re, celula[0].duto.rug / celula[0].duto.a);
        }
        double tmparea = celula[0].duto.area;
        double tmpperi = celula[0].duto.peri;
        // estimativa da perda por friccao no sistema, se o acessorio no inicio da tubulacao
        // nao for uma fonte de liquido, esta estimativa sera zero
        perdafric = (f1 * rmis * j * fabs(j) / 2.) * tmpperi / tmparea;
        // se o acessorio for uma IPR, a estimativa serÃ¡ uma pressao prÃ³xima a uma vazao baixa
        // no fundo do poco
        // se nao for IPR, a estimativa continua, considerando a perda de carga e a hidrostatica,
        // que serÃ¡ avaliada neste laco
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            if (taux < arq.tmin)
                taux = arq.tmin;
            double rhol = celula[i].flui.MasEspLiq(pchute, taux);
            double rhog = celula[i].flui.MasEspGas(pchute, taux);
            // se o sistema nao for um anel principal de GL
            // para a estimativa de pressao, se utilizara so a hidrostatica de liquido, o que dara um
            // chute inicial de pressao muito alta
            double alfa = 0.;
            if (betaChute < 0.5) {
                double tit = celula[i].flui.FracMassHidra(pchute, taux);
                alfa = tit * rhol / (rhog - tit * rhog + tit * rhol);
            }
            if (derivaAnel == 0)
                alfa = 1.; // se for o anel, so se tem gas
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.5 && chuteHol > -1e-15)
                alfa = 1. - chuteHol * 2.;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            // avanco da pressao por meio da hidrostatica e da perda por friccao estimada
            pchute += ((rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5);
            // incremento de pressao devido a algum ganho de pressao constante em alguma celula
            if (celula[i - 1].acsr.tipo == 7)
                pchute += celula[i - 1].acsr.delp;
            // caso exista alguma IPR no meio do duto, verifica se a pressao estimada esta maior que
            // a pressao estatica, nÃ£o se trabalha com vazoes negativas neste solver
            // para evitar isto, se corrige a pressao para um valor proximo da
            // pressao estatica da IPR da celula
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchute) > -0.01 * celula[i - 1].acsr.ipr.Pres) && i == 1) {
                pchute = (10 / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchute > 1.1 * celula[i - 1].acsr.ipr.Pres)
                    pchute = 1.1 * celula[i - 1].acsr.ipr.Pres;
                if (pchute < 1.01 * celula[i - 1].acsr.ipr.Pres)
                    pchute = 1.01 * celula[i - 1].acsr.ipr.Pres;
            }
            // tambÃ©m se evita um chute com valores maiores que o limite maximo de uma tabela
            // no caso de se estar usando PVTSim
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
                pchute = 0.9 * arq.tabent.pmax;
        }

        if (pchute > 1000)
            pchute = 1000; // pressao maxima de chute
    } else
        pchute = chute; // caso chute nao seja negativo utiliza a estimativa enviada na
    // lista de parametro do metodo
    double pchute2;        // segundo chute de pressao da busca
    double pchuteAux = 0.; // auxiliar na busca de dos chutes de pressao
    // necessarios para se dar partida no metodo de calculo de raiz
    double val; // val recebe sempre o valor retornado pelo procedimento de marcha
    // neste caso, Ã© a pressao a montante do choke-a pressao da ultima celula, calculada pela
    // marcha
    val = marchaProdPerm1Rev(pchute);
    buscaIni = 1;
    // a marcha pode dar problemas, ou a pressao em algum ponto deu acima da pressao estÃ¡tica de
    // de alguma IPR no meio do caminho, retorna 1e10,
    // ou antes de atingir a ultima celula, a pressao ficou proximo de zero, ou a pressao
    // retorna -1e10, ou a pressao, para o caso PVTSim, ficou acima da pressao maxima da tabela
    // retorna 1e10
    if (val < -0.9e10) { // pressao muito baixa na marcha, deve ser aumentado o valor de chute
        // faz-se uma nova estimativa, sÃ³ que agora admitindo uma hidrostÃ¡tica de Ã¡gua,
        // o que darÃ¡ uma pressao de fundpo mais alta
        pchuteAux = pGSup;
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            double rhol = 1000 + 0 * celula[i].flui.MasEspLiq(pchuteAux, taux);
            double rhog = celula[i].flui.MasEspGas(pchuteAux, taux);
            double alfa = 0.;
            if (derivaAnel == 0)
                alfa = 1.;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.5 && chuteHol > -1e-15)
                alfa = 1. - chuteHol * 2.;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            pchuteAux += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            if (celula[i - 1].acsr.tipo == 7)
                pchuteAux -= celula[i - 1].acsr.delp;
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchuteAux) > -0.01 * celula[i - 1].acsr.ipr.Pres && i == 1)) {
                pchuteAux = (10 / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchuteAux > 1.01 * celula[i - 1].acsr.ipr.Pres)
                    pchuteAux = 1.01 * celula[i - 1].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
        }
        if (pchuteAux > 1100)
            pchuteAux = 1100;  // limite de pchuteAux
    } else if (val > 0.9e10) { // pressÃƒÂ£o em algum ponto ficou acima de alguma pressao estatica
        // deve-se diminuir o valor do chute
        pchuteAux = pGSup;
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            double rhol = celula[i].flui.MasEspLiq(pchuteAux, taux);
            double rhog = celula[i].flui.MasEspGas(pchuteAux, taux);
            // neste caso, utiliza-se uma fraÃ§Ã£o de vazio alta para a hidrostÃ¡tica
            double alfa = 0.8;
            if (derivaAnel == 0)
                alfa = 1.;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.2 && chuteHol > -1e-15)
                alfa = 1. - chuteHol;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            pchuteAux += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            if (celula[i - 1].acsr.tipo == 7)
                pchuteAux -= celula[i - 1].acsr.delp;
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchuteAux) > -0.01 * celula[i - 1].acsr.ipr.Pres && i == 1)) {
                pchuteAux = (10 / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchuteAux > 1.01 * celula[i - 1].acsr.ipr.Pres)
                    pchuteAux = 1.01 * celula[i - 1].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
        }
        if (pchuteAux > 1100)
            pchuteAux = 1100;
    }

    double mult1 = 0.9;
    double mult2 = 1.1;
    int tipo = 1;

    // para o caso de ter dado eerado a primeira marcha, com pchuteAux faz-se uma nova tentativa
    int kontaiter = 0; // contador para o laco em que se tentara uma nova estimativa
    // em que ao menos os valores 1e10 ou 1e-10 nÃ£o seja retornados
    double pchuteAux0 = pchuteAux;
    if ((val < -0.9e10 || val > 0.9e10) && kontaiter <= 100) {
        double valtemp;
        valtemp = marchaProdPerm1Rev(pchuteAux);   // marcha com pchuteAux
        while (valtemp > 0.9e10 && val > 0.9e10) { // estimativa de pressao de fundo ainda alta
            pchuteAux *= 0.99;                     // reduzindo a estimativa
            valtemp = marchaProdPerm1Rev(pchuteAux);
            kontaiter++; // 50 iteracoes no maximo
        }
        while (valtemp < -0.9e10 && val < -0.9e10 && kontaiter < 100) { // estimativa de pressao de fundo ainda baixa
            if (celula[0].acsr.tipo == 1 || celula[0].acsr.tipo == 2 || celula[0].acsr.tipo == 10)
                pchuteAux *= 1.1;
            else
                pchuteAux *= 1.01; // aumentando a estimativa
            // verificando se este aumento ultrapassa o limite de pressao de uma eventual IPR no
            // fundo
            int limpres = 0;
            if (celula[0].acsr.tipo == 3 &&
                (celula[0].acsr.ipr.Pres - pchuteAux) < -0.01 * celula[0].acsr.ipr.Pres) {

                pchuteAux = (10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                if (pchuteAux > 1.01 * celula[0].acsr.ipr.Pres)
                    pchuteAux = 1.01 * celula[0].acsr.ipr.Pres;

                limpres = 1; // indicadpor de que este avanÃ§o de pressao esta muito alto
                // outros avancos devem ser feitos em um passo menor
            }
            // verificando se este aumento de estimativa foca acima do valor maximo de pressao
            // de uma eventual tabela PVTSim
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
            valtemp = marchaProdPerm1Rev(pchuteAux); // nova tentativa
            if (valtemp < -0.9e10 && limpres == 1) { // continua alto e existe o indicador
                // de que deve-se usar um passo de aumento de pressao menor
                int iterpres = 0;
                while (valtemp < -0.9e10 && iterpres < 10) { // novo laco com um passo menor,
                    // maximo de 10 iteracoes
                    pchuteAux *= 1.001;
                    valtemp = marchaProdPerm1Rev(pchuteAux);
                    iterpres++;
                }
                if (iterpres >= 10) {
                    // caso em que atingiu o maximo de passos de incremento de pressa em uma situacao
                    // de passo pequeno, retorna um aviso que deu problema ou finaliza a simulacao
                    if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                        // neste caso, se finaliza a simulacao, nao tem um transiente
                        // a ser feito a seguir e nem se estÃ¡ em uma rede
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                    else {
                        // apresenta apenas um aviso
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        // se for em uma iteracao de rede, apos a primeira iteracao
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        // se logo apÃƒÂ³s tem uma simulacao transiente ou se esta na primeira iteracao de rede
                        else
                            return 1.1e10;
                    }
                }
            }
            if (kontaiter == 100) {
                pchuteAux = 0.5 * pchuteAux0;
            }
            kontaiter++;
        }
        if (valtemp > -0.9e10 && valtemp < 0.9e10) { // conseguiu fazer a marcha atÃƒÂ© o final
            val = valtemp;
            pchute = pchuteAux;
        }
    }
    if (kontaiter > 100) { // chegou ao limite da iteracao
        if ((*vg1dSP).chaverede == 0) {
            // neste caso, se finaliza a simulacao, nao tem um transiente
            // a ser feito a seguir e nem se estÃ¡ em uma rede
            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
            else {
                // apresenta apenas um aviso
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                // se for em uma iteracao de rede, apos a primeira iteracao
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                // se logo apos tem uma simulacao transiente ou se esta na primeira iteracao de rede
                else
                    return 1.1e10;
            }
        } else {
            // se for em uma iteracao de rede, apos a primeira iteracao
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            // se logo apÃ³s tem uma simulacao transiente ou se esta na primeira iteracao de rede
            else
                return 1.1e10;
        }
    }

    // caso o laÃ§o anterior tenha levado a estimativa de um valor 1e10 para um valor -1e10
    // ou tenha levado para um valor de -1e10 para 1e10
    // ou seja, de um chute de pressao alto demais para finalizar a marcha
    // para um chute de pressao baixo demais para terminar a marcha ou vice versa
    while ((val < -0.9e10 || val > 0.9e10) && kontaiter < 100) {
        pchute = 0.5 * (pchute + pchuteAux); // busca-se um valor medio
        // entre o alto demais e o baixo demais
        if ((fabs(pchute - pchuteAux) / pchute) < 0.001 && val < -0.9e10) {
            pchute = 1.05 * pchute;
            if (celula[0].acsr.tipo == 3 && (celula[0].acsr.ipr.Pres - pchute) > 0.00 * celula[0].acsr.ipr.Pres) {
                pchute = (10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                if (pchute > 1.01 * celula[0].acsr.ipr.Pres)
                    pchute = 1.01 * celula[0].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
                pchute = 0.9 * arq.tabent.pmax;
            pchuteAux = pchute;
        }
        val = marchaProdPerm1Rev(pchute);
        kontaiter++;
    }
    if (kontaiter >= 100) {
        if ((*vg1dSP).chaverede == 0) {
            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
            else {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
        } else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    }
    // caso tenha sido possivel obter um chute de pressao em que foi possivel finalizar a marcha
    kontaiter = 0;
    pchute2 = pchute;
    double chuteNeg = 0.;
    double chutePos = 0.;
    double chutelim = 0.;
    arq.buscaFC = fabs(arq.buscaFC);
    double reduz = 1. - arq.buscaFC;
    double amplifica = 1. + arq.buscaFC;
    double aumenta = amplifica;
    if (fabs(val) < 1e-3)
        return pchute;
    else {
        if (val < 0.) { // caso em que pressao a montante do choke < pressao da ultima celula, calculada pela
            // marcha, isto implica em pressao de chute alta , deve-se agora buscar
            // uma pressao de chute baixa para que val seja positivo e assim iniciar o processo
            // de calculo de zero de funcao
            chuteNeg = pchute; // armazenando o valor de chute de pressao que da o valor negativo
            // a cada nova busca em que val se aproxima de zero, mas ainda negativo
            // chuteNeg Ã© atualizado com a Ãºltima pressao de chute
            while (val < 0) {
                if (fabs(pchute2 - pchute) / pchute < (1. - reduz) / 10. && kontaiter > 100) {
                    pchute2 *= 0.5;
                }
                pchuteAux = pchute2;
                pchute2 *= reduz; // Diminuindo a pressao na busca de val>0
                if (pchute2 <= chutelim)
                    pchute2 = 0.5 * (pchuteAux + chutelim); // chutelim inicialmente
                // e zero, mas pode acontecer de baixar demais pchute2 ao ponto de val=-1e10
                //(pressao abaixo de 0.5 no meio da marcha), neste caso, chutelim se torna este valor de
                // pchute2, pois, com isto, ja se sabe que nao se pode ir abaixo de chutelim
                val = marchaProdPerm1Rev(pchute2);
                if (val < 0 && val > -0.9e10)
                    chuteNeg = pchute2; // atualizando o chuteNeg
                kontaiter++;
                if (kontaiter > 100) { // limite de iteracoes, falha na busca do segundo chute
                    // fim da simulacao ou aviso de falha
                    if ((*vg1dSP).chaverede == 0) {
                        if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                            NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                        else {
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    } else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val < -0.9e10) { // a reduÃ§Ã£o de pressao foi demais e a marcha nÃ£o foi capaz
                    // de ir ate o final sem que a pressao ficasse inferior a 0.5kgf/cm2
                    // deve-se aumentar a estimativa de pressao baixa
                    chutelim = pchute2;
                    pchute2 = 0.5 * (pchute2 + pchuteAux); // valor intermediario entre a menor pressao
                    // que leva a val<0 e a pressao baixa demais
                    val = marchaProdPerm1Rev(pchute2);
                    if (val < 0 && val > -0.9e10)
                        chuteNeg = pchute2;
                    kontaiter++;
                    if (kontaiter > 100) {
                        if ((*vg1dSP).chaverede == 0) {
                            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                            else {
                                cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                                if ((*vg1dSP).iterRede > 0)
                                    return -1.1e10;
                                else
                                    return 1.1e10;
                            }
                        } else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = pchute2;
        } else if (val > 0.) { // caso em que pressao a montante do choke > pressao da ultima celula,
            // calculada pela
            // marcha, isto implica em pressao de chute baixa , deve-se agora buscar
            // uma pressao de chute alta para que val seja negativo e assim iniciar o processo
            // de calculo de zero de funcao
            chutePos = pchute; // armazenando o valor de chute de pressao que da o valor positivo
            // a cada nova busca em que val se aproxima de zero, mas ainda positivo
            // chutePos e atualizado com a Ãºltima pressao de chute
            while (val > 0) {
                pchuteAux = pchute2;
                pchute2 *= aumenta; // Aumentando a pressao na busca de val>0
                int limpres = 0;
                val = marchaProdPerm1Rev(pchute2);
                if (val > 0 && val < 0.9e10)
                    chutePos = pchute2; // atualizando o chutePos
                kontaiter++;
                if (kontaiter > 100) { // limite de iteracoes, falha na busca do segundo chute
                    // fim da simulacao ou aviso de falha
                    if ((*vg1dSP).chaverede == 0) {
                        if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                            NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                        else {
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    } else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val > 0.9e10) { // o incremento de pressao foi demais e a marcha nao foi capaz
                    // de ir ate o final sem que a pressao ficasse maior do que a pressao estatica
                    // em um eventual IPR no meio da marcha ou maior que o limitre maximo de pressao
                    // da tabela PVTSim, quando for este o caso
                    // deve-se diminuir a estimativa de pressao baixa
                    chutelim = pchute2;
                    pchute2 = 0.5 * (pchute2 + pchuteAux); // valor intermediario entre a maior pressao
                    // que leva a val>0 e a pressao alta demais
                    val = marchaProdPerm1Rev(pchute2);
                    if (val > 0 && val < 0.9e10)
                        chutePos = pchute2;
                    kontaiter++;
                    if (kontaiter > 100) {
                        if ((*vg1dSP).chaverede == 0) {
                            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
                            else {
                                cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;

                                if ((*vg1dSP).iterRede > 0)
                                    return -1.1e10;
                                else
                                    return 1.1e10;
                            }
                        } else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = pchute2;
        }

        return zriddr(chuteNeg, chutePos, 1, 0); // com as duas estimativas de pressao
        // em posicoes de sinal contrario da curva, inicia-se o processo de calculo de zero
        // de funcao
    }
}

double SProd::buscaProdPfundoPerm2(double chute, int kontaTenta) {
    // busca de dois chutes iniciais com valores com sinais opostos
    // para marchaProdPerm1 e assim iniciar o prpocesso de calculo de erro de funcao.
    revPerm = 0;
    monitConvPerm = 1000.;
    double pchute = pGSup; // inicializando o valor de pchute com o valor da pressao a jusante
    // do choke, pchute sera o valor de chute de fato no processo de busca
    // se o valor de chute>0, pchute=chute, senao, ele e estimado
    double taux; // valor de temperatura auxiliar para o eventual calculo
    // de pchute
    double j = 0.;
    double rmis = 0.;
    double f1 = 0.;
    double perdafric = 0.;
    double betaChute = 0.;

    if (chute < 0) {
        if (celula[0].acsr.tipo == 2 && fabs(celula[0].acsr.injl.QLiq) > 0.) {
            // este espaco faz uma estimativa de quanto deve ser a perda de carga media
            // a partir do valor da vazao no inicio da tubulacao, isto e feito apenas
            // se existir uma fonte de liquido, celula[0].acsr.tipo == 2
            taux = arq.celp[0].textern;
            // a temperatura em que esta estimativa sera feita
            // e dada pela temperatura da fonte, a pressao para o calculo das propriedades
            // e feita por pGSup
            if (taux < arq.tmin)
                taux = arq.tmin;
            ///////////propriedades fisicas:
            double bet = celula[0].acsr.injl.bet;
            betaChute = bet;
            double visC = celula[0].acsr.injl.fluidocol.VisFlu(pchute, taux);
            double visP = celula[0].acsr.injl.FluidoPro.ViscOleo(pchute, taux);
            double visG = celula[0].acsr.injl.FluidoPro.ViscGas(pchute, taux);
            double visMis = (1 - bet) * visP + bet * visC;
            double rC = celula[0].acsr.injl.fluidocol.MasEspFlu(pchute, taux);
            double rP = celula[0].acsr.injl.FluidoPro.MasEspLiq(pchute, taux);
            double rG = celula[0].acsr.injl.FluidoPro.MasEspGas(pchute, taux);
            rmis = (1 - bet) * rP + bet * rC;
            double rlpA = celula[0].acsr.injl.FluidoPro.MasEspLiq(1., 15.);
            double rlcA = celula[0].acsr.injl.fluidocol.MasEspFlu(1.001, 15.);
            // estimativa grosseira da vazao massica do liquido complementar
            double massicC = rlcA * celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet / 86400;
            // estimativa grosseira da vazao massica de liquido e gas produzidos
            double massic = rlpA * celula[0].acsr.injl.QLiq * (1. - celula[0].acsr.injl.bet) / 86400;
            // massas especificas, condicao standard
            double Rhogs = celula[0].acsr.injl.FluidoPro.Deng * 1.225; // cel[ind].acsr.injl.FluidoPro.MasEspGas(1, 15);
            double Rhols = (1000 * 141.5 / (131.5 + celula[0].acsr.injl.FluidoPro.API)) * (1 - celula[0].acsr.injl.FluidoPro.BSW) + 1000. * celula[0].acsr.injl.FluidoPro.Denag * celula[0].acsr.injl.FluidoPro.BSW;
            // multiplicador da vazao standard para se obter a Vazao massicagÃ¡s+liquido produzido
            double multiplicador = (Rhols + celula[0].acsr.injl.FluidoPro.RGO * Rhogs * (1 - celula[0].acsr.injl.FluidoPro.BSW));
            // massic *= multiplicador;//alteracao8
            if ((*vg1dSP).chaverede == 1)
                massic = 1 * multiplicador * celula[0].acsr.injl.QLiq * (1. - celula[0].acsr.injl.bet) / 86400;
            else
                massic = 1 * multiplicador * celula[0].acsr.injl.QLiq * (1. - celula[0].acsr.injl.bet) / 86400;
            // titulo do gas em relacao a misturaoleo+agua+gas
            double fracmasshidra = celula[0].acsr.injl.FluidoPro.FracMassHidra(pchute, taux);
            double massicP = (1. - fracmasshidra) * massic; // vazao massica de liquido produzido
            double massicG = fracmasshidra * massic;        // vazao massaica de gas
            /// estimativa grosseira da velocidade da mistura
            j = (massicP / rP + massicC / rC + massicG / rG) / celula[0].duto.area;
            // estimativa da fracao de vazio sem escorregamento
            double alfmis = (massicG / rG) / (massicP / rP + massicC / rC + massicG / rG);
            // propriedades de mistura
            rmis = (1 - alfmis) * rmis + alfmis * rG;
            visMis = (1 - alfmis) * visMis + alfmis * visG;
            // estimativa de numero de Reynolds
            double re;
            if (celula[0].duto.revest == 0)
                re = celula[0].Rey(celula[0].duto.a, j, rmis, visMis);
            else {
                double dhid = 4 * celula[0].duto.area / celula[0].duto.peri;
                re = celula[0].Rey(dhid, j, rmis, visMis);
            }
            // estimativa de um fator de friccao
            f1 = celula[0].fric(re, celula[0].duto.rug / celula[0].duto.a);
        }
        // estimativa da perda por friccao no sistema, se o acessorio no inicio da tubulacao
        // nao for uma fonte de liquido, esta estimativa sera zero
        perdafric = (f1 * rmis * j * fabs(j) / 2.) * celula[0].duto.peri / celula[0].duto.area;
        // se o acessorio for uma IPR, a estimativa serÃ¡ uma pressao prÃ³xima a uma vazao baixa
        // no fundo do poco
        // se nao for IPR, a estimativa continua, considerando a perda de carga e a hidrostatica,
        // que serÃ¡ avaliada neste laco
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            if (taux < arq.tmin)
                taux = arq.tmin;
            double rhol = celula[i].flui.MasEspLiq(pchute, taux);
            double rhog = celula[i].flui.MasEspGas(pchute, taux);
            // se o sistema nao for um anel principal de GL
            // para a estimativa de pressao, se utilizara so a hidrostatica de liquido, o que dara um
            // chute inicial de pressao muito alta
            double alfa = 0.;
            if (betaChute < 0.5) {
                double tit = celula[i].flui.FracMassHidra(pchute, taux);
                alfa = tit * rhol / (rhog - tit * rhog + tit * rhol);
            }
            if (derivaAnel == 0)
                alfa = 1.; // se for o anel, so se tem gas
            else if ((*vg1dSP).chaverede == 0 && chuteHol > -1e-15)
                alfa = 1. - chuteHol;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.5 && chuteHol > -1e-15)
                alfa = 1. - chuteHol * 2.;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            // avanco da pressao por meio da hidrostatica e da perda por friccao estimada
            pchute += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            // caso exista alguma IPR no meio do duto, verifica se a pressao estimada esta maior que
            // a pressao estatica, nÃ£o se trabalha com vazoes negativas neste solver
            // para evitar isto, se corrige a pressao para um valor proximo da
            // pressao estatica da IPR da celula
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchute) < 0.01 * celula[i - 1].acsr.ipr.Pres && i == 1)) {
                double qchute = 0.15 * celula[i - 1].duto.area * 86400;
                pchute = -(qchute / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchute > 0.99 * celula[i - 1].acsr.ipr.Pres)
                    pchute = 0.99 * celula[i - 1].acsr.ipr.Pres;
                if (pchute < 0.9 * celula[i - 1].acsr.ipr.Pres)
                    pchute = 0.9 * celula[i - 1].acsr.ipr.Pres;
            }
            // tambÃ©m se evita um chute com valores maiores que o limite maximo de uma tabela
            // no caso de se estar usando PVTSim
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
                pchute = 0.9 * arq.tabent.pmax;
        }

        if (pchute > 1000)
            pchute = 1000.; // pressao maxima de chute
    } else
        pchute = chute; // caso chute nao seja negativo utiliza a estimativa enviada na
    // lista de parametro do metodo
    double pchute2;        // segundo chute de pressao da busca
    double pchuteAux = 0.; // auxiliar na busca de dos chutes de pressao
    // necessarios para se dar partida no metodo de calculo de raiz
    double val; // val recebe sempre o valor retornado pelo procedimento de marcha
    // neste caso, Ã© a pressao a montante do choke-a pressao da ultima celula, calculada pela
    // marcha
    val = marchaProdPerm2(pchute);
    buscaIni = 1;
    // a marcha pode dar problemas, ou a pressao em algum ponto deu acima da pressao estatica de
    // de alguma IPR no meio do caminho, retorna 1e10,
    // ou antes de atingir a ultima celula, a pressao ficou proximo de zero, ou a pressao
    // retorna -1e10, ou a pressao, para o caso PVTSim, ficou acima da pressao maxima da tabela
    // retorna 1e10
    if (val < -0.9e10) { // pressao muito baixa na marcha, deve ser aumentado o valor de chute
        // faz-se uma nova estimativa, sÃ³ que agora admitindo uma hidrostÃ¡tica de Ã¡gua,
        // o que darÃ¡ uma pressao de fundpo mais alta
        pchuteAux = pGSup;
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            double rhol = 1000 + 0 * celula[i].flui.MasEspLiq(pchuteAux, taux);
            double rhog = celula[i].flui.MasEspGas(pchuteAux, taux);
            double alfa = 0.;
            if (derivaAnel == 0)
                alfa = 1.;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.5 && chuteHol > -1e-15)
                alfa = 1. - chuteHol * 2.;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            pchuteAux += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchuteAux) < 0.01 * celula[i - 1].acsr.ipr.Pres && i == 1)) {
                pchuteAux = -(10 / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchuteAux < 0.99 * celula[i - 1].acsr.ipr.Pres)
                    pchuteAux = 0.99 * celula[i - 1].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
        }
        if (pchuteAux > 1100)
            pchuteAux = 1100;  // limite de pchuteAux
    } else if (val > 0.9e10) { // pressao em algum ponto ficou acima de alguma pressao estatica
        // deve-se diminuir o valor do chute
        pchuteAux = pGSup;
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            double rhol = celula[i].flui.MasEspLiq(pchuteAux, taux);
            double rhog = celula[i].flui.MasEspGas(pchuteAux, taux);
            // neste caso, utiliza-se uma fraÃ§Ã£o de vazio alta para a hidrostÃ¡tica
            double alfa = 0.8;
            if (derivaAnel == 0)
                alfa = 1.;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.2 && chuteHol > -1e-15)
                alfa = 1. - chuteHol;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            pchuteAux += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            if (celula[i - 1].acsr.tipo == 3 &&
                ((celula[i - 1].acsr.ipr.Pres - pchuteAux) < 0.01 * celula[i - 1].acsr.ipr.Pres && i == 1)) {
                pchuteAux = -(10 / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchuteAux < 0.99 * celula[i - 1].acsr.ipr.Pres)
                    pchuteAux = 0.99 * celula[i - 1].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
        }
        if (pchuteAux > 1100)
            pchuteAux = 1100;
    }

    double mult1 = 0.9;
    double mult2 = 1.1;

    // para o caso de ter dado eerado a primeira marcha, com pchuteAux faz-se uma nova tentativa
    int kontaiter = 0; // contador para o laco em que se tentara uma nova estimativa
    // em que ao menos os valores 1e10 ou 1e-10 nÃ£o seja retornados
    double pchuteAux0 = pchuteAux;
    if (val < -0.9e10 || val > 0.9e10) {
        double valtemp;
        valtemp = marchaProdPerm2(pchuteAux); // marcha com pchuteAux
        if (fabs(valtemp) > 1.01e10) {
            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
        while (valtemp > 0.9e10 && val > 0.9e10 && kontaiter < 50) { // estimativa de pressao de fundo ainda alta
            pchuteAux *= 0.99;                                       // reduzindo a estimativa
            valtemp = marchaProdPerm2(pchuteAux);
            if (fabs(valtemp) > 1.01e10) {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
            kontaiter++; // 50 iteracoes no maximo
        }
        while (valtemp < -0.9e10 && val < -0.9e10 && kontaiter <= 50) { // estimativa de pressao de fundo ainda baixa
            if (celula[0].acsr.tipo == 1 || celula[0].acsr.tipo == 2 || celula[0].acsr.tipo == 10)
                pchuteAux *= 1.1;
            else
                pchuteAux *= 1.01; // aumentando a estimativa
            // verificando se este aumento ultrapassa o limite de pressao de uma eventual IPR no
            // fundo
            if (celula[0].acsr.tipo == 3 &&
                (celula[0].acsr.ipr.Pres - pchuteAux) < 0.01 * celula[0].acsr.ipr.Pres) {
                pchuteAux = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                if (pchuteAux < 0.99 * celula[0].acsr.ipr.Pres)
                    pchuteAux = 0.99 * celula[0].acsr.ipr.Pres;
            }
            // verificando se este aumento de estimativa foca acima do valor maximo de pressao
            // de uma eventual tabela PVTSim
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
            valtemp = marchaProdPerm2(pchuteAux); // nova tentativa
            if (fabs(valtemp) > 1.01e10) {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
            if (kontaiter == 50) {
                pchuteAux = 0.5 * pchuteAux0;
            }
            kontaiter++;
        }
        valtemp = marchaProdPerm2(pchuteAux);
        if (fabs(valtemp) > 1.01e10) {
            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
        if (valtemp > -0.9e10 && valtemp < 0.9e10) { // conseguiu fazer a marcha atÃƒÂ© o final
            val = valtemp;
            pchute = pchuteAux;
        }
    }
    if (kontaiter > 50) { // chegou ao limite da iteracao
        if ((*vg1dSP).chaverede == 0) {
            // neste caso, se finaliza a simulacao, nao tem um transiente
            // a ser feito a seguir e nem se estÃ¡ em uma rede
            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
            else {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
        } else {
            // se for em uma iteracao de rede, apos a primeira iteracao
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            // se logo apÃ³s tem uma simulacao transiente ou se esta na primeira iteracao de rede
            else
                return 1.1e10;
        }
    }
    // caso o laÃ§o anterior tenha levado a estimativa de um valor 1e10 para um valor -1e10
    // ou tenha levado para um valor de -1e10 para 1e10
    // ou seja, de um chute de pressao alto demais para finalizar a marcha
    // para um chute de pressao baixo demais para terminar a marcha ou vice versa
    while ((val < -0.9e10 || val > 0.9e10) && kontaiter <= 50) {
        pchute = 0.5 * (pchute + pchuteAux); // busca-se um valor medio
        // entre o alto demais e o baixo demais
        if ((fabs(pchute - pchuteAux) / pchute) < 0.001 && val < -0.9e10) {
            pchute = 1.05 * pchute;
            if (celula[0].acsr.tipo == 3 &&
                (celula[0].acsr.ipr.Pres - pchute) < 0.01 * celula[0].acsr.ipr.Pres) {
                pchute = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                if (pchute < 0.99 * celula[0].acsr.ipr.Pres)
                    pchute = 0.99 * celula[0].acsr.ipr.Pres;
            }
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
                pchute = 0.9 * arq.tabent.pmax;
            pchuteAux = pchute;
        }

        val = marchaProdPerm2(pchute);
        if (fabs(val) > 1.01e10) {
            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
        kontaiter++;
    }
    if (kontaiter > 50) {
        if ((*vg1dSP).chaverede == 0) {
            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm atingiu maximo de iteracoes");
            else {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
        } else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    }

    // caso tenha sido possivel obter um chute de pressao em que foi possivel finalizar a marcha
    kontaiter = 0;
    pchute2 = pchute;
    double chutelim = 0.;
    double chuteNeg = 0.;
    double chutePos = 0.;
    double atenua = 1.;
    double reduz = 1. - arq.buscaFC;
    double amplifica = 1. + arq.buscaFC;
    if (fabs(val) < 1e-3)
        return pchute;
    else {
        if (val < 0.) { // caso em que o chute de pressao foi alto
            // Vazao no choke>Vazao da mistura na tubulaÃ§Ã£o
            // calculada pela marcha, isto implica em pressao de chute alta , deve-se agora buscar
            // uma pressao de chute baixa para que val seja positivo e assim iniciar o processo
            // de calculo de zero de funcao
            chuteNeg = pchute; // armazenando o valor de chute de pressao que da o valor negativo
            // a cada nova busca em que val se aproxima de zero, mas ainda negativo
            // chuteNeg Ã© atualizado com a Ãºltima pressao de chute
            while (val < 0) {
                if (fabs(pchute2 - pchute) / pchute < (1. - reduz) / 10. && kontaiter > 50) {
                    pchute2 *= 0.5;
                }
                pchuteAux = pchute2;
                if ((*vg1dSP).chaverede == 0)
                    pchute2 *= reduz; // Diminuindo a pressao na busca de val>0
                else
                    pchute2 *= mult1; // Diminuindo a pressao na busca de val>0
                if (pchute2 <= chutelim)
                    pchute2 = 0.5 * (pchuteAux + chutelim); // chutelim inicialmente
                // e zero, mas pode acontecer de baixar demais pchute2 ao ponto de val=-1e10
                //(pressao abaixo de 0.5 no meio da marcha), neste caso, chutelim se torna este valor de
                // pchute2, pois, com isto, ja se sabe que nao se pode ir abaixo de chutelim
                if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute2) < (*vg1dSP).localtiny)
                    pchute2 = 0.9 * arq.tabent.pmax;
                if (celula[0].acsr.tipo == 3 &&
                    (celula[0].acsr.ipr.Pres - pchute2) < 0.001 * celula[0].acsr.ipr.Pres) {
                    pchute2 = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                    if (pchute2 < 0.999 * celula[0].acsr.ipr.Pres)
                        pchute2 = 0.999 * celula[0].acsr.ipr.Pres;
                }
                val = marchaProdPerm2(pchute2);
                if (fabs(val) > 1.01e10) {
                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
                if (val < 0 && val > -0.9e10)
                    chuteNeg = pchute2; // atualizando o chuteNeg
                kontaiter++;
                if (kontaiter > 50) { // limite de iteracoes, falha na busca do segundo chute
                    // fim da simulacao ou aviso de falha
                    if ((*vg1dSP).chaverede == 0) {
                        if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                            NumError("Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm2 atingiu maximo de iteracoees");
                        else {
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    } else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val < -0.9e10) { // a reduÃ§Ã£o de pressao foi demais e a marcha nÃ£o foi capaz
                    // de ir ate o final sem que a pressao ficasse inferior a 0.5kgf/cm2
                    // deve-se aumentar a estimativa de pressao baixa
                    chutelim = pchute2;
                    pchute2 = 0.5 * (pchute2 + pchuteAux); // valor intermediario entre a menor pressao
                    // que leva a val<0 e a pressao baixa demais
                    if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute2) < (*vg1dSP).localtiny)
                        pchute2 = 0.9 * arq.tabent.pmax;
                    if (celula[0].acsr.tipo == 3 &&
                        (celula[0].acsr.ipr.Pres - pchute2) < 0.001 * celula[0].acsr.ipr.Pres) {
                        pchute2 = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                        if (pchute2 < 0.999 * celula[0].acsr.ipr.Pres)
                            pchute2 = 0.999 * celula[0].acsr.ipr.Pres;
                    }
                    val = marchaProdPerm2(pchute2);
                    if (fabs(val) > 1.01e10) {
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                    if (val < 0 && val > -0.9e10)
                        chuteNeg = pchute2;
                    kontaiter++;
                    if (kontaiter > 50) {
                        if ((*vg1dSP).chaverede == 0) {
                            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                                NumError(
                                    "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm2 atingiu maximo de iteracoes");
                            else {
                                cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                                if ((*vg1dSP).iterRede > 0)
                                    return -1.1e10;
                                else
                                    return 1.1e10;
                            }
                        } else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = pchute2;
        } else if (val > 0.) { // caso em que Vazao no choke<Vazao da mistura na tubulaÃ§Ã£o,
            // isto implica em pressao de chute baixa , deve-se agora buscar
            // uma pressao de chute alta para que val seja negativo e assim iniciar o processo
            // de calculo de zero de funcao
            chutePos = pchute; // armazenando o valor de chute de pressao que da o valor positivo
            // a cada nova busca em que val se aproxima de zero, mas ainda positivo
            // chutePos e atualizado com a Ãºltima pressao de chute
            while (val > 0) {
                pchuteAux = pchute2;
                if ((*vg1dSP).chaverede == 0)
                    pchute2 *= amplifica; // Aumentando a pressao na busca de val>0
                else
                    pchute2 *= mult2; // Aumentando a pressao na busca de val>0
                if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute2) < (*vg1dSP).localtiny)
                    pchute2 = 0.9 * arq.tabent.pmax;
                int limpres = 0;
                if (celula[0].acsr.tipo == 3 &&
                    (celula[0].acsr.ipr.Pres - pchute2) < 0.001 * celula[0].acsr.ipr.Pres) {
                    if (chute < 0) {
                        pchute2 = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                        if (pchute2 < 0.999 * celula[0].acsr.ipr.Pres)
                            pchute2 = 0.999 * celula[0].acsr.ipr.Pres;
                        if (fabs(pchute2 - pchute) < 1e-15) {
                            pchute2 = -(1 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                            if (pchute2 < 0.999 * celula[0].acsr.ipr.Pres)
                                pchute2 = 0.999 * celula[0].acsr.ipr.Pres;
                        }
                    } else {
                        pchute2 = pchuteAux * 1.0001;
                    }
                    limpres = 1;
                }
                val = marchaProdPerm2(pchute2);
                if (fabs(val) > 1.01e10) {
                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
                if (val > 0. && limpres == 1) {
                    int iterpres = 0;
                    while (val > 0 && iterpres < 10) {
                        pchute2 *= 1.001;
                        val = marchaProdPerm2(pchute2);
                        if (fabs(val) > 1.01e10) {
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                        iterpres++;
                    }
                    if (iterpres >= 10) {
                        if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm2 atingiu maximo de iteracoes");
                        else {
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
                if (val > 0 && val < 0.9e10)
                    chutePos = pchute2; // atualizando o chutePos
                kontaiter++;
                if (kontaiter > 50) { // limite de iteracoes, falha na busca do segundo chute
                    // fim da simulacao ou aviso de falha
                    if ((*vg1dSP).chaverede == 0) {
                        if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm2 atingiu maximo de iteracoes");
                        else {
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    } else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val > 0.9e10) { // o incremento de pressao foi demais e a marcha nao foi capaz
                    // de ir ate o final sem que a pressao ficasse maior do que a pressao estatica
                    // em um eventual IPR no meio da marcha ou maior que o limitre maximo de pressao
                    // da tabela PVTSim, quando for este o caso
                    // deve-se diminuir a estimativa de pressao baixa
                    chutelim = pchute2;
                    pchute2 = 0.5 * (pchute2 + pchuteAux); // valor intermediario entre a maior pressao
                    // que leva a val>0 e a pressao alta demais
                    if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute2) < (*vg1dSP).localtiny)
                        pchute2 = 0.9 * arq.tabent.pmax;
                    if (celula[0].acsr.tipo == 3 &&
                        (celula[0].acsr.ipr.Pres - pchute2) < 0.001 * celula[0].acsr.ipr.Pres) {
                        pchute2 = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                        if (pchute2 < 0.999 * celula[0].acsr.ipr.Pres)
                            pchute2 = 0.999 * celula[0].acsr.ipr.Pres;
                    }
                    val = marchaProdPerm2(pchute2);
                    if (fabs(val) > 1.01e10) {
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                    if (val > 0 && val < 0.9e10)
                        chutePos = pchute2;
                    kontaiter++;

                    if (kontaiter > 50) {
                        if ((*vg1dSP).chaverede == 0) {
                            if (arq.transiente == 0 && chute < 0 && arq.AP == 0)
                                NumError(
                                    "Busca de valores iniciais para calculo de zero de funcao em buscaProdPfundoPerm2 atingiu maximo de iteracoes");
                            else {
                                cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                                if ((*vg1dSP).iterRede > 0)
                                    return -1.1e10;
                                else
                                    return 1.1e10;
                            }
                        } else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = pchute2;
        }
        return zriddr(chuteNeg, chutePos, 1, 1); // com as duas estimativas de pressao
        // em posicoes de sinal contrario da curva, inicia-se o processo de calculo de zero
        // de funcao
    }
}

double SProd::buscaProdPfundoPerm3(double pentrada) {
    revPerm = 0;
    monitConvPerm = 1000.;
    double alfini;
    double betini;

    // estimativa da fracao de vazio na primeira celula do sistema
    // fonte de massa no inicio da tubulacao
    // da mesma maneira que no caso de fonte de liquido, fracao de vazio= sem escorregamento
    celula[0].temp = celula[0].acsr.injm.temp;
    if (arq.flashCompleto == 2) {
        if (arq.tabelaDinamica == 0)
            celula[0].flui.atualizaPropComp(pentrada, celula[0].temp);
        celula[0].acsr.injm.FluidoPro.atualizaPropComp(pentrada, celula[0].temp);
    }
    celula[0].acsr.injm.condTermo = 0;
    celula[0].pres = pentrada;
    renovaFonte(0);
    double qgas = celula[0].acsr.injm.MassG /
                  celula[0].acsr.injm.FluidoPro.MasEspGas(pentrada, celula[0].temp);
    double qliq = celula[0].acsr.injm.MassP /
                      celula[0].acsr.injm.FluidoPro.MasEspLiq(pentrada, celula[0].temp) +
                  celula[0].acsr.injm.MassC /
                      celula[0].acsr.injm.fluidocol.MasEspFlu(pentrada, celula[0].temp);
    celula[0].acsr.injm.FluidoPro.MasEspLiq(pentrada, celula[0].temp);
    double qcomp = celula[0].acsr.injm.MassC /
                   celula[0].acsr.injm.fluidocol.MasEspFlu(pentrada, celula[0].temp);
    alfini = qgas / (qliq + qgas);
    if ((fabs(qcomp) + fabs(qliq)) > 1e-15)
        betini = fabs(qcomp) / (fabs(qcomp) + fabs(qliq));
    else
        betini = 0.;

    // esta marcha e feita para quando se tem alguma fonte no inicio da tubulacao,
    // portanto, admite-se que o duto esta fechado e coloca-se uma fonte no centro da
    // primeira celula. As vazoes na fronteira esquerda da celula sÃ£o portanto = 0
    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].MComp = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;
    double masfim = 1.;
    double masfim0 = -10;
    double presteste = 1.;
    double presteste0 = -10;
    iterperm = 0;

    int limIter = 200; // limite de iteracoes quando a opcao de aceleracao da convergencia esta desligado. desaconselhavel
    // desligar esta opcao, os ganhos sao pouco e a convergencia se torna instavel, principalmente
    // quando o tramo faz perte de um sistema de redes
    if (arq.AceleraConvergPerm == 1) { // opcao aceleracao de convergencia ligada
        limIter = 2;                   // em geral faz-se apenas duas iteracoes de marcha para um determinado chute
        if (arq.lingas == 1 && arq.gasinj.tipoCC == 0)
            limIter = 3; // no caso de se ter
        // uma condicao de contorno na injecao de gas = pressao, observou-se que o acoplamento dinamico
        // entre a linha de gas e de producao e mais difoicil, para se conseguir um sistema
        // melhor acoplado, deve-se fazer uma marcha iterativa a mais
    }
    // arq.CriterioConvergPerm Ã© um criterio de convergencia da marcha, so faz sentido
    // quando a aceleracao de convergencia esta desligada. Observe que esta convergencia nÃ£o
    // e de fato a conevregencia do problema, e apenas um repeticao de marcha para um determinado
    // chute de pressao ou de vazao no inicio da tubulacao. O que a convergencia busca de fato e
    // determinar qual a pressao ou vazao de fundo que satisfaz as condicoes de contorno no fim
    // da tubulacao, esta busca e feita nos metodos de busca.
    while ((fabs(masfim - masfim0) / fabs(masfim) > arq.CriterioConvergPerm ||
            fabs(presteste - presteste0) / fabs(presteste) > arq.CriterioConvergPerm) &&
           iterperm < limIter) {

        masfim0 = masfim;
        presteste0 = presteste;
        if (iterperm == 0 && arq.lingas > 0 && arq.nvalvgas > 0 && verificaAcop == 1)
            IniciaconectaColunaPerm(); // antes de iniciar a primeira iteracao de marcha,
        // faz-se uma estimativa inicial de como se da o acopamento termico entre a coluna e o anular,
        // caso se tenha linha de gas
        else if (arq.lingas > 0 && arq.nvalvgas > 0 && verificaAcop == 1)
            conectaColunaPerm(); // acoplamento termico feito com valores de pressao e temperatura
        // obtidas na primeira iteracao de marcha
        int i;
        int corrigechute = 1;
        while (corrigechute == 1) { // opcao antiga, ja nao tem mais efeito
            // efetivamente, este while sempre so e feito uma vez, quando a marcha consegue ir ate
            // a ultima celula sem problemas, caso ocorra algum problema, a marcha e finalizada e
            // sai do metodo retornando ou 1e10 ou -1e10

            // inicializando as pressoes e fracoes volumetricas das celulas iniciais, centro de celula
            // e fronteira de celula
            celula[0].presauxL = pentrada;
            celula[0].presLini = pentrada;
            celula[0].presL = pentrada;
            celula[0].pres = pentrada;
            celula[1].presL = pentrada;
            celula[0].presini = pentrada;
            celula[1].presLini = pentrada;
            celula[0].presaux = pentrada;
            celula[1].presauxL = pentrada;

            celula[0].alf = alfini;
            celula[0].alfini = alfini;
            celula[0].bet = betini;
            celula[0].betini = betini;
            celula[1].alfL = celula[0].alf;
            celula[1].alfLini = celula[0].alf;
            celula[0].alfPigD = celula[0].alf;
            celula[0].alfPigDini = celula[0].alf;
            celula[0].alfPigE = celula[0].alf;
            celula[0].alfPigEini = celula[0].alf;
            celula[1].betL = celula[0].bet;
            celula[1].betLini = celula[0].bet;
            celula[0].betPigD = celula[0].bet;
            celula[0].betPigDini = celula[0].bet;
            celula[0].betPigE = celula[0].bet;
            celula[0].betPigEini = celula[0].bet;
            celula[0].betI = celula[0].bet;
            celula[1].betLI = celula[0].bet;

            // verifica se ja existe algum problema no inicio da marcha
            if (celula[0].pres <= 0.1 ||
                (arq.usaTabela == 1 && (arq.tabent.pmin - celula[0].pres) > (*vg1dSP).localtiny))
                return -1e10;
            else if ((celula[0].acsr.tipo == 3 &&
                      (celula[0].acsr.ipr.Pres - celula[0].pres) < (*vg1dSP).localtiny))
                return 1e10;
            else if ((celula[0].acsr.tipo == 15 &&
                      (celula[0].acsr.radialPoro.pRes[0] - celula[0].pres) < (*vg1dSP).localtiny))
                return 1e10;
            else if ((celula[0].acsr.tipo == 16 &&
                      (celula[0].acsr.poroso2D.dados.pRes - celula[0].pres) < (*vg1dSP).localtiny))
                return 1e10;
            // IniciaVazValvGasPerm e um metodo que faz uma estimativa da vazao na valvula de GL
            // quando ainda nao foi feita a marcha na linha de gas. Neste caso, ele recebe o
            // indice da celula de producao e verifica se nesta celula existe uma VGL, se existir,
            // caso a condicao na linha de gas seja vazao injetada, divide a vazao injetada pelo numero de
            // valvulas e indica este valor para a VGL relacionada a celula de producao
            // caso a condicao seja pressao de injecao, faz-se uma estimativa da pressao na linha de gas
            // na posicao da VGL por hidrotatica e com isto se calcula a vazao de injecao da VGL
            if (arq.lingas > 0 && arq.nvalvgas > 0 && iterperm == 0)
                IniciaVazValvGasPerm(0);
            i = 1;
            // inicio da marcha propriamente dita
            while (i <= ncel && celula[i - 1].pres >= 0.1 && fabs(pentrada - celula[0].pres) < (*vg1dSP).localtiny) {

                RenovaPresPermMon(i, 0); // avanco da marcha para obter a pressao na fronteira esquerda
                // da celula i
                // teste para ver se ocorreu algum problema:
                if (arq.usaTabela == 1 && (arq.tabent.pmax - celula[i].presaux) < (*vg1dSP).localtiny)
                    return 1e10;
                if (celula[i].presaux <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i].presaux) > (*vg1dSP).localtiny)) {
                    return -1e10;
                }
                atualizaPeriPmonProd(i); // atualizacao da pressao da fronteira esquerda,
                // caso exista alguma BCS ou incremento de pressao
                if (arq.flashCompleto != 2)
                    RenovaMassPerm(i); // verifica se existe alguma fonte na celula anterior, com isto, atualiza
                // as vazoes massica na fronteira a esquerda, alÃ©m das propriedades dos fluidos,
                // densidade do gas, RGO, API, BSW, beta
                else
                    RenovaMassPermComp(i);
                RenovaTempPerm(i, 0); // faz o avanco da temperatura, da celula i-1 para a celula i
                // verifica se teve algum problema nos limites de temperatura
                // caso se esteja trabalhando com tabela PVTSim
                if (isnan(celula[i].temp))
                    NumError("Temperatrura na linha de producao com valor NaN");
                if (arq.usaTabela == 1 && (celula[i].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                    celula[i].temp = arq.tabent.tmin;
                atualizaPeriTempProd(i); // mera atualizacao de atributos de temperatura a esquerda e a direita
                RenovaPresPermJus(i, 0); // evolui a pressao  fronteira a esquerda da celula i para o
                // seu centro de celula
                // verifica se ocorreu algum problema nesta evolucao de de pressao no centro da
                // celula
                if (arq.usaTabela == 1 && (arq.tabent.pmax - celula[i].pres) < (*vg1dSP).localtiny) {
                    return 1e10;
                }
                if (celula[i].pres <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i].pres) > (*vg1dSP).localtiny)) {
                    return -1e10;
                }
                atualizaPeriPjusProd(i); // mera atualizacao de atributos que guardam valores de pressao
                // das celulas a esquerda e a direita
                for (int j = 0; j < arq.nvalvgas; j++) { // reavaliacao da vazao da valvula de gas lift, quando
                    // a celula tem uma.
                    // P.S. parece uma acao desnecessÃ¡ria e talvez atÃ© um complicador
                    // densecessario, em vavliacao
                    if (posicVGLP[j] == i) {
                        int k = posicVGLG[j];
                        calcVazGasPerm(k);
                    }
                }
                if (arq.tipoFluido == 0)
                    RenovaTransMassPerm(i - 1);
                else
                    RenovaTransMassPermGas(i - 1); // caso seja uma tabela PVTSim, calcula-se a
                // taxa de transferÃªncia de massa entre as fases para o uso no calculo de
                // calor latente da equacao de energia
                if (arq.ordperm > 1) { // correcao de segunda ordem
                    double D0presaux = celula[i].presaux - celula[i - 1].pres;
                    double D0pres = celula[i].pres - celula[i].presaux;
                    double D0temp = celula[i].temp - celula[i - 1].temp;
                    RenovaPresPermMon(i, 1);
                    atualizaPeriPmonProd(i);
                    RenovaMassPerm(i);
                    RenovaTempPerm(i, 1);
                    RenovaPresPermJus(i, 1);
                    celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                    celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                    celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                    atualizaPeriPjusProd(i);
                    atualizaPeriPmonProd(i);
                    atualizaPeriTempProd(i);
                    RenovaMassPerm(i);
                    if (arq.tipoFluido == 0)
                        RenovaTransMassPerm(i - 1);
                    else
                        RenovaTransMassPermGas(i - 1);
                }

                // apÃ³s se atingir a pressao no centro da celula i, primeira iteracao de marcha
                // verifica-se se existe uma VGL em i e faz-se uma estimativa inicial da Vazao de
                // GL (caso exista linha de gas). Observar que isto sÃ³ Ã© feito para a iteracao zero.
                if (arq.lingas > 0 && arq.nvalvgas > 0 && iterperm == 0)
                    IniciaVazValvGasPerm(i);
                i++;

                // teste para verificar se a pressao do centro de celula ficou acima
                // da pressao estatica de uma eventual IPR
                if (celula[i - 1].pres <= 0.1 ||
                    (arq.usaTabela == 1 && (arq.tabent.pmin - celula[i - 1].pres) > (*vg1dSP).localtiny)) {
                    return -1e10;
                } else if ((celula[i - 1].acsr.tipo == 3 &&
                            ((celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres) < (*vg1dSP).localtiny) && i == 1)) {
                    return 1e10;
                } else if ((celula[i - 1].acsr.tipo == 15 &&
                            ((celula[i - 1].acsr.radialPoro.pRes[0] - celula[i - 1].pres) < (*vg1dSP).localtiny) && i == 1)) {
                    return 1e10;
                } else if ((celula[i - 1].acsr.tipo == 16 &&
                            ((celula[i - 1].acsr.poroso2D.dados.pRes - celula[i - 1].pres) < (*vg1dSP).localtiny) && i == 1)) {
                    return 1e10;
                }
            }
            if (i == ncel + 1)
                corrigechute = 0; // fim da marcha
        }
        // apÃ³s o fim da marcha da linha de produÃ§Ã£o, Ã© feita a marcha da linha de gas
        // caso exista
        if (pentrada > 0 && arq.lingas > 0 && arq.nvalvgas > 0) {
            if (celulaG[0].tipoCC == 0) {            // marcha para o caso, pressao de injecao
                if (arq.chokes.abertura[0] >= 0.2) { // choke de injecao inativo
                    for (int iter = 0; iter < 1; iter++) {
                        marchaGasPerm1();
                    }
                } else
                    buscaGasPresPerm3(); // choke de injecao ativo
            } else
                buscaGasPresPerm2(); // marcha na linha de gas para o caso de vazao de injecao
        }

        if (arq.acopColAnulPermForte > 0 && arq.lingas > 0) {
            atualizaProp();
            atualizaVelTermPerm();
            calcDTPseudoTrans();
            conectaColuna();
            for (int kontaPseudo = 0; kontaPseudo < arq.acopColAnulPermForte; kontaPseudo++) {
                for (int i = 0; i <= ncelGas; i++)
                    celulaG[i].tempini = celulaG[i].temp;
                for (int i = 1; i <= ncelGas; i++) {
                    calctempGas(i, celulaG[i - 1].tempini, 1);
                }
                celula[0].tempini = celula[0].temp;
                celula[1].tempLini = celula[1].tempL;
                celula[1].tempL = celula[0].temp;
                for (int i = 1; i <= ncel; i++) {
                    celula[i].tempini = celula[i].temp;
                }
                for (int i = 1; i <= ncel; i++) {
                    calctemp(i, celula[i].tempini, 1);
                }
                atualizaProp();
                calcDTPseudoTrans();
                conectaColuna();
            }
        }

        masfim = celula[ncel - 1].MC; // guarda valor de vazao para se calcular o erro, quando a
        // opcao de acelerador de convergencia esta desligado
        presteste = celula[ncel].pres; // guarda valor de pressao para se calcular o erro, quando a
        // opcao de acelerador de convergencia esta desligado
        iterperm++; // atualiza a ieteracao da marcha
        if (iterperm > 200 && arq.AP == 0)
            NumError("ConvergÃƒÂªncia em marchaProdPerm1 atingiu maximo de iteracoes");
        else if (iterperm > 200)
            return 1.1e10;
    }

    double corrigePresF = 0.;
    if (((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1) && arq.corrigeContSep == 1)
        corrigePresF = RenovaPresPermNcel();

    pGSup = (celula[ncel].pres + corrigePresF);

    return celula[ncel].pres; // caso a marcha tenha conseguido ir atÃ© a Ãºltima celula,
    // retorna a pressao da ultima celula calculada pela marcha
}

double SProd::marchaProdPresPres1(double mchute) {

    int corrigechute = 1;
    double alfini = 0.;
    double betini = 0.;

    if (celula[0].acsr.tipo == 1) {
        celula[0].temp = celula[0].acsr.injg.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(celula[0].pres, celula[0].temp);
            celula[0].acsr.injg.FluidoPro.atualizaPropComp(celula[0].pres, celula[0].temp, -1, NULL, NULL, celula[0].acsr.injg.seco);
        }
        celula[0].acsr.injg.QGas = mchute;
    } else if (celula[0].acsr.tipo == 2) {
        celula[0].temp = celula[0].acsr.injl.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(celula[0].pres, celula[0].temp);
            celula[0].acsr.injl.FluidoPro.atualizaPropComp(celula[0].pres, celula[0].temp);
        }
        celula[0].acsr.injl.QLiq = mchute;
    }

    if (celula[0].acsr.tipo == 1) {
        if (celula[0].acsr.injg.seco == 1) {
            alfini = 1.;
            betini = 0.;
        } else {
            double masgas = celula[0].acsr.injg.VMas(celula[0].pres, celula[0].temp);
            double tit;
            if (arq.flashCompleto != 2)
                tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
            else
                tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
            double masT = masgas / tit;
            tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(celula[0].pres, celula[0].temp);
            double qgas = masT * tit /
                          celula[0].acsr.injg.FluidoPro.MasEspGas(celula[0].pres, celula[0].temp);
            double qliq = masT * (1. - tit) /
                          celula[0].acsr.injg.FluidoPro.MasEspLiq(celula[0].pres, celula[0].temp);
            double qcomp = celula[0].acsr.injg.razCompGas *
                           celula[0].acsr.injg.QGas * celula[0].acsr.injg.fluidocol.MasEspFlu(1., 20.) /
                           celula[0].acsr.injg.fluidocol.MasEspFlu(celula[0].pres, celula[0].temp);
            qcomp /= 86400.;
            alfini = qgas / (qliq + qcomp + qgas);
            if ((fabs(qcomp) + fabs(qliq)) > 1e-15)
                betini = fabs(qcomp) / (fabs(qcomp) + fabs(qliq));
            else
                betini = 0.;
        }
    } else if (celula[0].acsr.tipo == 2) {
        double qgas = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                      (1. - celula[0].acsr.injl.FluidoPro.BSW) *
                      (celula[0].acsr.injl.FluidoPro.RGO -
                       celula[0].acsr.injl.FluidoPro.rDgD * celula[0].acsr.injl.FluidoPro.RS(celula[0].pres, celula[0].temp) * 6.29 / 35.31467) *
                      celula[0].acsr.injl.FluidoPro.Deng * 1.225 / celula[0].acsr.injl.FluidoPro.MasEspGas(celula[0].pres, celula[0].temp);
        double qliq = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          (1. - celula[0].acsr.injl.FluidoPro.BSW) * celula[0].acsr.injl.FluidoPro.BOFunc(celula[0].pres, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          celula[0].acsr.injl.FluidoPro.BSW * celula[0].acsr.injl.FluidoPro.BAFunc(celula[0].pres, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet;
        alfini = qgas / (qliq + qgas);
        betini = celula[0].acsr.injl.bet;
    }

    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].MComp = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;

    int i;

    celula[0].presauxL = celula[0].pres;
    celula[0].presLini = celula[0].pres;
    celula[0].presL = celula[0].pres;
    celula[1].presL = celula[0].pres;
    celula[0].presini = celula[0].pres;
    celula[1].presLini = celula[0].pres;
    celula[0].presaux = celula[0].pres;
    celula[1].presauxL = celula[0].pres;

    celula[0].alf = alfini;
    celula[0].alfini = alfini;
    celula[0].bet = betini;
    celula[0].betini = betini;
    celula[1].alfL = celula[0].alf;
    celula[1].alfLini = celula[0].alf;
    celula[0].alfPigD = celula[0].alf;
    celula[0].alfPigDini = celula[0].alf;
    celula[0].alfPigE = celula[0].alf;
    celula[0].alfPigEini = celula[0].alf;
    celula[1].betL = celula[0].bet;
    celula[1].betLini = celula[0].bet;
    celula[0].betPigD = celula[0].bet;
    celula[0].betPigDini = celula[0].bet;
    celula[0].betPigE = celula[0].bet;
    celula[0].betPigEini = celula[0].bet;
    celula[0].betI = celula[0].bet;
    celula[1].betLI = celula[0].bet;

    iterperm = 0;
    while (iterperm < 3) {
        i = 1;
        while (i <= ncel && celula[i - 1].pres >= 1.) {

            RenovaPresPermMon(i, 0);
            atualizaPeriPmonProd(i);
            if (arq.flashCompleto != 2)
                RenovaMassPerm(i);
            else
                RenovaMassPermComp(i);
            RenovaTempPerm(i, 0);
            if (i > 1160) {
                int para;
                para = 0;
            }
            if (arq.usaTabela == 1 && (celula[i].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                celula[i].temp = arq.tabent.tmin;
            atualizaPeriTempProd(i);
            RenovaPresPermJus(i, 0);
            atualizaPeriPjusProd(i);
            if (arq.tipoFluido == 0)
                RenovaTransMassPerm(i - 1);
            else
                RenovaTransMassPermGas(i - 1);
            if (arq.ordperm > 1) {
                double D0presaux = celula[i].presaux - celula[i - 1].pres;
                double D0pres = celula[i].pres - celula[i].presaux;
                double D0temp = celula[i].temp - celula[i - 1].temp;
                RenovaPresPermMon(i, 1);
                atualizaPeriPmonProd(i);
                RenovaMassPerm(i);
                RenovaTempPerm(i, 1);
                if (isnan(celula[i].temp))
                    NumError("Temperatrura na linha de producao com valor NaN");
                RenovaPresPermJus(i, 1);
                celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                atualizaPeriPjusProd(i);
                atualizaPeriPmonProd(i);
                atualizaPeriTempProd(i);
                RenovaMassPerm(i);
                if (arq.tipoFluido == 0)
                    RenovaTransMassPerm(i - 1);
                else
                    RenovaTransMassPermGas(i - 1);
            }
            i++;
            if (celula[i - 1].pres <= 1)
                return -1e10;
        }

        iterperm++;
    }

    double corrigePresF = 0.;
    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)
        corrigePresF = RenovaPresPermNcel();

    monitConvPermBase = pGSup;
    return celula[ncel].pres + corrigePresF - pGSup;
}

double SProd::marchaProdPresPres1Rev(double mchute) {

    double alfini = 0.;
    double betini = 0.;
    trocaTermicaLenta = 0.05;

    if (celula[0].acsr.tipo == 1) {
        celula[0].temp = celula[0].acsr.injg.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(celula[0].pres, celula[0].temp);
            celula[0].acsr.injg.FluidoPro.atualizaPropComp(celula[0].pres, celula[0].temp, -1, NULL, NULL, celula[0].acsr.injg.seco);
        }
        celula[0].acsr.injg.QGas = mchute;
    } else if (celula[0].acsr.tipo == 2) {
        celula[0].temp = celula[0].acsr.injl.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(celula[0].pres, celula[0].temp);
            celula[0].acsr.injl.FluidoPro.atualizaPropComp(celula[0].pres, celula[0].temp);
        }
        celula[0].acsr.injl.QLiq = mchute;
    }

    if (celula[0].acsr.tipo == 1) {
        if (celula[0].acsr.injg.seco == 1) {
            alfini = 1.;
            betini = 0.;
        } else {
            double masgas = celula[0].acsr.injg.VMas(celula[0].pres, celula[0].temp);
            double tit;
            if (arq.flashCompleto != 2)
                tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
            else
                tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
            double masT = masgas / tit;
            tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(celula[0].pres, celula[0].temp);
            double qgas = masT * tit /
                          celula[0].acsr.injg.FluidoPro.MasEspGas(celula[0].pres, celula[0].temp);
            double qliq = masT * (1. - tit) /
                          celula[0].acsr.injg.FluidoPro.MasEspLiq(celula[0].pres, celula[0].temp);
            double qcomp = celula[0].acsr.injg.razCompGas *
                           celula[0].acsr.injg.QGas * celula[0].acsr.injg.fluidocol.MasEspFlu(1., 20.) /
                           celula[0].acsr.injg.fluidocol.MasEspFlu(celula[0].pres, celula[0].temp);
            qcomp /= 86400.;
            alfini = qgas / (qliq + qcomp + qgas);
            if ((fabs(qcomp) + fabs(qliq)) > 1e-15)
                betini = fabs(qcomp) / (fabs(qcomp) + fabs(qliq));
            else
                betini = 0.;
        }
    } else if (celula[0].acsr.tipo == 2) {
        double qgas = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                      (1. - celula[0].acsr.injl.FluidoPro.BSW) *
                      (celula[0].acsr.injl.FluidoPro.RGO -
                       celula[0].acsr.injl.FluidoPro.rDgD * celula[0].acsr.injl.FluidoPro.RS(celula[0].pres, celula[0].temp) * 6.29 / 35.31467) *
                      celula[0].acsr.injl.FluidoPro.Deng * 1.225 / celula[0].acsr.injl.FluidoPro.MasEspGas(celula[0].pres, celula[0].temp);
        double qliq = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          (1. - celula[0].acsr.injl.FluidoPro.BSW) * celula[0].acsr.injl.FluidoPro.BOFunc(celula[0].pres, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          celula[0].acsr.injl.FluidoPro.BSW * celula[0].acsr.injl.FluidoPro.BAFunc(celula[0].pres, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet;
        alfini = qgas / (qliq + qgas);
        betini = celula[0].acsr.injl.bet;
    }

    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].MComp = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;

    int i;

    celula[0].presauxL = celula[0].pres;
    celula[0].presLini = celula[0].pres;
    celula[0].presL = celula[0].pres;
    celula[1].presL = celula[0].pres;
    celula[0].presini = celula[0].pres;
    celula[1].presLini = celula[0].pres;
    celula[0].presaux = celula[0].pres;
    celula[1].presauxL = celula[0].pres;

    celula[0].alf = alfini;
    celula[0].alfini = alfini;
    celula[0].bet = betini;
    celula[0].betini = betini;
    celula[1].alfL = celula[0].alf;
    celula[1].alfLini = celula[0].alf;
    celula[0].alfPigD = celula[0].alf;
    celula[0].alfPigDini = celula[0].alf;
    celula[0].alfPigE = celula[0].alf;
    celula[0].alfPigEini = celula[0].alf;
    celula[1].betL = celula[0].bet;
    celula[1].betLini = celula[0].bet;
    celula[0].betPigD = celula[0].bet;
    celula[0].betPigDini = celula[0].bet;
    celula[0].betPigE = celula[0].bet;
    celula[0].betPigEini = celula[0].bet;
    celula[0].betI = celula[0].bet;
    celula[1].betLI = celula[0].bet;

    iterperm = 0;
    double tempteste = celula[0].temp;
    double tempteste0 = -1000;
    while (iterperm < 3 || fabs(tempteste - tempteste0) / ((tempteste) + 273) > 0.001) {
        i = 1;
        tempteste0 = tempteste;
        while (i <= ncel && celula[i - 1].pres >= 1.) {

            RenovaPresPermMon(i, 0);
            atualizaPeriPmonProd(i);
            if (arq.flashCompleto != 2)
                RenovaMassPerm(i);
            else
                RenovaMassPermComp(i);
            if (isnan(celula[i].temp))
                NumError("Temperatrura na linha de producao com valor NaN");
            if (arq.usaTabela == 1 && (celula[i].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                celula[i].temp = arq.tabent.tmin;
            atualizaPeriTempProd(i);
            RenovaPresPermJus(i, 0);
            atualizaPeriPjusProd(i);
            if (arq.tipoFluido == 0)
                RenovaTransMassPerm(i - 1);
            else
                RenovaTransMassPermGas(i - 1);
            if (arq.ordperm > 1) {
                double D0presaux = celula[i].presaux - celula[i - 1].pres;
                double D0pres = celula[i].pres - celula[i].presaux;
                double D0temp = celula[i].temp - celula[i - 1].temp;
                RenovaPresPermMon(i, 1);
                atualizaPeriPmonProd(i);
                RenovaMassPerm(i);
                RenovaPresPermJus(i, 1);
                celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                atualizaPeriPjusProd(i);
                atualizaPeriPmonProd(i);
                atualizaPeriTempProd(i);
                RenovaMassPerm(i);
                if (arq.tipoFluido == 0)
                    RenovaTransMassPerm(i - 1);
                else
                    RenovaTransMassPermGas(i - 1);
            }
            i++;
            if (celula[i - 1].pres <= 1)
                return -1e10;
        }

        iterperm++;
        if (iterperm > 200 && arq.AP == 0)
            NumError("ConvergÃƒÂªncia em marchaProdPerm1 atingiu maximo de iteracoes");
        else if (iterperm > 200)
            return 1.1e10;
        if (arq.tipoFluido != 10000) {
            celula[ncel].temp = tempRev;
            if (iterperm < 100) {
                int lento = 0;
                trocaTermicaLenta = 0;
                for (int ktemp = ncel - 1; ktemp >= 0; ktemp--) {

                    double area = celula[ktemp].duto.area;
                    double ugsmed;
                    ugsmed = fabs(celula[ktemp].QG) / area; // velocidade superficial de gas
                    double ulsmed;
                    ulsmed = fabs(celula[ktemp].QL) / area; // velocidade superficial de liquido
                    if (fabs(ugsmed + ulsmed) <= 0.1)
                        lento += 1;
                }
                if (iterperm > 10 && trocaTermicaLenta < 0.01) {
                    trocaTermicaLenta -= 0.01;
                    iterperm = 0;
                }
                for (int ktemp = ncel - 1; ktemp >= 0; ktemp--) {
                    RenovaTempPermRev(ktemp, 0);
                    if (arq.usaTabela == 1 && (celula[ktemp].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                        celula[ktemp].temp = arq.tabent.tmin;
                    atualizaPeriTempProd(ktemp); // mera atualizacao de atributos de temperatura a esquerda e a direita
                }
            }
            tempteste = celula[0].temp;
        } else {
            atualizaProp();
            atualizaVelTermPerm();
            calcDTPseudoTrans();
            for (int kontaPseudo = 0; kontaPseudo < 20; kontaPseudo++) {
                celula[0].tempini = celula[0].temp;
                celula[1].tempLini = celula[1].tempL;
                celula[1].tempL = celula[0].temp;
                celula[ncel].temp = tempRev;
                for (int i = 0; i < ncel; i++) {
                    celula[i].tempini = celula[i].temp;
                }
                for (int itemp = 1; itemp <= ncel; itemp++) {
                    calctemp(itemp, celula[itemp].tempini, 1);
                }
                atualizaProp();
                calcDTPseudoTrans();
            }
        }
    }
    double corrigePresF = 0.;
    if ((*vg1dSP).chaverede == 0 || noextremo == 1 || (*vg1dSP).chaveRedeParalela == 1)
        corrigePresF = RenovaPresPermNcel();
    monitConvPermBase = pGSup;
    return celula[ncel].pres + corrigePresF - pGSup;
}

double SProd::buscaProdPresPresPerm(double chute, double maxvaz, int kontaiter) {
    revPerm = 0;
    monitConvPerm = 1000.;
    double mchute = chute;
    double val = marchaProdPresPres1(mchute);
    buscaIni = 1;
    if (val < -0.9e10) {
        while (val < -0.9e10 && kontaiter < 800) {
            mchute *= 0.9;
            val = marchaProdPresPres1(mchute);
            if (mchute < 1e-5) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
        }
        val = marchaProdPresPres1(mchute);
        kontaiter++;
    }
    double mchuteAux;
    double mchute2 = mchute;
    double chutelim = 0.;

    double chuteNeg = 0.;
    double chutePos = 0.;
    int testaEscoa = 1;
    double sentido = 1.;
    double val0 = val;
    int reversao = 0;
    int kontaReverso;
    if (fabs(val) < 1e-3)
        return mchute;
    else {
        if (val * sentido < 0.) {
            chuteNeg = mchute;
            kontaReverso = 0;
            while (val * sentido < 0) {
                mchuteAux = mchute2;
                mchute2 *= (1. - arq.buscaFC);
                val = marchaProdPresPres1(mchute2);
                if (val > -0.9e10) {
                    if (val < val0) {
                        kontaReverso++;
                        if (kontaReverso == 2) {
                            reversao = 1;
                            val = 1;
                        } else {
                            arq.buscaFC = -fabs(arq.buscaFC);
                            val0 = val;
                        }
                    } else
                        val0 = val;
                }
                if (val * sentido < 0 && val > -0.9e10)
                    chuteNeg = mchute2;
                kontaiter++;
                double vel = mchute2;
                double bet = celula[1].bet;
                double alf = celula[1].alf;
                double p1 = celula[0].pres;
                double t1 = celula[0].temp;
                double rC = celula[0].fluicol.MasEspFlu(p1, t1);
                double rP = celula[0].flui.MasEspLiq(p1, t1);
                double rG = celula[0].flui.MasEspGas(p1, t1);
                double rmisL = (1 - bet) * rP + bet * rC;
                double rmis = (1 - alf) * rmisL + alf * rG;
                double rCst = celula[0].fluicol.MasEspFlu(1., 20.);
                double rPst = celula[0].flui.MasEspLiq(1., 20.);
                double rGst = celula[0].flui.MasEspGas(1., 20.);
                double rmisLst = (1 - bet) * rPst + bet * rCst;
                double multiplica;
                if (celula[0].acsr.tipo == 2)
                    multiplica = rmisLst;
                else
                    multiplica = rGst;
                vel = mchute2 * multiplica / (rmis * celula[0].duto.area * 86400);
                if (kontaiter > 200 && vel > 0.01) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (vel <= 0.01) {
                    testaEscoa = 0;
                    val = 1.;
                }
                while (val < -0.9e10) {
                    chutelim = mchute2;
                    mchute2 = 0.5 * (mchute2 + mchuteAux);
                    val = marchaProdPresPres1(mchute2);
                    if (val * sentido < 0 && val > -0.9e10)
                        chuteNeg = mchute2;
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = mchute2;
        } else if (val * sentido > 0.) {
            chutePos = mchute;
            kontaReverso = 0;
            while (val * sentido > 0) {
                mchuteAux = mchute2;
                mchute2 *= (1. + arq.buscaFC);
                val = marchaProdPresPres1(mchute2);
                if (val > -0.9e10) {
                    if (val > val0) {
                        kontaReverso++;
                        if (kontaReverso == 2) {
                            reversao = 1;
                            val = -1;
                        } else {
                            arq.buscaFC = -fabs(arq.buscaFC);
                            val0 = val;
                        }
                    } else
                        val0 = val;
                }
                if ((*vg1dSP).chaverede != 0 && mchute2 > maxvaz && maxvaz > 0) {
                    testaEscoa = 0;
                    val = -1.;
                }
                if (val * sentido > 0 && val > -0.9e10)
                    chutePos = mchute2;
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm  atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val < -0.9e10) {
                    chutelim = mchute2;
                    mchute2 = 0.5 * (mchute2 + mchuteAux);
                    val = marchaProdPresPres1(mchute2);
                    if (val * sentido > 0 && val > -0.9e10)
                        chutePos = mchute2;
                    kontaiter++;

                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = mchute2;
        }
        if (testaEscoa == 1 && reversao == 0)
            return zriddr(chuteNeg, chutePos, 2, 0);
        else if (reversao == 0) {
            if (celula[0].acsr.tipo == 1) {
                celula[0].acsr.injg.QGas = -2121212121;
            } else if (celula[0].acsr.tipo == 2) {
                celula[0].acsr.injl.QLiq = -2121212121;
            }
            return 0.;
        } else {
            return buscaProdPresPresPermRev(-chute, -maxvaz, kontaiter);
        }
    }
}

double SProd::buscaProdPresPresPermRev(double chute, double maxvaz, int kontaiter) {
    revPerm = 1;
    monitConvPerm = 1000.;
    if (celula[0].acsr.tipo == 1) {
        celula[0].acsr.injg.FluidoPro = fluiRevRede;
    } else if (celula[0].acsr.tipo == 2) {
        celula[0].acsr.injl.FluidoPro = fluiRevRede;
    } else if (celula[0].acsr.tipo == 10) {
        celula[0].acsr.injm.FluidoPro = fluiRevRede;
    }
    double mchute = chute;

    double val = marchaProdPresPres1Rev(mchute);
    buscaIni = 1;
    if (val < -0.9e10) {
        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
            NumError(
                "Busca de valores iniciais para callculo de zero de funcao em buscaProdPresPresPermRev com problemas, pressao abaixo de 1 kgf/cm2 na marcha reversa");
        else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    }
    double mchuteAux;
    double mchute2 = mchute;
    double chutelim = 0.;
    arq.buscaFC = fabs(arq.buscaFC);

    double chuteNeg = 0.;
    double chutePos = 0.;
    int testaEscoa = 1;
    double val0 = val;
    int reversao = 0;
    if (fabs(val) < 1e-3)
        return mchute;
    else {
        if (val < 0.) {
            chuteNeg = mchute;
            while (val < 0) {
                mchuteAux = mchute2;
                mchute2 *= (1. + arq.buscaFC);
                val = marchaProdPresPres1Rev(mchute2);
                if (val > -0.9e10) {
                    if (val < val0) {
                        reversao = 1;
                        val = 1;
                    } else
                        val0 = val;
                }
                if (val < 0 && val > -0.9e10)
                    chuteNeg = mchute2;
                kontaiter++;
                double vel = mchute2;
                double bet = celula[1].bet;
                double alf = celula[1].alf;
                double p1 = celula[0].pres;
                double t1 = celula[0].temp;
                double rC = celula[0].fluicol.MasEspFlu(p1, t1);
                double rP = celula[0].flui.MasEspLiq(p1, t1);
                double rG = celula[0].flui.MasEspGas(p1, t1);
                double rmisL = (1 - bet) * rP + bet * rC;
                double rmis = (1 - alf) * rmisL + alf * rG;
                double rCst = celula[0].fluicol.MasEspFlu(1., 20.);
                double rPst = celula[0].flui.MasEspLiq(1., 20.);
                double rGst = celula[0].flui.MasEspGas(1., 20.);
                double rmisLst = (1 - bet) * rPst + bet * rCst;
                double multiplica;
                if (celula[0].acsr.tipo == 2)
                    multiplica = rmisLst;
                else
                    multiplica = rGst;
                vel = mchute2 * multiplica / (rmis * celula[0].duto.area * 86400);
                if (kontaiter > 200 && fabs(vel) > 0.01) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPermRev atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (fabs(vel) <= 0.01) {
                    testaEscoa = 0;
                    val = 1.;
                }
            }
            chutePos = mchute2;
        } else if (val > 0.) {
            chutePos = mchute;
            while (val > 0) {
                mchuteAux = mchute2;
                mchute2 *= (1. - arq.buscaFC);
                val = marchaProdPresPres1Rev(mchute2);
                if (val > -0.9e10) {
                    if (val > val0) {
                        reversao = 1;
                        val = -1;
                    } else
                        val0 = val;
                }
                if (val > 0 && val > -0.9e10)
                    chutePos = mchute2;
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm  atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
            }
            chuteNeg = mchute2;
        }
        if (testaEscoa == 1 && reversao == 0)
            return zriddr(chuteNeg, chutePos, 2, 0);
        else if (reversao == 0) {
            if (celula[0].acsr.tipo == 1) {
                celula[0].acsr.injg.QGas = 0.;
            } else if (celula[0].acsr.tipo == 2) {
                celula[0].acsr.injl.QLiq = 0.;
            }
            return 0.;
        } else {
            return buscaProdPresPresPerm(-chute, -maxvaz, kontaiter);
        }
    }
}

double SProd::marchaProdPresPres2(double mchute) {

    int corrigechute = 1;
    double alfini = 0.;
    double betini = 0.;

    if (celula[0].acsr.tipo == 1) {
        celula[0].temp = celula[0].acsr.injg.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(celula[0].pres, celula[0].temp);
            celula[0].acsr.injg.FluidoPro.atualizaPropComp(celula[0].pres, celula[0].temp, -1, NULL, NULL, celula[0].acsr.injg.seco);
        }
        celula[0].acsr.injg.QGas = mchute;
    } else if (celula[0].acsr.tipo == 2) {
        celula[0].temp = celula[0].acsr.injl.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(celula[0].pres, celula[0].temp);
            celula[0].acsr.injl.FluidoPro.atualizaPropComp(celula[0].pres, celula[0].temp);
        }
        celula[0].acsr.injl.QLiq = mchute;
    }

    if (celula[0].acsr.tipo == 1) {
        if (celula[0].acsr.injg.seco == 1) {
            alfini = 1.;
            betini = 0.;
        } else {
            double masgas = celula[0].acsr.injg.VMas(celula[0].pres, celula[0].temp);
            double tit;
            double masT;
            if (arq.ConContEntrada != 1) {
                if (arq.flashCompleto != 2)
                    tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
                else
                    tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
                masT = masgas / tit;
                tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(celula[0].pres, celula[0].temp);
            } else {
                tit = titE;
                masT = masgas / tit;
            }
            double qgas = masT * tit /
                          celula[0].acsr.injg.FluidoPro.MasEspGas(celula[0].pres, celula[0].temp);
            double qliq = masT * (1. - tit) /
                          celula[0].acsr.injg.FluidoPro.MasEspLiq(celula[0].pres, celula[0].temp);
            double qcomp = celula[0].acsr.injg.razCompGas *
                           celula[0].acsr.injg.QGas * celula[0].acsr.injg.fluidocol.MasEspFlu(1., 20.) /
                           celula[0].acsr.injg.fluidocol.MasEspFlu(celula[0].pres, celula[0].temp);
            qcomp /= 86400.;
            if (fabs(qliq + qgas) > 1e-15)
                alfini = qgas / (qliq + qcomp + qgas);
            else
                alfini = titE;
            if ((fabs(qcomp) + fabs(qliq)) > 1e-15)
                betini = fabs(qcomp) / (fabs(qcomp) + fabs(qliq));
            else
                betini = 0.;
        }
    } else if (celula[0].acsr.tipo == 2) {
        double qgas = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                      (1. - celula[0].acsr.injl.FluidoPro.BSW) *
                      (celula[0].acsr.injl.FluidoPro.RGO -
                       celula[0].acsr.injl.FluidoPro.rDgD * celula[0].acsr.injl.FluidoPro.RS(celula[0].pres, celula[0].temp) * 6.29 / 35.31467) *
                      celula[0].acsr.injl.FluidoPro.Deng * 1.225 / celula[0].acsr.injl.FluidoPro.MasEspGas(celula[0].pres, celula[0].temp);
        double qliq = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          (1. - celula[0].acsr.injl.FluidoPro.BSW) * celula[0].acsr.injl.FluidoPro.BOFunc(celula[0].pres, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          celula[0].acsr.injl.FluidoPro.BSW * celula[0].acsr.injl.FluidoPro.BAFunc(celula[0].pres, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet;
        if (fabs(qliq + qgas) > 1e-15)
            alfini = fabs(qgas / (qliq + qgas));
        else
            alfini = titE;
        betini = celula[0].acsr.injl.bet;
    }

    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].MComp = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;

    int i;

    celula[0].presauxL = celula[0].pres;
    celula[0].presLini = celula[0].pres;
    celula[0].presL = celula[0].pres;
    celula[1].presL = celula[0].pres;
    celula[0].presini = celula[0].pres;
    celula[1].presLini = celula[0].pres;
    celula[0].presaux = celula[0].pres;
    celula[1].presauxL = celula[0].pres;

    celula[0].alf = alfini;
    celula[0].alfini = alfini;
    celula[0].bet = betini;
    celula[0].betini = betini;
    celula[1].alfL = celula[0].alf;
    celula[1].alfLini = celula[0].alf;
    celula[0].alfPigD = celula[0].alf;
    celula[0].alfPigDini = celula[0].alf;
    celula[0].alfPigE = celula[0].alf;
    celula[0].alfPigEini = celula[0].alf;
    celula[1].betL = celula[0].bet;
    celula[1].betLini = celula[0].bet;
    celula[0].betPigD = celula[0].bet;
    celula[0].betPigDini = celula[0].bet;
    celula[0].betPigE = celula[0].bet;
    celula[0].betPigEini = celula[0].bet;
    celula[0].betI = celula[0].bet;
    celula[1].betLI = celula[0].bet;

    iterperm = 0;
    while (iterperm < 3) {
        i = 1;
        while (i <= ncel && celula[i - 1].pres >= 1.) {

            RenovaPresPermMon(i, 0);
            atualizaPeriPmonProd(i);
            if (arq.flashCompleto != 2)
                RenovaMassPerm(i);
            else
                RenovaMassPermComp(i);
            RenovaTempPerm(i, 0);
            if (isnan(celula[i].temp))
                NumError("Temperatrura na linha de producao com valor NaN");
            if (arq.usaTabela == 1 && (celula[i].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                celula[i].temp = arq.tabent.tmin;
            atualizaPeriTempProd(i);
            RenovaPresPermJus(i, 0);
            atualizaPeriPjusProd(i);
            if (arq.tipoFluido == 0)
                RenovaTransMassPerm(i - 1);
            else
                RenovaTransMassPermGas(i - 1);
            if (arq.ordperm > 1) {
                double D0presaux = celula[i].presaux - celula[i - 1].pres;
                double D0pres = celula[i].pres - celula[i].presaux;
                double D0temp = celula[i].temp - celula[i - 1].temp;
                RenovaPresPermMon(i, 1);
                atualizaPeriPmonProd(i);
                RenovaMassPerm(i);
                RenovaTempPerm(i, 1);
                RenovaPresPermJus(i, 1);
                celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                atualizaPeriPjusProd(i);
                atualizaPeriPmonProd(i);
                atualizaPeriTempProd(i);
                RenovaMassPerm(i);
                if (arq.tipoFluido == 0)
                    RenovaTransMassPerm(i - 1);
                else
                    RenovaTransMassPermGas(i - 1);
            }

            i++;
        }
        iterperm++;
    }

    double maxSup;
    if (celula[ncel].pres >= pGSup) {
        double tESup = celula[ncel].temp;
        double alfSup = celula[ncel].alf;
        double betSup = celula[ncel].bet;

        double masentrada = celula[ncel - 1].MR;
        double massgas = celula[ncel - 1].MR - celula[ncel - 1].MliqiniR;
        maxSup = 0.;

        double tit;
        presfim = celula[ncel].pres;
        double rholp = celula[ncel].flui.MasEspLiq(celula[ncel].pres, celula[ncel].temp);
        double rholc = celula[ncel].fluicol.MasEspFlu(celula[ncel].pres, celula[ncel].temp);
        tit = fabs(massgas / masentrada);

        double masChk;

        double ypres = pGSup / presfim;
        masChk = chokeSup.vazmassSachd(ypres, presfim, tESup, alfSup, betSup, tit, celula[ncel - 1].flui,
                                       celula[ncel - 1].fluicol);
        maxSup = chokeSup.vazmaxSachd(presfim, tESup, alfSup, betSup, tit, celula[ncel - 1].flui, celula[ncel - 1].fluicol);
        if (fabs(ypres) > fabs(chokeSup.razpres))
            maxSup = masChk;

        if (chokeSup.AreaGarg > (1e-3) * celula[ncel - 1].duto.area && ypres < 1.) {
            double cplM = (1. - betSup) * celula[ncel].flui.CalorLiq(presfim, tESup) -
                          betSup * celula[ncel].fluicol.CalorLiq(presfim, tESup);
            double jtlM = (1. - betSup) * celula[ncel].flui.JTL(presfim, tESup) - betSup / rholc;
            double cpg = celula[ncel].flui.CalorGas(presfim, tESup);
            double jtgM = celula[ncel].flui.JTG(presfim, tESup);
            arq.valTempChokeJus = tESup + ((1. - tit) * jtlM / cplM + tit * jtgM / cpg) * (pGSup - presfim) * 98066.52;
        }
    } else {
        maxSup = 0.;
        arq.valTempChokeJus = celula[ncel].temp;
    }

    if (fabs(maxSup) > 1e-15)
        monitConvPermBase = fabs(maxSup);
    else
        monitConvPermBase = 1.;
    return maxSup - celula[ncel - 1].MR;
}

double SProd::buscaProdPresPresPerm2(double chute, double maxvaz) {
    revPerm = 0;
    monitConvPerm = 1000.;
    double mchute = chute;
    double val = marchaProdPresPres2(mchute);
    buscaIni = 1;
    int kontaiter = 0;
    if (val < -0.9e10) {
        while (val < -0.9e10 && kontaiter < 400) {
            mchute *= 0.9;
            val = marchaProdPresPres2(mchute);
            if (mchute < 1e-5) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
            val = marchaProdPresPres2(mchute);
            kontaiter++;
        }
    }
    double mchuteAux;
    double mchute2 = mchute;
    double chutelim = 0.;
    kontaiter = 0;

    double chuteNeg = 0.;
    double chutePos = 0.;

    int testaEscoa = 1;
    if (fabs(val) < 1e-3)
        return mchute;
    else {
        if (val < 0.) {
            chuteNeg = mchute;
            while (val < 0) {
                mchuteAux = mchute2;
                mchute2 *= (1. - arq.buscaFC);
                val = marchaProdPresPres2(mchute2);
                if (val < 0 && val > -0.9e10)
                    chuteNeg = mchute2;
                kontaiter++;
                double vel = mchute2;
                double bet = celula[1].bet;
                double alf = celula[1].alf;
                double p1 = celula[0].pres;
                double t1 = celula[0].temp;
                double rC = celula[0].fluicol.MasEspFlu(p1, t1);
                double rP = celula[0].flui.MasEspLiq(p1, t1);
                double rG = celula[0].flui.MasEspGas(p1, t1);
                double rmisL = (1 - bet) * rP + bet * rC;
                double rmis = (1 - alf) * rmisL + alf * rG;
                double rCst = celula[0].fluicol.MasEspFlu(1., 20.);
                double rPst = celula[0].flui.MasEspLiq(1., 20.);
                double rGst = celula[0].flui.MasEspGas(1., 20.);
                double rmisLst = (1 - bet) * rPst + bet * rCst;
                double multiplica;
                if (celula[0].acsr.tipo == 2)
                    multiplica = rmisLst;
                else
                    multiplica = rGst;
                vel = mchute2 * multiplica / (rmis * celula[0].duto.area * 86400);
                if (kontaiter > 200 && vel > 0.01) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (vel <= 0.01) {
                    testaEscoa = 0;
                    val = 1.;
                }
                while (val < -0.9e10) {
                    chutelim = mchute2;
                    mchute2 = 0.5 * (mchute2 + mchuteAux);
                    val = marchaProdPresPres2(mchute2);
                    if (val < 0 && val > -0.9e10)
                        chuteNeg = mchute2;
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = mchute2;
        } else if (val > 0.) {
            chutePos = mchute;
            while (val > 0) {
                mchuteAux = mchute2;
                mchute2 *= (1. + arq.buscaFC);
                val = marchaProdPresPres2(mchute2);
                if ((*vg1dSP).chaverede != 0 && mchute2 > maxvaz) {
                    testaEscoa = 0;
                    val = -1.;
                }
                if (val > 0 && val < 0.9e10)
                    chutePos = mchute2;
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm  atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val < -0.9e10) {
                    chutelim = mchute2;
                    mchute2 = 0.5 * (mchute2 + mchuteAux);
                    val = marchaProdPresPres2(mchute2);
                    if (val > 0 && val > -0.9e10)
                        chutePos = mchute2;
                    kontaiter++;

                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = mchute2;
        }
        if (testaEscoa == 1)
            return zriddr(chuteNeg, chutePos, 2, 1);
        else {
            if (celula[0].acsr.tipo == 1) {
                celula[0].acsr.injg.QGas = 0.;
            } else if (celula[0].acsr.tipo == 2) {
                celula[0].acsr.injl.QLiq = 0.;
            }
            return 0.;
        }
    }
}

double SProd::marchaProdPresPres3(double mchute) {

    double alfini = 0.;
    double betini = 0.;

    if (celula[0].acsr.tipo == 1) {
        celula[0].temp = celula[0].acsr.injg.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(celula[0].pres, celula[0].temp);
            celula[0].acsr.injg.FluidoPro.atualizaPropComp(celula[0].pres, celula[0].temp, -1, NULL, NULL, celula[0].acsr.injg.seco);
        }
        celula[0].acsr.injg.QGas = mchute;
    } else if (celula[0].acsr.tipo == 2) {
        celula[0].temp = celula[0].acsr.injl.temp;
        if (arq.flashCompleto == 2) {
            if (arq.tabelaDinamica == 0)
                celula[0].flui.atualizaPropComp(celula[0].pres, celula[0].temp);
            celula[0].acsr.injl.FluidoPro.atualizaPropComp(celula[0].pres, celula[0].temp);
        }
        celula[0].acsr.injl.QLiq = mchute;
    }

    if (celula[0].acsr.tipo == 1) {
        if (celula[0].acsr.injg.seco == 1) {
            alfini = 1.;
            betini = 0.;
        } else {
            double masgas = celula[0].acsr.injg.VMas(celula[0].pres, celula[0].temp);
            double tit;
            if (arq.flashCompleto != 2)
                tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
            else
                tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
            double masT = masgas / tit;
            tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(celula[0].pres, celula[0].temp);
            double qgas = masT * tit /
                          celula[0].acsr.injg.FluidoPro.MasEspGas(celula[0].pres, celula[0].temp);
            double qliq = masT * (1. - tit) /
                          celula[0].acsr.injg.FluidoPro.MasEspLiq(celula[0].pres, celula[0].temp);
            double qcomp = celula[0].acsr.injg.razCompGas *
                           celula[0].acsr.injg.QGas * celula[0].acsr.injg.fluidocol.MasEspFlu(1., 20.) /
                           celula[0].acsr.injg.fluidocol.MasEspFlu(celula[0].pres, celula[0].temp);
            qcomp /= 86400.;
            alfini = qgas / (qliq + qcomp + qgas);
            if ((fabs(qcomp) + fabs(qliq)) > 1e-15)
                betini = fabs(qcomp) / (fabs(qcomp) + fabs(qliq));
            else
                betini = 0.;
        }
    } else if (celula[0].acsr.tipo == 2) {
        double qgas = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                      (1. - celula[0].acsr.injl.FluidoPro.BSW) *
                      (celula[0].acsr.injl.FluidoPro.RGO -
                       celula[0].acsr.injl.FluidoPro.rDgD * celula[0].acsr.injl.FluidoPro.RS(celula[0].pres, celula[0].temp) * 6.29 / 35.31467) *
                      celula[0].acsr.injl.FluidoPro.Deng * 1.225 / celula[0].acsr.injl.FluidoPro.MasEspGas(celula[0].pres, celula[0].temp);
        double qliq = celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          (1. - celula[0].acsr.injl.FluidoPro.BSW) * celula[0].acsr.injl.FluidoPro.BOFunc(celula[0].pres, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * (1 - celula[0].acsr.injl.bet) *
                          celula[0].acsr.injl.FluidoPro.BSW * celula[0].acsr.injl.FluidoPro.BAFunc(celula[0].pres, celula[0].temp) +
                      celula[0].acsr.injl.QLiq * celula[0].acsr.injl.bet;
        alfini = qgas / (qliq + qgas);
        betini = celula[0].acsr.injl.bet;
    }

    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].MComp = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;

    int i;

    celula[0].presauxL = celula[0].pres;
    celula[0].presLini = celula[0].pres;
    celula[0].presL = celula[0].pres;
    celula[1].presL = celula[0].pres;
    celula[0].presini = celula[0].pres;
    celula[1].presLini = celula[0].pres;
    celula[0].presaux = celula[0].pres;
    celula[1].presauxL = celula[0].pres;

    celula[0].alf = alfini;
    celula[0].alfini = alfini;
    celula[0].bet = betini;
    celula[0].betini = betini;
    celula[1].alfL = celula[0].alf;
    celula[1].alfLini = celula[0].alf;
    celula[0].alfPigD = celula[0].alf;
    celula[0].alfPigDini = celula[0].alf;
    celula[0].alfPigE = celula[0].alf;
    celula[0].alfPigEini = celula[0].alf;
    celula[1].betL = celula[0].bet;
    celula[1].betLini = celula[0].bet;
    celula[0].betPigD = celula[0].bet;
    celula[0].betPigDini = celula[0].bet;
    celula[0].betPigE = celula[0].bet;
    celula[0].betPigEini = celula[0].bet;
    celula[0].betI = celula[0].bet;
    celula[1].betLI = celula[0].bet;

    iterperm = 0;
    while (iterperm < 3) {
        i = 1;
        while (i <= ncel && celula[i - 1].pres >= 1.) {

            RenovaPresPermMon(i, 0);
            atualizaPeriPmonProd(i);
            if (arq.flashCompleto != 2)
                RenovaMassPerm(i);
            else
                RenovaMassPermComp(i);
            RenovaTempPerm(i, 0);
            if (isnan(celula[i].temp))
                NumError("Temperatrura na linha de producao com valor NaN");
            if (arq.usaTabela == 1 && (celula[i].temp - arq.tabent.tmin) < (*vg1dSP).localtiny)
                celula[i].temp = arq.tabent.tmin;
            atualizaPeriTempProd(i);
            RenovaPresPermJus(i, 0);
            atualizaPeriPjusProd(i);
            if (arq.tipoFluido == 0)
                RenovaTransMassPerm(i - 1);
            else
                RenovaTransMassPermGas(i - 1);
            if (arq.ordperm > 1) {
                double D0presaux = celula[i].presaux - celula[i - 1].pres;
                double D0pres = celula[i].pres - celula[i].presaux;
                double D0temp = celula[i].temp - celula[i - 1].temp;
                RenovaPresPermMon(i, 1);
                atualizaPeriPmonProd(i);
                RenovaMassPerm(i);
                RenovaTempPerm(i, 1);
                RenovaPresPermJus(i, 1);
                celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                atualizaPeriPjusProd(i);
                atualizaPeriPmonProd(i);
                atualizaPeriTempProd(i);
                RenovaMassPerm(i);
                if (arq.tipoFluido == 0)
                    RenovaTransMassPerm(i - 1);
                else
                    RenovaTransMassPermGas(i - 1);
            }

            i++;
            if (celula[i - 1].pres <= pGSup)
                return -1e10;
        }
        iterperm++;
    }

    monitConvPermBase = 1.;
    return 0. - celula[ncel - 1].MR;
}

double SProd::buscaProdPresPresPerm3(double chute, double maxvaz) {

    revPerm = 0;
    monitConvPerm = 1000.;
    double taux;
    double pchute = arq.CCPres.pres[0];
    double vazTot = 0.;
    if (chute < 0.) {
        for (int i = 0; i < ncel; i++) {
            taux = arq.celp[i].textern;
            double rhol = celula[i].flui.MasEspLiq(pchute, taux);
            double rhog = celula[i].flui.MasEspGas(pchute, taux);
            // se o sistema nao for um anel principal de GL
            // para a estimativa de pressao, se utilizara so a hidrostatica de liquido, o que dara um
            // chute inicial de pressao muito alta
            double alfa = 0.1;
            if ((*vg1dSP).chaverede == 1 && chuteHol < 0.5 && chuteHol > -1e-15)
                alfa = 1. - chuteHol * 2.;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i + 1].dx);
            // avanco da pressao por meio da hidrostatica e da perda por friccao estimada
            pchute -= ((rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed) / 98066.5);
            // incremento de pressao devido a algum ganho de pressao constante em alguma celula
            if (celula[i].acsr.tipo == 7)
                pchute -= celula[i].acsr.delp;
            // caso exista alguma IPR no meio do duto, verifica se a pressao estimada esta menor que
            // a pressao estatica, nao se trabalha com vazoes negativas neste solver
            // para evitar isto, se corrige a pressao para um valor proximo da
            // pressao estatica da IPR da celula
            if (celula[i].acsr.tipo == 3 && ((celula[i].acsr.ipr.Pres - pchute) > 0.0) && i == ncel - 1) {

                pchute = -(10 / celula[i - 1].acsr.ipr.ip) + celula[i - 1].acsr.ipr.Pres;
                if (pchute < 0.99 * celula[i - 1].acsr.ipr.Pres)
                    pchute = 0.99 * celula[i - 1].acsr.ipr.Pres;
            }
            double adVaz = 0.;
            if (celula[i].acsr.tipo == 3)
                adVaz = -(celula[i].acsr.ipr.Pres - pchute) * celula[i].acsr.ipr.ip;
            else if (celula[i].acsr.tipo == 1 && arq.tipoFluido == 1)
                adVaz = -celula[i].acsr.injg.QGas;
            else if (celula[i].acsr.tipo == 2 && arq.tipoFluido == 0)
                adVaz = -celula[i].acsr.injl.QLiq;
            else if (celula[i].acsr.tipo == 9 && celula[0].acsr.tipo == 1) {
                adVaz = 10000.;
            } else if (celula[i].acsr.tipo == 9 && celula[0].acsr.tipo == 2) {
                adVaz = 1000.;
            }

            vazTot += adVaz;
            if (vazTot < 0)
                vazTot -= adVaz;
            // tambÃ©m se evita um chute com valores maiores que o limite maximo de uma tabela
            // no caso de se estar usando PVTSim
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
                pchute = 0.9 * arq.tabent.pmax;
        }
        chute = vazTot;
    }
    double mchute = chute;
    double val = marchaProdPresPres2(mchute);
    buscaIni = 1;
    int kontaiter = 0;
    if (val < -0.9e10) {
        while (val < -0.9e10 && kontaiter < 400) {
            mchute *= (1. - arq.buscaFC);
            val = marchaProdPresPres2(mchute);
            if (mchute < 1e-5) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
            val = marchaProdPresPres2(mchute);
            kontaiter++;
        }
    }
    double mchuteAux;
    double mchute2 = mchute;
    double chutelim = 0.;
    kontaiter = 0;

    double chuteNeg = 0.;
    double chutePos = 0.;

    int testaEscoa = 1;
    if (fabs(val) < 1e-3)
        return mchute;
    else {
        if (val < 0.) {
            chuteNeg = mchute;
            while (val < 0) {
                mchuteAux = mchute2;
                mchute2 *= (1. - arq.buscaFC);
                val = marchaProdPresPres2(mchute2);
                if (val < 0 && val > -0.9e10)
                    chuteNeg = mchute2;
                kontaiter++;
                double vel = mchute2;
                double bet = celula[1].bet;
                double alf = celula[1].alf;
                double p1 = celula[0].pres;
                double t1 = celula[0].temp;
                double rC = celula[0].fluicol.MasEspFlu(p1, t1);
                double rP = celula[0].flui.MasEspLiq(p1, t1);
                double rG = celula[0].flui.MasEspGas(p1, t1);
                double rmisL = (1 - bet) * rP + bet * rC;
                double rmis = (1 - alf) * rmisL + alf * rG;
                double rCst = celula[0].fluicol.MasEspFlu(1., 20.);
                double rPst = celula[0].flui.MasEspLiq(1., 20.);
                double rGst = celula[0].flui.MasEspGas(1., 20.);
                double rmisLst = (1 - bet) * rPst + bet * rCst;
                double multiplica;
                if (celula[0].acsr.tipo == 2)
                    multiplica = rmisLst;
                else
                    multiplica = rGst;
                vel = mchute2 * multiplica / (rmis * celula[0].duto.area * 86400);
                if (kontaiter > 200 && vel > 0.01) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (vel <= 0.01) {
                    testaEscoa = 0;
                    val = 1.;
                }
                while (val < -0.9e10) {
                    chutelim = mchute2;
                    mchute2 = 0.5 * (mchute2 + mchuteAux);
                    val = marchaProdPresPres2(mchute2);
                    if (val < 0 && val > -0.9e10)
                        chuteNeg = mchute2;
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = mchute2;
        } else if (val > 0.) {
            chutePos = mchute;
            while (val > 0) {
                mchuteAux = mchute2;
                mchute2 *= (1. + arq.buscaFC);
                val = marchaProdPresPres2(mchute2);
                if ((*vg1dSP).chaverede != 0 && mchute2 > maxvaz && maxvaz > 0) {
                    testaEscoa = 0;
                    val = -1.;
                }
                if (val > 0 && val < 0.9e10)
                    chutePos = mchute2;
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm  atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val > 0.9e10) {
                    chutelim = mchute2;
                    mchute2 = 0.5 * (mchute2 + mchuteAux);
                    val = marchaProdPresPres2(mchute2);
                    if (val > 0 && val < 0.9e10)
                        chutePos = mchute2;
                    kontaiter++;

                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaProdPresPresPerm atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = mchute2;
        }
        if (testaEscoa == 1)
            return zriddr(chuteNeg, chutePos, 2, 1);
        else {
            if (celula[0].acsr.tipo == 1) {
                celula[0].acsr.injg.QGas = 0.;
            } else if (celula[0].acsr.tipo == 2) {
                celula[0].acsr.injl.QLiq = 0.;
            }
            return 0.;
        }
    }
}

double SProd::marchaGasPerm1(double chutemass) {
    int nvalv = arq.nvalvgas;
    double massGas = 0.;
    double erro = 10.;
    double erro1 = 10.;
    if (chutemass < 0) {
        for (int j = 0; j < nvalv; j++)
            massGas += celulaG[posicVGLG[j]].massfonteCH; // caso nÃƒÂ£o se tenha um chute da vazao massica
        // na entrada da linha de injecao, faz-se o somatÃƒÂ³rio dos valores de vazao em cada VGL
    } else
        massGas = chutemass;
    int itera = 0;
    double relaxa = 0.5; // relaxacao para a iteracao da vazao total de injecao estimada a cada iteracao
    double presteste = -10;
    double presteste0 = -10;
    int vazBaix = 0;
    while (((erro > 0.00001 && vazBaix == 0) || erro1 > 0.00001) && itera < 600) { // iteracao de convergencia
        presteste0 = presteste;
        if (itera > 20)
            relaxa = 0.1;
        else if (itera > 50)
            relaxa = 0.05;
        else if (itera > 100)
            relaxa = 0.01;

        celulaG[0].presL = presiniG;
        celulaG[0].pres = presiniG;
        celulaG[0].presini = presiniG;
        celulaG[1].presL = presiniG;
        celulaG[0].tempL = tempiniG;
        celulaG[0].temp = tempiniG;
        celulaG[1].tempL = tempiniG;
        celulaG[0].rg = celulaG[0].flui.MasEspGas(presiniG, tempiniG);
        celulaG[0].u1L = celulaG[0].duto.area * celulaG[0].rg;
        celulaG[0].u1LL = celulaG[0].u1L;
        celulaG[1].u1LL = celulaG[0].u1L;
        celulaG[0].VGasL = 0;
        celulaG[0].massfonteCH = massGas;
        celulaG[0].VGasR = massGas;
        celulaG[1].VGasL = massGas;
        for (int i = 1; i <= ncelGas; i++) { // marcha
            RenovaPresGasPerm(i);            // avanco do valor de pressao de uma celula para outra, no centro da celula
            RenovaTempGasPerm(i);            // avanco do valor de temperatura de uma celula para outra, centro da celula
            if (isnan(celulaG[i].temp))
                NumError("Temperatrura na linha de servico com valor NaN");
            calcVazGasPerm(i); // verifica se no centro desta celula tem uma VGL, calcula a vazao da VGL
            // retira este valor da vazÃƒÂ£o total na linha
            celulaG[i].rg = celulaG[i].flui.MasEspGas(celulaG[i].pres, celulaG[i].temp);
            celulaG[i - 1].rgR = celulaG[i].rg;
            celulaG[i].u1L = celulaG[i].duto.area * celulaG[i].rg;
            celulaG[i - 1].u1R = celulaG[i].u1L;
            if (i < ncelGas)
                celulaG[i + 1].u1LL = celulaG[i].u1L;
        }
        celulaG[ncelGas].rgR = celulaG[ncelGas].rg;
        double massGas2 = massGas; // guarda o valor antigo de injecao de gas
        massGas = 0;
        for (int j = 0; j < nvalv; j++)
            massGas += celulaG[posicVGLG[j]].massfonteCH; // atualiza a vazao de injecao a partir
        // das vazoes calculadas nas VGLs
        itera++;
        massGas = (relaxa * massGas + (1. - relaxa) * massGas2); // relaxacao da estimativa de vazao de injecao
        if (0.05 * celulaG[0].duto.area * celulaG[0].rg > massGas)
            vazBaix = 1;
        else
            vazBaix = 0;
        if (fabs(massGas) > 1e-15)
            erro = fabs(massGas - massGas2) / fabs(massGas); // erro na estimativa de vazao
        // de uma iteracao para a outra
        else if (fabs(massGas2) > 1e-15)
            erro = fabs(massGas - massGas2) / fabs(massGas2); // erro = erro/2.;
        else
            erro = 0.;
        presteste = celulaG[ncelGas].pres;
        erro1 = fabs(presteste - presteste0) / presteste0; // erro no valor da pressao na ultima celula
    }
    if (itera >= 600) {
        if ((*vg1dSP).chaverede == 0 && arq.AP == 0) {
            NumError("Busca de valores iniciais para calculo de zero de funcao em marchaGasPerm1 atingiu maximo de iteracoes");
            return 0.0;
        } else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    } else
        return erro1;
}

double SProd::buscaGasPresPerm2() {
    int nvalv = arq.nvalvgas;
    double pchute;
    int maisprof = 0;
    for (int i = 1; i < nvalv; i++)
        if (posicVGLP[i] < posicVGLP[maisprof])
            maisprof = i;                      // busca a vÃ¡lvula mais profunda
    pchute = celula[posicVGLP[maisprof]].pres; // estima uma pressao para a valvula mais profunda da linha,
    // a pressao na coluna de producao para esta valvula, observar que quando este metodo e chamado,
    // a marcha na linha de producao jÃ¡ foi feita
    double tempchute;
    if (iterperm == 0) {                              // para o caso da primeira iteracao do sistema linha de producao-linha de gas
        tempchute = celula[posicVGLP[maisprof]].temp; // estimativa da temperatura na VGL mais profunda
        for (int k = ncelGas; k > 0; k--) {           // marcha da ultima celula para a primeira
            // para fazer a estimativa da pressao de njecao
            double dx = 0.5 * (celulaG[k].dx0 + celulaG[k].dxL);
            double rhog = celulaG[k].flui.MasEspGas(pchute, tempchute);
            pchute += rhog * 9.81 * sin(celulaG[k].duto.teta) * dx / 98066.5; // avanco apenas pela hidrostatica
        }
    } else {
        for (int k = ncelGas; k > 0; k--)
            pchute += delpGasPerm(k); // avanco usando os resultados da ireacao anterior, usando a hidrostatica
        // e a friccao
    }

    double delmas = 10;
    delmas = marchaGasPerm2(pchute); // marcha feita com a pressao de injecao estimada e a vazao de
    // injecao definida, retorna a diferenca entre a soma das vazÃµes em cada VGL calculadas na marcha
    // e a vazao de injecao definida, se negativa->pchute baixo, se positiva-> pchute alto
    double pchute2 = pchute;
    int kontaiter = 0;
    double chuteNeg = 0.;
    double chutePos = 0.;
    if (fabs(delmas) < 1e-3)
        return pchute;
    else {
        if (delmas < 0) { // pchute baixo, nova estimativa aumentando pchute, para encontrar um outro valor
            // de delmas positivo, para iniciar a falsa corda
            chuteNeg = pchute2;
            while (delmas < 0 && kontaiter < 800) {
                pchute2 *= 1.1;
                delmas = marchaGasPerm2(pchute2);
                if (delmas < 0 && delmas > -0.9e10)
                    chuteNeg = pchute2;
                kontaiter++;
            }
            if (kontaiter >= 800) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError("Busca de valores iniciais para calculo de zero de funcao em buscaGasPresPerm2 atingiu maximo de iteracoes");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
            chutePos = pchute2;
        } else if (delmas > 0) { // pchute alto, nova estimativa diminuindo pchute, para encontrar um outro valor
            // de delmas negativo, para iniciar a falsa corda
            chutePos = pchute2;
            while (delmas > 0 && kontaiter < 800) {
                pchute2 *= 0.9;
                delmas = marchaGasPerm2(pchute2);
                if (delmas > 0 && delmas < 0.9e10)
                    chutePos = pchute2;
                kontaiter++;
            }
            if (kontaiter >= 800) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError("Busca de valores iniciais para calculo de zero de funcao em buscaGasPresPerm2 atingiu maximo de iteracoes");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
            chuteNeg = pchute2;
        }
        return zriddr(chuteNeg, chutePos, 0, 0); // calculo de zero de funcao
    }
}

double SProd::buscaGasPresPerm3() {
    int nvalv = arq.nvalvgas;
    // dois chutes de pressao a montante do choke de injecao, a convergencia para este caso,
    // choke de injecao e mais dificil, portanto, se testa mais possibiliodades de hutes:
    double pchute = chokeInj.presEstag * 0.8; // pressao a montante do choke 20% menor que a pressao a jusante
    double pchute2 = celulaG[0].pres;         // Na primeira iteracao este valor Ã© a prÃ³pria pressao a montante

    // duas marchas para os dois chutes:
    double delmas;
    delmas = marchaGasPerm3(pchute); // o valor retornado Ã© a diferenca entre a soma das vazoes VGL
    // e o valor calculado da vazao no choke de injecao, se der negativo, significa que a pressao a montante
    // se positivo, pressao a montante muito grande
    double delmas2;
    delmas2 = marchaGasPerm3(pchute2);

    int kontaiter = 0;
    double chuteNeg = 0.;
    double chutePos = 0.;
    if (fabs(delmas) < 1e-3)
        return pchute;
    else {
        if (delmas < 0) { // chute de pressao pequeno, deve ser aumentado
            chuteNeg = pchute;
            while (delmas < 0 && kontaiter < 800) {
                pchute *= 1.01;
                if (pchute > chokeInj.presEstag)
                    pchute = chokeInj.presEstag;
                delmas = marchaGasPerm3(pchute);
                if (delmas < 0 && delmas > -0.9e10)
                    chuteNeg = pchute;
                kontaiter++;
            }
            chutePos = pchute;
        } else if (delmas >= 0) { // chute de pressao grande, deve ser diminuido
            kontaiter = 0;
            chutePos = pchute;
            while (delmas > 0 && kontaiter < 800) {
                pchute *= 0.9;
                delmas = marchaGasPerm3(pchute);
                if (delmas > 0 && delmas < 0.9e10)
                    chutePos = pchute;
                kontaiter++;
            }
            chuteNeg = pchute;
        }
        // caso nÃ£o funcione com o primeiro chute
        if (delmas2 >= 0 && kontaiter >= 800) { // chute de pressao pequeno, deve ser aumentado
            kontaiter = 0;
            chutePos = pchute2;
            while (delmas2 > 0 && kontaiter < 800) {
                pchute2 *= 0.9;
                delmas2 = marchaGasPerm3(pchute2);
                if (delmas2 > 0 && delmas2 < 0.9e10)
                    chutePos = pchute2;
                kontaiter++;
            }
            if (kontaiter >= 800) { // falha na busca de estimativa para a falsa corda
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError("Busca de valores iniciais para calculo de zero de funcao em buscaGasPresPerm3 atingiu maximo de iteracoes");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
            chuteNeg = pchute2;
        } else if (delmas2 <= 0 && kontaiter >= 800) { // chute de pressao grande, deve ser diminuido
            kontaiter = 0;
            chuteNeg = pchute2;
            while (delmas2 < 0 && kontaiter < 800) {
                pchute2 *= 1.01;
                if (pchute2 > chokeInj.presEstag)
                    pchute2 = chokeInj.presEstag;
                delmas2 = marchaGasPerm3(pchute2);
                if (delmas2 < 0 && delmas2 > -0.9e10)
                    chuteNeg = pchute2;
                kontaiter++;
            }
            if (kontaiter >= 800) { // falha na busca de estimativa para a falsa corda
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError("Busca de valores iniciais para calculo de zero de funcao em buscaGasPresPerm3 atingiu maximo de iteracoes");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
            chutePos = pchute2;
        }
        return zriddr(chuteNeg, chutePos, 0, 1); // calculo de zero de funcao
    }
}

double SProd::marchaGasPerm2(double pchute, double chutemass) {
    celulaG[0].presL = pchute;
    celulaG[0].pres = pchute;
    celulaG[0].presini = pchute;
    celulaG[1].presL = pchute;
    celulaG[0].tempL = tempiniG;
    celulaG[0].temp = tempiniG;
    celulaG[1].tempL = tempiniG;
    celulaG[0].rg = celulaG[0].flui.MasEspGas(pchute, tempiniG);
    celulaG[0].u1L = celulaG[0].duto.area * celulaG[0].rg;
    celulaG[0].u1LL = celulaG[0].u1L;
    celulaG[1].u1LL = celulaG[0].u1L;
    celulaG[0].VGasL = 0.;
    if (chutemass < 0) // se nenhum valor de chutemass for colocado na lista de parÃ£metro,
                       // usa o valor dado no json para a injecao de gas
        celulaG[0].massfonteCH = arq.gasinj.vazgas[0] * celulaG[0].flui.MasEspGas(1., 15.6) / 86400.;
    else
        celulaG[0].massfonteCH = chutemass * celulaG[0].flui.MasEspGas(1., 15.6) / 86400.;
    celulaG[0].VGasR = celulaG[0].massfonteCH;
    celulaG[1].VGasL = celulaG[0].massfonteCH;

    for (int i = 1; i <= ncelGas; i++) { // marcha na linha de servico
        RenovaPresGasPerm(i);            // avanco do valor de pressao de uma celula para outra, no centro da celula
        RenovaTempGasPerm(i);            // avanco do valor de temperatura de uma celula para outra, centro da celula
        if (isnan(celulaG[i].temp))
            NumError("Temperatrura na linha de servico com valor NaN");
        calcVazGasPerm(i); // verifica se no centro desta celula tem uma VGL, calcula a vazao da VGL
        // retira este valor da vazÃƒÂ£o total na linha
        celulaG[i].rg = celulaG[i].flui.MasEspGas(celulaG[i].pres, celulaG[i].temp);
        celulaG[i - 1].rgR = celulaG[i].rg;
        celulaG[i].u1L = celulaG[i].duto.area * celulaG[i].rg;
        celulaG[i - 1].u1R = celulaG[i].u1L;
        if (i < ncelGas)
            celulaG[i + 1].u1LL = celulaG[i].u1L;
    }
    celulaG[ncelGas].rgR = celulaG[ncelGas].rg;

    int nvalv = arq.nvalvgas;
    double mastot = 0.;
    for (int j = 0; j < nvalv; j++)
        mastot += celulaG[posicVGLG[j]].massfonteCH;
    return mastot - celulaG[0].massfonteCH; // diferenca entre a soma das vazoes nas VGL
    // e a vazao de injecao na linha
}

double SProd::marchaGasPerm3(double pchute) {
    celulaG[0].presL = pchute; // pressao a jusante do choque de injecao
    celulaG[0].pres = pchute;
    celulaG[0].presini = pchute;
    celulaG[1].presL = pchute;
    celulaG[0].tempL = tempiniG;
    celulaG[0].temp = tempiniG;
    celulaG[1].tempL = tempiniG;
    celulaG[0].rg = celulaG[0].flui.MasEspGas(pchute, tempiniG);
    celulaG[0].u1L = celulaG[0].duto.area * celulaG[0].rg;
    celulaG[0].u1LL = celulaG[0].u1L;
    celulaG[1].u1LL = celulaG[0].u1L;
    celulaG[0].VGasL = 0.;
    chokeInj.presGarg = pchute;
    double chutemass = chokeInj.massica(); // vazao de injecao na linha obtido a partir da vazao
    // massica do choke de injecao
    celulaG[0].massfonteCH = chutemass;
    celulaG[0].VGasR = celulaG[0].massfonteCH;
    celulaG[1].VGasL = celulaG[0].massfonteCH;

    for (int i = 1; i <= ncelGas; i++) { // marcha na linha de gas
        RenovaPresGasPerm(i);            // avanco do valor de pressao de uma celula para outra, no centro da celula
        RenovaTempGasPerm(i);            // avanco do valor de temperatura de uma celula para outra, centro da celula
        if (isnan(celulaG[i].temp))
            NumError("Temperatura na linha de servico com valor NaN");
        calcVazGasPerm(i); // verifica se no centro desta celula tem uma VGL, calcula a vazao da VGL
        // retira este valor da vazÃƒÂ£o total na linha
        celulaG[i].rg = celulaG[i].flui.MasEspGas(celulaG[i].pres, celulaG[i].temp);
        celulaG[i - 1].rgR = celulaG[i].rg;
        celulaG[i].u1L = celulaG[i].duto.area * celulaG[i].rg;
        celulaG[i - 1].u1R = celulaG[i].u1L;
        if (i < ncelGas)
            celulaG[i + 1].u1LL = celulaG[i].u1L;
    }
    celulaG[ncelGas].rgR = celulaG[ncelGas].rg;

    int nvalv = arq.nvalvgas;
    double mastot = 0.;
    for (int j = 0; j < nvalv; j++)
        mastot += celulaG[posicVGLG[j]].massfonteCH;
    return mastot - chutemass; // diferenca entre a soma das vazoes nas VGL
    // e a vazao de injecao na linha
}

void SProd::RenovaPresPermMon(int i, int RK) {

    if (arq.tipoModeloDrift == 1) {
        double dx = 0.5 * celula[i].dxL;
        double dia = celula[i].dutoL.a;
        double area = 0.25 * M_PI * dia * dia;
        double si = celula[i].dutoL.peri;
        if (RK == 0) {
            double alfmed = celula[i - 1].alf;
            double betmed = celula[i - 1].bet;
            double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
            double rhol = (1. - betmed) * celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp) + betmed * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
            double ugsmed = (celula[i - 1].MC - celula[i - 1].Mliqini) / (area * rhog);
            double ulsmed = celula[i - 1].Mliqini / (area * rhol);
            double j = ugsmed + ulsmed;
            double sinalJ = 1.;
            if (fabs(j) > 1e-15)
                sinalJ = j / fabs(j);

            double rhomix = alfmed * rhog + (1 - alfmed) * rhol;
            double visl = ((1. - betmed) * celula[i - 1].flui.ViscOleo(celula[i - 1].pres, celula[i - 1].temp) + betmed * celula[i - 1].fluicol.VisFlu(celula[i - 1].pres, celula[i - 1].temp));
            double viscmix = alfmed * celula[i - 1].flui.ViscGas(celula[i - 1].pres, celula[i - 1].temp) + (1 - alfmed) * visl;

            double re1;
            if (celula[i].dutoL.revest == 0)
                re1 = celula[i - 1].Rey(celula[i].dutoL.a, j, rhomix, viscmix);
            else {
                double dhid = 4 * area / si;
                re1 = celula[i - 1].Rey(dhid, j, rhomix, viscmix);
            }
            double f1 = celula[i - 1].fric(re1, celula[i].dutoL.rug / dia);
            if (i > 1 && celula[i - 1].fluicol.tipoF == 2) {
                f1 *= (1 - celula[i - 1].dR);
            }
            double gradfric = celula[i - 1].dPdLFric * (0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area);
            double gradhidro = celula[i - 1].dPdLHidro * (9.82 * sin(celula[i].dutoL.teta) * rhomix * dx);
            celula[i].presaux = celula[i - 1].pres - (gradfric + gradhidro) / 98066.5;

            celula[i].termoHidro = sinalJ * gradhidro / dx;
            celula[i].termoFric = gradfric / dx;
        } else {
            double alfmed = celula[i].alf;
            double betmed = celula[i].bet;
            double razdx = celula[i].dx / (celula[i].dx + celula[i].dxL);
            double tmed = razdx * celula[i].temp + (1. - razdx) * celula[i - 1].temp;
            double pmed = celula[i].presaux;
            double rhog = celula[i - 1].flui.MasEspGas(pmed, tmed);
            double rhol = (1. - betmed) * celula[i - 1].flui.MasEspLiq(pmed, tmed) + betmed * celula[i - 1].fluicol.MasEspFlu(pmed, tmed);
            double ugsmed = (celula[i].QG) / (area);
            double ulsmed = celula[i].QL / (area);
            double j = ugsmed + ulsmed;
            double sinalJ = 1.;
            if (fabs(j) > 1e-15)
                sinalJ = j / fabs(j);

            double rhomix = alfmed * rhog + (1 - alfmed) * rhol;
            double viscmix = alfmed * celula[i - 1].flui.ViscGas(pmed, tmed) + (1 - alfmed) * ((1. - betmed) * celula[i - 1].flui.ViscOleo(pmed, tmed) + betmed * celula[i - 1].fluicol.VisFlu(pmed, tmed));

            double re1;
            if (celula[i].dutoL.revest == 0)
                re1 = celula[i - 1].Rey(celula[i].dutoL.a, j, rhomix, viscmix);
            else {
                double dhid = 4 * area / si;
                re1 = celula[i - 1].Rey(dhid, j, rhomix, viscmix);
            }
            double f1 = celula[i - 1].fric(re1, celula[i].dutoL.rug / dia);
            double gradfric = celula[i - 1].dPdLFric * (0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area);
            double gradhidro = celula[i - 1].dPdLHidro * (9.82 * sin(celula[i].dutoL.teta) * rhomix * dx);
            celula[i].presaux = celula[i - 1].pres - (gradfric + gradhidro) / 98066.5;

            celula[i].termoHidro = sinalJ * gradhidro / dx;
            celula[i].termoFric = gradfric / dx;
        }
    } else {
        double holdup;
        double frictionGrad;
        double gravityGrad;
        double totalGrad;
        double reynolds;
        unsigned char flowType;
        char *errorMsg;
        unsigned char errorFlag;

        double dx = 0.5 * celula[i].dxL;
        double dia = celula[i].dutoL.a;
        double area = 0.25 * M_PI * dia * dia;
        double si = celula[i].dutoL.peri;
        executarCorrelacao(celula, i, 1, arq.AceleraConvergPerm,
                           celula[i - 1].correlacaoMR2,
                           holdup, frictionGrad, gravityGrad, totalGrad,
                           reynolds, flowType);

        double gradfric = celula[i - 1].dPdLFric * frictionGrad * 22620.6 * dx;
        double gradhidro = celula[i - 1].dPdLHidro * gravityGrad * 22620.6 * dx;
        celula[i].presaux = celula[i - 1].pres - (gradfric + gradhidro) / 98066.5;

        celula[i].termoHidro = gradhidro / dx;
        celula[i].termoFric = gradfric / dx;
    }
}

double SProd::RenovaPresPermNcel() {

    double dx = 0.5 * celula[ncel].dx;
    double dia = celula[ncel].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double si = celula[ncel].duto.peri;
    double alfmed = celula[ncel].alf;
    double betmed = celula[ncel].bet;
    double rhog = celula[ncel].flui.MasEspGas(celula[ncel].pres, celula[ncel].temp);
    double rhol = (1. - betmed) * celula[ncel].flui.MasEspLiq(celula[ncel].pres, celula[ncel].temp) + betmed * celula[ncel].fluicol.MasEspFlu(celula[ncel].pres, celula[ncel].temp);
    double ugsmed = (celula[ncel].MC - celula[ncel].Mliqini) / (area * rhog);
    double ulsmed = celula[ncel].Mliqini / (area * rhol);

    double j = ugsmed + ulsmed;
    double sinalJ = 1.;
    if (fabs(j) > 1e-15)
        sinalJ = j / fabs(j);

    double rhomix = alfmed * rhog + (1 - alfmed) * rhol;
    double viscmix = alfmed * celula[ncel].flui.ViscGas(celula[ncel].pres, celula[ncel].temp) + (1 - alfmed) * ((1. - betmed) * celula[ncel].flui.ViscOleo(celula[ncel].pres, celula[ncel].temp) + betmed * celula[ncel].fluicol.VisFlu(celula[ncel].pres, celula[ncel].temp));

    double re1;
    if (celula[ncel].duto.revest == 0)
        re1 = celula[ncel].Rey(celula[ncel].duto.a, j, rhomix, viscmix);
    else {
        double dhid = 4 * area / si;
        re1 = celula[ncel].Rey(dhid, j, rhomix, viscmix);
    }
    double f1 = celula[ncel].fric(re1, celula[ncel].duto.rug / dia);
    double gradfric = celula[ncel].dPdLFric * (0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area);
    double gradhidro = celula[ncel].dPdLHidro * (9.82 * sin(celula[ncel].duto.teta) * rhomix * dx);
    return -(1. * gradfric + gradhidro) / 98066.5;
}

double SProd::calcDpArea(int i, double rhomix, double rey, double jmix) {
    double dpArea = 0.;
    if ((celula[i].duto.area != celula[i].dutoR.area && rey > 2400) && (celula[i].mudaArea == 1 && (fabs(jmix) >= 0.1))) {
        double areaMenor;
        double areaMaior;
        double bernou;
        double perdLoc;
        bernou = 0.5 * (celula[i].MR * celula[i].MR / (celula[i].duto.area * celula[i].duto.area * rhomix)) *
                 (1. - pow(celula[i].duto.area / celula[i].dutoR.area, 2.));
        if (celula[i].duto.area > celula[i].dutoR.area) {
            areaMenor = celula[i].dutoR.area;
            areaMaior = celula[i].duto.area;
            if (celula[i].MR > 0.) {
                perdLoc = 0.42 * 0.5 * (celula[i].MR * celula[i].MR / (celula[i].duto.area * celula[i].duto.area * rhomix)) *
                          (1. - pow(celula[i].duto.area / celula[i].dutoR.area, 2.));
            } else {
                double expanse = (1. - pow(areaMenor / areaMaior, 2.));
                perdLoc = 0.5 * (celula[i].MR * celula[i].MR / (areaMenor * areaMenor * rhomix)) * expanse * expanse;
            }
        } else {
            areaMenor = celula[i].duto.area;
            areaMaior = celula[i].dutoR.area;
            if (celula[i].MR < 0.) {
                perdLoc = 0.42 * 0.5 * (celula[i].MR * celula[i].MR / (celula[i].duto.area * celula[i].duto.area * rhomix)) *
                          (1. - pow(celula[i].duto.area / celula[i].dutoR.area, 2.));
            } else {
                double expanse = (1. - pow(areaMenor / areaMaior, 2.));
                perdLoc = -0.5 * (celula[i].MR * celula[i].MR / (areaMenor * areaMenor * rhomix)) * expanse * expanse;
            }
        }
        dpArea = bernou + perdLoc;
    }
    return dpArea / 98066.5;
}

void SProd::RenovaPresPermJus(int i, int RK) {

    double dx;
    double dia;
    double area;
    double si;
    double alfmed;
    double betmed;
    double rhog;
    double rhol;
    double ugsmed;
    double ulsmed;
    double j;

    double rhomix;
    double viscmix;
    double re1;
    double f1;
    double gradfric;
    double gradhidro;

    double tmed;

    double dpArea = 0.;

    dx = 0.5 * celula[i].dx;
    dia = celula[i].duto.a;
    area = 0.25 * M_PI * dia * dia;
    si = celula[i].duto.peri;

    if (arq.tipoModeloDrift == 1) {
        if (RK == 0) {
            alfmed = celula[i].alf;
            betmed = celula[i].bet;
            double razdx = celula[i].dx / (celula[i].dx + celula[i].dxL);
            if (arq.AceleraConvergPerm == 0)
                tmed = razdx * celula[i].temp + (1. - razdx) * celula[i - 1].temp;
            else
                tmed = celula[i - 1].temp;
            double pmed = celula[i].presaux + celula[i - 1].dpB / 98066.5;
            rhog = celula[i].rgCi; // celula[i].flui.MasEspGas(pmed, tmed);
            rhol = (1. - betmed) * celula[i].rpCi + betmed * celula[i].rcCi;
            ugsmed = (celula[i].QG) / (area);
            ulsmed = celula[i].QL / (area);
            j = ugsmed + ulsmed;
            double sinalJ = 1.;
            if (fabs(j) > 1e-15)
                sinalJ = j / fabs(j);

            rhomix = alfmed * rhog + (1 - alfmed) * rhol;
            double visl = ((1. - betmed) * celula[i].flui.ViscOleo(pmed, tmed) + betmed * celula[i].fluicol.VisFlu(pmed, tmed));
            viscmix = alfmed * celula[i].flui.ViscGas(pmed, tmed) + (1 - alfmed) * visl;

            if (celula[i].duto.revest == 0)
                re1 = celula[i].Rey(celula[i].duto.a, j, rhomix, viscmix);
            else {
                double dhid = 4 * area / si;
                re1 = celula[i].Rey(dhid, j, rhomix, viscmix);
            }
            f1 = celula[i].fric(re1, celula[i].duto.rug / dia);
            if (celula[i].fluicol.tipoF == 2) {
                double ulmed = 0.;
                double reL;
                if (fabs(celula[i].MComp) > 1e-15) {
                    if (alfmed < 1 - 1e-15)
                        ulmed = ulsmed / (1 - alfmed);
                    else
                        ulmed = 0.;
                    if (celula[i].duto.revest == 0)
                        reL = celula[i].Rey(celula[i].duto.a, ulmed, rhol, visl);
                    else {
                        double dhid = 4 * area / si;
                        reL = celula[i].Rey(dhid, ulmed, rhol, visl);
                    }
                    celula[i].dR = celula[i].fluicol.calcDR(reL);
                } else
                    celula[i].dR = 0.;
                f1 *= (1 - celula[i].dR);
            }
            gradfric = celula[i].dPdLFric * (0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area);
            gradhidro = celula[i].dPdLHidro * (9.82 * sin(celula[i].duto.teta) * rhomix * dx);
            if (celula[i].mudaArea == 1)
                dpArea = calcDpArea(i - 1, rhomix, re1, fabs(j));

            celula[i].pres = pmed - (gradfric + gradhidro) / 98066.5 + dpArea;
        } else {
            alfmed = celula[i].alf;
            betmed = celula[i].bet;
            double razdx = celula[i].dx / (celula[i].dx + celula[i].dxL);
            tmed = celula[i].temp;
            double pmed = celula[i].pres;
            rhog = celula[i].flui.MasEspGas(pmed, tmed);
            rhol = (1. - betmed) * celula[i].flui.MasEspLiq(pmed, tmed) + betmed * celula[i].fluicol.MasEspFlu(pmed, tmed);
            ugsmed = (celula[i].MC - celula[i].Mliqini) / (area * rhog);
            ulsmed = celula[i].Mliqini / (area * rhol);
            j = ugsmed + ulsmed;
            double sinalJ = 1.;
            if (fabs(j) > 1e-15)
                sinalJ = j / fabs(j);

            rhomix = alfmed * rhog + (1 - alfmed) * rhol;
            viscmix = alfmed * celula[i].flui.ViscGas(pmed, tmed) + (1 - alfmed) * ((1. - betmed) * celula[i].flui.ViscOleo(pmed, tmed) + betmed * celula[i].fluicol.VisFlu(pmed, tmed));

            if (celula[i].duto.revest == 0)
                re1 = celula[i].Rey(celula[i].duto.a, j, rhomix, viscmix);
            else {
                double dhid = 4 * area / si;
                re1 = celula[i].Rey(dhid, j, rhomix, viscmix);
            }
            f1 = celula[i].fric(re1, celula[i].duto.rug / dia);
            gradfric = celula[i].dPdLFric * (0.5 * f1 * rhomix * (fabs(j) * j) * si * dx / area);
            gradhidro = celula[i].dPdLHidro * (9.82 * sin(celula[i].duto.teta) * rhomix * dx);
            if (celula[i].mudaArea == 1)
                dpArea = calcDpArea(i - 1, rhomix, re1, fabs(j));

            celula[i].pres = celula[i].presaux + celula[i - 1].dpB / 98066.5 - (gradfric + gradhidro) / 98066.5 + dpArea;
        }
    } else {
        double holdup;
        double frictionGrad;
        double gravityGrad;
        double totalGrad;
        double reynolds;
        unsigned char flowType;
        char *errorMsg;
        unsigned char errorFlag;
        executarCorrelacao(celula, i, 2, arq.AceleraConvergPerm,
                           celula[i - 1].correlacaoMR2,
                           holdup, frictionGrad, gravityGrad, totalGrad,
                           reynolds, flowType);

        double gradfric = celula[i - 1].dPdLFric * frictionGrad * 22620.6 * dx;
        double gradhidro = celula[i - 1].dPdLHidro * gravityGrad * 22620.6 * dx;
        celula[i].pres = celula[i].presaux + celula[i - 1].dpB / 98066.5 - (gradfric + gradhidro) / 98066.5;

        celula[i].termoHidro = gradhidro / dx;
        celula[i].termoFric = gradfric / dx;
    }
}

void SProd::corrDeng(int i) {
    if (arq.corrDeng == 0) { // nesta chave, para o caso black oil, se faz a disntincao
        // entre a densidade do gas dissolvido e do gas livre
        // rDgD= razao entre a densidade do gas dissolvido e o gas nas condicoes standard
        // rDgL= razao entre a densidade do gas livre e o gas nas condicoes standard
        // arq.corrDeng==0 implica em nÃƒÂ£o haver distincao entre as densidades
        celula[i].flui.rDgD = 1.;
        celula[i].flui.rDgL = 1.;
        celula[i].flui.PCis = celula[i].flui.PC;
        celula[i].flui.TCis = celula[i].flui.TC;
        if (celula[i].acsr.tipo == 1 && celula[i].acsr.injg.seco == 0) {
            celula[i].acsr.injg.FluidoPro.rDgD = 1.;
            celula[i].acsr.injg.FluidoPro.rDgL = 1.;
        }
        if (celula[i].acsr.tipo == 2) {
            celula[i].acsr.injl.FluidoPro.rDgD = 1.;
            celula[i].acsr.injl.FluidoPro.rDgL = 1.;
        } else if (celula[i].acsr.tipo == 3) {
            celula[i].acsr.ipr.FluidoPro.rDgD = 1.;
            celula[i].acsr.ipr.FluidoPro.rDgL = 1.;
        } else if (celula[i].acsr.tipo == 10) {
            celula[i].acsr.injm.FluidoPro.rDgD = 1.;
            celula[i].acsr.injm.FluidoPro.rDgL = 1.;
        }
        if (celula[i].acsr.tipo == 15) {
            celula[i].acsr.radialPoro.flup.rDgD = 1.;
            celula[i].acsr.radialPoro.flup.rDgL = 1.;
            for (int iRP = 0; iRP < celula[i].acsr.radialPoro.ncel; iRP++) {
                celula[i].acsr.radialPoro.celula[iRP].flup.rDgD = 1.;
                celula[i].acsr.radialPoro.celula[iRP].flup.rDgL = 1.;
            }
        }
        if (celula[i].acsr.tipo == 16) {
            celula[i].acsr.poroso2D.dados.flup.rDgD = 1.;
            celula[i].acsr.poroso2D.dados.flup.rDgL = 1.;
            for (int iRP = 0; iRP < celula[i].acsr.poroso2D.dados.transfer.ncel; iRP++) {
                celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.rDgD = 1.;
                celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.rDgL = 1.;
            }
            for (int iRP = 0; iRP < celula[i].acsr.poroso2D.malha.nele; iRP++) {
                celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.rDgD = 1.;
                celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.rDgL = 1.;
            }
        }
    } else { // caso em que arq.corrDeng==1 que implica em haver distincao entre as densidades
        int k = i - 1;
        if (iterperm != 0)
            k = i;
        celula[i].flui.razDegD(celula[k].pres, celula[k].temp);
        celula[i].flui.rzDegL(celula[k].pres, celula[k].temp);
        celula[i].flui.PcTcIS(); // como a densidade de gas livre mudou, suas pressoes
        // criticas devem ser recalculadas
        // caso tenha alguma fonte, se calcula as densidades in situ das fontes
        if (celula[i].acsr.tipo == 1 && celula[i].acsr.injg.seco == 0) { // fonte de liquido
            celula[i].acsr.injg.FluidoPro.razDegD(celula[k].pres, celula[k].temp);
            celula[i].acsr.injg.FluidoPro.rzDegL(celula[k].pres, celula[k].temp);
        }
        if (celula[i].acsr.tipo == 2) { // fonte de liquido
            celula[i].acsr.injl.FluidoPro.razDegD(celula[k].pres, celula[k].temp);
            celula[i].acsr.injl.FluidoPro.rzDegL(celula[k].pres, celula[k].temp);
        } else if (celula[i].acsr.tipo == 3) { // ipr
            celula[i].acsr.ipr.FluidoPro.razDegD(celula[k].pres, celula[k].temp);
            celula[i].acsr.ipr.FluidoPro.rzDegL(celula[k].pres, celula[k].temp);
            ;
        } else if (celula[i].acsr.tipo == 10) { // fonte de massa generica
            celula[i].acsr.injm.FluidoPro.razDegD(celula[k].pres, celula[k].temp);
            celula[i].acsr.injm.FluidoPro.rzDegL(celula[k].pres, celula[k].temp);
        }
        if (celula[i].acsr.tipo == 15) {
            celula[i].acsr.radialPoro.flup.razDegD(celula[i].pres, celula[i].temp);
            celula[i].acsr.radialPoro.flup.rzDegL(celula[i].pres, celula[i].temp);
            for (int iRP = 0; iRP < celula[i].acsr.radialPoro.ncel; iRP++) {
                double pres = celula[i].acsr.radialPoro.celula[iRP].Pcamada;
                double temp = celula[i].acsr.radialPoro.tRes;
                celula[i].acsr.radialPoro.celula[iRP].flup.razDegD(pres, temp);
                celula[i].acsr.radialPoro.celula[iRP].flup.rzDegL(pres, temp);
            }
        }
        if (celula[i].acsr.tipo == 16) {
            celula[i].acsr.poroso2D.dados.flup.razDegD(celula[i].pres, celula[i].temp);
            celula[i].acsr.poroso2D.dados.flup.rzDegL(celula[i].pres, celula[i].temp);
            for (int iRP = 0; iRP < celula[i].acsr.poroso2D.dados.transfer.ncel; iRP++) {
                double pres = celula[i].acsr.poroso2D.dados.transfer.celula[iRP].Pcamada;
                double temp = celula[i].acsr.poroso2D.dados.transfer.tRes;
                celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.razDegD(pres, temp);
                celula[i].acsr.poroso2D.dados.transfer.celula[iRP].flup.rzDegL(pres, temp);
            }
            for (int iRP = 0; iRP < celula[i].acsr.poroso2D.malha.nele; iRP++) {
                double pres = celula[i].acsr.poroso2D.malha.mlh2d[iRP].cel2D.presC;
                double temp = celula[i].acsr.poroso2D.malha.mlh2d[iRP].tRes;
                celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.razDegD(pres, temp);
                celula[i].acsr.poroso2D.malha.mlh2d[iRP].flup.rzDegL(pres, temp);
            }
        }
    }
}

void SProd::RenovaMassPerm(int i) {
    int mudaRGO = 1;
    if (arq.flashCompleto == 1)
        mudaRGO = 1;

    if (celula[i - 1].acsr.tipo == 1 && celula[i - 1].acsr.injg.QGas < 0) {
        celula[i - 1].acsr.injg.FluidoPro = celula[i - 1].flui;
    } else if (celula[i - 1].acsr.tipo == 2 && celula[i - 1].acsr.injl.QLiq < 0) {
        celula[i - 1].acsr.injl.FluidoPro = celula[i - 1].flui;
    } else if (celula[i - 1].acsr.tipo == 10 && celula[i - 1].acsr.injm.MassC + celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP < 0) {
        celula[i - 1].acsr.injm.FluidoPro = celula[i - 1].flui;
    }

    renovaFonte(i - 1); // metodo que verifica se existe uma fonte na celula e calcula o valor das
    // vazoes massicas de liquido produzido (oleo+agua), gas e liquido complementar
    // relacao entre fonte a esquerda e a direita de uma celula:
    celula[i].fontemassLL = celula[i - 1].fontemassLR;
    celula[i].fontemassCL = celula[i - 1].fontemassCR;
    celula[i].fontemassGL = celula[i - 1].fontemassGR;
    celula[i].MComp = celula[i - 1].MComp + celula[i - 1].fontemassCR;

    double trF = 0.;
    double sinalQ = 1.;
    if (i > 1) {
        if (fabs(celula[i - 1].QL + celula[i - 1].QG) > 1e-15)
            sinalQ = (celula[i - 1].QL + celula[i - 1].QG) / fabs(celula[i - 1].QL + celula[i - 1].QG);
    } else {
        if (fabs(celula[i - 1].fontemassLR + celula[i - 1].fontemassCR + celula[i - 1].fontemassGR) > 1e-15)
            sinalQ = (celula[i - 1].fontemassLR + celula[i - 1].fontemassCR + celula[i - 1].fontemassGR) /
                     fabs(celula[i - 1].fontemassLR + celula[i - 1].fontemassCR + celula[i - 1].fontemassGR);
    }
    double boI;
    double baI;
    double fwI = 0;
    // definicao de temperaturas maximas e minimas para uma eventual reavaliacao do metodo
    // ASTM quando ocorre mistura de fluidos e se deseja atualizar o modelo
    // de viscosidade de oleo morto se este for o ASTM que trabalha com um par de temperatura
    double tL;
    if (arq.flashCompleto == 1 && (arq.tabent.tmin - 0) > (*vg1dSP).localtiny)
        tL = arq.tabent.tmin + 0.1;
    else
        tL = 0.;
    double tH;
    if (arq.flashCompleto == 1 && (70 - arq.tabent.tmax) < (*vg1dSP).localtiny)
        tH = arq.tabent.tmax - 0.1;
    else
        tH = 70.;
    double bo;
    double ba;
    double rs;
    // calcula os valores de RS, Bo e Ba na celula anterior a i-esima celula
    if (celula[i - 1].flui.RGO < 1e7) {
        rs = celula[i - 1].flui.RS(celula[i - 1].pres, celula[i - 1].temp);
        bo = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp, rs);
        ba = celula[i - 1].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        rs = rs * 6.29 / 35.31467;
    } else {
        bo = 1;
        rs = 0;
        ba = 0.;
    }
    // BSW in-situ da celula anterior, na marcha, a i-esima celula
    celula[i - 1].FW = celula[i - 1].flui.BSW * ba / (bo + ba * celula[i - 1].flui.BSW - celula[i - 1].flui.BSW * bo);
    celula[i - 1].FWini = celula[i - 1].FW;
    double tmed;
    // temperatura na fronteira entre a celula i-1 e a celula i, da segunda iteracao em diante, pode-se
    // usar a temperatura da celula i, pois ja a tem calculada, mas isto pode ser um complicador de
    // convergencia, mais seguro manter o criterio em todas as iteracoes, neste caso, a temperatura na
    // fronteira e admitida = a temperatura da celula i-1
    if (iterperm != 0 && arq.AceleraConvergPerm == 0)
        tmed = (celula[i].dx * celula[i].temp + celula[i].dxL * celula[i].tempL) / (celula[i].dx + celula[i].dxL);
    else
        tmed = celula[i - 1].temp;
    // primeiro teste: nÃƒÂ£o ha fontes na celula i-1:
    if (celula[i - 1].acsr.tipo != 1 && celula[i - 1].acsr.tipo != 2 && celula[i - 1].acsr.tipo != 3 && celula[i - 1].acsr.tipo != 10 && (celula[i - 1].acsr.tipo != 9 || (celula[i - 1].acsr.tipo == 9 && celula[i - 1].acsr.fontechk.abertura <= 1e-6)) && celula[i - 1].acsr.tipo != 15 && celula[i - 1].acsr.tipo != 16) {
        // neste caso, variaveis como RGO de separador, BSW, API, densidade de gas e outras nÃƒÂ£o muda, sao iguais
        // aos valores da celula i-1
        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // o valor de volume de leve ÃƒÂ© atualizado neste ponto,
        // seguindo o equacionamento mostrado em relatorio, nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas
        // deve ser calculado, pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;

        if (arq.trackRGO == -1) {
            // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
            // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny))
                celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
            if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1 && celula[i].flui.RGO < 1e7)
                celula[i].flui.RGO = (*vg1dSP).RGOMax;
        } else if (mudaRGO == 1)
            celula[i].flui.RGO = celula[i - 1].flui.RGO;

        celula[i].flui.BSW = celula[i - 1].flui.BSW;

        if (arq.flashCompleto == 0) { // nesta chave se faz o carregamento na celula i
            // de variaveis importantes para o modelo black oil
            celula[i].flui.Deng = celula[i - 1].flui.Deng;
            celula[i].flui.yco2 = celula[i - 1].flui.yco2;
            if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
                celula[i].flui.API = celula[i - 1].flui.API;
                celula[i].flui.Denag = celula[i - 1].flui.Denag;
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }
        if (celula[i - 1].bet > (*vg1dSP).localtiny * 1e-6) { // reavaliacao da fracao volumetrica do liquido complementar
            // mesmo que nÃƒÂ£o tenha fonte, ela pode mudar, devido ao encolhimento do liquido produzido
            if (celula[i].flui.RGO < 1e6)
                boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            else
                boI = 1.;
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qo = celula[i - 1].QL * (1 - celula[i - 1].FW) * (1 - celula[i - 1].bet) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet);
            double qc;
            if (celula[i].flui.RGO < 1e7)
                qc = celula[i - 1].QL * (celula[i - 1].bet) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            else
                qc = 0.;
            ////////////////////////esperar//////////////////////////////////////
            if ((fabs(qo) + fabs(qw) + fabs(qc)) > 0)
                celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
            else
                celula[i].bet = 0.;
        } else
            celula[i].bet = celula[i - 1].bet;
    }
    // caso em que se tem uma fonte de gas na celula i-1, o que mudara a RGO e a densidade de gas em i
    else if (celula[i - 1].acsr.tipo == 1 && celula[i - 1].acsr.injg.seco == 1) {
        double api = celula[i - 1].flui.API;
        double rhololeo = 1000. * 141.5 / (131.5 + api);
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        fwI = 0.;
        // vazao de oleo standard
        double qostd;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) * (1. - celula[i - 1].bet) / (bo * rholis);
        else
            qostd = 0.;
        // calculo da nova vazao de gas standard com a soma da fonte de gas:
        double qgstd = qostd * celula[i - 1].flui.RGO + celula[i - 1].acsr.injg.QGas / 86400;
        double deng;
        double yco2;
        // balanco que define a densidade de gas e a fracao de CO2 devido  aa fonte de gas
        if (fabs(qgstd) > (*vg1dSP).localtiny && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15) {
            deng = (qostd * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + celula[i - 1].acsr.injg.QGas * celula[i - 1].acsr.injg.FluidoPro.Deng / 86400) / qgstd;
            yco2 = (qostd * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + celula[i - 1].acsr.injg.QGas * celula[i - 1].acsr.injg.FluidoPro.yco2 / 86400) / qgstd;
        } else {
            deng = celula[i - 1].flui.Deng;
            yco2 = celula[i - 1].flui.yco2;
        }

        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas deve ser calculado,
        // pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;
        if (arq.trackRGO == -1) {
            // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
            // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny) && mudaRGO == 1)
                celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
            if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1 && celula[i].flui.RGO < 1e7)
                celula[i].flui.RGO = (*vg1dSP).RGOMax;
        }
        double rgo;
        // calculo de novo RGO de separador - sem escorregamento - a partir das vazÃƒÂµes satndard de gas e oleo
        if (qostd > (*vg1dSP).localtiny && mudaRGO == 1)
            rgo = qgstd / qostd;
        else
            rgo = celula[i - 1].flui.RGO;
        celula[i].flui.RGO = rgo;

        celula[i].flui.BSW = celula[i - 1].flui.BSW; // bsw nÃƒÂ£o muda devido a uma fonte de gas

        if (arq.flashCompleto == 0) { // nesta chave se faz o carregamento na celula i
            // de variaveis importantes para o modelo black oil
            celula[i].flui.Deng = deng;
            celula[i].flui.yco2 = yco2;
            if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
                celula[i].flui.API = celula[i - 1].flui.API;
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }

        if (celula[i - 1].bet > (*vg1dSP).localtiny * 1e-6 && celula[i - 1].alf < 1. - (*vg1dSP).localtiny * 1e-6 && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15) {
            // reavaliacao da fracao volumetrica do liquido complementar
            // mesmo que nÃƒÂ£o tenha fonte de liquido, ela pode mudar,
            // devido ao encolhimento do liquido produzido
            boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qo = celula[i - 1].QL * (1 - celula[i - 1].FW) * (1 - celula[i - 1].bet) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet);
            double qc = celula[i - 1].QL * (celula[i - 1].bet) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
        } else
            celula[i].bet = celula[i - 1].bet;
    } else if (celula[i - 1].acsr.tipo == 1 && celula[i - 1].acsr.injg.seco == 0) {
        double api = celula[i - 1].flui.API;
        double rhololeo = 1000. * 141.5 / (131.5 + api);
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double rholisF;
        double boinjl;
        double bainjl;
        double fwinjl;
        trF = celula[i - 1].acsr.injg.fluidocol.TR;
        if (fabs(celula[i - 1].acsr.injg.QGas) > 1e-15) {
            rholisF = celula[i - 1].acsr.injg.FluidoPro.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            boinjl = celula[i - 1].acsr.injg.FluidoPro.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            bainjl = celula[i - 1].acsr.injg.FluidoPro.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            fwinjl = celula[i - 1].acsr.injg.FluidoPro.BSW * bainjl / (boinjl + bainjl * celula[i - 1].acsr.injg.FluidoPro.BSW - celula[i - 1].acsr.injg.FluidoPro.BSW * boinjl);
        } else {
            rholisF = rholis;
            boinjl = bo;
            bainjl = ba;
            fwinjl = celula[i - 1].FW;
        }
        // vazao de oleo standard
        double qostd = 0.;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) *
                    (1. - celula[i - 1].bet) / (bo * rholis);
        // vazao de oleo standard da fonte:
        double qostd2 = 0.;
        if (celula[i - 1].acsr.injg.FluidoPro.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd2 = celula[i - 1].fontemassLR * (1. - fwinjl) / (boinjl * rholisF);
        // calculo da nova vazao de gas standard com a soma da fonte de gas:
        double qgstd = qostd * celula[i - 1].flui.RGO + celula[i - 1].acsr.injg.QGas / 86400;
        double deng;
        double yco2;
        // balanco que define a densidade de gas e a fracao de CO2 devido  aa fonte de gas
        if (fabs(qgstd) > (*vg1dSP).localtiny * 1e-10 && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15) {
            deng = (qostd * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + celula[i - 1].acsr.injg.QGas * celula[i - 1].acsr.injg.FluidoPro.Deng / 86400) / qgstd;
            yco2 = (qostd * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + celula[i - 1].acsr.injg.QGas * celula[i - 1].acsr.injg.FluidoPro.yco2 / 86400) / qgstd;
        } else {
            deng = celula[i - 1].flui.Deng;
            yco2 = celula[i - 1].flui.yco2;
        }

        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas deve ser calculado,
        // pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;
        if (arq.trackRGO == -1) {
            // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
            // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny) && mudaRGO == 1)
                celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
            if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1 && celula[i].flui.RGO < 1e7)
                celula[i].flui.RGO = (*vg1dSP).RGOMax;
        }

        // calculo do novo RGO do separador modeificado pela fonte de liquido
        if (fabs(qostd + qostd2) > (*vg1dSP).localtiny && mudaRGO == 1 && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15)
            celula[i].flui.RGO = qgstd / (qostd + qostd2);
        else if (mudaRGO == 1 && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15)
            celula[i].flui.RGO = (*vg1dSP).RGOMax;
        else
            celula[i].flui.RGO = celula[i - 1].flui.RGO;

        if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
            // o modelo seja ASTM. Observar que isto sÃƒÂ³ faz sentido se se tiver mais de um fluido
            // de producao cadastrado no JSON, ou a simulacao se insere em um sistema de redes
            // com varios tramos alimentando outros tramos com fluidos com propriedades diferentes
            // entre cada tramo
            if (fabs(qostd + qostd2) > 1e-15 && arq.flashCompleto == 0 && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15) {
                double denmixSTD = (141.5 / (131.5 + celula[i - 1].flui.API)) * qostd + (141.5 / (131.5 + celula[i].flui.API)) * qostd2;
                denmixSTD /= (qostd + qostd2);
                celula[i].flui.API = 141.5 / denmixSTD - 131.5;
            } else if (arq.flashCompleto == 0)
                celula[i].flui.API = celula[i - 1].flui.API;
            double qw1;
            if ((1. - celula[i - 1].flui.BSW) > 0)
                qw1 = qostd * celula[i - 1].flui.BSW / (1. - celula[i - 1].flui.BSW);
            else
                qw1 = celula[i - 1].Mliqini * celula[i - 1].FW * (1. - celula[i - 1].bet) / (1000. * celula[i - 1].flui.Denag);
            double qw2;
            if ((1. - celula[i - 1].acsr.injg.FluidoPro.BSW) > 0)
                qw2 = qostd2 * celula[i - 1].acsr.injg.FluidoPro.BSW / (1. - celula[i - 1].acsr.injg.FluidoPro.BSW);
            else
                qw2 = celula[i - 1].fontemassLR * fwinjl / (1000. * celula[i - 1].acsr.injg.FluidoPro.Denag);

            if (fabs(qw1 + qw2 + qostd + qostd2) > 1e-15 && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15)
                celula[i].flui.BSW = (qw1 + qw2) / (qw1 + qw2 + qostd + qostd2);
            else
                celula[i].flui.BSW = celula[i - 1].flui.BSW;
            if (fabs(qw1 + qw2) > 1e-15)
                celula[i].flui.Denag = (qw1 * celula[i - 1].flui.Denag +
                                        qw2 * celula[i - 1].acsr.injg.FluidoPro.Denag) /
                                       (qw1 + qw2);
            else
                celula[i].flui.Denag = celula[i - 1].flui.Denag;
            // reavaliacao dos pares necessarios para o modelo de viscosidade de oleo morto
            // ASTM. Isto ÃƒÂ© feito tendo sempre os mesmos valores de temperatura maxima e
            // e minima para a construcao dos pares
            if (fabs(qostd + qostd2) > 1e-15 && arq.flashCompleto == 0 && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15) { // vazao de liquido >0
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
                celula[i].flui.LVisL = (qostd * celula[i - 1].flui.VisOM(tL) + qostd2 * celula[i - 1].acsr.injg.FluidoPro.VisOM(tL)) / (qostd + qostd2);
                celula[i].flui.LVisH = (qostd * celula[i - 1].flui.VisOM(tH) + qostd2 * celula[i - 1].acsr.injg.FluidoPro.VisOM(tH)) / (qostd + qostd2);
            } else if (arq.flashCompleto == 0 || celula[i - 1].acsr.injg.QGas <= 0.) { // se,m vazao de liquido
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
        }

        if (arq.flashCompleto == 0) { // nesta chave se faz o carregamento na celula i
            // de variaveis importantes para o modelo black oil
            celula[i].flui.Deng = deng;
            celula[i].flui.yco2 = yco2;
            if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
                celula[i].flui.API = celula[i - 1].flui.API;
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }

        if (celula[i - 1].bet > (*vg1dSP).localtiny && celula[i - 1].alf < 1. - (*vg1dSP).localtiny * 1e-10 && fabs(celula[i - 1].acsr.injg.QGas) > 1e-15) {
            // reavaliacao da fracao volumetrica do liquido complementar
            double boN = celula[i].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baN = celula[i].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwN = celula[i].flui.BSW * baN / (boN + baN * celula[i].flui.BSW - celula[i].flui.BSW * boN);
            boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qlpF = celula[i - 1].fontemassLR / celula[i - 1].acsr.injg.FluidoPro.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            double qlcF = celula[i - 1].fontemassCR / celula[i - 1].acsr.injg.fluidocol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);

            double betN = (celula[i - 1].QL * celula[i - 1].bet + qlcF) / (celula[i - 1].QL + qlpF + qlcF);
            double qo = (celula[i - 1].QL + qlpF + qlcF) * (1 - fwN) * (1 - betN) * boI / bo;

            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet) + qlpF;
            //* celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp)

            double qc = (celula[i - 1].QL * (celula[i - 1].bet) + qlcF) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);

            celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
        } else
            celula[i].bet = celula[i - 1].bet;
    }
    // caso de fonte de liquido na celula i-1:
    else if (celula[i - 1].acsr.tipo == 2) {
        double api = celula[i - 1].flui.API;
        double rhololeo = 1000. * 141.5 / (131.5 + api);
        double rhololeoF = 1000. * 141.5 / (131.5 + celula[i - 1].acsr.injl.FluidoPro.API);
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double rholisF;
        double boinjl;
        double bainjl;
        double fwinjl;
        trF = celula[i - 1].acsr.injl.fluidocol.TR;
        if (fabs(celula[i - 1].acsr.injl.QLiq) > 1e-15) {
            rholisF = celula[i - 1].acsr.injl.FluidoPro.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            boinjl = celula[i - 1].acsr.injl.FluidoPro.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            bainjl = celula[i - 1].acsr.injl.FluidoPro.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            fwinjl = celula[i - 1].acsr.injl.FluidoPro.BSW * bainjl / (boinjl + bainjl * celula[i - 1].acsr.injl.FluidoPro.BSW - celula[i - 1].acsr.injl.FluidoPro.BSW * boinjl);
        } else {
            rholisF = rholis;
            boinjl = bo;
            bainjl = ba;
            fwinjl = celula[i - 1].FW;
        }
        // vazao de oleo sytandard antes da fonte:
        double qostd1 = 0.;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd1 = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) * (1. - celula[i - 1].bet) / (bo * rholis);
        // vazao de oleo standard da fonte:
        double qostd2 = 0.;
        if (celula[i - 1].acsr.injl.FluidoPro.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd2 = celula[i - 1].fontemassLR * (1. - fwinjl) / (boinjl * rholisF);
        // vazao total de gas nas condicoes standard fruto da soma do gas transportado
        // da fronteira a esquerda
        // da celula e do gas associado da fonte de liquido
        double qgstd = qostd1 * celula[i - 1].flui.RGO + qostd2 * celula[i - 1].acsr.injl.FluidoPro.RGO;
        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas deve ser calculado,
        // pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;

        if (fabs(celula[i - 1].acsr.injl.QLiq) > 1e-15) {
            if (arq.trackRGO == -1) {
                // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
                // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
                if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny) && mudaRGO == 1)
                    celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
                if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else {
                // calculo do novo RGO do separador modeificado pela fonte de liquido
                if (fabs(qostd1 + qostd2) > (*vg1dSP).localtiny && mudaRGO == 1)
                    celula[i].flui.RGO = qgstd / (qostd1 + qostd2);
                else if (mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            }
        } else
            celula[i].flui.RGO = celula[i - 1].flui.RGO;
        // calculo da nova densidade de gas nas condicoes standard modificada pela fonte de liquido
        if (fabs(qgstd) > (*vg1dSP).localtiny && arq.flashCompleto == 0 && fabs(celula[i - 1].acsr.injl.QLiq) > 1e-15 &&
            fabs(qgstd) > 1e-15)
            celula[i].flui.Deng = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + qostd2 * celula[i - 1].acsr.injl.FluidoPro.RGO * celula[i - 1].acsr.injl.FluidoPro.Deng) / qgstd;
        else if (fabs(celula[i - 1].acsr.injl.QLiq) <= 1e-15)
            celula[i].flui.Deng = celula[i - 1].flui.Deng;
        // calculo da nova fracao de co2 modificad pela fonte de liquido
        if (fabs(qgstd) > (*vg1dSP).localtiny && arq.flashCompleto == 0 && fabs(celula[i - 1].acsr.injl.QLiq) > 1e-15 &&
            fabs(qgstd) > 1e-15)
            celula[i].flui.yco2 = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + qostd2 * celula[i - 1].acsr.injl.FluidoPro.RGO * celula[i - 1].acsr.injl.FluidoPro.yco2) / qgstd;
        else if (fabs(celula[i - 1].acsr.injl.QLiq) <= 1e-15)
            celula[i].flui.yco2 = celula[i - 1].flui.yco2;
        if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
            // o modelo seja ASTM. Observar que isto sÃƒÂ³ faz sentido se se tiver mais de um fluido
            // de producao cadastrado no JSON, ou a simulacao se insere em um sistema de redes
            // com varios tramos alimentando outros tramos com fluidos com propriedades diferentes
            // entre cada tramo
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 &&
                fabs(celula[i - 1].acsr.injl.QLiq) > 1e-15) {
                double denmixSTD = (141.5 / (131.5 + celula[i - 1].flui.API)) * qostd1 +
                                   (141.5 / (131.5 + celula[i - 1].acsr.injl.FluidoPro.API)) * qostd2;
                denmixSTD /= (qostd1 + qostd2);
                celula[i].flui.API = 141.5 / denmixSTD - 131.5;
            } else if (arq.flashCompleto == 0 ||
                       fabs(celula[i - 1].acsr.injl.QLiq) <= 1e-15)
                celula[i].flui.API = celula[i - 1].flui.API;
            double qw1;
            if ((1. - celula[i - 1].flui.BSW) > 0)
                qw1 = qostd1 * celula[i - 1].flui.BSW / (1. - celula[i - 1].flui.BSW);
            else
                qw1 = celula[i - 1].Mliqini * celula[i - 1].FW * (1. - celula[i - 1].bet) / (1000. * celula[i - 1].flui.Denag);
            double qw2;
            if ((1. - celula[i - 1].acsr.injl.FluidoPro.BSW) > 0)
                qw2 = qostd2 * celula[i - 1].acsr.injl.FluidoPro.BSW / (1. - celula[i - 1].acsr.injl.FluidoPro.BSW);
            else
                qw2 = celula[i - 1].fontemassLR * fwinjl / (1000. * celula[i - 1].acsr.injl.FluidoPro.Denag);

            if (fabs(qw1 + qw2 + qostd1 + qostd2) > 1e-15 &&
                fabs(celula[i - 1].acsr.injl.QLiq) > 1e-15)
                celula[i].flui.BSW = (qw1 + qw2) / (qw1 + qw2 + qostd1 + qostd2);
            else
                celula[i].flui.BSW = celula[i - 1].flui.BSW;
            if (fabs(qw1 + qw2) > 1e-15)
                celula[i].flui.Denag = (qw1 * celula[i - 1].flui.Denag +
                                        qw2 * celula[i - 1].acsr.injl.FluidoPro.Denag) /
                                       (qw1 + qw2);
            else
                celula[i].flui.Denag = celula[i - 1].flui.Denag;
            // reavaliacao dos pares necessarios para o modelo de viscosidade de oleo morto
            // ASTM. Isto ÃƒÂ© feito tendo sempre os mesmos valores de temperatura maxima e
            // e minima para a construcao dos pares
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 &&
                fabs(celula[i - 1].acsr.injl.QLiq) > 1e-15) { // vazao de liquido >0
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
                celula[i].flui.LVisL = (qostd1 * celula[i - 1].flui.VisOM(tL) + qostd2 * celula[i - 1].acsr.injl.FluidoPro.VisOM(tL)) / (qostd1 + qostd2);
                celula[i].flui.LVisH = (qostd1 * celula[i - 1].flui.VisOM(tH) + qostd2 * celula[i - 1].acsr.injl.FluidoPro.VisOM(tH)) / (qostd1 + qostd2);
            } else if (arq.flashCompleto == 0) { // se,m vazao de liquido
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
        }
        if (arq.flashCompleto == 0) {
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }
        if ((celula[i - 1].bet > (*vg1dSP).localtiny * 1e-6 || fabs(celula[i - 1].fontemassCR) > (*vg1dSP).localtiny * 1e-6) &&
            fabs(celula[i - 1].acsr.injl.QLiq) > 1e-15) {
            // reavaliacao da fracao volumetrica do liquido complementar
            // observar que a fonte de liquido pode ter uma fracao de liquido complementar
            // distinta da onservada a esquerda da celula i-1
            double boN = celula[i].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baN = celula[i].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwN = celula[i].flui.BSW * baN / (boN + baN * celula[i].flui.BSW - celula[i].flui.BSW * boN);
            boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qlpF = celula[i - 1].fontemassLR / celula[i - 1].acsr.injl.FluidoPro.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            double qlcF = celula[i - 1].fontemassCR / celula[i - 1].acsr.injl.fluidocol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
            double betN = (celula[i - 1].QL * celula[i - 1].bet + qlcF) / (celula[i - 1].QL + qlpF + qlcF);
            double qo = (celula[i - 1].QL + qlpF + qlcF) * (1 - fwN) * (1 - betN) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet) + qlpF;
            double qc = (celula[i - 1].QL * (celula[i - 1].bet) + qlcF) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
        } else
            celula[i].bet = celula[i - 1].bet;
    }
    // caso de IPR na celula i-1:
    else if (celula[i - 1].acsr.tipo == 3) {
        double api = celula[i - 1].flui.API;
        double rhololeo = 1000. * 141.5 / (131.5 + api);
        double rhololeoF = 1000. * 141.5 / (131.5 + celula[i - 1].acsr.ipr.FluidoPro.API);
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double rholisF;
        double boipr;
        double baipr;
        double fwipr;
        trF = 0.;
        if (fabs(celula[i - 1].fontemassLR) > 1e-15) {
            rholisF = celula[i - 1].acsr.ipr.FluidoPro.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            boipr = celula[i - 1].acsr.ipr.FluidoPro.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            baipr = celula[i - 1].acsr.ipr.FluidoPro.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            fwipr = celula[i - 1].acsr.ipr.FluidoPro.BSW * baipr / (boipr + baipr * celula[i - 1].acsr.ipr.FluidoPro.BSW - celula[i - 1].acsr.ipr.FluidoPro.BSW * boipr);
        } else {
            rholisF = rholis;
            boipr = bo;
            baipr = ba;
            fwipr = celula[i - 1].FW;
        }
        // vazao de oleo standard antes da fonte:
        double qostd1 = 0.;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd1 = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) * (1. - celula[i - 1].bet) / (bo * rholis);
        // vazao de oleo standard da fonte:
        double qostd2 = 0.;
        if (celula[i - 1].acsr.ipr.FluidoPro.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd2 = celula[i - 1].fontemassLR * (1. - fwipr) / (boipr * rholisF);
        // vazao total de gas nas condicoes standard fruto da soma do gas transportado
        // da fronteira a esquerda
        // da celula e do gas associado da IPR
        double qgstd = qostd1 * celula[i - 1].flui.RGO + qostd2 * celula[i - 1].acsr.ipr.FluidoPro.RGO;
        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas deve ser calculado,
        // pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;
        if (arq.trackRGO == -1) {
            // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
            // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if ((celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15) {
                if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny) &&
                    mudaRGO == 1 && fabs(celula[i - 1].fontemassLR) > 1e-15)
                    celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
                if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else
                celula[i].flui.RGO = celula[i - 1].flui.RGO;

        } else { // calculo do novo RGO do separador modificado pela IPR
            if ((celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15) {
                if (fabs(qostd1 + qostd2) > (*vg1dSP).localtiny * 1e-10 && mudaRGO == 1)
                    celula[i].flui.RGO = qgstd / (qostd1 + qostd2);
                else if (mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else
                celula[i].flui.RGO = celula[i - 1].flui.RGO;
        }
        // calculo da nova densidade de gas nas condicoes standard modificada pela IPR
        if (arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15 &&
            fabs(qgstd) > 1e-15)
            celula[i].flui.Deng = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + qostd2 * celula[i - 1].acsr.ipr.FluidoPro.RGO * celula[i - 1].acsr.ipr.FluidoPro.Deng) / qgstd;
        else
            celula[i].flui.Deng = celula[i - 1].flui.Deng;
        // calculo da nova fracao de co2 modificada pela fonte de liquido
        if (arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15 && fabs(qgstd) > 1e-15)
            celula[i].flui.yco2 = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + qostd2 * celula[i - 1].acsr.ipr.FluidoPro.RGO * celula[i - 1].acsr.ipr.FluidoPro.yco2) / qgstd;
        else
            celula[i].flui.yco2 = celula[i - 1].flui.yco2;
        if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
            // o modelo seja ASTM. Observar que isto sÃƒÂ³ faz sentido se se tiver mais de um fluido
            // de producao cadastrado no JSON, ou a simulacao se insere em um sistema de redes
            // com varios tramos alimentando outros tramos com fluidos com propriedades diferentes
            // entre cada tramo
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 &&
                (celula[i - 1].fontemassLR) > 1e-15) {
                double denmixSTD = (141.5 / (131.5 + celula[i - 1].flui.API)) * qostd1 +
                                   (141.5 / (131.5 + celula[i - 1].acsr.ipr.FluidoPro.API)) * qostd2;
                denmixSTD /= (qostd1 + qostd2);
                celula[i].flui.API = 141.5 / denmixSTD - 131.5;
            } else if (arq.flashCompleto == 0 || (celula[i - 1].fontemassLR) <= 1e-15)
                celula[i].flui.API = celula[i - 1].flui.API;
            double qw1;
            if ((1. - celula[i - 1].flui.BSW) > 0)
                qw1 = qostd1 * celula[i - 1].flui.BSW / (1. - celula[i - 1].flui.BSW);
            else
                qw1 = celula[i - 1].Mliqini * celula[i - 1].FW * (1. - celula[i - 1].bet) / (1000. * celula[i - 1].flui.Denag);
            double qw2;
            if ((1. - celula[i - 1].acsr.ipr.FluidoPro.BSW) > 0)
                qw2 = qostd2 * celula[i - 1].acsr.ipr.FluidoPro.BSW / (1. - celula[i - 1].acsr.ipr.FluidoPro.BSW);
            else
                qw2 = celula[i - 1].fontemassLR * fwipr / (1000. * celula[i - 1].acsr.ipr.FluidoPro.Denag);

            if (fabs(qw1 + qw2 + qostd1 + qostd2) > 1e-15 &&
                (celula[i - 1].fontemassLR) > 1e-15)
                celula[i].flui.BSW = ((qw1 + qostd1) * celula[i - 1].flui.BSW + (qw2 + qostd2) * celula[i - 1].acsr.ipr.FluidoPro.BSW) / (qw1 + qw2 + qostd1 + qostd2);
            else
                celula[i].flui.BSW = celula[i - 1].flui.BSW;
            if ((qw1 + qw2) > 1e-15)
                celula[i].flui.Denag = (qw1 * celula[i - 1].flui.Denag +
                                        qw2 * celula[i - 1].acsr.ipr.FluidoPro.Denag) /
                                       (qw1 + qw2);
            else
                celula[i].flui.Denag = celula[i - 1].flui.Denag;
            if ((qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15) {
                // reavaliacao dos pares necessarios para o modelo de viscosidade de oleo morto
                // ASTM. Isto ÃƒÂ© feito tendo sempre os mesmos valores de temperatura maxima e
                // e minima para a construcao dos pares
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
                celula[i].flui.LVisL = (qostd1 * celula[i - 1].flui.VisOM(tL) + qostd2 * celula[i - 1].acsr.ipr.FluidoPro.VisOM(tL)) / (qostd1 + qostd2);
                celula[i].flui.LVisH = (qostd1 * celula[i - 1].flui.VisOM(tH) + qostd2 * celula[i - 1].acsr.ipr.FluidoPro.VisOM(tH)) / (qostd1 + qostd2);
            } else if (arq.flashCompleto == 0 || fabs(celula[i - 1].fontemassLR) <= 1e-15) { // sem vazao de liquido
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
        }
        if (arq.flashCompleto == 0) {
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }
        if (celula[i - 1].bet > (*vg1dSP).localtiny * 1e-6 && fabs(celula[i - 1].fontemassLR) > 1e-15) {
            // reavaliacao da fracao volumetrica do liquido complementar
            // observar que a IPR pode ter uma fracao de liquido complementar
            // distinta da onservada a esquerda da celula i-1
            double boN = celula[i].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baN = celula[i].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwN = celula[i].flui.BSW * baN / (boN + baN * celula[i].flui.BSW - celula[i].flui.BSW * boN);
            boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qlpF = celula[i - 1].fontemassLR / celula[i - 1].acsr.ipr.FluidoPro.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            double qlcF = 0.;
            double betN = (celula[i - 1].QL * celula[i - 1].bet + qlcF) / (celula[i - 1].QL + qlpF + qlcF);
            double qo = (celula[i - 1].QL + qlpF + qlcF) * (1 - fwN) * (1 - betN) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet) + qlpF;
            double qc = (celula[i - 1].QL * (celula[i - 1].bet) + qlcF) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
        } else
            celula[i].bet = celula[i - 1].bet;
    }
    // caso de fonte de massa na celula i-1:
    else if (celula[i - 1].acsr.tipo == 10) {
        double api = celula[i - 1].flui.API;
        double rhololeo = 1000. * 141.5 / (131.5 + api);
        double rhololeoF = 1000. * 141.5 / (131.5 + celula[i - 1].acsr.injm.FluidoPro.API);
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double rholisF;
        double boinjl;
        double bainjl;
        double fwinjl;
        trF = celula[i - 1].acsr.injm.fluidocol.TR;
        if (fabs(celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15) {
            rholisF = celula[i - 1].acsr.injm.FluidoPro.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            boinjl = celula[i - 1].acsr.injm.FluidoPro.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            bainjl = celula[i - 1].acsr.injm.FluidoPro.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            fwinjl = celula[i - 1].acsr.injm.FluidoPro.BSW * bainjl / (boinjl + bainjl * celula[i - 1].acsr.injm.FluidoPro.BSW - celula[i - 1].acsr.injm.FluidoPro.BSW * boinjl);
        } else {
            rholisF = rholis;
            boinjl = bo;
            bainjl = ba;
            fwinjl = celula[i - 1].FW;
        }
        // vazao de oleo sytandard antes da fonte:
        double qostd1 = 0.;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd1 = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) * (1. - celula[i - 1].bet) / (bo * rholis);
        // vazao de oleo standard da fonte:
        double qostd2 = 0.;
        if (celula[i - 1].acsr.injm.FluidoPro.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd2 = celula[i - 1].fontemassLR * (1. - fwinjl) / (boinjl * rholisF);
        // vazao total de gas nas condicoes standard fruto da soma do gas transportado
        // da fronteira a esquerda
        // da celula e do gas associado da fonte de liquido
        double qgstd = qostd1 * celula[i - 1].flui.RGO + qostd2 * celula[i - 1].acsr.injm.FluidoPro.RGO;
        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas deve ser calculado,
        // pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;

        if (arq.trackRGO == -1) {
            // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
            // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if ((celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15) {
                if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny) && mudaRGO == 1)
                    celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
                if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else
                celula[i].flui.RGO = celula[i - 1].flui.RGO;
        } else {
            // calculo do novo RGO do separador modeificado pela fonte de liquido
            if ((celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15) {
                if (fabs(qostd1 + qostd2) > (*vg1dSP).localtiny && mudaRGO == 1)
                    celula[i].flui.RGO = qgstd / (qostd1 + qostd2);
                else if (mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else
                celula[i].flui.RGO = celula[i - 1].flui.RGO;
        }
        // calculo da nova densidade de gas nas condicoes standard modificada pela fonte de liquido
        if (fabs(qgstd) > (*vg1dSP).localtiny * 1e-10 && arq.flashCompleto == 0 &&
            (celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15)
            celula[i].flui.Deng = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + qostd2 * celula[i - 1].acsr.injm.FluidoPro.RGO * celula[i - 1].acsr.injm.FluidoPro.Deng) / qgstd;
        else
            celula[i].flui.RGO = celula[i - 1].flui.RGO;
        // calculo da nova fracao de co2 modificad pela fonte de liquido
        if (fabs(qgstd) > (*vg1dSP).localtiny * 1e-10 && arq.flashCompleto == 0 &&
            (celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15)
            celula[i].flui.yco2 = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + qostd2 * celula[i - 1].acsr.injm.FluidoPro.RGO * celula[i - 1].acsr.injm.FluidoPro.yco2) / qgstd;
        else
            celula[i].flui.yco2 = celula[i - 1].flui.yco2;
        if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
            // o modelo seja ASTM. Observar que isto sÃƒÂ³ faz sentido se se tiver mais de um fluido
            // de producao cadastrado no JSON, ou a simulacao se insere em um sistema de redes
            // com varios tramos alimentando outros tramos com fluidos com propriedades diferentes
            // entre cada tramo
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 &&
                fabs(celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15) {
                double denmixSTD = (141.5 / (131.5 + celula[i - 1].flui.API)) * qostd1 +
                                   (141.5 / (131.5 + celula[i - 1].acsr.injm.FluidoPro.API)) * qostd2;
                denmixSTD /= (qostd1 + qostd2);
                celula[i].flui.API = 141.5 / denmixSTD - 131.5;
            } else if (arq.flashCompleto == 0 ||
                       fabs(celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) <= 1e-15)
                celula[i].flui.API = celula[i - 1].flui.API;
            double qw1;
            if ((1. - celula[i - 1].flui.BSW) > 0)
                qw1 = qostd1 * celula[i - 1].flui.BSW / (1. - celula[i - 1].flui.BSW);
            else
                qw1 = celula[i - 1].Mliqini * celula[i - 1].FW * (1. - celula[i - 1].bet) / (1000. * celula[i - 1].flui.Denag);
            double qw2;
            if ((1. - celula[i - 1].acsr.injm.FluidoPro.BSW) > 0)
                qw2 = qostd2 * celula[i - 1].acsr.injm.FluidoPro.BSW / (1. - celula[i - 1].acsr.injm.FluidoPro.BSW);
            else
                qw2 = celula[i - 1].fontemassLR * fwinjl / (1000. * celula[i - 1].acsr.injm.FluidoPro.Denag);

            if (fabs(qw1 + qw2 + qostd1 + qostd2) > 1e-15 &&
                (celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15)
                celula[i].flui.BSW = (qw1 + qw2) / (qw1 + qw2 + qostd1 + qostd2);
            else
                celula[i].flui.BSW = celula[i - 1].flui.BSW;
            if (fabs(qw1 + qw2) > 1e-15 &&
                (celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15)
                celula[i].flui.Denag = (qw1 * celula[i - 1].flui.Denag +
                                        qw2 * celula[i - 1].acsr.injm.FluidoPro.Denag) /
                                       (qw1 + qw2);
            else
                celula[i].flui.Denag = celula[i - 1].flui.Denag;
            // reavaliacao dos pares necessarios para o modelo de viscosidade de oleo morto
            // ASTM. Isto ÃƒÂ© feito tendo sempre os mesmos valores de temperatura maxima e
            // e minima para a construcao dos pares
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 &&
                (celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) > 1e-15) { // vazao de liquido >0
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
                celula[i].flui.LVisL = (qostd1 * celula[i - 1].flui.VisOM(tL) + qostd2 * celula[i - 1].acsr.injm.FluidoPro.VisOM(tL)) / (qostd1 + qostd2);
                celula[i].flui.LVisH = (qostd1 * celula[i - 1].flui.VisOM(tH) + qostd2 * celula[i - 1].acsr.injm.FluidoPro.VisOM(tH)) / (qostd1 + qostd2);
            } else if (arq.flashCompleto == 0 ||
                       fabs(celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP) <= 1e-15) { // se,m vazao de liquido
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
        }
        if (arq.flashCompleto == 0) {
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }
        if ((celula[i - 1].bet > (*vg1dSP).localtiny * 1e-6 || fabs(celula[i - 1].fontemassCR) > (*vg1dSP).localtiny) &&
            fabs(celula[i - 1].acsr.injm.MassG + celula[i - 1].acsr.injm.MassP + celula[i - 1].acsr.injm.MassC) > 1e-15) {
            // reavaliacao da fracao volumetrica do liquido complementar
            // observar que a fonte de liquido pode ter uma fracao de liquido complementar
            // distinta da onservada a esquerda da celula i-1
            double boN = celula[i].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baN = celula[i].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwN = celula[i].flui.BSW * baN / (boN + baN * celula[i].flui.BSW - celula[i].flui.BSW * boN);
            boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qlpF = celula[i - 1].fontemassLR / celula[i - 1].acsr.injm.FluidoPro.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            double qlcF = celula[i - 1].fontemassCR / celula[i - 1].acsr.injm.fluidocol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
            double betN = (celula[i - 1].QL * celula[i - 1].bet + qlcF) / (celula[i - 1].QL + qlpF + qlcF);
            double qo = (celula[i - 1].QL + qlpF + qlcF) * (1 - fwN) * (1 - betN) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet) + qlpF;
            double qc = (celula[i - 1].QL * (celula[i - 1].bet) + qlcF) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
        } else
            celula[i].bet = celula[i - 1].bet;
    }
    // caso especial, fonte de vazamento:
    else if (celula[i - 1].acsr.tipo == 9 && celula[i - 1].acsr.fontechk.abertura > 1e-6) {
        ProFlu fluF;
        // define qual o fluido envolvido na fonte, se a pressÃƒÂ£o ambiente for menor do que a
        // pressao da tubulacao, fluido da tubulacao, senao, fluido definido como
        // fluido ambiente
        if (celula[i - 1].acsr.fontechk.presT > celula[i - 1].acsr.fontechk.pamb) {
            fluF = celula[i - 1].acsr.fontechk.fluidoP;
        } else {
            fluF = celula[i - 1].acsr.fontechk.fluidoPamb;
        }
        double api = celula[i - 1].flui.API;
        double rhololeo = 1000. * 141.5 / (131.5 + api);
        double rhololeoF = 1000. * 141.5 / (131.5 + fluF.API);
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double rholisF = fluF.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double boinjl = fluF.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
        double bainjl = fluF.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        double fwinjl = fluF.BSW * bainjl / (boinjl + bainjl * fluF.BSW - fluF.BSW * boinjl);
        // vazao de oleo standard antes da fonte:
        double qostd1 = 0.;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd1 = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) * (1. - celula[i - 1].bet) / (bo * rholis);
        // vazao de oleo standard da fonte:
        double qostd2 = 0.;
        if (fluF.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd2 = celula[i - 1].fontemassLR * (1. - fwinjl) / (boinjl * rholisF);
        // vazao total de gas nas condicoes standard fruto da soma do gas transportado
        // da fronteira a esquerda
        // da celula e do gas associado ao vazamento
        double qgstd;
        if (fabs(qostd2) > 0.)
            qgstd = qostd1 * celula[i - 1].flui.RGO + qostd2 * fluF.RGO;
        else {
            qgstd = qostd1 * celula[i - 1].flui.RGO + celula[i - 1].fontemassGR / (fluF.Deng * 1.225);
        }
        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas deve ser calculado,
        // pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;

        if (arq.trackRGO == -1) {
            // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
            // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny * 1e-6) && bsw < (1. - (*vg1dSP).localtiny) && mudaRGO == 1)
                celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
            if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1)
                celula[i].flui.RGO = (*vg1dSP).RGOMax;
        } else { // calculo do novo RGO do separador modificado pela IPR
            if (fabs(qostd1 + qostd2) > (*vg1dSP).localtiny * 1e-6 && mudaRGO == 1)
                celula[i].flui.RGO = qgstd / (qostd1 + qostd2);
            else if (mudaRGO == 1)
                celula[i].flui.RGO = (*vg1dSP).RGOMax;
        }
        // calculo da nova densidade de gas nas condicoes standard modificada pelo vazamento
        if (fabs(qgstd) > (*vg1dSP).localtiny && arq.flashCompleto == 0) {
            if (fabs(qostd2) > 0)
                celula[i].flui.Deng = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + qostd2 * fluF.RGO * fluF.Deng) / qgstd;
            else
                celula[i].flui.Deng = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + celula[i - 1].fontemassGR / (fluF.Deng * 1.225)) / qgstd;
        }
        // calculo da nova fracao de co2 modificada pelo vazamento
        if (fabs(qgstd) > (*vg1dSP).localtiny && arq.flashCompleto == 0) {
            if (fabs(qostd2) > 0)
                celula[i].flui.yco2 = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + qostd2 * fluF.RGO * fluF.yco2) / qgstd;
            else
                celula[i].flui.yco2 = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + fluF.yco2 * celula[i - 1].fontemassGR / (fluF.Deng * 1.225)) / qgstd;
        }
        if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
            // o modelo seja ASTM. Observar que isto sÃƒÂ³ faz sentido se se tiver mais de um fluido
            // de producao cadastrado no JSON, ou a simulacao se insere em um sistema de redes
            // com varios tramos alimentando outros tramos com fluidos com propriedades diferentes
            // entre cada tramo
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0) {
                double denmixSTD = (141.5 / (131.5 + celula[i - 1].flui.API)) * qostd1 +
                                   (141.5 / (131.5 + fluF.API)) * qostd2;
                denmixSTD /= (qostd1 + qostd2);
                celula[i].flui.API = 141.5 / denmixSTD - 131.5;
            } else if (arq.flashCompleto == 0)
                celula[i].flui.API = celula[i - 1].flui.API;
            double qw1;
            if ((1. - celula[i - 1].flui.BSW) > 0)
                qw1 = qostd1 * celula[i - 1].flui.BSW / (1. - celula[i - 1].flui.BSW);
            else
                qw1 = celula[i - 1].Mliqini * celula[i - 1].FW * (1. - celula[i - 1].bet) / (1000. * celula[i - 1].flui.Denag);
            double qw2;
            if ((1. - fluF.BSW) > 0)
                qw2 = qostd2 * fluF.BSW / (1. - fluF.BSW);
            else
                qw2 = celula[i - 1].fontemassLR * fwinjl / (1000. * fluF.Denag);

            if (fabs(qw1 + qw2 + qostd1 + qostd2) > 1e-15)
                celula[i].flui.BSW = (qw1 + qw2) / (qw1 + qw2 + qostd1 + qostd2);
            else
                celula[i].flui.BSW = celula[i - 1].flui.BSW;

            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0) {
                // reavaliacao dos pares necessarios para o modelo de viscosidade de oleo morto
                // ASTM. Isto ÃƒÂ© feito tendo sempre os mesmos valores de temperatura maxima e
                // e minima para a construcao dos pares
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
                celula[i].flui.LVisL = (qostd1 * celula[i - 1].flui.VisOM(tL) + qostd2 * fluF.VisOM(tL)) / (qostd1 + qostd2);
                celula[i].flui.LVisH = (qostd1 * celula[i - 1].flui.VisOM(tH) + qostd2 * fluF.VisOM(tH)) / (qostd1 + qostd2);
            } else if (arq.flashCompleto == 0) {
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
        }
        if (arq.flashCompleto == 0) {
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }
        if (celula[i - 1].bet > (*vg1dSP).localtiny * 1e-6 || fabs(celula[i - 1].fontemassCR) > (*vg1dSP).localtiny * 1e-6) {
            // reavaliacao da fracao volumetrica do liquido complementar
            // observar a fonte de vazamento pode ter uma fracao de liquido complementar
            // distinta da observada a esquerda da celula i-1
            double boN = celula[i].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baN = celula[i].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwN = celula[i].flui.BSW * baN / (boN + baN * celula[i].flui.BSW - celula[i].flui.BSW * boN);
            boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qlpF = celula[i - 1].fontemassLR / fluF.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            double qlcF = celula[i - 1].fontemassCR / celula[i - 1].acsr.fontechk.fluidocol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
            double betN = (celula[i - 1].QL * celula[i - 1].bet + qlcF) / (celula[i - 1].QL + qlpF + qlcF);
            double qo = (celula[i - 1].QL + qlpF + qlcF) * (1 - fwN) * (1 - betN) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet) + qlpF;
            double qc = (celula[i - 1].QL * (celula[i - 1].bet) + qlcF) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
        } else
            celula[i].bet = celula[i - 1].bet;
    } else if (celula[i - 1].acsr.tipo == 15) {
        double api = celula[i - 1].flui.API;
        double rhololeo = 1000. * 141.5 / (131.5 + api);
        double rhololeoF = 1000. * 141.5 / (131.5 + celula[i - 1].acsr.radialPoro.flup.API);
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double rholisF;
        double boipr;
        double baipr;
        double fwipr;
        trF = 0.;
        if (fabs(celula[i - 1].fontemassLR) > 1e-15) {
            rholisF = celula[i - 1].acsr.radialPoro.flup.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            boipr = celula[i - 1].acsr.radialPoro.flup.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            baipr = celula[i - 1].acsr.radialPoro.flup.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            fwipr = celula[i - 1].acsr.radialPoro.BSW * baipr / (boipr + baipr * celula[i - 1].acsr.radialPoro.BSW - celula[i - 1].acsr.radialPoro.BSW * boipr);
        } else {
            rholisF = rholis;
            boipr = bo;
            baipr = ba;
            fwipr = celula[i - 1].FW;
        }
        // vazao de oleo standard antes da fonte:
        double qostd1 = 0.;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd1 = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) * (1. - celula[i - 1].bet) / (bo * rholis);
        // vazao de oleo standard da fonte:
        double qostd2 = 0.;
        if (celula[i - 1].acsr.radialPoro.flup.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd2 = celula[i - 1].fontemassLR * (1. - fwipr) / (boipr * rholisF);
        // vazao total de gas nas condicoes standard fruto da soma do gas transportado
        // da fronteira a esquerda
        // da celula e do gas associado da IPR
        double qgstd = qostd1 * celula[i - 1].flui.RGO + qostd2 * celula[i - 1].acsr.radialPoro.flup.RGO;
        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas deve ser calculado,
        // pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;
        if (arq.trackRGO == -1) {
            // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
            // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if (fabs(celula[i - 1].fontemassLR) > 1e-15) {
                if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny) &&
                    mudaRGO == 1 && fabs(celula[i - 1].fontemassLR) > 1e-15)
                    celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
                if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else
                celula[i].flui.RGO = celula[i - 1].flui.RGO;

        } else { // calculo do novo RGO do separador modificado pela IPR
            if ((celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15) {
                if (fabs(qostd1 + qostd2) > (*vg1dSP).localtiny * 1e-10 && mudaRGO == 1)
                    celula[i].flui.RGO = qgstd / (qostd1 + qostd2);
                else if (mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else
                celula[i].flui.RGO = celula[i - 1].flui.RGO;
        }
        // calculo da nova densidade de gas nas condicoes standard modificada pela IPR
        if (arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15 &&
            fabs(qgstd) > 1e-15)
            celula[i].flui.Deng = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + qostd2 * celula[i - 1].acsr.radialPoro.flup.RGO * celula[i - 1].acsr.radialPoro.flup.Deng) / qgstd;
        else
            celula[i].flui.Deng = celula[i - 1].flui.Deng;
        // calculo da nova fracao de co2 modificada pela fonte de liquido
        if (arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15 && fabs(qgstd) > 1e-15)
            celula[i].flui.yco2 = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + qostd2 * celula[i - 1].acsr.radialPoro.flup.RGO * celula[i - 1].acsr.radialPoro.flup.yco2) / qgstd;
        else
            celula[i].flui.yco2 = celula[i - 1].flui.yco2;
        if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
            // o modelo seja ASTM. Observar que isto sÃƒÂ³ faz sentido se se tiver mais de um fluido
            // de producao cadastrado no JSON, ou a simulacao se insere em um sistema de redes
            // com varios tramos alimentando outros tramos com fluidos com propriedades diferentes
            // entre cada tramo
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 &&
                (celula[i - 1].fontemassLR) > 1e-15) {
                double denmixSTD = (141.5 / (131.5 + celula[i - 1].flui.API)) * qostd1 +
                                   (141.5 / (131.5 + celula[i - 1].acsr.radialPoro.flup.API)) * qostd2;
                denmixSTD /= (qostd1 + qostd2);
                celula[i].flui.API = 141.5 / denmixSTD - 131.5;
            } else if (arq.flashCompleto == 0 || fabs(celula[i - 1].fontemassLR) <= 1e-15)
                celula[i].flui.API = celula[i - 1].flui.API;
            double qw1;
            if ((1. - celula[i - 1].flui.BSW) > 0)
                qw1 = qostd1 * celula[i - 1].flui.BSW / (1. - celula[i - 1].flui.BSW);
            else
                qw1 = celula[i - 1].Mliqini * celula[i - 1].FW * (1. - celula[i - 1].bet) / (1000. * celula[i - 1].flui.Denag);
            double qw2;
            if ((1. - celula[i - 1].acsr.radialPoro.BSW) > 0)
                qw2 = qostd2 * celula[i - 1].acsr.radialPoro.BSW / (1. - celula[i - 1].acsr.radialPoro.BSW);
            else
                qw2 = celula[i - 1].fontemassLR * fwipr / (1000. * celula[i - 1].acsr.radialPoro.flup.Denag);

            if (fabs(qw1 + qw2 + qostd1 + qostd2) > 1e-15 &&
                fabs(celula[i - 1].fontemassLR) > 1e-15)
                celula[i].flui.BSW = ((qw1 + qostd1) * celula[i - 1].flui.BSW + (qw2 + qostd2) * celula[i - 1].acsr.radialPoro.BSW) / (qw1 + qw2 + qostd1 + qostd2);
            else
                celula[i].flui.BSW = celula[i - 1].flui.BSW;
            if ((qw1 + qw2) > 1e-15 && (celula[i - 1].fontemassLR) > 1e-15)
                celula[i].flui.Denag = (qw1 * celula[i - 1].flui.Denag +
                                        qw2 * celula[i - 1].acsr.radialPoro.flup.Denag) /
                                       (qw1 + qw2);
            else
                celula[i].flui.Denag = celula[i - 1].flui.Denag;
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15) {
                // reavaliacao dos pares necessarios para o modelo de viscosidade de oleo morto
                // ASTM. Isto ÃƒÂ© feito tendo sempre os mesmos valores de temperatura maxima e
                // e minima para a construcao dos pares
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
                celula[i].flui.LVisL = (qostd1 * celula[i - 1].flui.VisOM(tL) + qostd2 * celula[i - 1].acsr.radialPoro.flup.VisOM(tL)) / (qostd1 + qostd2);
                celula[i].flui.LVisH = (qostd1 * celula[i - 1].flui.VisOM(tH) + qostd2 * celula[i - 1].acsr.radialPoro.flup.VisOM(tH)) / (qostd1 + qostd2);
            } else if (arq.flashCompleto == 0 || fabs(celula[i - 1].fontemassLR) <= 1e-15) { // sem vazao de liquido
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
        }
        if (arq.flashCompleto == 0) {
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }
        if (celula[i - 1].bet > (*vg1dSP).localtiny * 1e-6 && fabs(celula[i - 1].fontemassLR) > 1e-15) {
            // reavaliacao da fracao volumetrica do liquido complementar
            // observar que a IPR pode ter uma fracao de liquido complementar
            // distinta da onservada a esquerda da celula i-1
            double boN = celula[i].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baN = celula[i].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwN = celula[i].flui.BSW * baN / (boN + baN * celula[i].flui.BSW - celula[i].flui.BSW * boN);
            boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qlpF = celula[i - 1].fontemassLR / celula[i - 1].acsr.radialPoro.flup.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            double qlcF = 0.;
            double betN = (celula[i - 1].QL * celula[i - 1].bet + qlcF) / (celula[i - 1].QL + qlpF + qlcF);
            double qo = (celula[i - 1].QL + qlpF + qlcF) * (1 - fwN) * (1 - betN) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet) + qlpF;
            double qc = (celula[i - 1].QL * (celula[i - 1].bet) + qlcF) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
        } else
            celula[i].bet = celula[i - 1].bet;
    } else if (celula[i - 1].acsr.tipo == 16) {
        double api = celula[i - 1].flui.API;
        double rhololeo = 1000. * 141.5 / (131.5 + api);
        double rhololeoF = 1000. * 141.5 / (131.5 + celula[i - 1].acsr.poroso2D.dados.flup.API);
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double rholisF;
        double boipr;
        double baipr;
        double fwipr;
        trF = 0.;
        if (fabs(celula[i - 1].fontemassLR) > 1e-15) {
            rholisF = celula[i - 1].acsr.poroso2D.dados.flup.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            boipr = celula[i - 1].acsr.poroso2D.dados.flup.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            baipr = celula[i - 1].acsr.poroso2D.dados.flup.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            fwipr = celula[i - 1].acsr.poroso2D.dados.transfer.BSW * baipr / (boipr + baipr * celula[i - 1].acsr.poroso2D.dados.transfer.BSW - celula[i - 1].acsr.poroso2D.dados.transfer.BSW * boipr);
        } else {
            rholisF = rholis;
            boipr = bo;
            baipr = ba;
            fwipr = celula[i - 1].FW;
        }
        // vazao de oleo standard antes da fonte:
        double qostd1 = 0.;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd1 = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) * (1. - celula[i - 1].bet) / (bo * rholis);
        // vazao de oleo standard da fonte:
        double qostd2 = 0.;
        if (celula[i - 1].acsr.poroso2D.dados.flup.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd2 = celula[i - 1].fontemassLR * (1. - fwipr) / (boipr * rholisF);
        // vazao total de gas nas condicoes standard fruto da soma do gas transportado
        // da fronteira a esquerda
        // da celula e do gas associado da IPR
        double qgstd = qostd1 * celula[i - 1].flui.RGO + qostd2 * celula[i - 1].acsr.poroso2D.dados.flup.RGO;
        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃƒÂ£o ÃƒÂ© relevante para o permanente, mas deve ser calculado,
        // pois ÃƒÂ© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;
        if (arq.trackRGO == -1) {
            // esta chave nÃƒÂ£o ÃƒÂ© utilizada, ÃƒÂ© mantida aqui como reserva, atualmente este
            // calculo nÃƒÂ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if (fabs(celula[i - 1].fontemassLR) > 1e-15) {
                if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny) &&
                    mudaRGO == 1 && fabs(celula[i - 1].fontemassLR) > 1e-15)
                    celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
                if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else
                celula[i].flui.RGO = celula[i - 1].flui.RGO;

        } else { // calculo do novo RGO do separador modificado pela IPR
            if ((celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15) {
                if (fabs(qostd1 + qostd2) > (*vg1dSP).localtiny * 1e-10 && mudaRGO == 1)
                    celula[i].flui.RGO = qgstd / (qostd1 + qostd2);
                else if (mudaRGO == 1)
                    celula[i].flui.RGO = (*vg1dSP).RGOMax;
            } else
                celula[i].flui.RGO = celula[i - 1].flui.RGO;
        }
        // calculo da nova densidade de gas nas condicoes standard modificada pela IPR
        if (arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15 &&
            fabs(qgstd) > 1e-15)
            celula[i].flui.Deng = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.Deng + qostd2 * celula[i - 1].acsr.poroso2D.dados.flup.RGO * celula[i - 1].acsr.poroso2D.dados.flup.Deng) / qgstd;
        else
            celula[i].flui.Deng = celula[i - 1].flui.Deng;
        // calculo da nova fracao de co2 modificada pela fonte de liquido
        if (arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15 && (celula[i - 1].fontemassGR) > 1e-15 && fabs(qgstd) > 1e-15)
            celula[i].flui.yco2 = (qostd1 * celula[i - 1].flui.RGO * celula[i - 1].flui.yco2 + qostd2 * celula[i - 1].acsr.poroso2D.dados.flup.RGO * celula[i - 1].acsr.poroso2D.dados.flup.yco2) / qgstd;
        else
            celula[i].flui.yco2 = celula[i - 1].flui.yco2;
        if (nfluP > 1 || (*vg1dSP).chaverede == 1) {
            // o modelo seja ASTM. Observar que isto sÃƒÂ³ faz sentido se se tiver mais de um fluido
            // de producao cadastrado no JSON, ou a simulacao se insere em um sistema de redes
            // com varios tramos alimentando outros tramos com fluidos com propriedades diferentes
            // entre cada tramo
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 &&
                (celula[i - 1].fontemassLR) > 1e-15) {
                double denmixSTD = (141.5 / (131.5 + celula[i - 1].flui.API)) * qostd1 +
                                   (141.5 / (131.5 + celula[i - 1].acsr.poroso2D.dados.flup.API)) * qostd2;
                denmixSTD /= (qostd1 + qostd2);
                celula[i].flui.API = 141.5 / denmixSTD - 131.5;
            } else if (arq.flashCompleto == 0 || (celula[i - 1].fontemassLR) <= 1e-15)
                celula[i].flui.API = celula[i - 1].flui.API;
            double qw1;
            if ((1. - celula[i - 1].flui.BSW) > 0)
                qw1 = qostd1 * celula[i - 1].flui.BSW / (1. - celula[i - 1].flui.BSW);
            else
                qw1 = celula[i - 1].Mliqini * celula[i - 1].FW * (1. - celula[i - 1].bet) / (1000. * celula[i - 1].flui.Denag);
            double qw2;
            if ((1. - celula[i - 1].acsr.poroso2D.dados.transfer.BSW) > 0)
                qw2 = qostd2 * celula[i - 1].acsr.poroso2D.dados.transfer.BSW / (1. - celula[i - 1].acsr.poroso2D.dados.transfer.BSW);
            else
                qw2 = celula[i - 1].fontemassLR * fwipr / (1000. * celula[i - 1].acsr.poroso2D.dados.flup.Denag);

            if (fabs(qw1 + qw2 + qostd1 + qostd2) > 1e-15 &&
                (celula[i - 1].fontemassLR) > 1e-15)
                celula[i].flui.BSW = ((qw1 + qostd1) * celula[i - 1].flui.BSW + (qw2 + qostd2) * celula[i - 1].acsr.poroso2D.dados.transfer.BSW) / (qw1 + qw2 + qostd1 + qostd2);
            else
                celula[i].flui.BSW = celula[i - 1].flui.BSW;
            if (fabs(qw1 + qw2) > 1e-15 &&
                (celula[i - 1].fontemassLR) > 1e-15)
                celula[i].flui.Denag = (qw1 * celula[i - 1].flui.Denag +
                                        qw2 * celula[i - 1].acsr.poroso2D.dados.flup.Denag) /
                                       (qw1 + qw2);
            else
                celula[i].flui.Denag = celula[i - 1].flui.Denag;
            if (fabs(qostd1 + qostd2) > 1e-15 && arq.flashCompleto == 0 && (celula[i - 1].fontemassLR) > 1e-15) {
                // reavaliacao dos pares necessarios para o modelo de viscosidade de oleo morto
                // ASTM. Isto ÃƒÂ© feito tendo sempre os mesmos valores de temperatura maxima e
                // e minima para a construcao dos pares
                celula[i].flui.TempL = tL;
                celula[i].flui.TempH = tH;
                celula[i].flui.LVisL = (qostd1 * celula[i - 1].flui.VisOM(tL) + qostd2 * celula[i - 1].acsr.poroso2D.dados.flup.VisOM(tL)) / (qostd1 + qostd2);
                celula[i].flui.LVisH = (qostd1 * celula[i - 1].flui.VisOM(tH) + qostd2 * celula[i - 1].acsr.poroso2D.dados.flup.VisOM(tH)) / (qostd1 + qostd2);
            } else if (arq.flashCompleto == 0 || fabs(celula[i - 1].fontemassLR) <= 1e-15) { // sem vazao de liquido
                celula[i].flui.TempL = celula[i - 1].flui.TempL;
                celula[i].flui.TempH = celula[i - 1].flui.TempH;
                celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
                celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
            }
        }
        if (arq.flashCompleto == 0) {
            celula[i].flui.RenovaFluido();
            corrDeng(i);
        }
        if (celula[i - 1].bet > (*vg1dSP).localtiny * 1e-6 && fabs(celula[i - 1].fontemassLR) > 1e-15) {
            // reavaliacao da fracao volumetrica do liquido complementar
            // observar que a IPR pode ter uma fracao de liquido complementar
            // distinta da onservada a esquerda da celula i-1
            double boN = celula[i].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baN = celula[i].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwN = celula[i].flui.BSW * baN / (boN + baN * celula[i].flui.BSW - celula[i].flui.BSW * boN);
            boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qlpF = celula[i - 1].fontemassLR / celula[i - 1].acsr.poroso2D.dados.flup.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            double qlcF = 0.;
            double betN = (celula[i - 1].QL * celula[i - 1].bet + qlcF) / (celula[i - 1].QL + qlpF + qlcF);
            double qo = (celula[i - 1].QL + qlpF + qlcF) * (1 - fwN) * (1 - betN) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet) + qlpF;
            double qc = (celula[i - 1].QL * (celula[i - 1].bet) + qlcF) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
        } else
            celula[i].bet = celula[i - 1].bet;
    }

    // atualizaÃ§Ã£o da vazao massica da mistura:
    celula[i].MC = celula[i - 1].MC + celula[i - 1].fontemassCR + celula[i - 1].fontemassLR + celula[i - 1].fontemassGR;
    celula[i - 1].MR = celula[i].MC;
    if (i < ncel)
        celula[i + 1].ML = celula[i].MC;
    celula[i - 1].MRini = celula[i - 1].MR;
    double pmed = celula[i].presaux;
    double rhog;
    double rhol = 1000.;
    // calculo da vazao massica de liquido e das vazoes volumetricas:
    double qo = 0.;
    double masoleo;
    if (celula[i].flui.RGO < 1e7) { // Caso exista liquido
        if (arq.tipoFluido == 0) {
            rs = celula[i].flui.RS(pmed, tmed);
            bo = celula[i].flui.BOFunc(pmed, tmed, rs);
            rs = rs * 6.29 / 35.31467;
            double rhogstd = celula[i].flui.Deng * 1.225;
            double rhololeostd = 1000. * 141.5 / (131.5 + celula[i].flui.API);
            double rhoa = celula[i].flui.Denag * 1000.;

            double denom = celula[i].flui.RGO * rhogstd + rhololeostd;
            if (fabs(1 - celula[i].flui.BSW) > (*vg1dSP).localtiny) {
                denom += rhoa * celula[i].flui.BSW / (1 - celula[i].flui.BSW);
                if (fabs(1 - celula[i].bet) > (*vg1dSP).localtiny * 1e-6 && fabs(celula[i].bet) > (*vg1dSP).localtiny * 1e-10) {
                    denom += (celula[i].fluicol.MasEspFlu(pmed, tmed) * celula[i].bet / (1 - celula[i].bet)) * bo * (1. + fwI / (1 - fwI));
                }
            }
            if (fabs(1 - celula[i].bet) > (*vg1dSP).localtiny * 1e-6 && fabs(1 - celula[i].flui.BSW) > (*vg1dSP).localtiny)
                qo = celula[i].MC / denom;

            if (fabs(1 - celula[i].bet) > (*vg1dSP).localtiny * 1e-6 && fabs(1 - celula[i].flui.BSW) > (*vg1dSP).localtiny) {
                if ((*vg1dSP).tipoFluidoRedeGlob == 0)
                    celula[i].Mliqini = celula[i].MC - (celula[i].flui.RGO - rs) * rhogstd * qo;
                else {
                    masoleo = qo * bo * celula[i].flui.MasEspoleo(pmed, tmed, rs) + qo * rhoa * celula[i].flui.BSW / (1 - celula[i].flui.BSW);
                    celula[i].Mliqini = masoleo + celula[i].MComp;
                }
            } else
                celula[i].Mliqini = celula[i].MC - (celula[i - 1].MC - celula[i - 1].Mliqini) - celula[i - 1].fontemassGR;
        } else {
            double tit = celula[i].flui.FracMass(pmed, tmed);
            celula[i].Mliqini = celula[i].MC * (1. - tit);
        }
        celula[i - 1].MliqiniR = celula[i].Mliqini;
        if (i < ncel)
            celula[i + 1].MliqiniL = celula[i].Mliqini;
        pmed = celula[i].presaux + celula[i - 1].dpB / 98066.5;
        rhog = celula[i].rgCi = celula[i].flui.MasEspGas(pmed, tmed);

        // calculo das massas especificas na interface a esquerda da celula
        celula[i].rpCi = celula[i].flui.MasEspLiq(pmed, tmed);
        celula[i].rcCi = celula[i].fluicol.MasEspFlu(pmed, tmed);
        rhol = (1 - celula[i].bet) * celula[i].rpCi + celula[i].bet * celula[i].rcCi;
        // rhol = (1 - celula[i].bet) * celula[i].flui.MasEspLiq(pmed, tmed)
        // vazoes volumetricas:
        celula[i].QL = celula[i].Mliqini / rhol;
        if (i < ncel)
            celula[i + 1].QLL = celula[i].QL;
        celula[i - 1].QLR = celula[i].QL;
        celula[i].QG = (celula[i].MC - celula[i].Mliqini) / rhog;
    } else {
        celula[i].Mliqini = 0.;
        celula[i - 1].MliqiniR = celula[i].Mliqini;
        if (i < ncel)
            celula[i + 1].MliqiniL = celula[i].Mliqini;
        celula[i].QL = 0.;
        if (i < ncel)
            celula[i + 1].QLL = celula[i].QL;
        celula[i - 1].QLR = celula[i].QL;
        pmed = celula[i].presaux + celula[i - 1].dpB / 98066.5;
        celula[i].rpCi = celula[i].flui.MasEspLiq(pmed, tmed);
        celula[i].rcCi = celula[i].fluicol.MasEspFlu(pmed, tmed);
        rhog = celula[i].rgCi = celula[i].flui.MasEspGas(pmed, tmed);
        celula[i].QG = (celula[i].MC) / rhog;
    }
    // Definicao das fracoes volumetricas:
    if (fabs(celula[i].QG + celula[i].QL) < (*vg1dSP).localtiny) {
        if (arq.tipoFluido == 1) {
            celula[i].alf = 0.;
        } else {
            celula[i].alf = 1.;
        }
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else if (fabs(celula[i].QG) < (*vg1dSP).localtiny) { // caso so exista liquido:
        celula[i].alf = 0.;
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else if (fabs(celula[i].QL) < (*vg1dSP).localtiny * 1e-6) { // caso so exista gas:
        celula[i].alf = 1.;
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        celula[i].c0 = 1.;
        celula[i].ud = 0.;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else { // caso bifasico
        double c0 = 1.;
        double ud = 0.;
        if (fabs(celula[i].QL) > (*vg1dSP).localtiny * 1e-6) {
            if (arq.tipoModeloDrift == 1) {
                if (iterperm == 0) { // primeira estimativa, primeira iteracao
                    // utiliza-se a fracao de vazio sem escorregamento, pois a propria correlacao para se obter a
                    // fracao de vazio depende do valor da fracao de vazio
                    if ((fabs(celula[i].QG) + fabs(celula[i].QL)) > (*vg1dSP).localtiny) {
                        if (monitConvPerm > 0.01)
                            celula[i].alf = fabs(celula[i].QG) /
                                            (fabs(celula[i].QG) + fabs(celula[i].QL));
                        CalcC0UdPerm(i, c0, ud);
                    } else
                        celula[i].alf = 0.;
                    celula[i].alfini = celula[i].alf;
                    celula[i - 1].alfR = celula[i].alf;
                    celula[i - 1].alfRini = celula[i].alf;
                    if (i < ncel)
                        celula[i + 1].alfL = celula[i].alf;
                    if (i < ncel)
                        celula[i + 1].alfLini = celula[i].alf;
                    celula[i].alfPigD = celula[i].alf;
                    celula[i].alfPigDini = celula[i].alf;
                    celula[i].alfPigE = celula[i].alf;
                    celula[i].alfPigEini = celula[i].alf;
                }
                if (fabs(rhog) / rhol > 0.9) {
                    c0 = 1.;
                    ud = 0.;
                }

                // para o caso permanente, a fracao de vazio e obtida a partir das relacoes de escorregamento
                // portanto, e neste ponto que se obtem Co e Ud:
                else if (fabs(celula[i].QG) > (*vg1dSP).localtiny && fabs(celula[i].QL) > (*vg1dSP).localtiny * 1e-6)
                    CalcC0UdPerm(i, c0, ud);
                celula[i].c0 = c0;
                celula[i].ud = ud;
                double area = celula[i].duto.area;
                if (fabs(celula[i].QG + celula[i].QL) > (*vg1dSP).localtiny) {
                    // alfa com escorregamento:
                    celula[i].alf = celula[i].QG / (c0 * (celula[i].QG + celula[i].QL) + ud * area);
                    double alfHomo = celula[i].QG / (celula[i].QG + celula[i].QL);
                    if (celula[i].alf > 1. - 1e-15 || celula[i].alf < 1e-15)
                        celula[i].alf = alfHomo;

                } else
                    celula[i].alf = 0.;
                if (celula[i].alf > (1 - (*vg1dSP).localtiny) && fabs(celula[i].QG + celula[i].QL) > (*vg1dSP).localtiny)
                    celula[i].alf = fabs(celula[i].QG) / fabs(celula[i].QG + celula[i].QL);
                else if (celula[i].alf > (1 - (*vg1dSP).localtiny))
                    celula[i].alf = 1.;
            } else {
                double holdup;
                double frictionGrad;
                double gravityGrad;
                double totalGrad;
                double reynolds;
                unsigned char flowType;
                char *errorMsg;
                unsigned char errorFlag;
                executarCorrelacao(celula, i, 0, arq.AceleraConvergPerm,
                                   celula[i - 1].correlacaoMR2,
                                   holdup, frictionGrad, gravityGrad, totalGrad,
                                   reynolds, flowType);
                celula[i].alf = 1. - holdup;
            }
        } else {
            c0 = 1.;
            ud = 0.;
            celula[i].alf = 1.;
        }
        if (celula[i].alf < 0.)
            celula[i].alf = 0.;
        else if (celula[i].alf > 1.)
            celula[i].alf = 1.;
        // atualizacoes dos valores das fracoes volumetricas da celula i armazendadas em
        // outras celulas, e inclusiove armazendo os valores para "tempo anterior", que nao
        // sao relevantes para o problema permanente mas importantes se o resultado permanente
        // der partida na solucao transiente:
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    }

    if (fabs(celula[i].QL) > 1e-15 && fabs(celula[i].MComp) > 1e-15) {
        double dxmed = 0.5 * (celula[i - 1].dx + celula[i].dx);
        double hol0 = 1. - celula[i - 1].alf;
        double hol1 = 1. - celula[i].alf;
        celula[i].fluicol.TR = (celula[i - 1].fluicol.TR * celula[i - 1].MComp + celula[i - 1].fontemassCR * trF) / celula[i].MComp;
        celula[i].fluicol.TR = celula[i].fluicol.TR + (0.5 * celula[i - 1].duto.area * celula[i - 1].dx * hol0 +
                                                       0.5 * celula[i].duto.area * celula[i].dx * hol1) /
                                                          celula[i].QL;
    } else
        celula[i].fluicol.TR = celula[i - 1].fluicol.TR;
}
void SProd::RenovaMassPermRev(int i) {
    int mudaRGO = 1;
    if (arq.flashCompleto == 1)
        mudaRGO = 1;
    ProFlu fluF;
    double trF = 0.;
    if (i == 1) {
        renovaFonte(i - 1); // metodo que verifica se existe uma fonte na celula e calcula o valor das
        // vazoes massicas de liquido produzido (oleo+agua), gas e liquido complementar
        // relacao entre fonte a esquerda e a direita de uma celula:
        celula[i].fontemassLL = celula[i - 1].fontemassLR;
        celula[i].fontemassCL = celula[i - 1].fontemassCR;
        celula[i].fontemassGL = celula[i - 1].fontemassGR;

        celula[i].MC = celula[i - 1].fontemassLR + celula[i - 1].fontemassCR + celula[i - 1].fontemassGR;

        celula[i - 1].MR = celula[i].MC;
        if (i < ncel)
            celula[i + 1].ML = celula[i].MC;
        celula[i - 1].MRini = celula[i - 1].MR;
        celula[i].MComp = celula[i - 1].MComp + celula[i - 1].fontemassCR;

        if (celula[i - 1].acsr.tipo == 1) {
            fluF = celula[i - 1].acsr.injg.FluidoPro;
            trF = celula[i - 1].acsr.injg.fluidocol.TR;
        } else if (celula[i - 1].acsr.tipo == 2) {
            fluF = celula[i - 1].acsr.injl.FluidoPro;
            trF = celula[i - 1].acsr.injl.fluidocol.TR;
        } else if (celula[i - 1].acsr.tipo == 3) {
            fluF = celula[i - 1].acsr.ipr.FluidoPro;
            trF = 0.;
        } else if (celula[i - 1].acsr.tipo == 15) {
            fluF = celula[i - 1].acsr.radialPoro.flup;
            trF = 0.;
        } else if (celula[i - 1].acsr.tipo == 16) {
            fluF = celula[i - 1].acsr.poroso2D.dados.flup;
            trF = 0.;
        } else if (celula[i - 1].acsr.tipo == 10) {
            fluF = celula[i - 1].acsr.injm.FluidoPro;
            trF = celula[i - 1].acsr.injm.fluidocol.TR;
        } else if (celula[i - 1].acsr.tipo == 9 && celula[i - 1].acsr.fontechk.abertura > 1e-6) {
            if (celula[i - 1].acsr.fontechk.presT > celula[i - 1].acsr.fontechk.pamb) {
                fluF = celula[i - 1].acsr.fontechk.fluidoP;
            } else {
                fluF = celula[i - 1].acsr.fontechk.fluidoPamb;
            }
        }
        celula[i].flui = fluF;
        celula[i].flui.RenovaFluido();
        corrDeng(i);
    } else {
        celula[i - 1].fontemassLR = 0.;
        celula[i - 1].fontemassCR = 0.;
        celula[i - 1].fontemassGR = 0.;

        celula[i].fontemassLL = celula[i - 1].fontemassLR;
        celula[i].fontemassCL = celula[i - 1].fontemassCR;
        celula[i].fontemassGL = celula[i - 1].fontemassGR;

        celula[i].MC = celula[i - 1].MC;

        celula[i - 1].MR = celula[i].MC;
        if (i < ncel)
            celula[i + 1].ML = celula[i].MC;
        celula[i - 1].MRini = celula[i - 1].MR;

        celula[i].MComp = celula[i - 1].MComp;

        celula[i].flui = celula[i - 1].flui;
        celula[i].flui.RenovaFluido();
        corrDeng(i);
    }
    if (i == 0) {
        if (celula[0].acsr.tipo == 15)
            celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
        else if (celula[0].acsr.tipo == 16)
            celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
    }

    double tmed;
    // temperatura na fronteira entre a celula i-1 e a celula i, da segunda iteracao em diante, pode-se
    // usar a temperatura da celula i, pois ja a tem calculada, mas isto pode ser um complicador de
    // convergencia, mais seguro manter o criterio em todas as iteracoes, neste caso, a temperatura na
    // fronteira e admitida = a temperatura da celula i-1
    if (iterperm != 0 && arq.AceleraConvergPerm == 0)
        tmed = (celula[i].dx * celula[i].temp + celula[i].dxR * celula[i].tempR) / (celula[i].dx + celula[i].dxR);
    else
        tmed = celula[i - 1].temp;

    double pmed = celula[i].presaux;

    celula[i].rpCi = celula[i].flui.MasEspLiq(pmed, tmed);
    celula[i].rcCi = celula[i].fluicol.MasEspFlu(pmed, tmed);
    celula[i].rgCi = celula[i].flui.MasEspGas(pmed, tmed);

    double MasCarb = celula[i].MC - celula[i].MComp;
    double MasGas = MasCarb * celula[i].flui.FracMassHidra(celula[i].presaux, tmed);
    double MasLiqProd = MasCarb - MasGas;
    double boI;
    double baI;
    if (celula[i].flui.RGO < 1e6)
        boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
    else
        boI = 1.;
    baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
    double fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
    double qo = MasLiqProd * (1 - celula[i - 1].FW) / celula[i].rpCi;
    double qw;
    if (fwI < (1 - (*vg1dSP).localtiny))
        qw = fwI * qo / (1 - fwI);
    else
        qw = MasLiqProd / celula[i].flui.MasEspAgua(pmed, tmed);
    double qc;
    qc = celula[i].MComp / celula[i].rcCi;
    ////////////////////////esperar//////////////////////////////////////
    celula[i].bet = qc / (qo + qw + qc);

    celula[i].Mliqini = MasLiqProd;
    celula[i - 1].MliqiniR = celula[i].Mliqini;
    if (i < ncel)
        celula[i + 1].MliqiniL = celula[i].Mliqini;

    double rhol = (1 - celula[i].bet) * celula[i].rpCi + celula[i].bet * celula[i].rcCi;
    double rhog = celula[i].rgCi;
    // vazoes volumetricas:
    celula[i].QL = celula[i].Mliqini / rhol;
    if (i < ncel)
        celula[i + 1].QLL = celula[i].QL;
    celula[i - 1].QLR = celula[i].QL;
    celula[i].QG = (celula[i].MC - celula[i].Mliqini) / rhog;

    if (i < ncel)
        celula[i + 1].QLL = celula[i].QL;
    celula[i - 1].QLR = celula[i].QL;

    // definicao de temperaturas maximas e minimas para uma eventual reavaliacao do metodo
    // ASTM quando ocorre mistura de fluidos e se deseja atualizar o modelo
    // de viscosidade de oleo morto se este for o ASTM que trabalha com um par de temperatura
    double tL;
    if (arq.flashCompleto == 1 && (arq.tabent.tmin - 0) > (*vg1dSP).localtiny)
        tL = arq.tabent.tmin + 0.1;
    else
        tL = 0.;
    double tH;
    if (arq.flashCompleto == 1 && (70 - arq.tabent.tmax) < (*vg1dSP).localtiny)
        tH = arq.tabent.tmax - 0.1;
    else
        tH = 70.;
    double bo;
    double ba;
    double rs;
    // calcula os valores de RS, Bo e Ba na celula anterior a i-esima celula
    if (celula[i - 1].flui.RGO < 1e7) {
        rs = celula[i - 1].flui.RS(celula[i - 1].pres, celula[i - 1].temp);
        bo = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp, rs);
        ba = celula[i - 1].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        rs = rs * 6.29 / 35.31467;
    } else {
        bo = 1;
        rs = 0;
        ba = 0.;
    }
    // BSW in-situ da celula anterior, na marcha, a i-esima celula
    celula[i - 1].FW = celula[i - 1].flui.BSW * ba / (bo + ba * celula[i - 1].flui.BSW - celula[i - 1].flui.BSW * bo);
    celula[i - 1].FWini = celula[i - 1].FW;
    // Definicao das fracoes volumetricas:
    if (fabs(celula[i].QG + celula[i].QL) < (*vg1dSP).localtiny) {
        if (arq.tipoFluido == 1) {
            celula[i].alf = 0.;
        } else {
            celula[i].alf = 1.;
        }
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else if (fabs(celula[i].QG) < (*vg1dSP).localtiny) { // caso so exista liquido:
        celula[i].alf = 0.;
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else if (fabs(celula[i].QL) < (*vg1dSP).localtiny * 1e-6) { // caso so exista gas:
        celula[i].alf = 1.;
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        celula[i].c0 = 1.;
        celula[i].ud = 0.;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else { // caso bifasico
        double c0 = 1.;
        double ud = 0.;
        if (fabs(celula[i].QL) > (*vg1dSP).localtiny * 1e-6) {
            if (iterperm == 0) { // primeira estimativa, primeira iteracao
                // utiliza-se a fracao de vazio sem escorregamento, pois a propria correlacao para se obter a
                // fracao de vazio depende do valor da fracao de vazio
                if ((fabs(celula[i].QG) + fabs(celula[i].QL)) > (*vg1dSP).localtiny) {
                    celula[i].alf = fabs(celula[i].QG) /
                                    (fabs(celula[i].QG) + fabs(celula[i].QL));
                    CalcC0UdPerm(i, c0, ud);
                } else
                    celula[i].alf = 0.;
                celula[i].alfini = celula[i].alf;
                celula[i - 1].alfR = celula[i].alf;
                celula[i - 1].alfRini = celula[i].alf;
                if (i < ncel)
                    celula[i + 1].alfL = celula[i].alf;
                if (i < ncel)
                    celula[i + 1].alfLini = celula[i].alf;
                celula[i].alfPigD = celula[i].alf;
                celula[i].alfPigDini = celula[i].alf;
                celula[i].alfPigE = celula[i].alf;
                celula[i].alfPigEini = celula[i].alf;
            }
            if (fabs(rhog) / rhol > 0.9) {
                c0 = 1.;
                ud = 0.;
            }

            // para o caso permanente, a fracao de vazio Ã© obtida a partir das relacoes de escorregamento
            // portanto, e neste ponto que se obtem Co e Ud:
            else if (fabs(celula[i].QG) > (*vg1dSP).localtiny && fabs(celula[i].QL) > (*vg1dSP).localtiny * 1e-6)
                CalcC0UdPerm(i, c0, ud);
            celula[i].c0 = c0;
            celula[i].ud = ud;
            double area = celula[i].duto.area;
            if (fabs(celula[i].QG + celula[i].QL) > (*vg1dSP).localtiny) {
                // alfa com escorregamento:
                celula[i].alf = celula[i].QG / (c0 * (celula[i].QG + celula[i].QL) + ud * area);
                double alfHomo = celula[i].QG / (celula[i].QG + celula[i].QL);
                if (celula[i].alf > 1. - 1e-15 || celula[i].alf < 1e-15)
                    celula[i].alf = alfHomo;

            } else
                celula[i].alf = 0.;
            if (celula[i].alf > (1 - (*vg1dSP).localtiny))
                celula[i].alf = celula[i].QG / (celula[i].QG + celula[i].QL);
        } else {
            c0 = 1.;
            ud = 0.;
            celula[i].alf = 1.;
        }
        if (celula[i].alf < 0.)
            celula[i].alf = 0.;
        else if (celula[i].alf > 1.)
            celula[i].alf = 1.;
        // atualizacoes dos valores das fracoes volumetricas da celula i armazendadas em
        // outras celulas, e inclusiove armazendo os valores para "tempo anterior", que nao
        // sao relevantes para o problema permanente mas importantes se o resultado permanente
        // der partida na solucao transiente:
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    }

    if (fabs(celula[i].QL) > 1e-15 && fabs(celula[i].MComp) > 1e-15) {
        double hol0 = 1. - celula[i - 1].alf;
        double hol1 = 1. - celula[i].alf;
        celula[i].fluicol.TR = (celula[i - 1].fluicol.TR * celula[i - 1].MComp + celula[i - 1].fontemassCR * trF) / celula[i].MComp;
        celula[i].fluicol.TR = celula[i].fluicol.TR + (0.5 * celula[i - 1].duto.area * celula[i - 1].dx * hol0 +
                                                       0.5 * celula[i].duto.area * celula[i].dx * hol1) /
                                                          celula[i].QL;
    } else
        celula[i].fluicol.TR = 0.;
}

void SProd::RenovaMassPermComp(int i) {
    int mudaRGO = 1;
    double titF = 0.;
    ProFlu fluF;

    double boF = 1.;
    double baF = 1.;
    double fwF = 1.;
    double rhoOF = 900.;
    double rhoWF = 1000.;

    double mHidro = 0.;
    double mComp;
    double trF = 0.;

    if (celula[i - 1].acsr.tipo == 1) {
        if (i > 0 && (celula[i - 1].flui.dCalculatedBeta > 0. && celula[i - 1].flui.dCalculatedBeta < 1.))
            celula[i - 1].acsr.injg.FluidoPro.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, celula[i - 1].flui.dCalculatedBeta,
                                                               celula[i - 1].flui.oCalculatedLiqComposition, celula[i - 1].flui.oCalculatedVapComposition, arq.pocinjec);
        else
            celula[i - 1].acsr.injg.FluidoPro.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, -1, NULL, NULL, arq.pocinjec);
        fluF = celula[i - 1].acsr.injg.FluidoPro;
        fwF = 0.;
        titF = 1.;
        trF = celula[i - 1].acsr.injg.fluidocol.TR;
    } else if (celula[i - 1].acsr.tipo == 2) {
        if (i > 0 && (celula[i - 1].flui.dCalculatedBeta > 0. && celula[i - 1].flui.dCalculatedBeta < 1.))
            celula[i - 1].acsr.injl.FluidoPro.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, celula[i - 1].flui.dCalculatedBeta,
                                                               celula[i - 1].flui.oCalculatedLiqComposition, celula[i - 1].flui.oCalculatedVapComposition, arq.pocinjec);
        else
            celula[i - 1].acsr.injl.FluidoPro.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, -1, NULL, NULL, arq.pocinjec);
        fluF = celula[i - 1].acsr.injl.FluidoPro;
        boF = fluF.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
        baF = fluF.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
        rhoOF = fluF.MasEspoleo(celula[i - 1].pres, celula[i - 1].temp);
        rhoWF = fluF.MasEspAgua(celula[i - 1].pres, celula[i - 1].temp);
        titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        trF = celula[i - 1].acsr.injl.fluidocol.TR;
    } else if (celula[i - 1].acsr.tipo == 3) {
        if (i > 0 && (celula[i - 1].flui.dCalculatedBeta > 0. && celula[i - 1].flui.dCalculatedBeta < 1.))
            celula[i - 1].acsr.ipr.FluidoPro.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, celula[i - 1].flui.dCalculatedBeta,
                                                              celula[i - 1].flui.oCalculatedLiqComposition, celula[i - 1].flui.oCalculatedVapComposition, arq.pocinjec);
        else
            celula[i - 1].acsr.ipr.FluidoPro.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, -1, NULL, NULL, arq.pocinjec);
        fluF = celula[i - 1].acsr.ipr.FluidoPro;
        boF = fluF.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
        baF = fluF.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
        rhoOF = fluF.MasEspoleo(celula[i - 1].pres, celula[i - 1].temp);
        rhoWF = fluF.MasEspAgua(celula[i - 1].pres, celula[i - 1].temp);
        titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        trF = 0.;
    } else if (celula[i - 1].acsr.tipo == 10) {
        if (i > 0 && (celula[i - 1].flui.dCalculatedBeta > 0. && celula[i - 1].flui.dCalculatedBeta < 1.))
            celula[i - 1].acsr.injm.FluidoPro.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, celula[i - 1].flui.dCalculatedBeta,
                                                               celula[i - 1].flui.oCalculatedLiqComposition, celula[i - 1].flui.oCalculatedVapComposition, arq.pocinjec);
        else
            celula[i - 1].acsr.injm.FluidoPro.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, -1, NULL, NULL, arq.pocinjec);
        fluF = celula[i - 1].acsr.injm.FluidoPro;
        boF = fluF.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
        baF = fluF.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
        rhoOF = fluF.MasEspoleo(celula[i - 1].pres, celula[i - 1].temp);
        rhoWF = fluF.MasEspAgua(celula[i - 1].pres, celula[i - 1].temp);
        titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        trF = celula[i - 1].acsr.injm.fluidocol.TR;
    } else if (celula[i - 1].acsr.tipo == 9 && celula[i - 1].acsr.fontechk.abertura > 1e-6) {

        if (i > 0 && (celula[i - 1].flui.dCalculatedBeta > 0. && celula[i - 1].flui.dCalculatedBeta < 1.))
            celula[i - 1].acsr.fontechk.fluidoP.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, celula[i - 1].flui.dCalculatedBeta,
                                                                 celula[i - 1].flui.oCalculatedLiqComposition, celula[i - 1].flui.oCalculatedVapComposition, arq.pocinjec);
        else
            celula[i - 1].acsr.fontechk.fluidoP.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, -1, NULL, NULL, arq.pocinjec);
        if (i > 0 && (celula[i - 1].flui.dCalculatedBeta > 0. && celula[i - 1].flui.dCalculatedBeta < 1.))
            celula[i - 1].acsr.fontechk.fluidoPamb.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, celula[i - 1].flui.dCalculatedBeta,
                                                                    celula[i - 1].flui.oCalculatedLiqComposition, celula[i - 1].flui.oCalculatedVapComposition, arq.pocinjec);
        else
            celula[i - 1].acsr.fontechk.fluidoPamb.atualizaPropComp(celula[i - 1].pres, celula[i - 1].temp, -1, NULL, NULL, arq.pocinjec);
        if (celula[i - 1].acsr.fontechk.presT > celula[i - 1].acsr.fontechk.pamb) {
            fluF = celula[i - 1].acsr.fontechk.fluidoP;
        } else {
            fluF = celula[i - 1].acsr.fontechk.fluidoPamb;
        }
        boF = fluF.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
        baF = fluF.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
        rhoOF = fluF.MasEspoleo(celula[i - 1].pres, celula[i - 1].temp);
        rhoWF = fluF.MasEspAgua(celula[i - 1].pres, celula[i - 1].temp);
        titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
    } else if (celula[i - 1].acsr.tipo == 15) {
        double tRes = celula[i - 1].acsr.radialPoro.tRes;
        if (i > 0 && (celula[i - 1].flui.dCalculatedBeta > 0. && celula[i - 1].flui.dCalculatedBeta < 1.))
            celula[i - 1].acsr.radialPoro.flup.atualizaPropComp(celula[i - 1].pres, tRes,
                                                                celula[i - 1].flui.dCalculatedBeta,
                                                                celula[i - 1].flui.oCalculatedLiqComposition, celula[i - 1].flui.oCalculatedVapComposition, arq.pocinjec);
        else
            celula[i - 1].acsr.radialPoro.flup.atualizaPropComp(celula[i - 1].pres, tRes, -1, NULL, NULL, arq.pocinjec);
        fluF = celula[i - 1].acsr.radialPoro.flup;
        boF = fluF.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
        baF = fluF.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
        rhoOF = fluF.MasEspoleo(celula[i - 1].pres, celula[i - 1].temp);
        rhoWF = fluF.MasEspAgua(celula[i - 1].pres, celula[i - 1].temp);
        titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        trF = 0.;
    } else if (celula[i - 1].acsr.tipo == 16) {
        double tRes = celula[i - 1].acsr.poroso2D.dados.transfer.tRes;
        if (i > 0 && (celula[i - 1].flui.dCalculatedBeta > 0. && celula[i - 1].flui.dCalculatedBeta < 1.))
            celula[i - 1].acsr.poroso2D.dados.flup.atualizaPropComp(celula[i - 1].pres, tRes,
                                                                    celula[i - 1].flui.dCalculatedBeta,
                                                                    celula[i - 1].flui.oCalculatedLiqComposition, celula[i - 1].flui.oCalculatedVapComposition, arq.pocinjec);
        else
            celula[i - 1].acsr.poroso2D.dados.flup.atualizaPropComp(celula[i - 1].pres, tRes, -1, NULL, NULL, arq.pocinjec);
        fluF = celula[i - 1].acsr.poroso2D.dados.flup;
        boF = fluF.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
        baF = fluF.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        fwF = fluF.BSW * baF / (boF + baF * fluF.BSW - fluF.BSW * boF);
        rhoOF = fluF.MasEspoleo(celula[i - 1].pres, celula[i - 1].temp);
        rhoWF = fluF.MasEspAgua(celula[i - 1].pres, celula[i - 1].temp);
        titF = (1 - fwF) * rhoOF / ((1 - fwF) * rhoOF + fwF * rhoWF);
        trF = 0.;
    }

    renovaFonte(i - 1); // metodo que verifica se existe uma fonte na celula e calcula o valor das
    // vazoes massicas de liquido produzido (oleo+agua), gas e liquido complementar
    // relacao entre fonte a esquerda e a direita de uma celula:
    celula[i].fontemassLL = celula[i - 1].fontemassLR;
    celula[i].fontemassCL = celula[i - 1].fontemassCR;
    celula[i].fontemassGL = celula[i - 1].fontemassGR;
    mComp = celula[i].MComp = celula[i - 1].MComp + celula[i - 1].fontemassCR;
    celula[i].MC = celula[i - 1].MC + celula[i - 1].fontemassLR + celula[i - 1].fontemassCR + celula[i - 1].fontemassGR;
    if (i == 0) {
        if (celula[0].acsr.tipo == 15)
            celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
        else if (celula[0].acsr.tipo == 16)
            celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
    }

    double fonteMasLiqL = celula[i].fontemassLL;
    double fonteMasGasL = celula[i].fontemassGL;
    double vazMasLiqL = celula[i].MliqiniL;
    double vazMasGasL = celula[i].ML - celula[i].MliqiniL;

    // definicao de temperaturas maximas e minimas para uma eventual reavaliacao do metodo
    // ASTM quando ocorre mistura de fluidos e se deseja atualizar o modelo
    // de viscosidade de oleo morto se este for o ASTM que trabalha com um par de temperatura
    double tL = 0;
    double tH = 70.;

    double bo;
    double ba;
    double rs;
    double titV = 0.;
    // calcula os valores de RS, Bo e Ba na celula anterior a i-esima celula
    if (celula[i - 1].flui.RGO < 1e7) {
        rs = celula[i - 1].flui.RS(celula[i - 1].pres, celula[i - 1].temp);
        bo = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp, rs);
        ba = celula[i - 1].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        rs = rs * 6.29 / 35.31467;
    } else {
        bo = 1;
        rs = 0;
        ba = 0.;
    }
    // BSW in-situ da celula anterior, na marcha, a i-esima celula
    celula[i - 1].FW = celula[i - 1].flui.BSW * ba / (bo + ba * celula[i - 1].flui.BSW - celula[i - 1].flui.BSW * bo);
    celula[i - 1].FWini = celula[i - 1].FW;
    double tmed;
    // temperatura na fronteira entre a celula i-1 e a celula i, da segunda iteracao em diante, pode-se
    // usar a temperatura da celula i, pois jÃ¡ a tem calculada, mas isto pode ser um complicador de
    // convergencia, mais seguro manter o criterio em todas as iteracoes, neste caso, a temperatura na
    // fronteira e admitida = a temperatura da celula i-1
    if (iterperm != 0 && arq.AceleraConvergPerm == 0)
        tmed = (celula[i].dx * celula[i].temp + celula[i].dxL * celula[i].tempL) / (celula[i].dx + celula[i].dxL);
    else
        tmed = celula[i - 1].temp;
    // primeiro teste: nÃ£o hÃ¡ fontes na celula i-1:
    if (celula[i - 1].acsr.tipo != 1 && celula[i - 1].acsr.tipo != 2 && celula[i - 1].acsr.tipo != 3 && celula[i - 1].acsr.tipo != 10 && (celula[i - 1].acsr.tipo != 9 || (celula[i - 1].acsr.tipo == 9 && celula[i - 1].acsr.fontechk.abertura <= 1e-6)) &&
        celula[i - 1].acsr.tipo != 15 && celula[i - 1].acsr.tipo != 16) {
        // neste caso, variaveis como RGO de separador, BSW, API, densidade de gas e outras nÃ£o muda, sao iguais
        // aos valores da celula i-1
        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // o valor de volume de leve Ã© atualizado neste ponto,
        // seguindo o equacionamento mostrado em relatorio, nÃ£o Ã© relevante para o permanente, mas
        // deve ser calculado, pois Ã© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;

        celula[i].flui.BSW = celula[i - 1].flui.BSW;

        double fwV = celula[i - 1].FW;
        double rhoOV = celula[i - 1].flui.MasEspoleo(celula[i - 1].pres, celula[i - 1].temp);
        double rhoWV = celula[i - 1].flui.MasEspAgua(celula[i - 1].pres, celula[i - 1].temp);
        titV = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);

        for (int j = 0; j < celula[i].flui.npseudo; j++)
            celula[i].flui.fracMol[j] = celula[i - 1].flui.fracMol[j];
        if (i > 1) {
            celula[i].flui.iCalculatedStockTankThermodynamicCondition = celula[i - 1].flui.iCalculatedStockTankThermodynamicCondition;
            celula[i].flui.dStockTankVaporMassFraction = celula[i - 1].flui.dStockTankVaporMassFraction;
            celula[i].flui.dStockTankLiquidDensity = celula[i - 1].flui.dStockTankLiquidDensity;
            celula[i].flui.dStockTankVaporDensity = celula[i - 1].flui.dStockTankVaporDensity;

            if (celula[i].flui.dStockTankLiquidDensity > 0.01) {
                celula[i].flui.API = 141.5 / (celula[i].flui.dStockTankLiquidDensity / 1000.) - 131.5;
            } else
                celula[i].flui.API = 50;
            celula[i].flui.Deng = celula[i].flui.dStockTankVaporDensity / 1.225;
            celula[i].flui.RGO = celula[i - 1].flui.RGO;
            celula[i].flui.IRGO = celula[i - 1].flui.IRGO;
        } else {
            celula[i].flui.atualizaPropCompStandard();
        }

        if (arq.tipoFluido == 0) { // reavaliacao da fracao volumetrica do liquido complementar
            // mesmo que nÃ£o tenha fonte, ela pode mudar, devido ao encolhimento do liquido produzido
            double boI;
            double baI;
            if (celula[i].flui.RGO < 1e6)
                boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            else
                boI = 1.;
            baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            double fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qo = celula[i - 1].QL * (1 - celula[i - 1].FW) * (1 - celula[i - 1].bet) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny * 1e-5))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet);
            double qc;
            qc = celula[i - 1].QL * (celula[i - 1].bet) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);

            ////////////////////////esperar//////////////////////////////////////
            if (fabs(qo + qw + qc) > 1e-15)
                celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
            else
                celula[i].bet = celula[i - 1].bet;
        } else {
            mHidro = celula[i].MC - mComp;
        }
    } else {
        double fwV = celula[i - 1].FW;
        double rhoOV = celula[i - 1].flui.MasEspoleo(celula[i - 1].pres, celula[i - 1].temp);
        double rhoWV = celula[i - 1].flui.MasEspAgua(celula[i - 1].pres, celula[i - 1].temp);
        titV = (1 - fwV) * rhoOV / ((1 - fwV) * rhoOV + fwV * rhoWV);
        vazMasLiqL *= titV;
        fonteMasLiqL *= titF;
        if (arq.tabelaDinamica == 0) {
            double pesoMolV = 0;
            double pesoMolF = 0;
            for (int j = 0; j < celula[i].flui.npseudo; j++) {
                pesoMolV += celula[i - 1].flui.masMol[j] * celula[i - 1].flui.fracMol[j];
                pesoMolF += fluF.masMol[j] * fluF.fracMol[j];
            }
            double vazMolV = (vazMasLiqL + vazMasGasL) / pesoMolV;
            double vazMolF = (fonteMasLiqL + fonteMasGasL) / pesoMolF;
            double razMolV = 1.;
            if (fabs(vazMolV + vazMolF) > 1e-15)
                razMolV = vazMolV / (vazMolV + vazMolF);
            if (vazMolF > 0.) {
                for (int j = 0; j < celula[i].flui.npseudo; j++) {
                    celula[i].flui.fracMol[j] = razMolV * celula[i - 1].flui.fracMol[j] +
                                                (1. - razMolV) * fluF.fracMol[j];
                }
            } else {
                for (int j = 0; j < celula[i].flui.npseudo; j++)
                    celula[i].flui.fracMol[j] = celula[i - 1].flui.fracMol[j];
            }
            celula[i].flui.Pmol = 0.;
            for (int j = 0; j < celula[i].flui.npseudo; j++)
                celula[i].flui.Pmol += celula[i].flui.fracMol[j] * celula[i].flui.masMol[j];

            celula[i].flui.atualizaPropCompStandard();
        }

        double api = celula[i - 1].flui.API;
        double rholis = celula[i - 1].flui.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
        double rholisF;
        double boinjl;
        double bainjl;
        double fwinjl;
        if (fabs(fonteMasLiqL + fonteMasGasL) > 1e-15) {
            rholisF = fluF.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            boinjl = fluF.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            bainjl = fluF.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            fwinjl = fluF.BSW * bainjl / (boinjl + bainjl * fluF.BSW - fluF.BSW * boinjl);
        } else {
            rholisF = rholis;
            boinjl = bo;
            bainjl = ba;
            fwinjl = celula[i - 1].FW;
        }
        // vazao de oleo sytandard antes da fonte:
        double qostd1;
        if (celula[i - 1].flui.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd1 = celula[i - 1].Mliqini * (1. - celula[i - 1].FW) * (1. - celula[i - 1].bet) / (bo * rholis);
        else
            qostd1 = 0.;

        // vazao de oleo standard da fonte:
        double qostd2;
        if (fluF.dStockTankVaporMassFraction < 1. - 1e-15)
            qostd2 = celula[i - 1].fontemassLR * (1. - fwinjl) / (boinjl * rholisF);
        else
            qostd2 = 0.;

        double hol = 1 - celula[i - 1].alf;
        double bet = celula[i - 1].bet;
        double bsw = celula[i - 1].FW;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp);
        double rhogST = celula[i - 1].flui.Deng * 1.225;
        // calculo do volume de leve na celula i, seguindo o equacionamento mostrado em relatorio,
        // nÃ£o Ã© relevante para o permanente, mas deve ser calculado,
        // pois Ã© utilizado como entrada no transiente
        celula[i - 1].VolLeveST = (((1 - hol) * rhog / rhogST) + hol * (1 - bet) * (1. - bsw) * rs / bo);
        if (celula[i - 1].VolLeveST < 1e-15)
            celula[i - 1].VolLeveST = 0.;
        if (arq.trackRGO == -1) {
            // esta chave nÃ£o Ã© utilizada, Ã© mantida aqui como reserva, atualmente este
            // calculo nÃ£o esta funcionando, lembrando que arq.trackRGO assume apenas 2 valores, 0 ou 1
            if (hol > (*vg1dSP).localtiny && bet < (1. - (*vg1dSP).localtiny) && bsw < (1. - (*vg1dSP).localtiny) && mudaRGO == 1)
                celula[i].flui.RGO = celula[i - 1].VolLeveST * bo / (hol * (1 - bet) * (1 - bsw));
            if (celula[i].flui.RGO > (*vg1dSP).RGOMax && mudaRGO == 1 && celula[i].flui.RGO < 1e6)
                celula[i].flui.RGO = (*vg1dSP).RGOMax;
        }

        double qw1;
        if ((1. - celula[i - 1].flui.BSW) > 0)
            qw1 = qostd1 * celula[i - 1].flui.BSW / (1. - celula[i - 1].flui.BSW);
        else
            qw1 = celula[i - 1].Mliqini * celula[i - 1].FW * (1. - celula[i - 1].bet) / (1000. * celula[i - 1].flui.Denag);
        double qw2;
        if ((1. - fluF.BSW) > 0)
            qw2 = qostd2 * fluF.BSW / (1. - fluF.BSW);
        else
            qw2 = celula[i - 1].fontemassLR * fwinjl / (1000. * fluF.Denag);

        if (fabs(qw1 + qw2 + qostd1 + qostd2) > 1e-15 && fabs(qw2 + qostd2) > 1e-15 && celula[i - 1].fontemassLR > 1e-15)
            celula[i].flui.BSW = (qw1 + qw2) / (qw1 + qw2 + qostd1 + qostd2);
        else
            celula[i].flui.BSW = celula[i - 1].flui.BSW;

        if (fabs(qw1 + qw2) > 1e-15 && celula[i - 1].fontemassLR > 1e-15)
            celula[i].flui.Denag = (qw1 * celula[i - 1].flui.Denag +
                                    qw2 * fluF.Denag) /
                                   (qw1 + qw2);
        else
            celula[i].flui.Denag = celula[i - 1].flui.Denag;

        if (fabs(qostd1 + qostd2) > 1e-15 && fabs(qostd2) > 1e-15 && celula[i - 1].fontemassLR > 1e-15) { // vazao de liquido >0
            celula[i].flui.TempL = tL;
            celula[i].flui.TempH = tH;
            celula[i].flui.LVisL = (qostd1 * celula[i - 1].flui.VisOM(tL) + qostd2 * fluF.VisOM(tL)) / (qostd1 + qostd2);
            celula[i].flui.LVisH = (qostd1 * celula[i - 1].flui.VisOM(tH) + qostd2 * fluF.VisOM(tH)) / (qostd1 + qostd2);
        } else {
            celula[i].flui.TempL = celula[i - 1].flui.TempL;
            celula[i].flui.TempH = celula[i - 1].flui.TempH;
            celula[i].flui.LVisL = celula[i - 1].flui.LVisL;
            celula[i].flui.LVisH = celula[i - 1].flui.LVisH;
        }

        if (arq.tipoFluido == 0) {
            // reavaliacao da fracao volumetrica do liquido complementar
            // observar que a fonte de liquido pode ter uma fracao de liquido complementar
            // distinta da onservada a esquerda da celula i-1
            double boN = celula[i].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
            double baN = celula[i].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
            double fwN = celula[i].flui.BSW * baN /
                         (boN + baN * celula[i].flui.BSW - celula[i].flui.BSW * boN);
            double boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
            double baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
            double fwI = celula[i].flui.BSW * baI /
                         (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
            double qlpF = celula[i - 1].fontemassLR / fluF.MasEspLiq(celula[i - 1].pres, celula[i - 1].temp);
            double qlcF = celula[i - 1].fontemassCR / celula[i].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp);
            double betN = (celula[i - 1].QL * celula[i - 1].bet + qlcF) /
                          (celula[i - 1].QL + qlpF + qlcF);
            double qo = (celula[i - 1].QL + qlpF + qlcF) * (1 - fwN) * (1 - betN) * boI / bo;
            double qw;
            if (fwI < (1 - (*vg1dSP).localtiny * 1e-5))
                qw = fwI * qo / (1 - fwI);
            else
                qw = celula[i - 1].QL * (1 - celula[i - 1].bet) + qlpF;
            double qc = (celula[i - 1].QL * (celula[i - 1].bet) + qlcF) * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);
            if (fabs(qo + qw + qc) > 1e-15)
                celula[i].bet = fabs(qc) / (fabs(qo) + fabs(qw) + fabs(qc));
            else
                celula[i].bet = celula[i - 1].bet;
        } else {
            mHidro = celula[i].MC - mComp;
        }
    }
    // atualizaÃ§Ã£o da vazao massica da mistura:
    celula[i].MC = celula[i - 1].MC + celula[i - 1].fontemassCR + celula[i - 1].fontemassLR + celula[i - 1].fontemassGR;
    celula[i - 1].MR = celula[i].MC;
    if (i < ncel)
        celula[i + 1].ML = celula[i].MC;
    celula[i - 1].MRini = celula[i - 1].MR;

    double pmed = celula[i].presaux + celula[i - 1].dpB / 98066.5;
    double rhog;
    double rhol;
    // calculo da vazao massica de liquido e das vazoes volumetricas:

    if (((iterperm == 0 && i <= 1 && buscaIni == 0) && ((*vg1dSP).chaverede == 0 || (*vg1dSP).iterRede == 1)) && arq.tabelaDinamica == 0) {
        celula[i].flui.atualizaPropComp(pmed, tmed, -1, NULL, NULL, arq.pocinjec);
    } else if (arq.tabelaDinamica == 0) {
        if ((iterperm == 0 && buscaIni == 0) && ((*vg1dSP).chaverede == 0 || (*vg1dSP).iterRede == 1)) {
            int veriI = i - 1;
            while ((celula[veriI].flui.dCalculatedBeta > 1 - (0.0 + 1e-15) || celula[veriI].flui.dCalculatedBeta < (0.0 + 1e-15)) && (veriI > 0))
                veriI--;
            if ((veriI == 0) && (celula[veriI].flui.dCalculatedBeta > 1 - (0.0 + 1e-15) || celula[veriI].flui.dCalculatedBeta < (0.0 + 1e-15)))
                celula[i].flui.atualizaPropComp(pmed, tmed, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].flui.atualizaPropComp(pmed, tmed, celula[veriI].flui.dCalculatedBeta, celula[veriI].flui.oCalculatedLiqComposition,
                                                celula[veriI].flui.oCalculatedVapComposition, arq.pocinjec);
        } else {
            int veriI;
            int veriIinf = i;
            while ((celula[veriIinf].flui.dCalculatedBeta > 1 - 1e-15 ||
                    celula[veriIinf].flui.dCalculatedBeta < 1e-15) &&
                   veriIinf > 0)
                veriIinf--;
            int veriIsup = i;
            while ((celula[veriIsup].flui.dCalculatedBeta > 1 - 1e-15 ||
                    celula[veriIsup].flui.dCalculatedBeta < 1e-15) &&
                   veriIsup < ncel)
                veriIsup++;
            if ((celula[veriIinf].flui.dCalculatedBeta > 1 - 1e-15 ||
                 celula[veriIinf].flui.dCalculatedBeta < 1e-15) &&
                (celula[veriIsup].flui.dCalculatedBeta < 1. &&
                 celula[veriIsup].flui.dCalculatedBeta > 0.))
                veriI = veriIsup;
            else if ((celula[veriIsup].flui.dCalculatedBeta > 1 - 1e-15 || celula[veriIsup].flui.dCalculatedBeta < 1e-15) &&
                     (celula[veriIinf].flui.dCalculatedBeta < 1. && celula[veriIinf].flui.dCalculatedBeta > 0.))
                veriI = veriIinf;
            else if (fabs(i - veriIsup) < fabs(i - veriIinf))
                veriI = veriIsup;
            else
                veriI = veriIinf;
            if ((celula[veriI].flui.dCalculatedBeta > 1 - (0.0 + 1e-15) || celula[veriI].flui.dCalculatedBeta < (0.0 + 1e-15)))
                celula[i].flui.atualizaPropComp(pmed, tmed, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].flui.atualizaPropComp(pmed, tmed, celula[veriI].flui.dCalculatedBeta, celula[veriI].flui.oCalculatedLiqComposition,
                                                celula[veriI].flui.oCalculatedVapComposition, arq.pocinjec);
        }
        if (celula[i].flui.iIER != 0) {
            int para;
            para = 0;
        }
    }
    double titulo = celula[i].flui.dVaporMassFraction;

    double betI;
    double fwI;
    ProFlu fluI;
    ProFluCol fluCI;
    if (celula[i].MC >= 0 && i > 0) {
        betI = celula[i].betL;
        fluI = celula[i - 1].flui;
        fluCI = celula[i - 1].fluicol;
    } else {
        betI = celula[i].bet;
        fluI = celula[i].flui;
        fluCI = celula[i].fluicol;
    }
    if (arq.tipoFluido == 0) {
        rs = celula[i].flui.RS(pmed, tmed);
        bo = celula[i].flui.BOFunc(pmed, tmed, rs);
        rs = rs * 6.29 / 35.31467;
        ba = celula[i].flui.BAFunc(pmed, tmed);
        double rhogstd = celula[i].flui.Deng * 1.225;
        double rhololeostd = 1000. * 141.5 / (131.5 + celula[i].flui.API);
        double rhoa = celula[i].flui.Denag * 1000.;

        if (titulo < 1. - 1e-15)
            celula[i].FW = celula[i].flui.BSW * ba / (bo + ba * celula[i].flui.BSW - celula[i].flui.BSW * bo);
        else
            celula[i].FW = 1.;
        double ro = celula[i].flui.MasEspoleo(pmed, tmed);
        double ra = celula[i].flui.MasEspAgua(pmed, tmed);
        double rc = celula[i].fluicol.MasEspFlu(pmed, tmed);
        double denom = rc * celula[i].bet + (1 - celula[i].bet) * celula[i].FW * ra;
        if (titulo < 1. - 1e-15) {
            denom += (1 - celula[i].bet) * (1. - celula[i].FW) * ro / (1. - titulo);
            celula[i].QL = celula[i].MC / denom;

            celula[i].Mliqini = celula[i].QL * (celula[i].bet * rc + (1 - celula[i].bet) * (celula[i].FW * ra +
                                                                                            (1. - celula[i].FW) * ro));
        } else if (celula[i].flui.BSW > 1.e-15 || celula[i - 1].betI > 1.e-15) {
            celula[i].Mliqini = celula[i - 1].QL * celula[i - 1].rpCi * (1. - celula[i - 1].betI) * (1. - titV) +
                                celula[i - 1].fontemassLR * (1. - titF) +
                                celula[i - 1].QL * celula[i - 1].betI * celula[i - 1].rcCi + celula[i - 1].fontemassCR;
            celula[i].QL = celula[i].Mliqini / denom;
        } else {
            celula[i].Mliqini = 0.;
            celula[i].QL = 0.;
        }
    } else {
        double ro = celula[i].flui.MasEspoleo(pmed, tmed);
        double rc = celula[i].fluicol.MasEspFlu(pmed, tmed);
        double qo = mHidro * (1. - titulo) / ro;
        double qc = mComp / rc;
        if (fabs(qo + qc) > 1e-15)
            celula[i].bet = qc / (qo + qc);
        else
            celula[i].bet = celula[i - 1].bet;
        double bet = celula[i].bet;
        double denom;
        if (titulo < 1. - 1e-15) {
            denom = rc * bet + (1. - bet) * ro;
            celula[i].QL = (mHidro * (1. - titulo) + mComp) / denom;
            celula[i].Mliqini = celula[i].QL * (celula[i].bet * rc + (1 - celula[i].bet) * ro);
        } else if (bet > (1 - 1e-15)) {
            celula[i].Mliqini = celula[i - 1].QL * celula[i - 1].betI * celula[i - 1].rcCi + celula[i - 1].fontemassCR;
            celula[i].QL = celula[i].Mliqini / (rc * bet + (1. - bet) * ro);
        } else {
            celula[i].Mliqini = 0.;
            celula[i].QL = 0.;
        }
    }

    celula[i - 1].MliqiniR = celula[i].Mliqini;
    if (i < ncel)
        celula[i + 1].MliqiniL = celula[i].Mliqini;
    rhog = celula[i].rgCi = celula[i].flui.MasEspGas(pmed, tmed);

    // calculo das massas especificas na interface a esquerda da celula
    celula[i].rpCi = celula[i].flui.MasEspLiq(pmed, tmed);
    celula[i].rcCi = celula[i].fluicol.MasEspFlu(pmed, tmed);
    rhol = (1 - celula[i].bet) * celula[i].rpCi + celula[i].bet * celula[i].rcCi;
    // rhol = (1 - celula[i].bet) * celula[i].flui.MasEspLiq(pmed, tmed)
    // vazoes volumetricas:
    if (i < ncel)
        celula[i + 1].QLL = celula[i].QL;
    celula[i - 1].QLR = celula[i].QL;
    celula[i].QG = (celula[i].MC - celula[i].Mliqini) / rhog;
    // Definicao das fracoes volumetricas:
    if (fabs(celula[i].QG + celula[i].QL) < (*vg1dSP).localtiny * 1e-5) {
        if (arq.tipoFluido == 1) {
            celula[i].alf = 0.;
        } else {
            celula[i].alf = 1.;
        }
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else if (fabs(celula[i].QG) < (*vg1dSP).localtiny * 1e-5) { // caso so exista liquido:
        celula[i].alf = 0.;
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else if (fabs(celula[i].QL) < (*vg1dSP).localtiny * 1e-5) { // caso so exista gas:
        celula[i].alf = 1.;
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        celula[i].c0 = 1.;
        celula[i].ud = 0.;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else { // caso bifasico
        double c0 = 1.;
        double ud = 0.;
        if (fabs(celula[i].QL) > (*vg1dSP).localtiny * 1e-5) {
            if (iterperm == 0) { // primeira estimativa, primeira iteracao
                // utiliza-se a fracao de vazio sem escorregamento, pois a propria correlacao para se obter a
                // fracao de vazio depende do valor da fracao de vazio
                if ((fabs(celula[i].QG) + fabs(celula[i].QL)) > (*vg1dSP).localtiny) {
                    if (monitConvPerm > 0.01)
                        celula[i].alf = fabs(celula[i].QG) /
                                        (fabs(celula[i].QG) + fabs(celula[i].QL));
                    CalcC0UdPerm(i, c0, ud);
                } else
                    celula[i].alf = 0.;
                celula[i].alfini = celula[i].alf;
                celula[i - 1].alfR = celula[i].alf;
                celula[i - 1].alfRini = celula[i].alf;
                if (i < ncel)
                    celula[i + 1].alfL = celula[i].alf;
                if (i < ncel)
                    celula[i + 1].alfLini = celula[i].alf;
                celula[i].alfPigD = celula[i].alf;
                celula[i].alfPigDini = celula[i].alf;
                celula[i].alfPigE = celula[i].alf;
                celula[i].alfPigEini = celula[i].alf;
            }
            if (arq.tipoModeloDrift == 1) {
                if (fabs(rhog) / rhol > 0.9) {
                    c0 = 1.;
                    ud = 0.;
                }
                // para o caso permanente, a fracao de vazio Ã© obtida a partir das relacoes de escorregamento
                // portanto, e neste ponto que se obtem Co e Ud:
                else if (fabs(celula[i].QG) > (*vg1dSP).localtiny * 1e-5 && fabs(celula[i].QL) > (*vg1dSP).localtiny * 1e-5)
                    CalcC0UdPerm(i, c0, ud);
                celula[i].c0 = c0;
                celula[i].ud = ud;
                double area = celula[i].duto.area;
                if (fabs(celula[i].QG + celula[i].QL) > (*vg1dSP).localtiny * 1e-5) {
                    // alfa com escorregamento:
                    celula[i].alf = celula[i].QG / (c0 * (celula[i].QG + celula[i].QL) + ud * area);
                    double alfHomo = celula[i].QG / (celula[i].QG + celula[i].QL);
                    if (celula[i].alf > 1. - 1e-15 || celula[i].alf < 1e-15)
                        celula[i].alf = alfHomo;
                } else
                    celula[i].alf = 0.;
                if (celula[i].alf > (1 - (*vg1dSP).localtiny) && fabs(celula[i].QG + celula[i].QL) > (*vg1dSP).localtiny * 1e-5)
                    celula[i].alf = celula[i].QG / (celula[i].QG + celula[i].QL);
                else if (celula[i].alf > (1 - (*vg1dSP).localtiny))
                    celula[i].alf = 1.;
            } else {
                c0 = 1.;
                ud = 0.;
                celula[i].alf = 1.;
            }
        } else {
            double holdup;
            double frictionGrad;
            double gravityGrad;
            double totalGrad;
            double reynolds;
            unsigned char flowType;
            char *errorMsg;
            unsigned char errorFlag;
            executarCorrelacao(celula, i, 0, arq.AceleraConvergPerm,
                               celula[i - 1].correlacaoMR2,
                               holdup, frictionGrad, gravityGrad, totalGrad,
                               reynolds, flowType);
            celula[i].alf = 1. - holdup;
        }
        if (celula[i].alf < 0.)
            celula[i].alf = 0.;
        else if (celula[i].alf > 1.)
            celula[i].alf = 1.;
        // atualizacoes dos valores das fracoes volumetricas da celula i armazendadas em
        // outras celulas, e inclusiove armazendo os valores para "tempo anterior", que nao
        // sao relevantes para o problema permanente mas importantes se o resultado permanente
        // der partida na solucao transiente:
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    }
    if (fabs(celula[i].QL) > 1e-15 && fabs(celula[i].MComp) > 1e-15) {
        double dxmed = 0.5 * (celula[i - 1].dx + celula[i].dx);
        double hol0 = 1. - celula[i - 1].alf;
        double hol1 = 1. - celula[i].alf;
        celula[i].fluicol.TR = (celula[i - 1].fluicol.TR * celula[i - 1].MComp + celula[i - 1].fontemassCR * trF) / celula[i].MComp;
        celula[i].fluicol.TR = celula[i].fluicol.TR + (0.5 * celula[i - 1].duto.area * celula[i - 1].dx * hol0 +
                                                       0.5 * celula[i].duto.area * celula[i].dx * hol1) /
                                                          celula[i].QL;
    } else
        celula[i].fluicol.TR = celula[i - 1].fluicol.TR;
}

void SProd::RenovaMassPermCompRev(int i) {
    int mudaRGO = 1;
    if (arq.flashCompleto == 1)
        mudaRGO = 1;
    ProFlu fluF;
    double trF = 0.;
    if (i == 1) {
        renovaFonte(i - 1); // metodo que verifica se existe uma fonte na celula e calcula o valor das
        // vazoes massicas de liquido produzido (oleo+agua), gas e liquido complementar
        // relacao entre fonte a esquerda e a direita de uma celula:
        celula[i].fontemassLL = celula[i - 1].fontemassLR;
        celula[i].fontemassCL = celula[i - 1].fontemassCR;
        celula[i].fontemassGL = celula[i - 1].fontemassGR;

        celula[i].MC = celula[i - 1].fontemassLR + celula[i - 1].fontemassCR + celula[i - 1].fontemassGR;

        celula[i - 1].MR = celula[i].MC;
        if (i < ncel)
            celula[i + 1].ML = celula[i].MC;
        celula[i - 1].MRini = celula[i - 1].MR;
        celula[i].MComp = celula[i - 1].MComp + celula[i - 1].fontemassCR;

        if (celula[i - 1].acsr.tipo == 1) {
            celula[i - 1].acsr.injg.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            fluF = celula[i - 1].acsr.injg.FluidoPro;
            trF = celula[i - 1].acsr.injg.fluidocol.TR;
        } else if (celula[i - 1].acsr.tipo == 2) {
            celula[i - 1].acsr.injl.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            fluF = celula[i - 1].acsr.injl.FluidoPro;
            trF = celula[i - 1].acsr.injl.fluidocol.TR;
        } else if (celula[i - 1].acsr.tipo == 3) {
            celula[i - 1].acsr.ipr.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            fluF = celula[i - 1].acsr.ipr.FluidoPro;
            trF = 0.;
        } else if (celula[i - 1].acsr.tipo == 10) {
            celula[i - 1].acsr.injm.FluidoPro.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            fluF = celula[i - 1].acsr.injm.FluidoPro;
            trF = celula[i - 1].acsr.injm.fluidocol.TR;
        } else if (celula[i - 1].acsr.tipo == 9 && celula[i - 1].acsr.fontechk.abertura > 1e-6) {
            celula[i - 1].acsr.fontechk.fluidoP.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            celula[i - 1].acsr.fontechk.fluidoPamb.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            if (celula[i - 1].acsr.fontechk.presT > celula[i - 1].acsr.fontechk.pamb) {
                fluF = celula[i - 1].acsr.fontechk.fluidoP;
            } else {
                fluF = celula[i - 1].acsr.fontechk.fluidoPamb;
            }
        } else if (celula[i - 1].acsr.tipo == 15) {
            celula[i - 1].acsr.radialPoro.flup.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            fluF = celula[i - 1].acsr.radialPoro.flup;
            trF = 0.;
        } else if (celula[i - 1].acsr.tipo == 16) {
            celula[i - 1].acsr.poroso2D.dados.flup.atualizaPropComp(celula[i].pres, celula[i].temp, -1, NULL, NULL, arq.pocinjec);
            fluF = celula[i - 1].acsr.poroso2D.dados.flup;
            trF = 0.;
        }
        celula[i].flui = fluF;

    } else {
        celula[i - 1].fontemassLR = 0.;
        celula[i - 1].fontemassCR = 0.;
        celula[i - 1].fontemassGR = 0.;

        celula[i].fontemassLL = celula[i - 1].fontemassLR;
        celula[i].fontemassCL = celula[i - 1].fontemassCR;
        celula[i].fontemassGL = celula[i - 1].fontemassGR;

        celula[i].MC = celula[i - 1].MC;

        celula[i - 1].MR = celula[i].MC;
        if (i < ncel)
            celula[i + 1].ML = celula[i].MC;
        celula[i - 1].MRini = celula[i - 1].MR;

        celula[i].MComp = celula[i - 1].MComp;

        celula[i].flui = celula[i - 1].flui;
    }
    if (i == 0) {
        if (celula[0].acsr.tipo == 15)
            celula[0].flui.BSW = celula[0].acsr.radialPoro.BSW;
        else if (celula[0].acsr.tipo == 16)
            celula[0].flui.BSW = celula[0].acsr.poroso2D.dados.transfer.BSW;
    }

    double tmed;
    // temperatura na fronteira entre a celula i-1 e a celula i, da segunda iteracao em diante, pode-se
    // usar a temperatura da celula i, pois ja a tem calculada, mas isto pode ser um complicador de
    // convergencia, mais seguro manter o criterio em todas as iteracoes, neste caso, a temperatura na
    // fronteira e admitida = a temperatura da celula i-1
    if (iterperm != 0 && arq.AceleraConvergPerm == 0)
        tmed = (celula[i].dx * celula[i].temp + celula[i].dxR * celula[i].tempR) / (celula[i].dx + celula[i].dxR);
    else
        tmed = celula[i - 1].temp;

    double pmed = celula[i].presaux;

    if (((iterperm == 0 && i <= 1 && buscaIni == 0) && ((*vg1dSP).chaverede == 0 || (*vg1dSP).iterRede == 1)) && arq.tabelaDinamica == 0) {
        celula[i].flui.atualizaPropComp(pmed, tmed, -1, NULL, NULL, arq.pocinjec);
    } else if (arq.tabelaDinamica == 0) {
        if ((iterperm == 0 && buscaIni == 0) && ((*vg1dSP).chaverede == 0 || (*vg1dSP).iterRede == 1)) {
            int veriI = i - 1;
            while ((celula[veriI].flui.dCalculatedBeta > 1 - (0.0 + 1e-15) || celula[veriI].flui.dCalculatedBeta < (0.0 + 1e-15)) && (veriI > 0))
                veriI--;
            if ((veriI == 0) && (celula[veriI].flui.dCalculatedBeta > 1 - (0.0 + 1e-15) || celula[veriI].flui.dCalculatedBeta < (0.0 + 1e-15)))
                celula[i].flui.atualizaPropComp(pmed, tmed, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].flui.atualizaPropComp(pmed, tmed, celula[veriI].flui.dCalculatedBeta, celula[veriI].flui.oCalculatedLiqComposition,
                                                celula[veriI].flui.oCalculatedVapComposition, arq.pocinjec);
        } else {
            int veriI;
            int veriIinf = i;
            while ((celula[veriIinf].flui.dCalculatedBeta > 1 - 1e-15 ||
                    celula[veriIinf].flui.dCalculatedBeta < 1e-15) &&
                   veriIinf > 0)
                veriIinf--;
            int veriIsup = i;
            while ((celula[veriIsup].flui.dCalculatedBeta > 1 - 1e-15 ||
                    celula[veriIsup].flui.dCalculatedBeta < 1e-15) &&
                   veriIsup < ncel)
                veriIsup++;
            if ((celula[veriIinf].flui.dCalculatedBeta > 1 - 1e-15 ||
                 celula[veriIinf].flui.dCalculatedBeta < 1e-15) &&
                (celula[veriIsup].flui.dCalculatedBeta < 1. &&
                 celula[veriIsup].flui.dCalculatedBeta > 0.))
                veriI = veriIsup;
            else if ((celula[veriIsup].flui.dCalculatedBeta > 1 - 1e-15 || celula[veriIsup].flui.dCalculatedBeta < 1e-15) &&
                     (celula[veriIinf].flui.dCalculatedBeta < 1. && celula[veriIinf].flui.dCalculatedBeta > 0.))
                veriI = veriIinf;
            else if (fabs(i - veriIsup) < fabs(i - veriIinf))
                veriI = veriIsup;
            else
                veriI = veriIinf;
            if ((celula[veriI].flui.dCalculatedBeta > 1 - (0.0 + 1e-15) || celula[veriI].flui.dCalculatedBeta < (0.0 + 1e-15)))
                celula[i].flui.atualizaPropComp(pmed, tmed, -1, NULL, NULL, arq.pocinjec);
            else
                celula[i].flui.atualizaPropComp(pmed, tmed, celula[veriI].flui.dCalculatedBeta, celula[veriI].flui.oCalculatedLiqComposition,
                                                celula[veriI].flui.oCalculatedVapComposition, arq.pocinjec);
        }
    }

    double titulo = celula[i].flui.dVaporMassFraction;

    celula[i].rpCi = celula[i].flui.MasEspLiq(pmed, tmed);
    celula[i].rcCi = celula[i].fluicol.MasEspFlu(pmed, tmed);
    celula[i].rgCi = celula[i].flui.MasEspGas(pmed, tmed);

    double MasCarb = celula[i].MC - celula[i].MComp;
    double MasGas = MasCarb * celula[i].flui.FracMassHidra(celula[i].presaux, tmed);
    double MasLiqProd = MasCarb - MasGas;
    double boI;
    double baI;
    if (titulo < 1 - 1e-15)
        boI = celula[i].flui.BOFunc(celula[i].presaux, tmed);
    else
        boI = 1.;
    baI = celula[i].flui.BAFunc(celula[i].presaux, tmed);
    double fwI = celula[i].flui.BSW * baI / (boI + baI * celula[i].flui.BSW - celula[i].flui.BSW * boI);
    double qo = MasLiqProd * (1 - celula[i - 1].FW) / celula[i].rpCi;
    double qw;
    if (fwI < (1 - (*vg1dSP).localtiny))
        qw = fwI * qo / (1 - fwI);
    else
        qw = MasLiqProd / celula[i].flui.MasEspAgua(pmed, tmed);
    double qc;
    qc = celula[i].MComp / celula[i].rcCi;
    ////////////////////////esperar//////////////////////////////////////
    celula[i].bet = qc / (qo + qw + qc);

    celula[i].Mliqini = MasLiqProd;
    celula[i - 1].MliqiniR = celula[i].Mliqini;
    if (i < ncel)
        celula[i + 1].MliqiniL = celula[i].Mliqini;

    double rhol = (1 - celula[i].bet) * celula[i].rpCi + celula[i].bet * celula[i].rcCi;
    double rhog = celula[i].rgCi;
    // vazoes volumetricas:
    celula[i].QL = celula[i].Mliqini / rhol;
    if (i < ncel)
        celula[i + 1].QLL = celula[i].QL;
    celula[i - 1].QLR = celula[i].QL;
    celula[i].QG = (celula[i].MC - celula[i].Mliqini) / rhog;

    if (i < ncel)
        celula[i + 1].QLL = celula[i].QL;
    celula[i - 1].QLR = celula[i].QL;

    // definicao de temperaturas maximas e minimas para uma eventual reavaliacao do metodo
    // ASTM quando ocorre mistura de fluidos e se deseja atualizar o modelo
    // de viscosidade de oleo morto se este for o ASTM que trabalha com um par de temperatura
    double tL;
    if (arq.flashCompleto == 1 && (arq.tabent.tmin - 0) > (*vg1dSP).localtiny)
        tL = arq.tabent.tmin + 0.1;
    else
        tL = 0.;
    double tH;
    if (arq.flashCompleto == 1 && (70 - arq.tabent.tmax) < (*vg1dSP).localtiny)
        tH = arq.tabent.tmax - 0.1;
    else
        tH = 70.;
    double bo;
    double ba;
    double rs;
    // calcula os valores de RS, Bo e Ba na celula anterior a i-esima celula
    if (titulo < 1 - 1e-15) {
        rs = celula[i - 1].flui.RS(celula[i - 1].pres, celula[i - 1].temp);
        bo = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp, rs);
        ba = celula[i - 1].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        rs = rs * 6.29 / 35.31467;
    } else {
        bo = 1;
        rs = 0;
        ba = 0.;
    }
    // BSW in-situ da celula anterior, na marcha, a i-esima celula
    celula[i - 1].FW = celula[i - 1].flui.BSW * ba / (bo + ba * celula[i - 1].flui.BSW - celula[i - 1].flui.BSW * bo);
    celula[i - 1].FWini = celula[i - 1].FW;
    // Definicao das fracoes volumetricas:
    if (fabs(celula[i].QG + celula[i].QL) < (*vg1dSP).localtiny) {
        if (arq.tipoFluido == 1) {
            celula[i].alf = 0.;
        } else {
            celula[i].alf = 1.;
        }
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else if (fabs(celula[i].QG) < (*vg1dSP).localtiny) { // caso so exista liquido:
        celula[i].alf = 0.;
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else if (fabs(celula[i].QL) < (*vg1dSP).localtiny * 1e-6) { // caso so exista gas:
        celula[i].alf = 1.;
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        celula[i].c0 = 1.;
        celula[i].ud = 0.;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    } else { // caso bifasico
        double c0 = 1.;
        double ud = 0.;
        if (fabs(celula[i].QL) > (*vg1dSP).localtiny * 1e-6) {
            if (iterperm == 0) { // primeira estimativa, primeira iteracao
                // utiliza-se a fracao de vazio sem escorregamento, pois a propria correlacao para se obter a
                // fracao de vazio depende do valor da fracao de vazio
                if ((fabs(celula[i].QG) + fabs(celula[i].QL)) > (*vg1dSP).localtiny) {
                    celula[i].alf = fabs(celula[i].QG) /
                                    (fabs(celula[i].QG) + fabs(celula[i].QL));
                    CalcC0UdPerm(i, c0, ud);
                } else
                    celula[i].alf = 0.;
                celula[i].alfini = celula[i].alf;
                celula[i - 1].alfR = celula[i].alf;
                celula[i - 1].alfRini = celula[i].alf;
                if (i < ncel)
                    celula[i + 1].alfL = celula[i].alf;
                if (i < ncel)
                    celula[i + 1].alfLini = celula[i].alf;
                celula[i].alfPigD = celula[i].alf;
                celula[i].alfPigDini = celula[i].alf;
                celula[i].alfPigE = celula[i].alf;
                celula[i].alfPigEini = celula[i].alf;
            }
            if (fabs(rhog) / rhol > 0.9) {
                c0 = 1.;
                ud = 0.;
            }

            // para o caso permanente, a fracao de vazio Ã© obtida a partir das relacoes de escorregamento
            // portanto, e neste ponto que se obtem Co e Ud:
            else if (fabs(celula[i].QG) > (*vg1dSP).localtiny && fabs(celula[i].QL) > (*vg1dSP).localtiny * 1e-6)
                CalcC0UdPerm(i, c0, ud);
            celula[i].c0 = c0;
            celula[i].ud = ud;
            double area = celula[i].duto.area;
            if (fabs(celula[i].QG + celula[i].QL) > (*vg1dSP).localtiny) {
                // alfa com escorregamento:
                celula[i].alf = celula[i].QG / (c0 * (celula[i].QG + celula[i].QL) + ud * area);
                double alfHomo = celula[i].QG / (celula[i].QG + celula[i].QL);
                if (celula[i].alf > 1. - 1e-15 || celula[i].alf < 1e-15)
                    celula[i].alf = alfHomo;

            } else
                celula[i].alf = 0.;
            if (celula[i].alf > (1 - (*vg1dSP).localtiny))
                celula[i].alf = celula[i].QG / (celula[i].QG + celula[i].QL);
        } else {
            c0 = 1.;
            ud = 0.;
            celula[i].alf = 1.;
        }
        if (celula[i].alf < 0.)
            celula[i].alf = 0.;
        else if (celula[i].alf > 1.)
            celula[i].alf = 1.;
        // atualizacoes dos valores das fracoes volumetricas da celula i armazendadas em
        // outras celulas, e inclusiove armazendo os valores para "tempo anterior", que nao
        // sao relevantes para o problema permanente mas importantes se o resultado permanente
        // der partida na solucao transiente:
        celula[i].alfini = celula[i].alf;
        celula[i - 1].alfR = celula[i].alf;
        celula[i - 1].alfRini = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfL = celula[i].alf;
        if (i < ncel)
            celula[i + 1].alfLini = celula[i].alf;
        celula[i].alfPigD = celula[i].alf;
        celula[i].alfPigDini = celula[i].alf;
        celula[i].alfPigE = celula[i].alf;
        celula[i].alfPigEini = celula[i].alf;
        if (i < ncel) {
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betL = celula[i].bet;
            celula[i + 1].betLini = celula[i].bet;
            celula[i + 1].betLI = celula[i].bet;
        }
        celula[i].betini = celula[i].bet;
        celula[i - 1].betR = celula[i].bet;
        celula[i - 1].betRini = celula[i].bet;
        celula[i].betPigD = celula[i].bet;
        celula[i].betPigDini = celula[i].bet;
        celula[i].betPigE = celula[i].bet;
        celula[i].betPigEini = celula[i].bet;
        celula[i].betI = celula[i].bet;
        celula[i - 1].betRI = celula[i].bet;
    }
    if (fabs(celula[i].QL) > 1e-15 && fabs(celula[i].MComp) > 1e-15) {
        double dxmed = 0.5 * (celula[i - 1].dx + celula[i].dx);
        double hol0 = 1. - celula[i - 1].alf;
        double hol1 = 1. - celula[i].alf;
        celula[i].fluicol.TR = (celula[i - 1].fluicol.TR * celula[i - 1].MComp + celula[i - 1].fontemassCR * trF) / celula[i].MComp;
        celula[i].fluicol.TR = celula[i].fluicol.TR + (0.5 * celula[i - 1].duto.area * celula[i - 1].dx * hol0 +
                                                       0.5 * celula[i].duto.area * celula[i].dx * hol1) /
                                                          celula[i].QL;
    } else
        celula[i].fluicol.TR = celula[i - 1].fluicol.TR;
}

void SProd::CalcC0UdPerm(int ind, double &c0, double &ud) {

    c0 = 1.;
    ud = 0.;
    if (celula[ind].acsr.tipo != 4 || celula[ind].acsr.bcs.freqnova <= 1.) {
        double hns;
        double razdx = celula[ind].dx / (celula[ind].dx + celula[ind].dxL);
        double razdx0;
        if (ind > 0)
            razdx0 = celula[ind - 1].dx / (celula[ind - 1].dx + celula[ind - 1].dxL);
        else
            razdx0 = razdx;
        hns = 1. - celula[ind].alf;

        double hol0 = hns;
        double alf0 = 1 - hol0;
        double alf1;
        alf1 = celula[ind].alf;

        double alfneg;
        if (ind > 1)
            alfneg = celula[ind - 2].alf;
        else if (ind > 0)
            alfneg = celula[ind - 1].alf;
        else
            alfneg = celula[ind].alf;

        double betI = celula[ind].betL;
        double betneg = celula[ind].betL;

        double pmed;
        double pmed0 = 0.;

        pmed = celula[ind].presaux;
        if (ind > 0)
            pmed0 = celula[ind - 1].presaux;
        else
            pmed0 = celula[ind].presaux;
        double tmed;
        if (iterperm != 0 && arq.AceleraConvergPerm == 0)
            tmed = razdx * celula[ind].temp + (1 - razdx) * celula[ind].tempL;
        else
            tmed = celula[ind - 1].temp;
        double tmed0;
        if (ind > 0 && arq.AceleraConvergPerm == 0)
            tmed0 = razdx0 * celula[ind - 1].temp + (1 - razdx0) * celula[ind - 1].tempL;
        else
            tmed0 = tmed;

        double correcHor = 1.;
        if (fabs(celula[ind].duto.teta) < 1e-10) {
            if (celula[ind].angEsq < 0 && celula[ind].angDir < 0)
                correcHor = -1.;
            else if (celula[ind].angEsq > 0 && celula[ind].angDir > 0)
                correcHor = 1.;
        }

        double rlm;
        double viscl1;
        double tensup1;
        if (ind > 0)
            rlm = (1 - betI) * celula[ind - 1].flui.MasEspLiq(pmed0, tmed0) + betI * celula[ind - 1].fluicol.MasEspFlu(pmed0, tmed0);
        else
            rlm = (1 - betI) * (*celula[ind].fluiL).MasEspLiq(pmed, tmed) + betI * celula[ind].fluicol.MasEspFlu(pmed, tmed);

        viscl1 = (1 - betI) * (*celula[ind].fluiL).ViscOleo(pmed0, tmed0) + betI * celula[ind].fluicol.VisFlu(pmed0, tmed0);
        tensup1 = (1 - betI) * (*celula[ind].fluiL).TensSuper(pmed, tmed) + betI * celula[ind].fluicol.TensSuper(pmed, tmed);

        double rgm;
        double viscg1;
        if (ind > 0)
            rgm = celula[ind - 1].flui.MasEspGas(pmed0, tmed0);
        else
            rgm = (*celula[ind].fluiL).MasEspGas(pmed, tmed);
        viscg1 = (*celula[ind].fluiL).ViscGas(pmed, tmed);

        double ug1 = fabs(celula[ind].MC - celula[ind].Mliqini) / rgm;
        double ul1 = fabs(celula[ind].Mliqini) / rlm;
        double dia1 = celula[ind].duto.a;
        if (ind > 0 && ug1 >= 0)
            dia1 = celula[ind - 1].duto.a;
        double A1 = M_PI * dia1 * dia1 / 4.;

        double rmed = hns * rlm + (1 - hns) * rgm;
        double visc = (hns * viscl1 + (1 - hns) * viscg1) / pow(10., 3.);
        double nrey = dia1 * rmed * (fabs(ug1) / A1 + fabs(ul1) / A1) / visc;
        double nreyl = dia1 * rlm * (fabs(ug1) / A1 + fabs(ul1) / A1) / (viscl1 / 1000.);

        int xarr1 = 1;
        double dtot = celula[ind].dxL + celula[ind].dx;
        double razL = celula[ind].dxL;
        double raz = celula[ind].dx;
        double ang = (razL * celula[ind].dutoL.teta + raz * celula[ind].duto.teta) / dtot;
        double sinalAng = 1.;
        if (fabs(celula[ind].MC) > 1e-15)
            sinalAng = celula[ind].MC / fabs(celula[ind].MC);
        if (rgm < 0.9 * rlm) {
            if (nrey > 1e-30) {
                if (fabs(0 * ang + sinalAng * celula[ind].duto.teta) < 45. * M_PI / 180. && hol0 < 0.99 && hol0 > 0.01 && ind < ncel - 1) {
                    double ug0;
                    double ul0;

                    ug1 = fabs(celula[ind].MC - celula[ind].Mliqini) / rgm;
                    ul1 = fabs(celula[ind].Mliqini) / rlm;

                    ug0 = ug1;
                    ul0 = ul1;
                    if (ind > 0) {
                        ug0 = fabs(celula[ind - 1].MC - celula[ind - 1].Mliqini) / celula[ind].flui.MasEspGas(pmed0, tmed0);
                        ul0 = fabs(celula[ind - 1].Mliqini) / ((1 - betneg) * celula[ind].flui.MasEspLiq(pmed0, tmed0) + betneg * celula[ind].fluicol.MasEspFlu(pmed0, tmed0));
                    }

                    estratificado testamapa(dia1, ul1, ug1, rlm, rgm, viscl1 / pow(10., 3.), viscg1 / pow(10., 3.), hol0,
                                            sinalAng * celula[ind].duto.teta, celula[ind].duto.rug / dia1);

                    testamapa.mapaTD();
                    xarr1 = testamapa.arr;
                    if (xarr1 == -1) {
                        if (((celula[ind].arranjo != xarr1) || celula[ind].transic > 0)) {
                            if ((celula[ind].arranjo != xarr1) && celula[ind].transic > 0)
                                celula[ind].transic = 0;
                            celula[ind].transic++;
                            if (celula[ind].transic > 19)
                                celula[ind].transic = 0;
                        }
                        celula[ind].arranjo = xarr1 = testamapa.arr;
                        if (ind > 0) {
                            celula[ind - 1].arranjoR = testamapa.arr;
                            celula[ind - 1].perdaEstratL = testamapa.fatorperdaLiq;
                            celula[ind - 1].perdaEstratG = testamapa.fatorperdaGas;
                        }

                        double c0D;
                        double udD;
                        double c0E;
                        double udE;
                        C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                     c0D, udD, correcHor, celula[ind].estabCol);
                        C0UdEstratificado(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                          c0E, udE, correcHor, celula[ind].estabCol);

                        double mult0, mult1;
                        mult0 = 1.;
                        if (ul0 < 0.)
                            mult0 = 0.;
                        mult1 = 0.;
                        if (ul1 < 0.)
                            mult1 = 1.;
                        double alf0E = celula[ind].alf;
                        if (ind > 0)
                            alf0E = celula[ind - 1].alf;

                        double jmax = 0.05;
                        double jmin = 0.005;
                        if ((fabs(ug1) + fabs(ul1)) / A1 > jmax) {
                            c0 = c0E;
                            ud = udE;
                        } else if ((fabs(ug1) + fabs(ul1)) / A1 < jmin) {
                            c0 = c0D;
                            ud = udD;
                        } else {
                            double raz = (jmax - (fabs(ug1) + fabs(ul1)) / A1) / (jmax - jmin);
                            c0 = ((1. - raz) * c0E + raz * c0D);
                            ud = ((1. - raz) * udE + raz * udD);
                        }
                    }
                }
                if (xarr1 == 1) {

                    arranjo testamapa2(dia1, ul1 / A1, ug1 / A1, rlm, rgm, viscl1 / pow(10., 3.), viscg1 / pow(10., 3.), hol0,
                                       sinalAng * celula[ind].duto.teta, tensup1, arq.mapaArranjo, vg1dSP);
                    xarr1 = testamapa2.verificaArr();

                    C0UdDisperso(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                 c0, ud, correcHor, celula[ind].estabCol);
                    if (xarr1 == -2) {
                        C0UdAnularChurn(rlm, rgm, tensup1, alf0, nrey, nreyl, ug1, ul1, dia1, celula[ind].duto.rug, ang,
                                        c0, ud, correcHor, celula[ind].estabCol);
                    }
                    celula[ind].arranjo = xarr1;
                    if (ind > 0)
                        celula[ind - 1].arranjoR = xarr1;
                }
                celula[ind].c0Spare = c0;
                celula[ind].udSpare = ud;
            }
        } else {
            c0 = 1.;
            ud = 0.;
            celula[ind].arranjo = 1;
            if (ind > 0)
                celula[ind - 1].arranjoR = 1;
            celula[ind].c0Spare = c0;
            celula[ind].udSpare = ud;
        }
    }
    if (arq.escorregaPerm == 0) {
        double ulsmed = celula[ind].QL / celula[ind].duto.area;
        double correcaoUd = 1 - (ulsmed - 0.15) / 0.35;
        double correcaoCo = c0 - (c0 - 1) * (ulsmed - 0.15) / 0.35;
        if (correcaoUd > 1.)
            correcaoUd = 1.;
        if (correcaoUd < 0.)
            correcaoUd = 0.;
        if (correcaoCo > c0)
            correcaoCo = c0;
        if (correcaoCo < 1)
            correcaoCo = 1;
        c0 = 1. + 0 * correcaoCo;
        ud = 0. + 0. * ud * correcaoUd;
    }
}

void SProd::RenovaTransMassPerm(int i) {

    double fwd;
    double fwe;

    double razdx = celula[i].dxR / (celula[i].dx + celula[i].dxR);
    double razdxL = celula[i].dx / (celula[i].dx + celula[i].dxL);
    double tmed;
    tmed = razdx * celula[i + 1].temp + (1 - razdx) * celula[i].temp;
    double tmed0;
    if (i > 1) {
        tmed0 = razdxL * celula[i].temp + (1 - razdxL) * celula[i - 1].temp;
    } else
        tmed0 = celula[i].temp;

    double bo1 = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp);
    double ba1 = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);
    fwd = celula[i].flui.BSW * ba1 / (bo1 + ba1 * celula[i].flui.BSW - celula[i].flui.BSW * bo1);
    double rsR = celula[i].flui.RS(celula[i + 1].presaux, tmed);
    double boR = celula[i].flui.BOFunc(celula[i + 1].presaux, tmed);
    double bo0;
    double ba0;
    double rsL;
    double boL;
    double baL;
    double dengD = celula[i].flui.Deng;
    double dengE;
    double rD = celula[i].flui.rDgD;
    double rE;
    if (i > 0) {
        bo0 = celula[i - 1].flui.BOFunc(celula[i - 1].pres, celula[i - 1].temp);
        ba0 = celula[i - 1].flui.BAFunc(celula[i - 1].pres, celula[i - 1].temp);
        fwe = celula[i - 1].flui.BSW * ba0 / (bo0 + ba0 * celula[i - 1].flui.BSW - celula[i - 1].flui.BSW * bo0);
        rsL = celula[i - 1].flui.RS(celula[i].presaux, tmed0);
        boL = celula[i - 1].flui.BOFunc(celula[i].presaux, tmed0);
        baL = celula[i - 1].flui.BAFunc(celula[i].presaux, tmed0);
        dengE = celula[i - 1].flui.Deng;
        rE = celula[i - 1].flui.rDgD;
    } else {
        bo0 = celula[i].flui.BOFunc(celula[i].pres, celula[i].temp);
        ba0 = celula[i].flui.BAFunc(celula[i].pres, celula[i].temp);

        fwe = celula[i].flui.BSW * ba0 / (bo0 + ba0 * celula[i].flui.BSW - celula[i].flui.BSW * bo0);
        rsL = celula[i].flui.RS(celula[i].presaux, tmed0);
        boL = celula[i].flui.BOFunc(celula[i].presaux, tmed0);
        baL = celula[i].flui.BAFunc(celula[i].presaux, tmed0);
        dengE = celula[i].flui.Deng;
        rE = celula[i].flui.rDgD;
    }
    double betI;
    double betL;
    if (i < ncel) {
        betI = celula[i].betPigD;
        if (celula[i + 1].Mliqini < 0)
            betI = celula[i + 1].betPigE;
    } else
        betI = celula[i].betPigD;
    if (i > 0) {
        betL = celula[i - 1].betPigD;
        if (celula[i].Mliqini < 0)
            betL = celula[i].betPigE;
    } else
        betL = celula[i].betPigE;

    if (celula[i].acsr.tipo == 0)
        celula[i].transmassR = (-(celula[i + 1].QL * (1. - betI) * rD * dengD * 1.225 * (1. - fwd) * rsR * (6.29 / 35.31467) / boR) + (celula[i].QL * (1. - betL) * rE * dengE * 1.225 * (1. - fwe) * rsL * (6.29 / 35.31467) / boL));
    else
        celula[i].transmassR = 0.;

    celula[i].transmassR /= celula[i].dx;
    celula[i + 1].transmassL = celula[i].transmassR;
    celula[i].FonteMudaFase = celula[i].transmassR;
    if (i == ncel - 1) {
        celula[i + 1].transmassR = celula[i].transmassR;
        celula[i + 1].FonteMudaFase = celula[i].transmassR;
    }
}

void SProd::RenovaTransMassPermGas(int i) {

    if (celula[i].acsr.tipo == 0 && fabs(celula[i + 1].flui.dVaporMassFraction - celula[i].flui.dVaporMassFraction) < 0.2) {
        celula[i].transmassR = ((celula[i + 1].MC - celula[i + 1].Mliqini) -
                                (celula[i].MC - celula[i].Mliqini));
    } else
        celula[i].transmassR = 0.;

    celula[i].transmassR /= celula[i].dx;
    celula[i + 1].transmassL = celula[i].transmassR;
    celula[i].FonteMudaFase = celula[i].transmassR;
    if (i == ncel - 1) {
        celula[i + 1].transmassR = celula[i].transmassR;
        celula[i + 1].FonteMudaFase = celula[i].transmassR;
    }
}

void SProd::RenovaTempPerm(int i, int RK) {
    double dx = 0.5 * (celula[i].dx + celula[i - 1].dx);
    double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
    double dTdLmed = (celula[i].dx * celula[i].dTdLCor + celula[i - 1].dx * celula[i - 1].dTdLCor) / (celula[i].dx + celula[i - 1].dx);
    double dia = celula[i - 1].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double alfmed;
    double betmed;
    double pmed;
    double tmed;
    if (RK == 0) {
        alfmed = celula[i - 1].alf;
        betmed = celula[i - 1].bet;
        pmed = celula[i - 1].pres;
        tmed = celula[i - 1].temp;
    } else {
        alfmed = celula[i].alf;
        betmed = celula[i].bet;
        pmed = celula[i].pres;
        tmed = celula[i].temp;
    }
    double ugsmed;
    ugsmed = celula[i].QG / area; // velocidade superficial de gas
    double ulsmed;
    ulsmed = celula[i].QL / area; // velocidade superficial de liquido
    double sinalJ = 1.;
    if (fabs(ugsmed + ulsmed) > 0.05 && semTermo == 0) { // calculo termico e feito para velocidades de mistura superiores a 0,1 m/s,
        // para velocidades inferiores se admite que a temperatura do fluidoÃƒÂ© igual ÃƒÂ  temperatura ambiente
        sinalJ = (ugsmed + ulsmed) / fabs(ugsmed + ulsmed);
        double pmedi = celula[i].presaux + 0 * celula[i - 1].dpB / 98066.5;
        double tmedi;
        if (iterperm != 0 && arq.AceleraConvergPerm == 0) // temperatura na interface esquerda da celula
            // caso em que se considera que ja foi
            // feita uma iteracao e ja se tem a temperatura na celula i vinda da iteracao anterior
            // isto pode dificultar a convergencia, quando se deseja a aceleracao da convergencia
            // admite-se que a temperatura na fronteira esquerda ÃƒÂ© a temperatura da celula esquerda
            tmedi = (celula[i].dx * celula[i].temp + celula[i].dxL * celula[i].tempL) / (celula[i].dx + celula[i].dxL);
        else
            tmedi = celula[i - 1].temp;
        double rp = celula[i - 1].flui.MasEspLiq(pmed, tmed);    // celula[i].rpCi;
        double rc = celula[i - 1].fluicol.MasEspFlu(pmed, tmed); // celula[i].rcCi;
        double rhol = (1. - betmed) * rp + betmed * rc;
        double rhog = celula[i - 1].flui.MasEspGas(pmed, tmed); // celula[i].rgCi;
        double cpl = (1. - betmed) * celula[i - 1].flui.CalorLiq(pmedi, tmedi) + betmed * celula[i - 1].fluicol.CalorLiq(pmedi, tmedi);
        double cpg = celula[i - 1].flui.CalorGas(pmedi, tmedi);
        // jtl=Joule Thomson do liquido X cp
        // jtg=Joule Thomson do gas X cp
        /////??????????????????????????????????????????????????????????????????????????????????????????
        double jtl = (1. - betmed) * celula[i - 1].flui.JTL(pmedi, tmedi) - betmed / rc;
        /////??????????????????????????????????????????????????????????????????????????????????????????
        if (arq.pocinjec > 0 && arq.condpocinj.tipoFlui == 2) {
            jtl = -(1 + (tmedi + 273.14) * celula[i - 1].fluicol.DrhoDtFlu(pmedi, tmedi) / rc) / rc;
        }
        double jtg = celula[i - 1].flui.JTG(pmedi, tmedi);
        // energia potencial:
        double hidro = (rhol * ulsmed + rhog * ugsmed) * area * 9.82 * sin(celula[i - 1].duto.teta);

        if (i > 120) {
            int para;
            para = 0;
        }

        // definicao dos parametros internos na tubulacao para se obter a troca termica om o meio ambiente
        celula[i - 1].calor.Tint = tmedi;
        celula[i - 1].calor.Vint = fabs(ugsmed + ulsmed);
        double condliq = (1. - betmed) * celula[i - 1].flui.CondLiq(pmedi, tmedi) + betmed * celula[i - 1].fluicol.CondLiq(pmedi, tmedi);
        celula[i - 1].calor.kint = condliq * (1 - alfmed) + celula[i - 1].flui.CondGas(pmedi, tmedi) * alfmed;
        celula[i - 1].calor.cpint = cpl * (1 - alfmed) + cpg * alfmed;
        celula[i - 1].calor.rhoint = rhol * (1 - alfmed) + rhog * alfmed;
        double viscliq = (1. - betmed) * celula[i - 1].flui.ViscOleo(pmedi, tmedi) + betmed * celula[i - 1].fluicol.VisFlu(pmedi, tmedi);
        celula[i - 1].calor.viscint = viscliq * (1 - alfmed) * 1.e-3 + celula[i - 1].flui.ViscGas(pmedi, tmedi) * alfmed * 1.e-3;

        double fluxrelax = 0; // variavel nao utilizada
        if (iterperm > 0)
            fluxrelax = celula[i - 1].fluxcalmed;
        double resanul = 0.;
        double fluxcal;
        double fluxcalG;
        // verifica se existe acoplamento com o anular:
        if (arq.lingas == 1 && (i - 1 <= ColunaAnulaIni && i - 1 >= ColunaAnulaFim)) {
            int j = ColunaAnulaIni + AnulaColunaIni - (i - 1);
            // caso tenha acoplamento:

            if (iterperm == 0 && (i - 1) < ColunaAnulaIni) {
                // na primeira iteracao, considera-se a resistencia do revestimento + cimento +
                // formacao, nas outras iteracoes
                // bastara determinar a troca termica entre a coluna e o gas do anular
                // no modelo acoplado, a coluna nao tem a definicao da parede revestimento+cimento+formacao
                // para se obter esta resistencia, precisa-se recorrer ao modelo de troca termica
                // do anular, o que e feito aqui:
                // obs:isto Ã© feito ate uma celula antes de se chegar na master (i-1)<ColunaAnulaIni,
                // se esta fazendo igual ao que se faz no simulador involuta, para melhorar
                // a estimativa da temperatura na ANM. Na celula da anm, so se considera a troca termica com o gas
                // desde a primeira iteracao
                celulaG[j].calor.Tint = tmedi;
                celulaG[j].calor.Vint = 100;
                celulaG[j].calor.kint = celula[i - 1].flui.CondGas(pmedi, tmedi);
                celulaG[j].calor.cpint = cpg;
                celulaG[j].calor.rhoint = rhog;
                celulaG[j].calor.viscint = celula[i - 1].flui.ViscGas(pmedi, tmedi) * 1.e-3;
                celulaG[j].fluxcal = celulaG[j].calor.transperm(); // troca termica no anular
                resanul = celulaG[j].calor.resGlob;                // resistencia das paredes
                // observe que nÃ£o se esta de fato interessado no fluxo de calor, mas apenas em obter a resistencia
                // termica do conjunto de paredes a partir do revestimento em direcao aa formacao
                celula[i - 1].calor.Vextern1 = 100.;
                celula[i - 1].calor.kextern1 = celulaG[j].calor.kint;
                celula[i - 1].calor.cpextern1 = celulaG[j].calor.cpint;
                celula[i - 1].calor.rhoextern1 = celulaG[j].calor.rhoint;
                celula[i - 1].calor.viscextern1 = celulaG[j].calor.viscint;
            } else { // apos a primeira iteracao, considera-se apenas a troca termica entre a coluna e o gas do anular
                // passando pela parede da coluna, claro
                resanul = 0.;
                celula[i - 1].calor.Vextern1 = celulaG[j].VGasR / celulaG[j].u1L;
                celula[i - 1].calor.kextern1 = celulaG[j].calor.kint;
                celula[i - 1].calor.cpextern1 = celulaG[j].calor.cpint;
                celula[i - 1].calor.rhoextern1 = celulaG[j].calor.rhoint;
                celula[i - 1].calor.viscextern1 = celulaG[j].calor.viscint;
            }
            if (iterperm == 0)
                celula[i - 1].calor.Textern1 = celulaG[j].calor.Textern1; // na primeira iteracao, como se
            // usa toda a resistencia termica do poco, a temperatura externa utilizada Ã© a geotermica
            else
                celula[i - 1].calor.Textern1 = celulaG[j].temp; // nas iteracoes seguintes, a temperatura ambiente
            // e a temperatura do gas
        }
        celula[i - 1].fluxcalmed = 0;
        if (verificaAcopRedeP == 1 && (i - 1) >= PrimSecFimRedeP && (i - 1) <= PrimSecIniRedeP) {
            fluxcal = sinalJ * celula[i - 1].calor.transperm(celula[i - 1].resAcopRedeP);
        } else
            fluxcal = sinalJ * celula[i - 1].calor.transperm(resanul);
        celula[i - 1].fluxcalmed = fluxcal; // fluxo de calor na coluna

        double coefdxT = (rhol * ulsmed * cpl + rhog * ugsmed * cpg) * area; // termo que multiplica
        // a derivada Dt/Dx
        double ulsmedTemp = ulsmed;
        if ((*vg1dSP).blackOilTemp == 1 && fabs(ulsmed) > 5)
            ulsmedTemp = 5 * ulsmed / ulsmed;
        double ugsmedTemp = ugsmed;
        if ((*vg1dSP).blackOilTemp == 1 && fabs(ugsmed) > 5)
            ugsmedTemp = 5 * ugsmed / fabs(ugsmed);
        double coefdxP = 1 * (rhol * ulsmedTemp * jtl + rhog * ugsmedTemp * jtg) * area; // termo que multiplica
        // a derivada Dp/Dx
        double dpdx;
        if ((celula[i - 1].acsr.tipo != 4 || celula[i - 1].acsr.bcs.freq < 1) && celula[i - 1].acsr.tipo != 7)
            // caso nao tenha BCS ou incremento de pressao  utiliza-se a pressao na fronteira esquerda
            //  e a pressao no centro de celula para o calculo de Dp/Dx
            dpdx = 2. * (celula[i].presaux - celula[i - 1].pres) * 98066.5 / celula[i - 1].dx;
        else {
            // caso tenha BCS ou incremento de pressao  utiliza-se a pressao da celulaa esquerda
            //  e a pressao no centro de celula para o calculoi de Dp/Dx
            dpdx = 2. * (celula[i].presaux - celula[i - 1].pres) * 98066.5 / celula[i - 1].dx;
        }
        celula[i].VTemper = ulsmed; // esta velocidade so e util no caso transiente, Ã© armazenada aqui
        // apenas para se ter um valor quando a simulacao transiente se iniciar
        double dtdx = (-celula[i - 1].temp) / dxmed;

        double cinetico = 0;
        double ugmed0 = 0;
        double ulmed0 = 0;
        double ugmed = 0;
        double ulmed = 0;
        // termo de energia cinetica:
        if (celula[i].acsr.tipo == 0 && celula[i - 1].acsr.tipo == 0 && i > 2) {
            double dxCin = celula[i - 1].dx;
            double diaaux = celula[i - 1].duto.a;
            double areaaux = 0.25 * M_PI * diaaux * diaaux;

            if (celula[i - 1].alf > 1e-3)
                ugmed = ugsmed / celula[i - 1].alf;
            if (celula[i - 1].alf < (1. - 1e-3))
                ulmed = ulsmed / (1. - celula[i - 1].alf);

            if (celula[i - 2].alf > 1e-3) {
                ugmed0 = celula[i - 1].QG / (areaaux);
                ugmed0 /= celula[i - 2].alf;
            }
            if (celula[i - 2].alf < (1. - 1e-3)) {
                ulmed0 = celula[i - 1].QL / (areaaux);
                ulmed0 /= (1. - celula[i - 2].alf);
            }

            if (arq.nCompTotalUnidadesP / dxCin < 1e6)
                cinetico = (celula[i].MC - celula[i].Mliqini) * ugmed * (ugmed - ugmed0) / dxCin + celula[i].Mliqini * ulmed * (ulmed - ulmed0) / dxCin;
            else
                cinetico = 0;
        }

        double fontemassG = 0.;
        double fontemassL = 0.;
        double fontemassC = 0.;
        double tfonte = celula[i - 1].temp;
        double cpgF;
        double razcpF = 0.;
        double cplF;

        // calculo da energia adicionada no sistema devido a fontes de massa
        if (celula[i - 1].acsr.tipo == 1) { // caso fonte de gas
            tfonte = celula[i - 1].acsr.injg.temp;
            cpgF = celula[i - 1].acsr.injg.FluidoPro.CalorGas(pmed, tfonte);
            razcpF = celula[i - 1].acsr.injg.FluidoPro.ConstAdG(pmed, tfonte);
            cplF = 0.;
        } else if (celula[i - 1].acsr.tipo == 2) { // caso fonte de liquido
            tfonte = celula[i - 1].acsr.injl.temp;
            cpgF = 0.;
            razcpF = 1.;
            cplF = (1. - celula[i - 1].acsr.injl.bet) * celula[i - 1].acsr.injl.FluidoPro.CalorLiq(pmed, tmed) + celula[i - 1].acsr.injl.bet * celula[i - 1].acsr.injl.fluidocol.CalorLiq(pmed, tmed);
        } else if (celula[i - 1].acsr.tipo == 3) { // caso IPR
            tfonte = celula[i - 1].acsr.ipr.Tres;
            cpgF = celula[i - 1].acsr.ipr.FluidoPro.CalorGas(pmed, tmed);
            razcpF = celula[i - 1].acsr.ipr.FluidoPro.ConstAdG(pmed, tmed);
            cplF = celula[i - 1].acsr.ipr.FluidoPro.CalorLiq(pmed, tmed);
        } else if (celula[i - 1].acsr.tipo == 9 && celula[i - 1].acsr.fontechk.abertura > 1e-6 &&
                   (celula[i - 1].fontemassCR + celula[i - 1].fontemassGR + celula[i - 1].fontemassLR) > 1e-9) {
            // caso vazamento
            tfonte = celula[i - 1].acsr.fontechk.tamb;
            cpgF = celula[i - 1].acsr.fontechk.fluidoPamb.CalorGas(pmed, tmed);
            razcpF = celula[i - 1].acsr.fontechk.fluidoPamb.ConstAdG(pmed, tmed);
            cplF = (1. - celula[i - 1].acsr.fontechk.betISamb) *
                       celula[i - 1].acsr.fontechk.fluidoPamb.CalorLiq(pmed, tmed) +
                   celula[i - 1].acsr.fontechk.betISamb * celula[i - 1].acsr.fontechk.fluidocol.CalorLiq(pmed, tmed);
        } else if (celula[i - 1].acsr.tipo == 15) { // caso IPR
            tfonte = celula[i - 1].acsr.radialPoro.tRes;
            cpgF = celula[i - 1].acsr.radialPoro.flup.CalorGas(pmed, tmed);
            razcpF = celula[i - 1].acsr.radialPoro.flup.ConstAdG(pmed, tmed);
            cplF = celula[i - 1].acsr.radialPoro.flup.CalorLiq(pmed, tmed);
        } else if (celula[i - 1].acsr.tipo == 16) { // caso IPR
            tfonte = celula[i - 1].acsr.poroso2D.dados.tRes;
            cpgF = celula[i - 1].acsr.poroso2D.dados.flup.CalorGas(pmed, tmed);
            razcpF = celula[i - 1].acsr.poroso2D.dados.flup.ConstAdG(pmed, tmed);
            cplF = celula[i - 1].acsr.poroso2D.dados.flup.CalorLiq(pmed, tmed);
        } else if (celula[i].acsrL != 0) {

            cpgF = 0.;
            razcpF = 1.;
            cplF = 0.;
        } else {
            cpgF = 0.;
            razcpF = 1.;
            cplF = 0.;
        }

        fontemassL = 0;
        if (celula[i - 1].fontemassLR > 0.)
            fontemassL = celula[i - 1].fontemassLR / dx;
        if (celula[i - 1].fontemassCR > 0.)
            fontemassL += celula[i - 1].fontemassCR / dx;
        fontemassL *= (cplF) * (tfonte - celula[i - 1].temp);

        fontemassG = celula[i - 1].fontemassGR / dx;
        if (fontemassG > 0.)
            fontemassG *= (cpgF / razcpF) * (tfonte - celula[i - 1].temp);
        else
            fontemassG = 0;

        // efeito do calor latente, quando este for solicitado
        double latente = 0.;
        if (isnan(celula[i - 1].FonteMudaFase))
            celula[i - 1].FonteMudaFase = 0.;
        double valTransMass = fabs(celula[i - 1].FonteMudaFase);
        double sigTransMass = 1.;
        if (valTransMass > 1e-25)
            sigTransMass = celula[i - 1].FonteMudaFase / valTransMass;
        if (arq.limTransMass < valTransMass)
            valTransMass = sigTransMass * arq.limTransMass;
        else
            valTransMass *= sigTransMass;
        if (celula[i].flui.dVaporMassFraction < (1 - 1e-15) && celula[i].flui.dVaporMassFraction > (1e-15)) {
            if (celula[i - 1].acsr.tipo == 1 || celula[i - 1].acsr.tipo == 2 || celula[i - 1].acsr.tipo == 3 || celula[i - 1].acsr.tipo == 15 || celula[i - 1].acsr.tipo == 16) {
                celula[i - 1].FonteMudaFase = 0.;
                valTransMass = 0.;
            }
            if (CalcLat > 0 && iterperm != 0 && arq.flashCompleto == 0) {
                latente = interpolaHLatente(pmed, tmed) * valTransMass;
            } else if ((arq.flashCompleto == 1 || arq.flashCompleto == 2) && CalcLat > 0 && iterperm != 0) {
                latente = (celula[i].flui.EntalpGas(pmed, tmed) -
                           celula[i].flui.EntalpLiq(pmed, tmed)) *
                          valTransMass;
            } else
                latente = 0;
        }

        double alfinter;
        double alfinterL;
        if (ugsmed > 0) {
            alfinter = celula[i - 1].alf;
            alfinterL = celula[i - 1].alfL;
        } else {
            alfinter = celula[i - 1].alfR;
            alfinterL = celula[i - 1].alf;
        }
        double delvel;
        if (alfinter > (*vg1dSP).localtiny && alfinter < (1. - (*vg1dSP).localtiny))
            delvel = ugsmed / alfinter - ulsmed / (1. - alfinter);
        else if (alfinter > (*vg1dSP).localtiny)
            delvel = ugsmed;
        else
            delvel = ulsmed;
        double verifica = area * celula[i].pres * 98600 * delvel * (alfinter - alfinterL) / celula[i].dx;

        if (fabs(coefdxT) > (*vg1dSP).localtiny) {
            // Energy terms for boundary work, potential and kinetic energy,
            // mass sources, latent heat, and shaft work.
            if (arq.latente == 0)
                latente = 0.;
            else if (arq.condlatente == 0 && latente < 0)
                latente = 0.;
            double parcenerg1 = dTdLmed * (coefdxP * dpdx - cinetico - (hidro) + (fontemassL + fontemassG) - latente + (celula[i - 1].potTermo + celula[i - 1].fonteCal) / dxmed) / coefdxT;
            // portion of energy related to heat exchange
            double parcenerg2 = dTdLmed * (fluxcal) / coefdxT;

            // Check whether heat transfer is too fast for the explicit temperature update.
            // If so, use additional substeps to prevent thermal instability.
            int npasso;
            double dxpasso;
            double limiteEstab = fabs(coefdxT) / dTdLmed;
            if (dxmed / (celula[i - 1].calor.resGlob + resanul) < (limiteEstab + 0. * 1000.)) { // Thermal resistance is not low.
                npasso = 1;
                dxpasso = dxmed;
            } else {
                // For low thermal resistance, determine the number of temperature update steps.
                npasso = (dxmed / (celula[i - 1].calor.resGlob + resanul)) / (limiteEstab + 0 * 1000) + 1;
                dxpasso = dxmed / npasso; // cell divided into npassos
            }
            double temppasso = celula[i - 1].temp;

            temppasso = dxpasso * (-(-celula[i - 1].temp) / dxpasso + parcenerg1 + parcenerg2); // first step
            for (int j = 1; j < npasso; j++) {                                                  // next steps
                // Keep all energy terms except heat flow, which is recalculated at each step.
                celula[i - 1].calor.Tint = temppasso;
                celula[i - 1].calor.Vint = ugsmed + ulsmed;
                condliq = (1. - betmed) * celula[i - 1].flui.CondLiq(pmed, temppasso) + betmed * celula[i - 1].fluicol.CondLiq(pmed, temppasso);
                celula[i - 1].calor.kint = condliq * (1 - alfmed) + celula[i - 1].flui.CondGas(pmed, temppasso) * alfmed;
                celula[i - 1].calor.cpint = cpl * (1 - alfmed) + cpg * alfmed;
                celula[i - 1].calor.rhoint = rhol * (1 - alfmed) + rhog * alfmed;
                viscliq = (1. - betmed) * celula[i - 1].flui.ViscOleo(pmed, temppasso) + betmed * celula[i - 1].fluicol.VisFlu(pmed, temppasso);
                celula[i - 1].calor.viscint = viscliq * (1 - alfmed) * 1.e-3 + celula[i - 1].flui.ViscGas(pmed, temppasso) * alfmed * 1.e-3;
                if (iterperm != 0 && arq.lingas == 1 && (i - 1 <= ColunaAnulaIni && i - 1 >= ColunaAnulaFim)) {
                    int k = ColunaAnulaIni + AnulaColunaIni - (i - 1);
                    double dtext = (celulaG[k - 1].temp - celulaG[k].temp) / npasso;
                    celula[i - 1].calor.Textern1 = celulaG[k].temp + (j - 1) * dtext;
                }
                fluxcal = sinalJ * celula[i - 1].calor.transperm(resanul);
                celula[i - 1].fluxcalmed += fluxcal;
                parcenerg2 = dTdLmed * (fluxcal) / coefdxT;
                temppasso = dxpasso * (-(-temppasso) / dxpasso + parcenerg1 + parcenerg2);
            }

            celula[i].temp = temppasso;
            celula[i - 1].fluxcalmed /= npasso;
            celula[i - 1].fluxcalmed = 1. * celula[i - 1].fluxcalmed;

            if (celula[i].temp < -50.)
                celula[i].temp = -50.;
            if (celula[i].temp > 200.)
                celula[i].temp = 200.;
        } else
            celula[i].temp = celula[i].calor.Textern1;
    } else { // case where the mixing speed is too low
        celula[i].temp = celula[i - 1].calor.Textern1;
        if (arq.lingas == 1 && (i - 1 <= ColunaAnulaIni && i - 1 >= ColunaAnulaFim)) {
            int j = ColunaAnulaIni + AnulaColunaIni - (i - 1);
            celula[i - 1].calor.Vextern1 = 100.;
            celula[i - 1].calor.kextern1 = celulaG[j].calor.kint;
            celula[i - 1].calor.cpextern1 = celulaG[j].calor.cpint;
            celula[i - 1].calor.rhoextern1 = celulaG[j].calor.rhoint;
            celula[i - 1].calor.viscextern1 = celulaG[j].calor.viscint;
            double fluxcal = celula[i - 1].calor.transperm(0);
        }
    }
}

void SProd::RenovaTempPermRev(int i, int RK) {
    double dx = 0.5 * (celula[i].dx + celula[i + 1].dx);
    double dxmed = 0.5 * (celula[i].dx + celula[i + 1].dx);
    double dTdLmed = (celula[i].dx * celula[i].dTdLCor + celula[i + 1].dx * celula[i + 1].dTdLCor) / (celula[i].dx + celula[i + 1].dx);
    double dia = celula[i + 1].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double alfmed;
    double betmed;
    double pmed;
    double tmed;
    if (RK == 0) {
        alfmed = celula[i + 1].alf;
        betmed = celula[i + 1].bet;
        pmed = celula[i + 1].pres;
        tmed = celula[i + 1].temp;
    } else {
        alfmed = celula[i].alf;
        betmed = celula[i].bet;
        pmed = celula[i].pres;
        tmed = celula[i].temp;
    }
    double ugsmed;
    ugsmed = fabs(celula[i + 1].QG) / area; // Superficial gas velocity
    double ulsmed;
    ulsmed = fabs(celula[i + 1].QL) / area; // Superficial liquid velocity
    double sinalJ = 1.;
    if (fabs(ugsmed + ulsmed) > trocaTermicaLenta) {
        // Perform the thermal calculation for mixture velocities above 0.1 m/s.
        // Otherwise, assume the fluid temperature equals the ambient temperature.
        sinalJ = (ugsmed + ulsmed) / fabs(ugsmed + ulsmed);
        double pmedi = celula[i + 1].presaux - celula[i].dpB / 98066.5;
        double tmedi;
        if (iterperm != 0 && arq.AceleraConvergPerm == 0)
            // Set the left interface temperature from the previous iteration.
            // Use the adjacent left cell value to accelerate convergence.
            tmedi = (celula[i].dx * celula[i].temp + celula[i].dxR * celula[i].tempR) / (celula[i].dx + celula[i].dxR);
        else
            tmedi = celula[i + 1].temp;
        double rp = celula[i + 1].flui.MasEspLiq(pmed, tmed);
        double rc = celula[i + 1].fluicol.MasEspFlu(pmed, tmed);
        double rhol = (1. - betmed) * rp + betmed * rc;
        double rhog = celula[i + 1].flui.MasEspGas(pmed, tmed);
        double cpl = (1. - betmed) * celula[i + 1].flui.CalorLiq(pmedi, tmedi) + betmed * celula[i + 1].fluicol.CalorLiq(pmedi, tmedi);
        double cpg = celula[i + 1].flui.CalorGas(pmedi, tmedi);
        // Liquid Joule-Thomson coefficient multiplied by cp.
        // Gas Joule-Thomson coefficient multiplied by cp.p
        double jtl = (1. - betmed) * celula[i + 1].flui.JTL(pmedi, tmedi) - betmed / rc;
        if (arq.pocinjec > 0 && arq.condpocinj.tipoFlui == 2) {
            jtl = -(1 + (tmedi + 273.14) * celula[i + 1].fluicol.DrhoDtFlu(pmedi, tmedi) / rc) / rc;
        }
        double jtg = celula[i + 1].flui.JTG(pmedi, tmedi);
        // potential energy:
        double hidro = -(rhol * ulsmed + rhog * ugsmed) * area * 9.82 * sin(celula[i + 1].duto.teta);

        if (i > 120) {
            int para;
            para = 0;
        }

        // Definition of internal parameters in the piping to achieve heat exchange with the environment
        celula[i + 1].calor.Tint = tmedi;
        celula[i + 1].calor.Vint = fabs(ugsmed + ulsmed);
        double condliq = (1. - betmed) * celula[i + 1].flui.CondLiq(pmedi, tmedi) + betmed * celula[i + 1].fluicol.CondLiq(pmedi, tmedi);
        celula[i + 1].calor.kint = condliq * (1 - alfmed) + celula[i + 1].flui.CondGas(pmedi, tmedi) * alfmed;
        celula[i + 1].calor.cpint = cpl * (1 - alfmed) + cpg * alfmed;
        celula[i + 1].calor.rhoint = rhol * (1 - alfmed) + rhog * alfmed;
        double viscliq = (1. - betmed) * celula[i + 1].flui.ViscOleo(pmedi, tmedi) + betmed * celula[i + 1].fluicol.VisFlu(pmedi, tmedi);
        celula[i + 1].calor.viscint = viscliq * (1 - alfmed) * 1.e-3 + celula[i + 1].flui.ViscGas(pmedi, tmedi) * alfmed * 1.e-3;

        double fluxrelax = 0;
        if (iterperm > 0)
            fluxrelax = celula[i + 1].fluxcalmed;
        double resanul = 0.;
        double fluxcal;
        double fluxcalG;
        // checks if coupling with the annular space exists:
        if (arq.lingas == 1 && (i + 1 <= ColunaAnulaIni && i + 1 >= ColunaAnulaFim)) {
            int j = ColunaAnulaIni + AnulaColunaIni - (i + 1);
            // in case there is coupling:

            if (iterperm == 0 && (i + 1) > ColunaAnulaFim) {
                // On the first iteration, include casing, cement, and formation resistance.
                // Later iterations consider only heat transfer between the tubing and annular gas.
                // For the coupled model, obtain the external resistance from the annulus heat-transfer model.
                // Apply this up to the cell before the master valve to improve the ANM temperature estimate.
                // At the ANM cell, consider only heat transfer with the gas from the first iteration.
                celulaG[j].calor.Tint = tmedi;
                celulaG[j].calor.Vint = 100;
                celulaG[j].calor.kint = celula[i + 1].flui.CondGas(pmedi, tmedi);
                celulaG[j].calor.cpint = cpg;
                celulaG[j].calor.rhoint = rhog;
                celulaG[j].calor.viscint = celula[i + 1].flui.ViscGas(pmedi, tmedi) * 1.e-3;
                celulaG[j].fluxcal = celulaG[j].calor.transperm(); // troca termica no anular
                resanul = celulaG[j].calor.resGlob;                // resistencia das paredes
                // Only the thermal resistance from the casing to the formation is needed, not the actual heat flow.
                celula[i + 1].calor.Vextern1 = 100.;
                celula[i + 1].calor.kextern1 = celulaG[j].calor.kint;
                celula[i + 1].calor.cpextern1 = celulaG[j].calor.cpint;
                celula[i + 1].calor.rhoextern1 = celulaG[j].calor.rhoint;
                celula[i + 1].calor.viscextern1 = celulaG[j].calor.viscint;
            } else { // After the first iteration, consider only heat transfer between the tubing and annular gas.
                // Through the tubing wall.
                resanul = 0.;
                celula[i + 1].calor.Vextern1 = celulaG[j].VGasR / celulaG[j].u1L;
                celula[i + 1].calor.kextern1 = celulaG[j].calor.kint;
                celula[i + 1].calor.cpextern1 = celulaG[j].calor.cpint;
                celula[i + 1].calor.rhoextern1 = celulaG[j].calor.rhoint;
                celula[i + 1].calor.viscextern1 = celulaG[j].calor.viscint;
            }
            if (iterperm == 0)
                celula[i + 1].calor.Textern1 = celulaG[j].calor.Textern1; // na primeira iteracao, como se
            // usa toda a resistencia termica do poco, a temperatura externa utilizada Ã© a geotermica
            else
                celula[i + 1].calor.Textern1 = celulaG[j].temp; // nas iteracoes seguintes, a temperatura ambiente
            // e a temperatura do gas
        }
        celula[i + 1].fluxcalmed = 0;
        fluxcal = sinalJ * celula[i + 1].calor.transperm(resanul);
        celula[i + 1].fluxcalmed = fluxcal; // fluxo de calor na coluna

        double coefdxT = (rhol * ulsmed * cpl + rhog * ugsmed * cpg) * area; // termo que multiplica
        // a derivada Dt/Dx
        double coefdxP = 1. * (rhol * ulsmed * jtl + rhog * ugsmed * jtg) * area; // termo que multiplica
        // a derivada Dp/Dx
        double dpdx;
        if ((celula[i + 1].acsr.tipo != 4 || celula[i + 1].acsr.bcs.freq < 1) && celula[i + 1].acsr.tipo != 7)
            // caso nao tenha BCS ou incremento de pressao  utiliza-se a pressao na fronteira esquerda
            //  e a pressao no centro de celula para o calculo de Dp/Dx
            dpdx = 2. * (celula[i + 1].presaux - celula[i + 1].pres) * 98066.5 / celula[i + 1].dx;
        else {
            // caso tenha BCS ou incremento de pressao  utiliza-se a pressao da celulaa esquerda
            //  e a pressao no centro de celula para o calculoi de Dp/Dx
            dpdx = (pmedi - celula[i + 1].pres) * 98600. / dx;
        }
        celula[i].VTemper = ulsmed; // esta velocidade so e util no caso transiente, Ã© armazenada aqui
        // apenas para se ter um valor quando a simulacao transiente se iniciar
        double dtdx = (-celula[i + 1].temp) / dxmed;

        double cinetico = 0;
        double ugmed0 = 0;
        double ulmed0 = 0;
        double ugmed = 0;
        double ulmed = 0;
        // termo de energia cinetica:
        if (celula[i].acsr.tipo == 0 && celula[i + 1].acsr.tipo == 0 && i < ncel) {
            double dxCin = celula[i + 1].dx;
            double diaaux = celula[i + 1].duto.a;
            double areaaux = 0.25 * M_PI * diaaux * diaaux;

            if (celula[i + 1].alf > 1e-3)
                ugmed = ugsmed / celula[i + 1].alf;
            if (celula[i + 1].alf < (1. - 1e-3))
                ulmed = ulsmed / (1. - celula[i + 1].alf);

            if (celula[i].alf > 1e-3) {
                ugmed0 = fabs(celula[i].QG) / (areaaux);
                ugmed0 /= celula[i].alf;
            }
            if (celula[i].alf < (1. - 1e-3)) {
                ulmed0 = fabs(celula[i].QL) / (areaaux);
                ulmed0 /= (1. - celula[i].alf);
            }

            cinetico = -fabs(celula[i].MC - celula[i].Mliqini) * ugmed * (ugmed - ugmed0) / dxCin -
                       fabs(celula[i].Mliqini) * ulmed * (ulmed - ulmed0) / dxCin;
        }

        double fontemassG = 0.;
        double fontemassL = 0.;
        double tfonte = celula[i + 1].temp;
        double cpgF;
        double razcpF = 0.;
        double cplF;

        // calculo da energia adicionada no sistema devido a fontes de massa
        if (celula[i + 1].acsr.tipo == 1) { // caso fonte de gas
            tfonte = celula[i + 1].acsr.injg.temp;
            cpgF = celula[i + 1].acsr.injg.FluidoPro.CalorGas(pmed, tfonte);
            razcpF = celula[i + 1].acsr.injg.FluidoPro.ConstAdG(pmed, tfonte);
            cplF = 0.;
        } else if (celula[i + 1].acsr.tipo == 2) { // caso fonte de liquido
            tfonte = celula[i + 1].acsr.injl.temp;
            cpgF = 0.;
            razcpF = 1.;
            cplF = (1. - celula[i + 1].acsr.injl.bet) * celula[i + 1].acsr.injl.FluidoPro.CalorLiq(pmed, tmed) + celula[i + 1].acsr.injl.bet * celula[i + 1].acsr.injl.fluidocol.CalorLiq(pmed, tmed);
        } else if (celula[i + 1].acsr.tipo == 3) { // caso IPR
            tfonte = celula[i + 1].acsr.ipr.Tres;
            cpgF = celula[i + 1].acsr.ipr.FluidoPro.CalorGas(pmed, tmed);
            razcpF = celula[i + 1].acsr.ipr.FluidoPro.ConstAdG(pmed, tmed);
            cplF = celula[i + 1].acsr.ipr.FluidoPro.CalorLiq(pmed, tmed);
        } else if (celula[i + 1].acsr.tipo == 15) { // caso IPR
            tfonte = celula[i + 1].acsr.radialPoro.tRes;
            cpgF = celula[i + 1].acsr.radialPoro.flup.CalorGas(pmed, tmed);
            razcpF = celula[i + 1].acsr.radialPoro.flup.ConstAdG(pmed, tmed);
            cplF = celula[i + 1].acsr.radialPoro.flup.CalorLiq(pmed, tmed);
        } else if (celula[i + 1].acsr.tipo == 16) { // caso IPR
            tfonte = celula[i + 1].acsr.poroso2D.dados.tRes;
            cpgF = celula[i + 1].acsr.poroso2D.dados.flup.CalorGas(pmed, tmed);
            razcpF = celula[i + 1].acsr.poroso2D.dados.flup.ConstAdG(pmed, tmed);
            cplF = celula[i + 1].acsr.poroso2D.dados.flup.CalorLiq(pmed, tmed);
        } else if (celula[i + 1].acsr.tipo == 9 && celula[i + 1].acsr.fontechk.abertura > 1e-6 &&
                   (celula[i + 1].fontemassCR + celula[i + 1].fontemassGR + celula[i + 1].fontemassLR) > 1e-9) {
            // caso vazamento
            tfonte = celula[i + 1].acsr.fontechk.tamb;
            cpgF = celula[i + 1].acsr.fontechk.fluidoPamb.CalorGas(pmed, tmed);
            razcpF = celula[i + 1].acsr.fontechk.fluidoPamb.ConstAdG(pmed, tmed);
            cplF = (1. - celula[i + 1].acsr.fontechk.betISamb) *
                       celula[i + 1].acsr.fontechk.fluidoPamb.CalorLiq(pmed, tmed) +
                   celula[i + 1].acsr.fontechk.betISamb * celula[i + 1].acsr.fontechk.fluidocol.CalorLiq(pmed, tmed);
        } else if (celula[i + 1].acsr.tipo != 0) {

            cpgF = 0.;
            razcpF = 1.;
            cplF = 0.;
        } else {
            cpgF = 0.;
            razcpF = 1.;
            cplF = 0.;
        }

        fontemassL = 0;
        if (celula[i + 1].fontemassLR > 0.)
            fontemassL = celula[i + 1].fontemassLR / dx;
        if (celula[i + 1].fontemassCR > 0.)
            fontemassL += celula[i + 1].fontemassCR / dx;
        fontemassL *= (cplF) * (tfonte - celula[i + 1].temp);

        fontemassG = celula[i + 1].fontemassGR / dx;
        if (fontemassG > 0.)
            fontemassG *= (cpgF / razcpF) * (tfonte - celula[i + 1].temp);
        else
            fontemassG = 0;

        // efeito do calor latente, quando este for solicitado
        double latente = 0.;
        if (celula[i].flui.dVaporMassFraction < (1 - 1e-15) && celula[i].flui.dVaporMassFraction > (1e-15)) {
            if (celula[i + 1].acsr.tipo == 1 || celula[i + 1].acsr.tipo == 2 || celula[i + 1].acsr.tipo == 3 || celula[i + 1].acsr.tipo == 15 || celula[i + 1].acsr.tipo == 16)
                celula[i + 1].FonteMudaFase =
                    0.;
            if (CalcLat > 0 && iterperm != 0 && arq.flashCompleto == 0) {
                latente = -interpolaHLatente(pmed, tmed) * celula[i + 1].FonteMudaFase;
            } else if ((arq.flashCompleto == 1 || arq.flashCompleto == 2) && CalcLat > 0 && iterperm != 0) {
                latente = -(celula[i].flui.EntalpGas(pmed, tmed) -
                            celula[i].flui.EntalpLiq(pmed, tmed)) *
                          celula[i + 1].FonteMudaFase;
            } else
                latente = 0;
        }

        //////trecho sem utilidade////////////////////////////////////////////////////////
        double alfinter;
        double alfinterL;
        if (ugsmed > 0) {
            alfinter = celula[i + 1].alf;
            alfinterL = celula[i + 1].alfL;
        } else {
            alfinter = celula[i + 1].alfR;
            alfinterL = celula[i + 1].alf;
        }
        double delvel;
        if (alfinter > (*vg1dSP).localtiny && alfinter < (1. - (*vg1dSP).localtiny))
            delvel = ugsmed / alfinter - ulsmed / (1. - alfinter);
        else if (alfinter > (*vg1dSP).localtiny)
            delvel = ugsmed;
        else
            delvel = ulsmed;
        double verifica = area * celula[i + 1].pres * 98600 * delvel * (alfinter - alfinterL) / celula[i + 1].dx;

        if (fabs(coefdxT) > (*vg1dSP).localtiny) {
            // parecela de energia relacionada ao ytrabalho de fronteira, energia potencial,
            // energia cinetica, fontes de massa, calor latente e trabalho de eixo:
            double parcenerg1 = dTdLmed * (coefdxP * dpdx - cinetico - (hidro) + (fontemassL + fontemassG) - latente - celula[i + 1].potBT / dxmed) / coefdxT;
            // parcela de energia relacionada aa troca termica
            double parcenerg2 = dTdLmed * (fluxcal) / coefdxT;

            // e feita uma avaliacao se a troca termica esta se dando de maneira muito rapida
            // como avanco da temperatura e explicita, isto pode levar a instabilidade no calculo termico
            // se for verificado que a troca termica esta ocorrendo de maneira rapida, o avanco e
            // feito em um numero maior de passos de uma celula para outra
            int npasso;
            double dxpasso;
            double limiteEstab = fabs(coefdxT) / dTdLmed;
            if (dxmed / (celula[i + 1].calor.resGlob + resanul) < (limiteEstab + 0. * 1000.)) {
                npasso = 1;
                dxpasso = dxmed;
            } else { // resistencia termica e pequena, se determinara em quantos passos se dara
                // o avanco de temperatrura
                npasso = (dxmed / (celula[i + 1].calor.resGlob + resanul)) / (limiteEstab + 0 * 1000) + 1;
                dxpasso = dxmed / npasso; // celula divida em npassos
            }
            double temppasso = celula[i + 1].temp;

            temppasso = dxpasso * (-(-celula[i + 1].temp) / dxpasso + parcenerg1 + parcenerg2); // primeiro avanco
            for (int j = 1; j < npasso; j++) {                                                  // avancos seguintes
                // as pacelas de energia sao mantidas, com excessao do fluxo de calor que e
                // reavalkiado a cada passo:
                celula[i + 1].calor.Tint = temppasso;
                celula[i + 1].calor.Vint = ugsmed + ulsmed;
                condliq = (1. - betmed) * celula[i + 1].flui.CondLiq(pmed, temppasso) + betmed * celula[i + 1].fluicol.CondLiq(pmed, temppasso);
                celula[i + 1].calor.kint = condliq * (1 - alfmed) + celula[i + 1].flui.CondGas(pmed, temppasso) * alfmed;
                celula[i + 1].calor.cpint = cpl * (1 - alfmed) + cpg * alfmed;
                celula[i + 1].calor.rhoint = rhol * (1 - alfmed) + rhog * alfmed;
                viscliq = (1. - betmed) * celula[i + 1].flui.ViscOleo(pmed, temppasso) + betmed * celula[i + 1].fluicol.VisFlu(pmed, temppasso);
                celula[i + 1].calor.viscint = viscliq * (1 - alfmed) * 1.e-3 + celula[i + 1].flui.ViscGas(pmed, temppasso) * alfmed * 1.e-3;
                if (iterperm != 0 && arq.lingas == 1 && (i + 1 <= ColunaAnulaIni && i + 1 >= ColunaAnulaFim)) {
                    int k = ColunaAnulaIni + AnulaColunaIni - (i + 1);
                    double dtext = (celulaG[k].temp - celulaG[k - 1].temp) / npasso;
                    celula[i + 1].calor.Textern1 = celulaG[k].temp + (j - 1) * dtext;
                }
                fluxcal = sinalJ * celula[i + 1].calor.transperm(resanul);
                celula[i + 1].fluxcalmed += fluxcal;
                parcenerg2 = dTdLmed * (fluxcal) / coefdxT;
                temppasso = dxpasso * (-(-temppasso) / dxpasso + parcenerg1 + parcenerg2);
            }

            celula[i].temp = temppasso;
            celula[i + 1].fluxcalmed /= npasso;
            celula[i + 1].fluxcalmed = 1. * celula[i + 1].fluxcalmed;

            if (celula[i].temp < -50.)
                celula[i].temp = -50.;
            if (celula[i].temp > 200.)
                celula[i].temp = 200.;
        } else
            celula[i].temp = celula[i].calor.Textern1;
    } else { // caso em que a velocidade da mistura e muito baixa
        celula[i].temp = celula[i + 1].calor.Textern1;
        if (arq.lingas == 1 && (i + 1 <= ColunaAnulaIni && i + 1 >= ColunaAnulaFim)) {
            int j = ColunaAnulaIni + AnulaColunaIni - (i + 1);
            celula[i + 1].calor.Vextern1 = 100.;
            celula[i + 1].calor.kextern1 = celulaG[j].calor.kint;
            celula[i + 1].calor.cpextern1 = celulaG[j].calor.cpint;
            celula[i + 1].calor.rhoextern1 = celulaG[j].calor.rhoint;
            celula[i + 1].calor.viscextern1 = celulaG[j].calor.viscint;
            double fluxcal = celula[i + 1].calor.transperm(0);
        }
    }
}

void SProd::atualizaPeriPmonProd(int i) {
    if (celula[i].presaux < 0.5)
        celula[i].presaux = 0.5;
    celula[i - 1].presauxR = celula[i].presaux;
    if (i < ncel)
        celula[i + 1].presauxL = celula[i].presaux;

    celula[i - 1].dpB = 0.;
    celula[i - 1].potB = 0.;
    celula[i - 1].potBT = 0.;
    double tmed;
    if (iterperm != 0 && arq.AceleraConvergPerm == 0)
        tmed = (celula[i].dx * celula[i].temp + celula[i].dxL * celula[i].tempL) / (celula[i].dx + celula[i].dxL);
    else
        tmed = celula[i - 1].temp;
    double sinalQ = 1.;
    if (fabs(celula[i - 1].QL + celula[i - 1].QG) > 1e-15)
        sinalQ = fabs(celula[i - 1].QL + celula[i - 1].QG) / (celula[i - 1].QL + celula[i - 1].QG);
    if (celula[i - 1].acsr.tipo == 4 && celula[i - 1].acsr.bcs.freqnova > 1.) {
        double vazmix = fabs(celula[i - 1].QL + celula[i - 1].QG);
        double alf0 = celula[i - 1].alf;
        double bet0 = celula[i - 1].bet;
        double rhomis = (alf0 * celula[i - 1].flui.MasEspGas(celula[i].presaux, tmed) + (1 - alf0) * ((1 - bet0) * celula[i - 1].flui.MasEspLiq(celula[i].presaux, tmed) + bet0 * celula[i - 1].fluicol.MasEspFlu(celula[i].presaux, tmed)));
        double vismis = alf0 * celula[i - 1].flui.ViscGas(celula[i].presaux, tmed) + (1 - alf0) * ((1. - bet0) * celula[i - 1].flui.ViscOleo(celula[i].presaux, tmed) + bet0 * celula[i].fluicol.VisFlu(celula[i].presaux, tmed));
        vazmix *= (86400 / 0.1589876);
        celula[i - 1].acsr.bcs.NovaVis(vismis, rhomis, vazmix);
        celula[i - 1].dpB = sinalQ * 0.3048 * celula[i - 1].acsr.bcs.Hvis * rhomis * 9.82;
        celula[i - 1].potB = celula[i - 1].acsr.bcs.Pvis * 745.7;
        celula[i - 1].potTermo = (1. - celula[i - 1].acsr.bcs.Evis / 100.) * celula[i - 1].potB;
        if (celula[i - 1].acsr.bcs.eficM > 0.)
            celula[i - 1].potBT = (1. + 100. * (1. - celula[i - 1].acsr.bcs.eficM / 100.) / celula[i - 1].acsr.bcs.eficM) * celula[i - 1].potB;
        else
            celula[i - 1].potBT = 0.;
        celula[i - 1].potTermo += celula[i - 1].potBT * (1. - celula[i - 1].acsr.bcs.eficM / 100.) * celula[i - 1].acsr.bcs.fracTermMotorEfic;

    } else if (celula[i - 1].acsr.tipo == 7) {
        celula[i - 1].dpB = sinalQ * celula[i - 1].acsr.delp * 98066.5;
        double bet0 = celula[i - 1].bet;
        double rhog = celula[i - 1].flui.MasEspGas(celula[i].presaux, tmed);
        double rhol = ((1 - bet0) * celula[i - 1].flui.MasEspLiq(celula[i].presaux, tmed) + bet0 * celula[i - 1].fluicol.MasEspFlu(celula[i].presaux, tmed));
        double qgMon = (celula[i - 1].MC - celula[i - 1].Mliqini) / rhog;
        double qlmon = celula[i - 1].Mliqini / rhol;
        double npoli = 1.;
        if (celula[i - 1].acsr.tipoCompGas == 0) {
            npoli = celula[i - 1].flui.ConstAdG(celula[i - 1].pres, celula[i - 1].temp);
            if ((npoli - 1.05) < 1e-2)
                npoli = 1.05;
        } else if (celula[i - 1].acsr.tipoCompGas == 1)
            npoli = celula[i - 1].acsr.fatPoli;
        double Wcomp;
        double Wbomb;
        Wbomb = (100. / celula[i - 1].acsr.eficLiq) * celula[i - 1].dpB * qlmon;
        double wcompiso = -celula[i].presaux * 98066.5 * qgMon * log(celula[i].presaux / (celula[i].presaux + celula[i - 1].acsr.delp));
        if (celula[i - 1].acsr.tipoCompGas != 2)
            Wcomp = -(celula[i].presaux * 98066.5 * qgMon / (1. - npoli)) *
                    (pow(1 + sinalQ * celula[i - 1].acsr.delp / celula[i].presaux, (npoli - 1.) / npoli) - 1.);
        else
            Wcomp = wcompiso;
        Wcomp *= (100. / celula[i - 1].acsr.eficGas);
        celula[i - 1].potB = sinalQ * (Wcomp + Wbomb);
        celula[i - 1].potTermo = sinalQ * ((1. - celula[i - 1].acsr.eficLiq / 100.) * Wbomb + (1. - celula[i - 1].acsr.eficGas / 100.) * Wcomp);
        celula[i - 1].potBT = celula[i - 1].potB;
    } else if (celula[i - 1].acsr.tipo == 17 && celula[i - 1].acsr.multibcs.freqnova > 1.) {
        double alf0 = celula[i - 1].alf;
        double bet0 = celula[i - 1].bet;
        celula[i - 1].acsr.multibcs.flui = celula[i - 1].flui;
        celula[i - 1].acsr.multibcs.fluicol = celula[i - 1].fluicol;
        celula[i - 1].acsr.multibcs.marchaMultiBcs(celula[i - 1].QG, celula[i - 1].QL, celula[i].presaux, tmed, alf0, bet0);
        celula[i - 1].dpB = celula[i - 1].acsr.multibcs.dpB * 98066.52;
        celula[i - 1].potB = celula[i - 1].acsr.multibcs.potBT;
        celula[i - 1].potBT = celula[i - 1].acsr.multibcs.potBT;
        celula[i - 1].potTermo = celula[i - 1].acsr.multibcs.potTermo;
        celula[i - 1].potTermo += celula[i - 1].potBT * (1. - celula[i - 1].acsr.multibcs.eficM / 100.) * celula[i - 1].acsr.multibcs.fracTermMotorEfic;
    }
}
void SProd::atualizaPeriPjusProd(int i) {
    if (celula[i].pres < 0.1)
        celula[i].pres = 0.1;
    celula[i - 1].presR = celula[i].pres;
    if (i < ncel) {
        celula[i + 1].presL = celula[i].pres;
        celula[i + 1].presLini = celula[i + 1].presL;
    }
    celula[i].presini = celula[i].pres;
}
void SProd::atualizaPeriTempProd(int i) {
    if (i > 0)
        celula[i - 1].tempR = celula[i].temp;
    if (i < ncel)
        celula[i + 1].tempL = celula[i].temp;
    celula[i].tempini = celula[i].temp;
}

void SProd::calcTempFim() {

    if (arq.chokep.abertura[0] <= 0.6 && arq.chokep.abertura[0] > (*vg1dSP).localtiny && presfim < pGSup) {
        double masentrada = celula[ncel - 1].MR;
        double massgas = celula[ncel - 1].MR - celula[ncel - 1].MliqiniR;
        double rholp = celula[ncel].flui.MasEspLiq(celula[ncel].pres, celula[ncel].temp);
        double rholc = celula[ncel].fluicol.MasEspFlu(celula[ncel].pres, celula[ncel].temp);
        double betEF = celula[ncel].bet;
        double tit = fabs(massgas / masentrada);

        double jtlM = (1. - betEF) * celula[ncel].flui.JTL(celula[ncel].pres, celula[ncel].temp) - betEF / rholc; // alteraacao2
        double jtgM = celula[ncel].flui.JTG(celula[ncel].pres, celula[ncel].temp);
        tempSup = celula[ncel].temp + ((1. - tit) * jtlM + tit * jtgM) * (celula[ncel].pres - celula[ncel].pres); //????????
                                                                                                                  //???????????????????????????????celula[ncel].pres - celula[ncel].pres????????????????????????????????????????????

    } else
        tempSup = celula[ncel - 1].temp;
}

double SProd::delpGasPerm(int i) {
    double dx = 0.5 * celulaG[i].dx0;
    double dia = celulaG[i].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double si = celulaG[i].duto.peri;
    double rhog = celulaG[i].flui.MasEspGas(celulaG[i].pres, celulaG[i].temp);

    double VGasmedR;
    VGasmedR = celulaG[i - 1].VGasR;

    double vel1 = VGasmedR / celulaG[i].u1L;

    double visc = celulaG[i].flui.ViscGas(celulaG[i].pres, celulaG[i].temp);

    double re1;
    if (celulaG[i].duto.revest == 0)
        re1 = celulaG[i].Rey(celulaG[i].duto.a, vel1, rhog, visc);
    else {
        double dhid = 4 * area / si;
        re1 = celulaG[i].Rey(dhid, vel1, rhog, visc);
    }
    double f1 = celulaG[i].fric(re1, celulaG[i].duto.rug / dia);
    double gradfric = celulaG[i].dPdLFric * (0.5 * f1 * rhog * (fabs(vel1) * vel1) * si * dx / area);
    double gradhidro = celulaG[i].dPdLHidro * (9.82 * sin(celulaG[i].duto.teta) * rhog * dx);

    double difpres = (gradfric + gradhidro) / 98066.5;
    double pmed = celulaG[i].pres + difpres;

    double tmed;
    tmed = (celulaG[i].dx0 * celulaG[i].temp + celulaG[i].dxL * celulaG[i].tempL) / (celulaG[i].dx0 + celulaG[i].dxL);

    dx = 0.5 * celulaG[i].dxL;
    dia = celulaG[i].dutoL.a;
    area = 0.25 * M_PI * dia * dia;
    si = celulaG[i].dutoL.peri;
    rhog = celulaG[i].flui.MasEspGas(pmed, tmed);

    VGasmedR = celulaG[i - 1].VGasR;
    vel1 = VGasmedR / (rhog * area);

    if (i > 0)
        visc = celulaG[i - 1].flui.ViscGas(pmed, tmed);
    else
        visc = celulaG[i].flui.ViscGas(pmed, tmed);

    if (celulaG[i].dutoL.revest == 0) {
        if (i > 0)
            re1 = celulaG[i].Rey(celulaG[i].dutoL.a, vel1, rhog, visc);
        else
            re1 = celulaG[i - 1].Rey(celulaG[i].dutoL.a, vel1, rhog, visc);
    } else {
        double dhid = 4 * area / si;
        if (i > 0)
            re1 = celulaG[i - 1].Rey(dhid, vel1, rhog, visc);
        else
            re1 = celulaG[i].Rey(dhid, vel1, rhog, visc);
    }
    double dpFLoc;
    double dpHLoc;
    if (i > 0) {
        f1 = celulaG[i - 1].fric(re1, celulaG[i].dutoL.rug / dia);
        dpFLoc = celulaG[i - 1].dPdLFric;
        dpHLoc = celulaG[i - 1].dPdLHidro;
    } else {
        f1 = celulaG[i].fric(re1, celulaG[i].dutoL.rug / dia);
        dpFLoc = celulaG[i].dPdLFric;
        dpHLoc = celulaG[i].dPdLHidro;
    }
    gradfric = dpFLoc * (0.5 * f1 * rhog * (fabs(vel1) * vel1) * si * dx / area);
    gradhidro = dpHLoc * (9.82 * sin(celulaG[i].dutoL.teta) * rhog * dx);
    difpres += (gradfric + gradhidro) / 98066.5;
    return difpres;
}

double SProd::delpInjPerm(int i) {
    double dx = 0.5 * celula[i].dx;
    double dia = celula[i].duto.a;
    double area = 0.25 * M_PI * dia * dia;
    double si = celula[i].duto.peri;
    double tmed;
    tmed = (celula[i].dx * celula[i].temp + celula[i].dxL * celula[i - 1].temp) / (celula[i].dx + celula[i].dxL);
    double rho = celula[i].fluicol.MasEspFlu(celula[i].presaux, tmed);

    double vel1 = celula[i - 1].QL / (area);

    double visc = celula[i].fluicol.VisFlu(celula[i].presaux, tmed);

    double re1;
    if (celula[i].duto.revest == 0)
        re1 = celula[i].Rey(celula[i].duto.a, vel1, rho, visc);
    else {
        double dhid = 4 * area / si;
        re1 = celula[i].Rey(dhid, vel1, rho, visc);
    }
    double f1 = celula[i].fric(re1, celula[i].duto.rug / dia);
    double gradfric = celula[i].dPdLFric * 0.5 * f1 * rho * (fabs(vel1) * vel1) * si * dx / area;
    double gradhidro = celula[i].dPdLHidro * 9.82 * sin(celula[i].duto.teta) * rho * dx;

    double difpres = (gradfric + gradhidro) / 98066.5;
    double pmed = celula[i - 1].pres;

    dx = 0.5 * celula[i].dxL;
    dia = celula[i].dutoL.a;
    area = 0.25 * M_PI * dia * dia;
    si = celula[i].dutoL.peri;
    tmed = celula[i - 1].temp;
    rho = celula[i].fluicol.MasEspFlu(pmed, tmed);

    if (i > 1)
        vel1 = celula[i - 2].QL / (area);

    visc = celula[i].fluicol.VisFlu(pmed, tmed);

    if (celula[i].dutoL.revest == 0) {
        if (i > 0)
            re1 = celula[i].Rey(celula[i].dutoL.a, vel1, rho, visc);
        else
            re1 = celula[i - 1].Rey(celula[i].dutoL.a, vel1, rho, visc);
    } else {
        double dhid = 4 * area / si;
        if (i > 0)
            re1 = celula[i - 1].Rey(dhid, vel1, rho, visc);
        else
            re1 = celula[i].Rey(dhid, vel1, rho, visc);
    }
    f1 = celula[i - 1].fric(re1, celula[i].dutoL.rug / dia);
    gradfric = celula[i - 1].dPdLFric * 0.5 * f1 * rho * (fabs(vel1) * vel1) * si * dx / area;
    gradhidro = celula[i - 1].dPdLHidro * 9.82 * sin(celula[i].dutoL.teta) * rho * dx;
    difpres += (gradfric + gradhidro) / 98066.5;
    return difpres;
}

void SProd::RenovaPresGasPerm(int i) {

    double dx = 0.5 * celulaG[i].dxL;
    double dia = celulaG[i].dutoL.a;
    double area = 0.25 * M_PI * dia * dia;
    double si = celulaG[i].dutoL.peri;
    double rhog = celulaG[i - 1].flui.MasEspGas(celulaG[i - 1].pres, celulaG[i - 1].temp);

    double VGasmedL;
    VGasmedL = celulaG[i - 1].VGasR;
    double vel1 = VGasmedL / celulaG[i - 1].u1L;

    double visc = celulaG[i - 1].flui.ViscGas(celulaG[i - 1].pres, celulaG[i - 1].temp);

    double re1;
    if (celulaG[i].dutoL.revest == 0)
        re1 = celulaG[i - 1].Rey(celulaG[i].dutoL.a, vel1, rhog, visc);
    else {
        double dhid = 4 * area / si;
        re1 = celulaG[i - 1].Rey(dhid, vel1, rhog, visc);
    }
    double f1 = celulaG[i - 1].fric(re1, celulaG[i].dutoL.rug / dia);
    double gradfric = celulaG[i - 1].dPdLFric * 0.5 * f1 * rhog * (fabs(vel1) * vel1) * si * dx / area;
    double gradhidro = celulaG[i - 1].dPdLHidro * 9.82 * sin(celulaG[i].dutoL.teta) * rhog * dx;
    celulaG[i - 1].termoFric = gradfric / dx;
    celulaG[i - 1].termoHidro = gradhidro / dx;

    double pmed = celulaG[i - 1].pres - (gradfric + gradhidro) / 98066.5;

    double tmed;
    if (iterperm != 0)
        tmed = (celulaG[i].dxL * celulaG[i - 1].temp + celulaG[i].dx0 * celulaG[i].temp) /
               (celulaG[i].dxL + celulaG[i].dx0);
    else
        tmed = celulaG[i - 1].temp;
    tmed = celulaG[i - 1].temp;

    dx = 0.5 * celulaG[i].dx0;
    dia = celulaG[i].duto.a;
    area = 0.25 * M_PI * dia * dia;
    si = celulaG[i].duto.peri;
    double razdx = celulaG[i].dx0 / (celulaG[i].dx0 + celulaG[i].dxL);
    rhog = celulaG[i].flui.MasEspGas(pmed, tmed);
    double VarArea = 1 / pow(celulaG[i].dutoL.area, 2.) - 1 / pow(celulaG[i].duto.area, 2.);
    double dpDina = 0.5 * VGasmedL * VGasmedL * VarArea / rhog;

    VGasmedL = celulaG[i - 1].VGasR;
    vel1 = VGasmedL / (rhog * area);

    visc = celulaG[i].flui.ViscGas(pmed, tmed);

    if (celulaG[i].duto.revest == 0)
        re1 = celulaG[i].Rey(celulaG[i].duto.a, vel1, rhog, visc);
    else {
        double dhid = 4 * area / si;
        re1 = celulaG[i].Rey(dhid, vel1, rhog, visc);
    }
    f1 = celulaG[i].fric(re1, celulaG[i].duto.rug / dia);
    gradfric = celulaG[i].dPdLFric * 0.5 * f1 * rhog * (fabs(vel1) * vel1) * si * dx / area;
    gradhidro = celulaG[i].dPdLHidro * 9.82 * sin(celulaG[i].duto.teta) * rhog * dx;

    celulaG[i].pres = pmed - (gradfric + gradhidro - dpDina) / 98066.5;
    celulaG[i - 1].presR = celulaG[i].pres;
    if (i < ncelGas)
        celulaG[i + 1].presL = celulaG[i].pres;
    celulaG[i].presini = celulaG[i].pres;
}

void SProd::calcVazGasPerm(int i) {

    int nvalv = arq.nvalvgas;
    int match = 0;
    for (int j = 0; j < nvalv; j++) {
        if (posicVGLG[j] == i) {
            match = 1;
            chokeVGL[j].presEstag = celulaG[posicVGLG[j]].pres;
            chokeVGL[j].presGarg = (celula[posicVGLP[j]].pres - chokeVGL[j].presEstag * chokeVGL[j].frec) /
                                   (1. - chokeVGL[j].frec);
            chokeVGL[j].tempEstag = celulaG[posicVGLG[j]].temp;
            if (celula[posicVGLP[j]].pres < celulaG[posicVGLG[j]].pres)
                celulaG[posicVGLG[j]].massfonteCH = chokeVGL[j].massica();
            else
                celulaG[posicVGLG[j]].massfonteCH = 0.;
            if (chokeVGL[j].tipo == 1) {
                double abre = areaValvCali(chokeVGL[j].pcalib * 14.223595, chokeVGL[j].tcalib, (chokeVGL[j].presEstag - 1.033211) * 14.223595,
                                           (chokeVGL[j].presGarg - 1.033211) * 14.223595, chokeVGL[j].dextern, chokeVGL[j].areagarg,
                                           chokeVGL[j].areagarg / chokeVGL[j].areafole,
                                           1.8 * chokeVGL[j].tempEstag + 32);
                celulaG[posicVGLG[j]].massfonteCH *= abre;
            }

            if (i > 0)
                celulaG[i].VGasR = celulaG[i - 1].VGasR - celulaG[i].massfonteCH;
            else
                celulaG[i].VGasR = celulaG[i].massfonteCH;

            int posGLP = posicVGLP[j];
            int posGLG = posicVGLG[j];
            double tg;
            celula[posGLP].acsr.injg.QGas = celulaG[posGLG].massfonteCH * 86400. /
                                            (1.225 * celulaG[posGLG].flui.Deng); // celulaG[posGLG].flui.MasEspGas(1.,15.);
            if (chokeVGL[j].presEstag > chokeVGL[j].presGarg) {
                tg = TempDescGL(j);
            } else
                tg = celula[posGLP].temp;
            celula[posGLP].acsr.injg.temp = tg;
            if (celula[posGLP].acsr.injg.temp < -50)
                celula[posGLP].acsr.injg.temp = -50;
            celula[posGLP].fontemassGR = celulaG[posGLG].massfonteCH;

            if (celulaG[i].VGasR <= 0. && i < ncelGas) {
                celulaG[i].VGasR = 0.;
                for (int j1 = j + 1; j1 < nvalv; j1++) {
                    celulaG[posicVGLG[j1]].massfonteCH = 0;
                    celula[posicVGLP[j1]].fontemassGR = 0.;
                }
            }

            celulaG[i - 1].VGasRR = celulaG[i].VGasR;
            if (i < ncelGas)
                celulaG[i + 1].VGasL = celulaG[i].VGasR;
        }
    }
    if (match == 0) {
        if (i > 0)
            celulaG[i].VGasR = celulaG[i - 1].VGasR - 0 * celulaG[i].massfonteCH;
        else
            celulaG[i].VGasR = celulaG[i - 1].VGasR + 0 * celulaG[i].massfonteCH;
        celulaG[i - 1].VGasRR = celulaG[i].VGasR;
        if (i < ncelGas)
            celulaG[i + 1].VGasL = celulaG[i].VGasR;
    }
}

void SProd::IniciaVazValvGasPerm(int i) {
    int nvalv = arq.nvalvgas;
    double vazvalv;
    double presRev = presiniG;
    double tempRev = celulaG[0].calor.Textern1;
    for (int j = 0; j < nvalv; j++) {
        int posGLP = posicVGLP[j];
        int posGLG = posicVGLG[j];
        if (i == posGLP) {
            if (celulaG[0].tipoCC == 1) {
                vazvalv = (arq.gasinj.vazgas[0] * celulaG[0].flui.MasEspGas(1., 15.) / 86400.) / nvalv;
                celulaG[posicVGLG[j]].massfonteCH = vazvalv;
                celula[posGLP].acsr.injg.QGas = celulaG[posGLG].massfonteCH * 86400. /
                                                celulaG[posGLG].flui.MasEspGas(1., 15.);
                celula[posGLP].acsr.injg.temp = tempRev;
                celula[posGLP].fontemassGR = celulaG[posGLG].massfonteCH;
            } else {
                presRev = celulaG[posGLG].pres;
                chokeVGL[j].presEstag = presRev;
                chokeVGL[j].presGarg = celula[posicVGLP[j]].pres;
                chokeVGL[j].tempEstag = tempRev;
                if (celula[posicVGLP[j]].pres < presRev)
                    celulaG[posicVGLG[j]].massfonteCH = chokeVGL[j].massica();
                else
                    celulaG[posicVGLG[j]].massfonteCH = 0.;
                celula[posGLP].acsr.injg.QGas = celulaG[posGLG].massfonteCH * 86400. /
                                                celulaG[posGLG].flui.MasEspGas(1., 15.);
                celula[posGLP].acsr.injg.temp = tempRev;
                celula[posGLP].fontemassGR = celulaG[posGLG].massfonteCH;
            }
        }
    }
}

void SProd::RenovaTempGasPerm(int i) {
    if (semTermo == 0) {
        if (((i) < AnulaColunaIni || (i) > AnulaColunaFim)) {
            double dx = celulaG[i].dx0;
            double dxmed = 0.5 * (celulaG[i].dx0 + celulaG[i - 1].dx0);
            double dTdLMed = (celulaG[i].dx0 * celulaG[i].dTdLCor + celulaG[i - 1].dx0 * celulaG[i - 1].dTdLCor) / (celulaG[i].dx0 + celulaG[i - 1].dx0);
            double area = celulaG[i].duto.area;
            double ugsmed;
            double rhog = celulaG[i - 1].flui.MasEspGas(celulaG[i - 1].pres, celulaG[i - 1].temp);
            ugsmed = celulaG[i - 1].VGasR / celulaG[i - 1].u1L;
            double cpg = celulaG[i - 1].flui.CalorGas(celulaG[i - 1].pres, celulaG[i - 1].temp);
            double cvg = celulaG[i - 1].flui.CalorGasVolMod(celulaG[i - 1].pres, celulaG[i - 1].temp);
            double jtg = celulaG[i - 1].flui.JTG(celulaG[i - 1].pres, celulaG[i - 1].temp);
            double hidro = (rhog * ugsmed) * area * 9.82 * sin(celulaG[i].duto.teta);

            celulaG[i - 1].calor.Tint = celulaG[i - 1].temp;
            celulaG[i - 1].calor.Vint = ugsmed;
            celulaG[i - 1].calor.kint = celulaG[i - 1].flui.CondGas(celulaG[i - 1].pres, celulaG[i - 1].temp);
            celulaG[i - 1].calor.cpint = cpg;
            celulaG[i - 1].calor.rhoint = rhog;
            celulaG[i - 1].calor.viscint = celulaG[i - 1].flui.ViscGas(celulaG[i - 1].pres, celulaG[i - 1].temp) * 1.e-3;
            celulaG[i - 1].fluxcal = celulaG[i - 1].calor.transperm();
            double flucal = celulaG[i - 1].fluxcal;
            double flucalCol = 0.;

            double razdx;
            if (i > 0)
                razdx = dx / (dx + celulaG[i - 1].dx0);
            else
                razdx = 0.5;
            double coefdxT = rhog * ugsmed * cpg * area;
            double coefdxP = rhog * ugsmed * jtg * area;
            double dpdx;
            dpdx = 2. * (celulaG[i].pres - ((1 - razdx) * celulaG[i - 1].pres + razdx * celulaG[i].pres)) * 98600. / dx;

            double cinetico;
            double deljmix = 0.;
            double rhomix = rhog;
            double ugsmed0 = celulaG[i - 1].VGasL / celulaG[i - 1].u1L;
            deljmix = (ugsmed - ugsmed0) / dx;

            if (arq.nCompTotalUnidadesG / dx < 1e6)
                cinetico = rhomix * area * ugsmed * ugsmed * deljmix;
            else
                cinetico = 0.;

            double fontemassG = 0.;
            double fontemassL = 0.;

            double fator = 0.;
            if ((*vg1dSP).lixo5 < 1000.)
                fator = 0.;

            if (ugsmed > 1e-3 && (celulaG[i].duto.a / celulaG[i - 1].duto.a > 0.5 &&
                                  celulaG[i - 1].duto.a / celulaG[i].duto.a > 0.5)) {
                double parcenerg1 = dTdLMed * (coefdxP * dpdx - cinetico - hidro + fontemassL + fontemassG) / coefdxT;
                double parcenerg2 = dTdLMed * (flucal + flucalCol) / coefdxT;
                int npasso;
                double dxpasso;
                if (dxmed / celulaG[i - 1].calor.resGlob < 1000.) {
                    npasso = 0.;
                    dxpasso = dxmed;
                } else {
                    npasso = 2 * (dxmed / celulaG[i - 1].calor.resGlob) / 1000 + 1;
                    dxpasso = dxmed / npasso;
                }
                double temppasso = celulaG[i - 1].temp;

                temppasso = dxpasso * (-(-celulaG[i - 1].temp) / dxpasso + parcenerg1 + parcenerg2);
                for (int j = 1; j < npasso; j++) {
                    celulaG[i - 1].calor.Tint = temppasso;
                    celulaG[i - 1].calor.Vint = ugsmed;
                    celulaG[i - 1].calor.kint = celulaG[i - 1].flui.CondGas(celulaG[i - 1].pres, temppasso);
                    celulaG[i - 1].calor.cpint = cpg;
                    celulaG[i - 1].calor.rhoint = rhog;
                    celulaG[i - 1].calor.viscint = celulaG[i - 1].flui.ViscGas(celulaG[i - 1].pres, temppasso) * 1.e-3;
                    celulaG[i - 1].fluxcal = celulaG[i - 1].calor.transperm();
                    flucal = celulaG[i - 1].fluxcal;
                    parcenerg2 = dTdLMed * (flucal + flucalCol) / coefdxT;
                    temppasso = dxpasso * (-(-temppasso) / dxpasso + parcenerg1 + parcenerg2);
                }

                celulaG[i].temp = temppasso;
            } else {
                celulaG[i].temp = celulaG[i].calor.Textern1;
            }

            if (celulaG[i].temp < -50.)
                celulaG[i].temp = -50.;
            if (celulaG[i].temp > 200.)
                celulaG[i].temp = 200.;

            if (i > 0)
                celulaG[i - 1].tempR = celulaG[i].temp;
            if (i < ncelGas)
                celulaG[i + 1].tempL = celulaG[i].temp;
        } else {
            if (verificaAcop == 1) {
                int kconecte = (i)-AnulaColunaIni;
                int iconecte = ColunaAnulaIni - kconecte;
                int iconecte2 = iconecte;
                if (iconecte2 == ncel)
                    iconecte2 -= 1;

                celulaG[i].temp = celula[iconecte2].calor.resGlob * celula[iconecte].calor.fluxFim + celula[iconecte].temp;

                if (arq.correcaoContracorPerm == 1) {

                    double dxmed = 0.5 * (celulaG[i].dx0 + celulaG[i - 1].dx0);
                    double cpg = celulaG[i - 1].flui.CalorGas(celulaG[i - 1].pres, celulaG[i - 1].temp);
                    double invCap = 1. / (celula[iconecte].calor.cpint * celula[iconecte].MC) + 1. / (cpg * celulaG[i - 1].VGasR);
                    double templog = -exp(-(1. / celula[iconecte2].calor.resGlob) * (1 + 0 * celula[iconecte].duto.peri) * dxmed * invCap) *
                                         (celula[iconecte2 + 1].temp - celulaG[i - 1].temp) +
                                     celula[iconecte2].temp;

                    if (templog < celulaG[i].temp)
                        celulaG[i].temp = templog;
                }
            }

            if (celulaG[i].temp < -50.)
                celulaG[i].temp = -50.;
            if (celulaG[i].temp > 200.)
                celulaG[i].temp = 200.;

            if (i > 0)
                celulaG[i - 1].tempR = celulaG[i].temp;
            if (i < ncelGas)
                celulaG[i + 1].tempL = celulaG[i].temp;

            double ugsmed;
            ugsmed = celulaG[i - 1].VGasR / celulaG[i - 1].u1L;
            double rhog = celulaG[i - 1].flui.MasEspGas(celulaG[i - 1].pres, celulaG[i - 1].temp);
            double cpg = celulaG[i - 1].flui.CalorGas(celulaG[i - 1].pres, celulaG[i - 1].temp);

            celulaG[i].calor.Tint = celulaG[i].temp;
            celulaG[i].calor.Vint = ugsmed;
            celulaG[i].calor.kint = celulaG[i - 1].flui.CondGas(celulaG[i - 1].pres, celulaG[i - 1].temp);
            celulaG[i].calor.cpint = cpg;
            celulaG[i].calor.rhoint = rhog;
            celulaG[i].calor.viscint = celulaG[i - 1].flui.ViscGas(celulaG[i - 1].pres, celulaG[i - 1].temp) * 1.e-3;
            celulaG[i].fluxcal = celulaG[i].calor.transperm();
        }
    } else {
        celulaG[i].temp = celulaG[i].calor.Textern1;
    }
}

void SProd::conectaColunaPerm() {
    for (int i = ColunaAnulaIni; i >= ColunaAnulaFim; i--) {
        int j = ColunaAnulaIni + AnulaColunaIni - i;

        celula[i].calor.Textern2 = celulaG[j].calor.Textern1;
        celula[i].calor.colunaDia = celulaG[j].duto.dia;

        double cpg = celulaG[j].flui.CalorGas(celulaG[j].pres, celulaG[j].temp);
        double rhog = celulaG[j].flui.MasEspGas(celulaG[j].pres, celulaG[j].temp);
        celula[i].calor.Textern1 = celulaG[j].calor.Textern1;
        celula[i].calor.Vextern1 = celulaG[j].VGasR / celulaG[j].u1L;
        celula[i].calor.kextern1 = celulaG[j].flui.CondGas(celulaG[j].pres, celulaG[j].temp);
        celula[i].calor.cpextern1 = cpg;
        celula[i].calor.rhoextern1 = rhog;
        celula[i].calor.viscextern1 = celulaG[j].flui.ViscGas(celulaG[j].pres, celulaG[j].temp) * 1.e-3;
    }
}

void SProd::IniciaconectaColunaPerm() {
    for (int i = ColunaAnulaIni; i >= ColunaAnulaFim; i--) {
        int j = ColunaAnulaIni + AnulaColunaIni - i;
        celula[i].calor.Textern2 = celulaG[j].calor.Textern2;
        celula[i].calor.colunaDia = celulaG[j].duto.dia;

        double cpg = celulaG[j].flui.CalorGas(celula[i].pres, arq.celg[j].textern);
        double rhog = celulaG[j].flui.MasEspGas(celula[i].pres, arq.celg[j].textern);
        celula[i].calor.Textern1 = celulaG[j].calor.Textern1;
        celula[i].calor.Vextern1 = 1.;
        celula[i].calor.kextern1 = celulaG[j].flui.CondGas(celula[i].pres, arq.celg[j].textern);
        celula[i].calor.cpextern1 = cpg;
        celula[i].calor.rhoextern1 = rhog;
        celula[i].calor.viscextern1 = 0.16 * 1.e-3;
    }
}

void SProd::atualizaProp() {
    for (int i = 0; i <= ncel; i++) {
        celula[i].rpC = celula[i].rpCi =
            celula[i].flui.MasEspLiq(celula[i].pres, celula[i].temp);
        celula[i].rgC = celula[i].rgCi =
            celula[i].flui.MasEspGas(celula[i].pres, celula[i].temp);
        celula[i].rcC = celula[i].rcCi =
            celula[i].fluicol.MasEspFlu(celula[i].pres, celula[i].temp);
        celula[i].rpL = celula[i].rpLi =
            celula[i].flui.MasEspLiq(celula[i].presL, celula[i].tempL);
        celula[i].rgL = celula[i].rgLi =
            celula[i].flui.MasEspGas(celula[i].presL, celula[i].tempL);
        celula[i].rcL = celula[i].rcLi =
            celula[i].fluicol.MasEspFlu(celula[i].presL, celula[i].tempL);

        celula[i].mipC = celula[i].flui.ViscOleo(celula[i].pres, celula[i].temp);
        celula[i].migC = celula[i].flui.ViscGas(celula[i].pres, celula[i].temp);
        celula[i].micC = celula[i].fluicol.VisFlu(celula[i].pres, celula[i].temp);

        if (i > 0) {
            celula[i - 1].rpR = celula[i - 1].rpRi = celula[i].rpC;
            celula[i - 1].rgR = celula[i - 1].rgRi = celula[i].rgC;
            celula[i - 1].rcR = celula[i - 1].rcRi = celula[i].rcC;

            celula[i - 1].mipR = celula[i].mipC;
            celula[i - 1].migR = celula[i].migC;
            celula[i - 1].micR = celula[i].micC;
        }
        if (i == ncel) {
            celula[i].rpR = celula[i].rpRi = celula[i].rpC;
            celula[i].rgR = celula[i].rgRi = celula[i].rgC;
            celula[i].rcR = celula[i].rcRi = celula[i].rcC;

            celula[i].mipR = celula[i].mipC;
            celula[i].migR = celula[i].migC;
            celula[i].micR = celula[i].micC;
        }
    }
}

void SProd::atualizaVelTermPerm() {
    for (int i = 0; i <= ncel; i++) {
        double dia = celula[i].duto.a;
        double area = 0.25 * M_PI * dia * dia;
        double alfmed = celula[i].alf;
        double betmed = celula[i].bet;
        double rhol = (1. - betmed) * celula[i].rpC + betmed * celula[i].rcC;
        double rhog = celula[i].rgC;
        double cpl = (1. - betmed) * celula[i].flui.CalorLiq(celula[i].presini, celula[i].temp) + betmed * celula[i].fluicol.CalorLiq(celula[i].presini, celula[i].temp);
        double cvl = cpl;
        double cpg = celula[i].flui.CalorGas(celula[i].presini, celula[i].temp);
        double cvg = celula[i].flui.CalorGasVolMod(celula[i].presini, celula[i].temp, celula[i].rgC);
        double coefTempo = (rhol * (1 - alfmed) * cvl + rhog * alfmed * cvg) * area;
        double ugsmed = celula[i].QG / area;
        double ulsmed = celula[i].QL / area;
        double coefdxT = (rhol * ulsmed * cpl + rhog * ugsmed * cpg) * area;
        celula[i].VTemper = coefdxT / coefTempo;
    }
}

void SProd::calcDTPseudoTrans() {
    dt = 100.;
    for (int i = 0; i <= ncel; i++) {
        double jmix = 0.;
        double dtaux;
        if (fabs(celula[i].VTemper) > jmix)
            jmix = fabs(celula[i].VTemper);
        if (fabs(jmix) > 1e-5)
            dtaux = 1.0 * celula[i].dx / jmix;
        else
            dtaux = dt;
        if (dtaux < dt)
            dt = dtaux;
    }
    if (arq.lingas == 1) {
        for (int i = 0; i <= ncelGas; i++) {
            double dtaux;
            double jmix = fabs(celulaG[i].VGasR / celulaG[i].u1L);
            if (fabs(jmix) > 1e-5)
                dtaux = celulaG[i].dx0 / jmix;
            else
                dtaux = dt;
            if (dtaux < dt)
                dt = dtaux;
        }
    }
    dt = 0.8 * dt;
    for (int i = 0; i <= ncel; i++)
        celula[i].dt = dt;
    if (arq.lingas == 1) {
        for (int i = 0; i <= ncelGas; i++)
            celulaG[i].dt = dt;
    }
}
double SProd::marchaInjPerm1(double chute) {

    int corrigechute = 1;

    if (arq.flashCompleto < 1)
        celula[0].temp = celula[0].acsr.injl.temp;
    else
        celula[0].temp = celula[0].acsr.injg.temp;
    double alfini = 0.;
    double betini = 1.;
    double delp = 0.;
    if (arq.condpocinj.CC == 1 || arq.condpocinj.CC == 2 || arq.condpocinj.CC == 3) {
        if ((chokeSup.AreaGarg / chokeSup.AreaTub) >= 0.6) {
            if (arq.flashCompleto < 1) {
                celula[0].acsr.injl.QLiq = chute;
            } else {
                celula[0].acsr.injg.QGas = chute;
                if (celula[0].acsr.injg.seco == 1) {
                    alfini = 1.;
                    betini = 0.;
                } else {
                    if (arq.flashCompleto == 2) {
                        if (arq.tabelaDinamica == 0)
                            celula[0].flui.atualizaPropComp(pGSup, celula[0].acsr.injg.temp, -1, NULL, NULL, arq.pocinjec);
                        celula[0].acsr.injg.FluidoPro.atualizaPropComp(pGSup, celula[0].acsr.injg.temp, -1, NULL, NULL, arq.pocinjec);
                    }
                    double masgas = celula[0].acsr.injg.VMas(pGSup, celula[0].acsr.injg.temp);
                    double tit;
                    if (arq.flashCompleto != 2)
                        tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
                    else
                        tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
                    double masT = masgas / tit;
                    tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(pGSup, celula[0].acsr.injg.temp);
                    double qgas = masT * tit /
                                  celula[0].acsr.injg.FluidoPro.MasEspGas(pGSup, celula[0].acsr.injg.temp);
                    double qliq = masT * (1. - tit) /
                                  celula[0].acsr.injg.FluidoPro.MasEspLiq(pGSup, celula[0].acsr.injg.temp);
                    alfini = qgas / (qliq + qgas);
                    betini = 0.;
                }
            }
        } else {
            if (arq.flashCompleto < 1) {
                celula[0].acsr.injl.QLiq = chute;
                delp = (1 / (celula[0].fluicol.MasEspFlu(pGSup, celula[0].acsr.injl.temp) * chokeSup.cdchk * 2.)) * pow((chute * celula[0].fluicol.MasEspFlu(1.01, 15.) / 86400) / (chokeSup.AreaGarg), 2.) / 98066.5;
            } else {
                celula[0].acsr.injg.QGas = chute;
                if (arq.flashCompleto == 2) {
                    if (arq.tabelaDinamica == 0)
                        celula[0].flui.atualizaPropComp(pGSup, celula[0].acsr.injg.temp, -1, NULL, NULL, arq.pocinjec);
                    celula[0].acsr.injg.FluidoPro.atualizaPropComp(pGSup, celula[0].acsr.injg.temp, -1, NULL, NULL, arq.pocinjec);
                }
                double rhogstd = celula[0].flui.Deng * 1.225;
                delp = (1 / (celula[0].flui.MasEspGas(pGSup, celula[0].acsr.injg.temp) * chokeSup.cdchk * 2.)) * pow((chute * rhogstd / 86400) / (chokeSup.AreaGarg), 2.) / 98066.5;
                if (celula[0].acsr.injg.seco == 1) {
                    alfini = 1.;
                    betini = 0.;
                } else {
                    double masgas = celula[0].acsr.injg.VMas(pGSup, celula[0].acsr.injg.temp);
                    double tit;
                    if (arq.flashCompleto < 2)
                        tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
                    else
                        tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
                    double masT = masgas / tit;
                    tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(pGSup, celula[0].acsr.injg.temp);
                    double qgas = masT * tit /
                                  celula[0].acsr.injg.FluidoPro.MasEspGas(pGSup, celula[0].acsr.injg.temp);
                    double qliq = masT * (1. - tit) /
                                  celula[0].acsr.injg.FluidoPro.MasEspLiq(pGSup, celula[0].acsr.injg.temp);
                    alfini = qgas / (qliq + qgas);
                    betini = 0.;
                }
            }
        }
    }
    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;

    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;
    if (arq.condpocinj.CC == 0 || arq.condpocinj.CC == 5) {
        pGSup = chute;
    }
    iterperm = 0;

    double masfim = 0.;

    int i;
    corrigechute = 1;
    while (corrigechute == 1) {
        celula[0].presauxL = pGSup - delp;
        celula[0].presLini = pGSup - delp;
        celula[0].presL = pGSup - delp;
        celula[0].pres = pGSup - delp;
        celula[1].presL = pGSup - delp;
        celula[0].presini = pGSup - delp;
        celula[1].presLini = pGSup - delp;
        celula[0].presaux = pGSup - delp;
        celula[1].presauxL = pGSup - delp;
        celula[0].alf = alfini;
        celula[0].alfini = alfini;
        celula[0].bet = betini;
        celula[0].betini = betini;
        celula[1].alfL = celula[0].alf;
        celula[1].alfLini = celula[0].alf;
        celula[0].alfPigD = celula[0].alf;
        celula[0].alfPigDini = celula[0].alf;
        celula[0].alfPigE = celula[0].alf;
        celula[0].alfPigEini = celula[0].alf;
        celula[1].betL = celula[0].bet;
        celula[1].betLini = celula[0].bet;
        celula[0].betPigD = celula[0].bet;
        celula[0].betPigDini = celula[0].bet;
        celula[0].betPigE = celula[0].bet;
        celula[0].betPigEini = celula[0].bet;
        celula[0].betI = celula[0].bet;
        celula[1].betLI = celula[0].bet;
        i = 1;
        while (i <= ncel && celula[i - 1].pres >= 1. && masfim >= -(*vg1dSP).localtiny) {

            RenovaPresPermMon(i, 0);
            atualizaPeriPmonProd(i);
            if (arq.flashCompleto != 2)
                RenovaMassPerm(i);
            else
                RenovaMassPermComp(i);
            RenovaTempPerm(i, 0);
            atualizaPeriTempProd(i);
            RenovaPresPermJus(i, 0);
            atualizaPeriPjusProd(i);
            RenovaTransMassPerm(i - 1);
            if (arq.ordperm > 1) { // correcao de segunda ordem
                double D0presaux = celula[i].presaux - celula[i - 1].pres;
                double D0pres = celula[i].pres - celula[i].presaux;
                double D0temp = celula[i].temp - celula[i - 1].temp;
                RenovaPresPermMon(i, 1);
                atualizaPeriPmonProd(i);
                RenovaMassPerm(i);
                RenovaTempPerm(i, 1);
                RenovaPresPermJus(i, 1);
                celula[i].pres = 0.5 * (celula[i].presaux + D0pres + celula[i].pres);
                celula[i].presaux = 0.5 * (celula[i - 1].pres + D0presaux + celula[i].presaux);
                celula[i].temp = 0.5 * (celula[i - 1].temp + D0temp + celula[i].temp);
                atualizaPeriPjusProd(i);
                atualizaPeriPmonProd(i);
                atualizaPeriTempProd(i);
                RenovaMassPerm(i);
                RenovaTransMassPerm(i - 1);
            }
            masfim += (celula[i - 1].fontemassCR + celula[i - 1].fontemassLR + celula[i - 1].fontemassGR);
            i++;
            if (celula[i - 1].pres <= 1 || (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres) > -(*vg1dSP).localtiny))
                return -1e10;
            else if (i < (ncel + 1) && masfim < -(*vg1dSP).localtiny)
                return 1e10;
        }
        if (i == ncel + 1)
            corrigechute = 0;
    }
    renovaFonte(ncel);
    masfim += (celula[ncel].fontemassCR + celula[ncel].fontemassLR + celula[ncel].fontemassGR);

    if (arq.condpocinj.CC != 3 && arq.condpocinj.CC != 5)
        return masfim;
    else
        return arq.condpocinj.presfundo - celula[ncel].pres;
}

double SProd::buscaInjPfundoPerm1(double chute) {
    double pavanc = pGSup;
    double mchute = 0;
    double taux = celula[0].calor.Textern1;
    if (chute < 0) {
        if (arq.flashCompleto < 1) {
            for (int i = 1; i <= ncel; i++) {
                double rhol = celula[i].fluicol.MasEspFlu(pavanc, taux);
                double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
                pavanc -= rhol * 9.81 * sin(celula[i].duto.teta) * dxmed / 98066.5;
                if (celula[i].acsr.tipo == 3 && (celula[i].acsr.ipr.Pres - pavanc) > -(*vg1dSP).localtiny) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Poco injetor provavelmente produzindo quando se utiliza este valor de pressao na superficie");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (celula[i].acsr.tipo == 3) {
                    if (arq.condpocinj.tipoFlui < 2)
                        mchute -= celula[i].acsr.ipr.ij * (celula[i].acsr.ipr.Pres - pavanc);
                    else
                        mchute -= (celula[i].acsr.ipr.ij * rhol) * (celula[i].acsr.ipr.Pres - pavanc) / celula[i].fluicol.MasEspFlu(1.01, 15.);
                }
                taux = celula[i].calor.Textern1;
            }
            if (arq.condpocinj.CC == 3) {
                if (celula[0].acsr.injl.QLiq > 1e-5)
                    mchute = celula[0].acsr.injl.QLiq;
                else if (arq.condpocinj.tipoFlui < 2)
                    mchute += 100.;
                else
                    mchute += 100. * celula[ncel].fluicol.MasEspFlu(pavanc, taux) / celula[ncel].fluicol.MasEspFlu(1.01, 15.);
            }
        } else {
            for (int i = 1; i <= ncel; i++) {
                double rhog = celula[i].flui.MasEspGas(pavanc, taux);
                double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
                pavanc -= rhog * 9.81 * sin(celula[i].duto.teta) * dxmed / 98066.5;
                if (celula[i].acsr.tipo == 3 && (celula[i].acsr.ipr.Pres - pavanc) > -(*vg1dSP).localtiny) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Poco injetor provavelmente produzindo quando se utiliza este valor de pressao na superficie");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (celula[i].acsr.tipo == 3) {
                    if (arq.condpocinj.tipoFlui < 2)
                        mchute -= celula[i].acsr.ipr.ij * (celula[i].acsr.ipr.Pres - pavanc);
                    else
                        mchute -= (celula[i].acsr.ipr.ij * rhog) * (celula[i].acsr.ipr.Pres - pavanc) / (celula[0].flui.Deng * 1.225);
                }
                taux = celula[i].calor.Textern1;
            }
            if (arq.condpocinj.CC == 3) {
                if (celula[0].acsr.injg.QGas > 1e-5)
                    mchute = celula[0].acsr.injg.QGas;
                else
                    mchute += 100. * celula[ncel].flui.MasEspGas(pavanc, taux) / (celula[0].flui.Deng * 1.225);
            }
        }
    } else
        mchute = chute;
    double mchute2;
    double mchuteAux;
    double val;
    val = marchaInjPerm1(mchute);
    int kontaiter = 0;
    if (val < -0.9e10) {
        while (val < -0.9e10) {
            mchute *= 0.9;
            val = marchaInjPerm1(mchute);
            kontaiter++;
            if (kontaiter > 200) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "PoÃ§o injetor com problemas na iteracao na busca de um chute de vazao inicial, perda de carga muito alta");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
        }
    } else if (val > 0.9e10) {
        while (val > 0.9e10) {
            mchute *= 1.1;
            val = marchaInjPerm1(mchute);
            kontaiter++;
            if (kontaiter > 200) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "PoÃ§o injetor com problemas na iteracao na busca de um chute de vazao inicial, Vazao muito baixa");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
        }
    }
    kontaiter = 0;
    mchute2 = mchute;
    double chuteNeg = 0.;
    double chutePos = 0.;
    if (fabs(val) < 1e-15)
        return mchute;
    else {
        if (val < 0.) {
            chuteNeg = mchute;
            while (val < 0) {
                mchuteAux = mchute2;
                mchute2 *= 1.1;
                val = marchaInjPerm1(mchute2);
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm1 atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val < -0.9e10) {
                    mchute2 = 0.5 * (mchute2 + mchuteAux);
                    val = marchaInjPerm1(mchute2);
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm1 atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = mchute2;
        } else if (val > 0.) {
            chutePos = mchute;
            while (val > 0) {
                mchuteAux = mchute2;
                mchute2 *= 0.9;
                val = marchaInjPerm1(mchute2);
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm1 atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val > 0.9e10) {
                    mchute2 = 0.5 * (mchute2 + mchuteAux);
                    val = marchaInjPerm1(mchute2);
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm1 atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = mchute2;
        }
        return zriddr(chuteNeg, chutePos, 1, 0);
    }
}

double SProd::buscaInjPfundoPerm2(double chute) {
    double pchute;
    if (chute < 0) {
        if (arq.flashCompleto < 1) {
            if (arq.condpocinj.tipoFlui < 2)
                pchute = celula[ncel].acsr.ipr.Pres + ((celula[0].acsr.injl.QLiq) / (celula[ncel].acsr.ipr.ij));
            else
                pchute = celula[ncel].acsr.ipr.Pres + ((celula[0].acsr.injl.QLiq) * celula[ncel].fluicol.MasEspFlu(1.01, 15.) / (celula[ncel].acsr.ipr.ij * celula[ncel].fluicol.MasEspFlu(celula[ncel].acsr.ipr.Pres, celula[ncel].calor.Textern1)));
            double taux = celula[0].calor.Textern1;
            double rmis;
            double j;
            double f1;

            double visC = celula[0].acsr.injl.fluidocol.VisFlu(pchute, taux);
            double visMis = visC;
            double rC = celula[0].acsr.injl.fluidocol.MasEspFlu(pchute, taux);
            rmis = rC;
            double rlcA = celula[0].acsr.injl.fluidocol.MasEspFlu(1.001, 15.);
            double massicC = rlcA * celula[0].acsr.injl.QLiq / 86400;
            j = (massicC / rC) / celula[0].duto.area;
            double re;
            if (celula[0].duto.revest == 0)
                re = celula[0].Rey(celula[0].duto.a, j, rmis, visMis);
            else {
                double dhid = 4 * celula[0].duto.area / celula[0].duto.peri;
                re = celula[0].Rey(dhid, j, rmis, visMis);
            }
            f1 = celula[0].fric(re, celula[0].duto.rug / celula[0].duto.a);

            double perdafric = (f1 * rmis * j * fabs(j) / 2.) * celula[0].duto.peri / celula[0].duto.area;
            for (int i = ncel; i > 0; i--) {
                double rhol = celula[i].fluicol.MasEspFlu(pchute, taux);
                double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
                pchute += (rhol * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
                if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - pchute) > -(*vg1dSP).localtiny)
                    pchute = 1.01 * celula[i - 1].acsr.ipr.Pres;
                taux = celula[i].calor.Textern1;
            }
        } else {
            pchute = celula[ncel].acsr.ipr.Pres + ((celula[0].acsr.injg.QGas) * (celula[0].flui.Deng * 1.225) / (celula[ncel].acsr.ipr.ij * celula[ncel].flui.MasEspGas(celula[ncel].acsr.ipr.Pres, celula[ncel].calor.Textern1)));
            double taux = celula[0].calor.Textern1;
            double rmis;
            double j;
            double f1;

            double visC = celula[0].acsr.injg.FluidoPro.ViscGas(pchute, taux);
            double visMis = visC;
            double rC = celula[0].acsr.injg.FluidoPro.MasEspGas(pchute, taux);
            rmis = rC;
            double rlcA = (celula[0].flui.Deng * 1.225);
            double massicC = rlcA * celula[0].acsr.injg.QGas / 86400;
            j = (massicC / rC) / celula[0].duto.area;
            double re;
            if (celula[0].duto.revest == 0)
                re = celula[0].Rey(celula[0].duto.a, j, rmis, visMis);
            else {
                double dhid = 4 * celula[0].duto.area / celula[0].duto.peri;
                re = celula[0].Rey(dhid, j, rmis, visMis);
            }
            f1 = celula[0].fric(re, celula[0].duto.rug / celula[0].duto.a);

            double perdafric = (f1 * rmis * j * fabs(j) / 2.) * celula[0].duto.peri / celula[0].duto.area;
            for (int i = ncel; i > 0; i--) {
                double rhol = celula[i].flui.MasEspGas(pchute, taux);
                double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
                pchute += (rhol * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
                if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - pchute) > -(*vg1dSP).localtiny)
                    pchute = 1.01 * celula[i - 1].acsr.ipr.Pres;
                taux = celula[i].calor.Textern1;
            }
        }
    } else
        pchute = chute;
    if (pchute <= 1)
        pchute = 1.01;
    double pchute2;
    double pchuteAux = 0.;
    double val;
    val = marchaInjPerm1(pchute);
    int kontaiter = 0;
    if (val < -0.9e10) {
        while (val < -0.9e10) {
            pchute *= 1.1;
            val = marchaInjPerm1(pchute);
            kontaiter++;
            if (kontaiter > 200) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "PoÃ§o injetor com problemas na iteracao na busca de um chute de pressao inicial, perda de carga muito alta");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
        }
    } else if (val > 0.9e10) {
        while (val > 0.9e10) {
            pchute *= 0.9;
            val = marchaInjPerm1(pchute);
            kontaiter++;
            if (kontaiter > 200) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "PoÃ§o injetor com problemas na iteracao na busca de um chute de vazao inicial, pressao muito alta");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
        }
    }
    kontaiter = 0;
    pchute2 = pchute;
    double chuteNeg = 0.;
    double chutePos = 0.;
    if (fabs(val) < 1e-15)
        return pchute;
    else {
        if (val < 0.) {
            chuteNeg = pchute;
            while (val < 0) {
                pchuteAux = pchute2;
                pchute2 *= 0.9;
                if (pchute2 < 1.) {
                    pchute2 = pchuteAux;
                    pchute2 *= 0.99;
                    if (pchute2 < 1. && arq.AP == 0)
                        NumError("Pressao de injecao abaixo da pressao atmosferica");
                    else if (pchute2 < 1.)
                        return 1.1e10;
                }
                val = marchaInjPerm1(pchute2);
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm2 atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val < -0.9e10) {
                    pchute2 = 0.5 * (pchute2 + pchuteAux);
                    val = marchaInjPerm1(pchute2);
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm2 atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = pchute2;
        } else if (val > 0.) {
            chutePos = pchute;
            while (val > 0) {
                pchuteAux = pchute2;
                pchute2 *= 1.1;
                val = marchaInjPerm1(pchute2);
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm2 atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val > 0.9e10) {
                    pchute2 = 0.5 * (pchute2 + pchuteAux);
                    val = marchaInjPerm1(pchute2);
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm2 atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = pchute2;
        }
        return zriddr(chuteNeg, chutePos, 1, 0);
    }
}

double SProd::buscaInjPfundoPerm3(double chute) {
    celula[ncel].pres = arq.condpocinj.presfundo;
    if (celula[ncel].pres < celula[ncel].acsr.ipr.Pres) {
        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
            NumError("Valor de pressao de fundo menor que a pressao de reservatÃ³rio");
        else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    }
    double mchute;
    if (chute < 0.) {
        if (arq.flashCompleto < 1) {
            if (arq.condpocinj.tipoFlui < 2)
                mchute = -(celula[ncel].acsr.ipr.ij) * (celula[ncel].acsr.ipr.Pres - celula[ncel].pres);
            else
                mchute = -(celula[ncel].acsr.ipr.ij * celula[ncel].fluicol.MasEspFlu(celula[ncel].pres,
                                                                                     celula[ncel].calor.Textern1)) *
                         (celula[ncel].acsr.ipr.Pres - celula[ncel].pres) / celula[ncel].fluicol.MasEspFlu(1.01, 15.);
            double taux = celula[0].calor.Textern1;
            for (int i = ncel; i > 0; i--) {
                double rhol = celula[i].fluicol.MasEspFlu(celula[i].pres, taux);
                double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
                celula[i - 1].pres = celula[i].pres + rhol * 9.81 * sin(celula[i].duto.teta) * dxmed / 98066.5;
                if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - celula[i + 1].pres) > -(*vg1dSP).localtiny) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError("Valor de pressao de fundo menor que a pressao de reservatÃ³rio");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (celula[i - 1].acsr.tipo == 3) {
                    if (arq.condpocinj.tipoFlui < 2)
                        mchute -= (celula[i - 1].acsr.ipr.ij) * (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres);
                    mchute -= (celula[i - 1].acsr.ipr.ij * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, 0.) / celula[ncel].fluicol.MasEspFlu(1.01, 15.)) * (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres);
                }
                taux = celula[i].calor.Textern1;
            }
        } else {
            if (arq.condpocinj.tipoFlui < 2)
                mchute = -(celula[ncel].acsr.ipr.ij) * (celula[ncel].acsr.ipr.Pres - celula[ncel].pres);
            else
                mchute = -(celula[ncel].acsr.ipr.ij * celula[ncel].flui.MasEspGas(celula[ncel].pres,
                                                                                  celula[ncel].calor.Textern1)) *
                         (celula[ncel].acsr.ipr.Pres - celula[ncel].pres) / (celula[0].flui.Deng * 1.225);
            double taux = celula[0].calor.Textern1;
            for (int i = ncel; i > 0; i--) {
                double rhol = celula[i].flui.MasEspGas(celula[i].pres, taux);
                double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
                celula[i - 1].pres = celula[i].pres + rhol * 9.81 * sin(celula[i].duto.teta) * dxmed / 98066.5;
                if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - celula[i + 1].pres) > -(*vg1dSP).localtiny) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError("Valor de pressao de fundo menor que a pressao de reservatÃ³rio");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (celula[i - 1].acsr.tipo == 3) {
                    if (arq.condpocinj.tipoFlui < 2)
                        mchute -= (celula[i - 1].acsr.ipr.ij) * (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres);
                    mchute -= (celula[i - 1].acsr.ipr.ij * celula[i - 1].flui.MasEspGas(celula[i - 1].pres, 0.) / (celula[0].flui.Deng * 1.225)) * (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres);
                }
                taux = celula[i].calor.Textern1;
            }
        }
    } else
        mchute = chute;

    pGSup = celula[0].pres;
    double val;
    val = marchaInjPerm1(mchute);
    if (val < -0.9e10) {
        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
            NumError(
                "PoÃ§o injetor com problemas na iteracao na busca de um chute de pressao inicial, perda de carga muito alta");
        else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    }
    if (val > 0.9e10) {
        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
            NumError(
                "PoÃ§o injetor com problemas na iteracao na busca de um chute de vazao inicial, pressao muito alta");
        else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    }
    int konta = 0;
    while (fabs(celula[ncel].pres - arq.condpocinj.presfundo) / arq.condpocinj.presfundo > 0.01 / 100 && konta < 200) {
        celula[ncel].pres = arq.condpocinj.presfundo;
        if (arq.flashCompleto < 1) {
            if (arq.condpocinj.tipoFlui < 2)
                mchute = -celula[ncel].acsr.ipr.ij * (celula[ncel].acsr.ipr.Pres - celula[ncel].pres);
            else
                mchute = -(celula[ncel].acsr.ipr.ij * celula[ncel].fluicol.MasEspFlu(celula[ncel].pres, celula[ncel].temp) / celula[ncel].fluicol.MasEspFlu(1.01, 15.)) * (celula[ncel].acsr.ipr.Pres - celula[ncel].pres);
            for (int i = ncel; i > 0; i--) {
                celula[i - 1].pres = celula[i].pres + delpInjPerm(i);
                if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - celula[i + 1].pres) > -(*vg1dSP).localtiny) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError("Valor de pressao de fundo menor que a pressao de reservatÃ³rio");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (celula[i - 1].acsr.tipo == 3) {
                    if (arq.condpocinj.tipoFlui < 2)
                        mchute -= celula[i - 1].acsr.ipr.ij * (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres);
                    else
                        mchute -= (celula[i - 1].acsr.ipr.ij * celula[i - 1].fluicol.MasEspFlu(celula[i - 1].pres, celula[i - 1].temp) / celula[ncel].fluicol.MasEspFlu(1.01, 15.)) * (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres);
                }
            }
        } else {
            if (arq.condpocinj.tipoFlui < 2)
                mchute = -celula[ncel].acsr.ipr.ij * (celula[ncel].acsr.ipr.Pres - celula[ncel].pres);
            else
                mchute = -(celula[ncel].acsr.ipr.ij * celula[ncel].flui.MasEspGas(celula[ncel].pres, celula[ncel].temp) / (celula[0].flui.Deng * 1.225)) * (celula[ncel].acsr.ipr.Pres - celula[ncel].pres);
            for (int i = ncel; i > 0; i--) {
                celula[i - 1].pres = celula[i].pres + delpInjPerm(i);
                if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - celula[i + 1].pres) > -(*vg1dSP).localtiny) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError("Valor de pressao de fundo menor que a pressao de reservatÃ³rio");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (celula[i - 1].acsr.tipo == 3) {
                    if (arq.condpocinj.tipoFlui < 2)
                        mchute -= celula[i - 1].acsr.ipr.ij * (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres);
                    else
                        mchute -= (celula[i - 1].acsr.ipr.ij * celula[i - 1].flui.MasEspGas(celula[i - 1].pres, celula[i - 1].temp) / (celula[0].flui.Deng * 1.225)) * (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres);
                }
            }
        }
        pGSup = celula[0].pres;
        val = marchaInjPerm1(mchute);
        konta++;
        if (val < -0.9e10) {
            if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                NumError(
                    "PoÃ§o injetor com problemas na iteracao na busca de um chute de pressao inicial, perda de carga muito alta");
            else {
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
        }
        if (val > 0.9e10) {
            if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                NumError(
                    "PoÃ§o injetor com problemas na iteracao na busca de um chute de pressao inicial, pressao muito alta");
            else {
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
        }
    }
    if (konta >= 200) {
        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
            NumError("PoÃ§o injetor com problemas na iteracao");
        else {
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
    }
    return (celula[ncel].pres - arq.condpocinj.presfundo);
}

double SProd::buscaInjPfundoPerm4() {

    int corrigechute = 1;

    if (arq.flashCompleto < 1)
        celula[0].temp = celula[0].acsr.injl.temp;
    else
        celula[0].temp = celula[0].acsr.injg.temp;
    double alfini = 0.;
    double betini = 1.;

    if (arq.flashCompleto >= 1) {
        if (celula[0].acsr.injg.seco == 1) {
            alfini = 1.;
            betini = 0.;
        } else {
            if (arq.flashCompleto == 2) {
                if (arq.tabelaDinamica == 0)
                    celula[0].flui.atualizaPropComp(pGSup, celula[0].acsr.injg.temp, -1, NULL, NULL, arq.pocinjec);
                celula[0].acsr.injg.FluidoPro.atualizaPropComp(pGSup, celula[0].acsr.injg.temp, -1, NULL, NULL, arq.pocinjec);
            }
            double masgas = celula[0].acsr.injg.VMas(pGSup, celula[0].acsr.injg.temp);
            double tit;
            if (arq.flashCompleto != 2)
                tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(1., 20.);
            else
                tit = celula[0].acsr.injg.FluidoPro.dStockTankVaporMassFraction;
            double masT = masgas / tit;
            tit = celula[0].acsr.injg.FluidoPro.FracMassHidra(pGSup, celula[0].acsr.injg.temp);
            double qgas = masT * tit /
                          celula[0].acsr.injg.FluidoPro.MasEspGas(pGSup, celula[0].acsr.injg.temp);
            double qliq = masT * (1. - tit) /
                          celula[0].acsr.injg.FluidoPro.MasEspLiq(pGSup, celula[0].acsr.injg.temp);
            alfini = qgas / (qliq + qgas);
            betini = 0.;
        }
    }

    celula[0].tempL = celula[0].temp;
    celula[1].tempL = celula[0].temp;
    celula[0].tempini = celula[0].temp;
    celula[0].ML = 0.;
    celula[0].MC = 0.;
    celula[1].ML = 0.;
    celula[0].MliqiniL = 0.;
    celula[0].Mliqini = 0.;
    celula[1].MliqiniL = 0.;
    celula[0].QLL = 0.;
    celula[0].QL = 0;
    celula[1].QLL = 0.;
    celula[0].QG = 0.;
    if (arq.condpocinj.CC == 0)
        pGSup = arq.condpocinj.presinj;
    iterperm = 0;

    double masfim = 0.;

    int i;
    corrigechute = 1;
    while (corrigechute == 1) {
        celula[0].presauxL = pGSup;
        celula[0].presLini = pGSup;
        celula[0].presL = pGSup;
        celula[0].pres = pGSup;
        celula[1].presL = pGSup;
        celula[0].presini = pGSup;
        celula[1].presLini = pGSup;
        celula[0].presaux = pGSup;
        celula[1].presauxL = pGSup;
        celula[0].alf = alfini;
        celula[0].alfini = alfini;
        celula[0].bet = betini;
        celula[0].betini = betini;
        celula[1].alfL = celula[0].alf;
        celula[1].alfLini = celula[0].alf;
        celula[0].alfPigD = celula[0].alf;
        celula[0].alfPigDini = celula[0].alf;
        celula[0].alfPigE = celula[0].alf;
        celula[0].alfPigEini = celula[0].alf;
        celula[1].betL = celula[0].bet;
        celula[1].betLini = celula[0].bet;
        celula[0].betPigD = celula[0].bet;
        celula[0].betPigDini = celula[0].bet;
        celula[0].betPigE = celula[0].bet;
        celula[0].betPigEini = celula[0].bet;
        celula[0].betI = celula[0].bet;
        celula[1].betLI = celula[0].bet;
        i = 1;
        while (i <= ncel && celula[i - 1].pres >= 1. && masfim >= -(*vg1dSP).localtiny) {

            RenovaPresPermMon(i, 0);
            atualizaPeriPmonProd(i);
            if (arq.flashCompleto != 2)
                RenovaMassPerm(i);
            else
                RenovaMassPermComp(i);
            RenovaTempPerm(i, 0);
            atualizaPeriTempProd(i);
            RenovaPresPermJus(i, 0);
            atualizaPeriPjusProd(i);
            RenovaTransMassPerm(i - 1);
            masfim += (celula[i - 1].fontemassCR + celula[i - 1].fontemassLR + celula[i - 1].fontemassGR);
            i++;
            if (celula[i - 1].pres <= 1) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError("pressao menor que 1");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            } else if ((celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - celula[i - 1].pres) > -(*vg1dSP).localtiny)) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError("Valor de pressao de fundo menor que a pressao de reservatÃ³rio");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            } else if (i < (ncel + 1) && masfim < -(*vg1dSP).localtiny) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError("Vazao massica de injeÃ§Ã£o menor que 0");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
        }
        if (i == ncel + 1)
            corrigechute = 0;
    }
    renovaFonte(ncel);
    masfim += (celula[ncel].fontemassCR + celula[ncel].fontemassLR + celula[ncel].fontemassGR);

    return masfim;
}

double SProd::buscaInjPfundoPerm5(double chute) {

    double pchute = arq.condpocinj.presfundo;
    double taux;
    double j = 0.;
    double rmis = 0.;
    double f1 = 0.;
    taux = arq.celp[0].textern;
    if (chute < 0.) {
        if (arq.flashCompleto < 1) {
            double visC = celula[0].acsr.injl.fluidocol.VisFlu(pchute, taux);
            double visMis = visC;
            double rC = celula[0].acsr.injl.fluidocol.MasEspFlu(pchute, taux);
            rmis = rC;
            double rlcA = celula[0].acsr.injl.fluidocol.MasEspFlu(1.001, 15.);
            double massicC = rlcA * celula[0].acsr.injl.QLiq / 86400;
            j = (massicC / rC) / celula[0].duto.area;
            double re;
            if (celula[0].duto.revest == 0)
                re = celula[0].Rey(celula[0].duto.a, j, rmis, visMis);
            else {
                double dhid = 4 * celula[0].duto.area / celula[0].duto.peri;
                re = celula[0].Rey(dhid, j, rmis, visMis);
            }
            f1 = celula[0].fric(re, celula[0].duto.rug / celula[0].duto.a);
            double perdafric = (f1 * rmis * j * fabs(j) / 2.) * celula[0].duto.peri / celula[0].duto.area;
            for (int i = ncel; i > 0; i--) {
                double rhol = celula[i].fluicol.MasEspFlu(pchute, taux);
                double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
                pchute += (rhol * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
                if (pchute < 0.5) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Poco injetor com problemas na iteracao na busca de um chute de pressao inicial, perda de carga muito alta");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - pchute) > -(*vg1dSP).localtiny)
                    pchute = 1.01 * celula[i - 1].acsr.ipr.Pres;
            }
        } else {
            double visC = celula[0].acsr.injg.FluidoPro.ViscGas(pchute, taux);
            double visMis = visC;
            double rC = celula[0].acsr.injg.FluidoPro.MasEspGas(pchute, taux);
            rmis = rC;
            double rlcA = (celula[0].acsr.injg.FluidoPro.Deng * 1.225);
            double massicC = rlcA * celula[0].acsr.injg.QGas / 86400;
            j = (massicC / rC) / celula[0].duto.area;
            double re;
            if (celula[0].duto.revest == 0)
                re = celula[0].Rey(celula[0].duto.a, j, rmis, visMis);
            else {
                double dhid = 4 * celula[0].duto.area / celula[0].duto.peri;
                re = celula[0].Rey(dhid, j, rmis, visMis);
            }
            f1 = celula[0].fric(re, celula[0].duto.rug / celula[0].duto.a);
            double perdafric = (f1 * rmis * j * fabs(j) / 2.) * celula[0].duto.peri / celula[0].duto.area;
            for (int i = ncel; i > 0; i--) {
                double rhol = celula[i].flui.MasEspGas(pchute, taux);
                double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
                pchute += (rhol * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
                if (pchute < 0.5) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Poco injetor com problemas na iteracao na busca de um chute de pressao inicial, perda de carga muito alta");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - pchute) > -(*vg1dSP).localtiny)
                    pchute = 1.01 * celula[i - 1].acsr.ipr.Pres;
            }
        }
    } else
        pchute = chute;
    double pchute2;
    double pchuteAux = 0.;
    double val;
    val = marchaInjPerm1(pchute);
    int kontaiter = 0;
    if (val < -0.9e10) {
        while (val < -0.9e10) {
            pchute *= 1.1;
            val = marchaInjPerm1(pchute);
            kontaiter++;
            if (kontaiter > 200) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "Poco injetor com problemas na iteracao na busca de um chute de pressao inicial, perda de carga muito alta");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
        }
    } else if (val > 0.9e10) {
        while (val > 0.9e10) {
            pchute *= 0.9;
            val = marchaInjPerm1(pchute);
            kontaiter++;
            if (kontaiter > 200) {
                if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                    NumError(
                        "Poco injetor com problemas na iteracao na busca de um chute de vazao inicial, pressao muito alta");
                else {
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
            }
        }
    }
    kontaiter = 0;
    pchute2 = pchute;
    double chuteNeg = 0.;
    double chutePos = 0.;
    if (fabs(val) < 1e-15)
        return pchute;
    else {
        if (val < 0.) {
            chuteNeg = pchute;
            while (val < 0) {
                pchuteAux = pchute2;
                pchute2 *= 0.9;
                val = marchaInjPerm1(pchute2);
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm2 atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val < -0.9e10) {
                    pchute2 = 0.5 * (pchute2 + pchuteAux);
                    val = marchaInjPerm1(pchute2);
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm2 atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chutePos = pchute2;
        } else if (val > 0.) {
            chutePos = pchute;
            while (val > 0) {
                pchuteAux = pchute2;
                pchute2 *= 1.1;
                val = marchaInjPerm1(pchute2);
                kontaiter++;
                if (kontaiter > 200) {
                    if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                        NumError(
                            "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm2 atingiu maximo de iteracoes");
                    else {
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                while (val > 0.9e10) {
                    pchute2 = 0.5 * (pchute2 + pchuteAux);
                    val = marchaInjPerm1(pchute2);
                    kontaiter++;
                    if (kontaiter > 200) {
                        if ((*vg1dSP).chaverede == 0 && arq.AP == 0)
                            NumError(
                                "Busca de valores iniciais para calculo de zero de funcao em buscaInjPfundoPerm2 atingiu maximo de iteracoes");
                        else {
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                    }
                }
            }
            chuteNeg = pchute2;
        }
        return zriddr(chuteNeg, chutePos, 1, 0);
    }
}

double SProd::multMarcha(double chute, int prod, int tipoCC) {
    if (arq.pocinjec == 0) {
        if (prod == 0) {
            if (tipoCC == 0)
                return marchaGasPerm2(chute);
            else
                return marchaGasPerm3(chute);
        } else {
            if (prod == 1) {
                if (tipoCC == 0) {
                    if (revPerm == 0)
                        return marchaProdPerm1(chute);
                    else
                        return marchaProdPerm1Rev(chute);
                } else
                    return marchaProdPerm2(chute);
            } else {
                if (tipoCC == 0) {
                    if (revPerm == 0)
                        return marchaProdPresPres1(chute);
                    else
                        return marchaProdPresPres1Rev(chute);
                } else {
                    if (arq.chokep.abertura[0] > 1e-15)
                        return marchaProdPresPres2(chute);
                    else
                        return marchaProdPresPres2(chute);
                }
            }
        }
    } else
        return marchaInjPerm1(chute);
}
double SProd::SIGN(double a, double b) {
    return (b >= 0 ? 1.0 : -1.0) * fabs(a);
}
double SProd::zbrent(double x1, double x2, int prod, int tipoCC, double tol, double epsn, int maxit) {
    double EPS = epsn;
    double a = x1;
    double b = x2;
    double c = x2;
    double fa = multMarcha(a, prod, tipoCC);
    double fb = multMarcha(b, prod, tipoCC);
    if (fabs(fa) > 1e9 || fabs(fb) > 1e9)
        return 1e10;
    double e = 0.;
    double d, fc, p, q, r, s, tol1, xm;

    if ((fa > 0.0 && fb > 0.0) || (fa < 0.0 && fb < 0.0)) {
        double val;
        val = falsacorda(x1, x2, prod, tipoCC);
        return val;
    } else {
        fc = fb;
        for (int iter = 0; iter < maxit; iter++) {
            if ((fb > 0.0 && fc > 0.0) || (fb < 0.0 && fc < 0.0)) {
                c = a;
                fc = fa;
                e = d = b - a;
            }
            if (fabs(fc) < fabs(fb)) {
                a = b;
                b = c;
                c = a;
                fa = fb;
                fb = fc;
                fc = fa;
            }
            tol1 = 2.0 * EPS * fabs(b) + 0.5 * tol;
            xm = 0.5 * (c - b);
            if (fabs(xm) <= tol1 || fb == 0.0)
                return b;
            if (fabs(e) >= tol1 && fabs(fa) > fabs(fb)) {
                s = fb / fa;
                if (a == c) {
                    p = 2.0 * xm * s;
                    q = 1.0 - s;
                } else {
                    q = fa / fc;
                    r = fb / fc;
                    p = s * (2.0 * xm * q * (q - r) - (b - a) * (r - 1.0));
                    q = (q - 1.0) * (r - 1.0) * (s - 1.0);
                }
                if (p > 0.0)
                    q = -q;
                p = fabs(p);
                double min1 = 3.0 * xm * q - fabs(tol1 * q);
                double min2 = fabs(e * q);
                if (2.0 * p < (min1 < min2 ? min1 : min2)) {
                    e = d;
                    d = p / q;
                } else {
                    d = xm;
                    e = d;
                }
            } else {
                d = xm;
                e = d;
            }
            a = b;
            fa = fb;
            if (fabs(d) > tol1)
                b += d;
            else
                b += SIGN(tol1, xm);
            fb = multMarcha(b, prod, tipoCC);
        }
        NumError("Metodo Van Winjngaarden-Dekker-Brent para calcular zero de funcaoo atingiu maximo de iteracoes");
        return 0.0;
    }
}

double SProd::falsacorda(double a, double b, int prod, int tipoCC) {
    double u = multMarcha(a, prod, tipoCC);
    double e = b - a;
    double c;
    int maxit = 100;
    double delta = 0.001;
    double epsn = 0.001;
    double multFC = 0.5;

    for (int k = 1; k <= maxit; k++) { // this block treats the 'falsacorda' properly
        e = b - a;
        e *= 0.5;
        c = a + e;
        double w = multMarcha(c, prod, tipoCC);
        if (fabs(e) < delta || fabs(w) < epsn)
            return c;
        ((u > 0 && w < 0) || (u < 0 && w > 0)) ? (b = c) : (a = c, u = w);
    }
    return c;
}

double SProd::zriddr(double x1, double x2, int prod, int tipoCC) {
    double xacc = 1e-5;
    int maxit = 100;
    double fmin;
    double xmin;
    double fl;
    double fh;
    if (revPerm == 0) {
        fl = multMarcha(x1, prod, tipoCC);
        fh = multMarcha(x2, prod, tipoCC);
    } else {
        fl = multMarcha(x1, prod, tipoCC);
        fh = multMarcha(x2, prod, tipoCC);
    }
    if (fabs(fl) > 1e9 || fabs(fh) > 1e9)
        return 1e10;
    if (fl >= 0.) {
        if (fl > 0.9e10) {
            x1 *= 0.9999;
            fl = multMarcha(x1, prod, tipoCC);
        } else {
            int konta = 0;
            while (konta < 100 && fl > 0.) {
                if (revPerm == 0) {
                    if (x2 < x1)
                        x1 *= 1.0001;
                    else
                        x1 *= 0.999;
                } else {
                    if (x2 < x1)
                        x1 *= 1.0001;
                    else
                        x1 *= 0.999;
                }
                fl = multMarcha(x1, prod, tipoCC);
                konta++;
            }
        }
    } else if (fh <= 0.) {
        if (fh < -0.9e10) {
            x2 *= 1.00001;
            fh = multMarcha(x2, prod, tipoCC);
        } else {
            int konta = 0;
            while (konta < 100 && fh < 0.) {
                if (revPerm == 0) {
                    if (x1 < x2)
                        x2 *= 1.0001;
                    else
                        x2 *= 0.999;
                } else {
                    if (x1 < x2)
                        x2 *= 1.0001;
                    else
                        x2 *= 0.999;
                }
                fh = multMarcha(x2, prod, tipoCC);
                konta++;
            }
        }
    }
    if (fabs(fh) < fabs(fl)) {
        fmin = fh;
        xmin = x2;
    } else {
        fmin = fl;
        xmin = x1;
    }
    if ((fl > 0.0 && fh < 0.0) || (fl < 0.0 && fh > 0.0)) {
        double xl = x1;
        double xh = x2;
        double ans = -1.e20;
        for (int j = 0; j < maxit; j++) {
            double xm = 0.5 * (xl + xh);
            double fm = multMarcha(xm, prod, tipoCC) / monitConvPermBase;
            if (prod != 0) {
                monitConvPerm = fabs(fm);
            }
            if (fabs(fm) < fabs(fmin)) {
                fmin = fm;
                xmin = xm;
            }
            double s = sqrt(fm * fm - fl * fh);
            if (s == 0.0) {
                fmin = multMarcha(xmin, prod, tipoCC);
                return xmin;
            }
            double xnew = xm + (xm - xl) * ((fl >= fh ? 1.0 : -1.0) * fm / s);
            if (fabs(xnew - ans) <= xacc) {
                fmin = multMarcha(xmin, prod, tipoCC);
                return xmin;
            }
            ans = xnew;
            double fnew = multMarcha(ans, prod, tipoCC) / monitConvPermBase;
            if (prod != 0) {
                monitConvPerm = fabs(fnew);
            }
            if (fabs(fnew) < fabs(fmin)) {
                fmin = fnew;
                xmin = ans;
            }
            if (fabs(fnew) <= xacc) {
                fmin = multMarcha(xmin, prod, tipoCC);
                return xmin;
            }
            if (SIGN(fm, fnew) != fm) {
                xl = xm;
                fl = fm;
                xh = ans;
                fh = fnew;
            } else if (SIGN(fl, fnew) != fl) {
                xh = ans;
                fh = fnew;
            } else if (SIGN(fh, fnew) != fh) {
                xl = ans;
                fl = fnew;
            } else
                return -1.e10;
            if (fabs(xh - xl) <= xacc) {
                fmin = multMarcha(xmin, prod, tipoCC);
                return xmin;
            }
        }
        return 1.e10;
    } else {
        if (fabs(fl) <= xacc) {
            return x1;
        }
        if (fabs(fh) <= xacc) {
            return x2;
        }
        return -1e10;
    }
}

double SProd::hidroreverso(double hol, double vaz, double vazG) {
    double pchute = pGSup;
    double taux;
    celula[ncel].pres = pchute;
    double j = 0.;
    double rmis = 0.;
    double f1 = 0.;
    for (int i = ncel; i > 0; i--) {
        if (vaz > 0. || vazG > 0.) {
            taux = arq.celp[0].textern;
            double bet = 0.;
            double visC = celula[i].fluicol.VisFlu(pchute, taux);
            double visP = celula[i].flui.ViscOleo(pchute, taux);
            double visG = celula[i].flui.ViscGas(pchute, taux);
            double visMis = (1 - bet) * visP + bet * visC;
            double rC = celula[i].fluicol.MasEspFlu(pchute, taux);
            double rP = celula[i].flui.MasEspLiq(pchute, taux);
            double rG = celula[i].flui.MasEspGas(pchute, taux);
            rmis = (1 - bet) * rP + bet * rC;
            double rlpA = celula[i].flui.MasEspLiq(1., 15.);
            double rlcA = celula[i].fluicol.MasEspFlu(1.001, 15.);
            double massicC = rlcA * vaz * bet;
            double massic = rlpA * vaz * (1. - bet);
            double Rhogs = celula[i].flui.Deng * 1.225;
            double Rhols = (1000 * 141.5 / (131.5 + celula[i].flui.API)) * (1 - celula[i].flui.BSW) + 1000. * celula[i].flui.Denag * celula[i].flui.BSW;
            double multiplicador = (Rhols + celula[i].flui.RGO * Rhogs * (1 - celula[i].flui.BSW));
            massic = 1 * vaz * (1. - bet) * multiplicador;
            double fracmasshidra = celula[i].flui.FracMassHidra(pchute, taux);
            double massicP = (1. - fracmasshidra) * massic;
            double massicG = fracmasshidra * massic + vazG * Rhogs;
            j = (massicP / rP + massicC / rC + massicG / rG) / celula[i].duto.area;
            double alfmis = (massicG / rG) / (massicP / rP + massicC / rC + massicG / rG);
            rmis = (1 - alfmis) * rmis + alfmis * rG;
            visMis = (1 - alfmis) * visMis + alfmis * visG;
            double re;
            if (celula[i].duto.revest == 0)
                re = celula[0].Rey(celula[i].duto.a, j, rmis, visMis);
            else {
                double dhid = 4 * celula[i].duto.area / celula[0].duto.peri;
                re = celula[i].Rey(dhid, j, rmis, visMis);
            }
            f1 = celula[i].fric(re, celula[0].duto.rug / celula[0].duto.a);
        }
        double perdafric = (f1 * rmis * j * fabs(j) / 2.) * celula[i].duto.peri / celula[i].duto.area;
        taux = arq.celp[i].textern;
        if (i == 500) {
            int para;
            para = 0;
        }
        double rhol = celula[i].flui.MasEspLiq(pchute, taux);
        double rhog = celula[i].flui.MasEspGas(pchute, taux);
        double alfa = 1. - hol;
        double rhomix = (1. - alfa) * rhol + alfa * rhog;
        double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
        pchute += ((rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5);
        if (celula[i - 1].acsr.tipo == 7)
            pchute -= celula[i - 1].acsr.delp;
        if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - pchute) < (*vg1dSP).localtiny)
            pchute = 0.99 * celula[i - 1].acsr.ipr.Pres;
        if (celula[i - 1].acsr.tipo == 15 && (celula[i - 1].acsr.radialPoro.pRes[0] - pchute) < (*vg1dSP).localtiny)
            pchute = 0.99 * celula[i - 1].acsr.radialPoro.pRes[0];
        if (celula[i - 1].acsr.tipo == 16 && (celula[i - 1].acsr.poroso2D.dados.pRes - pchute) < (*vg1dSP).localtiny)
            pchute = 0.99 * celula[i - 1].acsr.poroso2D.dados.pRes;

        celula[i - 1].dpB = 0.;
        if (celula[i - 1].acsr.tipo == 4 && celula[i - 1].acsr.bcs.freqnova > 1. && vaz >= 0.) {
            double vazmix = j * celula[i - 1].dutoL.area;
            double rhomis = celula[i - 1].flui.MasEspLiq(pchute, taux);
            double vismis = celula[i - 1].flui.ViscOleo(pchute, taux);
            vazmix *= (86400 / 0.1589876);
            celula[i - 1].acsr.bcs.NovaVis(vismis, rhomis, vazmix);
            celula[i - 1].dpB = 0.3048 * celula[i - 1].acsr.bcs.Hvis * rhomis * 9.82;
        }
        if (celula[i - 1].acsr.tipo == 17 && celula[i - 1].acsr.multibcs.freqnova > 1. && vaz >= 0.) {
            double alf0 = celula[i - 1].alf;
            double bet0 = celula[i - 1].bet;
            celula[i - 1].acsr.multibcs.flui = celula[i - 1].flui;
            celula[i - 1].acsr.multibcs.fluicol = celula[i - 1].fluicol;
            celula[i - 1].acsr.multibcs.marchaMultiBcs(celula[i - 1].QG, celula[i - 1].QL,
                                                       pchute, taux, alf0, bet0);
            celula[i - 1].dpB = celula[i - 1].acsr.multibcs.dpB * 98066.52;
        }
        pchute -= celula[i - 1].dpB / 98066.5;
        celula[i - 1].pres = pchute;
    }
    return pchute;
}

double SProd::hidroreversoInj(double hol, double vaz) {
    double pchute = 0.;
    if (arq.condpocinj.presfundo > 1e-5)
        pchute = arq.condpocinj.presfundo;
    else if (celula[ncel].acsr.tipo == 3)
        pchute = celula[ncel].acsr.ipr.Pres;
    else
        NumError("Sem pressao no fim do tramo e sem IPR-metodo hidroreversoInj");
    double taux;
    double rmis = 0.;
    double j = 0.;
    double f1 = 0.;

    if (vaz > 0.) {
        taux = arq.celp[0].textern;
        celula[ncel].pres = pchute;
        double visC = celula[0].acsr.injl.fluidocol.VisFlu(pchute, taux);
        double visMis = visC;
        double rC = celula[0].acsr.injl.fluidocol.MasEspFlu(pchute, taux);
        rmis = rC;
        double rlcA = celula[0].acsr.injl.fluidocol.MasEspFlu(1.001, 15.);
        double massicC = rlcA * vaz;
        j = (massicC / rC) / celula[0].duto.area;
        double re;
        if (celula[0].duto.revest == 0)
            re = celula[0].Rey(celula[0].duto.a, j, rmis, visMis);
        else {
            double dhid = 4 * celula[0].duto.area / celula[0].duto.peri;
            re = celula[0].Rey(dhid, j, rmis, visMis);
        }
        f1 = celula[0].fric(re, celula[0].duto.rug / celula[0].duto.a);
    }
    double perdafric = (f1 * rmis * j * fabs(j) / 2.) * celula[0].duto.peri / celula[0].duto.area;
    for (int i = ncel; i > 0; i--) {
        taux = arq.celp[i].textern;
        double rhol = celula[i].fluicol.MasEspFlu(pchute, taux);
        double rhog = 0.;
        double alfa = 1. - hol;
        double rhomix = (1. - alfa) * rhol + alfa * rhog;
        double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
        pchute += ((rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5);
        if (celula[i - 1].acsr.tipo == 7)
            pchute -= celula[i - 1].acsr.delp;
        if (celula[i - 1].acsr.tipo == 3 && (celula[i - 1].acsr.ipr.Pres - pchute) < (*vg1dSP).localtiny)
            pchute = 0.99 * celula[i - 1].acsr.ipr.Pres;
        celula[i - 1].pres = pchute;
    }
    return pchute;
}

double SProd::hidroTramoSecundario(double titulo) {
    double pchute;
    if (arq.ConContEntrada == 1)
        pchute = arq.CCPres.pres[0];
    else
        pchute = celula[0].pres;
    double taux;
    double hol;
    double bet;
    if (arq.ConContEntrada == 1)
        bet = arq.CCPres.bet[0];
    else
        bet = celula[0].bet;
    celula[0].bet = bet;
    taux = arq.celp[0].textern;
    double titRef = celula[0].flui.FracMassHidra(pchute, taux);
    if (arq.ConContEntrada == 1)
        celula[0].pres = arq.CCPres.pres[0];
    celula[0].temp = taux;
    double rhol = celula[0].flui.MasEspLiq(celula[0].pres, taux);
    double rhoc = celula[0].fluicol.MasEspFlu(celula[0].pres, taux);
    double rhog = celula[0].flui.MasEspGas(celula[0].pres, taux);
    double titEntra;
    if (arq.ConContEntrada == 1)
        titEntra = arq.CCPres.tit[0];
    else
        titEntra = titulo;
    celula[0].alf = titEntra * (rhol * (1. - bet) + rhoc * bet) / (rhog * (1. - titEntra) + titEntra * (rhol * (1. - bet) + rhoc * bet));
    for (int i = 0; i < ncel; i++) {
        taux = arq.celp[i].textern;
        rhol = celula[i].flui.MasEspLiq(pchute, taux);
        rhoc = celula[i].fluicol.MasEspFlu(pchute, taux);
        rhog = celula[i].flui.MasEspGas(pchute, taux);
        double tit0 = celula[i].flui.FracMassHidra(pchute, taux);
        double delTit = tit0 - titRef;
        double novoTit = titulo + delTit * (titulo / titRef);
        double alfa = novoTit * (rhol * (1. - bet) + rhoc * bet) / (rhog * (1. - novoTit) + novoTit * (rhol * (1. - bet) + rhoc * bet));
        celula[i + 1].alf = alfa;
        celula[i + 1].bet = bet;

        double rhomix = (1. - alfa) * ((1. - bet) * rhol + bet * rhoc) + alfa * rhog;
        double dxmed = 0.5 * (celula[i].dx + celula[i + 1].dx);
        pchute -= ((rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed) / 98066.5);
        celula[i + 1].pres = pchute;
        celula[i + 1].temp = taux;
    }
    return pchute;
}

void SProd::hidroLinServ() {
    double pchute = arq.gasinj.presinj[0];
    celulaG[0].pres = pchute;
    double taux;
    taux = arq.celg[0].textern;
    celula[0].temp = taux;
    double rhog = celulaG[0].flui.MasEspGas(celulaG[0].pres, taux);
    for (int i = 0; i < ncelGas; i++) {
        taux = arq.celg[i].textern;
        rhog = celulaG[i].flui.MasEspGas(pchute, taux);
        double dxmed = 0.5 * (celulaG[i].dx0 + celulaG[i + 1].dx0);
        pchute -= ((rhog * 9.81 * sinl(celulaG[i].duto.teta) * dxmed) / 98066.5);
        celulaG[i + 1].pres = pchute;
        celulaG[i + 1].temp = taux;
    }
}

double SProd::buscaTramoSecVazPerm(double pPartida, int indPartida) {
    // Find two initial guesses with opposite signs to bracket the root.
    revPerm = 0;
    monitConvPerm = 1000.;

    // Initialize the pressure guess with the downstream choke pressure.
    // If non-positive, estimate a new value.
    double pchute = pPartida;
    double taux; // Auxiliary temperature used to estimate pchute.
    double j = 0.;
    double rmis = 0.;
    double f1 = 0.;
    double perdafric = 0.;
    double betaChute = 0.;
    if (celula[0].acsr.tipo == 2)
        betaChute = celula[0].acsr.injl.bet;

    for (int i = indPartida; i > 0; i--) {
        taux = arq.celp[i].textern;
        if (taux < arq.tmin)
            taux = arq.tmin;
        double rhol = celula[i].flui.MasEspLiq(pchute, taux);
        double rhog = celula[i].flui.MasEspGas(pchute, taux);
        // For systems other than the main gas-lift ring, estimate the pressure
        // using liquid hydrostatics, resulting in a high initial pressure guess.
        double alfa = 0.;
        if (betaChute < 0.5) {
            double tit = celula[i].flui.FracMassHidra(pchute, taux);
            alfa = tit * rhol / (rhog - tit * rhog + tit * rhol);
        }
        if (celula[0].acsr.tipo == 1)
            alfa = 1.;
        double rhomix = (1. - alfa) * rhol + alfa * rhog;
        double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
        // Advance the pressure using hydrostatic head and estimated friction loss.
        pchute += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
        if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
            pchute = 0.9 * arq.tabent.pmax;
    }
    if (pchute > 1000)
        pchute = 1000.; // maximum pressure of maximum chute

    // Method parameters.
    double pchute2;        // Second pressure guess.
    double pchuteAux = 0.; // Helper used to bracket the root.
    double val;            // val stores the value returned by the marching procedure.
    // Difference between the upstream choke pressure and the final cell pressure.
    val = marchaProdPerm2(pchute);
    buscaIni = 1;
    // The marching process may fail if pressure exceeds an IPR static pressure
    // or the PVTSim table limit, returning 1e10, or approaches zero before
    // reaching the final cell, returning -1e10.
    if (val < -0.9e10) {
        // Pressure is too low. Increase the guess using water hydrostatics
        // to obtain a higher bottom-hole pressure.
        pchuteAux = pGSup;
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            double rhol = 1000 + 0 * celula[i].flui.MasEspLiq(pchuteAux, taux);
            double rhog = celula[i].flui.MasEspGas(pchuteAux, taux);
            double alfa = 0.;
            if (celula[0].acsr.tipo == 1)
                alfa = 1.;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.5 && chuteHol > -1e-15)
                alfa = 1. - chuteHol * 2.;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            pchuteAux += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
        }
        if (pchuteAux > 1100)
            pchuteAux = 1100;  // limit of pchuteAux
    } else if (val > 0.9e10) { // pressure at some point exceeded some static pressure
        // must decrease the chute value
        pchuteAux = pGSup;
        for (int i = ncel; i > 0; i--) {
            taux = arq.celp[i].textern;
            double rhol = celula[i].flui.MasEspLiq(pchuteAux, taux);
            double rhog = celula[i].flui.MasEspGas(pchuteAux, taux);
            // in this case, use a high void fraction for the hydrostatic calculation.
            double alfa = 0.8;
            if (celula[0].acsr.tipo == 1)
                alfa = 1.;
            else if ((*vg1dSP).chaverede == 1 && chuteHol < 0.2 && chuteHol > -1e-15)
                alfa = 1. - chuteHol;
            double rhomix = (1. - alfa) * rhol + alfa * rhog;
            double dxmed = 0.5 * (celula[i].dx + celula[i - 1].dx);
            pchuteAux += (rhomix * 9.81 * sin(celula[i].duto.teta) * dxmed + perdafric * dxmed) / 98066.5;
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
        }
        if (pchuteAux > 1100)
            pchuteAux = 1100;
    }

    double mult1 = 0.9;
    double mult2 = 1.1;

    // Retry with pchuteAux if the first marching attempt fails.
    int kontaiter = 0; // Count attempts until the result differs from 1e10 or 1e-10.
    double pchuteAux0 = pchuteAux;
    if (val < -0.9e10 || val > 0.9e10) {
        double valtemp;
        valtemp = marchaProdPerm2(pchuteAux); // marcha with pchuteAux
        if (fabs(valtemp) > 1.01e10) {
            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
        while (valtemp > 0.9e10 && val > 0.9e10 && kontaiter < 50) { // Estimated background pressure remains high
            pchuteAux *= 0.99;                                       // reducing the estimate
            valtemp = marchaProdPerm2(pchuteAux);
            if (fabs(valtemp) > 1.01e10) {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
            kontaiter++; // 50 iterations maximum
        }
        while (valtemp < -0.9e10 && val < -0.9e10 && kontaiter <= 50) { // Estimated background pressure remains low
            if (celula[0].acsr.tipo == 1 || celula[0].acsr.tipo == 2 || celula[0].acsr.tipo == 10)
                pchuteAux *= 1.1;
            else
                pchuteAux *= 1.01; // increasing the estimate
            // Ensure the increased estimate does not exceed the PVTSim pressure limit.
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchuteAux) < (*vg1dSP).localtiny)
                pchuteAux = 0.9 * arq.tabent.pmax;
            valtemp = marchaProdPerm2(pchuteAux); // new try
            if (fabs(valtemp) > 1.01e10) {
                cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                if ((*vg1dSP).iterRede > 0)
                    return -1.1e10;
                else
                    return 1.1e10;
            }
            if (kontaiter == 50) {
                pchuteAux = 0.5 * pchuteAux0;
            }
            kontaiter++;
        }
        valtemp = marchaProdPerm2(pchuteAux);
        if (fabs(valtemp) > 1.01e10) {
            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
        if (valtemp > -0.9e10 && valtemp < 0.9e10) { // The marching process completed successfully.
            val = valtemp;
            pchute = pchuteAux;
        }
    }
    if (kontaiter > 50) { // Iteration limit reached.
        // During a network iteration after the first one.
        if ((*vg1dSP).iterRede > 0)
            return -1.1e10;
        // If followed by a transient simulation or during the first network iteration.
        else
            return 1.1e10;
    }
    // The previous loop jumped between pressure guesses that were too high
    // and too low to complete the marching process.
    while ((val < -0.9e10 || val > 0.9e10) && kontaiter <= 50) {
        pchute = 0.5 * (pchute + pchuteAux); // Use the midpoint between the upper and lower bounds.
        if ((fabs(pchute - pchuteAux) / pchute) < 0.001 && val < -0.9e10) {
            pchute = 1.05 * pchute;
            if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute) < (*vg1dSP).localtiny)
                pchute = 0.9 * arq.tabent.pmax;
            pchuteAux = pchute;
        }

        val = marchaProdPerm2(pchute);
        if (fabs(val) > 1.01e10) {
            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
            if ((*vg1dSP).iterRede > 0)
                return -1.1e10;
            else
                return 1.1e10;
        }
        kontaiter++;
    }
    if (kontaiter > 50) {
        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
        if ((*vg1dSP).iterRede > 0)
            return -1.1e10;
        else
            return 1.1e10;
    }

    // A pressure guess was found that allowed the marching process to complete.
    kontaiter = 0;
    pchute2 = pchute;
    double chutelim = 0.;
    double chuteNeg = 0.;
    double chutePos = 0.;
    double atenua = 1.;
    double reduz = 1. - arq.buscaFC;
    double amplifica = 1. + arq.buscaFC;
    if (fabs(val) < 1e-3)
        return pchute;
    else {
        if (val < 0.) {
            // Choke flow exceeds tubing flow, indicating a high pressure guess.
            // Decrease the pressure until val becomes positive and brackets the root.
            chuteNeg = pchute; // Store the pressure guess yielding val < 0.

            // Update the negative bound as val approaches zero.
            while (val < 0) {
                if (fabs(pchute2 - pchute) / pchute < (1. - reduz) / 10. && kontaiter > 50) {
                    pchute2 *= 0.5;
                }
                pchuteAux = pchute2;
                if ((*vg1dSP).chaverede == 0)
                    pchute2 *= reduz; // Decreasing the pressure in the search for val>0
                else
                    pchute2 *= mult1; // Decreasing the pressure in the search for val>0
                if (pchute2 <= chutelim)
                    pchute2 = 0.5 * (pchuteAux + chutelim);
                // chutelim starts at zero. If pchute2 drops too far and val reaches -1e10,
                // set chutelim to pchute2, establishing the minimum allowed pressure.
                if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute2) < (*vg1dSP).localtiny)
                    pchute2 = 0.9 * arq.tabent.pmax;
                val = marchaProdPerm2(pchute2);
                if (fabs(val) > 1.01e10) {
                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
                if (val < 0 && val > -0.9e10)
                    chuteNeg = pchute2; // updating chuteNeg
                kontaiter++;
                if (kontaiter > 50) {
                    // Iteration limit reached while searching for the second guess.
                    // Stop the simulation or report the failure.
                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
                while (val < -0.9e10) {
                    // The pressure reduction was too large, causing the marching process
                    // to fall below 0.5 kgf/cm². Increase the lower pressure estimate.
                    chutelim = pchute2;
                    pchute2 = 0.5 * (pchute2 + pchuteAux); // Midpoint between the lowest pressure yielding val < 0 and the lower pressure bound.
                    if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute2) < (*vg1dSP).localtiny)
                        pchute2 = 0.9 * arq.tabent.pmax;
                    if (celula[0].acsr.tipo == 3 &&
                        (celula[0].acsr.ipr.Pres - pchute2) < 0.001 * celula[0].acsr.ipr.Pres) {
                        pchute2 = -(10 / celula[0].acsr.ipr.ip) + celula[0].acsr.ipr.Pres;
                        if (pchute2 < 0.999 * celula[0].acsr.ipr.Pres)
                            pchute2 = 0.999 * celula[0].acsr.ipr.Pres;
                    }
                    val = marchaProdPerm2(pchute2);
                    if (fabs(val) > 1.01e10) {
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                    if (val < 0 && val > -0.9e10)
                        chuteNeg = pchute2;
                    kontaiter++;
                    if (kontaiter > 50) {
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
            }
            chutePos = pchute2;
        } else if (val > 0.) {
            // Choke flow is below tubing flow, indicating a low pressure guess.
            // Increase the pressure until val becomes negative and brackets the root.
            chutePos = pchute; // Store the pressure guess yielding val > 0.

            // Update the positive bound as val approaches zero.
            while (val > 0) {
                pchuteAux = pchute2;
                if ((*vg1dSP).chaverede == 0)
                    pchute2 *= amplifica; // Increasing the pressure in search of val>0
                else
                    pchute2 *= mult2; // Increasing the pressure in search of val>0
                if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute2) < (*vg1dSP).localtiny)
                    pchute2 = 0.9 * arq.tabent.pmax;
                int limpres = 0;
                val = marchaProdPerm2(pchute2);
                if (fabs(val) > 1.01e10) {
                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
                if (val > 0. && limpres == 1) {
                    int iterpres = 0;
                    while (val > 0 && iterpres < 10) {
                        pchute2 *= 1.001;
                        val = marchaProdPerm2(pchute2);
                        if (fabs(val) > 1.01e10) {
                            cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                            if ((*vg1dSP).iterRede > 0)
                                return -1.1e10;
                            else
                                return 1.1e10;
                        }
                        iterpres++;
                    }
                    if (iterpres >= 10) {
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
                if (val > 0 && val < 0.9e10)
                    chutePos = pchute2; // updating chutePos
                kontaiter++;
                if (kontaiter > 50) {
                    // Iteration limit reached while searching for the second guess.
                    // End the simulation or report the failure.
                    cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                    if ((*vg1dSP).iterRede > 0)
                        return -1.1e10;
                    else
                        return 1.1e10;
                }
                while (val > 0.9e10) {
                    // The pressure step was too large, causing the marching process to exceed
                    // the static or PVTSim pressure limit. Reduce the lower pressure estimate.
                    chutelim = pchute2;
                    pchute2 = 0.5 * (pchute2 + pchuteAux); // Midpoint between the highest pressure yielding val > 0 and the upper pressure bound.
                    if (arq.usaTabela == 1 && (arq.tabent.pmax - pchute2) < (*vg1dSP).localtiny)
                        pchute2 = 0.9 * arq.tabent.pmax;
                    val = marchaProdPerm2(pchute2);
                    if (fabs(val) > 1.01e10) {
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGÃŠNCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                    if (val > 0 && val < 0.9e10)
                        chutePos = pchute2;
                    kontaiter++;

                    if (kontaiter > 50) {
                        cout << "#################PERMANENTE FALHOU EM SUA CONVERGENCIA##############################" << endl;
                        if ((*vg1dSP).iterRede > 0)
                            return -1.1e10;
                        else
                            return 1.1e10;
                    }
                }
            }
            chuteNeg = pchute2;
        }
        return zriddr(chuteNeg, chutePos, 1, 1); // Find the root using pressure bounds with opposite signs.
    }
}
